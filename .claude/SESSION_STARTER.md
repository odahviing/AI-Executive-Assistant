# Maelle session context

Working on Maelle at `E:/Code/Maelle`. **Every fact in this file that a command can print is NAMED BY ITS COMMAND, never copied** — a number written here is stale by the next wrap, and this file is the first thing a fresh chat reads. **Version:** the `version` field in `package.json`. **HEAD:** `git log -1` — a wrap commit's subject opens with the version it shipped. **Open bug tickets:** `gh issue list --label Bug`; when that comes back empty the work is in the ledger backlog and the logs.

## He flags a bug directly — record it, don't investigate it

He can name a bug in one sentence, in **any** chat, at any time, and never think about it again. **Run this — do not read the code, do not trace a root cause, and do not open the Manager skill or `.claude/skills/manager/SKILL.md` for this. Staying cheap is the entire point:**

```
node scripts/ledger-file.cjs --ref "<short-slug>" --source owner \
  --finding "Owner says: <his words, one sentence>" \
  --verdict needs-owner-decision --invariant none \
  --note "Flagged directly, no investigation done — triage on the next /manager run."
```

Capture his words as close to verbatim as reasonable for `--finding`. **Never hand-compose the JSON line into `ledger.jsonl` yourself — always go through the script**, the same rule every other ledger writer follows. Once it lands, reply with one short line and stop: *"Flagged — `<ref>`. Surfaces on your next /manager run."* This is for a bug he wants **recorded**, not fixed now and not something this chat investigates or builds — it becomes a normal atomic item, triaged and built like any GitHub issue or log finding, on the next real `/manager run`.

**Two lessons from 4.3.7–4.3.8 that are worth more than either release.** First: **before you trust any shipped guard, check that its reader can reach it.** gh#165's 4.3.4 mechanism had never fired once in production — the field was written only on a passing verdict and a colleague's strict check cannot produce one — and it had *replaced* a reachable lookup, so the ticket was worse after the fix than before. Second: **a fix for one thing twice became a change to who can see what.** The gh#165 invite-list fix was reverted twice — once for making the requester's work hours a hard clip that deleted the owner's real evening availability, once for sitting *above* two sensitivity gates whose condition it then guaranteed, reducing a deterministic privacy control to a model-supplied flag. Placement relative to a gate is part of the fix, not a detail. Version-by-version history is in `CHANGELOG.md`, which is canonical — this file carries only what a session needs before it acts.

**gh#154 SHIPPED in 4.5.0 — the permission layer, and it is the thing to read before touching any surface gate.** One flag used to decide both *who may read this turn* and *whose word carries owner authority*; they are now two REQUIRED fields — **`authority`** (`'owner'|'colleague'`, from the authenticated Slack sender id, gating ACTIONS only) and **`surface`** (`'owner_dm'|'colleague_dm'|'room'`) — resolved once at each transport front door. `senderRole` keeps its old meaning as DATA scope and stays clamped to `'colleague'` on any room surface, **including for the owner**, so `registry.ts`'s two tool chokepoints never moved and remain the privacy floor. Five local re-derivations and a second membership clamp were deleted; colleague-test is gone. The owner's rulings, which are the spec: an MPIM and a channel are the SAME thing for security · in a room he keeps ACTION tools and never data tools (*"action only, as any data extracting tools is problem by privacy nature"*) · a rule-bend routes to his private approval thread deterministically in code and is **never** announced in the room · a person's memory `.md` may reach the person it describes (*"its not gossip"*) but never a room · email and WhatsApp were explicitly OUT of scope and are byte-identical. **Do not add a third surface flag, and do not re-derive either field locally — read them.** Cost the truth: five build rounds and ~9.7M tokens, because a change this size across the privacy perimeter cannot be verified per-item; each round's fix repeatedly hollowed out another round's, and only full-diff adversarial passes caught it.

**One live shape worth knowing before you read any code:** since 4.3.0 she has a **second transport, email** (gh#24, a narrow slice of #5) — the owner forwards a meeting request to `maelle@reflectiz.com`, she computes options and **replies by email to him only**, so he stays the gatekeeper and she never emails an external. The recipient cap is in code, an email turn is clamped to four tools, and the whole path is inert without `channels.email.{enabled,mailbox}` plus a token from `scripts/email-auth.mjs`.

## ⚠️ Workshop state — read before you run anything (shipped in 4.3.3)

**The loop was rebuilt on 2026-07-30 and shipped in 4.3.3.** No `src/` change in that release — it is entirely the framework: both engines, the Manager skill, this file, the architect charter, `scripts/{ledger-stats,spend,architect-file,check-syntax}.cjs`. In plain terms:

- **The report is grouped by status and may never print a number it did not compute** — including every group heading. `ledger-stats --report` checks each heading against the rows beneath it; the headline's open total comes from `ledger-stats --open` and names the command.
- **Report row ids are a source prefix plus a number** — `gh#` github · `l#` logs · `o#` owner (his ruling 2026-07-31, replacing the `G`/`L`/`B`/`O` letters). A github row carries **the ticket's own number**; `state.nextReportId` mints `l#` and `o#` only. Github wins on merge — a finding in both a ticket and the logs is `gh#166-a`. Defined once, in the Manager skill's report spec.
- **Framework findings live on `X`, the architect's charter rules on `A`** — `X` rows in `.claude/agent-loop/architect-ledger.jsonl` (`node scripts/ledger-stats.cjs --architect` prints the range), `A` rules in `.claude/agents/architect.md`. Renamed off `A` on 2026-07-31 so one letter never means two things. **Before filing, check whether the row already exists** — a repeat is resent with an update, never given a new number, and `architect-file.cjs` refuses one it recognises. It also picks the id — the lowest free number, so a merged row's number is reused rather than left behind.
- **A run that puts more than 12 items on his desk WARNS and holds nothing back** (12, measured from seven runs). Nothing anywhere caps the report on row count.
- **The backlog is a source on EVERY run** (his ruling: *"put backlog in every run"*), and every row he can rule on reaches the report — in harm order, `still-real` rows only, each carrying its recommendation, and no cap on how many (X82). `ledger-stats --open` splits into *still-real* · *need a re-read* · *cite no file*: **`still-real` means a lane opened the code and the defect is still there** — it was called `confirmed` until 2026-07-31, which read as *done* and had him inverting 38 of 44 open rows (X86). A row is re-read when its cited code MOVED **or nobody has ever looked at it** (28 of 52 on 2026-07-30, against 0 that had moved). A row citing no file can never be checked mechanically — those are named for a hand read. A re-read marks and confirms; it never closes on its own and never spends the decision budget.
- **A verify DISCOVERY is no longer a row on his desk** — it is the next build's intake. An **overturn** still blocks the wrap.
- **A ticket's numbered complaints each become their own issue**, and every issue's id names its ticket (`156-a`); a blocker raised on a ticket is `153-blockA`, not a fresh flat number.
- **The wrap states verified-against-shipped**, so a lane that skipped its own verify pass is visible.
- **`spend.cjs` checks each dispatch's tier against that agent's charter** and names the offender.

**A chat that was open before today runs the OLD engine with none of this, silently** — framework files load once per chat. Start fresh.

## Which chat is this?

**One chat per job — his split, 2026-07-29:** a **bugs** chat · a **framework** chat (the engines, the Manager skill, this file, the architect — it never touches `src/`) · and **one chat per big feature**. If the first message does not make it obvious, ask. The standing mode below governs any **interactive** chat; the loop's agents are deliberately exempt from it (see the framework section).

Standing mode:

- **Answer the question he asked, in its first sentence. Nothing else comes before it.** He asked *"which letters are free"* and got a report about a cleanup instead of a list — that is the failure, and it repeats. **A question gets an answer, not a summary of the work that produced it.** If he wanted the work he would have asked for the work. Do not circle: no lead-in, no context first, no "worth knowing", no burying the number in paragraph three. State it, then stop — and let him ask for more, which he does, reliably.
- **Answer first.** Lead with the answer — the finding, the number, the yes/no — then at most the one fact that backs it. Never: a preamble, his question restated back, a summary above or below the answer, options you considered and rejected, how you reached the conclusion, or a correction re-explained. A table, a diff or a file listing is data, not prose. He asks when he wants more. **This one bullet binds the loop’s agents too**, despite the exemption above — each charter carries its own wording of it. (His rule, 2026-07-31: *"I want less text more bottom lines and clean direct answers. If I will want more data I will ask."*)
- **Propose-first, code-first, root-cause.** Trace her live log (`powershell -File scripts/vm-logs.ps1 [term] [lines]` — she runs on the VM, and the local `logs/` dir is stale; see Operational) + the code before attributing; state root cause as `file:line`. Bug reports / frustration / screenshots are diagnostic signals, **not** build signals.
- **Trace the RUNTIME, not the design.** Check the actual log line / DB row / deployed dist / tool-call tape, not the code's or a verdict-log's *description* of itself. (Re-earned three times in one session — see Lessons.)
- **Build only on an explicit per-item "fix it / build it / do it"** on a *specific* bug/feature. Bare "ok / yes / go" is ambiguous — ask. On a build signal: edit → typecheck → **STOP** (uncommitted).
- **Wrap only on an explicit ship word** ("wrap / ship / commit / cut a version / bundle"). Default bump **PATCH**.
- **Prompt is a budget, not a junk drawer.** Enforcement → code (chokepoint guard / a return-value the model reacts to / a tool that owns the decision). Prompt only for judgment / tone / format / language.

- **A chat MOVES items. The agent decides the fix.** You have no charter — this file is your rules. File a defect as symptom, evidence and harm, **never the remedy**, and dispatch the problem rather than the solution: the architect proves the root and designs the repair for a framework bug exactly as a lane does for a Maelle bug. Check the closed rows for the same finding under another name before filing. *(Earned 2026-07-31, after three bad days: the framework chat ran the discussion, designed the fix, wrote `fix shape:` into every row, and handed the architect an order — which it then built with its own authority, so a wrong design shipped unchecked. X83's remedy was invented here and was wrong; X87 was filed as new and is X55 re-found, closed `built` while the defect stood.)*
- **A new mechanism, a new rule or a new surface is HIS call — never an order to an agent.** Caps, ceilings, gates, thresholds, new fields. Everything this framework fights is a gate somebody added to be safe, and losing an argument is not the same as him accepting it — silence is not agreement. Measure before claiming a number, and never strip a surface you did not design. *(A 12-row cap and a set of per-item word ceilings were each added and deleted inside a day; a charter rule was rewritten to say the opposite of what he asked; the built-uncommitted list — the one thing he reviews before a wrap — was deleted as narration while `--report` stayed green.)*

## The Workshop — how Maelle is built now (`.claude/agents/` + `.claude/skills/manager`)

**The Workshop is the name for this whole machine** (his call, 2026-08-07): work — a bug, a feature — is sent here to be built. The agents below are trades working in it, and `.claude/WORKSHOP.md` holds the twelve rules (`W1`–`W12`) every builder carries into every dispatch — ONE file, read first, never copied into a lane's own charter.

Maelle is built by **eight lane agents** whose charter file IS their system prompt, plus a **Manager** that orchestrates them. Two modes, one set of lanes.

**Mode 1 — the autonomous bug loop ("Bugger").** The **Manager** (`/manager`, `.claude/skills/manager/SKILL.md`) is the owner's control panel. **The owner triggers every run — there is no timer, and none is ever armed** (his call, 2026-07-28: he reaches the machine remotely, so a clock was only a substitute for being there, and it failed silently in four different ways). One run does everything: the **`editor`** finds and routes the work (open GitHub `Bug` issues **+** the log review since the watermark **+** the stale-row re-read, merged into atomic issues) → dispatch the code lanes **in parallel** (Matchmaker · Registrar · Gatekeeper · Librarian · SlackMaster · Diplomat · Handyman) → **`instructor` LAST** → chain dependencies until nothing is owed → one **`bouncer`** pass over the combined diff → cumulative report at `.claude/agent-loop/report.md`. Engine: `.claude/workflows/bugger.js`. Agents **build within their charter WITHOUT a per-item "go"** — that is the loop's whole point, and it **supersedes the propose-first standing mode above, which governs interactive human chats, NOT the loop's agents.**

**Mode 2 — feature and improvement work.** Also the **Manager**, via `/manager feature` — but on a different engine, `.claude/workflows/feature.js`. It does not go through the bug loop, whose intake and verdicts are bug-shaped (symptom / root cause / reappearance) and whose "one root = one issue" rule is bug logic. **Two invocations, deliberately:** `mode:'plan'` reads the ticket against the code and returns a per-lane decomposition plus `blockingQuestions` — **and builds nothing**; the owner approves or reshapes; then `mode:'build'` dispatches the owning lanes in dependency order, `instructor` last, and runs one `bouncer` pass over the combined diff. For one ticket, pass `refs:['#<n>']` and it skips the backlog listing. **For an idea not on GitHub yet, pass `items:[{title, asks}]`** — the ticket is filed when the owner approves the plan, not before, so a rejected idea leaves no litter and an approved one arrives with its decomposition already in it. **Give it its own session** — a design decision does not fit inside a nightly bug report without deforming it. **A build return checks `manifest.ownSpec.complete` before it is called done** — a piece of THIS PLAN that comes back `needs-owner-decision` re-enters the SAME wave (`resume.pieces` + `resumeFromRunId`, never `build <ids>`/`bugger.js`); see SKILL.md's "THE WAVE'S OWN SPEC".

> **A chat does not edit a lane's files — it dispatches.** If a change touches code a lane owns, that lane builds it, however small the diff. The charter is the quality bar, not the size of the change (this is Manager rule M1, and it binds every chat too). Two rationalizations to catch in yourself: **"it's small"** is not an exception — a one-line edit in a lane's file still has to pass that lane's rules; and **silence is not approval** — build only on the owner's explicit per-item word, never because an offer to build went unchallenged. When you're unsure who owns a file, ask the WhatsApp/seam tests below or ask the owner — don't default to doing it yourself.
>
> A chat may still edit files **no lane owns** (docs, `.claude/**`, scratch), and may always investigate, read, trace and propose freely.
>
> **Approval is per piece of WORK, not per lane — chain dependencies automatically.** Once the owner has approved a change, routing it through however many lanes it touches is *your* job, not a new decision for him. If a lane returns "this also needs `librarian`", dispatch `librarian` and finish the job — that is completing approved work, not new scope. Ask again ONLY when the scope genuinely grows beyond what was approved (a new capability, a different subsystem, a product call). The Bugger loop already chains `needs-dependency` automatically; an interactive chat is hand-running the same orchestration and should behave the same way. "Should this be built?" is the owner's question; "which lane builds it?" is never his.
>
> **Name every dispatch with its lane.** The background-task panel shows only the `description` and a generic "Agent" label — it never shows which agent is running. So prefix it with the lane: `slackmaster: coda delivery split`, `gatekeeper: the attendee fix`. With several agents in flight, "Agent · Agent · Agent" is unreadable; lane-first matches the workflow engines' own labels and stays scannable — those now carry an item count too (`matchmaker(5)`), and a `dep:` prefix on any dispatch that is a lane's second in the run (`dep:matchmaker(2)`) — `rebuild:`/`owner:` mark the other two reasons a lane comes back — so a chained run never shows two identical labels with no way to tell them apart (X168/X177).

**Feature dispatches — one charter clarification that matters.** The charters' *reduce-LOC · reduce-prompt · no-new-state* reflexes are **bug hygiene, not a ban on building.** New capability legitimately ADDS code, and sometimes state or prompt. The bar for an addition is: it **rides an existing spine, duplicates nothing, and deletes whatever it replaces.** An agent must not refuse a sanctioned feature on "the diff must trend net-negative" grounds.

**Shared by BOTH modes:** only the **owner** commits / wraps (agents never commit — they build in the tree and stop) · code-first · prove the root cause from code + logs · **security & privacy are enforced in code, never prompt** · deep fix, never a patch · **no-guess: unsure → escalate, don't build** · no regex on natural language (multilingual). Ambiguous log-review findings are **shown to the owner, never auto-fixed**. An agent that is unsure, blocked by its charter, or facing an owner-only judgment returns a verdict up the chain — it does not guess.

### The squad — lanes & boundaries

Every lane takes the product requirement into a different area. At a glance:

- **Matchmaker — the secretary.** How Maelle thinks and works on **meetings and the calendar**. Not news, not people.
- **Registrar — the spine / tasker.** Owns the **lifecycle stage** of every async process: raise → track → decide → replay → close → loop back.
- **Gatekeeper — what stops her making mistakes.** The output-time net, and nothing more.
- **Librarian — who she works with, and what's worth knowing.** Identity, what she remembers, the social layer, and — since 2026-08-11 — news, the brief's content, meeting-summary archival, venue memory and the knowledge base (the seed of a future RAG).
- **Instructor.** Everything she is told before she acts — the system prompt, tool descriptions, learned prefs.
- **SlackMaster — inside the workspace.** How a message reaches her and how an answer reaches a person, **on Slack** — the authenticated directory, org membership, threads, the three surfaces.
- **Diplomat — outside the workspace.** Every channel reaching someone who is *not* in Slack. Mail is the live one (poll, sender gate, forwarded-header extraction, the one-address reply); **WhatsApp and iMessage land here too.** Split off Slack 2026-08-01 as `exchanger`; **became the outside-world lane 2026-08-02** because the seam is a trust boundary, not a provider.
- **Handyman — the machinery.** ⭐ **WIDENED 2026-08-03:** *"the guy that makes sure the machinery is working."* **Infrastructure and the cloud** (the VM, the deploy watcher, PM2, automation) · **the database and its migrations** · **what the LLM costs per turn and how fast she answers** (absorbed from the deleted `quartermaster`, standalone as H4–H5; tier moved to the Workshop's W12) · plus whatever no lane owns yet. It **incubates** a small skill until that skill is big enough to earn its own agent, if ever.

*Rule tags are one letter per lane:* **M**atchmaker · **R**egistrar · **G**atekeeper · **L**ibrarian · **I**nstructor · **S**lackMaster · **D**iplomat · **H**andyman. **Three letters have MOVED since 2026-08-01** — `outrider` became **`handyman`** on 2026-08-03 (`O`→`H`), his call: *"the one that fixes stuff no one else is fixing — if you have a problem at the house that isn't for a specific profession, you call the handyman."* Outrider described escorting a carriage and said nothing about building; Journeyman was rejected because it names a skill level, not the absence of a specialist. **`O` and `H` were both 0 in `src/` before the move** (`O` cleared under X118 the day before), so no clearing pass was owed. And on 2026-08-01 — the requests spine went `S`→`R` (shepherd → registrar) and Slack went `T`→`S` (transporter → slacker, renamed slackmaster 2026-08-02 with `S` unchanged) — and every `src/` rule tag was rewritten in that same change, so an **`S` tag now means Slack, never requests, and no `T` names a lane.** `T` does not grep to zero the way `E` does: `\bT[0-9]+\b` in `src/` is **38** — 22 clock fragments (`${date}T00:00`) and 16 spent build-piece ids (`gh#144` pieces `T0`–`T2`; thread-actions phases `T1`, `T3`–`T5`). Read the line before you read a `T` as a rule. The names before 2026-07-28 — meeting / requests / guard / people / context / slack / outer — are retired. **`D` is the outward lane's, minted 2026-08-02** when the mail agent became the Diplomat: `\bD[0-9]+\b` in `src/` is **0**, so a `D` tag can only mean that lane. **`E` IS THE EDITOR'S as of 2026-08-03** — reserved by the `usher`→`editor` rename, no tag minted yet (gh#171). It had been cleared on 2026-08-01 for the Exchanger (the spent `gh#24` build-piece ids `E1`–`E10` and two older `E1` sets deleted from `src/` comments, leaving `E[0-9]` at **0**), that agent became the Diplomat on `D`, and the letter sat clean in the pool until the Editor took it — so `\bE[0-9]+\b` in `src/` is still **0** and an `E` tag can only ever mean the Editor. **`W` was believed free after 2026-08-02** (WhatsApp folding into the Diplomat), but `\bW[0-9]+\b` in `src/` is **9 files, not 0** — a closed, dated gh#154 investigation (2026-07-27–2026-08-06) used `W1`–`W5` as its own worth-knowing labels, left as prose in code comments once that design shipped. None of that is a lane's rule-tag registry, so it never blocked a new lane taking the letter; **`W` is now spoken for regardless — it is the Workshop's own shared-rule prefix** (`W1`–`W12`, `.claude/WORKSHOP.md`), so no future lane may mint a `W`-tagged rule of its own. **`Q` is free as of 2026-08-03**, released when the `quartermaster` was deleted into Handyman — it never needed clearing, because `\bQ[0-9]+\b` in `src/` is **13 matches across 12 lines** and every one is a fiscal quarter inside quoted prose (`"focused on Q3 targets"`; `summary.ts:476` carries two on one line, which is how a line count read 12), never where a rule tag sits. **`U` is free as of 2026-08-03**, released by this same rename and never used for anything. So the free pool is **Q · U · W**. **`P` RETIRED 2026-08-11**, when `profiler` renamed to **`librarian`** on **`L`** (`\bL[0-9]+\b` in `src/` is **27 matches across 10 files**, all real rule citations). **The comment sweep from `P` to `L` is now DONE**, re-verified 2026-08-11: Cleaner relabeled 27 real citations across 10 files, and the four heaviest old citations — P2/P4/P6/P7 — are all at **0**. `P` still does not grep to zero the way `D`/`E` do: `\bP[0-9]+\b` in `src/` is **49 matches across 20 files** (down from 76 pre-sweep), but every one of the 49 that remain is confirmed to be leftover piece-numbering from the 4.2.0 charter-audit wave (shipped 2026-07-26) — `P3`, `P13`, `P15`, `P20`, `P22`, `P24`, `P25`, `P26`, `P29`, `P30`, `P32`, none of which falls inside a Profiler/Librarian charter's own rule range, and every occurrence sits in files Profiler never owned (`scheduleRules.ts`, `findAvailableSlots.ts`, `bookingRequest.ts`, `moveMeeting.ts`, `runOutputGates.ts`, `systemPrompt.ts` — Matchmaker/Gatekeeper/Instructor territory). None of the 49 is a stale rule citation waiting on a relabel; they are the same kind of frozen build-piece residue `T`'s 38 and `W`'s 9 already are. Read the line before you read a `P` as a rule, exactly as for `T`. `P` still does not join the free pool — like `T`, a letter carrying frozen piece-numbering residue from a past wave is not the same as clean.

**Not a lane — `bouncer`** (`.claude/agents/bouncer.md`). The gate before a wrap: one adversarial read over a finished wave's combined diff, asking first *did it actually fix the reported problem*, then *is this safe to ship* and *does it meet our standard* (no dead code · reuse before add · root not patch · one spine · cheap at runtime · security in code). Owns no code, holds no `Edit`/`Write`, runs on **Opus** while the lanes run on Sonnet. It refuses to run on an unfinished wave. Took this job off `gatekeeper` on 2026-07-27, because a lane that owns code cannot review a diff containing its own. **Rule tag `B`, fourteen rules (B1–B14) — minted and numbered 2026-08-07**, the last-line-of-defence audit gh#181 applied to every other agent but had skipped on this one; `\bB[0-9]+\b` in `src/` is **0**, so the letter is clean there (the only prior `B1`/`B2` is in the dormant, pre-lane `.claude/APPROVAL_CHAT_CHARTER.md`, outside this tag registry and cited by nothing current). Since 2026-08-07 it also reads the architect's own framework diffs with these same five questions and no separate step — the "cold read" ceremony A13 used to name is retired; a framework diff is ordinary work for it. Two renames: `verifier`→`examiner` on 2026-07-31, because `dateVerifier` is one of Maelle's own guards and *"the verifier flagged it"* named two different things; `examiner`→`bouncer` on 2026-08-01, his call — *"the bouncer, like in the club. It's not just examining the code, it's sending people back."* Examining is half the job; turning work away is the other half, and the charter already biases it toward blocking.

**Not a lane — `editor`** (`.claude/agents/editor.md`). It owns no code and builds nothing: it pulls the open GitHub `Bug` issues, reviews the logs since the watermark, re-reads the stale open rows, merges what it finds, and routes each atomic issue to the lane that owns the fix. Read-only (`Read · Grep · Glob · Bash`). **The lane table above is the map it routes against — keeping that table current is what keeps routing correct.** **Renamed `usher` → `editor` on 2026-08-03, his call** — *"I hate the Usher name. It's not a known profession in my own language, no one uses it."* That is his standing naming test (the one that also refused `Foreman` and `Handler`): a name needing explanation fails. **`Editor` won on his own reasoning** — *"an editor in a newspaper mostly doesn't write the articles — they assign tasks, put focus, and let them run"* — so read-only stops being ironic and becomes the point, and it is a real profession in every language including Hebrew (עורך). `Quartermaster` was runner-up and lost on substance: it issues what is asked and holds no judgement about merit, where judging what is a real finding is this agent's hardest work. Two earlier names are retired: `scout` (2026-08-01, it named only the finding half) and `triager` (refused — a back-formation with no dictionary entry). **One boundary the name invites: a newsroom editor also checks the finished piece; this one does NOT — that is the `bouncer`.** It assigns and shapes; it never reviews finished work. **Letter `E` is reserved with NO rule tag minted** — a tag nothing can cite invites a reference to an `E1` that does not exist, and it writes itself when rules land (that is gh#171). `\bE[0-9]+\b` in `src/` is **0**, re-verified at the rename. **It is the BUG door only** — the feature door is the `framer` below, split off 2026-08-02.

**Not a lane — `framer`** (`.claude/agents/framer.md`). ⭐ **NEW 2026-08-02.** The **feature door**, and the counterpart to the editor: it takes a product item (a GitHub `Improvement`/`Feature`, or an idea not filed yet), works out what it actually means against the code, **drafts a plan the owner rules on, and only once the shape is agreed cuts it into per-lane pieces**. Read-only (`Read · Grep · Glob · Bash`); it owns no code and builds nothing. It runs the three `feature.js` passes — intake (`:484`, haiku), recon (`:608`, **Opus**, one agent per item, because nothing backstops a bad code-read), decompose (`:653`). **Both doors route into the same eight builder lanes against the lane table above; only the editor reads the logs.** Split from the editor on 2026-08-02: those three passes had run with **no `agentType` and therefore no charter** (X125), and giving them the editor's charter put four bug-only rules on the feature track — the very hard "obvious defect" bar, *one root = one issue*, *do not diagnose*, and *a product decision means do not dispatch*, the last of which would have made every improvement undispatchable. **Rule tag `F`, ten rules (F1–F10), every one extracted from a prompt string already in `feature.js`** rather than invented. `\bF[0-9]+\b` in `src/` is **0**, so the letter is clean.

**Not a lane — `architect`** (`.claude/agents/architect.md`). It maintains **the framework itself** — the two engines, the Manager skill, this file, `agent-loop/` state, and the framework's own tooling. It never writes `src/` and never rules on product code quality; its subject is whether the agents are in sync, doing their job, and efficient. **Rule tag `A`.**

> **If you hit a problem in the FRAMEWORK: do not fix it, and — as of 2026-08-04 — do not file it yourself unless you ARE the architect.** His words: *"I don't want other agents, or other chat to send messages to the framework. only here."* If you are a lane, the editor, the framer, the bouncer or any other non-architect chat, say what you found in your own reply/report instead — it reaches an architect session from there, the same way any other finding for him travels. **Do not ask Idan to relay it either** — name it plainly and let the next architect session pick it up.
>
> **If you ARE the architect:**
> ```bash
> node scripts/architect-file.cjs --session architect --finding "…" --evidence "…" --target feature.js --source "this chat"
> ```
> `--session architect` is REQUIRED on every write — filing, `--close`, `--recheck`, `--duplicate` — and the script refuses outright without it (X163). A framework problem is an engine that did nothing while reporting success, a manifest that lied, a skill instruction that contradicts the code, a charter that no longer matches what a lane does. **A Maelle bug is not one** — that is a GitHub issue or a report row, and it belongs to a lane. The script refuses a row with no checkable evidence (`--targets` lists valid targets); read the backlog with `node scripts/ledger-stats.cjs --architect`. It also refuses a **re-file**: if your finding matches an open row it stops outright, and if it matches an **already-built** row it asks you to read that row and pass `--amends <id>` or `--amends none` — because a match against built work is usually an amendment, and building a mechanism that already exists costs a full dispatch for nothing. Closings live there too: `--close <id> --built "<what shipped, and where>"`, `--close <id> --declined "<his reason>"`, and `--recheck <id> --checked "<what you opened>"` for a row you re-read and found still real.
>
> **The architect proposes; Idan approves; then it edits — except for a DEFECT**, which it fixes and reports (a mechanism that cannot fire, a field nothing reads, a check that passes on known-bad input, a count that is wrong). **The question to ask is his: is this a PRODUCT DECISION, or a bug that must be fixed to complete the product spec?** Changing the framework — or a new process, a new idea, a problem in how it is **run** — is his. A bug in what was already agreed about how the workflow and process should work is the architect's. The propose-first bar is stricter than a lane's on purpose: Maelle's code ships through a deploy, framework code does not — an engine edit is live on the very next run, so there is no gap in which a mistake gets caught.
>
> **Filing is architect-only; RUNNING a session is only ever his call.** He triggers every architect dispatch — no timer, no wrap step and no returning run does it for him. A non-architect chat that finds something still loses nothing by not filing it itself: naming it plainly in a reply costs about the same as a filing command, and it is what gets an architect session dispatched with the finding in hand.
>
> **And a framework edit only takes effect in a NEW session** — engines, skills and agent definitions all load once per session and none of them says so. If a framework change was made in this chat, it is not in force here.

**Not a lane — `cleaner`** (`.claude/agents/cleaner.md`). ⚠️ **NEW 2026-07-31 and NOT IN ROTATION — do not dispatch it.** The owner runs it himself the first time, after the current bug wave; until he says otherwise the editor must not route to it and no engine may call it. The periodic hygiene sweep: dead code and dead files, comments that contradict the code, dead config keys, and cloneability outside the prompt layer. **Its line is PROVABLE vs JUDGMENT** — it acts only on what the code settles (*is this called, does this comment match, is this key read*) and reports anything needing a decision about what the code should BE. That makes it explicitly **not the auditor**: seams, splits and design questions are returned as `audit`, never acted on. Read-write on `src/`, `scripts/` and the `.claude/*.md` documents (a doc whose every citation is dead gets deleted — git holds it); **detect-only on every YAML** (the user interface) **and on the framework's own surfaces** — `agents/`, `SESSION_STARTER.md`, `skills/`, `workflows/`, `agent-loop/`. It never changes behaviour — a diff that does is out of scope by definition — and it has **no verify pass behind it**, so its per-change proof sentences plus typecheck are the whole gate. **Rule tag `C`.** Verdicts: `cleaned` · `needs-lane` · `needs-owner` · `audit` · `nothing-to-do`.

**DELETED — `quartermaster`, created and removed on 2026-08-03.** It owned runtime cost and latency for one morning. The case against it, which the owner accepted: its **measurable** half was already three charters' property (Gatekeeper's *cheap in model, count and time* · Instructor's I2/I4 · the bouncer's *no extra LLM call where a deterministic check would do*), so a fourth copy could only drift; and its **defining** half had no data source at all — `processMessage.ts:681` logs no duration and nothing in `src/` times a turn, while its own Q1 forbade editing and Q2 forbade estimating, so its first dispatch could return exactly one thing. **Zero dispatches, ever.** Its subject is now **Handyman's**. Of the four rules that ever stood on their own here, two keep a number today (**H4** — measure, now also carrying latency's content — and **H5**, the always-on tax) out of Handyman's 2026-08-04 16-rule charter (gh#181, reconciled down from a 19-rule draft); latency lost its own number by folding into H4; and **tier — the model a call runs on is his call — moved out entirely to the Workshop's W12**, which also carries a new rule with no quartermaster lineage at all (a new LLM call needs his sign-off before it ships). **Letter `Q` is back in the free pool** — `\bQ[0-9]+\b` in `src/` is 13 and every one is a fiscal quarter inside quoted prose, so it was never a tag and needs no clearing pass. Tracked as **gh#172**; do not re-create it without turn-duration instrumentation in place first.

> **A HAND-DISPATCHED WHOLE-PROJECT AGENT'S FINDINGS ARE WRITTEN BY WHOEVER DISPATCHED IT — that is you, in the same turn the verdicts come back.** `cleaner` is the first of these and it will not be the last (a cost/speed sweep is next). They are **scoped agents the owner runs by hand on purpose** — that stays; what travels with the dispatch is the write duty. **No engine persists their output, so if you only report it in chat, the whole run is on the floor.** That is the 2026-07-28 failure exactly: four overturns reported in chat while `report.md` still read *"Empty, nothing is waiting on you."*
>
> **AND FOR THE `cleaner`, ONE MORE THING IN THAT SAME TURN — ADVANCE ITS WATERMARK (X131).** Set `state.lastCleanSha` to the HEAD sha the sweep actually read, **only when the run COMPLETED**; leave it untouched on an abort or an error, so the skipped window is re-scanned rather than lost. It is the cleaner's own clock and **not `lastWrapIso`** — that one belongs to the wrap, and reusing it is what made the cleaner's default scope the live working tree. `null` means no run has ever completed and the next sweep is the full `src/` pass. Forget this and the cleaner re-reads the whole repo every time, which is the cost the field exists to remove.
>
> **Where those rows go and what shape they take is defined once** — the *"WHERE A `cleaner` RUN'S FINDINGS LAND"* block under **The report** in `.claude/skills/manager/SKILL.md`. Read it **before** you dispatch. It names the ref to use, the existing verdict spellings to map onto, and the one spelling that would make the row vanish from `--open`. Do not restate it here and do not invent a second shape.

| Lane | Owns | Never touches |
|---|---|---|
| **Matchmaker** | deterministic scheduling core — search / validate / book / move / cancel, free-busy, TZ + Working-Elsewhere, floating blocks, the Graph CALENDAR layer + cache | the requests spine · the guards · prompt wording · transport |
| **Registrar** | the async work-item spine — everything with a row in `requests` (approvals, outreach, reminders, follow-ups, research): raise → track → decide → replay → close → loop back, incl. timers/expiry, the requester relay and the owner's daily decision thread | the meeting planner core · the guards · the prompt · **what an item DOES when it fires** (that's its domain lane) |
| **Gatekeeper** | output-time gate stack (claimChecker / humanGate / dateVerifier / securityGate / availabilityPreCheck) + `postReply` orchestration + `summarizeToolCall` truthfulness | the flows the guards protect — a broken flow is fixed in ITS lane, never papered over · **the framework's verify pass** (that is `bouncer` now) |
| **Librarian** | identity + the person store (`db/people.ts`) + people memory + social (topics, codas, engagement) + Maelle's own self row + news + the brief's content + meeting-summary archival + venue memory + the knowledge base (`knowledge.ts`, the RAG seed) + `tavilySearch` | other lanes' *use* of person data · the brief's scheduled trigger (Handyman) |
| **Instructor** | the context budget — `systemPrompt.ts`, tool descriptions, learned-MD prefs. **Runs LAST** | anything code can enforce; **never** security / privacy; conversation/thread context (SlackMaster) |
| **SlackMaster** | **INSIDE the workspace** — Slack end to end: inbound routing + queue, threading, DM/MPIM/channel posture, authority by authenticated sender, dedup + catch-up, the `postReply` delivery pipeline (Slack-only), Slack's `Connection` implementation, media/platform features | the **gate decisions** inside postReply (Gatekeeper) · what an event *means* (Registrar) · person data (Librarian) · **anyone outside the workspace (Diplomat)** · **the `Connection` spine (Handyman's)** |
| **Diplomat** | **OUTSIDE the workspace** — every channel reaching someone who is not in Slack. Mail is the live one: `connectors/email/` (sender gate, forwarded-header extraction, HTML→text), `connections/email/` (`EmailConnection`, the one-address send cap, reply-not-compose), `connectors/graph/mail*.ts` (poll, delta/isRead dedup, auth), `scripts/email-auth.mjs`, the `channels.email` config block, the email leg's tempo. **`connectors/whatsapp.ts` is his the day it opens to a non-owner** — dormant and owner-only today | Slack (SlackMaster) · the `Connection` **spine** (Handyman's) · what the output gates DECIDE (Gatekeeper) · the reply-shape prompt (Instructor) · the person store (Librarian) · booking (Matchmaker) |
| **Handyman** | **the machinery** — infrastructure and the cloud (VM, deploy watcher, PM2, automation), the DB layer **and its schema/migrations**, **runtime cost and latency** (H4–H5, from the deleted quartermaster; tier is the Workshop's W12) · **the connective plumbing between lanes** (the `Connection` contract itself — shape and plumbing both, since 2026-08-11 — the Graph CLIENT layer only — auth, tokens, `graphClient`, user/profile reads; calendar is Matchmaker, mail is **Diplomat**) · plus the genuine catch-all, non-knowledge only: thread-actions, routines (the dispatch mechanism — the brief's content moved to **Librarian** 2026-08-11) and non-request async jobs, core orchestrator (non-prompt / non-gate), health, config, scripts. **Incubates a small skill until it earns its own lane** | anything a lane owns — including **WhatsApp, which is Diplomat's and never the catch-all's** · the knowledge-shaped skills, now **Librarian's** (news, brief content, summary, venue, knowledge base, web search) · the **prompt** budget (Instructor's, so a prompt-token cost finding is `needs-dependency`) |

**Seams that cause bouncing — settle them, don't guess:**
- **Route by where the durable FIX lives, not where the symptom appeared.** A leak *appears* at output but is usually fixed in the flow that produced the data.
- **Gatekeeper vs flow:** the gates = Gatekeeper; whatever fed them = its own lane. **A missing backstop is not its own bug.**
- **Librarian vs Matchmaker:** the person store + its semantics = Librarian; which attendees enter a search = Matchmaker.
- **Librarian vs Instructor:** person *facts* = the store (Librarian); the owner's *opinion* of a person = learned MD (Librarian routes the content, Instructor owns the injection).
- **Librarian vs Handyman:** what the morning brief SAYS = Librarian; the cron that FIRES it (`routine.ts`) = Handyman. Same split as any other routine — the dispatch mechanism is machinery, the content is the domain lane's.
- **SlackMaster vs Diplomat — ask WHO is on the other end, not which API:** is this code here because the person is **inside** the workspace, with an authenticated id and a directory behind them? → **SlackMaster** (so the `postReply` **pipeline** is his — Slack-only, no outward channel enters it — while the **gates inside it** are Gatekeeper; a ✅ reaction *arrives* via SlackMaster, what it *resolves* is Registrar). Because they are **outside** it — no directory, identity by claim, the reply forwardable anywhere? → **Diplomat**, whatever the provider: mail today, **WhatsApp and iMessage when they open**. Survives both because Maelle has transports at all — `src/connections/{types,registry}.ts` → **Handyman's** since 2026-08-11 (reversed the 2026-08-01 ruling that it was ownerless — that call predates Handyman's seams-between-lanes facet, the concept it needed to be held by; every lane still carries the routing rule as the Workshop's W11). Otherwise → the domain lane.
- **`instructor` is a last-resort destination** — never route there merely because a symptom is visible in a reply.
- **`handyman` only when no specialist owns it** — it is not a bin for the unclear; unclear = escalate to the owner.
- **calendar-health belongs to `matchmaker`** (settled): dense packing, auto-move/defrag and floating-block rebalance (`calendarHealth`, `rebalanceFloatingBlocks`, `calendarDensity`) are governed by M7 — there is no separate calendar lane. Matchmaker owns both halves: the commitment *and* the shape of the day.
- **Matchmaker vs Instructor, inside ONE file (2026-08-03):** `src/skills/meetings.ts`'s tool definitions, descriptions and prompt section are Instructor's entirely; the `check_join_availability` handler body and the single delegate line to `ops.ts` are Matchmaker's — real scheduling behavior sitting in a file Instructor otherwise owns.
- **Registrar vs Instructor, inside ONE file (2026-08-03):** `src/tasks/skill.ts`'s `executeToolCall` (the lifecycle, ~70% of the file) is Registrar's; `getTools()` and `getSystemPromptSection()` in that same file are Instructor's.

## Shared tree — several lanes edit at once

Assume multiple lanes (and chats) hold uncommitted edits at any time. At wrap: `git fetch`, read the **FULL** working tree, and bundle everyone's work — never commit only your own files without checking the rest. Re-baseline before editing; line numbers move under you. Cross-lane hand-offs are self-contained blocks: root cause + `file:line` + log/DB evidence + a fix framed as a **suggestion to verify, not a mandate**.

**Infrastructure is HANDYMAN'S as of 2026-08-03** — the VM, the deploy watcher, PM2, automation and the DB schema all have an owner now, where this line said *"no lane owns these"* for a week while 69 infrastructure-shaped ledger refs spread across all eight lanes with no concentration anywhere. **Still owner-driven:** model/LLM-layer campaigns (`llm/models.ts`, thinking policy, effort per surface) — a tier is his call (the Workshop's W12, as of 2026-08-04 — every lane's, not only Handyman's), and these are deliberate projects rather than nightly bug flow.

## Open / deferred bugs

**The live list is `node scripts/ledger-stats.cjs --open` and `gh issue list`** — both print their own counts and their own split, so no number from either is copied here. Only what neither of those can tell you is kept here:

- **1.4 — MPIM owner-clamp: KEEP it, do NOT loosen.** The clamp is an **anti-cheat security boundary across ALL tools** (colleagues must never gain owner authority / book for themselves), not just calendar privacy. Keyed on the Slack-authenticated sender, not spoofable (`app.ts`, the sender-identity read; `processMessage.ts` holds the clamp). The owner's "flag-and-override" in his own group DM is delivered SAFELY via the approval flow, NOT by re-granting in-group authority. An earlier "Option A" loosen was stood down. Live context: **#157 and #158 are open MPIM tickets** — do not read either as licence to loosen this.
- **Move-path roster: union or replace?** — still undecided, and the harm it named is already guarded. `findAvailableSlots.ts` (`resolveMovingEventAttendees` + the `#145b` overlap test) folds a moving event's roster in only when it shares an attendee with the explicit set, so the 4→7 balloon cannot recur. The open question is whether the event roster should be **authoritative** (replace) rather than merged — his call, no ticket.
- **M3 no-TZ fallback frame.** An attendee with no stored timezone is assumed in the **owner's** zone (`attendeeAvailability.ts`, `fallbackTimezone`); he asked for the **requester's** zone. Identical for owner-initiated searches, differs when a colleague in another zone requests.
- **`hasApproval` / `actionCount` log uniformly 0** (`processMessage.ts`, the `Orchestrator completed` line). A logging artifact, **not** an escalation failure — 2026-07-21 forensics showed `create_approval` did fire and resolve. Fix the counters, don't chase the escalation.
- **Remaining audit items** (`.claude/V4_AUDIT_HANDOFF.md`): mediums M5/M8–M11 and the Wave-5 lows. None block anything.
- **B&H "Outside" event** — one already-booked event still carries the wrong Outlook category. The category-authority fix is forward-only; this row needs a manual recategorize or one ask to Maelle.

## Lessons re-earned (read before trusting your own analysis)

- **Trace the runtime, not the design — three wrong calls in one session:** (1) graded a push-fallback paper-trace 14/14, but the finder's `movingEventForbiddenZones` rule rejected the move at runtime — the helper's math was traced, not the finder contract; (2) blamed model "context-bleed" for a 4→7 attendee balloon when the log showed a *code* path (the move-path roster union); (3) concluded a calendar category was "correct" from the planMeeting *verdict* log while the actual Graph write used a different value. In every case the real runtime (log / dist / tool-tape) contradicted the reasoning-from-intent.
- **When many symptoms share one cause, fix the cause — don't whack-a-mole.** Six+ distinct bugs in one session traced to a single model swap; the one-line revert collapsed the wave. The individual code fixes were still worth keeping (they close real model-independent gaps), but the leverage was in the common cause.
- **A model swap exposes prompt-only enforcement.** Behaviours held by prompt alone (approval escalation, attendee tracking, honest "I checked", category choice, mention formatting) broke when the model changed, because nothing enforced them in code. Move enforcement to code.

## Operational

- **Maelle runs on the GCP VM `maelle-agent-vm` (europe-west4-b), NOT this machine.** Deploys are AUTOMATIC: `git push` to master → the VM's `maelle-deploy-watcher` pulls, builds, and restarts her within ~2 min. **Never `npm run deploy` / `pm2 restart maelle` here** — there is no local Maelle, and starting one opens a second Slack socket. Read her live logs with `powershell -File scripts/vm-logs.ps1 [term] [lines]`; confirm a deploy via the boot stamp (gitSha must equal HEAD). The local `logs/` dir is STALE (frozen at the cutover).
- **Exactly ONE Slack socket** — two Maelle processes on the same app → Slack `too_many_connections`. Post-cutover (2026-07-31) the VM holds the only socket; never start a local Maelle.
- **Typecheck** (from the main repo, not a worktree): `npx tsc --noEmit -p E:/Code/Maelle/tsconfig.json`.
- **After editing an engine or a framework script:** `node scripts/check-syntax.cjs`. **Never `node --check` on `.claude/workflows/*.js`** — it exits 0 without parsing, so a file with a deliberate `{,}` in it reports success (X28). The engines run as an async function body, which no standard parser accepts; this script compiles them that way and proves it by requiring a broken copy of each file to FAIL.
- **NEVER run the loop in a git worktree — it fails SILENTLY and looks like success.** Every write to `report.md` / `state.json` / `agent-loop/` lands in the worktree, never reaching master, so the wrap sees none of it. Run from `E:/Code/Maelle` itself. This is why the spawned-task chip (which offers a fresh worktree) is the wrong door for a run — open a normal new chat instead.
- **GitHub:** bugs flow through chat / the spawned-task chip; never `gh issue create` without an explicit "file it."

Read at session start: memory `project_architecture.md`, `project_overview.md`, `project_we_timezone_spine.md`, `project_gcp_migration.md`, and the `feedback_*` memories (auto-load via `MEMORY.md`).
