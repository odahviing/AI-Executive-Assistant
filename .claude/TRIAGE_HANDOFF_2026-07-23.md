# Triage Handoff — 2026-07-23 real-day bug wave

Checkpoint written mid-session (context filling). Every open finding from today, grouped by the **owning chat** that should resolve it, each with Issue / Root cause (file:line) / Plan / Risk / Owner's comment / status.

## State
- **Running: v4.0.1** (`gitSha 9f4ce88`). Confirmed: EVERY flow today ran on 4.0.1 — the 4.0.2 changes were NOT live, so they did not cause any of this. Stop chasing version/timing.
- **Committed, NOT deployed: v4.0.2** (`051a46e`) — thinking-per-surface (brief/summary/knowledge) + guard-parallelize + humanGate forced-tool + close_loop→Haiku. Owner-authored → won't auto-pull; deploy manually + watch.
- **All traces complete** — every item below is root-caused with file:line + log evidence (confirmed-vs-inferred noted where it matters). No open investigation threads.

## Cross-cutting themes (the real bottom line)
1. **Coordination/slot-finding is strong on the COLLEAGUE path, weak/absent on the OWNER path** — the "propose times, never bounce" rule + the `availabilityPreCheck` pre-compute are colleague-only. (Gidon bounce, and part of Simon.)
2. **Images are lost across turns** — bytes attached only on the arrival turn; a lossy Haiku one-liner is all that carries forward. (Gidon image + "under a bit of doubt".)
3. **Guard coverage gaps** — claim-checker shield backs claims by tool-NAME not outcome; humanGate misses image-handling self-talk.
4. **Slot presentation under-informs** — surfaces point-conflicts, not "blocked all day"; cross-TZ attendee hours not applied early.

---

## MEETING chat (slot-finder + cross-TZ + owner-path coordination)
- **M1 — Simon Sunday under-surfaced.** Issue: owner pushed "Sunday works"; Maelle said "Simon busy at 10:15 and 12:00" (two point-conflicts) when Simon was booked ALL Sunday (25/25 positions). Root: shipped `day_summary` is the owner-tagged *backstop* pass's, not the *strict* pass's that carries the "blocked 25/25" signal (`findAvailableSlots.ts:1301-1304`); only 2 of 4 windows surfaced (`MAX_PER_DAY=4`, `graph/findAvailableSlots.ts:583` + `pickSpreadSlots`). Plan: when the tagged backstop fires, also ship the strict blame ("busy every slot in this window"). Risk: low (keep to blame counts already surfaced; no attendee-calendar leak). Owner: "findslot broken → got bad times."
- **M2 — Simon Thursday: "today" treated as a HARD constraint → lunch-squeeze escalated.** Findings (confirmed): "today ≤16:30" was **Simon's own literal words** (his inbound template) — Maelle did NOT invent it, so the owner's "he didn't ask for today" is factually off (likely a careless template default by Simon). The day was genuinely full for a same-day colleague booking: `within_lead_time:17` (colleague 4h lead-time from 10:05 → earliest ~14:05, `findAvailableSlots.ts:664-666`) + `focus_time_office:4` + `owner_busy:4` → 0 clean. **11:35 was a bad suggestion**: it only surfaced via the `must_be` relaxed-recovery path (`findAvailableSlots.ts:764-769`), which bypassed soft protections and returned a slot breaking TWO — the 4h colleague lead-time AND the lunch block (`details_json.rule=floating_block_overlap`, "no room for Idan's lunch"). It's owner-meeting-free but IS his lunch — hence "No im not free." **Root:** Sonnet classified Simon's "today" as `must_be` (HARD) → went straight to squeeze-a-lunch-slot-and-escalate instead of "today's full — want a clean later slot?" Plan: don't treat a colleague's stated same-day timeframe as `must_be` on the first pass; offer clean later options first, reserve squeeze+escalate for an explicit insist. (Also: 30 min asked, 25 searched — 30 not in `allowed_durations`, minor.) Owning: meeting + the `must_be` classification upstream.
- **M3 — Yael/Tyler cross-TZ: the deep one.** Issue: Maelle offered Israel-morning slots (03:00–08:00 Boston) for a Boston attendee TWICE, and only landed the one workable slot after Yael manually supplied Tyler's email AND "from 16:00." **ROOT CAUSE (confirmed): Tyler Cullen had NO `people_memory` row until booking** (`timezone NULL`, created `2026-07-23 11:50` from being added as an attendee). Attendee **identity resolution AND the timezone/work-hours clip are both backed SOLELY by `people_memory`** (`searchPeopleMemory`, `people.ts:668`) with **no Graph-directory / mailbox fallback**. So a real company employee who never messaged the bot is invisible to both: (1) `resolveNamedInternalAttendees` (`resolveAttendeeEmails.ts:135-188`) → Tyler unresolved → searches ran with `attendee_emails:[]` (turns 2-3); (2) `loadAttendeeAvailabilityForEmails` (`attendeeAvailability.ts:114-118`) skips when no `person.timezone` → **Tyler's Boston hours never applied on ANY turn**, even turn 4 (his row didn't exist yet). The correct landing was driven ENTIRELY by Yael's manual inputs (email → email-keyed Graph busy annotation; "from 16:00" → a non-wrapping per-day band standing in for Boston hours), NOT by the system resolving Tyler. Bonus trap: when Sonnet tagged the search-window timezone (turn 3), the endpoint-shift produced a **wrapping band** (16:00 IL→01:00 IL next day) that silently DISABLED the per-day clamp (`graph/findAvailableSlots.ts:672`) → interior days offered owner-hours again → 07-28 10:00 re-offered. **Plan: (A, real fix)** add a Graph `mailboxSettings` fallback (timeZone + workingHours) when an attendee email resolves to a live mailbox but has no people_memory tz — feed into `attendeeAvailability.ts:114-118` instead of `continue`; **no mailboxSettings reader exists in src today**. (B) directory-backed identity resolution when `searchPeopleMemory` misses. (C) harden the wrapping-band clamp. (D) backstop narration ("I don't know <email>'s TZ — using owner hours"). Risk: affects EVERY never-seen/external attendee (3 internal rows already have `timezone NULL`); silently proposes middle-of-night slots and relies on a human catching it. Owner: "Yael really suffer this entire chat."
- **M5 — online cross-country meeting booked with a PHYSICAL location** (secondary, confirmed in the Yael flow): the Idan×Tyler online intro got location "Idan Office" (planMeeting "office day + internal ≤3 → Idan Office"), ignoring `meeting_mode:online` + a remote attendee. Also the dual-TZ grounding (#148) never ran (no attendee availability entry) so Sonnet had no "17:45 = 10:45 Boston" string. Owning: meeting.
- **M4 — owner-path "propose times" (shared with PROMPT P3).**

## APPROVAL chat (close-loop / approval lifecycle)
- **AP1 — double-notify delivered the WRONG message to Simon.** Issue: owner asked to relay a QUESTION to Simon (needs you, or can Rita cover?); Simon got a REJECT relay instead, and the question was silently dropped. Root: Sonnet called `resolve_approval(reject)` + `message_colleague(question)` same turn; the resolver auto-relays the reject (`resolver.ts:210`, compose `:761-836`), marking Simon "already relayed-to," so the double-notify guard (`index.ts:444-462`) short-circuited the follow-up carrying the real question. Guard assumes both notifications are the same; here content was opposite. Plan: suppress only a same-OUTCOME close-loop, not a content-bearing new message — key on presence of distinct body text (keeps the Ayala double-DM guard intact). Risk: re-intro double-DM if not keyed on body.
- **AP2 — approval REJECTED mid-negotiation → three divergent end-states, nothing tracked (THE primary issue).** Bottom line (confirmed from request `req_1784790347846_13vkc` + logs): the owner's final instruction was "tell Simon he's on vacation next week, ask if it must be him or Rita can cover" — a **defer + relay-a-question**, i.e. a LIVE coordination. Sonnet resolved it with `resolve_approval(reject)`, which (a) `cancelled` the whole request (its `deferred_action` was `create_meeting`, so cancelling killed the entire coordination, not just the dead 11:35 slot), (b) auto-relayed a **decline** to Simon (`notifyRequesterOfDecision(reject)`, `resolver.ts:210`), and (c) created **no** follow-up request/task to track Simon's pending answer. End state: **System** = cancelled, nothing pending; **Owner** = told "I flagged the question to Simon, I'll let you know when he replies" → believes it's open+tracked; **Simon** = got a decline, never cleanly asked the question (that `message_colleague` was the one AP1's guard dropped). Three parties, three beliefs, no source of truth; if Simon replies there's nothing to reconnect to. **Root:** wrong verdict — the right one was **`amend` with `relay_to_requester`** (`resolver.ts:279-322`), which flips to `awaiting_colleague`, DMs the question (renders "Idan asked: …", `:766-789`), and keeps it OPEN. Plan: for a "relay a question / defer" instruction, use `amend/relay_to_requester` (or open a follow-up `awaiting_colleague` request), close the dead 11:35 as *superseded* (not a colleague-facing reject), and give the owner a status backed by a real open item. AP1 (the dropped question) is part of THIS same miscommunication, not separate. Owner: "the approval flow was broken and NOT communicated."

## GUARD chat (honesty / voice)
- **G1 — claim-checker shield too loose → false owner-facing claim shipped.** Issue: Maelle told the OWNER "Already flagged it to Simon…" — never sent. The checker FLAGGED it, the false-positive shield SKIPPED the rewrite. Root: shield decides a claim is "backed" by regex-matching a tool NAME on the turn tape with no outcome/content check (`postReply.ts:569-604`): `resolve_approval` unconditionally backs any `message` claim (`:579`); `message_colleague` matches even when the tape says `skipped` (`:571`, vs the correct `\bOK\b` gate at `:285`). Plan: make it outcome+content aware — exclude `skipped`/`FAILED`; don't let `resolve_approval` back a claim asserting specific un-sent content; better, feed the checker the actual relay text so "claimed ≠ sent" reads as `claim_specifics_mismatch` (already bypasses the shield at `:634`). Risk: tightening re-opens the false-positive class the shield exists for (mitigated by the rewriter's Sonnet veto + fail-open). Owner: "handoff to guard to help us be better" (explicitly SECONDARY to approval/slots).
- **G2 — humanGate gap: image-handling self-talk.** Issue: "Under a bit of doubt here, I don't have the actual image content, just the gist" shipped raw. Root: humanGate (`utils/humanGate.ts`) scopes to self-as-infrastructure + mechanical-refusal only; "I only have the gist / I don't have the actual image" matches nothing → passed. Plan: extend leak detection to image-handling self-talk (weaker); the ROOT fix is C1 (re-attach the image so there's nothing to editorialize).

## PROMPT chat (routing / prompt judgment)
- **P1 — `isBriefRequest` misroutes a CONFIG message.** Issue: "for my morning brief, can we include Reflectiz?" tripped `isBriefRequest` → short-circuit to `sendMorningBriefing` → never reached the orchestrator → `update_my_preferences` never fired → not saved. Root: the Haiku judge (`briefIntent.ts:32`) distinguishes "brief me on X" / "briefing TIME" but has no case for "CHANGE what the brief COVERS." Plan: sharpen the judge — `is_brief_request` true only for "send/receive the brief now," false for "change/add/cover/track/stop X." (LLM judgment, not regex — multilingual.) **Status: DEFERRED by owner ("lets not fix A now").**
- **P2 — `update_my_preferences` wrong skill bucket.** Issue: Reflectiz saved under `skill:'brief'` (`created:false`), not `skill:'news'` → invisible to the news planner; then a false "saved, will show in news" claim. Root: the model picked the skill category from surface words ("brief"). Plan: constrain/validate the skill arg against the known set + steer a news-config ask to `skill='news'`. (Related to P1 — the word "brief" poisons both.)
- **P3 — owner-path "propose times, never bounce" is missing (shared with MEETING).** Issue: owner relayed Gidon's "when are you free?"; Maelle echoed it back instead of proposing the owner's open times. Root: STRUCTURAL — the coordinate/propose rule lives only in the COLLEAGUE branch (`systemPrompt.ts:345-360`); the owner branch is one line ("follow their instructions", `:341-342`); `availabilityPreCheck.ts` is colleague-only. classify was correct (meetings+calendar, `find_available_slots` shipped) — the model just didn't call it (it bundled the Zoom-vs-in-person owner-decision with the timing question and bounced both). Plan: **Option 1 (preferred)** add an owner-path availability pre-compute mirroring `availabilityPreCheck` (extend past its colleague-only gate): meetings+calendar + `meetingPeople` non-empty + an availability question → pre-run `find_available_slots` for the parsed window and inject concrete slots. **Option 2 (weaker)** owner-branch prompt rule. Risk: false triggers (bounded — pre-compute only adds context; fail-open). Owner flagged the Gidon bounce.

## EFFICIENCY chat (already has a kickoff prompt, delivered in chat)
- **E1 — ~8 model calls/turn.** Partly fixed in 4.0.2 (guards parallelized 3→1 wall-clock; humanGate forced-tool kills the reparse; close_loop→Haiku). Remaining: condition/skip passes (date-verify only when a date is present; concision only above a length; close_loop only when a thread has an open request), and consider merging the post-reply guards into one combined Haiku call. Full kickoff already written.

## THIS CHAT (general) / core ingestion
- **C1 — owner's own image lost across turns (NOT #144).** Issue: an image the owner uploads is only seen by the model on the arrival turn; every follow-up sees just a lossy one-line gist → "I don't have the actual image content." Root: bytes attached only on the arrival turn, never persisted; carry-forward is a Haiku one-liner that dropped Gidon's name/words/topic. Files: `vision/index.ts:118-157`, `processMessage.ts:226-237`, `buildTurnContext.ts:264-281`. Plan: **Option A (best)** on a follow-up turn that references the image, re-download the persisted `url_private` (stored, bot-token-valid) and re-attach the real bytes (gated to recent image + short window, fail-open to gist). **Option B (cheap)** make the carried description a verbatim transcript for text-heavy screenshots. Risk: image tokens/latency (cap to most-recent); injection surface (scope owner/trusted — colleague-image guard already exists). Owner: "picture didn't work again." Distinct from #144 (that's forwarding a COLLEAGUE's image to the owner) — arguably worse.
- **C2 — Reflectiz data fix (quick).** Save Reflectiz to `skill='news'` now to unblock (independent of the P1/P2 code fixes). Owner wanted it saved; it "didn't save in the end."
- **C3 — deploy + watch.** 4.0.1 (Sonnet-5 retry live) and 4.0.2 (thinking-per-surface + guard-parallelize) — deploy when ready; watch truncation, the forced-tool path, tool-reaching, and the guard-parallelize coverage.
- **[FIXED, shipped in 4.0.2] Brief news omit + truncation** — brief → adaptive `medium` + max_tokens 6000/4000 + `content.find` extraction fix.

## Known / deferred
- GitHub **#144** — Forward colleague images to owner (Improvement/Medium). Distinct from C1.
- No open `Bug`-labeled GitHub issues; today's wave is all unfiled/new.
