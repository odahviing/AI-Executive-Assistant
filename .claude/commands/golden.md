---
description: Run the golden-path battery (.claude/GOLDEN_PATHS.md) in full — 30 fixed paper-traces, one verdict each
---

Run the `golden` skill: dispatch a general-purpose Sonnet agent to read `.claude/GOLDEN_PATHS.md` and run every item per that file's own header, then report the table. Rules live in the battery file, nowhere else.
