{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"gatekeeper","finding":"H1 checkSlot occupancy masked by early-return rules (verify pass, findings-only)","verdict":"audit","note":"5 HIGH + 2 MED found; rule 0b at ladder position 2 returned level:undefined for the whole lead-time window (owner 1h / colleague 4h)"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"H1 hoist occupancy scan above the rule ladder; level required; owner_busy_collision to position 2","verdict":"built","note":"chose shape (B) over (A) to avoid a cross-lane dependency on guard's availabilityPreCheck; new shared occupancyRoleOf collapses 3 skip-set copies; verdict-preserving"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"H2 alternatives search offered slots sooner than the one refused for being too soon","verdict":"built","note":"planMeeting base carried no minBufferHours/category/excludeEventIds"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"H4 join tool claimed 'calendar is clear' over an all-day OOF","verdict":"built","note":"closed by H1; ownerIsBusy rekeyed on overCommitment; directConflicts retained for the partial-join carve"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"M1 rule 7 counted free-shows, lunch and blocks as travel-buffer collisions","verdict":"built","note":"4.2.0 regression - owner-side padding moved out of the walker, which had already filtered them"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"M2 moveMeeting inline label switch never got the six new labels","verdict":"built","note":"last of FIVE copies, not two of three as relayed"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"L dead minBufferHours arg in the relaxed recovery search + false comment","verdict":"built","note":"set-but-unread, Shared rule 5"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"L checkSlot labels third-person to the owner","verdict":"built","note":"fixed all five via existing viewer rather than only rule 0b"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"gatekeeper","finding":"H3 gap probe used a zero-width search window - every gap query answered 'nothing bookable there'","verdict":"built","note":"ONLY finding with runtime tape: 20 getFreeBusy zero-window lines across 07-20/23/24 = 5 real gap questions. Retired the caller (last user of the narrow-window path) rather than repairing the window"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"gatekeeper","finding":"adversarial verify of the combined round-1 diff","verdict":"audit","note":"could not refute the core; reorder proven verdict-preserving; found F1 HIGH + F3 regression + F2 MED"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"registrar","finding":"F1 owner approval DM always dropped its hard-conflict lead","verdict":"built","note":"dmText assembled twice; rebuild discarded honestHardReason. Replaced the branch with one ordered filter(Boolean).join. Corrected the brief: consequence is truthy 100% of the time via the default arm, and F1 bites the create path only"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"registrar","finding":"re-ask revival posts bare askText on a resolving decision surface","verdict":"needs-owner-decision","note":"DECLINED to build - clean fix needs details.honest_hard_reason persisted; the shortcut would widen a 6-reader field with a private meeting subject (Shared rule 10)","ref":"re-ask-revival-posts-bare-asktext"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"F3 alternatives returned zero options on Thursday requests","verdict":"built","note":"regression introduced by round 1 - autoExpand:false + real day bounds removed the widening the old zero-width window relied on. Replaced two hand-sliced searches with one search + pickSpreadSlots, forward reach counted in workdays"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"F2 double-book notice suppressed on the relaxed off-day path","verdict":"built","note":"rule 1 is the only ungated rule, so passes:false killed the notice; soft tier announced on failures while the hard tier was not"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"F7 rule 7 docstring claimed a benefit the code does not deliver","verdict":"built","note":"comments only; narrowed my claim - the isAllDay skip is nearly inert, still guards a neighbouring day's all-day pulled into the buffer window near midnight"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"F8 join tool's directConflicts || operand dead, justifying comment false","verdict":"built","note":"occupancy is computed above rule 0, so overCommitment rides in_the_past too"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"F10 renderWeDualClock hardcoded second person in a colleague-facing ask on trip days","verdict":"built","note":"ownerName already existed and was never passed; whenText now reads input.viewer"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"F2b rule 1 has no allowRelaxed gate - the lone exception in the ladder","verdict":"needs-owner-decision","note":"real bite is owner-in-MPIM: gateRelaxed returns relaxed:true while the clamp keeps initiator colleague. Security seam the owner reserved","ref":"f2b-rule-1-has-no-allowrelaxed"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"gatekeeper","finding":"targeted verify of round-2 fixes","verdict":"audit","note":"primary concern (ISO-week fetch across a boundary) settled CLEAN - startOf(first)/endOf(last) is a strict superset. Found 2 MED + 2 LOW, none blocking"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"D3 requested day wins - anchorDay was day-diversity round-robin","verdict":"built","note":"owner decision. New opt-in anchorMode on the one spreader, not a fork. Split lives in the data (three keys), not the wording"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"D4 Graph outage fails open - search validated against an empty calendar","verdict":"built","note":"owner decision: refuse with 'Idan's calendar is offline'. Builder caught that my brief's fix would not have fired in the most likely outage - getFreeBusy throws first"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"registrar","finding":"D6 re-ask revival posts bare askText on a resolving surface","verdict":"built","note":"owner approved. One shared composer; conflict replayed time-stamped, suppressed under a counter. Caught that my brief would have promised the original slot while the tick booked the counter's"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"round-3 regression - every throwable wrapped as 'calendar offline', colleague-reachable via a sub-6-min candidate window","verdict":"built","note":"one isOutageShaped taxonomy; model's candidate end removed from the schema entirely as incoherent with how the verdict is computed"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"D5 priority of reasons - soft label won over a real conflict, then a hint denied real meetings existed","verdict":"built","note":"owner decision. DELETED the walker's lead-time pre-filter rather than adding a second ranking. Verdict-identical for all 13 callers"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"D8 all-day OOO - day-quality read a vacation day as fully free","verdict":"built","note":"owner decision. Booking was ALREADY blocked by round 1; this was narration. My suspicion was backwards - read as free, not packed"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"D1 group-DM override silently waived eight rules; ownerProposedSlot authorized on message content","verdict":"built","note":"SECURITY. Authorization on a claim in a message, not the authenticated sender (rule 10), via regex over an English/Hebrew phrase list (rule 4). Deleted 107 lines. allowRelaxed now implies owner"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"matchmaker","finding":"D2 offered alternatives never recorded","verdict":"built","note":"owner approved. Both lists to the existing per-turn stash. Removed a competing slice(0,6) that would have truncated exactly the widened list"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"outrider","finding":"OOO day rendered as 0m free in the owner's free-time block, reading as packed solid","verdict":"built","note":"needs-dependency chained from meeting. Kept the meeting count - an OOO day can still carry real bookings"}
{"date":"2026-07-26","runId":"manual-checkslot-verify","lane":"gatekeeper","finding":"final adversarial verify before wrap","verdict":"audit","note":"WRAP. Every destructive path fails closed. 8 residuals parked per owner's stop-scope call. Notable: isOutageShaped is blind to transport faults (SDK erases the cause chain, statusCode -1) and moveMeeting:1094 still sets relaxed raw on a colleague-allowed tool"}
{"date":"2026-07-26","runId":"manual-replypath-audit","lane":"gatekeeper","finding":"targeted audit of postReply + runOutputGates (findings-only)","verdict":"audit","note":"3 HIGH + 3 MED. Corrected the premise: the 989-348 restructure was 4.1.0 (6fe4251), not 34ee3e7 - so this path had NEVER had a scoped pass. The 640 deleted lines are a MOVE into runOutputGates, not a loss. Verdict: 1:1 DMs safe, group DMs not"}
{"date":"2026-07-26","runId":"manual-replypath-audit","lane":"gatekeeper","finding":"H1 in a group DM a colleague-readable reply is gated as owner-facing - securityGate skipped entirely, humanGate in the wrong audience frame","verdict":"built","note":"SECURITY. 55 real turns with isOwnerInGroup:true. Agent corrected my diagnosis: the two conditions were exact De Morgan complements, never drift - ONE predicate was answering two questions (is the owner acting / can only the owner read this). Split into two axes. Kept claimChecker with code-level proof: its v1.7.5 MPIM branch is reachable ONLY from this path, so a straight re-route would have made a deliberate capability dead code"}
{"date":"2026-07-26","runId":"manual-replypath-audit","lane":"slacker","finding":"H2 an exception on the reply path produces total silence; the overload fallback is unreachable","verdict":"built","note":"postOrchestratorReply runs inside the queue-runner closure, so processMessage's catch never fires. Real tape 07-20: a 529, logged warn, user got nothing - and catch-up SKIPPED it (markProcessed already claimed), so it was never recovered. Wider than briefed: the throw was in runOrchestrator, not postReply. Added onDelivered so a post-delivery failure stays silent; queue backstop warn->error"}
{"date":"2026-07-26","runId":"manual-framework","lane":"outrider","finding":"framework: verify discipline into code, feature.js door, dependency close-out, ledger reader","verdict":"built","note":"infrastructure. Per-fix verify replaced by one combined-diff pass (dearer AND blinder). Dependency close-out was a correctness hole - the originator was never told its dependency landed. Ledger reader caught itself lying on first run: guard read as ungoverned when all its rows were findings-only verify passes"}
{"date":"2026-07-26","runId":"manual-framework","lane":"outrider","finding":"framework: Locate pass + traced/priorClean forwarding","verdict":"built","note":"owner's principle: move data to the agent, never re-derive. Locate resolves cited file:line once per run instead of once per lane. Builder's paper-trace now reaches the verify so it attacks gaps; verify's clean list persists to state.json and returns as priorClean. Owner REJECTED per-issue effort grading - effort is a property of the AREA's complexity, not the task's size, which is why it is per-lane"}
{"date":"2026-07-26","runId":"wf_6b869440-ef7","lane":"matchmaker","ref":"gh#147","finding":"B1 delete/decline relay asserted a notification instead of reporting one; false retraction to the owner","rootCause":"src/skills/meetings/ops/handlers/calendarReads.ts:720 + src/connectors/graph/calendarMutations.ts:138","verdict":"built","state":"built","note":"relayStatus='sent' set before the fire-and-forget setImmediate DM ran and never read SendResult; /cancel is organizer-only so EVERY attendee-side delete fell back to bare DELETE (17 log lines) and Outlook told nobody. New declineMeeting() POST /decline {SendResponse:true}; notified_via/notified_who derived from the call that ran. NOT TRACED: /decline never exercised against a live tenant - watch 'declineMeeting - /decline rejected' on the first live attendee cancel"}
{"date":"2026-07-26","runId":"wf_6b869440-ef7","lane":"matchmaker","ref":"gh#147","finding":"B2 eleven Slack DMs to Julia for one 'decline the standups' instruction","rootCause":"src/skills/meetings/ops/handlers/calendarReads.ts:719-732","verdict":"built","state":"built","note":"same root as B1/B3 - one relay layer guessing at delivery, recipient and scope. Owner's stated premise ('he will get outlook cancel email') was FALSE in code, so deleting the DM alone would have created a silent no-notify"}
{"date":"2026-07-26","runId":"wf_6b869440-ef7","lane":"matchmaker","ref":"gh#147","finding":"B3 decline DM said 'anymore' for a two-date decline, reading as permanent","rootCause":"src/skills/meetings/planMeeting.ts:286","verdict":"built","state":"built","note":"suggestedDmText was hardcoded and scope-blind, generated per occurrence but worded as permanent - no wording could have been correct. Whole DM removed; Outlook's decline is per-occurrence by construction"}
{"date":"2026-07-26","runId":"wf_6b869440-ef7","lane":"matchmaker","ref":"gh#147","finding":"B4 'All 11 standups declined' when 12 were cancelled - Aug 27 never mentioned, dates reconstructed by the model","rootCause":"src/utils/threadEventLedger.ts:49 + src/core/orchestrator/index.ts:785","verdict":"built","state":"built","note":"forgetThreadEvent pruned only the created/edited ledger not viewedLedger; orchestrator read input.event_id ?? .id while the schema declares meeting_id, so the prune AND the double-delete guard have NEVER fired. cancelled_label now Graph-derived, owner-local; stale id exits as event_not_found"}
{"date":"2026-07-26","runId":"wf_6b869440-ef7","lane":"outrider","ref":"gh#148","finding":"B5 every routine thread starts with zero conversation history, so calendar health could not see its own question","rootCause":"src/tasks/dispatchers/routine.ts:234-267 + src/tasks/briefs.ts:806","verdict":"built","state":"built","note":"needs-dependency chained from meeting. New recordAssistantThreadPost on the existing conversations spine, keyed on the DELIVERED ts so both fallback send paths are correct; bracketed marker row prevents an assistant-first history. Wider than #148 - affects every routine that asks the owner anything. Cost delta ~2 Haiku calls per ignored firing"}
{"date":"2026-07-26","runId":"wf_6b869440-ef7","lane":"matchmaker","ref":"gh#148","finding":"N1 missing_category was the one issue class never written to calendar_issues, so an answered category question had no state","rootCause":"src/skills/calendarHealth/handlers/checkHealth.ts:1298","verdict":"needs-owner-decision","state":"built","note":"OVERTURNED by wave-verify: tracking the class means a SETTLED category question would silence later REAL double-bookings on the same event (getSuppressedEventIds is event-id-only and terminal-only) - a missed conflict, and invisible. Owner decision: should a settled category question silence a different later issue on the same event? Recommended NO; minimal fix is class-aware suppression in src/db/calendarIssues.ts, precedent at :456"}
{"date":"2026-07-26","runId":"wf_6b869440-ef7","lane":"gatekeeper","ref":"wave-verify","finding":"combined-diff adversarial verify of the 14-file wave","verdict":"audit","state":"built","note":"Overturned N1 (see above). Everything else held: typecheck green on all 14 files, delete_meeting still yields mutated=book so the claim-checker shield covers a cancel narration, event_not_found can never read as a cancellation, no circular import from calendarMutations importing verifyEventDeleted, removed relay surface fully dead. 10 new verifiedClean entries persisted to state.json"}
{"date":"2026-07-26","runId":"wf_6b869440-ef7","lane":"outrider","ref":"bugger-depask-dropped","finding":"ENGINE GAP: three lanes returned built + a dependencyAgent/dependencyAsk and none was dispatched","rootCause":".claude/workflows/bugger.js depAsksFor","verdict":"needs-owner-decision","state":"open","note":"depAsksFor filters verdict==='needs-dependency' only, so a built+ask verdict has nowhere to go. Dropped this run: ->guard and ->outer (turnHelpers.ts:290 delete_meeting summary carries no date/notification field) and ->context (buildTurnContext.ts:754 RECENT CALENDAR ISSUES guidance, which materially strengthens N1). Recorded in report.md so they are not lost"}
{"date":"2026-07-26","runId":"wf_40d78c7e-80b","lane":"matchmaker","ref":"gh#148","finding":"B6 closes N1 - missing_category untracked AND the cluster model conflates a metadata question with a day-shape complaint","rootCause":"src/skills/calendarHealth/handlers/checkHealth.ts:1317 + src/db/calendarIssues.ts:204/:324/:478","verdict":"built","state":"built","note":"Closes the N1 needs-owner-decision from wf_6b869440-ef7 in the recommended direction (axis-aware), and extends it: found THIS dispatch that an uncategorized event which also clashes merged into one overlap-anchored row, so tracking the class alone still left #148 reproducing. ONE axis concept now governs clustering, row identity, suppression and the cascade. Runtime-verified read-only on the live DB. NOT TRACED: no live end-to-end run - a missing_category row has never existed in production"}
{"date":"2026-07-26","runId":"wf_40d78c7e-80b","lane":"instructor","ref":"N2","finding":"RECENT CALENDAR ISSUES guidance had no route from a category-naming reply to set_event_category","rootCause":"src/core/orchestrator/buildTurnContext.ts:763","verdict":"needs-owner-decision","state":"built","note":"BUILT (+10/-1, +97 uncached tokens, owner-path only, only when a live row exists) then overturned by wave-verify to an owner ship/hold call. Residuals: (i) tool availability rests on a Haiku scope guess that has failed on this exact word - failure is a MISS not a wrong action; (ii) a category confirmation is the one promoted mutation claim with no deterministic honesty backstop, so a false positive could rewrite a TRUE claim into a retraction. No leak, no destructive write. Verify recommends landing two mechanical turnHelpers one-liners first"}
{"date":"2026-07-26","runId":"wf_40d78c7e-80b","lane":"matchmaker","ref":"gh#147","finding":"re-dispatched because the prior run's work is uncommitted","verdict":"already-fixed","state":"built","note":"Reappearance check per shared rule 6, verified against the code on disk not the report. Cannot reproduce: no Slack send remains on any cancel path (grep-proved) and closeMeetingArtifacts gates its DM on positiveBooking which excludes 'deleted', so the payload's 'I did NOT send any Slack message' is TRUE. Cost note: a full lane dispatch was spent to reach already-fixed"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"outrider","ref":"B5","finding":"routine/brief threads had zero conversation history - REBUILT after the owner challenged the design","rootCause":"src/tasks/dispatchers/routine.ts:234-267 + src/tasks/briefs.ts:806","verdict":"built","state":"built","note":"OWNER WAS RIGHT. Previous build added a new verb (recordAssistantThreadPost); agent DELETED it (-44) and conversations.ts is byte-identical to HEAD again. One primitive appendToConversation, called the same way the interactive path calls it. The 'no inbound message' asymmetry is not real - 8 existing unprompted-speech sites already write one assistant row and no user row. The assistant-first API assumption is DISPROVEN by runtime data (2 live DB threads genuinely start with an assistant row and were answered; 28 have assistant-first heads; zero role-validation errors in logs). The marker row was actively HARMFUL: capturePass renders any non-assistant row as the human speaking, so '[Scheduled routine: ...]' was fed to Haiku as Idan's own utterance. Also closed a second gap: the on-demand brief short-circuits before postReply, so the ask was stored and the answer never was. 19 scenarios traced, typecheck green. NOT runtime-proven: that it yields historyLength:1 after deploy"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"profiler","ref":"CF1","finding":"md fold ran after the row transaction committed, so an interrupted merge orphaned a person md file the sweep never revisits","rootCause":"src/db/people.ts (md fold after apply.immediate())","verdict":"built","state":"built","note":"Manager's premise FALSIFIED by the agent: the orphan does NOT exist - log 2026-07-25:157 shows the fold completed 2ms after the row merge, 72 md files and no mq97. Latent, not already-happened; no data cleanup, nothing under data/ or config/ mutated. Also corrected the briefed fix: files and SQLite are separate durability domains so the halves can NEVER be one atomic commit - the only choosable thing is which side residue lands on, and row-first picked the unrecoverable side. Fix INVERTS the order (fold before commit) and mergePersonMdFiles now returns a boolean asserting the postcondition ON DISK via existsSync, deferring the DB write if incomplete. Every failure now leaves the pair still duplicated = the state the boot sweep already retries, and the fold is idempotent. 11/11 traced"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"profiler","ref":"CF4","finding":"gender_confirmed was a second authority signal the provenance chain never consulted - and the documented no-override invariant was NOT actually enforced","rootCause":"src/db/people.ts:520-527 (confirmPersonGenderById raw UPDATE) + :354 (setCoreFieldWithProvenanceById short-circuit on NULL currentSetBy)","verdict":"built","state":"built","note":"Deeper than briefed and NOT purely latent: genderDetect.ts:95 documents 'NEVER overrides a gender_confirmed=1 row, enforced in people.ts' - it was not. With currentSetBy NULL the guard short-circuited so an auto detection could overwrite a CONFIRMED gender. Fix: one genderRank(setBy, confirmed) where a confirmation floors the rank at person; confirmPersonGenderById delegates instead of raw UPDATE so the split cannot be re-minted; zero-caller confirmPersonGender wrapper deleted. After any merge confirmed=1 <=> set_by in {owner,person} by construction. 20/20 traced. Touched src/core/assistant.ts for the confirm_gender tool"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"profiler","ref":"CF2","finding":"OWNER CORRECTION: CF2 is NOT done - the migration still re-dumps a backup JSON every boot when a group exists, with no guard for a permanently-refused group","rootCause":"src/db/migrations/v4_0_4_dedupe_people_email.ts:55-78","verdict":"needs-owner-decision","state":"open","note":"Owner said 'was already done, no need' - not built and not fixed. It is simply not REACHABLE today because no duplicate-email group exists: exactly one v4_0_4 backup file on disk and 12 'Database initialized' boots today with zero 'backed up affected rows' lines, so the clean-table early return works and Luke's group is gone. Latent-unbuilt; harmless until two Slack accounts claim one email. Deliberately NOT built per the owner's decline"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"registrar","ref":"P1","finding":"a colleague's subject-only counter withheld the hard conflict reason, so a tick booked over an existing meeting with the clash never named","rootCause":"src/core/requests/resolver.ts:1057 + src/core/approvals/approvalCallbacks.ts:230","verdict":"built","state":"built","note":"TWO halves both required - the Manager briefed only the first. (1) resolver:1057 composed its own text; (2) approvalCallbacks:230 did `countered ? undefined : honest_hard_reason`, and a subject-only counter merges into no time field so the tick books the ORIGINAL slot - the proven collision still described what he authorized and was withheld anyway. Fixing (1) alone would still have shipped the bug. Reason gate now slot-based not counter-based. M6 ANSWERED and grep-enforceable: terminalDmMsgTs has exactly 3 writers, all now call composeOwnerAskText - a surface that does not stamp that column is not a decision surface. Resolver net -4 lines. Bonus: a colleague REJECT bounce-back now carries a consequence for the first time. Code-only evidence"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"registrar","ref":"gh#44","finding":"approval midpoint nag had no work-hours clamp - a Thursday approval pinged on Saturday","rootCause":"src/tasks/skill.ts:1029-1031 (raw wall-clock midpoint over a workday-aware expiry)","verdict":"built","state":"built","note":"Arithmetic proof of the exact symptom from idan.yaml (no Fri/Sat hours): Thu 16:00 raise -> addWorkdays(+2) skips Fri/Sat -> Mon 16:00 -> midpoint = Sat 16:00. Clamped at SEND (runner.ts:273-292) not at raise, because runApprovalReminder is the only emitter and a per-date owner_schedule_overrides row added after the raise invalidates a raise-time clamp but never a fire-time one. Rides the existing workTimeBaseFromNow / isWithinOwnerWorkHours / getEffectiveWorkDay trio - no new accessor. R4 preserved: only the nudge defers, expiry never delayed. 12 scenarios. Code-only (0 hits for the nag text in retained logs)"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"registrar","ref":"gh#41","finding":"the parallel work-item lifecycle is outreach_jobs.status; owner ruled ONE spine","rootCause":"src/db/jobs.ts:12 + src/core/requests/closeRequest.ts:118","verdict":"needs-dependency","state":"built","note":"Owner ruling: 'only one spine'. Structural cause: createOutreachJob hands a fire-and-forget send a request that is RESOLVED AT BIRTH, so closeRequest never runs and the cascade never fires - the column can never express a lifecycle it is never told about. Manager-verified live: 12 rows status=sent with request resolved+closed_at NULL, 77 orphans with request_id NULL (agent said 86 - overstated), 0 rows with an actually-open request. Built its half: two spine-backed readers, duplicate filters collapsed, column down to ONE reader (not its own). STOPPED at a real seam: checkHealth.ts:862-865 uses intent=meeting_reschedule AND status=sent at INVERTED polarity (a match SUPPRESSES the overlap autofix) - stopping the cascade suppresses that autofix permanently, dropping the column makes the probe fail OPEN = possible double-ping. Retirement order: 1 meeting swaps the probe, 2 slack deletes the status=replied write, 3 requests deletes the cascade, 4 outer drops the column"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"registrar","ref":"outreach-task-third-spine","finding":"a THIRD work-item lifecycle - message_colleague mints a tasks row with its own status enum, own due_at and own sweep, and DISPATCHERS has no outreach entry so the sweep marks it failed","rootCause":"src/skills/outreach.ts:252 + :291; src/tasks/dispatchers/index.ts:25-31","verdict":"needs-owner-decision","state":"open","note":"NOT built. Manager-verified live: 8 tasks rows at type=outreach status=failed (plus completed 36, informed 14, cancelled 2). Its stated purpose is FALSE - the comment says 'so it shows up in get_my_tasks' but get_my_tasks reads getOpenRequestsForOwner i.e. the spine. THE TRAP: the obvious minimal fix (drop due_at, the only field the sweep reads) would REMOVE the accidental closer - that bogus 'failed' write is what currently takes these rows out of getActiveJobsForThread's open set, so removing it leaves pending_colleague rows rendering in the owner prompt forever. Correct fix is deleting the row, which first needs a home for the tick-on-last-message react fired via fireCompletionReact (unexported hook, USER-VISIBLE behaviour) - a design call plus coordinator.ts (slack) readers. Wants its own dispatch"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"matchmaker","ref":"B6","finding":"OWNER CHALLENGE SETTLED - missing_category flagging is 3 months old but the calendar_issues row never existed; NOT a regression","rootCause":"src/skills/calendarHealth/handlers/checkHealth.ts (write gate excluded the class from the initial commit onward)","verdict":"built","state":"built","note":"Manager-verified from git: exactly 5 commits ever touch missing_category; at 84d399c (initial commit, v1.7.0) it is already the type + detector in calendarHealth.ts; `git log -S missing_category -- src/db/calendarIssues.ts` returns NOTHING. The write gate was type==double_booking||oof_conflict from 84d399c through ef18d1b^, and the 3.0.3 redesign carried the exclusion forward EXPLICITLY as `if (!cls) continue; // missing_category / unknown - not tracked`. So BOTH are true: the owner is right that flagging is long-standing (since v1.7.0, plus the 2.1.1 auto-tagger), and no commit broke it because there was never a working state. IMPORTANT CORRECTION to the Manager's earlier reasoning: N2 (the context guidance) is LOAD-BEARING, not belt-and-braces - at 10:39:22Z the owner retried with historyLength:2 (history present) and there were STILL zero tool calls. Fix shape unchanged; two comment claims upgraded from snapshot to structural"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"matchmaker","ref":"P22","finding":"colleague-path move_meeting set the relaxed override raw - and the grant was never a chokepoint, so six copies of one authorization decision existed","rootCause":"src/skills/meetings/ops/handlers/moveMeeting.ts:1092","verdict":"built","state":"built","note":"Contradicted two written contracts (scheduleRules.ts:56 and planMeeting.ts:112). Inert today - the colleague gate validates the destination strictly and returns needs_owner_approval first - but one edit from waiving rules 0b/2-4/5/6/7/8/9 on colleague authority. DEEPER root: gateRelaxed was private to normalizeBookingRequest so five more sites re-derived it inline, and all five were provably the SAME predicate under three spellings. Fix: exported grantRelaxed(args, context) as the one grant. Owner-in-MPIM move now denied, matching what D1 did to create (M2). Deleted an unreachable deferred_replay grant + an unproducible relaxedReason variant. 13 scenarios. Flagged not built: createMeeting.ts:933 reads raw args.relaxed to suppress the dense-packing counter-offer (grants nothing)"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"matchmaker","ref":"P13","finding":"an already-elapsed time was sold to a colleague as a day-load policy exclusion, with an invitation to ask the owner to override it","rootCause":"src/connectors/graph/findAvailableSlots.ts:667 (in_the_past -> within_lead_time) + no universal past floor","verdict":"built","state":"built","note":"Runtime shape from logs/maelle-2026-07-20.log:197 - within_lead_time:65 with rejectedExamples at 00:00/00:15/00:30/00:45/01:00, i.e. the elapsed morning PLUS the genuine 4h lead band collapsed under one soft label, so colleagues were invited to ask the owner to override MIDNIGHT. Fix: a universal per-slot past floor above every other reason, and in_the_past maps to itself. Deliberately per-slot rather than a search_from clamp: a fully-elapsed window would otherwise produce ZERO rejections and the single-window callers read Object.keys(rejectedCounts)[0], which humanizes an empty map to 'unknown' - the mechanical non-answer M11 forbids. 13 scenarios"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"matchmaker","ref":"P15","finding":"three getFreeBusy fail-opens returned {} - the wire shape of 'nobody is busy'. The owner overruled the earlier deliberately-not-fixed and the evidence changed the fix","rootCause":"src/connectors/graph/calendarReads.ts:609 / :616 / :707","verdict":"built","state":"built","note":"The old framing ('malformed-request classes') was WRONG. The zero-window branch fired 4x in ONE turn and 8x that day, every hit with startDate===endDate, and the producer is availabilityPreCheck normalizing a colleague's Hebrew 'אחרי 17:00 יש משהו' into a single INSTANT with no end - a well-formed question wearing a malformed window, needing an ANSWER not a refusal. Fix 1: widen the query to the smallest window Graph accepts, then filter back to blocks that genuinely cover the instant (start<=t<end); retires 100% of observed fail-open traffic and improves the guard lane's behaviour without touching its file. Fix 2: genuinely unanswerable cases still do not throw (a throw IS #137) but report addresses in a new diagnostics.notChecked, kept separate from `unresolved` because unresolved's consumers say 'these addresses do NOT exist in the directory' - about a live mailbox that is the same dishonesty in a smaller font. Every caller wired to an honest notice, none refuses. ALSO fixed unasked: the >62-day recursion dropped forceRefresh AND diagnostics so a wide window reported unresolved permanently empty (the phantom-attendee failure). HAND-OFF to guard: availabilityPreCheck does not request diagnostics so an inverted/unparseable window would still read {} as free - not a blocker, its live case is now answered. Flagged not built: windows of 0-6 min throw on ErrorInvalidMergedFreeBusyInterval which is not caught - fail-CLOSED, no log evidence"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"gatekeeper","ref":"P19","finding":"P19 close-out - the two corrective history appends deleted; the gate stack can no longer write history at all","rootCause":"src/utils/guards/runOutputGates.ts (claim-rewrite append + date-swap append)","verdict":"built","state":"built","note":"Closes P19 jointly with slack's postReply.ts:474 move. Both appends and the appendToConversation import deleted (Manager-verified: 0 occurrences left in the file); recorded as a module invariant. The duplicate-row state was LIVE not theoretical - 5 own-the-miss rewrites on 07-26 and 2 real date swaps in the window, each appending. Fixed 5 stale comments and CORRECTED the Manager: :13-15 was NOT still accurate (it justified runDeliberationGuard with 'runs BEFORE postReply persists the draft', now true of every step). HAND-OFF to slack: postReply.ts:425-431 still describes the colleague gate order as claim->date->security->humanGate, but P20 made it claim->security->humanGate->date"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"gatekeeper","ref":"P15-residual","finding":"the P15 hand-off premise was WRONG - availabilityPreCheck has no getFreeBusy call, so diagnostics.notChecked cannot apply there","verdict":"already-fixed","state":"built","note":"Refuted with grep-verified call-graph facts: availabilityPreCheck uses getCalendarEvents + checkSlot (a pure sync function over input.events); no getFreeBusy in the file in the tree OR in HEAD. The failure mode cannot occur either - getCalendarEvents THROWS rather than returning [] precisely because an empty list is indistinguishable from a free calendar; the throw is caught per pair, the pair is skipped, no verdict is emitted, and buildTurnContext injects nothing. 'No data -> no verdict' is already structural. The live bare-instant case reached getFreeBusy through the old narrow-window findAvailableSlots, which H3 removed - the log string no longer exists in src. BUILT INSTEAD: that catch logged at debug and debug is NOT persisted (0 debug rows vs 1963 info), so the one blind path was invisible to a log review; now a warn naming the error class. Behaviour deliberately unchanged - an unreadable owner calendar already has an owner in D4"}
{"date":"2026-07-26","runId":"backlog","lane":"slacker","ref":"P7","finding":"a Hebrew or Russian speaker hits an error and gets an English apology; cannot use an LLM because on the 529 path the model is what is unavailable","verdict":"needs-owner-decision","state":"open","note":"Owner: don't fix now (2026-07-26). Needs detectMessageLanguage + a static table"}
{"date":"2026-07-26","runId":"backlog","lane":"matchmaker","ref":"P31","finding":"the occupancy scan is now unconditional, roughly 3x scan work on a 21-day search - UNMEASURED","verdict":"needs-owner-decision","state":"open","note":"The only backlog item where nobody knows the answer. Needs a wall-clock delta, not a code read"}
{"date":"2026-07-26","runId":"backlog","lane":"matchmaker","ref":"P23","finding":"the offline retry has no backoff - two calls in the same tick, so a 429 or rolling-restart 503 reads as a full outage more often than needed","verdict":"needs-owner-decision","state":"open"}
{"date":"2026-07-26","runId":"backlog","lane":"matchmaker","ref":"P24","finding":"calendarHealth is not routed through the offline wrapper, so a health check dumps a raw Graph error into her context during an outage","verdict":"needs-owner-decision","state":"open","note":"Fail-closed is right; only the wording is inconsistent"}
{"date":"2026-07-26","runId":"backlog","lane":"matchmaker","ref":"P25","finding":"out-of-hours occupied slots outrank the true in-hours blocker in day narration - they return rule 8 instead of rule 5, escaping the noise filter","verdict":"needs-owner-decision","state":"open"}
{"date":"2026-07-26","runId":"backlog","lane":"matchmaker","ref":"P29","finding":"two copies of the all-day-OOF predicate (scheduleRules.ts:409 and inline at analysis.ts:367) - they agree exactly today","verdict":"needs-owner-decision","state":"open"}
{"date":"2026-07-26","runId":"backlog","lane":"matchmaker","ref":"P30","finding":"analysis.ts:346 - the day-off push path does not carry outOfOfficeAllDay; not live because the renderer skips day-off rows","verdict":"needs-owner-decision","state":"open"}
{"date":"2026-07-26","runId":"backlog","lane":"matchmaker","ref":"P32","finding":"all-day workingElsewhere counts as a hard commitment in one place and explicitly does not in another; zero occurrences in the logs","verdict":"needs-owner-decision","state":"open"}
{"date":"2026-07-26","runId":"backlog","lane":"gatekeeper","ref":"P26","finding":"runOutputGates.ts:35 still states 'Every gate FAILS OPEN' - false on the colleague leg, two unwrapped paths. Comment only","verdict":"needs-owner-decision","state":"open"}
{"date":"2026-07-26","runId":"backlog","lane":"slacker","ref":"P27","finding":"inboundQueue.ts:199-201 says an aborted turn's message is still in pending - it isn't, scheduleRun clears it. Comment only","verdict":"needs-owner-decision","state":"open"}
{"date":"2026-07-26","runId":"backlog","lane":"slacker","ref":"P28","finding":"a third copy of the overload string at handlers.ts:182 - different shape, deliberately not folded into failureReply","verdict":"needs-owner-decision","state":"open"}
{"date":"2026-07-26","runId":"backlog","lane":"slacker","ref":"A1-whatsapp-no-gates","finding":"whatsapp.ts:349 appends the reply raw and never runs the gate stack at all - the P19 divergence plus no gates whatsoever","verdict":"needs-owner-decision","state":"open","note":"Dormant transport, nothing leaks today"}
{"date":"2026-07-26","runId":"backlog","lane":"matchmaker","ref":"A2-short-window-throw","finding":"Graph windows of 0-6 minutes throw on ErrorInvalidMergedFreeBusyInterval which is not caught - fail-CLOSED, no log evidence, no real meeting window is under 10 min","verdict":"needs-owner-decision","state":"open"}
{"date":"2026-07-26","runId":"backlog","lane":"matchmaker","ref":"A3-createmeeting-raw-relaxed","finding":"createMeeting.ts:933 reads raw args.relaxed to suppress the dense-packing counter-offer; grants nothing so P22's invariant holds","verdict":"needs-owner-decision","state":"open","note":"Routing it through the gated value would change colleague-path counter-offer behaviour with no evidence behind it"}
{"date":"2026-07-26","runId":"backlog","lane":"gatekeeper","ref":"A4-lone-anglebracket-id","finding":"an id preceded by a lone < is invisible to BOTH scrubber patterns - pre-existing, outside P3","verdict":"needs-owner-decision","state":"open"}
{"date":"2026-07-26","runId":"backlog","lane":"gatekeeper","ref":"A5-slack-channel-ref","finding":"slack_channel_ref flags a RENDERED channel link - same false-positive class as the retired slack_id_mention","verdict":"needs-owner-decision","state":"open","note":"Kept because a private channel NAME is a real disclosure; its last-resort path now deletes the link instead of shipping it. Retiring vs reclassifying is an owner call"}
{"date":"2026-07-26","runId":"backlog","lane":"profiler","ref":"A6-gender-reconfirm","finding":"re-confirming a gender already stored with the same tag answers higher_authority_already_set - 'refused' for something already true","verdict":"needs-owner-decision","state":"open","note":"Pre-existing, unchanged by CF4"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"matchmaker","ref":"gh#41-step1","finding":"the last reader of outreach_jobs.status swapped to the spine reader; it was permanently suppressing an overlap autofix","rootCause":"src/skills/calendarHealth/handlers/checkHealth.ts:856-879","verdict":"built","state":"built","note":"+14/-5, one hunk. Live before/after: OLD probe matched 1 row, NEW matches 0. That row (out_1783936884258_ueze, Ysrael Gurt 2026-07-13, request_id NULL) can never be closed by closeRequest so it sits at 'sent' forever, and at INVERTED polarity it permanently suppressed the overlap autofix for that BiWeekly occurrence - a stuck-open suppression, notice 13 days stale. So 1->0 is the fix. Duplicate-ping safety RUNTIME-proven from logs/maelle-2026-07-21.log:420-449: during the 7.5-min open window both old probe and new reader are TRUE, both FALSE after - set-equivalent on the real path. Also caught that SQL LIKE treats _ as a wildcard and Graph ids contain _, so the old probe was marginally looser; verified 0 divergent pairs on live data. CORRECTION to the Manager's brief: the '2 rows' figure was the SWEEPS' filter, not this probe's - this probe matched 1. Confirms the probe was the LAST reader, so steps 3 and 4 are unblocked. 10/10 traced"}
{"date":"2026-07-26","runId":"backlog","lane":"registrar","ref":"dup-ping-2026-07-13","finding":"a colleague WAS pinged twice about the same meeting move - closeMeetingArtifacts resolved the outreach 16 minutes after the notice was sent, while the colleague had not replied","rootCause":"src/utils/closeMeetingArtifacts.ts (closes an outreach the instant the move lands)","verdict":"needs-owner-decision","state":"open","note":"PROVEN, not hypothetical: out_1783917319399_5ilp at 04:35 via brief_health_, then out_1783936884258_ueze at 10:01 in an owner thread - same event, same proposed time, same text. The first request was resolved at 04:51:38Z with closure_reason 'meeting_moved', 16 min after the notice and before any reply, so the dedup probe (old AND new) correctly goes false at that moment. Not caused or worsened by the #41 step-1 swap - this is requests-lane semantics: an outreach awaiting a human reply should not be closed just because the calendar write landed"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"slacker","ref":"gh#41-step2","finding":"the status='replied' write deleted; NEAR MISS - the Manager's brief asked the wrong safety question","verdict":"built","state":"built","note":"-62/+30. The brief asked whether any reader of the VALUE 'replied' existed (true, zero) but that was not the safety question: the write's real effect was moving a row OFF 'sent', and checkHealth's probe read ='sent' at INVERTED polarity. Deleting it while the old probe lived would have parked such a row at 'sent' FOREVER (followup_closed_at stamped, so nothing can re-match) = permanent overlap-autofix suppression. Runtime-proven shape on disk: out_1780397715922_h773, await_reply=0, request resolved, status='replied' written by this exact branch; it escaped the old probe only because the model omitted meeting_id from context - luck, not design. The agent was drafting needs-dependency when meeting's step-1 swap landed mid-investigation. MANAGER ERROR: steps 1 and 2 were dispatched in parallel as independent; they were order-dependent. WRAP CONSTRAINT: meeting's checkHealth change and this must land in the SAME commit, or checkHealth first"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"slacker","ref":"P27+P28+gate-order","finding":"three comment/dead-path items cleared, and the code half of P26 proved for guard","verdict":"built","state":"built","note":"Gate-order comment fixed by POINTER to guard's module header, not a duplicated description (duplication is what rotted). CORRECTED ITS OWN P19 text: 'delivers NOTHING' was wrong - a gate throw propagates to the runner catch and the person gets the FAILURE LINE, not silence; what is lost is the answer. P27 proven false as written (scheduleRun clears pending; nothing is lost because every inbound is appended to history at arrival, so a superseded message arrives as the preceding user turn). P28 COUNT CORRECTION: it is the SECOND copy not the third - the string appears exactly twice; folded to one OVERLOAD_REPLY const, byte-identical, and documented why the other four doc-ingestion lines cannot be shared. CROSS-LANE PROOF sent to guard mid-flight: 'Every gate FAILS OPEN' is FALSE on the colleague leg - the ../../db dynamic import and the runSecurityGate chain are the only awaits not inside a try, so a throw costs the ENTIRE answer on the one surface a non-owner reads"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"profiler","ref":"A6","finding":"one boolean meant two opposite things, so a correct re-statement was reported as refused","rootCause":"src/db/people.ts:386 vs :388 (both returned false)","verdict":"built","state":"built","note":"Fixed at the chokepoint with a CoreFieldWrite union (applied|already_set|refused_lower_authority|no_value|no_person); zero rank comparison leaks to callers. FOUND A BUG THAT WOULD HAVE BITTEN THE FIX: upsertPersonMemory's timezoneSetBy param - its only caller also wrote the same field through the provenance helper moments later, so the field was written TWICE per call, and with the richer return the second write would always report already_set, turning a genuine owner update into 'already true'. Param removed. CORRECTED the Manager's brief: name_he did NOT share setCoreFieldWithProvenanceById - it was a byte-identical second copy of the rank chain returning void, now deleted (-33). A third weaker copy in the colleague-self path (re-read + string compare) also deleted. All four fields covered: gender, timezone, state, name_he. No migration needed - the bug mis-REPORTED, never mis-wrote. Reachable today: p_U01K6D19LUW person-set + confirmed. Code+DB proven, no log evidence (confirm_gender absent from retained logs). FLAGGED not built: the render gates exclude only 'auto', so a NULL-tagged gender still steers gendered forms - 13 male + 11 female legacy rows; product call under P12, which the owner declined. Note: npm run lint is DEAD in this repo, no eslint config - typecheck is the only gate"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"registrar","ref":"gh#41-step3","finding":"the cascade block deleted; the column now has zero readers and zero writers","rootCause":"src/core/requests/closeRequest.ts","verdict":"built","state":"built","note":"-60 lines. Independently confirmed zero behavioural readers: the OutreachJob interface has no status member so every SELECT * reader discards it structurally. Own stale notes in db/jobs.ts corrected from ONE READER LEFT to zero. FOR STEP 4: OutreachStatus is NOT unreferenced - still the param type of createOutreachJob/updateOutreachJob where it picks the request birth state; only 4 of 8 union members have producers, making 4 arms dead. Droppable now: the physical column at db/client.ts:100"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"registrar","ref":"dup-ping-2026-07-13","finding":"WORSE than reported - the 04:51 write was the OWNER REVERT, so the recorded outcome resolved/meeting_moved was FALSE and the colleague was never told his time had been undone","rootCause":"src/skills/meetingReschedule.ts (no reply_deadline) + src/utils/closeMeetingArtifacts.ts step 2","verdict":"built","state":"built","note":"Reconstructed from audit_log (the 07-13 log has rotated): 04:35 autofix to Tue 12:45 plus notice 1; 04:44-04:51 owner says Revert / return it for today; 04:51:38 moved BACK to today 13:30 AND request closed resolved/meeting_moved; 10:01 moved to Tue 12:45 again plus byte-identical notice 2. ROOT is upstream of the cascade: reply_deadline is the ONLY thing that arms the outreach_expiry timer and it was never passed - 8 of 9 recent meeting_reschedule rows have it NULL; the expiry tombstone is also gated on owner_dm_channel which createOutreachJob never sets - 13 of 31 NULL. Both halves of R4/R5 were STRUCTURALLY UNREACHABLE for this class, which is why a calendar write looked load-bearing as a closer. BUILT: reply_deadline plus ownerDmChannel now stamped (channel only, never the thread ts - the autofix ownerThreadTs is the pseudo-key brief_health_ownerId); an undelivered DM now cancels the ask. ALSO FIXED: reason updated (venue/subject/category branch, provably cannot change the time) was closing a live colleague ask as resolved/meeting_updated - now skipped; and deleted closed the ask as a SUCCESS - now cancelled"}
{"date":"2026-07-26","runId":"backlog","lane":"registrar","ref":"revert-relay-decision","finding":"OWNER DECISION: when a later calendar write VOIDS a notice a colleague already received, should Maelle DM the correction?","verdict":"needs-owner-decision","state":"open","note":"R4 (whoever waits hears the real outcome) and R5 (never pester) point opposite ways: 07-13 alone would have produced 3 DMs to Ysrael in 5.5 hours. It would be a NEW outbound colleague message class on an autonomous path with no output guard beneath it (R1), so the lane refused to mint it. Recommendation (lane and Manager): YES but only when the new time DIFFERS from what he was told - correcting a false statement Maelle made to a human is not a chase. Needs new start/end passed into closeMeetingArtifacts = needs-dependency on meeting (5 call sites). ALSO GATES a content-level dedup, which is only correct if the revert is NOT relayed"}
{"date":"2026-07-26","runId":"backlog","lane":"matchmaker","ref":"autofix-reapplied-revert","finding":"the autofix re-applied at 10:01 a move the owner had explicitly reverted at 04:51 - the duplicate ping is downstream of an unwanted re-move","verdict":"needs-dependency","state":"open","note":"Neither probe could have helped: at 10:01 the first ask had legitimately closed so both correctly return false. Question for meeting: should an autofix re-propose a move the owner reverted within the same day, and does calendar_dismissed_issues record the revert?"}
{"date":"2026-07-26","runId":"backlog","lane":"registrar","ref":"pseudo-threadts-relays","finding":"seven owner-facing relays post to a pseudo thread ts, so every owner-side confirmation on a reschedule reply for the autofix class would fail to land","rootCause":"src/skills/meetingReschedule.ts:202,236,266,295,326,461,491","verdict":"needs-owner-decision","state":"open","note":"For the autofix class owner_thread_ts is brief_health_ownerId (tasks/briefs.ts:712) and connections/slack/index.ts:86-89 forwards threadTs to chat.postMessage unsanitised - Slack rejects an invalid thread_ts. Code-provable, runtime-UNVERIFIED (the 07-13 log has rotated). An R4 close-loop hole; the lane own new code avoids the pattern"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"gatekeeper","ref":"P26","finding":"the every-gate-fails-open contract was false on the colleague leg - a throw cost the entire answer on the one surface a non-owner reads","rootCause":"src/utils/guards/runOutputGates.ts","verdict":"built","state":"built","note":"Fixed the CODE so the comment becomes true, and split the two paths deliberately: the db read fails OPEN (answer preserved) clearing ALL THREE spoof inputs together because a half-filled set is the DANGEROUS state - detectClaimedEmail would run without the sender verified address and every on-domain email in the thread would read as an identity claim, handing a wrong refusal to a correct reply. The leak gate fails SAFE not open - it deliberately does NOT pass the unvetted draft through (formatForSlack is no substitute; it does not know graph ids, account ids, tz strings or tool names), substitutes a fixed line, logs ERROR with lostDraftPreview, and lets delivery plus history plus remaining gates proceed. Declined to reuse securityGate SAFE_FALLBACK because it lives in the module that just failed to load and promises a follow-up nobody makes. Evidence code plus topology only - the failure has ZERO occurrences in any log. Manager phrasing corrected by slack: the provable claim is every await and rewrite on both legs sits inside a try, not an absolute cannot-throw"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"gatekeeper","ref":"A4","finding":"an id behind a lone angle bracket escaped BOTH scrubber patterns - unwrappable and undetectable","rootCause":"src/utils/textScrubber.ts","verdict":"built","state":"built","note":"The exclusion was answering am-I-inside-a-rendered-mention through a proxy that is wrong exactly when the mention is broken. Fixed in ONE place: a lookahead at RENDERED_MENTION (closing bracket REQUIRED, since Slack prints an unclosed one as literal text) and one shared UNRENDERED_SLACK_ID predicate from which both regexes are built. VERIFIED AT RUNTIME not on paper - compiled the real file and ran an 18-case matrix plus 3 assertion groups, ALL PASS, output byte-identical on all 12 P3-protected forms including both 2026-07-21 drafts, naked 9- and 11-char ids, all-caps prose and Hebrew-with-mention. Same one line also fixed the unclosed mention case. Code-only: zero instances in any log"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"gatekeeper","ref":"A5","finding":"slack_channel_ref RETIRED - it broke its own class contract by deleting the fact the sentence was about","rootCause":"src/utils/securityGate.ts","verdict":"built","state":"built","note":"Reclassifying was already the state of the code (identifier-class since 4.2.x) so the live choice was retire vs narrow. Retired because an identifier is meant to be a token that means NOTHING to the reader, but a rendered link IS a channel name - redactIdentifiers turned Posted-to-#general-with-Alex-tagged into Posted-to-with-Alex-tagged, G6 corruption not a safe miss. humanGate own prompt protects the rendered form verbatim, so two gates in one stack disagreed about one token - exactly what shipped the 2026-07-21 regression. Zero fires in any log against a 1.5-2.8s Sonnet rewrite on every colleague reply naming a channel. No coverage lost (a naked channel id never matched it either, and an id with no name is opaque). ALSO fixed the rewriter prompt that still said never include a raw Slack ID with no carve-out - the surviving half of the 2026-07-21 de-tagging regression"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"slacker","ref":"find-slack-channel-payload","finding":"the payload was ALREADY minimal at the tool boundary; the real finds were two unread fields and a dead duplicate listing path","verdict":"built","state":"built","note":"Honest split answer. Both consumers already mapped to id plus name so there was no leak to close there. FIXED: is_private and is_archived were set-but-never-read (deleted; the rule-10 trap is that an unread field on a model-facing payload invites the next reader to widen it - is_archived was dead anyway since exclude_archived is already passed). BIGGER FIND: coordinator.ts:434 findSlackChannel was a dead SECOND channel-listing path with ZERO callers - a hand-copy of findChannelByName with the same conversations.list and the same private_channel types. Deleted, -23 lines; one listing path now exists behind the Connection where S12 says it belongs. Runtime-proven the tool has NEVER been invoked - the 5 log hits are the tool-catalog array being logged, not calls"}
{"date":"2026-07-26","runId":"backlog","lane":"slacker","ref":"private-channel-name-exposure","finding":"find_slack_channel still searches private_channel, so a private channel NAME can be fuzzy-matched and handed to the model - and on a colleague-readable surface read out to the room","verdict":"needs-owner-decision","state":"closed-owner-declined","note":"OWNER DECISION 2026-07-26: LEAVE IT - capability kept. The only payload-level control is narrowing to public_channel, which removes posting to private channels entirely. A surface-aware version needs surface context plumbed into the Connection tool boundary, which executeToolCall(toolName, args) does not receive - bigger than a payload trim. Exposure requires the owner to ask for a channel by name IN A SHARED ROOM and the match to land on a private channel he did not name; the tool has never fired at all. Recorded as a warning block at findChannelByName so it is found at the code. CAVEAT: that conversations.list returns private channels the bot is a member of is documented Slack behaviour, NOT proven from this repo"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"matchmaker","ref":"P25","finding":"day narration named an out-of-hours slot as the blocker instead of the real in-hours conflict","rootCause":"src/utils/scheduleRules.ts (occupancy hoisted above the soft ladder in af19500) + src/connectors/graph/findAvailableSlots.ts:1232 noise filter keyed on the LABEL","verdict":"built","state":"built","note":"RUNTIME-PROVEN: logs/maelle-2026-07-26.log 06:59:36.883, window 2026-08-30T20:30-23:59 entirely past a 19:00 day end returned outside_owner_work_hours:10 plus owner_busy_collision:2 with BOTH busy examples at 20:30 and 20:45; the filter drops the 10 so top_reasons became owner_busy_collision - he-is-already-busy-then for a window his working day does not cover. Caused by tonight earlier commit af19500. REFUSED the literal fix the item wording suggested (flip rule 5 above rule 8) because it re-opens D5 in reverse: availabilityPreCheck has outside_working_hours in ESCALATABLE and owner_busy_collision not, so a colleague asking for a slot the owner is genuinely committed on would be told it is not a hard conflict and is Idan to override; and tasks/skill.ts:831 keys honest_hard_reason on owner_busy_collision so the owner approval DM would stop naming the double-booking. Both are other lanes files and both correct as written. Instead: the work-band fit is computed ONCE above the ladder, rule 5 becomes the report half only, outsideWorkHours rides every verdict, and only the PER-DAY map files out-of-band rejections under the noise label while rejectedCounts keeps the true per-slot label. NOT COVERED (verifier target): the pre-checkSlot walker passes still land in the day map with their own labels - the same log line shows top_reasons attendee_busy_collision for that same window. Pre-dates 4.2.0, deliberate, not P25"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"matchmaker","ref":"P24","finding":"a raw Graph error reached her context during an outage, and a second causeless voice swallowed every fault","rootCause":"CalendarHealthSkill.executeToolCall had no D4 catch; checkHealth.ts:118-124 swallowed every fault","verdict":"built","state":"built","note":"New src/skills/meetings/calendarOffline.ts holds the refusal BYTE-IDENTICAL to the old inline one (verified with diff); both dispatchers route through it, so D4 rides rather than being duplicated. Fail-closed unchanged as instructed. REAL BUG FOUND UNASKED: floatingBlockOps second planMeeting catch runs with allowRelaxed:false, so swallowing an unreadable calendar BOOKED A BLOCK WITH NO RULE CHECK - now rethrows CalendarOfflineError like the create/move/search handlers already do. Behaviour delta: a deterministic fault (403 consent / 404 mailbox / malformed window) now propagates with its own true reason instead of the causeless string, which is what every other meeting tool does"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"matchmaker","ref":"P29+P30","finding":"two copies of the all-day-OOF predicate; and the day-off push path missing a flag","verdict":"built","state":"built","note":"P29: scheduleRules.ts:475 widened to the three fields it reads (which also removed its as-any casts) and analysis.ts:381 now calls it. Truth table identical. P30: DELIBERATELY NOT CARRIED - carrying it would create exactly the set-but-unread field the item forbids, because the only structured reader continues on dayType day_off and the second consumer already gets isWorkDay false plus dayType day_off. The field exists to correct a WORK day free-time arithmetic and a day-off row has none. Contract documented at analysis.ts:253-268 so nobody fixes it later. No behaviour change"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"matchmaker","ref":"P32","finding":"all-day workingElsewhere counted as a hard commitment in one place and explicitly not in another","rootCause":"src/utils/scheduleRules.ts:496 (occupancyRoleOf) vs findAvailableSlots.ts:265-271 and :408-413","verdict":"built","state":"built","note":"CHOSE: NOT a hard commitment. The walker already said so TWICE while occupancyRoleOf made checkSlot reject all ~40 slots of that day as owner_busy_collision. On the merits the full-day WE travel spine was deleted in 4.0.0 (M14) and away days are per-date overrides (#143), so what remains is a marker for a day he IS working - blocking it refuses a WFH-marked day with you-are-already-busy, a false reason for a total refusal (M11). optional would also be wrong (that tier means a skippable MEETING). Rule 6 was the site the collision had been masking: without the fix every slot on a WE day would have flipped to floating_block_overlap. BEHAVIOURAL DELTA: an all-day-WE day goes from zero offered slots to the day real openings. Zero occurrences in the logs"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"matchmaker","ref":"A2+A3","finding":"an uncaught Graph window error; and the last raw args.relaxed read","verdict":"built","state":"built","note":"A2: both window rejections now share P15 one branch (nothingChecked, no throw, every address in notChecked), naming which code fired. No third path and no attempt to prevent the 400 - Graph minimum availabilityViewInterval is 5 min AND must be under the window, so a 1-5 minute window has no valid interval. Traced for a new fail-open: none, because a window narrower than any allowed duration walks ZERO candidates. A3: createMeeting.ts:955 now reads bookingRequest.relaxed. It granted nothing but it waived an owner PROTECTION on an arg the model fills from message content rather than from the authenticated identity. DELTA: a colleague-initiated create_meeting arriving with a spurious relaxed true now gets the dense-packing counter-offer instead of skipping it - zero occurrences in any log, and it is what the same booking gets today without the flag. The policy_exception replay keeps the suppression because it runs on a synthetic owner context so grantRelaxed grants it"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"outrider","ref":"gh#41-step4","finding":"the outreach_jobs.status column DROPPED and OutreachStatus narrowed to a 4-value OutreachTransition","rootCause":"src/db/client.ts (CREATE TABLE + ALTER TABLE DROP COLUMN) + src/db/jobs.ts","verdict":"built","state":"built","note":"Completes the four-step chain. Did NOT need the leave-it-in-place fallback: SQLite is 3.45.3 and the project already depends on ALTER TABLE DROP COLUMN at client.ts:339. Proven on a THROWAWAY DB in the scratchpad built from the real sqlite_master DDL (never mutated data/maelle.db): drop ok, both indexes survive, rows preserved, second run throws and is caught so it is idempotent, the 17-column INSERT ok, and the stripped UPDATE ok while an unstripped SET status throws - i.e. the strip is now load-bearing and that is documented at the strip. No history kept (115 rows discarded, all written by removed code), matching the approvals DROP TABLE and social_topics precedents. OutreachStatus -> OutreachTransition (sent|pending_scheduled|replied|cancelled); all 19 call sites enumerated and typecheck green is itself the proof no caller passes a removed value; four dead arms deleted. MIGRATION IS ONE-WAY: ADD COLUMN status is not in columnMigrations, so reverting to status-writing code would throw. FLAGGED not built: the reached-out-never-heard-back people-memory line is not written today and never was since expiry became a spine timer - belongs on the spine expiry path (requests lane)"}
{"date":"2026-07-26","runId":"manual-owner-batch","lane":"gatekeeper","ref":"combined-verify","finding":"single combined adversarial verify over the whole 51-file diff before wrap - SAFE TO SHIP","verdict":"audit","state":"built","note":"46 tool uses against a 60 budget. Could not refute any of the eight seams: P19's cross-lane history move (written exactly once, above the ack-return and above sendReply; no third writer on the reply path), P26's two failure modes (after the fail-safe substitution humanGate rewrites one apology into another at worst and date-verify is structurally a no-op; no path ships the draft with the leak gate skipped), #41 all four sub-questions (zero remaining reads/writes; the inverted-polarity probe is strictly NARROWER so it can only suppress less; initSchema safe on both fresh and existing DBs), P25's relabel (the noise fold keys on the validator's own outsideWorkHours fact so an in-hours blocker can never be relabeled, and every single-window caller reads the global rejectedCounts which keeps true labels), P22+A3 (grantRelaxed grants only on senderRole owner; the replaced sites spelled the same conjunction or an exact alias, so no site widened), P15+A2 (parseGraphFreeBusySlot returns offset-carrying ISO so the instant filter resolves absolutely - the server-zone hazard does not exist), CF1+CF4+A6 (the deferral cannot strand the common case; already_set only returns when value equal AND rank not raised), P32 (consistent with every consumer; a genuinely-away day is blocked by a DIFFERENT fact - all-day oof or a per-date override). KEY POSITIVE INTERACTION, only visible to a combined pass: P25's rule-8 promotion and P32's WE demotion are LOAD-BEARING ON EACH OTHER - without P32 the promotion would have turned every all-day-WE day into a hard NOT BOOKABLE on the colleague pre-check instead of an escalatable outside-his-hours. Correct together, neither correct alone; never revert one without the other. Also confirmed the #41 DROP COLUMN is the ONLY schema change in the entire diff, so the checkHealth-before-slack ordering constraint is satisfied trivially by a single commit and there is no third constraint. No new dead code: a reader enumerated for every field and union arm added tonight"}
{"date":"2026-07-26","runId":"combined-verify","lane":"profiler","ref":"V1","finding":"mergePersonRows false now means two different things - setPersonEmail reads a FILE-SIDE DEFERRAL as an identity refusal and logs a false reason","rootCause":"src/db/people.ts:1164","verdict":"needs-owner-decision","state":"open","note":"PARKED by the owner 2026-07-26 (may fix later). Returns null and logs 'email write refused, address held by another identity' - a false reason - and the email is not recorded. Reachable only on a filesystem fault (readdirSync of config/users failing, or a rename/unlink leaving the loser md on disk). Consequence is duplicate-shaped, the same family as the orphan-md bug CF1 replaced, and it fails in the recoverable direction. Severity low"}
{"date":"2026-07-26","runId":"combined-verify","lane":"matchmaker","ref":"V2","finding":"an unlinked reschedule row no longer suppresses the overlap autofix, so the autofix can re-move and re-DM the colleague","rootCause":"src/db/jobs.ts:233 (bridge catch leaves request_id NULL, the JOIN drops the row)","verdict":"needs-owner-decision","state":"open","note":"PARKED by the owner 2026-07-26. Backstopped by recentlyAutoMovedIds (12h) in the same detector, so it needs TWO faults to reach a person. Severity low"}
{"date":"2026-07-26","runId":"combined-verify","lane":"matchmaker","ref":"V3","finding":"P15 does not cover equal date-only windows - start_date === end_date hits the instant branch, gets midnight-covering blocks, and reads as 'they are free' with NO notChecked flag","rootCause":"src/skills/meetings/ops/handlers/calendarReads.ts:564 (handleGetFreeBusy passes model args straight through)","verdict":"needs-owner-decision","state":"open","note":"PARKED by the owner 2026-07-26. NOT a regression - pre-tonight an empty map was also read as free - but it is the most likely malformed input on that tool and the P15 fix leaves it uncovered. Severity low-MEDIUM as a pre-existing hole. The Manager's recommendation was to build this one"}
{"date":"2026-07-26","runId":"combined-verify","lane":"matchmaker","ref":"V4","finding":"non-outage Graph faults now escape handleCheckHealth to the registry as a raw string in her context, where they used to be a clean error object","verdict":"needs-owner-decision","state":"open","note":"PARKED by the owner 2026-07-26. Both callers are crash-guarded (the calendarHealth wrapper and briefs try/catch), so this is context hygiene, not breakage. Deliberate consequence of removing the causeless local catch in P24"}
{"date":"2026-07-26","runId":"combined-verify","lane":"matchmaker","ref":"V5","finding":"walker-side trackReject calls omit the noise flag, so a walker-rejected out-of-window cursor lands in the per-day map under owner_busy_collision","rootCause":"src/connectors/graph/findAvailableSlots.ts:1092 and :1113","verdict":"needs-owner-decision","state":"open","note":"PARKED by the owner 2026-07-26. Narration cosmetics only. Severity low"}
{"date":"2026-07-26","runId":"manual-owner-regression","lane":"matchmaker","ref":"relaxed-offers-owner-busy","finding":"HIGH - find_available_slots' relaxed recovery offered times the owner was already committed on, and narrated them as 'clean for both of you'. He was double-booked twice in one conversation","rootCause":"src/utils/scheduleRules.ts:640 (hard collision gated on !allowRelaxed) + src/connectors/graph/findAvailableSlots.ts (the only compensating filter was owner-domain-scoped)","verdict":"built","state":"built","note":"NOT a 4.2.x regression - originates at 5ad8076 (v3.7.3, the #142d landing) and 4.2.1 LEAVES it; neither P25 nor P32 nor af19500 caused it. Amplifier: scoreSlotDensity only looks at commitments ending BEFORE or starting AFTER a slot, so a slot buried INSIDE a meeting scores the maximum 4 - which is why the two double-booked slots were the TOP picks (density ranking arrived in 3.7.5). af19500 actually HELPED: hoisting the occupancy scan put overCommitment on every verdict, so the truth was already available and simply never read - that made the fix a one-line read instead of a rewrite. RUNTIME-PROVEN: 19:17:02.045 strict rejected 15:30 and 16:00 with owner_busy_collision (his own calendar only - Maayan was free and in-hours at both); 19:17:02.546 relaxed accepted 8; 19:17:28 create_meeting's planMeeting verdict reported the collision from the SAME validator on the SAME events. HONESTY HALF was worse than silence: the candidate was byte-identical to a free slot (overCommitment discarded, the walker read only overOptional) and _recovery_note asserted the slots bypassed only soft rules while pointing the model at day_summary.top_reasons - the STRICT pass's per-day top-2, which said nothing about the offered times. She had no way to be honest. FIX reads verdict.overCommitment, keyed on overCommitment NOT level so the Optional tier (timed WE, standup) stays offerable-and-tagged; keepWorkHours reads P25's outsideWorkHours; per-slot broken_rule_label from the same checkSlot, gated on viewer==='owner'. ALSO deleted a 22-line post-hoc work-day clip that ran AFTER the budget was spent and was silently killing recoveries (log 06:59:36: relaxedAccepted 8 -> kept 0). THIRD symptom of the same root fixed: candidate_slots on a committed time under relaxed returned available:true, now available:false with a real reason. RE-TRACE of the incident on fixed code: the first answer now includes 17:30-17:55 - the gap he had to drag out on the third try - with 13:00 labelled 'no room for your lunch' and 17:30 'below the 120-min floor'; 15:30 and 16:00 are gone. 17/17 traced. Q5 (should recovery offer anything at all) COLLAPSED - it was only a question because the offer was undeliverable"}
{"date":"2026-07-26","runId":"manual-owner-regression","lane":"instructor","ref":"relaxed-prompt-contradiction","finding":"the tool description told the model relaxed:true bypasses hard rules - false after the proposal fix, and it licensed the model to disbelieve its own no-conflict-tag guarantee","rootCause":"src/skills/meetings.ts:1148","verdict":"built","state":"built","note":"CORRECTED THE MANAGER'S BRIEF: 'relaxed bends his SOFT rules' would have shipped a SECOND inaccuracy, because on the EXPLICIT relaxed path findAvailableSlots unions a widened window with his native hours and scheduleRules.ts:723 waives rule 5 - and meetings.ts:1122 calls his hours HARD RULES. keepWorkHours is opt-in and only the auto-recovery sets it. So it wrote the invariant the code actually holds instead: relaxed bends the rules about his own day, each returned slot carries the broken_rule_label it bends, a proposal is still a time he is free, and he books over a commitment by naming it to create_meeting. Independently verified that keying the guard on overCommitment keeps the Optional tier offerable (scheduleRules.ts:571-576 sets overOptional without overCommitment). +83 chars = +21 tokens, owner static section only; colleague DM and MPIM prompts measured UNCHANGED, so no mechanism leaks. Positive form, no new rule block, no new tier. 11 trace cells"}
{"date":"2026-07-26","runId":"manual-owner-regression","lane":"outrider","ref":"stale-isSlotInWorkHours-comment","finding":"a comment names isSlotInWorkHours, which the meeting lane deleted","rootCause":"src/config/userProfile.ts:609","verdict":"needs-dependency","state":"open","note":"PARKED - comment-only, one line, zero behaviour. The sentence is about hours_end=00:00 -> 23:59 normalisation; the arithmetic now lives in checkSlot's fitsWorkWindow. Suggested wording matching the meeting lane's own replacement at workHours.ts:58: 'so a slot-fit test does not wrap around midnight'. Two other isSlotInWorkHours hits are DELIBERATE and should stay (connectors/graph/findAvailableSlots.ts:121 and handlers/findAvailableSlots.ts:1036 narrate the deleted implementation on purpose); the CHANGELOG and V3 handoff hits are historical records"}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"P2","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"P4","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"P5","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"P6","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"P7","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"P8","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"P9","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"P10","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"P12","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"P14","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"P16","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"P21","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"P23","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"P31","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"A1","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"A7","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"CF2","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"CF3","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"CF5","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"#48","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"#56","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"D7","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"D8","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-decline-4.2.1","lane":"","ref":"#1","finding":"owner declined during the 4.2.1 review","verdict":"declined","state":"declined","note":"Backfilled from report.md prose: \"Owner-declined tonight and not to be re-raised\". The decline was never written as a ledger row, so --open kept surfacing all 24 and would have re-raised them. Not to be re-raised."}
{"date":"2026-07-26","runId":"owner-review","lane":"profiler","ref":"V1","finding":"mergePersonRows false means two things - a file-side deferral reports as an identity refusal","verdict":"needs-owner-decision","state":"closed-owner-declined","note":"OWNER DECLINED 2026-07-26: 'remove, not going to do it'. Reachable only on a filesystem fault, fails in the recoverable direction. Do not re-raise"}
{"date":"2026-07-26","runId":"owner-review","lane":"matchmaker","ref":"V2","finding":"an unlinked reschedule row no longer suppresses the overlap autofix","verdict":"needs-owner-decision","state":"closed-owner-declined","note":"OWNER DECLINED 2026-07-26: 'remove, not going to do it'. Needs TWO faults to reach a person (the bridge must throw AND the 12h recentlyAutoMovedIds backstop must miss). Do not re-raise"}
{"date":"2026-07-26","runId":"owner-review","lane":"matchmaker","ref":"V4","finding":"non-outage Graph faults escape handleCheckHealth as raw text in her context","verdict":"needs-owner-decision","state":"closed-owner-declined","note":"OWNER DECLINED 2026-07-26: 'remove, not going to do it'. Context hygiene only - both callers are crash-guarded. A deliberate consequence of removing P24's causeless local catch. Do not re-raise"}
{"date":"2026-07-26","runId":"owner-review","lane":"outrider","ref":"stale-isSlotInWorkHours-comment","finding":"a comment named isSlotInWorkHours, deleted by the meeting lane","rootCause":"src/config/userProfile.ts:609","verdict":"built","state":"built","note":"Owner approved: 'outer - fix the comment'. The agent verified the comment's SUBSTANCE was still accurate - the hours_end 00:00 -> 23:59 normalisation genuinely prevents a wrap, because parseRange (workHours.ts:53) rejects any range whose end is <= its start, so an unnormalised 22:30-00:00 would be dropped outright. Only the NAME was stale. 2 lines changed, 0 net; also closed a bookwork word-split the re-flow exposed. Confirmed the other two isSlotInWorkHours hits are deliberate history and left them. typecheck green"}
{"date":"2026-07-26","runId":"owner-review","lane":"matchmaker","ref":"V3","finding":"P15 does not cover equal date-only windows - start_date === end_date reads as 'they are free' with no notChecked flag","rootCause":"src/skills/meetings/ops/handlers/calendarReads.ts:564","verdict":"needs-owner-decision","state":"open","note":"OWNER 2026-07-26: KEEP - 'we might do, if we will have another meeting agent run, lets see'. Still the Manager's pick of the parks: same class P15 just fixed, on the most likely malformed input on that tool, and the fix leaves it uncovered. Not a regression - an empty map read as free before too"}
{"date":"2026-07-26","runId":"owner-review","lane":"matchmaker","ref":"V5","finding":"walker-side trackReject calls omit the noise flag, so an out-of-window cursor lands under owner_busy_collision","rootCause":"src/connectors/graph/findAvailableSlots.ts:1092 and :1113","verdict":"needs-owner-decision","state":"open","note":"OWNER 2026-07-26: KEEP - 'we might do, if we will have another meeting agent run, lets see'. Narration cosmetics only"}
{"date":"2026-07-26","runId":"owner-review","lane":"profiler","ref":"V1","finding":"mergePersonRows false means two things - a file-side deferral reports as an identity refusal","verdict":"declined","state":"closed-owner-declined","note":"OWNER DECLINED 2026-07-26: 'remove, not going to do it'. Reachable only on a filesystem fault and fails in the recoverable direction. Do not re-raise"}
{"date":"2026-07-26","runId":"owner-review","lane":"matchmaker","ref":"V2","finding":"an unlinked reschedule row no longer suppresses the overlap autofix","verdict":"declined","state":"closed-owner-declined","note":"OWNER DECLINED 2026-07-26: 'remove, not going to do it'. Needs TWO faults to reach a person - the bridge must throw AND the 12h recentlyAutoMovedIds backstop must miss. Do not re-raise"}
{"date":"2026-07-26","runId":"owner-review","lane":"matchmaker","ref":"V4","finding":"non-outage Graph faults escape handleCheckHealth as raw text in her context","verdict":"declined","state":"closed-owner-declined","note":"OWNER DECLINED 2026-07-26: 'remove, not going to do it'. Context hygiene only - both callers are crash-guarded. A deliberate consequence of removing P24's causeless local catch. Do not re-raise"}
{"date":"2026-07-26","runId":"owner-review","lane":"matchmaker","ref":"A2-short-window-throw","finding":"Graph windows of 0-6 minutes threw on an uncaught error code","verdict":"built","state":"built","note":"Superseding row for the reader: this WAS built 2026-07-26 under the combined ref A2+A3 (both window rejections now share P15's one branch), which the ledger reader cannot match back to the original ref. Shipped in 4.2.1"}
{"date":"2026-07-26","runId":"owner-review","lane":"matchmaker","ref":"A3-createmeeting-raw-relaxed","finding":"createMeeting read raw args.relaxed for the dense-packing counter-offer","verdict":"built","state":"built","note":"Superseding row for the reader: this WAS built 2026-07-26 under the combined ref A2+A3 - createMeeting.ts:955 now reads bookingRequest.relaxed. Shipped in 4.2.1"}
{"date":"2026-07-26","runId":"owner-review","lane":"slacker","ref":"P27","finding":"an inboundQueue comment described a mechanism that does not exist","verdict":"built","state":"built","note":"Superseding row for the reader: built 2026-07-26 under the combined ref P27+P28+gate-order. Shipped in 4.2.1"}
{"date":"2026-07-26","runId":"owner-review","lane":"slacker","ref":"P28","finding":"a duplicate overload string","verdict":"built","state":"built","note":"Superseding row for the reader: built 2026-07-26 under the combined ref P27+P28+gate-order - folded to one OVERLOAD_REPLY const. Shipped in 4.2.1"}
{"date":"2026-07-26","runId":"owner-review","lane":"matchmaker","ref":"P29","finding":"two copies of the all-day-OOF predicate","verdict":"built","state":"built","note":"Superseding row for the reader: built 2026-07-26 under the combined ref P29+P30. Shipped in 4.2.1"}
{"date":"2026-07-26","runId":"owner-review","lane":"matchmaker","ref":"P30","finding":"the day-off push path did not carry outOfOfficeAllDay","verdict":"built","state":"built","note":"Superseding row for the reader: resolved 2026-07-26 under the combined ref P29+P30 - deliberately NOT carried, because carrying it would create the set-but-unread field the item forbids; the contract is documented at analysis.ts:253-268 instead. Shipped in 4.2.1"}
{"date":"2026-07-26","runId":"owner-review","lane":"matchmaker","ref":"gh#41","finding":"the parallel work-item lifecycle outreach_jobs.status","verdict":"built","state":"built","note":"Superseding row for the reader: COMPLETED 2026-07-26 across all four steps (refs gh#41-step1 through step4) - reader swapped to the spine, the status='replied' write deleted, the cascade deleted, the column dropped and OutreachStatus narrowed to OutreachTransition. Shipped in 4.2.1. Migration is ONE-WAY"}
{"date":"2026-07-26","runId":"owner-review","lane":"matchmaker","ref":"gh#148","finding":"CalendarHealth had no memory of the category question it asked","verdict":"built","state":"built","note":"Superseding row for the reader: built 2026-07-26 (refs B5/B6/N2) and the GitHub issue is CLOSED. Not a regression - flagging ran since v1.7.0 but the calendar_issues row never existed. Shipped in 4.2.1"}
{"date":"2026-07-26","runId":"owner-review","lane":"instructor","ref":"N2","finding":"the RECENT CALENDAR ISSUES guidance had no route from a category reply to set_event_category","verdict":"built","state":"built","note":"Superseding row for the reader: built 2026-07-26 and the owner ruled SHIP AS SHAPED. Proven load-bearing, not belt-and-braces - at 10:39 he retried with history present and there were still zero tool calls. Shipped in 4.2.1"}
{"date":"2026-07-26","runId":"owner-review","lane":"profiler","ref":"A6-gender-reconfirm","finding":"re-confirming an already-stored gender answered 'refused' for something already true","verdict":"built","state":"built","note":"Superseding row for the reader: built 2026-07-26 under the ref A6 - a CoreFieldWrite union across all four core fields. Shipped in 4.2.1"}
{"date":"2026-07-26","runId":"owner-review","lane":"matchmaker","ref":"P23","finding":"the offline retry has no backoff","verdict":"declined","state":"closed-owner-declined","note":"OWNER DECLINED 2026-07-26: 'p23 - don't fix'. Do not re-raise"}
{"date":"2026-07-26","runId":"owner-review","lane":"matchmaker","ref":"P31","finding":"the occupancy scan is unconditional, roughly 3x scan work on a 21-day search - UNMEASURED","verdict":"declined","state":"closed-owner-declined","note":"OWNER DECLINED 2026-07-26: 'drop the own unknown'. Explicitly not to be measured either. Do not re-raise"}
{"date":"2026-07-26","runId":"owner-review","lane":"slacker","ref":"P7","finding":"a Hebrew or Russian speaker hits an error and gets an English apology","verdict":"declined","state":"closed-owner-declined","note":"OWNER DECLINED 2026-07-26: 'p7 don't fix now'. Note the 'now' - he may revisit. Cannot use an LLM because on the 529 path the model is what is unavailable; needs detectMessageLanguage plus a static table"}
{"date":"2026-07-26","runId":"owner-review","lane":"slacker","ref":"A1-whatsapp-no-gates","finding":"whatsapp.ts appends the reply raw and never runs the gate stack at all","verdict":"declined","state":"closed-owner-declined","note":"OWNER DECLINED 2026-07-26: 'A1 - no fix'. Dormant transport, nothing leaks today. Worth revisiting BEFORE that transport is ever enabled"}
{"date":"2026-07-26","runId":"owner-review","lane":"registrar","ref":"outreach-task-third-spine","finding":"a THIRD work-item lifecycle - message_colleague mints a tasks row with its own status enum, own due_at and no dispatcher","verdict":"declined","state":"closed-owner-declined","note":"OWNER DECLINED 2026-07-26: 'a7 - no fix'. 8 rows sit at 'failed'. The trap if revisited: the obvious minimal fix (drop due_at) removes the accidental closer, leaving pending_colleague rows in the prompt forever; the correct fix deletes the row, which first needs a home for the tick-on-last-message react (user-visible)"}
{"date":"2026-07-26","runId":"owner-review","lane":"gatekeeper","ref":"P26","finding":"the every-gate-fails-open contract was false on the colleague leg","verdict":"built","state":"built","note":"Superseding row for the reader: built 2026-07-26 - the CODE was fixed so the comment becomes true, splitting the identity read (fails open) from the leak gate (fails safe). Shipped in 4.2.1"}
{"date":"2026-07-26","runId":"owner-review","lane":"slacker","ref":"private-channel-name-exposure","finding":"find_slack_channel searches private_channel, so a private channel name can be handed to the model","verdict":"declined","state":"closed-owner-declined","note":"OWNER DECLINED 2026-07-26: 'slack open item - leave it'. Capability kept. Recorded as a warning block at findChannelByName so it is found at the code. The only payload-level control would remove posting to private channels entirely"}
{"date":"2026-07-26","runId":"owner-review","lane":"gatekeeper","ref":"A4-lone-anglebracket-id","finding":"an id behind a lone < was invisible to BOTH scrubber patterns","verdict":"built","state":"built","note":"Superseding row for the reader: built 2026-07-26 under the ref A4 - one shared UNRENDERED_SLACK_ID predicate keyed on a CLOSED rendered mention, runtime-verified byte-identical on all 12 P3-protected forms. Shipped in 4.2.1"}
{"date":"2026-07-26","runId":"owner-review","lane":"gatekeeper","ref":"A5-slack-channel-ref","finding":"slack_channel_ref fired on a RENDERED channel link","verdict":"built","state":"built","note":"Superseding row for the reader: RETIRED 2026-07-26 under the ref A5 - the trigger broke its own class contract by deleting the fact the sentence was about, and had zero fires in any log. Also fixed the rewriter prompt that banned rendered mentions outright. Shipped in 4.2.1"}
{"date":"2026-07-26","runId":"owner-review","lane":"outrider","ref":"bugger-depask-dropped","finding":"three lanes returned built plus a dependency ask and none was dispatched","verdict":"built","state":"built","note":"Superseding row for the reader: FIXED by the process chat in commit 82d476d ('bugger: stop discarding dependency asks - four were lost on the first real run'). Not this chat's work; recorded so it stops showing as awaiting the owner"}
{"date":"2026-07-26","runId":"owner-decisions","lane":"registrar","ref":"revert-relay-decision","finding":"DECIDED - when a later calendar write voids a notice a colleague already received, relay the correction","verdict":"needs-dependency","state":"open","note":"OWNER CHOSE OPTION C 2026-07-26: relay ONLY when the new time DIFFERS from what the colleague was told, capped once per event per day. Rationale accepted: correcting a false statement Maelle made to a human is not chasing; re-confirming an unchanged time is. Requires the executed new start/end in closeMeetingArtifacts (5 call sites, meeting's files) - SEQUENCED: meeting plumbs the data first, then requests builds the relay. Also gates the content-level dedup, which is only safe now that the revert IS relayed"}
{"date":"2026-07-26","runId":"owner-decisions","lane":"matchmaker","ref":"autofix-reapplied-revert","finding":"DECIDED - an owner move that undoes an autofix suppresses that event for a window","verdict":"needs-dependency","state":"open","note":"OWNER CHOSE OPTION B 2026-07-26, in his words: 'if i change the auto fix, don't change it again.' Any owner move undoing an autofix marks the event dismissed for a window (24h recommended, lane to justify). First job: establish what records a revert TODAY - revert_last_auto_move writes a terminal dismissed row, but it is unknown whether a CONVERSATIONAL revert ('move it back') reaches that path or only the explicit tool does. Highest-harm item of the six: an autonomous action repeated something the owner explicitly undid AND messaged a colleague both times"}
{"date":"2026-07-26","runId":"owner-decisions","lane":"registrar","ref":"pseudo-threadts-relays","finding":"seven owner-facing relays post to a pseudo thread ts, so an owner-side confirmation on a reschedule reply would fail to land","verdict":"declined","state":"closed-owner-declined","note":"OWNER DECLINED 2026-07-26: chose option C, leave it. Code-provable but runtime-UNVERIFIED (the 07-13 log rotated, and it needs a colleague to reply to an autofix notice, which is rare). Consequence if it ever fires: the calendar changed and the owner was not told. The requests lane's own new code already avoids the pattern; only the 7 legacy sites remain"}
{"date":"2026-07-26","runId":"owner-decisions","lane":"registrar","ref":"outreach-task-third-spine","finding":"DECIDED - delete the third lifecycle's tasks row and rehome the tick reaction","verdict":"needs-dependency","state":"open","note":"OWNER CHOSE OPTION A 2026-07-26, REVERSING his earlier 'a7 - no fix' after fuller detail. Delete the tasks row entirely and keep the tick-on-last-message react by rehoming fireCompletionReact onto the send path. THE TRAP: the obvious minimal fix (drop due_at) removes the ACCIDENTAL closer - the bogus 'failed' write is what currently takes these rows out of the open set, so removing it leaves pending_colleague rows in the owner's prompt forever. May touch coordinator.ts (slack) readers"}
{"date":"2026-07-26","runId":"owner-decisions","lane":"profiler","ref":"CF2","finding":"DECIDED - gate the migration backup on at least one mergeable pair","verdict":"needs-dependency","state":"open","note":"OWNER CHOSE OPTION A 2026-07-26, REVERSING his earlier 'cf2 - just delete it ignore' after being shown it is latent-unbuilt rather than done. The migration writes a fresh backup JSON on EVERY boot whenever a duplicate-email group exists, with no guard for a group that will be refused forever - unbounded file growth in data/migrations/ from the first time two Slack accounts claim one email address (deactivate-then-rehire the realistic trigger). Not firing today only because no group exists"}
{"date":"2026-07-26","runId":"owner-decisions","lane":"matchmaker","ref":"F2b","finding":"DECIDED - re-check whether P22 already closed the rule-1 relaxed gate","verdict":"needs-dependency","state":"open","note":"OWNER APPROVED THE RE-CHECK 2026-07-26. Rule 1 (vacation/off-day) is the lone rule in the ladder with no allowRelaxed gate; the real bite was owner-in-MPIM, where gateRelaxed returned relaxed:true while the clamp kept the initiator as colleague. Tonight's P22 changed exactly that neighbourhood - grantRelaxed now grants only on senderRole==='owner' and owner-in-MPIM is denied. If P22 closed it, close F2b as already-fixed at no cost; if not, it remains the owner's reserved security seam"}
{"date":"2026-07-26","runId":"owner-decisions","lane":"profiler","ref":"CF2","finding":"the migration wrote a fresh backup JSON every boot for a group that can never merge","rootCause":"src/db/migrations/v4_0_4_dedupe_people_email.ts:65 (early return only on a CLEAN table)","verdict":"built","state":"built","note":"Owner chose option A. Groups partitioned into actionable/stuck; only actionable reaches the backup and the merge loop. THE GATE IS EXACT, NOT HEURISTIC: slack_id carries a UNIQUE index (verified against live sqlite_master), so two non-null ids in one email group are ALWAYS distinct - which is precisely mergePersonRows' permanent refusal. With the existing ORDER BY (slack_id IS NOT NULL) DESC, a loser pairs cleanly with rows[0] exactly when the LOSER's slack_id is NULL, so one NULL-slack row in the group implies at least one real merge. Correctly EXCLUDED the other two refusals: kind='self' is unreachable (both queries filter it) and the md-fold refusal is a DEFERRAL, so those groups stay actionable and still get backed up. CF1's invariant intact - the gate sits before any file write and any mutation and decides only whether anything needs backing up. Also collapsed a duplicated query so the backup is literally the pre-image of the rows touched. WARN kept (once per boot, terminal wording naming the email and both slack_ids) because the unbounded thing was the FILE - nothing ages out data/migrations/ while the log rotates daily; the old collapsed line read merged:0 refused:1, like a repeating failed operation rather than a standing condition. NO cleanup of the existing file: it is the legitimate pre-image of the only merge that ever ran (Luke, groups:1 rows:2 -> merged:1 refused:0) and deleting it would discard the CF1 safety artifact. Code-only forward protection; 21 boots on 07-26 with zero people-dedupe lines. FLAGGED not built: a DETERMINISTIC md-fold failure (readdirSync throwing) would refuse forever while the group still looks actionable, reviving file-per-boot growth by another route - cannot be gated predictively because the fold only reports after the backup must exist, and gating it would break the invariant. That is a stuck-fold bug with a different fix"}
{"date":"2026-07-26","runId":"owner-decisions","lane":"registrar","ref":"outreach-task-third-spine","finding":"the third work-item lifecycle deleted; minting it is now a COMPILE ERROR, not a convention","rootCause":"src/skills/outreach.ts:252 and :291 + src/tasks/dispatchers/index.ts:25-31 (no 'outreach' entry)","verdict":"built","state":"built","note":"Owner chose option A, reversing his earlier decline. 6 files, +103/-139 (net -36). 'outreach' REMOVED from TaskType so the third lifecycle cannot be re-created - follows that file's own v3.1 precedent. The tick react is rehomed onto the send path as exported reactActivityComplete in threadActivity.ts, beside getLastMaelleMessage which is its only data source; fired AFTER a confirmed send. REAL BUG FIXED IN PASSING (user-visible): the old setImmediate raced ahead of the awaited send, so A FAILED SEND STILL GOT A TICK - the one behaviour delta, in the direction of truth. THE TRAP IS CLOSED BY CONSTRUCTION: getActiveJobsForThread's tasks half can no longer match an outreach row at all, so nothing renders pending_colleague forever without needing the bogus 'failed' write. WHY IT WAS SILENT: the unknown-dispatcher path RETURNS instead of throwing, so the owner-facing 'I couldn't complete X' DM never fired - runtime proof at logs/maelle.log:4136. Stated purpose was false: get_my_tasks reads getOpenRequestsForOwner, never the tasks table. Both pre-conditions re-confirmed: open type='outreach' rows = 0, open tasks joined to any outreach job = 0. Also merged terminalForFollowup and terminalTask after proving they were the same predicate, deleted 4 hand-rolled UPDATE tasks closures in meetingReschedule, and deleted a dead react branch in tasks/index.ts. Verified all 4 surviving createTask callers pass status:'new' so no tick was lost. 60 legacy rows LEFT as inert history - all terminal and unreachable by every reader, so a migration would mutate rows no code can see. 17/17 traced"}
{"date":"2026-07-26","runId":"owner-decisions","lane":"slacker","ref":"dead-update-tasks-coordinator","finding":"coordinator.ts:333-336 and :388-391 hold the same now-dead UPDATE tasks WHERE skill_ref statements","verdict":"needs-owner-decision","state":"open","note":"Dead-code cleanup, NOT a dependency - the third-lifecycle fix is complete without it, and both paths already close the request through updateOutreachJob. Provably no-ops after that change. Raised by requests; slack's file"}
{"date":"2026-07-26","runId":"owner-decisions","lane":"outrider","ref":"stale-tasktype-members","finding":"other stale TaskType members (coordination/reminder/follow_up/research/briefing) - nothing creates them, but two are still load-bearing in the legacy-row queries","verdict":"needs-owner-decision","state":"open","note":"Its own item, deliberately not bundled into the third-lifecycle deletion. Also: client.ts:835-847 is a one-time target_slack_id backfill keyed on type='outreach', idempotent and long since applied (0 changes per boot) - left as a historical migration. Raised by requests"}
{"date":"2026-07-26","runId":"owner-decisions","lane":"matchmaker","ref":"autofix-reapplied-revert","finding":"an owner move that undoes an autofix now suppresses that event for 24h - and only the EXPLICIT tool recorded a revert before this","rootCause":"src/skills/meetings/ops/handlers/calendarReads.ts:354-362 (dismissOverlapIssue had exactly one caller, handleRevertLastAutoMove) + src/db/requests.ts:371-383 (the 12h window was timed from Maelle's move, not the owner's decision)","verdict":"built","state":"built","note":"Owner chose option B: 'if i change the auto fix, don't change it again'. FORENSIC CORRECTION - the literal double-DM CANNOT reproduce inside 12h today, because the machinery postdates the incident: on 2026-07-13 the running build was v3.7.0 (8a81d25), while the auto_move spine record shipped in v3.7.1 at 07-13 20:14 and the 12h memory + revert tool in v3.7.2 at 07-14 01:13. Zero subkind='auto_move' rows exist for 07-13. audit_log is exact: 7548 04:35:18 assistant update_meeting -> Tue 12:45; 7556 04:51:38 OWNER via move_meeting (NOT revert_last_auto_move) -> back to today 13:30; 7594 10:01:20 assistant update_meeting -> Tue 12:45 again; same target event id on all three, so no Graph-id instability - the guard simply had nothing to match. THE REAL GAP, which is what was fixed: the only protection was a 12h window timed from Maelle's move rather than the owner's decision, read off a best-effort write - so undoing it 13h later let the next sweep re-do it. FIX: an owner-authenticated move of an event in getRecentlyAutoMovedEventIds writes the terminal dismissal on THAT event only (never the peer), eventEndMs = min(eventEnd, now+24h). 24h DERIVED not round: active mode runs twice daily, widest sweep gap ~18.5h, so 12h would let the next morning re-override him. Landing it in getSuppressedEventIds is what makes it hold regardless of detector - verified read by the double-booking pair scan, the dead-gap scan and all three defrag paths. CAUGHT A HARM THE MANAGER DID NOT ANTICIPATE: dismissOverlapIssue's row lookup was event_id-ONLY, so a day-shape dismissal would have flipped an open missing_category question to 'dismissed' - tonight's B6 fix IN REVERSE. Now problem-axis scoped, and its UPDATE re-stamps event_date/event_end_ms so a decision cannot inherit an older row's longer expiry. Also gave upsertCluster's terminal check the event_end_ms > now bound the read path always had - an expired terminal row used to silence TRACKING of that event forever while detection had resumed. STATED BOUND: the trigger inherits the 12h record horizon; beyond it there is no evidence a move was an autofix, and inventing that would suppress moves he never disputed"}
{"date":"2026-07-26","runId":"owner-decisions","lane":"matchmaker","ref":"F2b","finding":"rule 1's missing allowRelaxed gate - re-checked and ALREADY CLOSED by tonight's P22","verdict":"already-fixed","state":"built","note":"Proven unreachable by construction, at zero cost, exactly the good outcome the re-check was for. grantRelaxed grants only on context.senderRole==='owner' and `initiator` derives from the SAME field; the MPIM clamp mutates input.senderRole to 'colleague' and the skill context reads that same mutated object, while turnSenderRole (identity-based) never reaches SkillContext - so owner-in-MPIM is denied. The last hardcoded initiator:'owner' + allowRelaxed:true site (floatingBlockOps book_floating_block) is absent from COLLEAGUE_ALLOWED_TOOLS and blocked again at registry.ts:565. Rule 1's ungated status remains the owner's settled call"}
{"date":"2026-07-26","runId":"owner-decisions","lane":"matchmaker","ref":"V3","finding":"a date-only endDate is now the END OF THAT DAY - and the same root was silently truncating every date-only RANGE","rootCause":"src/connectors/graph/calendarReads.ts (getFreeBusy, after the validity check)","verdict":"built","state":"built","note":"Chose WIDEN over refuse: the window IS that day, so nothing is invented and notChecked's claim ('could not be queried') would be false - refusing would be a wrong reason (M11), and P15's own reasoning applies. Fixed in the CONNECTOR not the handler so one rule serves every caller (M2). BIGGER THAN V3 ITSELF: it also fixes the silent same-root truncation of every date-only range - '07-30' to '07-31' was reading only the 30th. Traced: date-only equal window recurses once with a time-bearing end -> 1440-min window -> the day's real blocks; the P15 instant branch still fires for time-bearing instants; inverted date-only still lands on nothingChecked; the >62d clamp still works at depth <= 2. Blast radius verified - every non-handler caller already passes a full ISO instant"}
{"date":"2026-07-26","runId":"owner-decisions","lane":"matchmaker","ref":"V5","finding":"PHANTOM - the parked line numbers were stale, and passing the flag would have been HARMFUL","verdict":"already-fixed","state":"built","note":"No change is the correct answer. The cited lines were pre-fca4b11 numbering: old :1092 was the hand-rolled external-only owner-busy re-scan, DELETED by fca4b11, and its replacement already passes the flag. Old :1113 (now :1176) is reached ONLY when inAnyWindow===true one branch above, so 'outside work hours' is FALSE there - passing true would file a truthful IN-HOURS collision under the noise label and erase the day's real reason. Its sibling line already IS the noise label, so the flag is a literal no-op. Every other flagless site runs BEFORE checkSlot, so the validator fact does not exist yet, and re-deriving it there is exactly what P25's comment forbids"}
{"date":"2026-07-26","runId":"owner-decisions","lane":"registrar","ref":"revert-relay-decision","finding":"MANAGER SEQUENCING ERROR - the plumbing was dispatched to meeting first, but the params type must change in requests' own file first","verdict":"needs-dependency","state":"open","note":"Meeting could not build its half: closeMeetingArtifacts' params type has no time field, passing one is an excess-property type error, and meeting is barred from editing that file. Order corrected - requests adds `newStartIso?: string; newEndIso?: string` (absent => no relay, so the fail-safe makes landing order harmless) and builds the relay, then meeting fills its call sites. Meeting verified that the count is 5 but only TWO can carry a changed time: moveMeeting.ts:1311 and :983, both reason:'moved', both using effectiveStart/effectiveEnd post-snap and post-verifyEventMoved. The other three are correctly excluded: :382 'updated' passes no start/end and is already skipped at step 2; createMeeting:1309 'created' is the first write for that event id so it cannot void a prior notice; calendarReads:823 'deleted' has no new time and closes cancelled"}
{"date":"2026-07-26","runId":"owner-decisions","lane":"registrar","ref":"revert-relay-decision","finding":"a colleague was never told the time he had been given was void - step 2 closed his ask as resolved/meeting_moved after the answer was discarded","rootCause":"src/utils/closeMeetingArtifacts.ts step 2","verdict":"built","state":"built","note":"Owner chose option C: relay only when the new time DIFFERS from what he was told, capped once per event per day. DB PROOF, exact: out_1783917319399_5ilp carried context_json.proposed_start = 2026-07-14T12:45:00.000+03:00 with owner_thread_ts = brief_health_U0F28CK6H (confirming the autofix path) and reply_deadline null; its request closed state=resolved, closure_reason=meeting_moved, requester_notified_at=null. closure_reason being meeting_moved rather than meeting_moved_and_notified_requester PROVES step 2 closed it, not step 5's close-loop. FIX: new step 2a before the close loop reads what the colleague was ACTUALLY told (ctx.proposed_start, read never re-derived) and compares INSTANTS via getTime() not strings - the told value carries +03:00 while the executed value is formatted independently, so equal moments differ as text and a string compare would have relayed a correction every single time, making R5 pestering the default. ORDERING IS LOAD-BEARING: the caller's snapshot is taken before the relay, so the correction's OWN new request is not closed by the same pass and becomes the new record of what the colleague was last told. NOT hand-rolled - the relay calls the SAME function that sent the notice being corrected, so one spine row, one expiry, one close-loop, tagged so a 'that doesn't work' reply still routes to the owner, and it self-cancels through the spine if the DM never lands. Passes ownerChannel with NO ownerThreadTs, so it can never hit the pseudo-key the owner declined to fix. originalStartIso became optional because a correction has no honest revert target - passing the voided time would offer the owner back the thing he just undid. notifyColleagueOfMove return void -> boolean so the relay cannot report a correction it never sent. FAIL-SAFE BY CONSTRUCTION: relayVoidedNotices returns 0 on its first line if either field is absent, so landing order is harmless. 10/10 traced. OWNER EDGES LEFT UNDECIDED: an undelivered correction still burns the day's budget (retry-vs-cap is his call), and a cap-suppressed correction still closes the ask resolved/meeting_moved. Content-level dedup does NOT fall out free - the cap is 'do not correct twice', which says nothing about 'do not tell him the same thing twice'; named and stopped"}
{"date":"2026-07-26","runId":"owner-decisions","lane":"matchmaker","ref":"revert-relay-plumbing","finding":"the executed new start/end now reaches closeMeetingArtifacts from the two call sites that can carry a changed time","rootCause":"src/skills/meetings/ops/handlers/moveMeeting.ts:1311 and :983","verdict":"built","state":"built","note":"Verified the type on disk before passing anything (params at :112-113; the Manager's quoted 86-102 was the doc block). :1311 main path confirmed BOTH properties - effectiveStart/effectiveEnd are re-assigned by the grid snap and the floating-block realignment, and the call site sits PAST verifyEventMoved whose !verify.ok branch returns. :983 owner floating-block branch is post-snap but has NO verifyEventMoved - STATED rather than claimed otherwise, and proved unreachable for harm: the branch fires only for a floating block, a block has no attendees, and every writer of ctx.proposed_start is a notice TO an attendee (autoMove's solo-event guard refuses an event with no non-owner attendee), so no open notice can reference a block's event id and step 2a finds nothing. Noted in code rather than built around; a second Graph round-trip there would be unjustified scope. TRACE USED REAL DB ROWS: out_1783917319399_5ilp's meeting_id is the exact id the handler carries at :1311, so requests' 'channel only, never this row's thread ts' precaution is verified against the real pseudo-key rather than assumed. Told 2026-07-14T09:45Z != executed 2026-07-13T10:30Z -> one correction -> only THEN does step 2 close the original, so resolved/meeting_moved becomes a truthful record. And the real tape's duplicate move (audit_log 7560, byte-identical to 7556, 112s later) is stopped one gate EARLIER: told now equals executed, so it is not contradicted at all - no chasing"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","ref":"notbookable-narrated-as-workable","finding":"three hard-blocked slots were narrated to the owner as 'tighter than your usual, workable if you want to push through' and then to a COLLEAGUE as 'works on his end', causing him to send a real invite onto a booked slot","rootCause":"src/utils/availabilityPreCheck.ts:673 (pre-fix) - the hard tier rendered as the bare string `- <when>: NOT BOOKABLE` with violation_kind, violation_label and overCommitment{subject,attendeeCount,window} all discarded","verdict":"built","state":"built","note":"MANAGER HYPOTHESIS WAS FALSE and the lane disproved it: `viewer` is NOT role-derived here - availabilityPreCheck.ts:421 hardcodes viewer:'other' on EVERY call, MPIM or not, and in checkSlot (scheduleRules.ts:521-529,:574,:582) viewer scopes only the colliding meeting's SUBJECT string and first-vs-third-person voice; violation_kind is returned regardless. fca4b11's broken_rule_label gating lives in findAvailableSlots.ts:1201, the SEARCH path, which never ran this turn (no find_available_slots in the tape). So the reason was never withheld for leak-safety - THE RENDERER THREW IT AWAY, next to a header handing the drafter the overridable vocabulary ('outside his usual hours', 'day-load protection', 'it is HIS to override, do NOT flatly refuse'). The one non-negotiable tier was the only tier with nothing to hold onto. G3 violation; the file's own D6 comment at :236-245 had already found exactly this for the vacation sub-case. UNRESOLVED AND OWNER-SETTLEABLE: whether Tue/Wed were HARD (owner_busy_collision) or SOFT (escalatable) CANNOT be determined from disk - the 'verdicts injected' log records counts only, `events` is an activity feed not a calendar mirror, and the exact reply (pulled from conversation_threads, not paraphrase) is compatible with BOTH: 'tighter than your usual / not clean as-is' is the ESCALATABLE render's own vocabulary AND is also sitting in the header where a bare hard line can borrow it. Fix made correct under both readings; soft tier deliberately NOT flattened. WHY claim_checker PASSED - structurally blind and correctly so: its inputs are reply+toolSummaries+bookingOccurred (claimChecker.ts:39-66) and its single rule is 'did the assistant claim to have DONE an external action'; 'Wednesday works on his end' is a STATE claim and no availability verdict is in its context. Fix built BESIDE it, not in it - folding in would give one guard two jobs, two remedies and an owner-path-only scope that cannot cover the colleague's own turn. BUILT: new src/utils/availabilityGate.ts - a per-OWNER in-memory ledger of instants checkSlot established as hard-blocked (45-min TTL, 8-entry cap, derived from the incident's own latency: owner replied +2min, colleague +24min), hardBlockClassPhrase reading violation_kind and returning null = say blocked and give NO reason, a CLOSED HARD_FLOOR_KINDS allow-list (owner_busy_collision, vacation_or_off_day, in_the_past) so the soft tier can never arm the destructive half, a Haiku three-value-enum detector over OUR instants (structured fields only, hallucinated index is a no-op), one tool-less Sonnet rewrite vetoed by the existing rewriteDroppedAFact. runOutputGates.ts +88: runAvailabilityFloorAndMaybeRewrite called ONCE above the leg split at :181 so the fact is reader-independent and the rewrite is still leak-scrubbed, voice-checked and date-verified by whichever leg follows; free pre-filter = fresh ledger non-empty AND no calendar mutation this turn (bookingOccurred / carried mutated=book) AND non-empty draft. NOT BUILT, judged: the 'Sorted, and locked in for Wednesday' claim - Luke said 'I will send an invite', so the calendar entry is HIS and 'locked in' narrates the AGREEMENT not Maelle's action, the same third-party carve-out claimChecker's own prompt already makes; a new claim rule there would corrupt correct replies. It is covered by the floor anyway for the right reason (presented_available on a hard-blocked slot). 10/10 traced including the must-not-break cases: soft tier byte-identical, genuine bookable slot skipped before any import or LLM call, 1:1 owner DM untouched (pre-check never runs there, buildTurnContext.ts:561), and owner-books-over-it stands the floor down via mutated=book. RESIDUALS STATED: if the rewriter drops a token rewriteDroppedAFact pins, the veto ships the ORIGINAL - the lie survives as a safe miss; a purely owner-path lie is uncovered because the pre-check that establishes ground truth is colleague-path only; the ledger is IN-MEMORY so a restart clears it and the floor silently stops protecting until the next pre-check. CHARTER TENSION DECLARED (G2): this is a 5th output gate. The lane checked for an existing owner (claimChecker blind and owner-scoped, dateVerifier answers a different question) and for a root fix that removes the need (the two roots above, both built) but neither can stop a drafter reinterpreting prose. G9: chose the active form over shadow-first because the miss is an external hard-to-undo booking. Cost: zero on a clean turn (module not even loaded), one Haiku per reply inside a live window, one Sonnet only on a flag - ~4 windows all day on 07-27"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","ref":"mpim-envelope-eats-thread-context","finding":"on a group turn the availability pre-check reads the machine transport preamble instead of the message, so it checked the WRONG WEEK and had no verdict at all for the dates the reply asserted","rootCause":"src/utils/availabilityPreCheck.ts:131 (pre-fix) - m.content.slice(0,400) runs on the STORED message, which on any group turn begins with the ~364-char preamble written at src/connectors/slack/app/handlers.ts:634","verdict":"built","state":"built","note":"FOUND BY THE LANE, NOT IN THE BRIEF - and it is the bigger root of the two. On turn 2 the thread block read only 'Maelle \\nBunnings next week - important call- do you have a pref' and the bullet list holding 'Tuesday 11 August' was cut off. PROOF IN THE LOG, NOT INFERENCE: turn 2 queried start:2026-07-26T21:00:00Z end:2026-08-02T20:59:59Z (logs/maelle-2026-07-27.log:208) - THIS week - so the two verdicts at :210 were for Jul 28/29 while the draft asserted 11/12 August. On the turn that produced the external-facing 'works on his end', the pre-check had NO verdict for the instants named. This CORRECTS the Manager's own report, which cited :210 as proof she had re-confirmed the August slots were blocked. It also kills the obvious design: carrying turn-1's verdicts forward would NOT have caught turn 2, because turn 2's failure was that the dates never arrived. FIX: stripTransportEnvelope before the 400-char cut, same structural shape processMessage.ts:423 already uses; the only new regexes match machine-envelope DELIMITERS (names sit inside), so multilingual is unaffected. Also in the same file: the hard-block line now carries the class phrase plus an explicit 'this is a FACT, not a preference - not workable/tight/possible/fine-if-he-pushes, do not offer to book it, never a reason other than this one'; the header now separates the two tiers explicitly; the all-day-OOO branch folded into the same path with its 'offer another DAY' instruction preserved; every not-bookable verdict recorded for the floor; fmt hoisted to formatSlotDisplay so the ledger and the prompt name a slot identically"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","ref":"no-alternatives-when-all-blocked","finding":"when every tested slot came back not-bookable the colleague point-check returned verdicts and stopped, so the drafter's only move was to ask permission to look ('want me to pull other options that week instead') - the owner answered 'where is my options?'","rootCause":"src/utils/availabilityPreCheck.ts:535 - the point-check completes its verdict loop and returns verdicts ONLY, never computing the alternatives the booking path computes for the identical condition","verdict":"needs-dependency","state":"open","note":"ENGINE BUILT AND TYPECHECK-GREEN; NOT YET WIRED, so it does nothing until the 8+2 line ask lands - deliberately NOT recorded as `built`, because a closed row would hide an outstanding dependency. PROVED M2 VIOLATION, three surfaces run the same checkSlot and give two different answers on a failing named time: booking (planMeeting.ts:608-710 -> :944-952) ran a nearby search and returned propose_alternative; search (ops/handlers/findAvailableSlots.ts:1280) says 'say this plainly then offer the alternatives in `slots`'; point-check (availabilityPreCheck.ts:535) returned the verdict and stopped. ROUTING CONCLUSION - CODE, NOT PROMPT, and the lane argued it rather than assumed it: (1) she already knew the tool existed, she OFFERED to run it, so a prompt line asking her to call a tool is the enforcement-by-prompt class this project has been burned by; (2) alternatives must clear the SAME bars the refused slot was measured against - planMeeting.ts:654-665 documents what happens without that, a colleague refused for 'needs 4h notice' was offered times that were SOONER, which is a computation not a judgment; (3) the prose only renders instants the code already computed, so it is not a behavioural instruction and not a context change. BUILT: new src/skills/meetings/nearbyAlternatives.ts (~300 lines, ~half comments) holding findNearbyAlternatives - THE nearby-compliant-alternatives search, MOVED OUT of planMeeting with one generalization (it takes the SET of days the asker named, not a single day) so there is one implementation rather than a second copy, which would have been the M2 sin again - plus blockedSlotAlternativesBlock which owns the predicate, the search, the stash write and the rendered block, predicate at the definition NOT the call site so trigger and computation cannot drift. planMeeting.ts -101/+27 = NET -74, the inlined search replaced by one call, 5 now-unused imports removed with no orphans. weTimeResolver.ts +38: profileDualClock(profile, viewer), one profile-bound dual clock replacing planMeeting's inline whenText closure, so both surfaces render the same instants for the same reader (two identical closures are one edit from disagreeing). HONESTY CAVEAT HONOURED BY CONSTRUCTION not wording: the block makes NO claim about the proposals - it says 'Not one of the times I checked above came back bookable' (scope word = tested) and carries a mandatory line forbidding any COUNT ('none of your three', 'all of them are booked'), because a thrown checkSlot pushes no verdict (:498) and pairs cap at 6 (:387), so with 2 tested of 3 proposed nothing in context asserts anything about the third. ANCHOR MODE: one named day -> pickSpreadSlots 'exhaustive', byte-identical to today (D3 'if he asked thursday, its thursday'); SEVERAL named days -> 'first_round' for day-diversity, because anchoring exhaustive on Tuesday would have answered 'options that week' with Tuesday five times. NOT CHANGED: viewer still not forwarded to the search (planMeeting never passed it and the default is the colleague-safe one that masks a private WE subject before it reaches an over_optional tag, findAvailableSlots.ts:1093 - forwarding would be a silent M12 change); no override path widened, no relaxed / allowRelaxed / ignore_attendee_availability, initiator:'colleague' holds the colleague lead time, the effectiveRole:'colleague' clamp untouched. 14/14 traced including planMeeting byte-equivalence across all nine findAvailableSlots args, the label bytes and the throw->escalate_approval fallback. DEPENDENCY ASK ROUTED BY THE MANAGER: 8 lines in availabilityPreCheck.ts (guard) - import, two optional params channelId/threadTs, a testedDurations array, a push after checkAt, and the call between the recorder loop and renderPromptBlock (order load-bearing, the block's text refers to the lines above) - THEN 2 lines in buildTurnContext.ts:566-572 (outer) passing channelId/threadTs. GUARD MUST GO FIRST: the params are optional-additive, so the reverse order is an excess-property type error. Reason the call cannot live at buildTurnContext instead: detectedCategory and the validated per-pair durations do not reach that far, and planMeeting.ts:661 records the cost of a missing category - the alternatives could break the very per-day cap the request broke"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","ref":"unresolved-attendee-vanishes","finding":"NON-BUG in the form stated - the client company name 'Bunnings' failing to resolve as a person does NOT produce a silently partial availability answer; the handling already exists and is correct","rootCause":"src/core/orchestrator/buildTurnContext.ts:266-273","verdict":"already-fixed","state":"built","note":"THE MANAGER'S TWO MECHANICAL CLAIMS WERE BOTH FALSE and the lane disproved them from code. (1) 'the failure went nowhere' - FALSE: buildTurnContext.ts:266-273 fires on exactly unresolved.length > 0 and injects a `## MEETING PARTICIPANTS` block reading 'Named, but NOT matched to an internal colleague: Bunnings... If external (candidate / another company / personal domain) - search Idan's side and show his open times NOW; you only need their email at BOOKING.' The failure is handled and the handling is CORRECT for what Bunnings is - owner-only availability is the designed and right answer for an external company name. (2) 'log :185 then queried exactly one calendar' is not an attendee query at all: that line is availabilityPreCheck.ts:423-428 fetching params.profile.user.email, and the point-check is owner-scoped BY CONSTRUCTION, saying 'per Idan's rules' on every line. No attendee query was dropped; none was attempted, because NO SEARCH TOOL RAN - which is the no-alternatives-when-all-blocked root, not a resolution-failure root. Luke was never an attendee because classifyTurn put only 'Bunnings' in meetingPeople (log :181), and 'Bunnings' failing to resolve carries ZERO information about whether Luke is on the call - trigger and remedy are unrelated facts. It would also have changed nothing: Luke PROPOSED those three times. RECOMMENDED AGAINST the Manager's preferred option-1 thread-participant fallback, as a judgment not a punt: adding a thread participant to the attendee set because a DIFFERENT name failed to resolve is an inferred widening of the attendee set, the most expensive bug class in this subsystem - findAvailableSlots.ts:292-303 (#145b) exists because a 4->7 balloon returned 0 slots on a day everyone was free, and SESSION_STARTER still carries addFromEvent union-vs-replace as open with the same shape. A colleague in an MPIM is often an attendee, not always ('can you set up Idan with the client?'), and when he is not, the cost is silently deleting the owner's options - an M10 violation produced by code, invisibly. Deterministic is not the same as correct; this is a deterministic GUESS. Option 2 (ask who is on the call) is the M4 anti-pattern the owner had just complained about. Option 3 (state the limit) is already present ('per Idan's rules') and adding it again is prose in guard's file. The invariant HOLDS today: every availability statement the pre-check feeds is explicitly owner-scoped and no code path presents it as multi-attendee. OWNER WAS RIGHT: he said 'Bunnings is the client name, but i don't think we can easily fix this LLM mistake' and declined to chase it; the Manager pushed back arguing the handling was fixable, and the handling was already correct. The classifier itself (company name -> meetingPeople) remains deliberately untouched"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","ref":"precheck-claims-booking-parity-it-does-not-have","finding":"the availability point-check tells the drafter its answer matches what happens at booking time, but the booking path additionally checks internal-attendee free/busy which the point-check never does - so a BOOKABLE verdict can still meet an attendee-busy heads-up at booking","rootCause":"src/utils/availabilityPreCheck.ts:766 (the closing claim) vs src/skills/meetings/planMeeting.ts:713+ (the extra check)","verdict":"needs-owner-decision","state":"open","note":"PROVED, not suspected - found by meeting while tracing the alternatives work, deliberately not bundled into it (different root, M2). Harm is SOFT: the booking-time attendee check informs, it never refuses, so the consequence is a heads-up the point-check did not predict rather than a wrong action. The false sentence lives in GUARD's file, so fixing it is a cross-lane ask - either narrow the claim to what the point-check actually verifies, or teach the point-check the attendee leg. Not dispatched: the owner has two other decisions open on this incident and this one changes no real-world outcome"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","ref":"self-mention-adds-maelle-as-attendee","finding":"LATENT, never proven to have fired - the search resolves every <@Uxxx> in the last user message via getPersonMemory and skips only the OWNER's id, so an @Maelle mention could add her own self row's email as a search attendee","rootCause":"src/connectors/graph/findAvailableSlots.ts:343-378","verdict":"needs-owner-decision","state":"open","note":"Flagged by meeting during the alternatives trace; NOT built and NOT dispatched. Owner-path-gated and near-unreachable in a 1:1 DM (the owner does not @-mention her there), but every MPIM turn in this incident carried a literal <@U0ARK5814PQ> self-mention, which is why it surfaced now. Would show as a phantom attendee narrowing the search. One-line skip on the bot's own id is the obvious shape; left to the owner because it has never been observed firing and the fix touches a hot path"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","ref":"no-alternatives-when-all-blocked-WIRING","finding":"the alternatives engine wired into the colleague point-check - and the wiring itself created a NEW regression risk that had to be closed before it could be called safe","rootCause":"src/utils/availabilityPreCheck.ts - five edits: import, optional channelId/threadTs params, testedDurations array, push after checkAt, and the blockedSlotAlternativesBlock call after the ledger loop and before the render","verdict":"built","state":"built","note":"Each anchor RE-DERIVED semantically rather than applied by the Manager's line numbers (they happened to be correct, but the file had just been edited by this same lane). testedDurations.push sits in the NON-GAP branch only, which is correct: a gap query probes several durations and validates no single length, so it contributes nothing and the call falls back to durationMinutes - the same default the point-check itself used; the `.length > 0` ternary is what keeps Math.max() off an empty array. THE SEAM DID NEED A FIX, and it is the exact 'two fixes each correct alone and wrong together' class the combined-verify discipline exists for. STRUCTURALLY the detector cannot see an alternative: the ledger has one writer, recordHardBlockedSlot, called from one place - the loop over verdicts where !bookable AND the kind is in the closed HARD_FLOOR_KINDS; alternatives are computed AFTER that loop by a different module and are never recorded, so freshHardBlockedSlots can only return blocked instants, the tool schema is {index,treatment} and any out-of-range index or non-presented_available treatment is discarded, so an alternative has no index to be reported under. An alternative also cannot BE a ledger instant, since it comes from the rule-aware search under the same lead time and category and so cannot be a slot checkSlot rejects. BUT THE REAL RESIDUAL WAS SEMANTIC CONFLATION, NEW AS OF THIS WIRING: before today a draft rarely named a free slot beside a blocked one; now the HEALTHY draft is exactly that ('11:30 doesn't work, but 14:00 that day is free'), often an hour apart on the same day. Haiku reading that as 'the draft says Tuesday works' would flag the 11:30 entry and rewrite a CORRECT reply into a refusal of a slot that IS free - worse than the original bug. HARDENED rather than declared safe: the detector prompt now states only the exact listed clock times are in scope, carries that case verbatim (listed 11:30 + offered 14:00 => presented_blocked, the offer irrelevant; offer alone with nothing said about 11:30 => not_mentioned) and forbids answering about a whole DAY when the entry is one clock time. Two further layers unchanged: rewriteBlockedSlotClaim's keep-veto, and rewriteDroppedAFact vetoing any rewrite that drops the alternative's own clock time. All three fail open, so the worst case is the honest draft shipping untouched. detectAffirmedBlockedSlots' doc now records WHY the constraint exists, so the next reader does not remove it. 5/5 traced, including one-bookable returning '' before ANY I/O (promptBlock byte-identical to pre-wiring, zero Graph calls, zero added latency) and the 3-proposed/2-tested case anchoring only on the tested dates while nothing asserts a count. buildTurnContext.ts deliberately untouched - its 2 lines routed to outer"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"outrider","ref":"no-alternatives-when-all-blocked-WIRING-outer","finding":"channelId/threadTs now reach the colleague point-check, so the offered alternatives bind to the thread instead of being written under a key nothing reads","rootCause":"src/core/orchestrator/buildTurnContext.ts:566-582 - the single precheckAvailability call site","verdict":"built","state":"built","note":"+10/-0 (2 code, 8 comment). No cast, no non-null assertion, no ??. CORRECTED THE MANAGER'S BRIEF, and it raises the stakes: I described the pair as an improvement (without them the slots are 'offered but unbound'). The real reason both are required is offeredSlotsStash.keyFor (src/utils/offeredSlotsStash.ts:50-52) - it keys a 1:1 DM by channelId ALONE but keys GROUP surfaces as `${channelId}|${threadTs ?? '_none_'}`, while the read side is getOfferedSlots(input.channelId, input.threadTs) at buildTurnContext.ts:623. So on an MPIM, passing the channel but omitting the thread would file the alternatives under `C...|_none_` while the binding block looks up `C...|<ts>` - silently offered-but-never-bindable, i.e. the very failure being closed, still present but harder to spot. Passing BOTH is what makes write-key and read-key identical. FIELD NAMES VERIFIED not assumed: input.channelId / input.threadTs are declared on OrchestratorInput (core/orchestrator/index.ts:16-17) as REQUIRED non-nullable string, so they satisfy guard's optional params with no coercion. Used the input. form rather than the bare destructured threadTs at :18 to match the adjacent recordOfferedSlots write (:595-596) and stash read (:623). Exactly ONE precheckAvailability call exists and it is the one gated on senderRole==='colleague' at :561. ALSO DISPROVED a scenario the Manager asked for: 'a turn with no thread' does not exist on any live path - every Slack entry point computes thread_ts || ts (handlers.ts:117,:310,:370,:448,:567,:905) so a top-level message becomes its own thread root, which is why the required string type is honest. Closest real case is a colleague's top-level 1:1 DM where channelId starts with D and keyFor ignores threadTs entirely, so write/read still agree. Hypothetical falsy values degrade rather than throw: empty channelId early-returns at helpers.ts:89, empty threadTs on a group yields key `C...|` which the read reproduces identically, and the whole block sits inside the try/catch at :602-606. whatsapp.ts:288-289 sets both to message.from, non-empty and consistent on both sides. 3/3 traced; 1:1 owner DM unreachable because the :561 gate is false and the two lines live inside that call's object literal. tsc --noEmit exit 0"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","ref":"no-alternatives-when-all-blocked","finding":"SUPERSEDES the earlier needs-dependency row - the alternatives engine is now fully wired end to end across three lanes","rootCause":"src/utils/availabilityPreCheck.ts:535 (original root) - fixed via src/skills/meetings/nearbyAlternatives.ts + the guard wiring + the outer call-site pass-through","verdict":"built","state":"built","note":"Chain complete and ordered as required: meeting built the engine (nearbyAlternatives.ts, planMeeting -74 by MOVING not copying the search, weTimeResolver +38 for the one shared dual clock) -> guard wired the call into availabilityPreCheck and hardened its own detector against the new same-block conflation risk -> outer passed channelId/threadTs so the offered slots bind to the thread. GUARD-BEFORE-OUTER ordering was load-bearing: the params are optional-additive, so the reverse order is an excess-property type error. Typecheck green at every step and after the last. NOT YET VERIFIED CROSS-LANE at the time of this row - a combined adversarial pass over all 7 source files was dispatched before deploy, prioritising the planMeeting extraction equivalence (the one substantial claim checked only by its own author) and the three-lane prompt-block seam"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","ref":"COMBINED-VERIFY-pre-deploy","finding":"pre-deploy adversarial pass over all 7 uncommitted source files: 1 BLOCKING, 7 worth-knowing, 2 cosmetic; the planMeeting extraction the Manager was most worried about came back CLEAN","verdict":"audit","state":"built","note":"Findings-only, nothing built. Run as a FRESH guard agent so the three files guard had authored were reviewed as someone else's code. tsc green. BLOCKING B1 - the hard-block ledger is never invalidated by newer, better knowledge (availabilityPreCheck.ts:563 record loop + availabilityGate.ts:158-183 + runOutputGates.ts:775-780): entries clear only by TTL, by re-blocking the same instant, or by forgetHardBlockedSlot after a rewrite lands, so a freshly recomputed bookable:true for that exact instant (the loop `continue`s past it) and a completed calendar mutation (stands down for THAT TURN only, then returns) both leave a stale entry armed. Scenario A: colleague asks Tue 11:30 -> blocked -> owner moves the clash -> next turn 'is 11:30 open now?' re-checks BOOKABLE -> draft says yes -> floor rewrites to 'it clashes with a meeting already on Idan's calendar'. Scenario B needs NO mutation at all: HardBlockedSlot is keyed by instant with NO duration, so a 60-min ask blocked by a 12:15 meeting leaves an entry that rewrites a truthful 'yes, 25 minutes at 11:30 works' - shortening a meeting is an ordinary negotiation move. Worse than the incident in one direction: that was an over-optimistic claim the owner could catch, this is a negative the colleague cannot challenge. Routed to guard. WORTH-KNOWING: W1 the owner's overlap-confirmation turn is exposed because the stand-down keys on a COMPLETED write and needs_confirmation/needs_owner_approval are explicitly not ok (turnHelpers.ts:141), so the M3 one-step book-through offer gets rewritten into a flat refusal - bundled to guard. W2 the alternatives block fires on soft-tier-only turns and its 'do NOT ask which of the blocked times to push through' sits below a verdict line saying 'it is HIS to override, raise policy_exception on insist', quietly narrowing #128's escalation scope - routed to meeting with instructions to RESTORE the status quo, not redesign it. W3 the new hard-block text narrows the OWNER inside an MPIM (the pre-check runs on senderRole==='colleague', which the clamp makes true for him) - verifier confirmed NO override path was widened anywhere in the diff, this is a narrowing and an OWNER DECISION, surfaced not fixed. W4 the floor's pre-filter is 'ledger non-empty' not 'this draft could touch a blocked instant', so once any colleague point-check hard-blocks a slot EVERY reply for that owner for 45 min pays one serialized Haiku call, including 1:1 DMs and unrelated threads - not a defect, but 'zero cost on a clean turn' is true only OUTSIDE the window; explicitly do NOT fix with a clock-time pre-filter because 'Tuesday works on his end' has no clock time. W5 nearbyAlternatives.ts:195 spreads overOptional into every alternative while PlanAction declares Array<{start;end;label}>, so the property exists at runtime invisible to the type; probably not a leak (viewer never forwarded, checkSlot defaults to 'other', so over_optional is subject-masked) but the verifier could NOT check how createMeeting.ts:828 / moveMeeting.ts:1127 serialize alternatives - routed to meeting as a possible leak that blocks deploy. W6 the floor catches 'Tuesday 11:30 works' but NOT 'Tuesday works' - the incident's sentence class minus the clock reports not_mentioned and is missed, so the header's 'may never be described to anyone as workable' overstates coverage; the incident's actual sentence carried a range and fires correctly. Also the listed instants render in ENGLISH while a draft may be Hebrew, making the backstop materially weaker on the Hebrew colleague path (failure direction is a safe miss). W7 verifiedClean's P25/P32 entry grew a dependent - invariant re-derived and INTACT, but reverting P32 would now additionally arm an output-time rewrite; entry text updated. COSMETIC: availabilityPreCheck.ts:557-559 claims the alternatives call must sit after the ledger loop 'so the floor has recorded first' - it does not, the block never reads the ledger; only appending BELOW the verdict lines is load-bearing. And the pre-existing :798 header 'I pre-checked the times in this colleague's question' implies all proposals when only the tested ones were checked. COULD NOT BREAK: the planMeeting extraction (all nine findAvailableSlots args identical in value/shape/order, FORWARD_WORKDAYS walk, 'exhaustive' preserved for the single-day caller so D3 holds, slotLocalDay partition, label bytes for BOTH viewer values, no per-call/once movement, no orphaned imports); the moved catch boundary is strictly safer since Settings.throwOnInvalid is set nowhere in src so an invalid date yields 'Invalid DateTime' filtered by .isValid; weTimeResolver purely additive with the lazy require inside the factory body; the detector hardening against same-day and 30-min-away alternatives; all three vetoes verified to fail open individually; count honesty; no override widened; no schema change; the gate cannot throw into postReply; zero added latency when any slot is bookable. Also checked unasked and safe: mutated=book substring matching exactly the calendar family, the double stash write merging rather than overwriting and preserving searchFingerprint, the alternatives search unable to offer a past instant (findAvailableSlots.ts:922), and stripTransportEnvelope matching the real preamble delimiters. NOT COVERED, stated: the two serializers in W5; no model exercised (detector assessments are prompt-reading not measurement - shadow for a day if a false-fire rate is wanted); no log greps"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","ref":"B1-stale-hardblock-false-refusal","finding":"BLOCKING defect INTRODUCED by the floor fix and caught by the pre-deploy verify: a hard-block ledger entry outlived the fact, so a truthful 'that time works now' was rewritten into a confident FALSE REFUSAL with a fabricated reason","rootCause":"src/utils/availabilityPreCheck.ts (the record loop `continue`d past a bookable verdict) + src/utils/guards/runOutputGates.ts:775 (the mutation stand-down was per-turn, not an invalidation)","verdict":"built","state":"built","note":"Worse than the incident it fixed in one direction, which is why it blocked: the original was an over-optimistic claim the owner could catch, this was a negative the colleague cannot challenge. FIXED IN THE THREE PLACES KNOWLEDGE CHANGES: (1) the record loop now INVALIDATES as well as records - startDt/instantIso computed before the branch, v.bookable => forgetHardBlockedSlot(email, instantIso) then continue, not-bookable => re-record which refreshes the TTL; a fresh bookable verdict is the newest possible knowledge for that instant (same validator, same calendar, milliseconds old) so it wins over any stored entry. (2) the mutation branch calls new clearHardBlockedSlots(profile.user.email) before returning instead of standing down for one turn - a move VACATES one slot and FILLS another, so no entry survives a completed mutation, not just the one touched. (3) availabilityGate's TTL paragraph replaced with an explicit INVALIDATION contract naming all three rules with TTL as the LAST line rather than the only one, and an honest residual that no longer claims an external actor is required. THE DURATION KEY WAS REJECTED, and for a better reason than 'subsumed': storing it unread is a set-but-unread field, and GATING THE FIRE on it would break the tail-overlap case - a 60-minute ask refused because it runs into a 12:15 meeting, then sold as '11:30 works'. Re-verifying at the shortest allowed duration before recording was rejected for the same reason: it would silently drop the incident's own slot whenever the collision is a tail overlap. Recorded in the module header so a later reader does not re-litigate it. THE REGRESSION TO FEAR WAS TRACED EXPLICITLY (scenario 3): a still-blocked slot re-checks blocked, takes the else branch and is RE-RECORDED with a fresh expiry, so it stays armed and a later turn with no pre-check still fires - only a DISPROVEN entry is removed. Scenario 6 re-traced the original incident end to end and the catch still fires. Scenario A is closed TWICE OVER on purpose (either rule alone suffices, and the pair also covers a mutation on a turn the floor never sees, e.g. a deferred replay). RESIDUAL STATED IN CODE: an event cancelled or moved by SOMEONE ELSE inside the 45-min window, or a shorter meeting proven to fit at that start by another tool with no fresh point-check to clear the entry"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","ref":"W1-owner-confirmation-turn-rewritten","finding":"the owner's overlap-confirmation turn was exposed: 'this clashes with your 12:15 - double-book anyway?' would have been rewritten into a flat 'that does not work'","rootCause":"src/utils/guards/runOutputGates.ts:775 keys the stand-down on a COMPLETED write, and src/core/orchestrator/turnHelpers.ts:141 makes needs_confirmation / needs_owner_approval explicitly NOT ok, so that turn carries no mutated=book while the ledger entry is still fresh","verdict":"built","state":"built","note":"TWO DISTINCT FIXES IN ONE FILE, not one - B1 is data freshness (an entry outliving the fact), W1 is MEANING (an honest draft that names the clash). Neither closes the other: the confirmation turn carries no completed-mutation marker BY DESIGN so B1's clear cannot fire there, and no spec wording rescues a stale entry. FIX: the detector spec's presented_blocked now explicitly includes a draft that STATES the clash and then asks whether to go ahead anyway - the reader has been told the truth and is being asked to decide, which is an override offer, not a claim of availability. CRITICALLY the spec also states a HEDGE is not that: 'tight', 'not clean', 'not ideal', 'tighter than his usual' never tell the reader the slot is TAKEN, so the 2026-07-27 wording still reads presented_available and still fires - the fix does not defang the catch it was built for (counter-checked in trace 4). A DETERMINISTIC trigger was looked for and deliberately NOT built: the only output-time signal would be 'a calendar tool name appears in the tape without mutated=book', which means re-introducing a tool-name list into a guard - exactly the guessing G3 removed from the claim-checker's shield - and it would also stand the floor down on a genuine FAILED. So W1 is drawn where it is genuinely a question of meaning, with the rewriter's keep-veto as the second layer. Also fixed here rather than in a second dispatch: the :557 comment no longer claims the ledger loop must precede the alternatives call (only appending BELOW the verdict lines is load-bearing; the block never reads the ledger)"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","ref":"W5-overoptional-in-colleague-payload","finding":"CONFIRMED REAL, not theoretical: an undeclared runtime field carrying the owner's soft day-mechanics - including the raw subject of a NON-PRIVATE optional event - would have ridden into every colleague-path propose_alternative","rootCause":"src/skills/meetings/nearbyAlternatives.ts:195 spread overOptional into every alternative while PlanAction declares Array<{start;end;label}> (planMeeting.ts:221,:417); both serializers attach the arrays WHOLESALE by reference - createMeeting.ts:849-850 and moveMeeting.ts:1146-1147","verdict":"built","state":"built","note":"Those returns are TOOL RESULTS, so they are serialized into the model's context - the verifier flagged this as an unverified gap and it turned out to be a live path. THE VERIFIER'S REASSURANCE WAS HALF WRONG and the lane re-derived it rather than accepting it: checkSlot sets the string at scheduleRules.ts:574 as displaySubject(ev, profile, viewer) || 'an optional meeting', and viewer does default to 'other' (scheduleRules.ts:521) with findNearbyAlternatives never forwarding one - BUT displaySubject (displaySubject.ts:83) returns [Private] ONLY when isEventPrivate, otherwise the RAW SUBJECT. So a private optional block was safe; a non-private one (Lunch, Gym, a dentist slot) would have shipped its real subject. Precisely: NOT a hard M12 breach (M12 permits a colleague seeing a subject) but an accidental widening of a colleague-facing payload with the owner's soft day-mechanics, which that surface is explicitly told never to surface (availabilityPreCheck.ts:687-689). FIX at planMeeting.ts:628-643: project to the three declared fields at the boundary via a fresh literal, so plan.alternatives is now PROVABLY {start;end;label} and the byte-equivalence claim covers the payload, not just the nine arguments. The field STAYS declared on NearbyAlternative because it has exactly one real reader (declared :97, produced :210, read :327) which takes it off findNearbyAlternatives' own return and never off a PlanAction. Unaffected: alternativesNote reads only .length, recordProposedAlternatives reads only .start, and planMeeting.ts:508/511's ruleResult.overOptional is a different field on a different object (the owner's own second-person heads-up), untouched. Projection sits inside the >0 branch so the empty->escalate_approval fallback is unchanged"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","ref":"W2-alternatives-block-suppressed-escalation","finding":"the alternatives block fired on soft-tier-only turns and its 'do NOT ask which of the blocked times to push through' sat directly below a verdict line saying 'it is HIS to override, raise policy_exception on insist' - quietly narrowing #128's escalation scope","rootCause":"src/skills/meetings/nearbyAlternatives.ts:263-264 - the trigger is 'no TESTED slot was bookable', which is also true when every verdict is one of the eight ESCALATABLE kinds","verdict":"built","state":"built","note":"THE LANE TOOK NEITHER SHAPE THE MANAGER OFFERED and its reasoning was better. (a) Gating the trigger on 'no owner-overridable block' would preserve escalation but RECREATE THE M2 DIVERGENCE IN REVERSE: the booking path has always given the soft tier alternatives AND kept the escalation (createMeeting.ts:854 / moveMeeting.ts:1151 'Do NOT escalate yet... if they INSIST... THEN call create_approval'), so gating hard-only would make the point-check STINGIER than the booking path for the same slot. (b) Scoping the sentence to hard blocks needs this file to classify tiers, i.e. either duplicating guard's ESCALATABLE set (availabilityPreCheck.ts:709-712) or importing availabilityGate while guard was editing it - and the pre-check's own comment at :541 warns against exactly that drifting copy. CHOSEN: scope-by-DELETION - remove the suppressing instruction, keep alternatives for BOTH tiers, so this file now holds no copy of the tier split at all. The sentence deserved deletion on its own merits: it was a THIRD copy of a rule two other layers already own (the hard-block verdict line at availabilityPreCheck.ts:758 and guard's output floor). The lane's own words: 'I stacked a layer on a rule another lane enforces, and it over-reached onto the soft tier. My own charter says to trust the layer behind me; I didn't.' New closing text defers explicitly, in the same last position, to each verdict line's own remedy including 'where it tells you the time is Idan's to override and to raise it for his decision'. No new policy, #128 scope untouched, no fourth owner decision. Reasoning recorded in the file header so it does not get re-added. 5/5 traced including the mixed hard+soft turn the old sentence broke worst"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","ref":"W3-hardblock-text-narrows-owner-in-mpim","finding":"the new hard-block text narrows the OWNER's own direct-book inside an MPIM: his '11:30 Tuesday, book it' now lands under 'This is a FACT... do NOT offer to book it'","rootCause":"src/core/orchestrator/buildTurnContext.ts:561 runs the pre-check whenever senderRole === 'colleague', and the MPIM clamp makes that true for the owner's own messages","verdict":"needs-owner-decision","state":"open","note":"SURFACED NOT FIXED - deliberately. The pre-deploy verify confirmed NO override path was widened anywhere in the 7-file diff (zero relaxed / allowRelaxed / ignore_attendee_availability / grantRelaxed / role-clamp changes); this is the opposite - the owner-only direct-book chain got NARROWER on one surface as a side effect of a colleague-facing fix. Same MPIM-authority seam already raised as an M6 signal on this incident: the owner has less authority in his own group chats than in a DM, and nothing tells him when a permission he just granted was discarded (he wrote 'it can be tight' on 2026-07-27 and, clamped to effectiveRole:'colleague', that authorization was structurally void). A side effect is the wrong way for a policy about the owner's own authority to change, so it needs his explicit call rather than a lane's judgment. LIVE AS OF 2026-07-27T09:45:48Z - the narrowing is in production while the decision is open"}
{"date":"2026-07-27","runId":"wf_b81e5b49-35b","lane":"matchmaker","ref":"A-requester-timezone-frame","finding":"a colleague's stated clock was read in the OWNER's zone, producing a false collision that the availability floor then used to OVERWRITE an honest draft with a confident false refusal - and making the point-check disagree with the search by exactly one hour on the same phrase","rootCause":"src/utils/availabilityPreCheck.ts:181 (pre-edit) - the Haiku normalizer's frame rule 'when no TZ is given, treat it as owner-local', plus :225 accepting a UTC-anchored instant the MODEL had computed","verdict":"needs-owner-decision","state":"open","note":"CODE IS BUILT AND IN THE TREE (+215/-75 in availabilityPreCheck.ts, +21 in buildTurnContext.ts, 20/20 traced) but the WAVE VERIFY OVERTURNED it from built, and the reason is the important part, quoted: 'a wrong requester zone does not just mis-verdict, it gets RECORDED as an ESTABLISHED hard block (:722-729) with a 45-minute owner-scoped TTL, and the floor's Sonnet rewriter is then authorized to overwrite a correct draft with a confident refusal plus a class reason. That is the G6 corruption the floor's own header forbids. Nothing else in A is in question - the frame resolution order, the wall-clock stripping and the regex-fallback parity all read correct.' TWO PROVEN CONSEQUENCES IN PRODUCTION, both AFTER the 09:45:48Z restart that deployed the floor (log line 307), i.e. the floor shipped this morning DID cause the harm it was built to prevent: (1) log :743 - Dirk (people_memory timezone=Europe/Brussels) said a bare '16:00'; it was point-checked as 16:00 Asia/Jerusalem -> owner_busy_collision -> recorded into the hard-block ledger (:582) -> the floor rewrote an honest draft into a false collision claim. His 16:00 = 17:00 Israel, which the same log proves FREE (create_meeting planMeeting verdict start 2026-08-03T17:00 level=free). (2) log :664 vs :682/:686 - the pre-check answered bookable:1 for '15:00 CET' while find_available_slots, which converts in CODE (findAvailableSlots.ts:162 reinterpretClockInZone, Europe/Brussels=CEST=+02:00 in Aug -> 16:00 Israel), returned owner_busy_collision for the SAME phrase. Point-check and search disagreeing by exactly one hour is the M2/M14 violation. FIX SHAPE: the model no longer does timezone arithmetic - the Haiku pass returns wall_clock (the clock exactly as written, no offset) + stated_timezone (OMITTED when none was named), the tool renamed normalize_slots with rules forbidding conversion outright. Code owns the frame through one helper resolveStatedZone: stated zone -> the zone the colleague WRITES FROM (getPersonMemory, the same row the search path already reads) -> the owner's, never the server's. Three hardening pieces: wallClockOnly strips any offset the model emits anyway; canonicalZone maps the legacy fixed-offset DST-less identifiers (EST/MST/HST + *5EDT forms) onto real region zones so 'EST' cannot land an hour off for eight months a year, with an unrecognised label falling back to the requester's own zone; the regex fallback applies the identical requester-frame rule. Each verdict line now carries the same instant in the asker's own zone via renderClockInZone ('Monday 3 Aug at 17:00 (= Mon 3 Aug 16:00 CEST where they are)') and formatSlotDisplay gained the optional zone so the prompt block and the ledger `display` emit an identical string - otherwise a floor rewrite would answer a question about 16:00 by naming 17:00, which is the thread's actual complaint. DELIBERATELY NOT COVERED, named: find_available_slots' own behaviour when the model omits search_window_timezone (still owner-local; no evidence it is broken and a second resolver risks double-converting); a TRAVELLING colleague (stored home zone, not getTravelRecord - strictly better than the old owner-frame but not travel-aware); weTimeResolver's away-day path. NOT the SESSION_STARTER open item 'M3 no-TZ fallback uses the owner's zone' - that was filed against attendeeAvailability.ts, the assumed WORK-HOURS frame, and is still open"}
{"date":"2026-07-27","runId":"wf_b81e5b49-35b","lane":"gatekeeper","ref":"B-floor-cites-mismatched-block","finding":"the availability floor rewrote a draft about Aug 3rd 13:00/14:00/16:00 CET citing evidence from a different DAY entirely - kind in_the_past, 'Monday 27 Jul at 00:00' - traced to a separate findAvailableSlots call earlier in the same thread","rootCause":"hypothesis (not proven): the floor detector keys off the most recently recorded established-block for the THREAD rather than scoping to the date/slot named in the draft being checked","verdict":"needs-owner-decision","state":"open","note":"NEEDS-SHAPING, deliberately NOT dispatched. Evidence: logs/maelle-2026-07-27.log:782 - 'Availability floor - the draft presents an ESTABLISHED-unavailable time as workable; rewriting' with slots:[{kind:'in_the_past', reasonGiven:'that time has already passed', when:'Monday 27 Jul at 00:00'}] attached to a draftPreview entirely about Monday 3 August. The in_the_past rejection traces back to a completely separate findAvailableSlots call at line 762 (search 07-27 to 07-29, in_the_past:60 examples) run earlier in the SAME thread. The rewriter KEPT the draft unchanged this time, so no visible harm - but the guard's own evidence was wrong, and it is live. CRITICALLY DISTINGUISHED from the fix shipped this morning: guard's hard-block invalidation (report row 102, built 2026-07-27) addresses staleness of a DISPROVEN entry, not a MISMATCHED one from an unrelated date - a different failure mode of the same mechanism family, NOT a re-occurrence of the fixed one. SHAPING QUESTION for the owner: should the detector's established-block matching be scoped per-instant/per-date so it can never cite a rejection for a different day than the draft names, and is that the same ledger row 102 maintains or a separate cache inside the floor detector? Line 782 is well past the 09:45:48Z boot at line 307, so this is post-deploy behaviour of code shipped today"}
{"date":"2026-07-27","runId":"wf_b81e5b49-35b","lane":"matchmaker","ref":"F-standing-calendar-pref-unenforceable","finding":"the calendar-health scan auto-categorized a Comeet duplicate 4 seconds after detecting it, which the owner's standing written instruction explicitly forbids - and no code path can read that instruction","rootCause":"src/skills/calendarHealth/handlers/checkHealth.ts:498-511 (the missing_category detector) and :759 (its ACTIVE-mode auto-fix) - neither has any access to the owner's standing calendar instructions, which live only as free text in config/users/idan_prefs/calendar.md whose single reader is the PROMPT at src/skills/calendarHealth.ts:315","verdict":"needs-owner-decision","state":"open","note":"NOT BUILT - the resolution is a storage-architecture choice and it contradicts an explicit stated contract, so it is the owner's call. PROVEN FROM THE LOG not inferred: :329 issueCount:6 -> :330 'Meeting updated fields:[categories]' on event ...AAjOJyJlAAA= -> :376 fixesApplied:1, i.e. the auto-categorize the pref forbids, executed 4 seconds after detection. So this is NOT the 'model didn't follow the prompt' story it looks like from the transcript. The layering makes the rule STRUCTURALLY unenforceable: the same prompt tells the model to narrate summary_text VERBATIM (:263-267) and to TRUST THE ANALYZER (:304-308) so it may not override a scan-layer flag, and in active mode the write has already happened before the model sees anything. skillPreferences.ts:12-13 states the blocking contract in so many words - the pref file is free text and 'code does not parse it'. ONE ROOT, TWO SYMPTOMS: the second flag the owner counted is the double_booking narration for the same event against a meeting Maelle herself booked (logs/maelle-2026-07-26.log:1965-1967); the pref says resolve that by deleting the Comeet copy, silently. RECOMMENDATION: keep the MD as the human record and give the scan a structured input beside it - profile.meetings.ignored_event_sources: [{organizer_email?, action:'skip'|'delete_if_duplicate'}] - consulted before the detection loop (:498), before the active-fix loop (:759) and in the double_booking detector. Match on ORGANIZER EMAIL not subject: scheduler@comeet-notifications.com is already named as the definitive Comeet signal in the owner's own YAML (config/users/idan.yaml:384), an email is language-independent, and Comeet subject prefixes are natural language that localises. action:'skip' ALONE closes BOTH reported flags with no autonomous deletion at all; ship skip first and treat delete_if_duplicate as a separate explicitly-authorised step, and even then as a narrated same-turn action rather than a silent one. THE ONE UNPROVEN FACT, named by the lane: it did NOT verify from Graph that the Einav Comeet event's organizer actually IS scheduler@comeet-notifications.com - the YAML asserts it and the subject convention matches, but the whole recommendation rests on it, so whoever builds it must confirm against a real event before keying on organizer. Escalated on three charter grounds: the storage choice contradicts a stated contract; hardcoding 'Comeet' into checkHealth.ts would be the tenant-specific hardcode skillPreferences exists to prevent; and the rule as written authorises SILENT AUTONOMOUS DELETION of calendar events, a higher-risk autonomous action than the defrag move already flagged as the riskiest path that lane owns"}
{"date":"2026-07-27","runId":"wf_b81e5b49-35b","lane":"registrar","ref":"gh#153","finding":"the requester-facing amend relay dropped the decision itself - Maayan was told 'Idan suggested a different approach. Does that work for you?' with the 55 minutes and the reason both absent","rootCause":"src/core/requests/resolver.ts:1122 (pre-edit) - summarizeCounter was a three-key WHITELIST (text/slot_iso/to) over a counter payload that is open-ended by design (R8/R9), returning '' for every other shape","verdict":"built","state":"built","note":"Verified in the DB: req_1785151198258_yiuue.details_json stores the counter as {duration_min:55, reason:'90 minutes isn\\'t workable, 55 is the max Idan can do'} - neither key was in the whitelist. Two aggravators at the same root: the LLM relay composer (:916) is gated to approve/reject so nothing recovered it, and the `reason` argument the tool accepts and relays on reject was silently dropped on amend. The same whitelist also fed the OWNER-facing bounce-back lead (:1076 -> 'countered with an alternative'). FIX, 4 files: summarizeCounter REPLACED not wrapped with a renderer that DROPS NO KEY - known keys get human phrasing (duration_min -> '55 minutes', time keys localised), any other key falls through to '<key with spaces>: <value>', nested objects/arrays recurse, ISO datetimes render in the READER's zone via an injected formatter with raw ISO as fallback. The amend gate in tasks/skill.ts switched from 'counter has >=1 key' to 'counter RENDERS to something' - the SAME predicate the relay uses, so a counter the relay would swallow can no longer be stored. No LLM in the amend path, so the decided number cannot drift (R3). Post-fix body: 'Hey Maayan - Idan suggested a different approach: 55 minutes; 90 minutes isn\\'t workable, 55 is the max Idan can do. Does that work for you?' NOT a repeat of the already-fixed P1 (which carried decided data into the relay's SUBJECT/time) - this is the counter PAYLOAD renderer, a new surface of the same class. Traced 15+ shapes including {duration_min:null} which the OLD code stored and relayed as a contentless counter and is now blocked, and {is_online:false}/{duration_min:0} which survive because the emptiness test is on the rendered string. NOT TRACED: whether {attendees:[...]} reads gracefully to a colleague - it reads clunkily but completely, chosen deliberately over an LLM paraphrase that could drift the decided roster"}
{"date":"2026-07-27","runId":"wf_b81e5b49-35b","lane":"registrar","ref":"gh#149","finding":"a task due at 10:32 owner-local fired at 13:35 - late by exactly the owner's UTC offset, not by the tick interval","rootCause":"src/tasks/skill.ts:430 (pre-edit) - create_task wrote the model's `due_at` verbatim onto next_check_at, while the sweep selects on datetime(next_check_at) <= datetime('now') (src/db/requests.ts:297) and SQLite's `now` is UTC","verdict":"built","state":"built","note":"PROVEN from the row plus the log, not inferred: req_1785137615335_8os20 stored due_at '2026-07-27T10:32:00' (log :271, created 07:33:35Z) - a bare owner-local wall clock with no offset. Compared against UTC it only came due at 10:32Z = 13:32 IL; closed_at is 10:35:49Z = 13:35:49 IL, matching the owner's '1:35 PM' exactly. The delay equals the +3 offset, not the 5-min tick. Every INTERNAL arming site already wrote .toUTC().toISO(); the invariant 'spine timers are UTC instants' was simply never enforced where a MODEL-authored time enters. FIX, 4 files: new toTimerInstant(raw, ownerTimezone) in core/requests/types.ts as the spine's ONE anchoring primitive - a bare clock means owner-local, an explicit offset/Z is preserved, unparseable returns null so callers REFUSE rather than arm a timer SQLite can never select (previously a garbage due_at hung open forever). create_task and edit_task anchor due_at and return bad_due_at on garbage; edit_task's new_due echo now derives from the normalized value (was parsed in the PROCESS zone - a latent 3h-wrong echo on a UTC VM); get_my_tasks hands the model the OWNER-LOCAL offset ISO so 'due to fire at 10:32' cannot become 07:32. FOUND AND CLOSED A SECOND REAL DEFECT IN PASSING: message_colleague passed scheduled_at regardless of isFuture, so an already-PAST send_at sent the DM immediately AND armed a past-due send_scheduled_outreach - sending it TWICE and pinning the row in phase 'outreach:scheduled'. Also removes a class of GCP-VM-only breakage: three separate readers of these strings (new Date(bare), DateTime.fromISO(bare), the SQLite comparison) each guessed a different zone and two were right only because the process runs in Asia/Jerusalem today. NOT TRACED, out of lane and flagged: the `tasks` table's own getTasksDueNow() (src/tasks/index.ts:126) uses the identical datetime(due_at) <= datetime('now') comparison and its writers were not individually verified"}
{"date":"2026-07-27","runId":"wf_b81e5b49-35b","lane":"slacker","ref":"gh#150","finding":"the owner's shadow mirror of a group turn showed the machine preamble instead of what the colleague actually said - both halves of the issue ('leaking' and 'blind to the real conversation') from one cause","rootCause":"src/connectors/slack/app/handlers.ts:704 and the twin at :1222 - the MPIM/mention handlers FUSED machine framing into the turn text (text: groupContext + resolvedText), so only the model-facing string existed downstream; the contract that allowed it is app/context.ts:24, where ProcessMessageParams.text had no notion of framing","verdict":"built","state":"built","note":"postReply.ts:569 (shadowPreview(userMessage)) is where the symptom surfaced, NOT where the fix belongs: groupContext (handlers.ts:633) is ~360 chars and SHADOW_PREVIEW_MAX is 350, so the owner's mirror rendered the preamble and the colleague's real words never fit. A strip at the render site COULD NOT have been the fix - the thread-action directive's roster/TASK body (core/threadActions/index.ts:173-202) carries no delimiters, so only the side that BUILT the framing can say where the person's words start. FIX, 4 files, ~+52/-20 with code roughly flat (a regex, a redundant local and a stale comment block deleted; most growth is the new contract's doc): ProcessMessageParams.text is now documented as the person's own words with a new optional framing?:{prefix?,suffix?} for machine blocks; processMessage composes framedText ONCE at the top and the split downstream is BY AUDIENCE - model/history/inbound-queue/orchestrator/outreach-matcher read framedText (byte-identical to the old string at every one) while everything a PERSON reads reads `text` (log+audit previews, the briefing logEvent detail, the addressee gate, and postReply's userMessage). Deleted the textForGate regex and the redundant const userMessage = text. Verified separately that no output gate's DECISION depends on userMessage - runOutputGates.ts:241 and :882 pass it only to verifyDates whose _userMessage param is unused, so the narrowing is inert for the gate stack. This is the same ~364-char preamble whose OTHER consequence was fixed this morning as mpim-envelope-eats-thread-context (it was truncating the availability pre-check's view of the thread); same string, two different downstream victims, two different roots"}
{"date":"2026-07-27","runId":"wf_b81e5b49-35b","lane":"matchmaker","ref":"D-calendar-cache-staleness","finding":"a decision was made against a cached calendar read","verdict":"already-fixed","state":"built","note":"THE DUPLICATE THAT COST A FULL DISPATCH. This is the direct consequence of the engine bug below: 11 alreadyBuilt refs were passed in args and the manifest reported passedIn:0, so triage had nothing to match against and dispatched a lane in full to be told the fix already existed. Its dependency ask WAS built (D>dep): the cache key became getCalendarEvents|<email>|<cleanStart>|<cleanEnd>|<tz> which still contains |<email>| so the cross-turn drop and the new per-turn memo sweep (calendarCache.ts:83-88) both still invalidate, plus a ReadFreshness conversion at the two tool handlers that is behaviour-preserving (force_refresh===true?'force':'cached' is exactly the old boolean). It also deleted the try/catch around planMeeting's nearby-alternatives search, which the verify confirmed safe because findNearbyAlternatives catches everything and returns empty"}
{"date":"2026-07-27","runId":"wf_b81e5b49-35b","lane":"outrider","ref":"ENGINE-alreadyBuilt-payload-dropped","finding":"alreadyBuilt.passedIn reported 0 although 11 refs were passed in args, so triage had nothing to dedupe against and a lane paid a full dispatch to answer already-fixed","rootCause":"unknown - the engine's arg handling; NOT investigated by this run","verdict":"needs-owner-decision","state":"open","note":"SIXTH instance of the class the manifest exists to catch: a step that did nothing while the run reported success. The others on record: the log watermark never filtering (a timezone slip, ~430k), the activity exit never firing, the Agent label never matching, dependency asks vanishing into a built verdict, and alreadyBuilt never matching gh#147 against #147. Note the failure was VISIBLE this time and only because the manifest prints the number - passedIn:0 beside 11 passed is exactly the zero-where-zero-is-impossible tell. Routed to the INFRASTRUCTURE chat, not to a bug lane: bugger.js was being rewritten in the same hour this run fired (mtime 17:14, run launched 17:16) and the scout pipeline is brand new, so this is very likely a rename or a shape change in the new arg contract rather than a Maelle defect. Everything ELSE in the manifest was healthy: cutoffUtcUsed 2026-07-26T16:19:00Z matched the watermark exactly (no timezone slip), filesRead covered BOTH the 07-26 and 07-27 logs (so the previous evening was genuinely reviewed), misroutedLanes 0, and verify.ran true with fixesToCheck 5 - the three tells that have burned this framework before all came back clean on the new engine's first run"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","ref":"103-disclose-discarded-owner-override","finding":"the owner granted an override in a group chat ('it can be tight'), the clamp voided it, and nothing told him - Maelle went on to tell the colleague 'Idan's OK pushing through the tighter slots'","rootCause":"src/skills/meetings/bookingRequest.ts:432-438 - grantRelaxed recorded the clamp-driven denial ONLY in a log line and returned relaxedReason:'none', byte-identical whether an override was never asked for or asked for and dropped, so no tool result could distinguish the two and no reply could disclose it","verdict":"built","state":"built","note":"OWNER PARKED THE AUTHORITY CHANGE - this is the disclosure only, per 'if you not sure about 103, also park it, or do the obvious steps'. The clamp is untouched, grantRelaxed is untouched (same predicate, same denial, same log), and NO path gains owner authority in a group chat - which is exactly the regression the owner was worried about ('how we can make sure by code she is blocked for data. I don't think she can'). BUILT: 2 files +81/-0. clampedRelaxedNotice(toolName,args,context) sits directly below grantRelaxed with a RELAXED_AWARE_TOOLS set (find_available_slots, create_meeting, move_meeting - the only three schemas exposing `relaxed`), returning an owner-facing string in the house style of the sibling _no_all_attendee_free_note / _over_optional_note payload notes: the fact, the consequence ('this result is the UN-overridden one - every one of his rules was still enforced') and the route (say it in our 1:1 DM). ops.ts: the switch moved into a private dispatch() and executeToolCall became a thin wrapper attaching owner_override_not_applied. THE PREDICATE IS THE EXACT COMPLEMENT OF THE GRANT BY CONSTRUCTION, not a second copy: args.relaxed===true AND senderRole!=='owner' (which IS grantRelaxed's denied branch, so the notice can never contradict a granted override) AND isOwnerInGroup===true (making the denial the clamp's rather than a real colleague's). Attached at ops.ts because that is the single point every direct op returns through, so it rides the booked / rule_violation / needs_owner_approval branches alike. Net-positive with nothing deleted because there was no prior layer to remove - the refusal already existed, only its invisibility was new-fixed. 7/7 traced: fires in an MPIM; SILENT in a 1:1 DM for two independent reasons so it cannot warn spuriously even against a malformed context; no owner-facing text ever lands in a colleague's thread; nothing fires on an ordinary group message; a stray `relaxed` on get_calendar exits with no false reason (M11); the dispatch extraction preserves the exact `null` 'not our tool' contract. Truthfulness of the consequence claim verified against find_available_slots' auto-relaxed recovery: shouldRecover needs isOwnerInitiatedSearch||mustBe and isOwnerInitiatedSearch===(senderRole==='owner') is false for a clamped owner, while a must_be recovery returns owner_approval_candidates which are never offered or booked - so 'every one of his rules was still enforced' is true. MANAGER'S PREMISE CORRECTED: I briefed that grantRelaxed discarded the override in the 2026-07-27 turn. The lane proved log :197 exactly as briefed BUT could not confirm the discard - there is NO grantRelaxed DENIED line anywhere in that log, and the 'it can be tight' turn (07:24:46-07:25:12) ran attendee pre-resolution -> availability_tz_normalize -> availabilityPreCheck verdicts injected {count:2,bookable:0,notBookable:2} -> one Sonnet call -> Orchestrator completed {actionCount:0} with NO `Tool executed` line at all. That turn called ZERO TOOLS so it never reached grantRelaxed; the reply was composed entirely from the injected verdicts. The mechanism is real and reachable and is now disclosed, but the specific logged silence had an additional cause. TWO DEPENDENCY ASKS, neither blocking: (1) context, optional - nothing in the prompt tells the model to relay owner_override_not_applied; it works as built (field name plus the directive inside the string, exactly like _over_optional_note) but one line at systemPrompt.ts:1105 would make relay near-certain; (2) guard or context - THE NO-TOOL PATH IS NOT COVERED and cannot be from meeting's files: when the model answers from injected preCheck verdicts without calling a tool, there is no tool result to carry the notice, so that disclosure must live in availabilityPreCheck.ts or the prompt. ALSO REPORTED, NOT BUILT (guard's file, relayed to guard mid-run): both verdicts that turn were bookable:0/notBookable:2 and availabilityPreCheck.ts:978 explicitly forbids softening a NOT BOOKABLE into 'tight but workable'/'he could push through', which is exactly what Maelle said - so the wording looks like a NOT-BOOKABLE-reported-as-NOT-CLEAN tier collapse at :935/:978, a competing root to the timezone-frame hypothesis. tsc --noEmit clean, exit 0"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","ref":"A-requester-timezone-frame","finding":"a bare cross-zone clock has no single honest reading - the tape holds one colleague of each convention - so the fix is to judge BOTH readings and make the reply name which clock it answered","rootCause":"src/utils/availabilityPreCheck.ts - resolveStatedZone's stated->requester->owner order was a guess dressed as a rule","verdict":"built","state":"built","note":"SUPERSEDES the earlier needs-owner-decision row. The lane REJECTED BOTH orders the Manager offered. resolveStatedZone -> resolveFrame returning {zone, otherZone?}: a STATED zone settles it unchanged; a BARE clock from someone outside the owner's zone returns BOTH candidates, both get a real checkSlot verdict, both are printed with their own clock, and the injected block requires the reply to say which clock it answered. The owner's bar - 'a bare clock can never be resolved to an instant that is never named anywhere in the reply' - is met BY CONSTRUCTION because there is no single resolution to hide. WHY NOT timezone_set_by: it records how the ZONE was established, a different fact from the CONVENTION a person uses writing a bare clock. Luke's Australia/Canberra row is CORRECT - reading it is not what got him wrong - and the store holds the counter-example, Shayan Memari Australia/Canberra timezone_set_by:'owner', so trusting a CONFIRMED zone more would resolve his bare clock FURTHER from the owner's. Two data points fitted, wrong mechanism. ECHO DETECTION deliberately not built (G2): the real explanation for Luke is that he wrote 'Wednesday 12 Aug 10am-11am is best' ECHOING a slot Maelle had offered him labelled '(Idan's time)' - not a colleague introducing a bare clock but one quoting an owner-framed offer back - and a wrong 'this is an echo' verdict would restore the silent single reading. Owner's zone is the PRIMARY reading not by likelihood but because every other line in the block, the alternatives search and the ledger are already owner-local. THE AMPLIFIER IS CUT AT THE SOURCE: an undecided frame arms the floor ONLY when BOTH readings are blocked; if they disagree both instants are FORGOTTEN. Replaying Dirk - 16:00 Israel busy, 17:00 (= his 16:00 CET) free -> nothing armed -> the honest 'ich halte mir 16:00 CET frei' SHIPS. The :743 false refusal is impossible. 16/16 traced including both-readings-blocked (the floor still fires - ambiguity is not blanket immunity), a throw on the second reading dropping the whole pair, cross-midnight anchoring both days, and an Israeli colleague with no zone being BYTE-IDENTICAL"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","ref":"BUNNINGS-ROOT-REFUTED","finding":"BOTH candidate roots for the Bunnings incident are REFUTED. Maelle was accurate: Tuesday and Wednesday were genuinely the OWNER-OVERRIDABLE soft tier, and she rendered all three verdict lines faithfully","rootCause":"none - no bug demonstrable on this evidence","verdict":"already-fixed","state":"built","note":"THE MANAGER'S HYPOTHESIS AND MEETING'S HYPOTHESIS BOTH DIE HERE, and the report row 100 diagnosis was substantially wrong. The log preview at :170 is TRUNCATED; the real message from conversation_threads for 1785136927.011059 carries the three bullets. FOUR FINDINGS KILL THE TIMEZONE HYPOTHESIS: the sender is U0F28CK6H, THE OWNER (rawRole:'owner'), whose people_memory.timezone is Asia/Jerusalem, so requesterTimezone equals the owner's zone and the frame fix is a NO-OP on that turn; the pre-fix extractor prompt contained ZERO colleague-zone information (Luke is never named in it) and said explicitly 'when no TZ is given, treat it as Asia/Jerusalem (owner-local)'; and Luke's own turn (:277) ran NO pre-check at all - no availability_tz_normalize between :286 and :290 - because '10am-11am' has no HH:MM, no '?' and no TZ cue. There is no 10am->03:00 conversion anywhere on the tape. THE TIER-COLLAPSE HYPOTHESIS FAILS ON THE REPLY'S OWN VOCABULARY: the same message renders the all-day HARD line verbatim for Thursday ('you're away that whole day' <- outOfOfficeAllDay) and uses this block's own NOT CLEAN label for the other two ('not clean as-is', 'None of the three land clean'), plus the soft branch's instruction ('workable if you want to push through' <- 'it's Idan's to override, don't refuse outright') and its escalate-on-insist shape. She was faithful to all three lines. THIS RECONCILES the owner's 'Wed 12 Aug 10:00 was FREE' with bookable:false WITHOUT A THIRD BUG: a soft block means no meeting is there but it breaks a day-load / focus / category rule - which is exactly what a human calls 'free'. No checkSlot bug on this evidence, and no dependency ask on meeting. WHAT CANNOT BE PROVED: the exact violation_kind, because pre-fix logging recorded only COUNTS. If the owner insists 10:00 broke NO rule at all, that is owner testimony to collect, not a bug anyone can demonstrate. THE MANAGER'S :978 CAUTION WAS RIGHT AND IT MATTERED: that 'never soften into tight but workable' sentence is text guard ADDED THIS MORNING, so citing it against an incident that predates it is circular - now said so in the code. notBookable:3 SUMS BOTH TIERS, and that single ambiguity is what generated two candidate roots; the injected-verdicts log now carries hardBlocked / notClean / undecidedFrame. The lane also CORRECTED ITS OWN FALSE COMMENT: its uncommitted hard-block change cited Bunnings as its evidence; the hole is real (the hard tier arrived with no reason under a header handing over the overridable vocabulary) and the fix stands on the owner's reason-by-class decision, but the comment no longer asserts a refuted incident"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","ref":"B-floor-cites-mismatched-block","finding":"the floor cited a past midnight instant against a draft three weeks later - but the scout's root (a bulk in_the_past sweep leaking into the ledger) is REFUTED, and a deterministic day-scope would have disarmed the only true catch on the tape","rootCause":"src/utils/availabilityGate.ts - in_the_past was in HARD_FLOOR_KINDS, and it is the only kind that MANUFACTURES itself: a point-check on a question naming no clock time resolved to today at 00:00, trivially past, and that became an ESTABLISHED hard block","verdict":"built","state":"built","note":"SCOUT'S PREMISE REFUTED: recordHardBlockedSlot has exactly ONE caller (availabilityPreCheck's verdict loop), so findAvailableSlots' rejection breakdown - the in_the_past:60 at :762 - can NEVER reach the ledger. The real chain: the point-check on 'welche Zeit is wirklich frei bei Idan?' (:757-:765, week query start 2026-07-26) resolved to today 00:00 and armed the floor; sixteen minutes later it cited that against a draft about 3 August. A DETERMINISTIC DAY-SCOPE IS UNSAFE AND THE TAPE PROVES IT - two candidates tested and both rejected: requiring the entry's clock token in the draft disarms the founding shape ('10-11am' contains no '10:00'), and requiring the entry's day-number disarms the ONLY TRUE CATCH on the tape, because :743's draft ('ich halte mir 16:00 CET solange frei') contains no day at all. Anything language-based is G7-banned. So the fix went to the SOURCE: in_the_past REMOVED from HARD_FLOOR_KINDS because nothing it catches is harmful (nobody books the past - checkSlot rule 0 refuses the write) while it is the only self-manufacturing kind, so it contributed a stream of entries that could only ever be wrong evidence in exchange for catching nothing; hardBlockClassPhrase KEEPS its case so the prompt block still renders 'that time has already passed', which is true and useful - only the ARMING is withdrawn. freshHardBlockedSlots now requires the instant to be in the FUTURE, making 'the ledger holds only bookable-in-principle instants' an INVARIANT rather than a side effect of which kinds arm it, so the removal survives a reordering of checkSlot's ladder in another lane's file. Detector prompt gained MATCH THE DATE BEFORE THE TIME - a shared weekday word is not a match ('Monday 27 Jul' vs a draft about 'Monday 3 August' -> not_mentioned) - while still reading only the treatment enum (G5 intact)"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","ref":"FLOOR-PRODUCTION-RECORD-0-for-2","finding":"the availability floor's production record on its first day is ZERO catches and TWO false fires - both from bad INPUT rather than bad judgement - and its founding incident was misread","verdict":"needs-owner-decision","state":"open","note":"THE LANE WROTE THIS INTO ITS OWN FILE HEADER rather than leave the flattering story, and it bears directly on the owner's pending 'cap what the floor may assert' decision. Both false fires (the Dirk frame error and the past-midnight mismatch) came from entries that should never have existed, and BOTH routes are now closed upstream. The founding incident (Bunnings) would not have fired the floor and was not meant to - those slots were soft. RECOMMENDATION TO THE OWNER, from the lane: do NOT cap the wording yet. The class phrase for owner_busy_collision / vacation_or_off_day cannot be frame-wrong once the both-readings rule holds, and rewriting to 'I can't confirm that time' would throw away the one thing that makes the correction useful. Instead let it run ONE MEASURED WINDOW now that the inputs are clean and decide on data (G9). If it still shows no catch, the honest move is to SHADOW it, not to soften it. Not done unilaterally because it overlaps a decision already put to the owner"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","ref":"no-tool-path-override-disclosure","finding":"DO NOT BUILD - there was nothing to disclose on the no-tool path, and the owner's override was not voided at all; it was honoured","rootCause":"none","verdict":"declined","state":"built","note":"Refutes the Manager's third premise of the day. actionCount:0 and no `Tool executed` line between :188 and :193 means no tool ran, so there were no args, so args.relaxed never existed and NOTHING WAS VOIDED - a disclosure fired there would narrate a void that did not happen. And what happened was AUTHORIZED: the two slots were the owner-overridable tier, the block says 'it is HIS to override, do NOT flatly refuse', the AUTHENTICATED OWNER (rawRole:'owner', isOwnerInGroup:true) said 'it can be tight', and she relayed his override. The MPIM clamp is an anti-cheat boundary against COLLEAGUES gaining owner authority; it was never in play on that turn. Detecting 'it can be tight' as an override request would need natural-language meaning as the PRIMARY trigger (G7-banned) or a new output-path LLM call for a rare condition (G8), and a trigger firing when he did not ask is worse than silence. Whether the owner's own soft-rule override in a group room should route through approval instead of being relayed is the PARKED #103 authority question, not a bug. No dependency, nothing for context. NOTE THIS DOES NOT INVALIDATE meeting's built disclosure - that covers the TOOL path where args.relaxed===true and the clamp genuinely denies, which meeting proved reachable; it is a correct fix for a real case that has simply not been observed in production yet"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"instructor","ref":"relay-rule-owner-override-not-applied","finding":"DECLINED - no prompt change. The generic contract that makes tool-result notes get relayed is already shipped once, globally, and the new payload note is STRONGER than the two precedents that work today","rootCause":"none - the ask was for a rule that already exists in general form","verdict":"declined","state":"built","note":"ZERO FILES CHANGED. Five reasons, each verified against code not asserted: (1) C5 duplication - _over_optional_note and _no_all_attendee_free_note are set at findAvailableSlots.ts:1546/1551 and NO prompt line anywhere tells the model to relay them; the only prompt mention of either name is meetings.ts:1181, a DIFFERENT rule (don't re-confirm an attendee conflict he already saw). What actually backs the pattern is generic and already shipped at systemPrompt.ts:612 - 'your tool-result fields are your notes' plus never name the field - so 'read the result, speak its content in your own voice' is established once and already covers owner_override_not_applied. (2) The new notice is STRONGER than either working precedent: bookingRequest.ts:495 already carries the imperative ('SAY THAT TO HIM in this reply, in one clause'), the content, the audience scoping ('It's for HIM - the others in this chat need no explanation') and the anti-pattern ('NEVER narrate as though it landed'), while _over_optional_note relays today on materially LESS directive force. (3) C4 BACKWARDS - the proposed anchor is a worse tier AND less reliable: line 1105 sits inside the categoriesBlock IIFE which returns '' when profile.categories is empty (meetings.ts:1036) and is appended OUTSIDE the isOwner ternary at :1150, so the line would be billed on EVERY scheduling turn, owner and colleague (~55-70 tokens x thousands of turns) yet would vanish entirely for a clone with no categories. The payload note has neither property - unconditional, and fires only on the exact turn it applies. (4) C6 nothing to diagnose - per guard's proof the motivating turn called zero tools so this field has NEVER fired in production; a rule added for an unobserved case cannot be aimed at a known failure mode. (5) C3 risk points the same way - a static line two lines below 'his yes IS the approval - retry with relaxed:true' invites contaminating ordinary owner-path override narration with 'your override may not have applied' hedging, which the payload approach structurally cannot do. TRACED the shipping mechanism instead: the notice reaches him in an MPIM (all four guards pass, string attached, language-safe because it specifies CONTENT not a phrase to paste, so Hebrew and English both work with no regex), and stays silent on all four non-cases via four early exits that occur BEFORE any text exists - the silence is STRUCTURAL, not instructed, whereas the proposed line would have been present on every one of them. CORRECTED THE MANAGER'S RELAYED ANCHOR: systemPrompt.ts is 781 lines and has no :1105; the OWNER-PATH OVERRIDE rule is at src/skills/meetings.ts:1104-1110. Anyone acting on the cited path would have edited the wrong file. Also noted its own charter cites scripts/measure-prompts.cjs while the file on disk is scripts/measure-prompt.ts - a charter doc fix for the framework chat. tsc green"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","ref":"103-disclosure-channel-surface-gap","finding":"the owner-override disclosure covers only the MPIM clamp; an owner override in a CHANNEL is still silently discarded - the identical bug on another surface","rootCause":"src/connectors/slack/app/processMessage.ts:139 clamps the owner on THREE surfaces (isMpim || isChannel || isColleagueTest) but isOwnerInGroup at :128 is isMpim-only, and the separate isOwnerInChannel at :138 is never read by clampedRelaxedNotice (bookingRequest.ts:491)","verdict":"needs-dependency","state":"open","note":"FOUND BY CONTEXT while checking a prompt ask it then declined - a good argument for asking a lane to judge rather than apply. isOwnerInChannel is not on SkillContext today so it needs plumbing, which may itself be a cross-lane ask. Dispatched to meeting with three things to settle rather than apply: the plumbing (dependency ask if SkillContext is outside its lane); an EXPLICIT ruling on the third surface isColleagueTest (Manager's read is that it should NOT fire, since that mode is the owner deliberately impersonating a colleague, so 'your override didn't apply' describes the thing he asked for - but it must be a decision in the code, not an accident of the predicate); and whether one 'the owner is clamped right now' fact should replace a widened per-surface conjunction, since three clamp surfaces and two partial flags is exactly how this gap existed. Context's closing note is the sharpest argument for the payload design: a prompt line at meetings.ts:1105 would have been equally silent in the channel case, because the FIELD would not exist to relay"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","ref":"103-disclosure-channel-surface-gap","finding":"the DETECTION class is closed - the clamped-owner test now keys on authenticated identity rather than an optional per-surface flag - but the CHANNEL surface still does not fire, and the lane said so in its first line rather than claiming otherwise","rootCause":"src/skills/types.ts:81-127 - SkillContext carries isMpim but NOT isChannel, and colleague-test's thread set is module-local to processMessage, so from SkillContext a clamped-owner turn that is not an MPIM is INDISTINGUISHABLE from colleague-test","verdict":"needs-dependency","state":"open","note":"RULING 1, one flag not a widened conjunction: `const ownerClamped = context.senderRole === 'colleague' && context.userId === context.profile.user.slack_user_id`. Provably the same comparison on the same operands as the ONE definition of rawRole (connectors/slack/app.ts:95-97), and postReply.ts:356,568 already uses exactly it to tell a clamped owner from a real colleague. Plumbing VERIFIED not assumed: processMessage.ts:655 passes userId:senderId -> orchestrator/index.ts:400 -> SkillContext.userId, so it is the authenticated sender. WHY IT BEATS isOwnerInGroup||isOwnerInChannel: that flag is one clamp surface of three AND `?:` OPTIONAL on SkillContext, so an absent flag reads as 'not clamped' - which is precisely how the channel gap survived the first fix - whereas userId and profile are both REQUIRED, so this half cannot be lost by an incomplete context and cannot miss a surface added later (charter rule 10: authorize on authenticated identity, never a flag that may not have been threaded). THE GAP'S REAL ROOT WAS CONFLATION and that is what got split: one conjunction meant 'he was clamped' AND 'which surface', so a missing surface silently meant NOT clamped; now they are two gates and a missing surface means 'clamped, surface unknown' - a VISIBLE decision point instead of an invisible false-negative. RULING 2, isColleagueTest must NOT fire, and for a harder reason than the Manager's: colleague-test is DM-ONLY (processMessage.ts:123) and the disclosure's remedy is 'tell me in our 1:1 DM', so in colleague-test he IS in a 1:1 DM and the remedy would be A LIE - M11 makes a confident wrong reason worse than silence - plus it corrupts the very behaviour under test. Ruled in code and in the doc comment, not left to the predicate. REJECTED inferring the surface from the channelId prefix (D = IM): charter rule 4 permits it on a structured id, but it would put Slack id conventions inside a scheduling normalizer and create a SECOND source of truth for 'which surface' competing with processMessage's flags - the same disease as the two partial flags. THE ASK, one field not a second boolean: `ownerClampSurface?: 'mpim' | 'channel' | 'colleague_test'` set where the clamp is DECIDED (processMessage.ts:139, which already computes all three inputs at :123,:128,:138). Three steps, SEQUENTIAL: outer adds it to SkillContext (skills/types.ts:89-91), slack decides and passes it (processMessage.ts:139 + both orchestrator call sites :663,:698), outer carries it (core/orchestrator/index.ts:32,405). Meeting's side is then a ONE-LINE change - swap `if (context.isMpim !== true) return undefined` for a switch where mpim/channel each get their true remedy clause and colleague_test returns undefined - and it would let isOwnerInGroup be retired in favour of the single fact. MANAGER'S DECISION: NOT dispatched tonight. A three-lane sequential chain to thread one new field, for a disclosure that has NEVER FIRED in production, on a surface of the #103 authority question the owner explicitly PARKED - and the current state is honest (unknown surface -> stay silent) rather than wrong. Goes to the owner as a report row. 8/8 traced with ZERO behaviour delta from the previous state; equivalence proven both directions on every context the transport builds, plus deferredActionReplay (senderRole 'owner' -> silent, correct) and non-Slack transports (userId is not a Slack id -> silent, no clamp concept). isOwnerInGroup is still read by knowledge.ts:502, runOutputGates.ts, postReply.ts, buildTurnContext.ts and findAvailableSlots.ts:92, so dropping this one read created no dead code. tsc clean"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","ref":"VERIFY-B1-arming-tier-mismatch","finding":"BLOCKING - the floor's arming test asks `bookable` while the ledger's own tier is HARD_FLOOR_KINDS, and the two predicates disagree on the entire owner-overridable tier. Both halves of the arming loop read the wrong one, giving TWO fresh false-refusal paths","rootCause":"src/utils/availabilityPreCheck.ts:841-863 (the arming loop: `readings.every(r => !r.bookable)`) against src/utils/availabilityGate.ts:151-154,233 (recordHardBlockedSlot no-ops unless kind is in the two-kind HARD_FLOOR_KINDS)","verdict":"needs-owner-decision","state":"open","note":"THE THIRD DISTINCT FALSE-REFUSAL ROUTE INTO THE SAME MECHANISM FOUND IN ONE DAY - after the stale-entry route (fixed this morning) and the wrong-frame + mismatched-date routes (fixed tonight). The floor's production record stands at ZERO catches and TWO false fires. NOT FIXED: the owner instructed 'no change in block - if you do have block block', so this stops here rather than being dispatched. (a) A MIXED HARD/SOFT AMBIGUOUS PAIR ARMS AS IF THE FRAME WERE SETTLED. The v4.2.2 justification - 'when BOTH readings are blocked the ambiguity stops mattering, that time does not work either way' - only holds when both are blocked IN THE SAME TIER. A Brussels colleague writes a bare '16:00'; readings are 16:00 Israel (owner_busy_collision, HARD) and 17:00 Israel (outside_working_hours, SOFT); everyReadingBlocked is TRUE so the hard reading arms. The drafter answers the their-clock reading ('16:00 your time (17:00 his) is past his usual hours, but it's his call - want me to raise it?'), the detector matches DATE then TIME with no frame concept at all, the draft carries the digits 16:00 on that date and hedges it as pushable, and the spec explicitly classifies a hedge as presented_available -> the rewriter fires -> the colleague is told '16:00 doesn't work, it clashes with a meeting already on Idan's calendar'. False about the instant he asked about, flattens a tier the floor's own header (availabilityGate.ts:61-66) says it must never flatten, and destroys the #128 escalation. This is the :743 false fire RE-ENTERING through the new both-blocked exemption. Not exotic: the people store has 7+ colleagues on America/New_York and one on America/Los_Angeles, so a bare cross-zone clock is routine. Mechanism certain; end-to-end probability medium-high. (b) A FRESH SOFT VERDICT NEITHER RECORDS NOR FORGETS, so a stale HARD entry outlives the fact. Invalidation rule 1 is written and implemented as 'a fresh bookable verdict deletes it', but the correct rule is 'a fresh verdict that is NOT A HARD BLOCK deletes it'. No cross-zone asker needed: 07:35 a colleague asks 16:00 -> owner_busy_collision -> armed 45min; 07:45 the owner drags that meeting to 15:00 IN OUTLOOK (no Maelle mutation, so no mutated=book and no cache invalidation); 08:00 the colleague asks again -> 16:00 now returns travel_buffer_collision (SOFT) -> record no-ops, forget is skipped, the stale hard entry stays armed -> 'could work if he's OK with the tight turnaround' is rewritten into a hard refusal citing a clash that no longer exists. W1 widens the window further because the point-check may not see the Outlook move for up to the cache TTL. THE ONE FIX THAT CLOSES BOTH AND SHRINKS THE STACK RATHER THAN GROWING IT: export the arming predicate from availabilityGate (armsHardFloor(kind) over HARD_FLOOR_KINDS - module-private today) and use it in the loop for BOTH branches - record iff EVERY reading is hard-blocked, forget in every other case (bookable, soft-blocked, unknown kind). One predicate read in the one place that produces the knowledge; no new guard, no exemption list, and the 'ambiguity is not immunity' property then genuinely holds because a mixed pair forgets both instants. STANDING ALTERNATIVE, the owner's call: availabilityGate.ts:34-38 already flags whether the floor keeps its ACTIVE form as an open owner decision; if he picks the canned 'I can't confirm that time' remedy, (a) becomes a vague reason rather than a wrong one. (b) needs fixing either way"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","ref":"COMBINED-VERIFY-three-waves","finding":"first pass to see all three of today's waves together across 24 source files: 1 BLOCKING, 7 worth-knowing, 5 cosmetic, plus 5 verifiedClean actions","verdict":"audit","state":"built","note":"Findings-only, nothing built. tsc green. WORTH-KNOWING: W1 the point-check still reads the CROSS-TURN CACHE (availabilityPreCheck.ts:656-668 calls getCalendarEvents with four args so freshness defaults to 'cached') while wave 2 moved every decision-backing read to LIVE via getOwnerEventsForDecision precisely because 'a proposal is a commitment' - so ONE INJECTED PROMPT BLOCK NOW MIXES FRESHNESSES, verdict lines up to the cache TTL old sitting above a 'TIMES THAT DO WORK' block from a live search, and a slot vacated four minutes ago is still recorded as an established hard block; a new INCONSISTENCY rather than a new regression, i.e. exactly the class no single lane could see. W2 the ledger `display` carries a REQUESTER-scoped second clock (:858) while HardBlockedSlot.display documents itself as owner-tz and the ledger is deliberately owner-keyed ACROSS THREADS, so an entry created in a Brussels thread can be listed to the detector during a turn with an Israeli or New York colleague where the parenthetical is simply wrong - and it is the ONLY frame signal the detector has, which is what makes B1(a) fire. W3 availabilityPreCheck.ts:783-785 cites CalendarOfflineError as its safety net, but that error is minted only inside getOwnerEventsForDecision and findAvailableSlots, neither of which this file calls; behaviour is still honest but the CITED net does not apply - flagged because it is exactly the kind of sentence a later lane trusts instead of re-deriving. W4 two instructions in the same block collide on the undecided-frame path (:1176 quote the second clock verbatim vs :1178 pick a reading and name whose clock), which for a bare 16:00 can produce a THIRD number in a thread that already has two. W5 the owner-clamp identity check is PROVEN correct with no leak (see verifiedClean), with two notes: the notice necessarily lands in a room where colleagues will read it, which is a disclosure the owner should consciously accept; and the field is attached by MUTATING the handler's returned object (ops.ts:70), harmless today because every relaxed-aware handler returns a fresh literal, worth keeping true. W6 summarizeCounter's widening from a 3-key whitelist to 'no key is ever dropped' now renders every model-authored key into a colleague DM, and securityGate does NOT run on spine relays, so scrubInternalLeakage covers graph ids / bare Slack ids / IANA tokens / sentinels / tool names but NOT req_/task_ ids - low likelihood, cheap to bound. W7 G8 - one colleague hard block arms a Haiku call on EVERY reply for that owner on every thread for 45 minutes, including the owner's unrelated DMs. COSMETIC: hardBlockClassPhrase('in_the_past') is NOT dead (the renderer calls it with the raw kind at :1142 and in_the_past is not in ESCALATABLE, so it still renders 'that time has already passed'; only the recorder can no longer reach it) and the removal is sound because freshHardBlockedSlots' future-only filter makes 'the ledger holds bookable-in-principle instants' an invariant of the LEDGER rather than of the kind list; eventsByWeek memoizes successes only so an unreadable calendar costs up to 6 Graph attempts per turn (pre-existing); the frame fork can flip the alternatives anchor from 'exhaustive' to 'first_round' for a cross-zone asker, quietly relaxing D3; a slot named twice in one message (once zoned, once bare) yields two verdict lines for the same instant. CHEAP CONFIRMATIONS: no new schema or migration anywhere (grep for added ALTER/CREATE/DROP/initSchema returns zero); no override path widened (grantRelaxed untouched, the one allowRelaxed deletion was a FRESHNESS argument not an authorization one, no ignore_attendee_availability change); the four-sided framing contract agrees on all four sides; ops.ts preserves the exact null 'not our tool' contract. HONEST GAPS NAMED: (1) the batched diff truncated at 400 lines so FIVE wave-2 files were never seen at all - findAvailableSlots, ops/handlers/{calendarReads,createMeeting}, annotateSlotsWithAttendeeStatus, meetingRoomAvailability - and specifically unverified are the 62-day and zero-width recursion branches end-to-end and invalidateCalendarCache's case-sensitive email match; (2) the requests wave's replay of a stored counter, with verifiedClean #62 taken on trust; (3) no log greps; (4) no end-to-end replay of the floor, so B1(a)'s detector step is reasoned from the prompt spec rather than observed"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","ref":"VERIFY-B1-arming-tier-mismatch","finding":"SUPERSEDES the blocking row - the arming loop and the ledger now share ONE tier predicate, closing both false-refusal paths","rootCause":"src/utils/availabilityPreCheck.ts arming loop (was readings.every(r => !r.bookable)) vs src/utils/availabilityGate.ts HARD_FLOOR_KINDS","verdict":"built","state":"built","note":"Owner approved as report row 111. FIX: HARD_FLOOR_KINDS membership exported as a TYPE GUARD, armsHardFloor(kind): kind is string, and recordHardBlockedSlot now calls it instead of its inline test - one definition, two readers. It is a predicate rather than a boolean because the inline test was ALSO doing the string|undefined -> string narrowing for the kind: field, so a type guard keeps the narrowing tied to the same test that decides the tier instead of adding a cast that could outlive it; that was the one typecheck error (TS2322 at the record site) and it was fixed AT THE PREDICATE, not at the call. The arming loop now reads `readings.every(r => armsHardFloor(r.rejection_reason))` on BOTH branches, and the `!bookable` test is DELETED rather than kept alongside because armsHardFloor subsumes it - evaluateInstant returns a bookable outcome with NO rejection_reason at all, and the gap branch attaches one only when maxFit === null. W2 FIXED in the same change: the ledger call site drops requesterTz so `display` is owner-local only (the prompt-block renderer still passes it, so the parameter is not dead), and new displayForAsker(slot, ownerTz, askerTz?) renders the asker's clock PER TURN in runOutputGates from getPersonMemory(ctx.senderId)?.timezone - the same people-store field the pre-check reads, off the authenticated sender - placed BELOW the mutation stand-down so a stood-down turn pays nothing. W3 FIXED: the false CalendarOfflineError / calendarReads.ts:281 citation replaced with the verified one (getCalendarEventsImpl logs and re-throws at calendarReads.ts:618-620, nothing between swallows it, and the typed error is minted only in getOwnerEventsForDecision :465 and the slot walker findAvailableSlots.ts:275) - the impl's catch was READ rather than the claim relayed. W4 FIXED: the verbatim-quote rule is scoped to single-reading lines and yields to the two-reading rule, and the undecided-frame rule now gives the asker THE CLOCK THEY WROTE labelled with whose clock it was read as, forbidding the (= ... where they are) parenthetical on those lines since either reading IS that stated clock from one of two zones, making the parenthetical the third number - which was the 2026-07-27 thread's actual complaint. PROSE MADE TRUE rather than stacked on: HardBlockedSlot.display now states the owner-tz-only invariant and points at the per-turn renderer; invalidation rule 1 now reads 'a fresh verdict that is not a hard block deletes it' with a note that the old wording WAS the implementation; rule 4 says HARD-blocked and why 'both blocked' was not enough; the header's :743 bullet records that its first close had a second door. ~15 executable lines across three files; two definitions of the tier line became one and no exemption list exists. 7/7 traced: mixed hard/soft pair forgets BOTH and the #128 escalation survives; both-readings-hard still records BOTH with their own owner-local displays so whichever clock the draft names matches (ambiguity is not immunity); fresh soft over stale hard forgets; fresh bookable forgets (rule 1 survives as a special case, not a separate branch); genuinely hard-blocked across two turns STAYS armed, re-recorded with a refreshed TTL and no duplicate, and an instant with no fresh verdict is untouched because both branches sit inside the per-reading loop while a verdict-less turn returns above it; unknown/undefined kind forgets and never records, so any future RuleViolationKind nobody has classified defaults to UNARMED; the cross-thread display renders correctly for an Israeli colleague (unchanged), a New York colleague (= Tue 11 Aug 04:30 EDT where they are), the owner's own turn (unchanged) and a garbage zone like 'PST' (unchanged, no malformed parenthetical), with the map copying so the store keeps its owner-local string and forgetHardBlockedSlot still deletes the right entry. MONOTONICITY (G6): every behaviour change moves entries OUT of the ledger or leaves the same ones in - none adds an entry that was not there before - so the fix is monotonically safer; a too-strict armsHardFloor leaves the floor unarmed (a rare over-optimistic claim ships, the closed list's documented default), and a wrong askerTz feeds matching and wording only, never the arming decision. G8: no new LLM call, one sync SQLite read only on turns that already have a non-empty ledger and no mutation"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","ref":"RECHECK-111-arming-predicate","finding":"narrow final gate on the 111 fix - NOTHING BLOCKS THE WRAP; all five questions came out in favour and no reachable path corrupts a correct reply","verdict":"audit","state":"built","note":"PROVEN not accepted, the question that mattered: `rejection_reason` is written in exactly two places, both inside evaluateInstant (availabilityPreCheck.ts:736 and :746) and both gated on the not-bookable outcome - the gap branch's reason spread is `...(maxFit === null && blockedBy ? ...)` so bookable skips it, and the point branch returns `{date,time,bookable:true}` BEFORE any reason is attached. So armsHardFloor(r.rejection_reason) IMPLIES r.bookable === false: the new predicate implies the old one, and deleting `!bookable` loses nothing. `readings` can only contain evaluateInstant output ([v, ...v.other] at :875, v from the single verdicts.push at :774, v.other from {...other, zone} at :778) with no later spread overwriting either field, so the silent-arm-on-a-free-slot failure is NOT REACHABLE. BONUS CASE THE OLD CODE GOT WRONG: a gap query with maxFit === null but blockedBy undefined (possible - the point branch's `?? 'unknown'` proves violation_kind can be absent) - old code had !bookable true so the exemption was satisfied, record no-op'd on the undefined kind and forget was SKIPPED, leaving a stale entry; new code forgets it. NO REGRESSION ON THE REAL CATCH: re-asked -> recordHardBlockedSlot filters the same instantIso before pushing with a refreshed expiresAt (availabilityGate.ts:284-297), one entry no duplicate; NOT re-asked -> both branches sit inside `for (const r of readings)` (:877-900) and forget is called only with that reading's own instantIso, so an unnamed instant is untouched; verdict-less turn -> `if (verdicts.length === 0) return empty` at :815 sits above the loop at :874; and cross-turn key identity holds because both branches derive instantIso from the OWNER zone (params.profile.user.timezone at :507), never the requester's, so the same wall time yields a byte-identical ISO across turns. MONOTONICITY TESTED AND TRUE: because new-true is a subset of old-true, the record branch strictly SHRINKS and the forget branch strictly GROWS, and within the record branch nothing new can enter because recordHardBlockedSlot already filtered on HARD_FLOOR_KINDS - so the only behavioural deltas both REMOVE entries and no new false fire is reachable; every new failure mode is a MISS. displayForAsker composes on all three sub-questions: forgetHardBlockedSlot matches on the instantIso STRING and {...b} copies it verbatim so the forget lands; the .map builds NEW objects rather than assigning b.display, so a cross-zone turn cannot poison a shared cross-thread entry (the one line where an in-place assignment would have been the bug, and it is not one); and a garbage or missing zone yields '' from renderClockInZone (timezoneConvert.ts:55-59, luxon setZone on an unsupported zone gives an invalid DateTime and Settings.throwOnInvalid is nowhere in src) so displayForAsker returns the stored string with no malformed parenthetical, while even a throw lands in the existing try. EXTRA CONFIRMATION THAT MATTERS: displayForAsker produces a string CHARACTER-IDENTICAL to the prompt block's, since both go through renderClockInZone with the same format and wrapper and both zones come from the same getPersonMemory(id)?.timezone field - so a rewrite cannot introduce a third clock format, which is the exact class the change targets. tsc clean with NO cast introduced: the only `as` additions in the whole wave are `as const` literal narrowings, and TS2322 is genuinely resolved at the predicate because `kind is string` narrows params.kind through the early return. WORTH-KNOWING, none blocking: (1) THE ONE REAL BEHAVIOURAL LOSS - an undecided-frame turn with mixed readings now forgets BOTH instants including one a previous UNAMBIGUOUS turn had legitimately established, so if that turn's draft says '16:00 works on his end' the floor is disarmed and misses it; the trade is correct per G6 because the old alternative was the :743 false refusal, but it is a real narrowing of coverage and the file's prose does not say so out loud. (2) On an owner turn askerTz is getPersonMemory(owner id)?.timezone, so if the owner's people row zone STRING differs from profile.user.timezone (equivalent-but-different id, or stale) the listed entry gains a third-person parenthetical about the owner himself which a rewrite could echo back to him - low probability, rewrite path only, and runHumanGate runs after and owns that voice defect. (3) A two-reading line still PRINTS the (= ... where they are) parenthetical that the next paragraph forbids quoting; the instruction is unambiguous but the cleaner shape is not to print it there at all. (4) An all-hard undecided turn records TWO entries per pair, up to 12 against MAX_PER_OWNER = 8, and kept.slice(-8) silently evicts the oldest - pre-existing, eviction direction is miss-safe. (5) Cosmetic: anyRequesterClock samples only the primary reading, so a turn where every line is two-reading emits a paragraph explaining a single-reading rule no line instantiates. W4 RECONCILIATION CLEAN: :1219 is explicitly scoped and explicitly yields to the two-reading rule, and :1221 forbids the parenthetical while supplying the positive rule in its place, so for a bare cross-zone clock both paragraphs now point at the same single number"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","ref":"VERIFY-GAPS-uncovered-five-files","finding":"the three named gaps closed: 1 dated latent crash, 5 worth-knowing, 2 cosmetic - and the two failures the Manager most feared (a partial live/cached conversion mixing freshness in one reply, and a three-hours-off timer render) were PROVEN not to exist","verdict":"audit","state":"built","note":"tsc clean, confirmed. ALL FIVE PREVIOUSLY-UNREAD FILES DID CONVERT with no behaviour change beyond freshness: findAvailableSlots.ts:269, annotateSlotsWithAttendeeStatus.ts:58, meetingRoomAvailability.ts:55 and createMeeting.ts:669 all moved getFreeBusy(...,false,diag) -> getFreeBusyForDecision(...,diag), and the dropped positional false is exactly the old forceRefresh default. The ReadFreshness claim holds at EVERY call site in those files, not just the two previously checked: exactly two sites ever passed a boolean (ops/handlers/calendarReads.ts:186 and :567) and both are `args.force_refresh === true ? 'force' : 'cached'`, bit-for-bit the old boolean, while every other site omits the argument and gets 'cached' = old false. NO SITE SILENTLY CHANGED MEANING. NO MIXED-FRESHNESS REPLY EXISTS: annotateSlotsWithAttendeeStatus is called only from ops/handlers/findAvailableSlots.ts:1328 decorating slots from the now-live search, checkMeetingRoomAvailability only from planMeeting.ts:817 as a pre-book decision, and the slot finder's own two reads are live (:269 free/busy, :428 owner events) - one offered set, one freshness. ZERO-WIDTH BRANCH TRACED END TO END: equal time-bearing instants widen by an hour, recurse ONCE, and terminate; parseGraphFreeBusySlot emits OFFSET-BEARING ISO so the filter's zone-hintless fromISO is still absolutely correct regardless of process zone; and the widened call writes diagnostics itself, so a notChecked window surfaces as {} PLUS a populated list rather than 'everyone is free'. invalidateCalendarCache CASING PROVEN not assumed: the key is built from the userEmail parameter, every upstream call site threads context.profile.user.email, and all 15 .toLowerCase() sites on an owner address in the tree are COMPARISON-ONLY - none constructs a cache key or is passed to invalidateCalendarCache/getCalendarEvents - so the includes match survives and the turn-memo scan uses the identical predicate. Fragile by convention (nothing normalizes at the boundary) but correct today. THE TICK REPLAY: handlers.ts:811-826 maps emoji to approve/reject ONLY, so the amend template and tonight's summarizeCounter widening are UNREACHABLE FROM A REACTION; the path merges details.counter into on_approve.args via the same mergeAmendIntoApprove the typed approve runs, the only skipped branch is the location-mode merge which a reaction cannot supply and which fails loudly rather than wrongly, and the relay is composed from the EXECUTED args so a 13:00->15:30 counter relays 15:30. Cannot re-close. THE scheduled_at READERS WERE PROBED, NOT ACCEPTED: toTimerInstant always emits a Z-bearing instant, SQLite's datetime() honours Z (so getDueRequests' comparison is right), and every reader in src is either fromISO(x).setZone(tz) or fromISO(x,{zone:tz}) - both render owner-local correctly (briefs.ts:113/:239-240, buildTurnContext.ts:374-375, tasks/skill.ts:512-513). No reader slices a wall clock or formats without setZone. THE FEARED 'THREE HOURS OFF' FAILURE DOES NOT EXIST, and the new form is additionally correct on a UTC VM where the old bare clock was not. NOT COVERED, named: the diffs of bookingRequest.ts, requests/types.ts beyond the toTimerInstant region, processMessage.ts, context.ts, meetings/ops.ts, weTimeResolver.ts, buildTurnContext.ts, postReply.ts, runOutputGates.ts, planMeeting.ts and availabilityPreCheck.ts - most declared settled by earlier dedicated passes, the rest outside the three gaps. Every diff requested was SINGLE-FILE and each was checked for a clean terminating hunk, so nothing was truncated this time"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","ref":"getfreebusy-62day-clamp-dst-fixed-point","finding":"getFreeBusy's 62-day clamp is a FIXED POINT across a DST fall-back, so any free/busy window wider than 62 days recurses forever and blows the stack with a RangeError - and it becomes reachable on 2026-08-24","rootCause":"src/connectors/graph/calendarReads.ts:784-794 - the clamp uses parsedStart.plus({days:62}) (CALENDAR arithmetic, preserves local wall clock) while the test uses diff(...,'minutes') (ABSOLUTE arithmetic), and the recursion re-derives the clamp from the UNCHANGED startDate so it produces the identical value","verdict":"needs-owner-decision","state":"open","note":"PROVEN BY PROBE not reasoning: start=2026-09-01T09:00 end=2026-12-31T17:00 -> iter0 174780 min over -> iter1 end=2026-11-02T09:00+02:00 = 89340 min, still over the 89280 limit, and clamp === current end => FIXED POINT. start=2026-07-27 -> 89280, safe today. start=2026-02-01 (spring-forward) -> 89220, safe. There is no await on the path to :794 so each iteration is SYNCHRONOUS - the stack blows after a flood of logger.warn lines rather than hanging. REACHABLE FROM 2026-08-24 ONWARD, when start+62d crosses Israel's 2026-10-25 fall-back: get_free_busy with a model-supplied range (ops/handlers/calendarReads.ts:567, where a date-only end widens to end-of-day first), and find_available_slots which passes searchFrom..currentTo with NO CHUNKING (findAvailableSlots.ts:242-243,:269) while the walker's catch RETHROWS non-outage errors (:271), so a wide or autoExpanded search takes the whole tool down with a raw RangeError. Recurs annually, roughly 24 Aug - 25 Oct in Asia/Jerusalem, and any tenant zone with a fall-back has its own window. NOT A REGRESSION AND NOT A WRAP BLOCKER: tonight's diff changed only the forwarded argument on :794; the clamp and the threshold pre-date it. So holding 4.2.2 buys nothing and costs 24 uncommitted files - ship, then fix before 24 August. ONE-LINE FIX, meeting/graph lane: clamp by parsedStart.plus({minutes: 62*24*60}) so the clamp and the test use the same arithmetic, or refuse when clamped === endDate"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","ref":"createmeeting-density-shift-reads-stale-cache","finding":"the dense-packing 'earlier connective start' shift reads the owner's day from the CROSS-TURN CACHE, validates a candidate against that same possibly-stale array, and books there - after planMeeting has already validated live","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:969 (and the :399 sibling) - two getCalendarEvents calls still take the 'cached' default while the file's free/busy reads were converted to live","verdict":"needs-owner-decision","state":"open","note":"THE RESIDUE THE FRESHNESS WAVE DID NOT FINISH, not damage it did. The shift runs at :956, AFTER planMeeting's live validation at :818, so the LAST WORD on where the meeting lands is a self-consistent decision over data up to CALENDAR_CACHE_TTL_SECONDS old. SCENARIO: the owner asked 'what's on Thursday' earlier in the thread (populating the exact normalized day window), then says 'book Simon 14:00', while three minutes ago he accepted a 13:30 invite in Outlook. planMeeting's live read sees the 13:30 and clears 14:00; the density shift then reads the STALE copy, sees 13:30 free, pulls the meeting to 13:30 and books a DOUBLE-BOOK. :399 is the same class on the create-vs-move sibling surface but only a missed heuristic, so safe-miss. Medium-high on the mechanism, medium on frequency (needs dense packing plus a connective gap plus an out-of-band change). Not a regression - pre-tonight every read here was cached, and the new key normalization makes cached reads FRESHER on average because a cached read can now hit an entry a live or force read just wrote. Fix is to route the two reads through getOwnerEventsForDecision like the rest of the wave"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"registrar","ref":"counter-renderer-english-in-hebrew-template","finding":"the widened counter renderer emits HARDCODED ENGLISH labels into the HEBREW amend template, and amend is the one relay the language composer never rewrites - so Maayan's counter reaches her as 'עידן הציע משהו אחר: 55 minutes'","rootCause":"src/core/requests/resolver.ts:1140-1223 (COUNTER_KEY_PHRASING is hardcoded English and the unlisted-key fallback is `${key.replace(/_/g,' ')}: ${value}`, also English) interpolated into the Hebrew sentence at :915-922, while the Haiku composer that would rewrite it is gated approve||reject at :932 - 'amend keeps its template'","verdict":"needs-owner-decision","state":"open","note":"CAUSED BY TONIGHT'S FIX and it is the ONE finding a real person will notice in the next week. An unlisted key reads as '... הציע משהו אחר: meeting room: Tel Aviv 3. זה עובד לך?'. Credit where due: ISO datetimes inside the counter ARE localised (formatStart at :810-827 does setLocale('he')); it is only the key labels and the phrasing wrappers. STILL A STRICT IMPROVEMENT ON THE OLD BEHAVIOUR, which is why it should ship rather than be held: the old whitelist rendered '' for an unlisted key, so the sentence was language-clean and CONTENT-FREE - the colleague was told 'Idan suggested a different approach' and never told what. Clumsy beats absent. But it is the 'a fix that only works in English' shape and should be closed promptly"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"registrar","ref":"amend-relay-bypasses-output-gates","finding":"the amend relay ships via conn.sendDirect rather than postReply, so securityGate / humanGate / runOutputGates never see it - and it now renders EVERY key of a model-authored payload","rootCause":"src/core/requests/resolver.ts:1033","verdict":"needs-owner-decision","state":"open","note":"A NEW EXPOSURE CLASS CREATED TONIGHT, on a surface the guard that would catch it is structurally absent from. Tolerable while summarizeCounter was a three-key whitelist; the new contract is 'no key is ever dropped' over a payload the owner's model authors freely. If a model ever writes {request_id:'req_abc123', duration_min:55} into counter, the raw req_ id lands verbatim in a colleague's DM - precisely the trigger securityGate exists to scrub. Low probability (the model would have to volunteer an internal key) but it should be a deliberate decision rather than a silent one. Guard's own view: the right shape is a key ALLOWLIST on what may be relayed, not a scrub after the fact - but that is a requests-lane change, reported not built"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","ref":"date-only-freebusy-with-midnight-time-reads-free","finding":"a fully-booked day can still read as FREE with an EMPTY notChecked - the same failure V3 removed, reached by a different spelling","rootCause":"src/connectors/graph/calendarReads.ts:741 - the date-only fix tests /^\\d{4}-\\d{2}-\\d{2}$/ on endDate, so a model writing start_date '2026-07-30T00:00:00' and end_date '2026-07-30T00:00:00' takes the INSTANT branch instead","verdict":"needs-owner-decision","state":"open","note":"Guard rated this cosmetic; the MANAGER is raising it because 'a booked day reads as free' is silent wrongness, which is the class the owner has been burned by twice today. Narrow in practice - the schema asks for a date range and bare dates are the shape models actually emit - and the instant branch must STAY because availabilityPreCheck depends on it, so the fix is a widened detection of the zero-width case rather than a changed branch"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"registrar","ref":"concurrent-double-resolve-no-lock","finding":"sequential double-resolve is blocked but CONCURRENT is not - two decisive reactions within ~2s can both replay a deferred action","rootCause":"src/core/requests/resolver.ts:186 (the state gate) and :549-578 (sync-then-close leaves the row awaiting_owner for the duration of runDeferredAction, with no lock)","verdict":"needs-owner-decision","state":"open","note":"PRE-EXISTING, not tonight's. The state gate rejects anything not awaiting_owner/awaiting_colleague, so a second decisive emoji or a checkmark after a typed approve returns ok:false with no relay and no re-execution. But the replay is deliberately sync-then-close, so during runDeferredAction the row is still open and Slack can deliver two reaction events concurrently (owner taps two decisive emoji within ~2s); both pass the gate and both replay. findDuplicateEvent is the backstop but concurrent replays can both clear it before either create lands. THE SYNC-THEN-CLOSE ORDERING IS CORRECT and guard would not change it - a failed replay must leave the row retryable and the requester untold - the gap is the missing in-process guard. Medium-high on the mechanism, low-medium on the timing being achievable"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"registrar","ref":"date-only-due-at-anchors-to-midnight","finding":"a date-only due_at now anchors to owner-local midnight instead of 03:00 local, so 'remind me Monday' fires at 00:00","rootCause":"src/tasks/skill.ts:402 - toTimerInstant on a bare date","verdict":"declined","state":"built","note":"COSMETIC and arguably an improvement. Probe: '2026-07-27' -> 2026-07-26T21:00:00.000Z = owner-local midnight, where previously the bare string was read by SQLite as UTC midnight = 03:00 local. runReminderFire (core/requests/runner.ts:354) DMs immediately with no work-hours gate, so both timings are nocturnal; the new one is more defensible by frame. Recorded so nobody re-raises it as a regression"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","source":"owner","ref":"103-disclose-discarded-owner-override","finding":"the owner's authority in a group chat is a design question, not a bug - it left the bug track","rootCause":"src/connectors/slack/app/processMessage.ts:139 (the three-surface clamp) vs grantRelaxed's senderRole==='owner' requirement","verdict":"converted","state":"built","note":"→ gh#154 'Owner authority across chat surfaces', labelled Improvement + High. Owner's words: 'should be open as github improvement high - to manage the permission landscape, its not for this bug wave. I suspect this should connect 112 together, as its the same idea.' MERGED WITH ROW 112 (the channel-surface gap) at his direction - correctly, since both are the same question: which surfaces clamp the owner, and what that clamp should govern. The DISCLOSURE half stays BUILT and ships in 4.2.2 (bookingRequest.ts clampedRelaxedNotice + ops.ts, keyed on authenticated identity, MPIM surface only); only the authority design and the channel surface went to the issue. THE CORRECTED PREMISE TRAVELS IN THE ISSUE BODY, which is the whole reason a converted bug is worth more than a feature request: the 2026-07-27 turn called ZERO tools, so nothing was voided and the owner's override was HONOURED - a later plan pass reading only the transcript would re-derive the wrong problem"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","source":"verify","ref":"103-disclosure-channel-surface-gap","finding":"the channel surface of the owner-clamp disclosure is part of the same design question","rootCause":"src/skills/types.ts:81-127 - SkillContext carries isMpim but no isChannel, so a clamped-owner turn that is not an MPIM is indistinguishable from colleague-test","verdict":"converted","state":"open","note":"→ gh#154, merged with 103 at the owner's direction ('I suspect this should connect 112 together, as its the same idea'). The proposed ownerClampSurface field and its three-lane plumbing are written into the issue body, along with why the channelId-prefix shortcut was rejected and why isColleagueTest must not fire. The DETECTION class is already closed in the built half - the test keys on authenticated identity rather than an optional per-surface flag, so it cannot miss a surface added later"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","source":"logs","ref":"F-standing-calendar-pref-unenforceable","finding":"standing calendar rules being unreadable by the scan is a design question about the whole skill, not a bug","rootCause":"src/skills/calendarHealth/handlers/checkHealth.ts:498-511 and :759 have no access to the owner's standing instructions, which live only as free text whose single reader is the prompt","verdict":"converted","state":"open","note":"→ gh#155 'Rebuild the calendar health skill', labelled Improvement + Low. Owner's words: 'should be improvment low - to rebuild the calendar health skill to make it better.' The issue body carries the log proof that the auto-categorize fired 4 seconds after detection (:330 update, :376 fixesApplied:1) so it is NOT the 'model ignored the prompt' story it looks like; the skillPreferences contract that blocks the obvious fix; the four things already ruled out; and THE ONE UNPROVEN LOAD-BEARING FACT - nobody verified from Graph that the Comeet event's organizer really is scheduler@comeet-notifications.com, and the whole organizer-matching recommendation rests on it. Also carries the wider question the owner actually asked for: whether the detector/auto-fix split is right at all, since in active mode the write precedes any narration"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","source":"verify","ref":"W3-hardblock-text-narrows-owner-in-mpim","finding":"the hard-block text narrowing the OWNER's own direct-book inside an MPIM is the same design question as 103 and 112, not a separate item","rootCause":"src/core/orchestrator/buildTurnContext.ts:561 runs the pre-check whenever senderRole === 'colleague', which the MPIM clamp makes true for the owner's own messages","verdict":"converted","state":"open","note":"→ gh#154 'Owner authority across chat surfaces'. MANAGER MISS, corrected: 103 and 112 were converted at the owner's direction ('I suspect this should connect 112 together, as its the same idea') and W3 is literally the same idea again - a colleague-facing fix narrowing the owner's authority on a clamped surface - but it was left sitting in his open list. Same question, same issue, same answer required: should the clamp govern visibility only, or authority too. The narrowing is LIVE in production while the question is open, which is stated in the issue body"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"outrider","source":"verify","ref":"ENGINE-alreadyBuilt-payload-dropped","finding":"alreadyBuilt.passedIn reported 0 although 11 refs were passed in args, so triage had nothing to dedupe against and a lane paid a full dispatch to answer already-fixed","rootCause":"the bugger engine's arg handling - almost certainly a rename in the new scout arg contract, since bugger.js was being rewritten at 17:14 and the run fired at 17:16","verdict":"converted","state":"open","note":"→ infrastructure chat (the owner: '110 - handoff, forget about it, the other chat will fix it'). Converting rather than leaving it open because a FRAMEWORK bug is not the owner's to route - Maelle did nothing, there is no chat problem, and it was sitting in his --open list making him a router for work he cannot do. Sixth instance of the class the manifest exists to catch: a step that did nothing while the run reported success. Left alone it costs a full dispatch per already-built symptom, every night, indefinitely"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","source":"verify","ref":"createmeeting-density-shift-reads-stale-cache","finding":"the density shift's two cached reads are now live - but the SEVERITY WAS WRONG: it cannot double-book, it produces a counter-offer whose stated reason is false","verdict":"built","state":"built","note":"MANAGER'S FRAMING OVERRIDDEN, and this corrects what the owner was told. The stale read CANNOT double-book: at createMeeting.ts:1029 the block returns {success:false, error:'efficiency_counter'} and never writes, so any actual write re-enters handleCreateMeeting and planMeeting's live validation. And with >=1 attendee the candidate is FULLY RE-VALIDATED LIVE at :1013 - findAvailableSlots puts the owner's own row in the free/busy read (findAvailableSlots.ts:227, busyFilterEmails = [params.userEmail, ...attendeeBusyEmails]) and runs the live owner rule pass at :428 - so a candidate colliding with a fresh 13:30 comes back rejected, attendeeClean=false, no counter-offer. THE UNBACKSTOPPED CASE IS attEmails.length === 0 (the gate at :1005): a SOLO BLOCK. There the stale array is the sole authority for both earlierConnectiveStart and the checkSlot that blesses its candidate, and the failure is an M11 VIOLATION - a counter-offer whose stated reason ('the requested time leaves a short, unfocusable gap') is FALSE - followed by a walk-back, or a knowing collision if he accepts it (M9/M10). Real bug, same fix, lower blast radius than 'double-books him'. PREMISE VERIFIED before building: the density block's T00:00:00/T23:59:59 args normalize through toStartOfDayLocal/toEndOfDayLocal (calendarReads.ts:284-286) to the IDENTICAL key an earlier get_calendar 'what's on Thursday' writes, so the warm hit is real; and invalidateCalendarCache fires on our own mutations (calendarCache.ts:66), so out-of-band Outlook acceptance is the staleness source. BOTH sites converted including :399 (the recurring-category week read) because its answer is refuse-or-create, i.e. a decision read by the taxonomy, and leaving one cached decision read in the file is how the pattern gets copied; CalendarOfflineError there falls into the existing catch at :433 and Guard B still rethrows at :512-521, so nothing books blind. Traced: the full scenario before/after; COLD CACHE COSTS NO NEW ROUND-TRIP (before = miss -> memo miss -> 1 fetch; after = skip cache -> memo miss -> 1 fetch; delta 0->1 only on a warm day-copy); offline -> one force retry, then the counter-offer is skipped and the booking proceeds on planMeeting's live verdict, because an M7 nicety must not veto a validated booking; non-dense tenant -> blocks not entered, zero added reads"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","source":"verify","ref":"date-only-freebusy-with-midnight-time-reads-free","finding":"the whole-day recognizer now keys on the INSTANT rather than the spelling, so a midnight-to-midnight pair queries the day instead of one hour at midnight","rootCause":"src/connectors/graph/calendarReads.ts:741 - the old test was a regex on how the date happened to be written","verdict":"built","state":"built","note":"endIsWholeDay = date-only end, OR start === end where that instant is exactly startOf('day') in the CALLER'S zone (timezone, never the server's). Widened IN PLACE rather than by recursion, so -6 recursive lines +7. PROBED, not assumed: 'T17:00:00' twice is not midnight so availabilityPreCheck's instant path is BYTE-IDENTICAL, and 'T00:00:00Z' is 03:00 in Jerusalem so it correctly keeps its instant reading. EXHAUSTIVENESS ARGUED rather than enumerated: the predicate differs from V3's only by `sameInstant && endAtStartOfDay`, and sameInstant requires start===end, so the ONLY inputs whose behaviour changed are start===end===local midnight. 8/8 traced including inverted bare dates (nothingChecked, logging the widened end exactly as the old recursion did), inverted midnight pair, midnight->next-midnight, and unparseable input where the branch runs before any widening so the log is byte-identical"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","source":"verify","ref":"getfreebusy-62day-clamp-dst-fixed-point","finding":"the clamp now satisfies BOTH the minute ceiling and Graph's own 62-day statement, so the fall-back fixed point is gone without introducing a spring-forward one","rootCause":"src/connectors/graph/calendarReads.ts:784-794 - two spellings of one limit, calendar arithmetic in the clamp against absolute arithmetic in the test","verdict":"built","state":"built","note":"REJECTED THE MANAGER'S ONE-LINER, with a measurement: plain plus({minutes: 62*24*60}) is on a SPRING-FORWARD window one hour WIDER than 62 calendar days (2026-02-01 gives 89,220 today vs 89,280 proposed), and 62 days is how Graph states its own ceiling - so the one-liner would trade a fall-back crash for a possible spring-forward ErrorInvalidTimeInterval. Built instead as min(start + 62 calendar days, start + MAX_FREEBUSY_WINDOW_MINUTES): both readings hold at once, and off a transition it is BYTE-IDENTICAL to the old clamp (verified for 2026-07-27 and 2026-02-01). ALSO CORRECTED THE MANAGER'S WINDOW: affected start dates are 2026-08-24 .. 2026-10-24, not '...25 Oct' - 2026-10-25 + 62d is exactly 89,280 and safe. SKIPPED the belt-and-braces 'refuse when clamped === endDate' guard DELIBERATELY because it would be DEAD CODE: with both narrowings in place the only remaining self-call is the instant branch, which recurses with a 60-minute window, so windowMinutes > ceiling cannot be true at depth >= 2 - the state that guard would catch is unreachable by construction. That removal also retires #124h here: there is no second invocation left to forget to forward freshness/diagnostics to. Added MAX_FREEBUSY_WINDOW_MINUTES (one const, one unit - two spellings of one limit WAS the bug) and queryEnd, so the cache key, the log lines and the POST all read the window actually decided. Independently reproduced the Manager's probe before changing anything"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","source":"verify","ref":"remaining-cached-decision-reads","finding":"more of the freshness wave's residue in the meeting lane - call sites still on the 'cached' default that are deciding something, with availabilityPreCheck:664 the worst","verdict":"needs-owner-decision","state":"open","note":"RAISED BY MEETING, not built - deliberately, because the top one is not a one-liner. PRIORITY: availabilityPreCheck.ts:664 eventsForWeek is the event set checkSlot validates a colleague's 'is 17:00 free?' against, so stale means 'that works' followed by a refusal at booking - the exact walk-back class the H3 note at :634 says was just fixed. It is NOT a one-liner because the comment at :655-659 documents that this file deliberately calls neither decision helper so its throw is not a typed CalendarOfflineError, and a catch below depends on that - so the fix is either an error-contract change or the hand-passed-'live' anti-pattern the wave removed, which is a judgement worth its own dispatch. NEXT: calendarReads.ts:493 findDuplicateEvent (THE idempotency primitive) and :543 findReschedulableSibling (the create-vs-move guard) - defensible as-is BECAUSE writes invalidate (calendarCache.ts:66), so our own retries are covered and only out-of-band duplicates are missed; a decision, not obviously a bug. UNEXAMINED, same shape: moveMeeting.ts:892 and calendarHealth/handlers/checkHealth.ts:80. CHECKED AND WOULD LEAVE: autoMove.ts:52, because revalidateActiveOverlapIssues is fail-safe by design and moves a health REPORT row, not a booking. CORRECTLY 'cached' per the taxonomy and NOT findings: summary, news, briefs, buildTurnContext, background - those are reading the calendar out, not deciding with it"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"registrar","source":"verify","ref":"counter-renderer-english-in-hebrew-template","finding":"the amend relay is now composed in the requester's ACTUAL language, with the decided values pinned in code so they cannot drift","rootCause":"src/core/requests/resolver.ts - COUNTER_KEY_PHRASING is hardcoded English interpolated into a Hebrew template, and the composer was gated approve||reject","verdict":"built","state":"built","note":"REJECTED the Manager's label-localisation option, with a decisive reason rather than a preference: A LABEL TABLE CANNOT FIX THE HALF THAT MATTERS, because #153 deliberately OPENED the key set, so the keys needing a Hebrew label are precisely the ones nobody wrote prose for - {venue:'Tel Aviv 3'} has no anticipated label to translate. It also caps at he/en while resolveOutboundLanguageForPerson already coerces ru/es to English, and charter rule 4 says a fix that works in two languages is not a fix. The composer needs zero key knowledge and writes in the requester's real language. THE MANAGER'S OBJECTION IS KEPT IN CODE, NOT BY ABSTENTION: `pinned` carries the pre-rendered decided values ({key:'duration_min', value:'55'}), the prompt hands them over as 'DECIDED VALUES, copy each one exactly as given' with the labels explicitly marked as the translatable part, and then amendCompositionFault verifies every pinned value survived VERBATIM and that the composition invented no internal id. Any fault discards the composition and ships the deterministic template - so the LLM owns phrasing, CODE owns the decision, and the worst case is tonight's clumsy-but-complete sentence. Two shapes deliberately STAY on the template, which is also what preserves the byte-identical requirement: a counter with no machine-labelled part (pure owner prose - already human words) and a QUESTION-shaped counter (his question must travel verbatim); composeAmend requires amendPinned.length > 0 and the question branch never fills it. NAMED RESIDUAL: a question BUNDLED with a structured key keeps its English parenthetical label. summarizeCounter is GONE, replaced not wrapped by one renderCounter(data, {audience, formatInstant}) returning {text, pinned, prose, withheld}, read by all four call sites - so 'may it render' and 'how does it render' are decided in the same loop iteration over the same entry, which is exactly the two-predicates-over-one-key-set hazard the Manager warned about"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"registrar","source":"verify","ref":"amend-relay-bypasses-output-gates","finding":"the relayed text is now constructed only from classified parts, and an internal work-item id is refused AT THE WRITE so it can never become a silent omission","rootCause":"src/core/requests/resolver.ts:1033 - the relay ships via conn.sendDirect so securityGate never sees it, while the renderer had begun emitting every model-authored key","verdict":"built","state":"built","note":"HOW THE ALLOWLIST-VS-#153 TENSION WAS RESOLVED, which is the part worth keeping: a PURE allowlist provably cannot decide this data, because {venue:'Tel Aviv 3'} (must relay) and {request_id:'req_abc123'} (must not) are both short opaque strings under unlisted keys - no key-list or value-shape allowlist separates them. The only thing that does is THE ID SHAPE WE OURSELVES MINT, which is not a guess: INTERNAL_WORK_ITEM_ID_RE is the inverse of the four minting expressions in this repo (db/requests.ts:51, tasks/index.ts:13, db/jobs.ts:164, db/calendarIssues.ts:372). req_/task_ match loosely because neither prefix begins an English word and a model-INVENTED req_abc123 must still be caught; out_/ci_ require the minted <epoch>_<base36> tail so out_of_office is not swallowed. Scope is exactly the residual gap: scrubInternalLeakage already runs on this path and handles account ids, Graph ids, IANA tokens, sentinels and tool names, so those need no second veto, and securityGate.ts / textScrubber.ts (guard's) were not touched. No attempt to route a relay through postReply - R1 says relays have no gate stack BY DESIGN, so the fix is the payload one. THE SILENT-DROP TENSION IS RESOLVED BY MOVING THE REFUSAL TO THE WRITE, NOT THE READ: resolve_approval is the ONLY construction site of verdict:'amend' (verified - handlers.ts:846 and threadBoundApprovalAutoResolve.ts:248 never produce amend), so it runs the same renderer BEFORE storing and returns `unrelayable_counter` NAMING THE KEY; the counter is never stored, the id never reaches the replay args either, and the model restates. Post-fix a withheld key is impossible, so it can never be a quiet omission - the relay keeps the veto intrinsically for rows written before this gate, plus a warn naming the keys. audience:'owner' withholds NOTHING, because hiding a decided value from the decider is worse than showing him his own req_ id on a surface no gate scrubs. 11/11 traced including the pin-fails case (Haiku writes 'חמישים וחמש' -> dropped -> warn -> template ships, no drift and no loss), an id nested inside {new_slot:{room:'req_...'}}, an id inside PROSE (the veto is on the rendered part so prose is not an entry route), and byte-identical confirmations for {text} legacy, both question-shaped forms, the owner bounce-back and approve/reject"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","source":"verify","ref":"securitygate-internal-ref-regex-looser-copy","finding":"securityGate's internal_ref_id pattern is a looser, drifted copy of the id token the requests lane now owns - it false-positives on out_of_office, and one of its prefixes is dead","rootCause":"src/utils/guards/securityGate.ts:126 - `(?:req|task|coord|out|ci)_[a-z0-9_]+`, where coord_ refers to a subsystem that was removed","verdict":"needs-owner-decision","state":"open","note":"Handed off by requests, explicitly non-blocking - nothing in tonight's fix waits on it. It should read INTERNAL_WORK_ITEM_ID_RE from the lane that MINTS the ids, exactly the way it already reads RAW_SLACK_ID_RE from the module that owns account ids. Two copies of one token is the same class as the arming-test-vs-tier-list defect that blocked tonight's wrap, and this one is already drifted rather than merely at risk of drifting"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"gatekeeper","source":"verify","ref":"FINAL-VERIFY-five-builds","finding":"last gate before the wrap: NOTHING BLOCKING across the four files touched in the last hour, and the cross-lane seam the Manager was most worried about turned out not to exist","verdict":"audit","state":"built","note":"tsc green, run by the verifier. All four diffs read INDIVIDUALLY and each checked for a clean terminating hunk - the truncation that lost five files earlier tonight did not recur. EARNED CLEAN, the parts worth keeping: verdict:'amend' has EXACTLY ONE construction site (tasks/skill.ts:1344), proven BY SEARCH not by reading - the string 'amend' does not occur anywhere in handlers.ts and threadBoundApprovalAutoResolve types its verdict as 'approve'|'reject'|'pass_to_sonnet' - so the write-time refusal's whole premise holds. All four minting expressions match the id veto AND three MORE of the same shape were found that also match (calendarIssues.ts:644, :726, categoryOps.ts:146); Date.now() is 13 digits so it satisfies \\d{10,}; out_of_office is correctly not swallowed; and a subtle safety nobody stated - the veto tests the RENDERED part while key.replace(/_/g,' ') strips underscores from key names first, so a `task_id` KEY cannot self-trigger, only a value can. The fallback genuinely ships the template on all three failure routes (empty completion, thrown request, fault), body is definitely assigned in the verdict chain, and `composed` never escapes its if-block so the check is before the send on the one and only body. THE CLAMP TERMINATES AND CANNOT RE-ENTER: after clamping to min(byCalendar, byMinutes) the window is under the ceiling in both directions of a DST transition, and the only surviving recursion (the instant branch) cannot re-enter either branch - notably the whole-day disjunct's CONJUNCTION is what saves the 23:00-instant case, where the widened end IS local midnight. The recognizer claim is exact: the added disjunct is literally `parsedEnd === parsedStart && parsedEnd === parsedEnd.startOf('day')`. THE SEAM THE MANAGER PRIORITISED DOES NOT EXIST: availabilityPreCheck never calls getFreeBusy at all - it imports only getCalendarEvents (:26, called at :664), and its getFreeBusy mention at :646 is a QUOTATION OF HISTORICAL LOG LINES. The untyped throw survives (nothing in the diff adds a catch between the impl's rethrow and :664). The offline path does not veto a planMeeting-validated booking and is now strictly LESS abort-prone, since both old and new throw but only the new retries first. NO NEW GRAPH ROUND-TRIP on a cold cache, because turnCache.memoize stores the PROMISE in a turn-scoped Map so sequential callers share a resolved read, not just concurrent ones - the added cost is cross-TURN only, which is the intended trade. The FbRead shape change cannot resurrect an old-shape entry because the fbKey format changed in the same edit. renderCounter's ISO detection is safe on prose (unanchored match, but formatInstant returns '' and `|| s` restores the original). Write-time and relay-time renderings agree, so a counter accepted at write time always renders at relay time. WORTH-KNOWING beyond the new row: amendCompositionFault is a SUBSTRING test, so the Manager's specific cases resolve correctly (55 -> '55 דקות' passes, 50 fails, a Hebrew numeral and a 1,500 separator both fail to template) but the other direction is open - 'instead of 55 he proposes 45' contains 55, passes, and inverts the decision, and for a 1-2 character pinned value the test is near-vacuous. SEVERITY IS CAPPED BY DESIGN AND THE CAP IS REAL: skill.ts:1344 stores the RAW counter, so the replay on her checkmark books the owner's actual decision - a drifted sentence never becomes a drifted booking. Also: the composed amend is genuinely multilingual only for HEBREW, since resolveOutboundLanguageForPerson collapses es/ru/ar to 'en' and formatStart then renders the pinned instant in English for the composer to copy into Spanish prose - not a defect and not new, but the change delivers less than its headline for exactly the readers the lane cited as justification. TWO of the four byte-identical claims are NOT byte-identical, both deliberately and both improvements: the owner bounce-back for a slot_iso counter went from 'a different time (2026-08-03T14:00:00)' to 'a different time: Monday 3 Aug, 14:00', and the question-shaped body is byte-identical only with no reason and no non-text key. One new within-turn coupling: the normalized getCalendarEvents key means availabilityPreCheck's cached read and a decision read for the same day now share a memo entry, so on a cold cross-turn cache a decision read's Graph failure poisons the memo and the preCheck read inherits it - lands in preCheck's own catch, reverse direction protected because 'force' skips the memo. NOT COVERED, named: the #149 due_at/toTimerInstant block in the same skill.ts diff, which ships at 4.2.2 but was not one of the five items (toTimerInstant itself sanity-read and correct)"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"registrar","source":"verify","ref":"amend-reason-bypasses-id-veto","finding":"the id veto guards the counter but NOT the `reason` beside it, so an internal work-item id in the owner's reason reaches a colleague - and the fault check's reaction actively ships the leaky path","rootCause":"src/core/requests/resolver.ts:910, :919, :931, :934 - `rationale` is concatenated straight into `body` and into `amendProse` without passing through renderCounter, and the write-time refusal in tasks/skill.ts calls renderCounter(counter, ...) on the COUNTER ONLY","verdict":"needs-owner-decision","state":"open","note":"Found by the final verify. SCENARIO: the owner replies 'amend req_1753... to 55' and Sonnet passes reason:'as per req_1753812004_k3m1'. The write-time refusal PASSES because the counter is clean; the id then enters both the template and the composer's prompt; and amendCompositionFault's second clause fires on the composition that faithfully ECHOED it - discarding the composed text in favour of the template, WHICH CONTAINS THE SAME ID. So the fault check makes it worse, not better, on this input. The docblock's claim that 'the id never reaches a colleague / a withheld part never enters the prompt' is true of counter keys and NOT of reason. Impact is an opaque token in a colleague DM with no private data, on a relay that ships via conn.sendDirect so securityGate never sees it. THE CLASS IS PRE-EXISTING - the reject template at :887 has relayed `reason` verbatim for a long time - what is NEW tonight is an id CHECK sitting next to an unchecked sibling, which is the asymmetry worth fixing: one function now decides whether a counter part may be relayed while the field beside it passes through by default. High confidence on the mechanism (both paths read), low-moderate on trigger rate"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"matchmaker","source":"verify","ref":"remaining-cached-decision-reads","finding":"OWNER DEFERRED to 2026-07-28 - more of the freshness wave's residue on the 'cached' default at sites that are deciding something","rootCause":"src/utils/availabilityPreCheck.ts:664 (worst) plus calendarReads.ts:493/:543, moveMeeting.ts:892, calendarHealth/handlers/checkHealth.ts:80","verdict":"needs-owner-decision","state":"deferred","note":"Owner's words: 'defer them both for tomorrow'. Report row 118. NOTHING IS FIXED, so the symptom recurs indefinitely and an honest log review will re-find it every night - this row MUST go into the next run's openKnown or it comes back as a fresh bug. Recommended build with its own dispatch: availabilityPreCheck:664 is the event set checkSlot validates a colleague's 'is 17:00 free?' against, so stale means 'that works' followed by a refusal at booking, and it is NOT a one-liner because the comment at :655-659 documents that the file deliberately calls neither decision helper so its throw is untyped, with a catch below depending on that"}
{"date":"2026-07-27","runId":"owner-critical-bunnings","lane":"registrar","source":"verify","ref":"amend-reason-bypasses-id-veto","finding":"OWNER DEFERRED to 2026-07-28 - the id veto guards the counter but not the `reason` beside it","rootCause":"src/core/requests/resolver.ts:910, :919, :931, :934 - rationale is concatenated straight into body and amendProse without passing through renderCounter, and the write-time refusal checks the counter only","verdict":"needs-owner-decision","state":"deferred","note":"Owner's words: 'defer them both for tomorrow'. Report row 119. NOTHING IS FIXED - must go into the next run's openKnown. Small fix: route `reason` through the same classifier as the counter parts. Note the fault check currently makes this input WORSE, not better: it fires on the composition that honestly echoed the id and falls back to the template, which contains the same id"}
{"date":"2026-07-28","runId":"wf_a9bc2bf7-43b","lane":"outrider","source":"owner","ref":"gh#24","finding":"gh#24 stage 1 — no email transport existed; owner wants to forward a meeting-request email to Maelle and get options back by email","rootCause":"src/connectors/graph/graphClient.ts:9-15 (app-only credential only; no mail module, no delegated auth mode)","verdict":"built","state":"built","note":"Graph mail module + DELEGATED OAuth (owner rejected app-only: tenant-wide Mail.* needs org Exchange RBAC, which makes Maelle un-cloneable) + channels.email schema extension + scripts/email-auth.mjs. Verify overturned once on a false doc claim (re-running the script alone does NOT resume polling; mailPoll latches revokedProfiles in memory, cleared only by restart) — fixed."}
{"date":"2026-07-28","runId":"wf_a9bc2bf7-43b","lane":"outrider","source":"owner","ref":"gh#24","finding":"gh#24 stage 2 — inbound email needs a cadence; owner rejected 5-minute latency and there is no ingress for a webhook","rootCause":"src/core/background.ts:206 (the only clock is the 5-min due-task tick)","verdict":"built","state":"built","note":"Own ~30s interval using /messages/delta with the deltaLink as a durable watermark; marks processed messages read as a second dedup. Residual: on a local box the interval stops when the machine sleeps, so real latency is unbounded — wants the GCP cutover."}
{"date":"2026-07-28","runId":"wf_a9bc2bf7-43b","lane":"slacker","source":"owner","ref":"gh#24","finding":"gh#24 stage 3 — Maelle must never email an external; the cap is the control the owner's whole risk position rests on","rootCause":"src/connections/email/index.ts:45 (cap validated to+cc but NOT opts.bcc, while SendOptions.bcc is legally passable)","verdict":"built","state":"built","note":"EmailConnection with a one-address send cap inside sendDirect; other verbs return not_supported. Verify overturned on a FALSE COMPLETENESS CLAIM on this exact function — lane made the claim true (bcc now validated) rather than softening the comment. Latent, never live: zero callers pass bcc."}
{"date":"2026-07-28","runId":"wf_a9bc2bf7-43b","lane":"slacker","source":"owner","ref":"gh#24","finding":"gh#24 stage 4 — inbound front door: sender gate, thread key, participant extraction from the forwarded body","rootCause":"no inbound email path existed; a forward's own toRecipients is only Maelle, so original participants live in the BODY","verdict":"built","state":"built","note":"Sender gate first, silent to sender but logs rejectedFrom. Verify overturned on three items, one REAL: conversation history was written BEFORE send success, so one failed send would commit a phantom assistant reply and a duplicate user turn on retry. Also an HTML entity double-decode ordering bug (&amp; decoded before &lt;). Both fixed."}
{"date":"2026-07-28","runId":"wf_a9bc2bf7-43b","lane":"gatekeeper","source":"owner","ref":"gh#24","finding":"gh#24 stage 5 — a non-Slack reply passed through NO output gates at all","rootCause":"runOutputGates reachable only from postReply; whatsapp.ts hand-strips markdown and sends with zero gates","verdict":"built","state":"built","note":"Transport-neutral gate entry at audience:'external' — a value humanGate.ts:83 defined and nothing had ever passed. Gated in the EXTERNAL frame though delivered to the owner, because he forwards it verbatim: the gate follows the eventual READER."}
{"date":"2026-07-28","runId":"wf_a9bc2bf7-43b","lane":"profiler","source":"owner","ref":"gh#24","finding":"gh#24 stage 6 — externals on a forwarded chain should earn person rows, but a long chain is a harvesting surface","rootCause":"no call site resolved email-sourced addresses; db/people.ts primitives already existed","verdict":"built","state":"built","note":"Reuses getPersonByEmail/upsertPersonMemory. Only the addressing of the message she is acting on earns a row; an address quoted deeper never does."}
{"date":"2026-07-28","runId":"wf_a9bc2bf7-43b","lane":"matchmaker","source":"owner","ref":"gh#24","finding":"gh#24 stage 7 — a forwarded pick must bind to the option already offered, across an overnight round trip","rootCause":"src/utils/offeredSlotsStash.ts:46-52 (TTL 2h, in-memory — a Slack tempo, not an email one)","verdict":"built","state":"built","note":"Reused the existing stash keyed on (channelId,threadTs) rather than a new table — chain-as-state was REJECTED as re-deriving a decided instant from prose (the Liza class). Email leg gets ~48h + restart survival, scoped so Slack is unchanged."}
{"date":"2026-07-28","runId":"wf_a9bc2bf7-43b","lane":"outrider","source":"owner","ref":"gh#24","finding":"gh#24 stage 9 — a connection's tools shipped on EVERY turn, and an email turn must not reach Slack or move/cancel meetings","rootCause":"src/skills/registry.ts:511 (looped listConnections and merged every connection's getTools into every turn)","verdict":"built","state":"built","note":"Owner's correction: 'the process need to be base of transport layer and load only what needed'. Seam fixed rather than worked around. Verify overturned: the clamp also stripped ALWAYS_ON_TOOLS, so an email turn could not get_person_memory — it could not read the rows stage 6 creates on the same turn. Stage 6 + stage 9 each right alone, wrong together — the class only a combined verify finds. Clamp is now the owner's exact four tools."}
{"date":"2026-07-28","runId":"wf_a9bc2bf7-43b","lane":"instructor","source":"owner","ref":"gh#24","finding":"gh#24 stage 10 — the prompt already told Maelle email 'reaches anyone with an email address', and it would go live the moment a connection registered","rootCause":"src/core/orchestrator/systemPrompt.ts:520","verdict":"built","state":"built","note":"A latent lie with an activation trigger — inert only because nothing was registered. Narrowed to the reach the code enforces. Same class as the Maya/Comsec incident that put that block there."}
{"date":"2026-07-28","runId":"direct-followup-wf_a9bc2bf7-43b","lane":"profiler","source":"owner","ref":"self-row-email-reconcile","finding":"assistant.email in the profile was an address that NEVER existed, and correcting it did not propagate to the person store","rootCause":"src/core/assistantSelf.ts:38 (early-returns when existing.name === profile name; the name never changes, so email/timezone/gender were never reconciled)","verdict":"built","state":"built","note":"Consequence: getPersonByEmail on the REAL address would not match the self row and would mint a SECOND person row for Maelle — the one-human-one-row violation v4.1.0 exists to prevent, made reachable by the email chain. Live DB row p_SELF_U0F28CK6H repaired out of band (invisible in any diff). Returned needs-dependency to meeting."}
{"date":"2026-07-28","runId":"direct-followup-wf_a9bc2bf7-43b","lane":"matchmaker","source":"owner","ref":"createMeeting-attendee-case","finding":"the owner/assistant attendee-exclusion filter compared addresses in raw case, so a differently-cased address stayed a booked attendee","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:650","verdict":"built","state":"built","note":"Routed from people as a dependency ask. Inert while assistant.email was a non-existent address; the owner-exclusion half was always live. Lane found and fixed a SIBLING with the same gap: the room-mailbox dedup would push a case-variant room address as a duplicate attendee. moveMeeting checked and left alone — it normalizes at source instead."}
{"date":"2026-07-28","runId":"verify-wf_a9bc2bf7-43b","lane":"slacker","source":"verify","ref":"gh#24-stage-4-send-retry","finding":"row 120 — a transient Graph send failure loses the owner's forwarded request PERMANENTLY; he forwards, waits, and nothing ever arrives","rootCause":"src/connectors/email/inbound.ts:19-24,189-195 documents a retry contract that mailPoll.ts:113-118 correctly contradicts — Graph delta is consume-once and the watermark has already advanced","verdict":"needs-owner-decision","state":"open","note":"OVERTURN, blocks the wrap. The history-ordering fix was built on a false premise about another lane's file — caused by the orchestrator dispatching slack and outer in PARALLEL onto a shared seam. Owner must choose: hold the deltaLink (real retry), inline send-retry then fail loudly, or accept loss + Slack DM. Recommended inline retry falling back to a Slack DM. Sequential with gh#24-E3E4-addrlist (shares inbound.ts)."}
{"date":"2026-07-28","runId":"verify-wf_a9bc2bf7-43b","lane":"instructor","source":"verify","ref":"gh#24-E9E10-prose-reachability","finding":"row 121 — the email turn's prompt teaches tools the clamp strips, so she promises to DM a colleague or escalate and then cannot, on text the owner forwards verbatim to an external","rootCause":"src/core/orchestrator/systemPrompt.ts:337 (owner branch has NO reachability filter; only the colleague branch at :342 does) plus a second unclamped derivation at src/skills/general.ts:228","verdict":"needs-owner-decision","state":"open","note":"OVERTURN, blocks the wrap. Highest-stakes surface in the feature — an honesty failure on text going to an external over the owner's name, plus full skills prose billed on every email turn. Fix is ONE seam and must not land twice. Spans context (systemPrompt) + outer (general.ts)."}
{"date":"2026-07-28","runId":"verify-wf_a9bc2bf7-43b","lane":"outrider","source":"verify","ref":"gh#24-stage-9-clamp-intersection","finding":"row 122 — a scope-classifier misfire silently strips find_available_slots and create_meeting, so she cannot schedule and answers with prose like 'I'll take a look'","rootCause":"src/skills/registry.ts filterToolsByScope computes (ALWAYS_ON union scope) then intersects with the clamp; find_available_slots/create_meeting are meetings-scope only, and intent_aware_tools:true runs the classifier over the whole forwarded chain","verdict":"needs-owner-decision","state":"open","note":"OVERTURN, blocks the wrap. Breaks the feature's core function invisibly on a Haiku misclassification. On this leg the classifier can only SUBTRACT from an already-4-tool set, so running it is pure downside — recommend pinning the email turn's scope and skipping it."}
{"date":"2026-07-28","runId":"verify-wf_a9bc2bf7-43b","lane":"slacker","source":"verify","ref":"gh#24-E3E4-addrlist","finding":"row 123 — two independent copies of the owner-address list under a comment claiming there is one shared source","rootCause":"src/connectors/email/inbound.ts:49-53 (ownerAllowedSenders) and src/connections/email/index.ts:37-41 (allowedRecipients)","verdict":"needs-owner-decision","state":"open","note":"OVERTURN, blocks the wrap. Divergence yields a sender accepted at the door whose reply the cap refuses — message read and acted on, no reply delivered. The lane fixed one false completeness claim in this same file and left this one. Sequential with gh#24-stage-4-send-retry."}
{"date":"2026-07-28","runId":"verify-wf_a9bc2bf7-43b","lane":"instructor","source":"verify","ref":"systemprompt-owner-branch-reachability","finding":"row 124 — LIVE ON SLACK TODAY: every scope-narrowed owner turn renders prose for skills whose tools were filtered out","rootCause":"src/core/orchestrator/systemPrompt.ts:337 — #15's reachability fix landed on the colleague branch only","verdict":"needs-owner-decision","state":"open","note":"DISCOVERY, not this wave's — do not build in-wave. Pre-existing and shipping now with Module G enabled; the email clamp only made it visible. Likely fixed by row 121's seam but must be verified separately rather than assumed."}
{"date":"2026-07-28","runId":"verify-wf_a9bc2bf7-43b","lane":"matchmaker","source":"verify","ref":"slot-hold-release-dm-role-gate","finding":"row 125 — the slot-hold release DM is not role-gated, so an email-triggered booking can Slack-DM a colleague whose tentative hold was released","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:1352-1375","verdict":"needs-owner-decision","state":"open","note":"The ONE Slack emission reachable from the email path. Arguably correct and identical to booking from Slack, discloses only to the internal holder — but constraint (3) 'no Slack shadow DM on this path' was stated absolutely. Recommended accept as-is."}
{"date":"2026-07-28","runId":"verify-followup-wf_a9bc2bf7-43b","lane":"instructor","source":"verify","ref":"gh#24-E9E10-prose-reachability","finding":"row 121 PARTLY closed — the owner-branch reachability filter is built and the live Slack leak is closed, but meetings.ts's ~400-line prose still renders in full on the email leg","rootCause":"src/skills/meetings/... prose block ~lines 983-1384: two of its ~9 tools survive the 4-tool clamp, so skill-LEVEL gating cannot suppress move/cancel and create_approval(policy_exception) language","verdict":"built","state":"built","note":"Measured on the live Slack path: a scope-narrowed owner turn went 55,183 -> 1,642 chars of prose (~13.4k tokens saved per turn), and a plain unscoped owner turn is BYTE-IDENTICAL. Net on an email turn -6,629 tokens. RESIDUAL, named not hidden: splitting the meetings monolith into channel-aware paragraphs is a separate larger effort; the new external frame is a prompt-only partial mitigant. Also corrected the orchestrator's own framing — 'DM a colleague' came from channelsYouCanReach (fixed), not from the skills prose, and 'escalate for approval' lives in the static HONESTY RULES block and was deliberately left."}
{"date":"2026-07-28","runId":"verify-followup-wf_a9bc2bf7-43b","lane":"instructor","source":"verify","ref":"orphaned-coremodule-prompt-sections","finding":"OutreachCoreSkill and TasksSkill carry getSystemPromptSection prose — the whole ## OUTREACH, ## TASKS and ## APPROVALS blocks — that has apparently NEVER rendered","rootCause":"both are CORE_MODULES, not members of getActiveSkills, and getSystemPromptSection is called from exactly two sites that both iterate getActiveSkills","verdict":"needs-owner-decision","state":"open","note":"DISCOVERY, not this wave's — do not build in-wave. Prose that is written and maintained but has never reached the model, including the entire approvals workflow guidance. Wiring it back in is an UNTESTED behaviour change on the live path, so it needs a deliberate decision rather than a quiet fix. Pending confirmation by the final verify pass."}
{"date":"2026-07-28","runId":"verify-followup-wf_a9bc2bf7-43b","lane":"instructor","source":"verify","ref":"channelsYouCanReach-colleague-near-miss","finding":"a first-pass fix to the CHANNELS YOU CAN REACH block silently broke the COLLEAGUE path — every colleague turn lost the Slack bullet and fell back to 'no channels currently registered'","rootCause":"the new gate on shippedToolNames.has('message_colleague') applied to both paths; colleagues never ship that tool at all, clamp or not","verdict":"built","state":"built","note":"Caught by the lane BEFORE returning, by re-executing the real builder rather than paper-tracing, and fixed with an !isOwner guard. Recorded because it is a live-Slack near-miss on the production colleague path and the class matters: the fix for an email-only leak reached a surface the email feature never touches. Sent to the final verify to confirm both directions."}
{"date":"2026-07-28","runId":"cleanup-wf_a9bc2bf7-43b","lane":"outrider","source":"verify","ref":"gh#24-dead-buildSkillsPromptSection","finding":"the prompt-budget tool was sizing a skills section production never emits — buildSkillsPromptSection lost its only production caller and kept a channel param nobody passed","rootCause":"src/skills/registry.ts:749 (dead export) + scripts/measure-prompt.ts:8,41 (its only remaining caller, reconstructing the section by string replace)","verdict":"built","state":"built","note":"Deleted rather than moved: the assembly loop's inputs are local to buildSystemPromptParts, so registry was always the wrong seam for prompt-assembly logic. measure-prompt.ts now calls buildSystemPromptParts directly and reports its real static/dynamic split — the same one the orchestrator caches on. Also updated the stale reference in .claude/PROMPT_REDUCTION_STARTER.md by hand (no lane owns .claude)."}
{"date":"2026-07-28","runId":"cleanup-wf_a9bc2bf7-43b","lane":"outrider","source":"verify","ref":"gh#24-clamp-comment-tense","finding":"the CHANNEL_TOOL_CLAMP doc block still asserted the OLD ordering in the present tense while a later paragraph explained that ordering was the bug","rootCause":"src/skills/registry.ts CHANNEL_TOOL_CLAMP doc block, paragraph 2","verdict":"built","state":"built","note":"Fixed to past tense with a forward pointer. Worth more than a normal comment fix: row 120 of this same wave exists BECAUSE a lane built real ordering logic on a wrong comment in this feature. Second false-claim-in-a-comment defect found in gh#24, after stage 3's send-cap completeness claim."}
{"date":"2026-07-28","runId":"row124-ruling","lane":"instructor","source":"owner","ref":"persona-block-reachability-exception","finding":"row 124 — the row-121 reachability filter also gated the PERSONA block, so Maelle read clipped and transactional on every scope-narrowed owner turn and on the whole email leg","rootCause":"src/core/orchestrator/systemPrompt.ts:353-357 gated SocialSkill.getSystemPromptSection on note_about_person/note_about_self, which live in the people scope and are not ALWAYS_ON","verdict":"built","state":"built","note":"Owner ruled: 'keep same prompt as internal, she is still same personally for external'. Restored as a second deliberate exception on an explicit IDENTITY-vs-CAPABILITY axis — the filter polices capability prose (a promise that dies on not_permitted); identity prose is not that. MEASURED: unscoped Slack owner turn byte-identical (0); scope-narrowed +304 tok; email +349 tok. Against row 121's ~53.5k-char saving that is ~2%, so the ruling cost almost nothing. The lane CORRECTED the orchestrator's premise: the persona block does instruct note_about_person calls, so 'teaches no tools' was an overstatement — but the block says the save never replaces the reply, and the end-of-chat capture pass is the designed backstop, so no honesty gap is reopened. Also found the email frame's precedence clause AMBIGUOUS once the persona started rendering there and strengthened it: 'same person, just the register a stranger reads, not a colleague.'"}
{"date":"2026-07-28","runId":"email-test-round-1","lane":"gatekeeper","source":"logs","ref":"dateverifier-hebrew-false-positive","finding":"the dateVerifier flagged a weekday mismatch where written and correct were IDENTICAL, then rewrote text that was already right — on the leg whose output is forwarded verbatim to a client","rootCause":"utils/dateVerifier.ts — writtenWeekday 'שלישי' vs correctWeekday 'שלישי' both logged as a mismatch for date 2026-07-28, matchedText 'ביום שלישי היום (28.7)'; a Hebrew string comparison or normalization defect","verdict":"needs-owner-decision","state":"open","note":"CONFIRMED from a real production run (first ever live email turn, 22:49:16). Not awaiting a decision — awaiting BATCHING: the owner is collecting more test results before dispatching a fix round. A guard that edits outgoing text on a false positive is worse than one that stands down."}
{"date":"2026-07-28","runId":"email-test-round-1","lane":"matchmaker","source":"owner","ref":"M3-attendee-tz-fallback-owner-zone","finding":"an external attendee with no known timezone (natanamid@gmail.com) was assumed Asia/Jerusalem 09:00-18:00 — the owner's own zone","rootCause":"utils/attendeeAvailability.ts no-TZ fallback uses the owner's zone","verdict":"declined","state":"closed","note":"OWNER RULED 2026-07-28: 'its ok, if you don't get timezone, assume my timezone so its ok.' Closed as acceptable behaviour, not a bug. NOTE the boundary: this settles the OWNER-initiated case, which is every email-path turn since he is always the forwarder. The separate SESSION_STARTER M3 item — that the fallback should use the REQUESTER's zone rather than the owner's — still stands for a COLLEAGUE in another zone, where the two differ. Do not read this ruling as closing that."}
{"date":"2026-07-29","runId":"email-test-round-1-fixes","lane":"outrider","source":"logs","ref":"gh#24-stage-4-reply-capability","finding":"row 130a — sending composed a fresh message, so the chain was cut and round-2 offer binding could never work","rootCause":"src/connectors/graph/mail.ts sendMail used /me/sendMail with no quote and no In-Reply-To/References","verdict":"built","state":"built","note":"Added replyToMail using createReply -> patch body -> send (chosen over the one-call /reply, whose comment lands in a Graph-owned template). Quote, Re: prefix and threading headers all come from Graph. ReplyToMailOptions deliberately has NO to/cc — the recipient is structural. Nothing wired to it here; transporter switched the caller over in round 2. NOTE: the Graph MAIL layer moved from outrider to TRANSPORTER at 00:08 the same night, so this was the last mail-layer work outrider will own."}
{"date":"2026-07-29","runId":"email-test-round-1-fixes","lane":"outrider","source":"logs","ref":"gh#24-social-spine-on-email","finding":"row 131 — the social spine ran on an email turn and produced a coda that was never delivered","rootCause":"src/core/orchestrator/buildTurnContext.ts:150 — needIntent did not consider the transport","verdict":"built","state":"built","note":"One-line gate: needIntent now also requires channel==='slack'. That kills BOTH mechanisms, because with socialClassification left null the end-of-turn coda block's own check is false too — no second gate needed. classifyTurn itself is untouched, since its meetingPeople half is load-bearing on the email leg. CORRECTED THE ORCHESTRATOR: no subject was actually burned — the coda log comes from orchestrator/index.ts, and recordCodaDelivered only fires from postReply on confirmed Slack delivery. It was wasted compute plus a dropped coda, not a spent topic."}
{"date":"2026-07-29","runId":"email-test-round-1-fixes","lane":"gatekeeper","source":"logs","ref":"gh#24-gate-rewrite-log","finding":"row 134 — humanGate logged a rewrite with byte-identical before/after previews, and dateVerifier claimed 'correcting' on a turn where nothing changed","rootCause":"humanGate.ts previewed with .slice(0,120) from index 0; runOutputGates.ts logged the date correction BEFORE the swap loop ran","verdict":"built","state":"built","note":"Established the truth empirically by pulling the persisted turn from conversation_threads: the humanGate rewrite was REAL, 217 chars, continuing past the 120-char cutoff. Fixed with a diff-windowed preview carrying both lengths. SEPARATE REAL BUG FOUND in the dateVerifier caller: the 'correcting deterministically' log fired on flag rather than on change, so row 126's Hebrew case logged a correction that the swap guard had already prevented — CORRECTING THE ORCHESTRATOR, which reported that guard as silently editing client-bound text. It was not. claimChecker, securityGate, the deliberation guard and availabilityGate all checked and left alone — they log accurately."}
{"date":"2026-07-29","runId":"email-test-round-1-fixes","lane":"profiler","source":"logs","ref":"gh#24-attendee-tz-persistence","finding":"row 129b — nothing could ever set a timezone on a pure-email external, so the owner-zone fallback was the only possible outcome","rootCause":"src/db/people.ts — setCoreFieldWithProvenanceById existed but had no email-keyed entry point; every timezone write required a slack_id","verdict":"built","state":"built","note":"Added setPersonTimezoneByEmail(email, timezone, by, opts) -> {outcome, personId}: resolves through the identity chokepoint, writes through the owner>person>auto chain, REFUSES non-IANA input, never throws. ALSO closed a gap that would have made row 129 fail anyway — refreshAutoWorkingHours was slack_id-only, so an external could get a zone and still have no hours, and loadAttendeeAvailabilityForEmails does `if (!wh) continue`, silently SKIPPING them from the clip. Additionally retrofitted the same gap in assistant.ts's update_person_profile external branch — an unrequested live-Slack-path change, flagged to the owner for veto."}
{"date":"2026-07-29","runId":"email-test-round-2","lane":"slacker","source":"logs","ref":"gh#24-email-round2","finding":"rows 132+133+137 (the second participant path), 129a (extract a stated zone), 130b (send as a real reply) — four items, one dispatch","rootCause":"connectors/email/extractParticipants.ts parsed Outlook's reply-quote header as a forward header; connections/email/index.ts called sendMail rather than replying","verdict":"built","state":"built","note":"CORRECTED THE ORCHESTRATOR TWICE, both from the persisted turn text rather than the log summary. (1) Row 133 was NOT lossy extraction — run 3 had NO genuine forward at all, because the prior reply had cut the chain, so the extractor found only maelle@+idan@ from Outlook's reply-quote block and the owner/assistant filter emptied it. Row 133 is a SYMPTOM of row 130. (2) Run 2's chain states no timezone anywhere, so the 6am was the owner's own sanctioned tier-3 fallback applying CORRECTLY; the 'He needs EST time' signal arrived in run 3, after the offer. Row 129 is a missing capability, not a defect that fired. Also: locationTz mapped 'et' but NOT 'est', so B1 would have silently failed on the evidenced text — added EST/EDT, CST/CDT, MST/MDT, PST/PDT. Extraction now feeds the SAME resolvedMeetingAttendees route via a new extractedAttendeeEmails field (Slack never sets it, so that path no-ops). sendDirect now requires replyToMessageId and calls replyToMail with the one-address cap still running first; sendMail and SendMailOptions DELETED, no second send path. Flagged its own boundary crossing into orchestrator/index.ts + buildTurnContext.ts for the verifier."}
{"date":"2026-07-29","runId":"email-test-round-3","lane":"matchmaker","source":"verify","ref":"gh#24-129c-present-in-timezone","finding":"row 129c — a cross-zone offer rendered only in owner-local time, so a 06:15 ET slot read as a comfortable 13:15 and nothing on the page said otherwise","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts — presentation_local rendered only when the caller explicitly passed present_in_timezone; it was never passed on any of the three live email runs","verdict":"built","state":"built","note":"Auto-fills present_in_timezone ONLY when exactly one distinct non-owner attendee zone is loaded — two or more distinct zones leave it unset rather than guessing, and an explicit caller value always short-circuits. Proven additive: the value is consumed after candidateSet/pickSpreadSlots/chosenStarts have already fixed the slot set, so a returned slot list is byte-identical except for the added presentation_local key. Transparency, not a second safety net — the work-hours clip already rejects the bad slot once the zone is right."}
{"date":"2026-07-29","runId":"email-test-round-3","lane":"matchmaker","source":"verify","ref":"preferredslotstatus-no-presentation-local","finding":"preferredSlotStatus never attaches presentation_local even when present_in_timezone IS passed explicitly, so a named-preferred-slot echo has no second-zone rendering","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts ~1263-1281","verdict":"needs-owner-decision","state":"open","note":"DISCOVERY, pre-existing, not this wave's and deliberately not built. Flagged by matchmaker while in the file. A separate single-instant echo rather than part of the offered-slots mechanism, so it is a scoped follow-up rather than a gap in row 129c."}
{"date":"2026-07-29","runId":"email-test-round-3","lane":"instructor","source":"logs","ref":"gh#24-row135-reply-split","finding":"row 135 — the reply asked the owner questions inside text meant to be forwarded verbatim to a client, so it could not be forwarded at all","rootCause":"src/core/orchestrator/systemPrompt.ts emailReplySection — one clause instructed putting the extracted attendee set INSIDE the forwardable body 'so Idan corrects it', which is owner-directed content in client-facing text","verdict":"built","state":"built","note":"Not a model slip — the frame's premise was impossible: one string was asked to serve a stranger and the owner at once. Split into PART 1 (FOR YOU, four labeled lines: Zone/Search/Attendees/Question) + a literal '===== FORWARD ONLY BELOW THIS LINE =====' marker + PART 2 (the forwardable body, all prior voice rules kept verbatim). Measured +275 tokens, email-tier only, Slack byte-unchanged. Chose label+colon lines over bullets because the static PUNCTUATION rule already forbids dash-prefixed lists in every outbound message — a bulleted list would have shipped a second contradiction while fixing the first."}
{"date":"2026-07-29","runId":"verify-email-live-wave","lane":"slacker","source":"verify","ref":"gh#24-cap-checks-wrong-address","finding":"the one-address cap validates message.from but the send goes wherever Graph addresses the reply — a From-spoof plus Reply-To exfiltrates the owner's availability over his own signature","rootCause":"src/connections/email/index.ts validates recipientRef while src/connectors/graph/mail.ts replyToMail never reads draft.toRecipients after createReply; MESSAGE_SELECT does not even fetch replyTo","verdict":"needs-owner-decision","state":"open","note":"OVERTURN, and the reason the orchestrator did not ship before fixing it: this is the control the owner's whole risk position rests on, and the file header asserts a guarantee the code does not have — the THIRD such comment in this feature. Reachability is low because RequireSenderAuthenticationEnabled means an attacker needs tenant auth AND a forged From, which Exchange normally blocks. But the poll runs unattended every ~30s, so it is not covered by supervised testing. Dispatched to transporter the same night."}
{"date":"2026-07-29","runId":"verify-email-live-wave","lane":"slacker","source":"verify","ref":"locationtz-ambiguous-cst-mst","finding":"cst now resolves confidently to America/Chicago (China Standard Time is 14h off) and mst to America/Denver (Myanmar) — reachable from the SLACK travel path, not just email","rootCause":"src/utils/locationTz.ts — this wave added EST/EDT, CST/CDT, MST/MDT, PST/PDT so 'est' would resolve; two of those four are genuinely ambiguous","verdict":"needs-owner-decision","state":"open","note":"OVERTURN — the orchestrator RECLASSIFIED this from the verify's 'discovery'. A defect this wave INTRODUCED on a live path is an overturn by the verify's own rule, not next-run work. attendeeAvailability calls inferTimezoneFromStateStatic(travel.location) on Slack, so a colleague travelling with CST in a location string gets a 14-hour error in a real slot search. Fix is removal, not disambiguation — an ambiguous token resolving to nothing falls to the owner-zone fallback, which is his stated ruling for the no-signal case."}
{"date":"2026-07-29","runId":"verify-email-live-wave","lane":"gatekeeper","source":"verify","ref":"humangate-rewrites-two-part-reply","finding":"humanGate('external') judges the whole two-part reply as one string and rewrites it wholesale — nothing protects the cut line, and the fact-preservation veto can push an owner-directed question back into the client-facing half","rootCause":"src/utils/guards/runOutputGates.ts runEmailLegGates hands the full draft to runHumanGate with an 'external' frame that knows nothing about two audiences; rewriteDroppedAFact pins question-presence but not the marker","verdict":"needs-owner-decision","state":"open","note":"NOT fixed tonight — the root fix is a contract decision spanning gatekeeper and instructor (gate the parts separately, PART 2 in the external frame and PART 1 untouched), and the owner is asleep. Email leg only. It regenerates the exact row-135 harm, this time caused by the gate rather than the model: a rewrite that correctly strips an owner-directed question from client-facing text gets VETOED for dropping a '?' and retried with the question pinned back in."}
{"date":"2026-07-29","runId":"verify-email-live-wave","lane":"slacker","source":"verify","ref":"forwarding-note-misclassification-owner-tier","finding":"a Haiku misclassification of chain text as the owner's forwarding note writes a timezone at OWNER authority, which no later auto correction can override","rootCause":"src/connectors/email/inbound.ts:216 — setBy is derived from hint.source, which is the model's own judgement about whether text sat above or below the quote line","verdict":"needs-owner-decision","state":"open","note":"Email leg only, low. The malformed-value default correctly falls to 'chain', so only the confidently-wrong case sticks. Worth a row because an owner-tier write is by design permanent against later auto writes — a wrong zone would silently survive every correct subsequent extraction."}
{"date":"2026-07-29","runId":"verify-email-live-wave","lane":"matchmaker","source":"verify","ref":"requested-time-local-false-provenance","finding":"_requested_time_local tells the model 'the zone the times were GIVEN in' — false when the zone came from the new auto-fill, because the times were given in the owner's zone","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts:748","verdict":"needs-owner-decision","state":"open","note":"LIVE SLACK path but benign: renderClockInZone computes the numbers deterministically so they are correct, only the provenance label is wrong. Left for tomorrow rather than touching the live path unattended for a wording fix. A model told the owner 'gave' a time in ET may echo that back at him."}
{"date":"2026-07-29","runId":"verify-email-live-wave","lane":"slacker","source":"verify","ref":"replytomail-orphan-draft","finding":"replyToMail is three Graph calls with no idempotency guard — a failure between the patch and the send leaves an orphaned draft in the owner's mailbox","rootCause":"src/connectors/graph/mail.ts replyToMail: createReply -> patch -> send, no cleanup on partial failure","verdict":"needs-owner-decision","state":"open","note":"DISCOVERY, next run. Cosmetic today — the owner gets the row-120 failure DM either way — but drafts accumulate in a mailbox he does not read."}
{"date":"2026-07-29","runId":"verify-email-live-wave","lane":"outrider","source":"verify","ref":"gh#5-outbound-to-externals-blocked-by-design","finding":"gh#5's third sub-task (outbound sendDirect to non-Slack recipients) is now architecturally unreachable while the one-address cap stands","rootCause":"src/connections/email/index.ts — the cap forecloses any non-owner recipient by construction","verdict":"needs-owner-decision","state":"open","note":"DISCOVERY. A product decision the cap made implicitly. Deserves an explicit note on gh#5 so nobody schedules it as buildable. Related: the verify also confirmed gh#55 must NOT be closed on the locationTz widening — that ticket explicitly lists 'improving the static map itself' as out of scope, and its real ask (pre-warm IANA onto the travel record at write time) is untouched."}
{"date":"2026-07-29","runId":"wrap-4.3.0","lane":"outrider","source":"owner","ref":"gh#24","finding":"v4.3.0 wrapped and deployed — the semi-manual email chain plus the lane rename, 72 files","rootCause":"n/a — release","verdict":"built","state":"wrapped","note":"Committed 0b70f0a, pushed, built, restarted. Boot stamp confirmed version 4.3.0 / gitSha 0b70f0a, Slack reconnected, mail poll timer started. gh#24 COMMENTED AND LEFT OPEN (row 143: meetings.ts prose still renders on the email leg); gh#5 commented as partial with its outbound sub-task marked blocked-by-design. Nothing closed — no row met all three conditions. Two verify overturns fixed before the commit: the one-address cap validated an address the send never used (now PATCHes toRecipients explicitly), and cst/mst were removed after the orchestrator reclassified them from discovery to overturn (this wave introduced them on the live Slack travel path, 14h error)."}
{"date":"2026-07-29","runId":"post-4.3.0-followup","lane":"slacker","source":"owner","ref":"mailpoll-revocation-silent","finding":"a revoked refresh token stopped the email transport permanently with NO owner-facing signal — the row-120 failure DM cannot fire because the poll skips the profile before the handler runs","rootCause":"src/connectors/graph/mailPoll.ts:70-75 — the MailAuthRevokedError branch logged once and returned; :141 then skips the profile every tick","verdict":"built","state":"built","note":"Owner: \"you can add the log alert if its happen.\" DMs him once on the transition INTO the revoked state, never from the per-tick skip. Names BOTH recovery steps because the latch is in-memory: re-run email-auth.mjs THEN restart. The lane found that profileName (yaml stem) is a different identifier from slack_user_id and email-auth.mjs only accepts the former, so a DM built on the wrong id would have been unusable. Also checked the !handler branch as the same class and correctly dismissed it — boot-wiring defence, already throttle-logged, would surface on the first tick after any restart rather than as a week of silence."}
{"date":"2026-07-29","runId":"owner-review","lane":"gatekeeper","source":"owner","ref":"142","finding":"humanGate('external') judges the two-part email reply as ONE string and can rewrite past the FORWARD-ONLY marker; the fact-preservation veto can pin an owner-directed question back into the client-facing half","rootCause":"utils/runOutputGates.ts (external frame, whole-string judgement)","verdict":"declined","state":"open","note":"Owner 2026-07-29: 'remove from report. Im not sure its a problem even.' Removed from report.md; kept here so openKnown stops the log review re-raising it."}
{"date":"2026-07-29","runId":"wf_a3bfe2a3-abe","lane":"instructor","source":"owner","ref":"gh#45","finding":"Save if-then handling rules per person","rootCause":"core/assistant.ts:340 + systemPrompt.ts:145 (owner path injects person-file HEADERS only, never the body) + memory/capturePass.ts:266-300 (destructive per-section overwrite)","verdict":"converted","state":"open","note":"Feature plan produced ZERO buildable pieces: the ticket's specified fix (save the rule via update_person_memory) is a REGRESSION - it would save, confirm as saved, then be invisible on the booking turn it was written to steer, and be overwritten by capturePass. Body rewritten with the evidence. The working shape is the prefs path (idan_prefs/calendar.md already holds a functioning if-then). Parked by owner 'ok for now'; the architectural decision belongs to -> gh#155."}
{"date":"2026-07-29","runId":"wf_a3bfe2a3-abe","lane":"profiler","source":"owner","ref":"gh#51","finding":"Detect gender from Hebrew text - wire into email/WhatsApp inbound per the ticket","rootCause":"connectors/email/inbound.ts:88-90 and connectors/whatsapp.ts:276-282 both drop every non-owner sender before any body is read","verdict":"needs-owner-decision","state":"open","note":"Premise factually wrong about WHERE the signal is: no external person's Hebrew ever reaches either named handler, so the ticket as written buys zero behaviour change plus an LLM bill. Body rewritten: redirected to SLACK, first-person morphology only (2nd/3rd person excluded by construction - the tombstoned Daniel bug). Pieces P1/I1/O4 approved, NOT dispatched. One detail open: equal-rank write behaviour so a later self-declaration beats an earlier inferred value."}
{"date":"2026-07-29","runId":"wf_a3bfe2a3-abe","lane":"slacker","source":"owner","ref":"gh#144","finding":"Forward colleague images to owner","rootCause":"connections/slack/messaging.ts:135-163 (SendOptions.attachments implemented on sendDM only; postToChannel cannot carry it, and every owner post goes through postToChannel)","verdict":"needs-owner-decision","state":"open","note":"Pieces T0/T1/O1/T2 approved, NOT dispatched. Decisions recorded: all clean colleague images; shadow surface only (S1 approval-evidence deferred, which kills double-delivery); forward at INGESTION not model-driven - the ticket's SkillContext plumbing is unnecessary because processMessage.ts:275 reads history BEFORE the file url is appended at :307. Colleague documents are a separate larger ticket."}
{"date":"2026-07-29","runId":"wf_a3bfe2a3-abe","lane":"matchmaker","source":"owner","ref":"gh#52","finding":"Action log for undo support","rootCause":"db/client.ts:151-161 (audit_log has no owner_user_id, writer returns no row id, no update path, 12 inconsistent writers, one reader)","verdict":"needs-owner-decision","state":"open","note":"SUBSTRATE DECIDED by owner: the requests spine, NOT audit_log. His rule: revertibility follows spine membership - not on the spine means not revertible, and making it revertible means putting it there. Also decided: a reverted cancellation is NOT an undo (Graph /cancel already emailed attendees) so she refuses honestly; the inverse cascade is out of scope (no reopen path exists in src). Cheap pieces M1/M2/O3 approved, NOT dispatched. M1 is a LIVE dead-code bug: create audit rows write details.start but the only reader expects event_start_iso, so the 'did you book that?' recall has never fired."}
{"date":"2026-07-29","runId":"wf_bb6eac4f-e8f","lane":"instructor","source":"owner","ref":"143","finding":"meetings.ts ~400-line prose renders in full on an email turn, teaching move_meeting/delete_meeting/create_approval on the leg forwarded verbatim to a client","rootCause":"src/skills/meetings.ts getSystemPromptSection (no channel awareness)","verdict":"built","state":"built","note":"Channel-aware ships(toolName) gate derived from the real CHANNEL_TOOL_CLAMP. MEASURED: Slack turn unchanged 13,081 tok; email turn 13,081 -> 9,498 (-3,583 / 27%). Verify confirmed byte-identical on Slack from the code. Earned instructor rule: a prompt block is scoped to the channel that can act on it."}
{"date":"2026-07-29","runId":"wf_bb6eac4f-e8f","lane":"matchmaker","source":"owner","ref":"145","finding":"_requested_time_local claims the zone was GIVEN by the asker when it came from the v4.3.0 auto-fill","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts (candidate_validation mode, groundTz)","verdict":"built","state":"built","note":"Split explicitGroundTz (stated) from the inferred fallback; the inferred branch now says nobody stated it. Conversion math untouched. The other producer of the field was checked and was already accurate. Earned rule: a tool result never asserts provenance it cannot know."}
{"date":"2026-07-29","runId":"wf_bb6eac4f-e8f","lane":"slacker","source":"owner","ref":"144","finding":"Email timezone auto-fill persisted at OWNER authority off a Haiku guess about where in the body the text sat, so a confidently-wrong zone was sticky","rootCause":"src/connectors/email/inbound.ts:232 (setBy derived from hint.source); predicate now at :241-242","verdict":"needs-owner-decision","state":"open","note":"VERIFY OVERTURNED. The finished half is correct and must NOT be rebuilt: the source field is deleted from the classifier and its schema, so a model can no longer choose a TIER. But the structural fact (Graph uniqueBody) is tested with a case-insensitive SUBSTRING match, too loose to carry it - a short zone token can match incidental text and re-mint owner tier. Only the predicate at inbound.ts:241-242 needs changing. Earned rule: authority follows a structural property of the message, never a model classification."}
{"date":"2026-07-29","runId":"wf_bb6eac4f-e8f","lane":"slacker","source":"owner","ref":"gh#144 T2","finding":"Forward a clean colleague image to the owner so he can see it, not just read a description of it","rootCause":"src/connectors/slack/app/fileIngestion.ts (no forward existed)","verdict":"needs-owner-decision","state":"open","note":"BUILT AND SAFE - the verify confirmed the scan-before-forward invariant is structural, the owner-DM target provable, the external-domain gate live. OVERTURNED ON COST ONLY: the forward is awaited inline before processMessage, about 9 sequential HTTP round trips (users.info + chat.postMessage + 4x download/upload) while the colleague waits, and it re-fetches users.info that processMessage.ts:190 fetches again for the same fact. Fix: void shadowNotify(...) plus reuse the existing fetch. Report row 146."}
{"date":"2026-07-29","runId":"wf_bb6eac4f-e8f","lane":"slacker","source":"owner","ref":"gh#144 T0+T1","finding":"Owner receipt read (image attached, no caption); SendOptions.attachments was a lie on every verb except sendDM","rootCause":"src/connectors/slack/postReply.ts:577 and src/connections/slack/messaging.ts:135-163","verdict":"built","state":"built","note":"T0 folds the already-computed Haiku imageDescPart into the shadow via a new inboundAttachmentNote field, deliberately NOT overloading userMessage (which feeds the output gates). T1 EXTRACTED the upload loop into one uploadAttachments helper called by both sendDM and postToChannel, surfaced attachments_failed, and corrected the false doc comment. Verify confirmed a true extraction with the thread fallback preserved verbatim. Two earned transporter rules."}
{"date":"2026-07-29","runId":"wf_bb6eac4f-e8f","lane":"outrider","source":"owner","ref":"gh#52 O3","finding":"audit_log is the only stateful table with no owner_user_id, and its rows reach the model context via the get_calendar recall","rootCause":"src/db/client.ts:151-161 (schema) and :890-928 (reader filters neither owner nor actor)","verdict":"built","state":"built","note":"Added owner_user_id plus index plus ALTER and backfill (backfills ONLY when exactly one profile exists - refuses to guess, per instruction). ownerUserId made required on auditLog() and recentAuditEntries(). FOUND A 13TH WRITER the ticket count of 12 missed: a raw INSERT INTO audit_log in core/requests/closeRequest.ts bypassing the typed helper - raised as a cross-lane ask, built by shepherd. Both write paths and both read paths are now owner-filtered. Earned rule: every stateful table is owner-scoped, no exceptions."}
{"date":"2026-07-29","runId":"wf_bb6eac4f-e8f","lane":"registrar","source":"verify","ref":"gh#52 O3-followup","finding":"closeRequest raw INSERT INTO audit_log bypasses O3 new owner_user_id column, because TypeScript cannot see inside a raw SQL string","rootCause":"src/core/requests/closeRequest.ts:99-116","verdict":"built","state":"built","note":"Cross-lane ask raised by outrider and dispatched in the dependency round - the ask mechanism worked end to end (attached 1, dispatched 1). Added owner_user_id to the column list, VALUES and params, sourced from the RequestRow already fetched at :45. Not an active leak today (no reader filters on request_closed) but the invariant now holds for real."}
{"date":"2026-07-29","runId":"wf_bb6eac4f-e8f","lane":"matchmaker","source":"owner","ref":"gh#52 M1+M2","finding":"get_calendar did-you-book-that recall had never once fired; move_meeting recorded where a meeting went but never where it was","rootCause":"src/connectors/graph/calendarMutations.ts (create audit row wrote details.start, the only reader expects event_start_iso); src/skills/meetings/ops/handlers/moveMeeting.ts:1436-1443","verdict":"built","state":"built","note":"M1 renamed the key so writer and reader speak one language - a live dead-code bug, one line, and direct evidence that an unschemad details blob already silently killed a shipped feature. M2 captured original_start/end/tz from the probe already in hand at zero extra Graph cost; deliberately speculative per the owner (no undo tool this wave). Earned rule: an audit row records the PRE state, not only the post state."}
{"date":"2026-07-29","runId":"wf_bb6eac4f-e8f","lane":"profiler","source":"owner","ref":"gh#51 P1","finding":"Hebrew speakers get stilted neutral forms or a gender question, though their own words often state it","rootCause":"src/utils/genderDetect.ts (only pronouns then photo then name-LLM; no morphology tier)","verdict":"built","state":"built","note":"Third tier: first-person Hebrew morphology only, Haiku not Sonnet, quoted lines structurally stripped, 6-char floor, deterministic Hebrew gate, new advanced yaml block DEFAULT OFF, wired at the one call site carrying the person own live words. Ordered BEFORE the weaker photo guess so a strong person-tier signal is not pre-empted. VERIFIED rather than built: the owner requirement that a person can always override their own value already holds - setCoreFieldWithProvenanceById refuses only when the incoming rank is strictly lower, so an equal-rank self-declaration already wins. Earned rule: a derived personal attribute never steers output at a tier it did not earn."}
{"date":"2026-07-29","runId":"wf_bb6eac4f-e8f","lane":"outrider","source":"owner","ref":"gh#51 O4","finding":"whatsapp.ts header claimed nothing imports startWhatsApp and that the file has zero runtime effect","rootCause":"src/connectors/whatsapp.ts:6 (index.ts:5 imports it, :174 calls it at boot, watchdog at :269)","verdict":"built","state":"built","note":"Comment-only. Corrected the header AND a second stale JSDoc note to state the true gating: wired at boot for every profile, inert unless whatsapp_phone is set, owner-phone-only inbound. Found while disproving the #51 premise that WhatsApp was a viable wiring target."}
{"date":"2026-07-29","runId":"wf_bb6eac4f-e8f","lane":"gatekeeper","source":"verify","ref":"147","finding":"A colleague image arriving on the CATCH-UP path reaches the model context with no prompt-injection scan at all","rootCause":"src/connectors/slack/app/handlers.ts:52-63 (download then buildImageBlock then images then processMessage at :84, no scanAndPrepareImage anywhere)","verdict":"needs-owner-decision","state":"open","note":"VERIFY DISCOVERY, severity high, PRE-EXISTING - not introduced by this wave. T2 named it and correctly stayed out of it. The live path scans every file at fileIngestion.ts:70-84; the replay path scans nothing, and it runs unattended after any downtime. Report row 147. NOT built in this wave per the discoveries rule - building a discovery invalidates the pass that found it."}
{"date":"2026-07-29","runId":"wf_bb6eac4f-e8f","lane":"instructor","source":"verify","ref":"gh#51 I1","finding":"A dispatched piece returned nothing and nobody said so","rootCause":"Manager error: I1 was in the approved ten but omitted from the build args - 12 pieces dispatched, not 13","verdict":"no-change-needed","state":"built","note":"The verifier caught the silence and independently established that ZERO lines was the correct answer: systemPrompt.ts:655-658 steers Hebrew gendered forms off the gender field and never asserts the person stated it, so a person-tier inferred value needs no wording change - exactly as the plan allowed. Nothing is owed. Recorded because a dropped piece that happens to be a no-op is still a dropped piece: count the args against the approved list before dispatching."}
{"date":"2026-07-29","runId":"direct-a6817977","lane":"gatekeeper","source":"verify","ref":"147","finding":"Colleague image on the catch-up/replay path reached the model with no prompt-injection scan","rootCause":"src/connectors/slack/app/handlers.ts registerInboundReplayHandler image branch (ad-hoc downloadSlackImage + buildImageBlock, no scan)","verdict":"built","state":"built","note":"Fixed by DELETING the ad-hoc loop and routing the branch through ctx.processImageFileShare - the same function the live handlers use, so one ingestion implementation instead of two. Confirmed reachable: catchUpMissedMessages scans ALL the bot 1:1 DMs, not just the owner. Combined verify re-derived the path and sampled the highest-stakes trace claim: suspicious colleague image returns null, continue skips both pushes, and the early return sits above every downstream call. Fail-closed, not merely logged."}
{"date":"2026-07-29","runId":"direct-aa14bfc9","lane":"slacker","source":"verify","ref":"144","finding":"The uniqueBody containment test was a case-insensitive substring match, so a short zone token could match inside an unrelated word and mint an owner-tier timezone","rootCause":"src/connectors/email/inbound.ts:247-248 (pre-fix .includes check)","verdict":"built","state":"built","note":"Replaced with statedZoneAppearsInUniqueBody() using lookaround word boundaries, token escaped for regex metacharacters (the + in GMT+2 would otherwise be a live quantifier). Fails safe on empty token or empty uniqueBody. The verify broke the one claim that would have been fatal if false: uniqueBody IS genuinely in MESSAGE_SELECT and normalised to empty string, so the predicate is neither crash-prone nor silently always-false. Residual logged separately as 144a."}
{"date":"2026-07-29","runId":"direct-a7ed38df","lane":"slacker","source":"verify","ref":"146","finding":"The prefetched users.info promise was pre-caught, turning a throw-on-failure identify path into a silent-null path that still logged Colleague identified and still wrote to people_memory - overwriting a colleague real name with their raw Slack ID","rootCause":"src/connectors/slack/app/processMessage.ts:178-183 consuming a pre-caught promise created at fileIngestion.ts:117-125; db/people.ts:511-513 sets name unconditionally","verdict":"built","state":"built","note":"REGRESSION INTRODUCED BY THE FIRST 146 BUILD AND CAUGHT BY THE COMBINED VERIFY - the exact reason a direct dispatch still owes a combined pass. Root fix: ONE hoisted users.info inside the original try (throw restored, so a failure means no write and no false identified log), reused at the logEvent and colleagueName sites (both duplicate fetches DELETED), and prefetchedSenderInfo removed entirely - context.ts is now a clean revert with an empty diff vs HEAD. The forward IIFE keeps its own isolated fetch where a failure is genuinely non-fatal. Manager verified independently: typecheck clean, zero dead refs, one senderId fetch (the remaining users.info at :426 resolves a DIFFERENT user). ALSO CORRECTED: the earlier cost claim of two round trips was wrong - there were THREE users.info calls for one fact."}
{"date":"2026-07-29","runId":"direct-a64ce53b","lane":"gatekeeper","source":"verify","ref":"147a","finding":"Dead expression left in the function row 147 rewrote: isMpim: source === assistant_panel ? false : false - a ternary with identical branches","rootCause":"src/connectors/slack/app/handlers.ts:101","verdict":"needs-owner-decision","state":"open","note":"One line. Standards only, no behaviour. Consequence is that the next reader assumes panel replay has distinct MPIM semantics and writes a branch for a distinction that does not exist. Owner asked whether to spend a dispatch on one line or bundle it with gatekeeper next real job."}
{"date":"2026-07-29","runId":"direct-a64ce53b","lane":"slacker","source":"verify","ref":"144a","finding":"The tightened predicate proves the zone token appears in the owner own text, but not that the owner asserted it ABOUT THAT PERSON","rootCause":"src/connectors/email/inbound.ts (statedZoneAppearsInUniqueBody proves presence, not attribution)","verdict":"needs-owner-decision","state":"open","note":"Owner writes I am ET, he needs 2pm; the extractor pairs ET with the colleague email; the predicate says yes; owner tier lands on the COLLEAGUE row and owner tier is sticky forever because no auto correction can outrank it. Much narrower than the substring bug that fired on the word market, so the honest word is NARROWED, not closed. Also: the boundary class is ASCII-only, so a Hebrew-body email can match a Latin token glued to Hebrew characters. Low."}
{"date":"2026-07-29","runId":"direct-a64ce53b","lane":"slacker","source":"verify","ref":"146c","finding":"Making the forward fire-and-forget removed the ordering guarantee that the forward and the reply receipt collapse into ONE owner-DM thread","rootCause":"src/utils/shadowNotify.ts:98-131 threads only if shadowThreadAnchors already holds an anchor for the conversation key","verdict":"deferred","state":"open","note":"The forward used to be awaited before processMessage so it always created the anchor first; now it races the Step 4.6 receipt on the same conversationKey. The receipt normally lands seconds later so the forward usually still wins, but with four uploads in flight both can miss the cache and the owner gets two separate DMs instead of one tidy thread. COSMETIC. Reported because the verify WITHDREW the prior clean stamp on it and could not re-prove it, not because it needs building."}
{"date":"2026-07-29","runId":"direct-a64ce53b","lane":"gatekeeper","source":"verify","ref":"147b","finding":"Routing replay through the live-path function imported three live-path behaviours into an unattended background routine","rootCause":"src/connectors/slack/app/handlers.ts replay branch now calls processImageFileShare","verdict":"needs-owner-decision","state":"open","note":"All three are true parity, none is a new part of the system, but all are new OUTWARD behaviour on catch-up: (1) catch-up colleague images are now forwarded to the owner as real file uploads, bounded to one message per thread by catchUpMissedMessages so bounded by DM count not backlog length; (2) the 4-image cap now applies to replay, per missed message not per batch (verified); (3) A DOWNLOAD FAILURE NOW ABORTS THE WHOLE MISSED MESSAGE, TEXT INCLUDED - previously replay logged the failure and still answered the colleague words, so a missed question with one oversized attachment now gets that image is a bit big and no answer. Item 3 is the one worth the owner attention."}
{"date":"2026-07-29","runId":"direct-a64ce53b","lane":"outrider","source":"verify","ref":"D1-dual-image-ingest-loop","finding":"The image download/scan loop exists twice and has now drifted twice","rootCause":"src/connectors/slack/app/fileIngestion.ts:42-83 and src/connectors/slack/app/handlers.ts:1210-1235","verdict":"deferred","state":"open","note":"DISCOVERY, not built - building a discovery invalidates the pass that found it. Same loop: download, friendly error, scanAndPrepareImage, push block and url, cap 4. The THIRD copy (replay) drifted by losing the scan entirely - that IS row 147. And the DM copy has now gained the #144 owner-forward that the channel copy does not have. Consequence: the next image-pipeline change (lifting the domain gate, adding channel forwarding) gets applied to one copy while the other silently keeps the old behaviour, exactly as the scan did. Naming it, not designing the extraction."}
{"date":"2026-07-29","runId":"direct-a64ce53b","lane":"slacker","source":"verify","ref":"D2-forward-inert-without-owner-email","finding":"The #144 colleague-image forward is one config field away from being silently inert","rootCause":"getOwnerDomain returns null when user.email is unset, so isInternal is always false","verdict":"deferred","state":"open","note":"DISCOVERY. Live for this owner (idan.yaml sets user.email so getOwnerDomain resolves), which is why the verify could affirm the withdrawn gate-is-not-always-false entry. But for a second tenant without user.email every colleague image is skipped with an info log and no error - a feature that appears built and does nothing. Earlier wave code."}
{"date":"2026-07-29","runId":"direct-ac07112f","lane":"gatekeeper","source":"owner","ref":"147b","finding":"Routing replay through the live-path function meant an image DOWNLOAD failure aborted the whole missed message, text included - a missed question with one oversized attachment got no answer","rootCause":"src/connectors/slack/app/fileIngestion.ts treated a download failure and a security refusal identically: both meant images.length===0 then return","verdict":"built","state":"built","note":"Owner ruled EXEMPT for catch-up. Fix splits the two reasons an image can be missing into hadDownloadFailure vs hadSecurityRefusal, and degrades ONLY when degradeOnDownloadFailure && hadDownloadFailure && !hadSecurityRefusal. Any security refusal in the batch keeps the original fail-closed return, so row 147 is not weakened. The flag is passed ONLY by the replay caller (handlers.ts:91) - live DM/MPIM/mention callers never set it and keep abort-on-download-failure verbatim. Manager verified the predicate and the single call site directly from the code. Owner-forward also gated on images.length>0 so an empty forward cannot fire in the degrade case. Owner accepted the other two parity behaviours (catch-up images ARE forwarded; the 4-image cap applies per missed message)."}
{"date":"2026-07-29","runId":"direct-ac07112f","lane":"gatekeeper","source":"owner","ref":"147a","finding":"Dead ternary with identical branches left by row 147's own rewrite","rootCause":"src/connectors/slack/app/handlers.ts (isMpim: source === assistant_panel ? false : false)","verdict":"built","state":"built","note":"Collapsed to the literal and dropped the now-unused source destructure, confirmed by grep across the file. Standards only, no behaviour change."}
{"date":"2026-07-29","runId":"owner-review","lane":"slacker","source":"owner","ref":"144a","finding":"The tightened predicate proves the zone token appears in the owner's own text but not that he asserted it about that person","rootCause":"src/connectors/email/inbound.ts statedZoneAppearsInUniqueBody","verdict":"declined","state":"open","note":"Owner ruled ACCEPT on 2026-07-29 - no ticket, no build. The residual stands knowingly: 'I am ET, he needs 2pm' can still stamp the owner's zone onto a colleague's row at owner tier, which is sticky. Much narrower than the substring bug it replaced. Recorded so the log review does not re-raise it and so nobody claims the class is closed."}
{"date":"2026-07-29","runId":"owner-review","lane":"outrider","source":"owner","ref":"earned-charter-rules-x9","finding":"The feature wave surfaced nine durable charterRule candidates across five lanes","rootCause":"feature.js surfaces earnedRules on every piece by design","verdict":"declined","state":"open","note":"Owner ruled on ALL NINE 2026-07-29: 'all not agreed, they are not charter stuff.' The distinction to carry forward: a charter describes HOW A LANE WORKS - scope, boundaries, standing method. A principle distilled from one fix is a good sentence about that fix and not charter material however true. Both engines will keep proposing these; report once, expect a decline, and never let a lane treat one as authorised."}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"github","ref":"gh#158-attendee-misresolution","invariant":"extraction-scope-must-match-defect-scope","finding":"a rhetorical challenge about a PAST search ('are you sure you didn't check Yael?') made Yael an attendee of the NEXT search, so her busy time shaped an answer that was purely about Levana","rootCause":"src/core/social/classifyTurn.ts:238 (the meetingPeopleSection prompt fed to the Haiku classify_turn call)","verdict":"needs-owner-decision","state":"open","note":"FIX IS IN THE TREE, ~4 lines of prompt text, net +2, but the WAVE VERIFY OVERTURNED IT - safe and a strict improvement, nothing regresses, shippable as-is if the owner prefers, but it does not reliably close its own ticket. RUNTIME CHAIN CONFIRMED, not inferred (logs/maelle-2026-07-28.log:594-605, threadTs 1785241243.988529): the challenge was classified meetingPeople:['Yael Aharon'] (:597) -> buildTurnContext.ts:266-301 resolved yael.a@reflectiz.com into context.resolvedMeetingAttendees (:598) -> findAvailableSlots.ts:394-411 UNCONDITIONALLY unioned it onto the model's own clean attendee_emails:['levana.b@'] (:604) giving finalAttendees both (:605). The union is CORRECT BY DESIGN - it exists to recover a genuinely dropped attendee - so the wrong data entered one step earlier and no downstream consumer needed changing. THE OVERTURN'S REASONING: the new rule is MESSAGE-scoped where the defect is NAME-scoped. Its exception reads 'return empty UNLESS the sender is ALSO giving a genuine new scheduling instruction in the same message', and once that fires nothing tells the model to exclude the CHALLENGED name - so 'you didn't check Yael - anyway find me 25 min with Levana next week' satisfies the exception and can return both, which is gh#158 verbatim. That mixed shape is NOT hypothetical: it is how the owner writes throughout the #158 and #156 transcripts. AND THE INVERSE RISK: an ADDITIVE ask phrased as a question ('did you also check Dana?') now reads as a question about the past -> empty array -> the attendee is SILENTLY DROPPED, which is precisely the Lori-07-08 bug the resolvedMeetingAttendees spine was built to prevent (findAvailableSlots.ts:386-393 says so in its own comment). One clause fixes both directions - see the deferred dependency ask to instructor. Standards clean: no dead code, no duplication, no new spine/table/tool/guard/skill, no extra LLM call, prompt growth the only pressure and judged justified because the harm reaches a real calendar invite. Lane NAMED what it did not verify: whether Haiku generalises the negative example to Hebrew/Russian/Spanish phrasings. typecheck green"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"instructor","source":"verify","ref":"gh#158-exception-must-be-name-scoped","invariant":"extraction-scope-must-match-defect-scope","finding":"DEPENDENCY ASK, raised and NOT dispatched - the overturn's remedy: make the classifier bullet's exception NAME-scoped rather than MESSAGE-scoped","rootCause":"src/core/social/classifyTurn.ts:238-243","verdict":"needs-dependency","state":"open","note":"Rendered in report.md as its own section because an unreported ask is indistinguishable from one that never happened. The ask: 'when the message contains BOTH a challenge and a real scheduling instruction, list ONLY the people the new instruction is WITH - never the person being challenged about.' Same bullet, no new lines - the current final sentence can be rewritten in place so the diff stays flat. Second half of the ask: consider one short POSITIVE counter-example for an additive question ('did you also check Dana?', 'can you add Yael too?'), since the present wording pushes those toward an empty array and a dropped attendee is the failure this spine exists to prevent"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"verify","ref":"availability-asserted-from-unsearched-window","finding":"DISCOVERY, not built - 'Yes, 11am tomorrow is free for her too' was asserted from a search that covered 10:00-10:25, not 11:00; the owner replied 'NOOOOOOO she has something'","rootCause":"logs/maelle-2026-07-28.log:752 - findAvailableSlots rejection breakdown with searchFrom 2026-07-29T10:00:00 searchTo 10:25:00+03:00 candidatesAccepted:0, answering the owner's 11am question logged at :707","verdict":"needs-owner-decision","state":"open","note":"THE HIGHEST-HARM ITEM IN gh#158 and complaint 2 of that ticket, untouched by this wave. She asserts a fact about a real calendar she never checked, and the owner only catches it because he is looking at the calendar himself. claim_checker RAN at 12:26:54 (:757) and PASSED it - its rule is about claiming to have DONE an action, and 'X is free' is a STATE claim, the same structural blindness found on 2026-07-27. Same class recurs in gh#156 ('Yep, both work for Yael too' -> 'sorry for the false confirm earlier'). Not built because a discovery built inside the wave that found it invalidates the pass"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"verify","ref":"classifier-guess-reaches-real-calendar-invite","invariant":"extraction-scope-must-match-defect-scope","finding":"DISCOVERY, not built - a person the owner never asked to invite can end up on a REAL calendar invite, seen by the recipient before anyone can catch it","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:150-156 unions context.resolvedMeetingAttendees into args.attendees, populated from the same Haiku meeting_people extraction (buildTurnContext.ts:266-301)","verdict":"needs-owner-decision","state":"open","note":"The same classifier guess that polluted the SEARCH is also unioned into the BOOKING, and owner-path bookings go straight through with no approval step, so cancelling afterwards does not undo it. THE ONLY DEFENCE ON THAT PATH TODAY IS PROMPT WORDING at classifyTurn.ts:238-243 - which is exactly the wording this wave's overturned fix was editing. M4 tier 2 (a wrong real-world action, external and hard to undo) and therefore ranked ABOVE the observed 11am false claim, even though this one has not been seen firing"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"verify","ref":"nondirectory-email-accepted-and-defaulted","finding":"DISCOVERY, not built - an availability answer was computed for an address that is not the colleague's, and the address was handed default work hours as though it were a known person","rootCause":"logs/maelle-2026-07-28.log:790-791 - find_available_slots call entry attendee_emails:['levana@reflectiz.com'] then attendeeAvailability auto-loaded that address with Asia/Jerusalem 09:00-18:00, while :749 in the SAME thread used the directory entry levana.b@","verdict":"needs-owner-decision","state":"open","note":"The guard that should have fired is src/connectors/graph/calendarReads.ts:677-681 and 958-976 - the `unresolved` diagnostic built for exactly this failure (the elinor.avny@ bug) produced NO warning anywhere in the log. The verify was careful about its own claim: it could NOT establish whether Graph resolved levana@ as a mailbox alias, so it does NOT claim the answer was wrong - only that an address which does not match the directory is accepted and defaulted with no check, so the answer carries the same confidence as a real one"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"slacker","source":"verify","ref":"owner-role-split-within-one-turn","finding":"DISCOVERY, not built - inside ONE MPIM turn the owner is treated as a colleague by some code and as the owner by other code, so which guard rails apply to his own request depends on which line you land in","rootCause":"logs/maelle-2026-07-28.log:589 processMessage role determined rawRole:'owner' effectiveRole:'colleague'; :598 attendee pre-resolution senderRole:'owner'; :604 find_available_slots call entry senderRole:'colleague' - all three in the SAME turn","verdict":"needs-owner-decision","state":"open","note":"This is the substrate under the open gh#154, and the NEW detail is that the split happens WITHIN a single turn rather than merely between surfaces. Recommend folding into gh#154 rather than opening a parallel track - the design question there is exactly 'what should the clamp govern', and this is evidence for it"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"both","ref":"flow-narrates-unexecuted-actions","finding":"NEEDS-SHAPING, deliberately not dispatched - five draft replies across two threads in one day claimed a completed action when no such tool had run that turn; the owner said 'you are lying' and 'you didn't test' more than once","rootCause":"unknown - the shaping question is precisely which layer controls when a confirmation-style reply may go out","verdict":"needs-owner-decision","state":"open","note":"SPANS AT LEAST TWO LANES, which is why it was surfaced rather than built. Five claim_checker fires, same shape, one day: log 2026-07-28 :372 ('both times were checked against her calendar', no tool activity), :446 ('I'll send you the complete rundown in our private DM', no message_colleague ran), :466 ('I'll drop the whole week in our DM right now'), :616 ('Confirmed now, checked you and Levana's calendars', no create_meeting), :818 ('lock in 11am tomorrow with Levana', no create_meeting or move_meeting). The claim-checker CAUGHT and rewrote some, but a false confirmation still reached the owner at least once per thread before a corrected reply arrived. SHAPING QUESTION: one gate at the shared draft-composition point (outrider / core orchestrator), or one per flow (matchmaker's booking confirmations, transporter's DM-send promises)? Not resolvable from the logs alone"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"github","ref":"gh#157-calendar-filter-and-history-gap","finding":"FLAGGED, clarity ambiguous, nothing dispatched - Maelle twice reported nothing on the calendar involving Yael next week (denying a 'Leadership Alignment' the owner believed existed) and separately told Yael she has 'no history of meetings or notes' with her","rootCause":"undetermined - three live possibilities","verdict":"flagged-for-owner","state":"open","note":"Held at the VERY HARD BAR rather than guessed at, which is the correct outcome for this shape. Could be a genuine attendee-filter bug in the next-week query (matchmaker), a people-memory / interaction-history gap for a colleague with many meetings but no captured notes (profiler), or MAELLE COULD SIMPLY BE RIGHT that the meeting is not booked yet and the owner's assumption is wrong. Nothing in the transcript settles it. SHAPING QUESTION FOR THE OWNER: is there actually a Leadership Alignment with Yael on next week's calendar, and is there real prior meeting history with her? The calendar and the DB need checking before any lane builds anything - a wrong guess here builds a fix for a bug that does not exist"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"github","ref":"gh#158","finding":"TICKET COVERAGE: partial - do NOT close #158 on this wave","verdict":"needs-owner-decision","state":"open","note":"Complaint 1 ('at start she looked on Yael calendar') is what the wave targeted, and even that is incomplete while the exception stays message-scoped. Complaint 2 ('Levana is not free on the 11 am time') is the unsearched-window assertion, untouched. Complaint 3 ('she is not answering my questions') - the 'does levana free tomorrow at 10am?' turn was answered about the OWNER's work hours until he said 'you are not answering what i'm asking' (log :657, :683) - untouched and has no row yet. Recorded so the wrap cannot close it by accident"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"outrider","source":"verify","ref":"MANAGER-args-passed-as-json-string","finding":"MANAGER ERROR - args reached the engine as a JSON STRING rather than an object; it recovered by parsing, but the tool contract says pass an actual JSON value","verdict":"audit","state":"built","note":"No harm this run - every arg was honoured (the manifest shows openKnown.passedIn:12 and the correct cutoff), so nothing was silently dropped. Recorded because a stringified arg is the documented failure mode where array args reach the script as one string and .filter/.map throw, and it would not always recover. Framework-adjacent, not a Maelle finding"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"gatekeeper","source":"both","ref":"flow-narrates-unexecuted-actions","finding":"SHAPING QUESTION ANSWERED WITHOUT A DISPATCH - the gate is already single and shared and fired correctly all five times; what the owner is experiencing is its REMEDY, which is deliberately 'confess, do not act', and the acting version was removed because it caused a real duplicate-send","rootCause":"src/utils/claimChecker.ts:404-408 - the v3.4 own-the-miss rewrite REPLACED the earlier remedy of re-invoking the orchestrator (which re-ran the tool loop and could re-fire a WRITE, producing the Amazia duplicate-send) and the force-message_colleague path (which auto-sent on a possibly-wrong verdict)","verdict":"needs-owner-decision","state":"open","note":"MANAGER INVESTIGATION, read-only, no lane dispatched - the owner asked whether recon was needed and it was not; the answer was in the log line plus one source comment. THE SCOUT'S SHAPING QUESTION WAS 'one gate at the shared draft-composition point, or one per flow?' - and it is neither: ONE place already, and the change would be to the REMEDY rather than the detector. All five fires on 2026-07-28 logged `Claim-checker: false claim - rewriting to own the miss (no tool re-fire)` at :372, :446, :466 (plus :616 and :818), each followed by a claim_checker_rewrite on Sonnet - so the detector caught every one and the reply he received was an honest admission, not the false confirmation. His 'you are lying' / 'you didn't test' is therefore NOT the guard failing: he asked for something, she claimed it was done, and the system handed him a confession instead of a result, so he had to ask again. THE FIX I WAS ABOUT TO RECOMMEND IS REFUTED BY THE CODE: 're-fire reads, confess on writes' is not a new idea, it is a retreat toward a remedy that already cost a duplicate send to a real person. The old path re-invoked the WHOLE orchestrator so it could not be selectively read-only, which is why the removal was wholesale rather than scoped. A narrower version - re-run only READ tools, then answer - never existed and would have to be built new. SO THIS IS A PRODUCT DECISION, NOT A REPAIR: does the owner want to revisit a trade that was already paid for once? Lane is gatekeeper (it owns claimChecker), but nothing should be dispatched until he rules. Also worth carrying: the rewriter already runs a Sonnet KEEP-veto that leaves the draft unchanged when it only proposes/offers/asks/future-commits, so the misfire direction is already guarded"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"github","ref":"candidate-slots-branch-skips-real-attendee-busy","invariant":"one-tool-one-rigour","finding":"BUILT - and the Manager's root cause was WRONG. The 11am answer was searched at the RIGHT window; the bug is that the candidate_slots branch never checked the attendee's real calendar at all on a colleague-path turn, only her stored work hours","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts:567-569 (pre-edit) - attendeeBusyEmails, the real Graph busy check for a named attendee, was gated to isOwnerInitiatedSearch","verdict":"built","state":"built","note":"CORRECTS THE MANAGER'S BRIEF, which said she asserted 11am free from a 10:00-10:25 search: :707 is only the message receipt, and the actual 11am answer at :724-728 WAS correctly searched at 11:00-11:25 with available_count:1. The real defect is worse and simpler - on a colleague-path or MPIM-clamped-owner turn the candidate_slots branch (every 'is X free at Y?' check) consulted only loadAttendeeAvailabilityForEmails, which is TZ+hours ONLY per its own header, plus the owner's own calendar. Levana's actual calendar was never read. AND THIS EXPLAINS THE 'ARE YOU SURE?' LOOP THAT THE OWNER'S OWN FIX WAS AIMED AT: the recheck ALREADY fired (confirmed :734-753) - it just never read real data, so re-running it returned the same wrong answer. So 'she needs to recheck' was right in spirit and wrong in mechanism; no re-check trigger was needed. M2 VIOLATION NAMED: the same tool's SPREAD-SEARCH branch (:1345-1389) already does this correctly for colleague-path via annotateSlotsWithAttendeeStatus, so two branches of one tool disagreed on how rigorously to establish the same fact. FIX: candidateAttendeeBusyEmails, role-independent but still honouring the explicit ignoreAttendeeBusy override, wired into the candidate_slots loop - reusing the existing attendee_busy_collision:<email> rejection plumbing and humanizeViolationLabel, no new mechanism. Traced: the challenge recheck now reads real data, a truthfully-free answer is untouched, and owner-path candidate checks are BYTE-IDENTICAL because they already had the busy check"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"verify","ref":"candidate-slots-branch-never-surfaced-unresolved-address","invariant":"one-tool-one-rigour","finding":"BUILT - and the 'guard that stayed silent' was not silent: it NEVER RAN for that address on that path, and separately the branch never read its output","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts candidate_slots branch - two compounding gaps","verdict":"built","state":"built","note":"The owner CONFIRMED levana.b@ is the correct address, which is the fact the wave verify could not establish, so levana@ was simply wrong rather than a possible alias. TWO GAPS, both established from the log at :790-795 rather than guessed: (a) before the sibling fix above, attendeeBusyEmails was never passed on the colleague path, so Graph (calendarReads.ts:668+) was NEVER ASKED about levana@ - the unresolved diagnostic at calendarReads.ts:958-976 was not silent, it simply never ran for this address on this path; (b) even where it does run, the candidate_slots branch never read diagnosticsOut.unresolvedAttendees / attendeesNotChecked, unlike the main branch's attendeeEmailWarning / attendeeNotCheckedWarning block. So this shares the SAME invariant as row 161 - the candidate_slots branch was systematically less rigorous than the main branch of the same tool. FIX: threaded the identical warning pattern into candidate_slots, reusing enrichUnresolvedInternal and the existing warning copy VERBATIM, aggregating unresolved/not-checked addresses across all per-candidate diagnostics. Traced: a non-directory address now surfaces unresolved_attendee_emails plus 'do NOT present any slot as working for that person until the address resolves'; a correct address is unaffected because the warnings only construct when non-empty"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"github","ref":"gh#157-withheld-calendar-reported-as-absence","invariant":"a-restriction-is-not-an-absence","finding":"BUILT the part that is matchmaker's - the shared-surface calendar read returned a payload BIT-IDENTICAL to a genuinely empty calendar, so 'withheld for privacy' and 'nothing exists' were indistinguishable to the model","rootCause":"src/skills/meetings/ops/handlers/calendarReads.ts:170-179 - the _owner_shared_surface branch's payload shape and its closing _scope_note","verdict":"built","state":"built","note":"THE OWNER'S DIAGNOSIS WAS CORRECT AND REPLACED THE MANAGER'S: he said 'i'm sure because of the MPIM, she just didn't extract calendar meetings, security', against the Manager's guess that a recurring series failed to expand. Verified against the gh#157 transcript AND the log at :487-540. THREE SEPARATE STATEMENTS, NOT ONE BUG: (1) 'Checked, there's nothing involving Yael specifically' DID follow a real get_calendar call (:502-503, _owner_shared_surface:true) - matchmaker's, and FIXED: the _scope_note now states an empty result means WITHHELD and never confirmed-empty, forbids the false-absence phrasing outright, and narrows the old 'You CAN still work on a specific meeting he names here' - which read as licence to CONFIRM involvement - to merely ACTING ON a named meeting. (2) 'there's nothing on the calendar at all, no Leadership Alignment' had NO FRESH READ BEHIND IT (:508-523, actionCount:0, no tool call) - Sonnet escalated the stale claim from memory; not built, noted as a secondary consequence that should self-resolve once (1) lands, with a possible gatekeeper claim-checker backstop if it recurs. (3) 'No history of meetings or notes with her, this looks like our first real interaction' had ZERO tool calls of any kind (:540) - not calendar, not people-memory - so it is PROFILER's territory or a pure narration hallucination; flagged, not built, and explicitly NOT assumed to share a fix with (1), which the Manager had also cautioned. Traced: the MPIM case now gets an honest 'can't check here' framing instead of a false absence, and the 1:1 owner DM is unaffected because the branch is gated on isSharedSurface = senderRole==='colleague', verified as the only reader of _owner_shared_surface"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"verify","ref":"gh#158-exception-must-be-name-scoped","invariant":"extraction-scope-must-match-defect-scope","finding":"DECIDED, not built - the code option is NOT VIABLE, and matchmaker proved it rather than preferring the prompt","rootCause":"there is no code-observable signal distinguishing 'Sonnet correctly omitted Yael' from 'Sonnet incorrectly omitted Lori'","verdict":"needs-dependency","state":"open","note":"THE PROOF, which is the valuable part: both cases produce the IDENTICAL runtime shape - a non-empty-but-different attendee_emails plus a resolvedMeetingAttendees entry - so narrowing the union at findAvailableSlots.ts:394-411 cannot tell a correct omission from the Lori-07-08 drop it exists to recover. The discrimination is about MEANING, and meaning extraction is the Haiku classifier, i.e. classifyTurn.ts, which is not matchmaker's file. So this is not 'prompt because easier', it is prompt because nothing else can see the difference - which is exactly the argument instructor needs, given the owner's stated doubt that instructor would agree. RECOMMENDED SHAPE, a 4-way distinction: pure backward-looking challenge -> empty; genuine new instruction -> the named people; MIXED challenge+instruction -> only who the NEW instruction is with, never the person challenged about; forward-looking ADDITIVE ('did you also check Dana?') -> Dana. Rows 3 and 4 are the ones that matter - row 3 is the hole in the uncommitted attempt, row 4 is what a naive fix breaks. UNCOMMITTED classifyTurn.ts EDIT: matchmaker recommends REVERT, having independently reproduced BOTH holes the wave verify found, and deliberately did not touch the file because it is not its lane. Dispatched to instructor with the per-turn budget fact (needMeetingPeople is unconditionally true for any message over one character on every channel) stated AGAINST the change, and the unguarded createMeeting union - 4 firings in 4 days on real invites - stated for it"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"instructor","source":"verify","ref":"gh#158-exception-must-be-name-scoped","invariant":"extraction-scope-must-match-defect-scope","finding":"BUILT rather than declined - the meeting_people extraction now distinguishes a disputing question about the past from a real ask to include, with the mixed and additive cases worked as literal examples","rootCause":"src/core/social/classifyTurn.ts:241-244 inside meetingPeopleSection","verdict":"built","state":"built","note":"THE OWNER DOUBTED INSTRUCTOR WOULD AGREE; it agreed, and said why: matchmaker had PROVEN the union has no code-observable signal to discriminate 'correctly omitted' from 'incorrectly omitted' - both look identical at runtime - which leaves meaning extraction, this file's job. And the same output feeds createMeeting.ts:150-165 unguarded on a path that fired 4/4 correctly in 4 days, so declining would leave that exposed with no alternative. It judged ~4 net lines the cheaper side of that trade, noting the section already fires on effectively every interactive turn (needMeetingPeople, buildTurnContext.ts:179) so the increment is the same handful of lines regardless, and it added no second copy or parallel block. PARTIAL REVERT, not a pure one: it KEPT the header rewording ('asking to...' -> 'giving a NEW instruction... WITH someone') because that change is implicated in neither hole and is what makes the header itself gate the pure-challenge case, and FULLY REPLACED the flawed negative-example bullet whose 'unless the sender is ALSO giving a genuine new scheduling instruction' clause reopened the bug by never saying WHOSE names to keep and silently dropped additive asks. THE DISCRIMINATOR IS SEMANTIC, NOT A KEYWORD LIST - 'a real ask to include vs a disputing question about the past' - which is the stated reason it should carry to Hebrew. Three-part contrast: disputing case excluded with an explicit example; ADDITIVE case included with explicit examples ('did you also check Dana?', 'can you add Yael too?') because that is the row a naive 'if disputed then empty' fix breaks; MIXED case worked using the ACTUAL production sentence shape, returning only the new instruction's person. Net +4/-1 in one template section, nothing else in the file touched. HONEST LIMITATION IT STATED ITSELF: the six traces were 'reasoned from the code, not re-read', so they are argument rather than observation - which is inherent to a meaning-based fix and is the gap the verifier was pointed at. typecheck clean"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"bouncer","source":"verify","ref":"VERIFY-four-item-scheduling-wave","finding":"combined pass over the finished wave: 2 built, 2 OVERTURNED to needs-owner-decision, 1 new row, 1 discovery, 1 wrap hazard, 9 verifiedClean - plus TWO Manager bookkeeping failures caught in the act","verdict":"audit","state":"built","note":"tsc clean across the tree. CAUGHT THE MANAGER MID-FAILURE, and was right on both: state.inFlight still carried all THREE dispatch entries, one literally renamed to end in -RETURNED rather than removed, while report.md still said ZERO built and carried the row-160 dependency ask as 'raised, NOT dispatched' even though instructor's fix for exactly that ask was in the tree. It confirmed the diff contained precisely the four items and nothing was half-written, so it proceeded - but flagged that both files owed an update before the wrap or the ledger would misdescribe the run. This is the same class as the 2026-07-28 failure the persist-before-narrate rule exists for; renaming an entry is not removing it, and inFlight is the STOP 1 lock so a stale entry refuses the next run the owner asks for. CORRECTED THE MANAGER'S STAKES CLAIM, twice repeated: createMeeting's union is NOT completely unguarded - createMeeting.ts:677-697 calls getFreeBusyForDecision on the internal attendees and returns success:false, error:'unresolved_attendee' IN CODE, so a PHANTOM name cannot reach a real invite. What is unguarded is a WRONG REAL PERSON, a resolved valid colleague pushed into attendees with no confirmation - still row 160's exact failure mode, but a narrower harm than the Manager framed. TICKET COVERAGE, including one nobody named: gh#158 partial (complaint 1 -> row 160 prompt-only and unproven; complaint 2 -> row 161 code and verified; complaint 3 -> NEW row 167, untouched, and it is the whole remainder); gh#157 partial (row 166 covers the false absence AND the 'I can confirm what involves Yael' over-offer that came from the old note's closing sentence; the 'no history of meetings or notes' turn is profiler's and not built); gh#156 'MITM Calendar issues' PARTIAL AND UNNAMED BY ANYONE - its complaint 2 ('Why she lied?' - 'Yep, both work for Yael too', then 'Confirmed properly this time' after being challenged) is THE SAME candidate_slots hole row 161 just closed, on the same MPIM surface, so recording it prevents the next wave re-diagnosing 161 from scratch under a different ticket number; complaints 1 and 3 untouched. gh#164/#165/#152/#151 checked, no overlap. No Improvement or Feature satisfied or contradicted. SHAPE: no new file, tool, skill, spine, table, guard, background job or outward connection in any of the three files - nothing needs an architectural ruling. NOT COVERED, stated: no behavioural claim about item 2 was attempted because a classifier prompt cannot be verified from code and the model was not run; and the seven modified charters plus the new untracked architect.md were not audited because they are another chat's diff"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"verify","ref":"candidate-slots-branch-never-surfaced-unresolved-address","invariant":"one-tool-one-rigour","finding":"OVERTURNED to needs-owner-decision - the mechanism works but the fix is a SECOND COPY of a block that already exists 90 lines below, against a written IOU in the same file, and it is ALREADY DRIFTING in the same commit","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts - the warning block duplicated rather than extracted; the file's own note at :680-681 says 'kept in sync (extract to a shared helper next time we touch this file)'","verdict":"needs-owner-decision","state":"open","note":"SUPERSEDES the earlier built row. The dependence on row 161 is genuine and confirmed - the diagnostic could not previously fire because the attendee was never queried - so the ordering is right. But the duplication is real and measurable: _attendee_email_warning at :797 is BYTE-IDENTICAL to :891, while _attendee_not_checked_warning at :808 SILENTLY DROPS two clauses the :914 original carries ('Present the slots as <owner>'s own openings' and 'or re-call with a narrower/valid window'). Same rule, two spellings, BORN DIVERGENT in the commit that created the copy. Consequence: the next wording change lands on one branch and misses the other, and two branches of one tool tell Sonnet different things about the same fact - which is precisely the M2 split this wave set out to fix. ASK: extract the ownerDomain -> filter -> enrichUnresolvedInternal -> warning block into ONE helper both branches call. createMeeting.ts:678-696 is a THIRD site but legitimately different (it refuses rather than warns) - leave it. RESIDUAL STATED: even after the fix a phantom address still returns available:true with the warning riding alongside, so honouring it is prose again - blast radius bounded by createMeeting's in-code refusal (verifiedClean #7)"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"verify","ref":"gh#157-withheld-calendar-reported-as-absence","invariant":"a-restriction-is-not-an-absence","finding":"OVERTURNED to needs-owner-decision - the wording closes a real gap but `events: []` is still there, and prose was already the control on this branch and prose is what failed","rootCause":"src/skills/meetings/ops/handlers/calendarReads.ts:170-179 - the payload still returns an empty array whose true meaning has to be explained in an adjacent string","verdict":"needs-owner-decision","state":"open","note":"SUPERSEDES the earlier built row. CREDIT WHERE DUE: the new wording shuts a specific hole the verify identified precisely - the OLD note forbade listing, summarising, counting and hinting but never forbade ASSERTING ABSENCE, which is exactly the sentence Maelle produced. That gap is now closed. BUT the row's own diagnosis is that the root is a payload indistinguishable from an empty calendar, and the fix's own comment concedes it: 'events: [] here means WITHHELD, never CONFIRMED EMPTY.' A field whose true meaning must be explained in an adjacent string IS the ambiguity, not the fix for it. MATCHMAKER'S OWN CHARTER RULE 10 RULES ON THIS DIRECTLY: 'The pattern is don't return it ... a guard that scrubs a leak is a backstop, never the control - fix the payload upstream.' CODE OPTION, one key: omit `events` on this branch, or return the withheld state through the existing error-shaped refusal that renderToolSummary already handles, so there is no empty array to read as data. OWNER'S CALL because it is a judgement about how much prose-only protection is acceptable here; if he wants the code version it is a small matchmaker ask. SECOND POINT, UNRESOLVED, needs one sentence from him: the note simultaneously says 'do NOT confirm or deny anything about his day' AND 'you can still ACT on one specific meeting he references by name/id here (move it, cancel it)' - and confirming a move in a shared thread reveals the meeting exists. The narrowing is an improvement on the old text but the tension is still live"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"verify","ref":"gh#158-availability-answered-about-owner-not-attendee","finding":"NEW ROW 167 - 'is Levana free tomorrow at 10am?' was answered about the OWNER's working hours rather than hers, until he said 'you are not answering what i'm asking'","rootCause":"not established - nothing in the tree addresses it","verdict":"needs-owner-decision","state":"open","note":"Found by the verify while checking ticket coverage. A question about person A answered with facts about person B. This is gh#158's THIRD complaint and the LAST OPEN PIECE of that ticket - rows 160 and 161 cover complaints 1 and 2, so closing #158 depends on this. No lane has looked at it"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"verify","ref":"owner-shared-surface-set-never-read","finding":"DISCOVERY, not built - `_owner_shared_surface: true` is set in the payload and read by nothing","rootCause":"src/skills/meetings/ops/handlers/calendarReads.ts:176 is the only occurrence in src","verdict":"needs-owner-decision","state":"open","note":"No code reads it and no prompt explains it, so it is a set-but-never-read field in a payload the model receives. Pre-existing, in a file this wave touched. Not built because a discovery built inside the wave that found it invalidates the pass. Note this also CONFIRMS matchmaker's own claim for row 166 - it verified no other code path reads the flag, which is what makes the 1:1-DM-unaffected trace sound"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"architect","source":"verify","ref":"WRAP-HAZARD-untracked-architect-agent","finding":"WRAP HAZARD - seven .claude/agents/*.md charters are modified and .claude/agents/architect.md is NEW AND UNTRACKED, so a `git add -A` wrap ships a brand-new AGENT inside a bug-fix commit, unreviewed by this pass","rootCause":".claude/agents/ - another chat's uncommitted work","verdict":"needs-owner-decision","state":"open","note":"Raised by the verify, which explicitly did NOT audit those files because they are outside this wave. Routed to architect, which now owns the framework's own files. The owner should decide this deliberately rather than by staging everything - the same 'take other chat code' instruction has been given before and was correct then, but a new agent definition is a different kind of thing from a charter edit and it has had no review"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"verify","ref":"gh#158-availability-answered-about-owner-not-attendee","finding":"OWNER DEFERRED to 2026-07-30 - 'is Levana free tomorrow at 10am?' answered about the OWNER's working hours rather than hers","rootCause":"not established - no lane has looked","verdict":"needs-owner-decision","state":"deferred","note":"Owner's words: '167 - defered for tomorrow'. Report row 167. NOTHING IS FIXED, so the symptom recurs and an honest log review will re-find it - this row MUST go into the next run's openKnown or it comes back as a fresh bug. It is gh#158's THIRD complaint and the LAST OPEN PIECE of that ticket, so #158 cannot close without it. First step when it is picked up is establishing which path answers an attendee-availability question using owner data; the fix follows from that"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"gatekeeper","source":"both","ref":"flow-narrates-unexecuted-actions","finding":"OWNER DECLINED - the claim-checker keeps its confess-do-not-act remedy; no read-only re-run will be built","rootCause":"src/utils/claimChecker.ts:404-408 - the v3.4 own-the-miss rewrite deliberately replaced the earlier remedy of re-invoking the orchestrator, which could re-fire a WRITE and caused the Amazia duplicate-send","verdict":"declined","state":"built","note":"Owner's words: '165 - lets ignore it'. Report row 165. RECORDED AS DECLINED RATHER THAN LEFT OPEN, per the 2026-07-26 lesson where 24 owner decisions were written as prose and nothing closed them, so the next run would have re-raised every one. DO NOT RE-RAISE THIS. What he declined, precisely: he was offered (a) re-run READ tools only then answer, never a write, or (b) leave it as is - and he chose (b). The consequence he accepted: when Maelle claims an action she did not take, she will keep confessing rather than doing it, so he gets an honest admission and has to ask again. Note the guard itself is working correctly - it caught all five fires on 2026-07-28 and the rewriter already runs a Sonnet keep-veto so the misfire direction is guarded. If the five-a-day rate rises materially, that is new evidence and a new conversation, not a re-raise of this row"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"verify","ref":"gh#157-withheld-calendar-reported-as-absence","invariant":"a-restriction-is-not-an-absence","finding":"BUILT on the owner's approval - the withheld shared-surface branch now returns an ERROR-SHAPED REFUSAL instead of an empty events array, and that closed a SECOND instance of the same ambiguity nobody had noticed","rootCause":"src/skills/meetings/ops/handlers/calendarReads.ts:170-192","verdict":"built","state":"built","note":"SUPERSEDES the overturn row. Returns { error: 'calendar_withheld_shared_surface', message: '...You have NOT checked his calendar: do NOT say or imply that nothing exists...' }, matching the existing owner_only / freebusy_not_read / event_not_found refusals ALREADY in this file - shape chosen over 'omit events' on REUSE grounds, not preference. THE SECOND INSTANCE, which is the real prize and was not in anyone's brief: turnHelpers.ts's generic FAILED detection (typeof result.error === 'string', checked BEFORE the per-tool switch) now renders the compact history line as [get_calendar FAILED: calendar_withheld_shared_surface] instead of the old '...: 0 events'. So the PERSISTED CONVERSATION HISTORY previously recorded '0 events' for this branch, meaning every LATER turn would re-read a false absence even after the immediate-turn wording was fixed. The prose fix could not reach that; the payload fix does. It also lands on index.ts's isErrorResult check, so the withheld response is never cached as a tool result. _owner_shared_surface CONFIRMED DEAD (one write, zero reads in src) and retired as a natural consequence of the restructure rather than as a separate change - closing the verify's own discovery in the same edit. CONFIRM-A-MOVE TENSION DELIBERATELY UNTOUCHED as instructed, and it reported rather than picked: the change only touches get_calendar's payload, not delete_meeting or move_meeting, so it neither worsens nor resolves the fact that acting on a named meeting in a shared thread still reveals it exists. The owner has not ruled on that. 1:1 owner DM byte-identical - isSharedSurface = senderRole === 'colleague' is false in a real DM so the whole block short-circuits before any edit"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"verify","ref":"candidate-slots-branch-never-surfaced-unresolved-address","invariant":"one-tool-one-rigour","finding":"BUILT on the owner's approval - the duplicated warning block is now ONE helper both branches call, and the file's own IOU is discharged","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts - the block was duplicated rather than extracted","verdict":"built","state":"built","note":"SUPERSEDES the overturn row. Extracted attendeeCheckWarnings at :46-102, called from the candidate-validation branch (:842-854) and the strict/spread pass (:913-920); BOTH inline copies deleted along with the file's own 'extract to a shared helper next time we touch this file' comment. Net 1712 -> 1711 lines, so down rather than flat. THE TWO MIGRATED CLAUSES WERE CONFIRMED CORRECT ON THE CANDIDATE BRANCH BEFORE CARRYING THEM, with per-clause reasoning rather than a blanket claim: 'Present the slots as <owner>'s own openings' is generic prose and not a literal reference to a `slots` field (the candidate branch's own local is `results`, and the sentence still reads correctly describing per-candidate times), and 'or re-call with a narrower/valid window' applies because attendeesNotChecked on that branch is populated by the same P15 cause - a malformed or rejected free-busy window - just scoped to one candidate's window instead of the whole search range. So the candidate branch GAINS two clauses it lacked, in the direction of the original, which was the intended outcome. createMeeting.ts:678-696 left untouched as instructed - legitimately different, it refuses rather than warns. Traced: non-directory address on the candidate branch and on the main branch both build identical text and log lines; a correct address leaves both warnings undefined, spread as {}, no extra keys, unchanged from pre-refactor. ONE INCIDENTAL LOG-ONLY CHANGE DISCLOSED: the main branch's email-warning log call now also carries searchFrom/searchTo, which the not-checked log already had - internal diagnostics only, no effect on the returned payload or anything model-facing. tsc clean"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"bouncer","source":"verify","ref":"VERIFY-narrow-166-163-refinements","finding":"narrow pass on the two rebuilt overturns: both BUILT, nothing blocking, tsc confirmed green - but ONE of matchmaker's per-clause confirmations is FALSE, and the '0 events' bug it just fixed in one place is still live behind three more doors","verdict":"audit","state":"built","note":"THE TAPE QUESTION ANSWERED IN BOTH DIRECTIONS, which is why item A was sent back: no new false flag, because claimChecker.ts:197 checks exactly one rule - did the draft claim a COMPLETED EXTERNAL ACTION - and an honest 'I can't check that here' states none, with future-tense commitments explicitly exempt at :256; and no lost catch, because the fabrication row 166 exists for was NEVER in the checker's remit - claimChecker.ts:253 lists 'describing what's ALREADY on the calendar' as NOT a false claim, so the ambiguity was always the payload's to fix. Action tape and mutation marker unchanged: get_calendar is absent from MUTATION_DOMAIN (turnHelpers.ts:106-120) and MUTATION_OK_RE (:363) requires a mutation name plus ' OK', so the FAILED line can never reach the pinned tape; the availability floor arms off checkSlot verdicts and reads the tape only for mutated=book. THE 'SECOND INSTANCE' CLAIM IS TRUE AND STRONGER THAN THE LANE STATED: the old return was an OBJECT, and turnHelpers.ts:209-211 renders get_calendar via `Array.isArray(result) ? result : []`, so an object ALWAYS rendered '0 events' - and postReply.ts:512-517 persists toolSummaries.join(' ') VERBATIM into conversation history. So persisted history genuinely recorded '0 events' for a calendar never read, and later turns re-read it. Not merely tidier. ONE UNREPORTED SIDE EFFECT, benign and disclosed: index.ts:974 sets errored on `typeof r.error === 'string'` -> turnLeftWorkPending -> codaEligible false (:1344), so a withheld-calendar turn now suppresses the social coda and skips that LLM call. The old success-shaped payload did not. Arguably correct and slightly cheaper, but a new orchestrator-level consequence the lane did not mention. Also confirmed: isErrorResult (index.ts:667-679) skips recordToolCall and the branch returns BEFORE getCalendarEvents, so a re-call costs no Graph round-trip; _owner_shared_surface has zero readers in src (only stale dist output); the 1:1 owner DM short-circuits at calendarReads.ts:161. NOT COVERED, stated: classifyTurn.ts (row 160), anything under .claude/, and the new architect agent/ledger/script trio - the last of which it flagged as an architectural addition arriving without a decision row, though the owner has ruled that out of this chat's scope"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"verify","ref":"candidate-branch-narrower-window-clause-wrong","invariant":"one-tool-one-rigour","finding":"REFUTES matchmaker's own per-clause confirmation - 'or re-call with a narrower/valid window' is WRONG on the candidate branch, where the window IS the candidate and the only failure of this class is a window already too narrow","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts:46-102 - the migrated clause in the newly shared attendeeCheckWarnings helper","verdict":"needs-owner-decision","state":"open","note":"THE MANAGER SPECIFICALLY ASKED THE LANE TO CONFIRM BOTH MIGRATED CLAUSES AND IT CONFIRMED THIS ONE WRONGLY - which is exactly why the clause-level check was worth asking for. attendeesNotChecked is populated on a Graph WINDOW REJECTION including ErrorInvalidMergedFreeBusyInterval (connectors/graph/calendarReads.ts:1011-1021, whose own comment says the remedy is to 'retry a sane window'), and connectors/graph/findAvailableSlots.ts:261-266 names the observed instance: 'one candidate slot was 3 minutes wide.' On the candidate branch searchFrom/searchTo ARE cand.start/cand.end, so the only way this fires is a window already too narrow - and the migrated clause tells the model to narrow it FURTHER. On the main branch, where the window is a user-supplied multi-day range with a 62-day clamp in play, 'narrower' is legitimate. CONSEQUENCE: a wasted re-call that fails identically, then a second unchecked answer. NO LIE and NOT a wrap blocker - the harm-bearing clauses ('These are NOT confirmed-free times', 'Never say they are free') are correct on both branches. ASK: make the remedy clause DIRECTION-NEUTRAL in the one helper (e.g. 're-ask with a window Graph can answer') rather than adding a per-branch flag - the verifier's own reasoning is that a boolean on a freshly shared helper would be worse than the duplication it replaced"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"verify","ref":"zero-events-false-absence-in-three-more-branches","invariant":"a-restriction-is-not-an-absence","finding":"DISCOVERY - the '0 events' false absence row 166 just fixed is STILL LIVE behind three more get_calendar doors, and the cheapest fix is ONE LINE in the renderer rather than four handlers","rootCause":"src/core/orchestrator/turnHelpers.ts:210 tests Array.isArray(result), so EVERY object-shaped get_calendar return renders '0 events' in persisted history - affecting calendarReads.ts:257-261, :298-302 and :313","verdict":"needs-owner-decision","state":"open","note":"THE COLLEAGUE VIEW ALWAYS RETURNS AN OBJECT, so a colleague thread's persisted history reads '0 events' however many shared meetings were actually listed - and an owner DM carrying a Working-Elsewhere or optional-join note does too. This is row 166's exact ambiguity behind three more doors, and it is also the mechanism that made the 'second instance' claim true. CHEAPEST FIX IS ONE LINE AT THE RENDERER: read result.events when the result is an object, instead of changing four handlers. Not built because a discovery built inside the wave that found it invalidates the pass that found it"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"verify","ref":"candidate-available-true-with-attendee-unchecked","invariant":"a-restriction-is-not-an-absence","finding":"DISCOVERY - the candidate branch still asserts available:true with prose as the only control when the attendee's free/busy was never read; structurally the SAME pattern the owner just ruled on for row 166, one layer down","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts - the per-result `available` field","verdict":"needs-owner-decision","state":"open","note":"Row 161 folded attendee busy into that verdict, so `available` now means 'free for both' - EXCEPT when attendeesNotChecked fires, where it silently means 'free for the owner, attendee unknown'. A positive structured field whose real meaning lives in an adjacent string, which is precisely the row-166 shape he ruled on by choosing the payload fix over the prose one. EXPLICITLY NOT AN OVERTURN - this wave strictly improves the situation, since before row 163 there was no warning at all - but it is the same class and whether the per-result shape should carry the unchecked state is the owner's call"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"verify","ref":"stale-labelfor-iou-comment","finding":"DISCOVERY - a false IOU comment that will send the next lane to extract something already extracted","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts:738-739 - 'Same rule-label mapping as Guard B uses; kept in sync (extract to a shared helper next time we touch this file)'","verdict":"needs-owner-decision","state":"open","note":"labelFor is a one-line alias over humanizeViolationLabel, which ALREADY lives in shared src/skills/meetings/ops/violationLabels.ts and is imported by five handlers. Nothing is kept in sync; the debt was already paid. Shared rule 11 - leave every comment true. One-line deletion, pre-existing, and directly analogous to the IOU this wave just discharged two hundred lines above it"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"verify","ref":"candidate-branch-narrower-window-clause-wrong","invariant":"one-tool-one-rigour","finding":"BUILT - the migrated remedy clause now drops the directional claim entirely rather than picking a branch, so it reads true on both","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts:94, inside the shared attendeeCheckWarnings helper","verdict":"built","state":"built","note":"Owner approved ('169 - ok build'). Changed 'or re-call with a narrower/valid window' to 'or re-call with a window Graph can actually answer (this one wasn't one)'. THE LANE'S WORDING IS BETTER THAN THE MANAGER'S SUGGESTION: dropping the directional claim outright, rather than substituting a different direction, is what makes one sentence true in both places. Traced both call sites: candidate branch (:784-785, searchFrom cand.start / searchTo cand.end) - the window that failed IS the candidate slot, sometimes already too narrow (the '3 minutes wide' case at connectors/graph/findAvailableSlots.ts:261-266), and the new sentence no longer prescribes the exact failure mode; main branch (:868-869, effectiveSearchFrom/To, the user-supplied multi-day range under the 62-day clamp) - 'a window Graph can actually answer' still reads correctly because a narrower range is one way to get there without the sentence asserting WHICH adjustment is needed. Both harm-bearing clauses re-read after the edit and confirmed BYTE-IDENTICAL: 'These are NOT confirmed-free times' and 'Never say they are free'. String-literal only, no logic change, net line count unchanged. NOT SENT TO A VERIFY PASS: two prose edits with no logic change, the helper's structure already covered by tonight's narrow pass, and the harm-bearing text confirmed unchanged - an Opus pass over two string literals is the disproportionate cost the framework warns about"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"verify","ref":"stale-labelfor-iou-comment","finding":"BUILT - the false IOU is replaced with a true description of what labelFor actually is","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts:738-741","verdict":"built","state":"built","note":"Owner approved ('171 - ok'). VERIFIED BEFORE DELETING, which is the right order for a comment fix: humanizeViolationLabel is defined once at src/skills/meetings/ops/violationLabels.ts:8 and imported by calendarReads.ts, createMeeting.ts, findAvailableSlots.ts and moveMeeting.ts - so the underlying extraction is real and only the comment's claim was false. CORRECTED THE MANAGER'S COUNT: four handlers plus the source file, not the 'five handlers' the brief asserted. New comment describes labelFor accurately - a local closure partially applying the already-shared helper by binding ownerFirst, matching the identical pattern at createMeeting.ts:551. labelFor SURVIVES because it is used at :815; comment-only change, no dead code. Shared rule 11 satisfied - and note the contrast with the IOU discharged 200 lines above it earlier tonight, which was REAL and was paid; this one described a debt that no longer existed, which is the more dangerous kind because it costs the next lane a wasted extraction"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"profiler","source":"github","ref":"gh#157-no-history-with-colleague","finding":"OWNER DEFERRED to 2026-07-30 - she told Yael she has 'no history of meetings or notes with her so far, this looks like our first real interaction' despite the owner meeting her regularly","rootCause":"undetermined - logs/maelle-2026-07-28.log:540 shows actionCount:0, so ZERO tool calls of any kind that turn: not calendar, not people-memory","verdict":"needs-owner-decision","state":"deferred","note":"Owner's words: 'move to tomorrow, already late'. Report row 172. MANAGER BOOKKEEPING FAILURE CORRECTED: this is a thing the owner SAW in a real chat, and until now it existed only as a sentence inside the gh#157 row's note rather than as a row with a status - which is exactly how a real complaint gets forgotten. It now has its own row and its own deferral. Matchmaker EXPLICITLY declined to assume it shares a fix with row 166 - same shared surface, different subsystem - so do not fold them. First step when picked up: establish whether the interaction history exists in the person store and was not read, or does not exist at all. gh#157 cannot close without it"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"matchmaker","source":"github","ref":"gh#156-unprompted-inperson-online-question","finding":"OWNER DEFERRED to 2026-07-30 - she asked an unprompted question about in-person vs online that the owner had not raised","rootCause":"undetermined - nobody has looked","verdict":"needs-owner-decision","state":"deferred","note":"Owner's words: 'move to tomorrow already late'. Report row 173. gh#156 complaint 1. FOUND BY THE VERIFY while checking ticket coverage, NOT by the scout - gh#156 has been open with nobody working it, and only its complaint 2 was closed today (as a side effect of row 161, since it was the same candidate_slots hole). LANE IS A GUESS: meeting-mode is matchmaker's territory, but if the prompting is what drives the question then the durable fix is instructor's. Do not let a lane inherit that guess as though it were established routing"}
{"date":"2026-07-29","runId":"wf_33541300-121","lane":"registrar","source":"github","ref":"gh#156-get-back-to-you-no-followthrough","finding":"OWNER DEFERRED to 2026-07-30 - she said 'I'll get back to you' and then did not","rootCause":"undetermined - nobody has looked","verdict":"needs-owner-decision","state":"deferred","note":"Owner's words: 'move to tomorrow already late'. Report row 174. gh#156 complaint 3. A promise with no async work item behind it is precisely what the requests spine exists to prevent - 'nothing raised gets lost' is its entire claim - which is why this routes to shepherd rather than to whoever wrote the sentence. Also found by the verify during ticket coverage rather than by the scout. Worth noting for the next run: TWO of gh#156's three complaints were invisible to the scout and surfaced only because the verifier reads the finished diff against every open issue - that is the ticketCoverage mechanism earning its place"}
{"date":"2026-07-30","runId":"wf_6852af85-afc","lane":"outrider","source":"github","ref":"gh#166","finding":"the morning brief dropped its whole news section because the gather lost an 8s race it was allowed 12s to run","rootCause":"src/tasks/briefs.ts:31 - NEWS_BRIEF_TIMEOUT_MS 8_000 raced against NEWS_PER_GOAL_TIMEOUT_MS 12_000 (src/skills/news.ts:54) plus an un-timed planNewsGoals Haiku call (news.ts:271)","verdict":"built","state":"built","invariant":"outer-budget-exceeds-inner","note":"Raised to 20_000 with the budget relationship written into the comment so it cannot be silently re-inverted. Verify CONFIRMED, ticketCoverage satisfied. VERIFY CORRECTION the owner should see because the trade is his: this is NOT only a once-a-morning cron pass - processMessage.ts:511-519 and tasks/skill.ts:598 short-circuit an owner 'send the brief' request into the same function, so the worst-case wait behind the eye-emoji receipt grew from ~8s to ~20s. No correctness defect and no hard ceiling; if that wait matters the lever is a shorter budget when force is true, not reverting the constant. Margin trend that proved it was a coin flip: 5.00s, 5.94s, 7.94s (6ms of headroom), 8.32s dropped."}
{"date":"2026-07-30","runId":"wf_6852af85-afc","lane":"outrider","source":"github","ref":"gh#167","finding":"the brief never said the news pass had failed, so a broken gather and a quiet news day were indistinguishable to the owner","rootCause":"src/tasks/briefs.ts:695 (pre-fix) - the Promise.race collapsed 'never finished in time' and 'finished with nothing' into one silent outcome","verdict":"built","state":"built","invariant":"failure-distinguishable-from-emptiness","note":"Three-way branch on the value the race already returns: WARN on the timeout (previously silent), use it when there are sources, INFO when the pass genuinely found nothing. +14/-1, log-only, no new state, mirrors the calendar-health vacuous pattern next door. The verify VERIFIED the load-bearing premise rather than taking it: gatherNews wraps its whole body in try/catch returning EMPTY_BUNDLE (news.ts:302) and every early return is a NewsBundle, so it can neither throw nor resolve undefined - !gathered is exactly and only the timeout branch. ticketCoverage satisfied."}
{"date":"2026-07-30","runId":"wf_6852af85-afc","lane":"matchmaker","source":"both","ref":"gh#165-b","finding":"asked to be added to a meeting just booked for her, Maelle re-ran create_meeting under an invented subject at the same slot, collided with the meeting itself, and raised a second approval to the owner","rootCause":"src/connectors/graph/calendarReads.ts:496-508 vs src/utils/scheduleRules.ts:485-504 - the lookup that NAMES the conflict filters only isCancelled and matches an exact start, while the collision that TRIGGERED the refusal means an overlapping commitment after occupancyRoleOf discards free/floating/all-day-WE events. Two predicates, one fact","verdict":"needs-owner-decision","state":"built","invariant":"occupancy-derived-once","note":"OVERTURNED BY THE VERIFY. The fix is on disk (+49/-17 across calendarReads.ts and createMeeting.ts) and it does resolve the reported case, but the verify REFUTED a neighbouring one: when a real blocker overlaps from an earlier start while an occupancyRoleOf-ignored event (a lunch/focus block, showAs=free) starts at the requested time, the message names the WRONG event and steers add_attendees at it. Dependency ask to matchmaker recorded separately. Two amplifiers the verify attached: the steer names update_meeting(meeting_id, add_attendees) but the schema also requires meeting_subject (skills/meetings.ts:512), so a masked subject makes the model pass [Private] which then renders inside moveMeeting's own refusals; and findDuplicateEvent's header calls it the ONE idempotency primitive while a null-subject call is not asking about a duplicate at all."}
{"date":"2026-07-30","runId":"wf_6852af85-afc","lane":"matchmaker","source":"both","ref":"gh#165-a","finding":"Ayala asked to book 15 minutes with herself, Idan, Scott and Chris and was not put on the invite - she had to ask include me as well please afterwards","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:141-145 - the owner-and-requester exclusion is unconditional for any colleague-initiated request, not gated on whether her own message implies she is attending","verdict":"needs-shaping","state":"open","note":"NOT DISPATCHED - the scout classified it needs-shaping and the engine correctly held it. The product call is the owner's: should a colleague who requests a meeting she is plainly part of be added by default? The code comment states the exclusion assumes the requester is organising on behalf of others rather than attending, and that assumption is exactly what fails here. This is gh#165's complaint 2 and its TITLE, and the verify said explicitly do NOT close #165 on the strength of complaint 4."}
{"date":"2026-07-30","runId":"wf_6852af85-afc","lane":"matchmaker","source":"github","ref":"gh#164","finding":"Maelle offered Michal a squeeze into 16:15 in prose, then refused it when Michal accepted, saying 16:15 was too soon to book on his side","rootCause":"unproven - the scout's hypothesis is the already-open row precheck-claims-booking-parity-it-does-not-have: an offer surfaced in prose is not validated against the rule ladder the booking path enforces","verdict":"needs-shaping","state":"open","note":"NOT DISPATCHED. clarity ambiguous, severity low. The shaping question is whether this is the same root as that open backlog row or a distinct prose-offering defect - worth confirming so a lane does not duplicate an already-tracked fix. It was eventually booked at that same 16:15 after being flagged to him."}
{"date":"2026-07-30","runId":"wf_6852af85-afc","lane":"gatekeeper","source":"github","ref":"gh#156-b","finding":"Yael asked whether Sunday and Tuesday work for her too and Maelle answered Yep both work for Yael too with no tool call behind it, admitting sorry for the false confirm earlier only after being pressed twice","rootCause":"same shape as the declined class flow-narrates-unexecuted-actions - the claim-checker catches and rewrites rather than re-running the tool","verdict":"needs-shaping","state":"open","note":"NOT DISPATCHED, and it is the HIGHEST-severity item this run found. The shaping question revisits a ruling he already made: he declined having the claim-checker re-run a tool on an unexecuted-action claim, because of duplicate-write risk. This instance reached a COLLEAGUE as a flat, specific, false yes-checked-clean about her own calendar. Does he want the checker to force a hedge or a refusal instead of letting a specific false claim ship as stated fact? That is a narrower ask than re-running the tool and carries none of the write risk."}
{"date":"2026-07-30","runId":"wf_6852af85-afc","lane":"matchmaker","source":"verify","ref":"recurring-category-subject-unmasked","finding":"a colleague can be told the real subject of a meeting the owner marked private, by casual narration on the recurring-category collision branch","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:428, :434, :436 - existing_subject: match.subject and the same raw value interpolated into the colleague-facing message with no displaySubject/subjectViewerFor call, in contrast to the same file's new :583 which does mask","verdict":"flagged-for-owner","state":"open","invariant":"subject-masked-at-production","note":"VERIFY DISCOVERY, severity medium - NOT this wave's work and deliberately not built in it. Pre-existing; the lane that touched the neighbouring branch named it as out of scope rather than scope-creeping it. Next build's intake, carried in state.pendingOverflow. An interview or a sensitive 1:1 leaks by narration - an existing Weekly occurrence, real subject, on Tuesday 5 Aug."}
{"date":"2026-07-30","runId":"wf_6852af85-afc","lane":"instructor","source":"verify","ref":"update-meeting-desc-claims-self-only","finding":"the update_meeting tool description tells the model colleagues can only add or remove themselves, but requester-controls have been implemented since v3.1.4 - so Maelle refuses attendee changes she is allowed to make, or escalates what the requester could have done herself","rootCause":"src/skills/meetings.ts:482 and :495 (stale self-only wording) vs src/skills/meetings/ops/handlers/moveMeeting.ts:162-210 (the requester gate: isRequester grants full add/remove)","verdict":"flagged-for-owner","state":"open","invariant":"prompt-describes-shipped-code","note":"VERIFY DISCOVERY, severity medium - next build's intake, carried in state.pendingOverflow. Directly relevant to gh#165-b, whose whole fix is a steer TOWARD update_meeting(add_attendees): the tool description currently argues against the steer the built fix depends on."}
{"date":"2026-07-30","ref":"requested-time-local-false-provenance","source":"audit","verdict":"already-fixed","note":"backlog re-read: src/skills/meetings/ops/handlers/findAvailableSlots.ts:860 now branches on groundTzStated to say the zone was inferred vs the zone the times were given in. Matches CHANGELOG 4.3.1 (commit b7a4a8b)."}
{"date":"2026-07-30","ref":"gh#158-attendee-misresolution","source":"audit","verdict":"already-fixed","note":"backlog re-read: src/core/social/classifyTurn.ts:242 now has an explicit rule excluding a disputed-past-search name from the attendee list. Matches CHANGELOG 4.3.2 (commit 1bc4fcb); the changelog itself flags this as prompt-only and unproven by construction."}
{"date":"2026-07-30","ref":"nondirectory-email-accepted-and-defaulted","source":"audit","verdict":"already-fixed","note":"backlog re-read: src/skills/meetings/ops/handlers/findAvailableSlots.ts:758-764 and :784, the candidate branch now passes candidateAttendeeBusyEmails and reuses the shared attendeeCheckWarnings helper, per CHANGELOG 4.3.2's directory-mismatch fix shared across both branches."}
{"date":"2026-07-30","ref":"candidate-available-true-with-attendee-unchecked","source":"audit","verdict":"already-fixed","note":"backlog re-read: same fix as nondirectory-email-accepted-and-defaulted; findAvailableSlots.ts:762-784 wires candidateAttendeeBusyEmails and the shared warnings into the candidate_slots branch."}
{"date":"2026-07-30","ref":"gh#24-stage-4-send-retry","source":"audit","verdict":"already-fixed","note":"backlog re-read: src/connectors/email/inbound.ts:19-41 header documents the corrected no-retry contract wrapping the handler in a try/catch that DMs the owner on failure (notifyOwnerOfMailFailure)."}
{"date":"2026-07-30","ref":"gh#24-E3E4-addrlist","source":"audit","verdict":"already-fixed","note":"backlog re-read: src/connectors/email/inbound.ts:47 and src/connections/email/index.ts:74,:89 now import the same ownerEmailAddresses from ./ownerAddresses; the two independent copies were consolidated."}
{"date":"2026-07-30","ref":"gh#24-stage-9-clamp-intersection","source":"audit","verdict":"already-fixed","note":"backlog re-read: src/skills/registry.ts:264-310 filterToolsByScope now checks CHANNEL_TOOL_CLAMP[channel] first and returns the clamp intersected with the full active tool set directly, skipping scope-narrowing entirely."}
{"date":"2026-07-30","ref":"systemprompt-owner-branch-reachability","source":"audit","verdict":"already-fixed","note":"backlog re-read: src/core/orchestrator/systemPrompt.ts:320-342 states one reachability filter now guards both paths, matching CHANGELOG 4.3.0 (commit 0b70f0a)."}
{"date":"2026-07-30","ref":"WRAP-HAZARD-untracked-architect-agent","source":"audit","verdict":"already-fixed","note":"backlog re-read CORRECTED BY HAND: the scout reported this still-real on the claim that git status shows .claude/agents/architect.md untracked, which is false - git ls-files --error-unmatch resolves it and the tree is clean. It was committed in the v4.3.3 wrap (0d61f42) alongside the seven lane charters. Closed on the verified repo state, not on the scout's assertion."}
{"date":"2026-07-30","ref":"precheck-claims-booking-parity-it-does-not-have","recheck":"still-real: src/utils/availabilityPreCheck.ts:741-748 checkAt/checkSlot path unchanged since the row was written; no parity note or shared-validator cross-check added between the precheck and planMeeting.ts's actual booking validation."}
{"date":"2026-07-30","ref":"self-mention-adds-maelle-as-attendee","recheck":"still-real: could not locate the cited block at src/connectors/graph/findAvailableSlots.ts:343-378 verbatim (the region now holds moving-event-attendee union logic) and found no fix reference elsewhere in the file; treated as unmoved/unverified rather than closed."}
{"date":"2026-07-30","ref":"remaining-cached-decision-reads","recheck":"still-real: src/utils/availabilityPreCheck.ts:660-672 eventsForWeek still caches per weekKey for the life of the call, with no explicit-refresh path distinct from the other cached sites the row names."}
{"date":"2026-07-30","ref":"slot-hold-release-dm-role-gate","recheck":"still-real: src/skills/meetings/ops/handlers/createMeeting.ts:1352-1375, releaseHoldsForOwner plus a DM to any h.holder_slack_id other than context.userId, with no role or authorization check before the DM fires."}
{"date":"2026-07-30","ref":"preferredslotstatus-no-presentation-local","recheck":"still-real: src/skills/meetings/ops/handlers/findAvailableSlots.ts:1368-1384, preferredSlotStatus has no presentation_local field, while the regular per-slot path at :1531 attaches it via spread."}
{"date":"2026-07-30","ref":"classifier-guess-reaches-real-calendar-invite","recheck":"still-real: src/skills/meetings/ops/handlers/createMeeting.ts:150-165 still unions context.resolvedMeetingAttendees into the real invite with no re-check against a confirmed-vs-disputed flag; the upstream classifier fix reduces frequency but the sink still trusts what it is given."}
{"date":"2026-07-30","ref":"zero-events-false-absence-in-three-more-branches","recheck":"still-real: src/core/orchestrator/turnHelpers.ts:209-211 get_calendar summary still coerces a non-array result to an empty array, so an object-shaped refusal still renders as 0 events - unaffected by the 4.3.2 fix, which only touched the direct calendar-read branch."}
{"date":"2026-07-30","ref":"dead-update-tasks-coordinator","recheck":"still-real: src/connectors/slack/coordinator.ts:333-336 and :388-391 still contain the two duplicate UPDATE tasks statements verbatim."}
{"date":"2026-07-30","ref":"concurrent-double-resolve-no-lock","recheck":"still-real: src/core/requests/resolver.ts:186 still does a plain check-then-act on row.state with no mutex or transaction around the read-then-resolve."}
{"date":"2026-07-30","ref":"amend-reason-bypasses-id-veto","recheck":"still-real: src/core/requests/resolver.ts:910 still takes reason directly and threads it into the requester-facing body at :919-938 with no id-veto scrub."}
{"date":"2026-07-30","ref":"stale-tasktype-members","recheck":"still-real: src/db/client.ts:749 comment and schema still list the full historical kind set including reminder, follow_up, research."}
{"date":"2026-07-30","ref":"dateverifier-hebrew-false-positive","recheck":"still-real: src/utils/dateVerifier.ts:187 only guards correctNum equal to pair.writtenWeekdayNum, a numeric comparison; it does not protect against the extraction step mis-assigning writtenWeekdayNum for a Hebrew token."}
{"date":"2026-07-30","runId":"wf_27f03aca-0dd","lane":"matchmaker","source":"both","ref":"gh#158-availability-owner-not-attendee","finding":"asked 'is Levana free tomorrow at 10am?' Maelle answered with the OWNER's hours - 'outside your usual hours, not a hard conflict' - a fact about Idan presented as the answer about Levana","rootCause":"src/utils/availabilityPreCheck.ts (precheckAvailability), invoked from buildTurnContext.ts:611-643 whenever senderRole is colleague and the message carries a schedulable signal. The module answers exactly one question - is the OWNER free at time Y - by running checkSlot against the owner's own calendar (eventsForWeek always fetches profile.user.email) and injecting that verdict into the prompt as settled ground truth. It has zero awareness the message might name a THIRD PARTY","verdict":"needs-owner-decision","state":"built","note":"OVERTURNED BY THE VERIFY, and the overturn is a genuine product fork, not a defect. Built: precheckAvailability takes namedAttendeeEmails and BAILS before spending the Haiku extraction whenever it is non-empty; buildTurnContext threads the already-resolved resolvedMeetingAttendees in. +25/-0, no new state, no new LLM call. Root cause re-derived from PRODUCTION LOGS by the verify rather than taken from the lane: maelle-2026-07-28.log:669 resolved levana.b@ at 12:24:09.680, then :671/:677 injected notBookable for 10:00 and :721/:722 injected bookable for 11:00. THE VERIFY'S CORRECTION: the prompt block was never the lie - renderPromptBlock (availabilityPreCheck.ts:1171-1184) is already owner-scoped in every phrase. What is missing is any statement of WHOSE calendar it covers. So there is a cheaper option than silence, and the choice is his: (a) keep the bail but ALSO forget the hard-block ledger entries for this turn's instants, so the output floor cannot keep asserting the same wrong-subject fact for 45 minutes; or (b) do not bail - keep the verdict, the cross-zone resolution and the floor arming, and add one line naming the block's scope. (a) is the MINIMUM: as built, the fix closes the prompt half and leaves the gate half open."}
{"date":"2026-07-30","runId":"wf_27f03aca-0dd","lane":"matchmaker","source":"both","ref":"gh#158-availability-asserted-unsearched","finding":"'Yes, 11am tomorrow is free for her too' asserted about Levana with no read of her actual calendar","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts:626-650 and :784 - candidateAttendeeBusyEmails was gated on isOwnerInitiatedSearch, so a colleague-path or MPIM-clamped-owner point-check only ever consulted the attendee's stored work-hours and never her real calendar","verdict":"already-fixed","state":"built","invariant":"one-fact-one-derivation","note":"A DUPLICATE REACHED A FULL DISPATCH - this is the run's already-fixed warning, and it cost a real dispatch for no result. The fix was already in the uncommitted tree from the 4.3.2 wave: candidateAttendeeBusyEmails is now computed unconditionally and feeds a genuine getFreeBusyForDecision read on the named attendee. The in-code comment at findAvailableSlots.ts:630-650 names this exact incident verbatim. It escaped alreadyBuilt because the owner's list carried only gh#166 and gh#167; the ledger row that would have caught it is the one whose fix is built-but-unclosed. Same rule-class as gh#165-b."}
{"date":"2026-07-30","runId":"wf_27f03aca-0dd","lane":"matchmaker","source":"logs","ref":"category-classifier-blind-to-location","finding":"the owner asked for an onsite meeting in the offices and create_meeting booked it as category Meeting instead of Physical, so he had to correct it by hand","rootCause":"src/skills/meetings/detectCategory.ts:37-103 - the classification prompt is built from subject/body/attendees/recurrence only and never sees the caller's location argument, even though input.locationHint was already in scope at every call site: planMeeting.ts:320-327 and :340-347, and moveMeeting.ts:269-274. A genuine onsite request with a stated venue read as 'no physical location indicated' and overrode the model's own correct Physical suggestion","verdict":"built","state":"built","note":"Added locationHint to DetectCategoryInput and one 'Location given: …' line to the prompt - raw text, never parsed in code (G7), the classifier reads it. Threaded through both planMeeting call sites and moveMeeting's. +20/-2 across 3 files. Verify confirmed the field is live end-to-end, not a dead parameter: args.location -> bookingRequest.ts:171 -> planMeeting.ts:161 -> :327/:348 -> detectCategory, and no-location meetings render byte-identically to the pre-fix prompt. The lane flagged honestly that the classifier's judgement given honest data is not itself code-provable - the fix closes the INPUT gap, which is the root."}
{"date":"2026-07-30","runId":"wf_27f03aca-0dd","lane":"matchmaker","source":"both","ref":"gh#165-b","finding":"asked to be added to a meeting just booked for her, Maelle re-ran create_meeting under an invented subject at the same slot, collided with the meeting itself, and raised a second approval to the owner","rootCause":"src/utils/scheduleRules.ts / src/connectors/graph/findAvailableSlots.ts - the occupancy scan that DECIDES owner_busy_collision did not carry which event caused it, so the refusal re-derived the answer a second, differently-matched way (findDuplicateEvent on start-time proximity alone) and could name an occupancyRoleOf-ignored free or floating block that merely started at the requested time","verdict":"built","state":"built","invariant":"one-fact-one-derivation","note":"REBUILT THIS RUN, and the previous attempt was REPLACED not patched - this supersedes the needs-owner-decision row from wf_6852af85-afc. The verify's refutation was correct and the lane fixed the cause rather than the symptom: checkSlot now stamps overCommitment.id with the exact event occupancyRoleOf classified as a real overlapping commitment (ignored/optional events are skipped in that same loop, so they structurally cannot produce this id), findAvailableSlots surfaces it via diagnosticsOut.conflictingEvent, and createMeeting reads it off the rule-check call it ALREADY made - zero extra Graph round-trips, where the old path spent a second read. The subject:null mode of findDuplicateEvent is now FULLY DELETED, not merely unused - signature reverted to subject:string, header comment corrected, both remaining callers pass a real subject. One spine, no back-support layer. Dep ask still open: suppress the steer when the conflict is an all-day OOF."}
{"date":"2026-07-30","runId":"wf_27f03aca-0dd","lane":"matchmaker","source":"github","ref":"152-refresh-stale-cache","finding":"the owner asked Maelle to check again for Michal's slot and did not get a fresh read","rootCause":"src/utils/availabilityPreCheck.ts - eventsForWeek called getCalendarEvents with no freshness argument, defaulting to 'cached', the cross-turn up-to-TTL-stale layer. Every other scheduling-decision read in the codebase already goes through getOwnerEventsForDecision, which is always 'live' precisely because a decision must not be answered from a warm copy. This was the last site on the old default","verdict":"built","state":"built","note":"One-line swap to getOwnerEventsForDecision (identical 4-arg signature, drop-in) which also brings its outage-retry and typed CalendarOfflineError contract. Updated the two comments that asserted the file called neither decision helper - true before, false now. IMPORTANT NUANCE the lane stated honestly rather than claiming the win: the ticket's own anecdote (Michal, 'you didn't clear the cache') ran on the OWNER find_available_slots path, which was already live and would not reproduce today - availabilityPreCheck only fires when senderRole is colleague. What was still real, and what shipped, is that this file's own read independently used the stale default. Scoped to exactly the file the issue cites; the other sites the remaining-cached-decision-reads row lists were deliberately left, each needing its own judgement."}
{"date":"2026-07-30","runId":"wf_27f03aca-0dd","lane":"matchmaker","source":"both","ref":"gh#156-unprompted-inperson-online-question","finding":"Maelle asked the owner whether the meeting with Yael should be in-person or online, unprompted - he does not recall being asked this in a regular DM before","verdict":"needs-shaping","state":"open","note":"WAS DEFERRED TO 2026-07-30 AND IS NOW SURFACED - this is one of the four dated deferrals the previous run silently dropped, and it reached his desk this time because openKnown carried converted rows only (A51). Severity low, clarity ambiguous, root undetermined - the scout confirms nobody has looked at the code yet. Not dispatched: needs shaping first, because whether she should ask at all is a product call, not a defect."}
{"date":"2026-07-30","runId":"wf_27f03aca-0dd","lane":"registrar","source":"both","ref":"gh#156-get-back-to-you-no-followthrough","finding":"Maelle said 'let me do that now… and get back to you' about checking Yael's calendar and then did not follow up - the owner had to ping her again","verdict":"needs-shaping","state":"open","note":"WAS DEFERRED TO 2026-07-30 AND IS NOW SURFACED - second of the four dated deferrals the previous run dropped. Routed to shepherd, not matchmaker: the defect is that a stated intention never became a tracked work item, which is the requests spine's job, not the scheduler's. Root undetermined, nobody has looked."}
{"date":"2026-07-30","runId":"wf_27f03aca-0dd","lane":"matchmaker","source":"verify","ref":"vacation-day-reported-as-already-booked","finding":"a colleague asking to book on a day the owner is on vacation is told the time is 'already booked', which invites 'what about 30 minutes later?' and the answer is the same all day - Maelle sounds like she is guarding a meeting when he is simply away","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:565 derives the human label from the violation KIND via humanizeViolationLabel, discarding checkSlot's own violation_label which already distinguishes the two cases at src/utils/scheduleRules.ts:653 - 'is out of office all day on <date>' vs 'already busy at this time'","verdict":"flagged-for-owner","state":"open","invariant":"one-fact-one-derivation","note":"VERIFY DISCOVERY, severity medium - pre-existing at HEAD, NOT this wave's work, and deliberately not built in it. Next build's intake, carried in state.pendingOverflow. Note the D6 comment in scheduleRules names this exact failure and this call site never got the benefit - the honest label already exists and is thrown away. Same invariant as gh#165-b: one fact, derived twice, and the second derivation is worse."}
{"date":"2026-07-30","runId":"wf_27f03aca-0dd","lane":"matchmaker","source":"verify","ref":"hardblock-ledger-has-no-refuter","finding":"Maelle can rewrite a true reply into a false refusal about a time that has since freed up - a slot established as blocked keeps overriding drafts for up to 45 minutes even when nothing in the current turn re-checked it, and the reader is told a 'deterministic calendar check has ALREADY established' it","rootCause":"the hard-block ledger has exactly one writer and one forgetter, both inside src/utils/availabilityPreCheck.ts (:909, :914, grep-confirmed unique in src), but its reader src/utils/guards/runOutputGates.ts:881-923 fires on EVERY turn against a 45-minute TTL (src/utils/availabilityGate.ts:264). Any turn where the pre-check does not run - an owner turn, a message with no schedulable signal, a Haiku-gate miss, and now a turn naming a colleague - inherits the previous verdict with no way to refute it","verdict":"flagged-for-owner","state":"open","invariant":"established-fact-needs-a-refuter","note":"VERIFY DISCOVERY, severity medium, and the sharpest thing this run produced. The asymmetry PREDATES the wave but the gh#158 bail WIDENS it - which is exactly why the verify would not sign off on that fix and why option (a) in its overturn is the minimum. An assertion with a writer and no independent refuter will outlive its truth; that is the general shape. Next build's intake, carried in state.pendingOverflow."}
{"date":"2026-07-30","runId":"wf_27f03aca-0dd","lane":"matchmaker","source":"verify","ref":"gh#158-availability-owner-not-attendee>dep","finding":"on a namedAttendeeEmails bail, also forget the hard-block ledger entries for the instants this turn is about - or do not bail and scope the block instead","verdict":"needs-owner-decision","state":"open","invariant":"established-fact-needs-a-refuter","note":"DEPENDENCY ASK RAISED BY THE VERIFY, severity HIGH, NOT DISPATCHED because its parent verdict is waiting on the owner. availabilityPreCheck.ts is the only armer and the only forgetter, and runOutputGates.ts:881 keeps reading the ledger for 45 minutes regardless. This is the gate half of gh#158 - without it the built fix closes the prompt half and the output floor can still assert the same wrong-subject fact. Same lane, matchmaker."}
{"date":"2026-07-30","runId":"wf_27f03aca-0dd","lane":"matchmaker","source":"verify","ref":"gh#165-b>dep","finding":"suppress the update_meeting(add_attendees) steer when the conflicting event is an all-day out-of-office","verdict":"needs-owner-decision","state":"open","note":"DEPENDENCY ASK RAISED BY THE VERIFY, severity HIGH, NOT DISPATCHED - its parent gh#165-b is BUILT, so this one is purely additive polish on a shipped fix rather than a blocker. Carry overCommitment.allDayOutOfOffice through diagnostics.conflictingEvent; scheduleRules.ts:599 already computes it, so the data exists and only the wiring is missing. Without it, a colleague asking to book on a vacation day is steered at adding herself to the owner's out-of-office block - which pairs with the vacation-day-reported-as-already-booked discovery from the same pass."}
{"date":"2026-07-30","ref":"owner-shared-surface-set-never-read","source":"audit","verdict":"already-fixed","note":"backlog re-read: src/skills/meetings/ops/handlers/calendarReads.ts:148-193 (commit 1bc4fcb touched the file) no longer sets any _owner_shared_surface field at all - the withheld-shared-surface branch now returns { error: 'calendar_withheld_shared_surface', message: … } with no such key; grepped the whole src tree for _owner_shared_surface and got zero matches, so the dead field itself is gone, not merely unread. THIS IS THE ROW THE ENGINE HELD BACK LAST RUN for having no citable evidence - the guard worked, and the re-read came back with a file:line and a commit this time."}
{"date":"2026-07-30","runId":"wf_27f03aca-0dd","lane":"matchmaker","source":"both","ref":"gh#156-unprompted-inperson-online-question","finding":"Maelle asked the owner whether the meeting with Yael should be in-person or online, unprompted - he did not recall being asked this in a regular DM before","verdict":"declined","state":"open","note":"OWNER DECLINED 2026-07-30, his words: 'no need to fix'. Recorded rather than deleted so nobody re-raises it - it was surfaced as one of the four dated deferrals the previous run dropped, and it came back to his desk correctly; the answer is simply that it is not a defect. Nothing was built and nothing should be. The Manager's own recommendation was DROP for the same reason: severity low, clarity ambiguous, and it may be correct behaviour he had not seen before rather than a regression. Do not re-propose."}
{"date":"2026-07-30","runId":"wf_0cebe938-c81","lane":"gatekeeper","source":"owner","ref":"gh#156-b","finding":"Yael asked whether Sunday/Tuesday work for her too and Maelle answered 'Yep, both work for Yael too' with no tool call behind it, admitting the false confirm only after being pressed twice","rootCause":"src/utils/guards/runOutputGates.ts:344-346 - the colleague-readable leg only invokes the phantom-action check `if (ownerIsActing)`, so when the COLLEAGUE is the acting sender no honesty check runs at all; and claimChecker's 'action' mode only detects a false completed-ACTION claim, not a false calendar-AVAILABILITY-CONFIRMATION","verdict":"needs-owner-decision","state":"built","invariant":"claim-backing-must-match-the-claims-subject","note":"BUILT THEN OVERTURNED, and the overturn is fatal to the fix as written: the verify proved the new gate SKIPS THE EXACT BUG IT WAS BUILT FOR. runOutputGates.ts:925-933 - the deterministic pre-filter returns before any check whenever a calendar-read TOOL NAME appears in this turn's tape or anywhere in assistant history, and production logs show that is true on BOTH turns of the reported case. Two more findings on the same row: (a) REUSE BEFORE ADD with production proof rather than resemblance - 'action' mode ALREADY flagged this exact draft in production (maelle-2026-07-28.log:372, action_type 'other'), so extending that one prompt may be the whole fix and a third checkReplyClaims mode may be unnecessary; the new mode and the old must otherwise change together or a reader gets a hedge or an own-the-miss depending on which saw the draft first. (b) The EMAIL leg (runEmailLegGates) got no coverage at all, and that is the highest-harm surface for this class - a false 'both times work for her' forwarded verbatim to an external reader. His build constraint WAS honoured: no tool re-run anywhere in the new code, verified in the diff."}
{"date":"2026-07-30","runId":"wf_0cebe938-c81","lane":"registrar","source":"owner","ref":"gh#156-get-back-to-you-no-followthrough","finding":"Maelle said 'let me do that now… and get back to you' about checking Yael's calendar and then did not follow up - the owner had to ping her again","rootCause":"src/core/orchestrator/systemPrompt.ts:722-723 (RULE 3) already tells the model that any relay or follow-up promise MUST be backed by a real tool call the same turn, but nothing in code ever verified it happened, so the model drafted the promise, called zero tools and shipped it","verdict":"needs-owner-decision","state":"built","note":"ONE CHANGE, TWO ROWS - do NOT count this as a second fix; the code lives on the >dep row. The routing was vindicated the useful way: shepherd re-read the create_task path and runner.ts:118, established the requests spine is correct end-to-end WHEN something is actually raised, and correctly returned needs-dependency to gatekeeper rather than building an output gate outside its charter. The defect was never in the spine - nothing raised anything, and the only place that absence is catchable is the output gate. Shepherd then verified gatekeeper's work in place on disk rather than trusting the brief. That is the dependency chain working as designed, in 3 rounds with nothing owed."}
{"date":"2026-07-30","runId":"wf_0cebe938-c81","lane":"gatekeeper","source":"owner","ref":"gh#156-get-back-to-you-no-followthrough>dep","finding":"nothing in code verified that a promised follow-up was actually raised, so an unbacked promise shipped","rootCause":"src/utils/claimChecker.ts - no rule class covered a commitment to act LATER; the 'unrecorded_promise' class that would have caught it was removed by the v3.0.6 latency pass (claimChecker.ts:356-361)","verdict":"needs-owner-decision","state":"built","note":"BUILT THEN OVERTURNED ON LATENCY, AND THE DECISION IS GENUINELY HIS, not a lane's. Built: a fourth claimChecker mode 'unbacked_promise' plus a tool-less Sonnet rewriter, behind a deterministic pre-filter that only fires when ZERO tools ran this turn, wired into all three owner-scoped legs. The lane argued the pre-filter avoids the always-on cost that got the old rule deleted. THE VERIFY FOUND THAT ARGUMENT WEAKER THAN STATED: a zero-tool turn is one of the most common turn shapes there is, so this adds ~2s of Haiku to a large share of replies - and on the colleague-readable leg a zero-tool turn ALSO triggers the new calendar-confirmation Haiku, so a single chatty reply can carry up to THREE Haiku calls plus TWO possible Sonnet rewrites. That is a user-facing latency trade. Worth noting what the lane got right and stated itself: the remedy deliberately cannot fire create_task (G4 bars persisting durable data off an LLM verdict), so an output gate's ceiling here is making the CURRENT reply honest about the gap. If Maelle should be structurally incapable of promising a follow-up without a tool call, that belongs in the orchestrator's tool-loop, not a gate."}
{"date":"2026-07-30","runId":"wf_0cebe938-c81","lane":"matchmaker","source":"verify","ref":"attendee-calendar-never-read","finding":"Maelle tells the owner a colleague's calendar is clear when nobody ever looked at it - asking for slots with a named attendee applies that person's WORK HOURS but never reads their calendar, and the reply narrates it as checked-and-clear","rootCause":"NOT YET PINNED, deliberately: logs/maelle-2026-07-28.log:338-343 and :391-397 show two find_available_slots calls with attendee_emails ['yael.a@reflectiz.com'] and ignore_attendee_availability:false, both logging 'attendeeAvailability — auto-loaded' with WORK HOURS ONLY, the only 'Querying calendar' lines being userEmail idan@reflectiz.com, and a rejection breakdown carrying outside_attendee_work_hours with no attendee-busy reason anywhere - although scheduleRules.ts:148 declares an attendee_busy_collision kind, so the capability exists. Whether it is not fetched, silently skipped, or fetched-and-empty needs the lane that owns findAvailableSlots, and those files are being edited concurrently so the tree must be re-read first","verdict":"flagged-for-owner","state":"open","invariant":"claim-backing-must-match-the-claims-subject","note":"VERIFY DISCOVERY, severity HIGH, AND IT IS THE ACTUAL ROOT OF THE WHOLE #156 EPISODE - the false confirmation, the empty promise and both extra pings all descend from it. THE OWNER'S OWN ACCUSATION IN THE TICKET WAS CORRECT: 'i'm pretty sure you are lying and you didn't look on Yael calendar.' She did not. This is a discovery rather than a built fix only because it surfaced during the verify of two symptom-level fixes, and building it would have invalidated the pass that found it. RECOMMEND HE PROMOTE THIS - it is the one row here that would make the two symptom fixes unnecessary rather than merely correct, and gh#156's complaint 2 ('Why she lied? did she lied?') cannot be closed by any output gate, only by this."}
{"date":"2026-07-30","runId":"wf_0cebe938-c81","lane":"gatekeeper","source":"verify","ref":"read-recap-has-no-shield","finding":"a truthful recap of a calendar READ done last turn can be inverted into a false 'actually, I haven't checked that yet' - the reply retracts something Maelle really did do, and the owner then re-asks for work already finished","rootCause":"src/utils/guards/runOutputGates.ts:779-792 - the claim-checker's recap shield is `mutationCarried = toolSummariesText.includes('mutated=' + verdict.action_type)`, and ONLY MUTATIONS carry a mutated= marker (turnHelpers.ts mutationDomain). Read tools carry none, so a recap of get_calendar / find_available_slots / check_join_availability has nothing that can shield it, and rewriteOwningTheMiss's SAFE-MISS clause (claimChecker.ts:622) is the only thing between it and an inverted true statement","verdict":"flagged-for-owner","state":"open","invariant":"claim-backing-must-match-the-claims-subject","note":"VERIFY DISCOVERY, severity medium. LATENT, not observed firing - in the one #156 candidate the flag was correct, because the attendee's calendar genuinely had not been read. Pre-existing, not this wave's work. Next build's intake, carried in state.pendingOverflow. Note the shape: the shield was built for mutations and the guard it protects also judges reads, so a whole class of true statements is unprotected by construction."}
{"date":"2026-07-30","runId":"wf_0cebe938-c81","lane":"gatekeeper","source":"verify","ref":"three-copies-of-one-rewriter","finding":"three copies of one function - rewriteOwningTheMiss, rewriteCalendarConfirmationHedge and rewriteUnbackedPromise are the same Sonnet keep-vs-rewrite rewriter, differing only in prompt text and label, and they have ALREADY DRIFTED on arrival","rootCause":"src/utils/claimChecker.ts:678 (the original meta-text veto regex, which includes 'the action was performed') versus the two new rewriters appended at the end of the same file (~:693-790 and ~:800-900), both of which DROP that phrase - so the same leaked sentence is blocked on one path and shipped on another. Roughly 150 of this wave's 365 added lines in that file are the triplication","verdict":"flagged-for-owner","state":"open","invariant":"one-rewriter-spine-not-three","note":"VERIFY DISCOVERY, severity medium, and it is about THIS WAVE's own code rather than pre-existing ground - which is why it matters if he approves either overturned row as-is. Any future hardening (a new leak phrase, a schema change, a retry) has to be made in three places or the paths disagree, and they already do. If the overturned fixes go ahead, extracting one rewriter spine should go with them rather than after them."}
{"date":"2026-07-30","runId":"wf_0cebe938-c81","lane":"gatekeeper","source":"verify","ref":"prefilter-depends-on-history-provenance","finding":"the new pre-filter's behaviour depends on where conversation history came from, so the same thread can be gated differently after a restart - an identical turn is checked or skipped depending on provenance","rootCause":"src/utils/guards/runOutputGates.ts:929-933 scans ctx.history assistant content for `[<tool>` markers, which src/connectors/slack/postReply.ts:513-515 writes only on the DB path; rows re-fetched from the Slack API to fill a gap carry none","verdict":"flagged-for-owner","state":"open","invariant":"claim-backing-must-match-the-claims-subject","note":"VERIFY DISCOVERY, severity low, evidenced from production: maelle-2026-07-28.log at 12:10:14 shows 'Thread messages merged from Slack' with dbCount 6, mergedCount 10, missedCount 4 - so four of the ten rows in that very thread's history had no tool markers by construction. Same root family as the gh#156-b overturn: the pre-filter is keying on a proxy (a marker's presence) rather than the fact it cares about."}
{"date":"2026-07-30","runId":"wf_0cebe938-c81","lane":"gatekeeper","source":"verify","ref":"gh#156-b>dep","finding":"the pre-filter must key on WHOSE calendar was read, not on whether a calendar tool name appears","verdict":"needs-owner-decision","state":"open","invariant":"claim-backing-must-match-the-claims-subject","note":"DEPENDENCY ASK RAISED BY THE VERIFY, severity HIGH, NOT DISPATCHED because its parent is awaiting the owner. The evidenced failure is a REAL find_available_slots that queried only the owner's calendar while the reply asserted the attendee's was clear - and the tool tape already carries the attendee resolution (attendeeEmailsResolved, attendeeAvailabilityCount), so this is derivable DETERMINISTICALLY rather than by name presence. Second half of the ask, and it may be the cheaper whole answer: settle whether a third checkReplyClaims mode is needed at all, since 'action' mode caught this exact draft in production (maelle-2026-07-28.log:372) with action_type 'other'. Extending that one prompt may be the entire fix. His 2026-07-30 decline of any tool re-run still stands."}
{"date":"2026-07-30","runId":"wf_0cebe938-c81","lane":"gatekeeper","source":"verify","ref":"gh#156-get-back-to-you-no-followthrough>dep>dep","finding":"the owner-private call site sits below date-verify so a rewriter can run after it, and the header comment is wrong either way","verdict":"needs-owner-decision","state":"open","note":"DEPENDENCY ASK RAISED BY THE VERIFY, severity HIGH, NOT DISPATCHED. The real constraint, which makes this awkward rather than trivial: the new gate must stay BELOW claim-check (that is what lets it catch rewriteOwningTheMiss's OWN promise) while landing ABOVE humanGate and date-verify, and the concurrent probe currently makes that ordering hard. SECOND HALF, and it is the cheaper root for the evidenced case: rewriteOwningTheMiss's STEP-2 instruction ALREADY forbids the 'I'll take care of it' shape and produced it anyway (claimChecker.ts:617) - so tightening that one prompt may cover the evidenced instance entirely, leaving the new gate to carry only promises the ORCHESTRATOR invents. That would shrink the latency question the parent row is blocked on."}
{"date":"2026-07-30","runId":"wf_0cebe938-c81","lane":"registrar","source":"owner","ref":"gh#156-get-back-to-you-no-followthrough","finding":"Maelle said 'let me do that now… and get back to you' about checking Yael's calendar and then did not follow up - the owner had to ping her again","verdict":"declined","state":"open","note":"OWNER DECLINED 2026-07-30 ON LATENCY, his words: 'ok so don't fix it. i don't want more latency. maybe we will do a project about it in the future.' The code was BUILT and is on disk in src/utils/claimChecker.ts and src/utils/guards/runOutputGates.ts pending a revert. He is declining the REMEDY, not disputing the defect - the promise really was unbacked and systemPrompt.ts:722-723 really is unenforced. The measured trade he refused: a zero-tool turn is one of the commonest turn shapes, so the gate adds ~2s of Haiku to a large share of replies, and on the colleague leg it stacks with the calendar-confirmation gate for up to three Haiku calls plus two Sonnet rewrites on one chatty reply. This is the same rule class the v3.0.6 latency pass deleted, and he has now declined it twice on the same grounds. NOT re-proposable as an output gate. He named a future project as the route, which is the honest one: making Maelle structurally incapable of promising a follow-up without a tool call belongs in the orchestrator's tool-loop, not a post-hoc gate."}
{"date":"2026-07-30","runId":"wf_0cebe938-c81","lane":"gatekeeper","source":"owner","ref":"gh#156-get-back-to-you-no-followthrough>dep","finding":"the unbacked_promise claimChecker mode and its tool-less rewriter, built to catch a promise no tool call backed","verdict":"declined","state":"open","note":"OWNER DECLINED 2026-07-30 with the parent row - 'i don't want more latency'. This is the code half; the parent carries his reasoning. TO REVERT: src/utils/claimChecker.ts and src/utils/guards/runOutputGates.ts. NOTE THE ENTANGLEMENT - the same two files also carry gh#156-b's calendar_confirmation mode, which he has questioned but not ruled on, so a revert of one is a revert of both. One thing worth carrying forward rather than losing with the code: the ZERO-LATENCY half of the verify's dep ask was never evaluated on its merits, because rewriteOwningTheMiss's STEP-2 instruction ALREADY forbids the 'I'll take care of it' shape and produced it anyway (claimChecker.ts:617). Tightening that one existing prompt costs no new LLM call and may cover the evidenced instance. His decline was of the new gate's latency, not of that."}
{"date":"2026-07-30","runId":"wf_27f03aca-0dd","lane":"matchmaker","source":"both","ref":"gh#165-a","finding":"Ayala asked to book 15 minutes with herself, Idan, Scott and Chris and was not put on the invite - she had to ask to be added afterwards","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:141-160 - the owner-and-requester exclusion is unconditional for any colleague-initiated request","verdict":"declined","state":"open","invariant":"judgment-belongs-to-the-model-enforcement-to-code","note":"OWNER DECLINED THE CODE FIX 2026-07-30 and gave a different design, his words: 'i think it should be LLM rule and not our coding, if its not clear that she need to be in the meeting, put as requester and let her add herself. don't try to force it. maybe the solution is that for someone who keep asking to add him, we can update the person.md file for the fact that he is not clear and to assume he is in.' So: the CURRENT behaviour is correct when intent is unclear - exclude her, let her add herself - and a deterministic code rule inferring attendance from her phrasing is explicitly NOT wanted ('don't try to force it'). The durable idea he named is a PERSON-LEVEL LEARNED FACT: when someone repeatedly asks to be added, record in their person memory that they should be assumed to be in meetings they request. That is a new capability, not a bug fix - it is profiler (the person store / the fact) plus instructor (the injection), and it belongs on the feature engine, not this loop. gh#165 stays OPEN and stays PARTIAL; its complaint 2 is answered by a design ruling rather than a fix. Do NOT re-propose the unconditional-include code change."}
{"date":"2026-07-30","runId":"direct-a8bca67e","lane":"matchmaker","source":"owner","ref":"gh#158-availability-owner-not-attendee>dep","finding":"on a namedAttendeeEmails bail the hard-block ledger entries stayed armed, so an honest reply about a named colleague could be rewritten into a false 'that doesn't work' carrying the OWNER's earlier verdict, for up to 45 minutes","rootCause":"src/utils/availabilityPreCheck.ts:513 bails before any verdict is computed, but the ledger it may have written on an EARLIER turn stays armed for TTL_MS (src/utils/availabilityGate.ts:264), and its only reader (runAvailabilityFloorAndMaybeRewrite in runOutputGates.ts) has no attendee-awareness and fires on every reply","verdict":"built","state":"built","invariant":"established-fact-needs-a-refuter","note":"OPTION (a) COMPLETE - the owner approved it after ruling out (b) ('even when I asked about Levana, she is looking by MY calendar'). ONE FILE: src/utils/availabilityPreCheck.ts:513-537 now calls a new forgetNamedInstantsFromHardBlockLedger before returning empty. HIS CONSTRAINT WAS MET AND PROVED FROM THE CODE, not asserted: (1) forgetHardBlockedSlot (availabilityGate.ts:334-339) only filters an in-memory Map - no I/O, no Graph call; (2) the ledger's only reader produces a TEXT rewrite via rewriteBlockedSlotClaim (tool-less, forced-tool, returns a string) and never calls a mutation tool, so FEWER entries can only make the floor fire LESS, never more; (3) recordHardBlockedSlot, grantRelaxed and the policy_exception path are neither called nor imported by the change. Reuse-before-add honoured: the pre-existing regex-fallback extraction was factored into a shared extractRawPairs used by BOTH the fallback branch and the new forgetter, so they cannot drift. Typecheck green. runOutputGates.ts and claimChecker.ts deliberately untouched, per the tree hazard. RESIDUAL WORTH KNOWING: the forgetter uses the REGEX extractor, not the Haiku one, because the bail happens before the Haiku spend - so its coverage is the regex's language coverage. On a time expression the regex does not catch (Hebrew being the live case) the forget silently does not fire and the stale block survives. That fails open to the ORIGINAL bug rather than to a new harm, but it bounds the fix. Also recorded: the lane noted the floor now sits at runOutputGates.ts ~:1049 at HEAD, shifted from :881-923 by the other wave's pending uncommitted additions - earlier line citations in this report were against the pending tree."}
{"date":"2026-07-30","runId":"direct-a1c117bf","lane":"matchmaker","source":"verify","ref":"attendee-calendar-never-read","finding":"Maelle told the owner a colleague's calendar was clear when the busy filter had never populated - 'Yep, both work for Yael too… clean for both of you' asserted from stored work-hours alone","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts - candidateAttendeeBusyEmails was gated on isOwnerInitiatedSearch, and the turn ran in an MPIM group DM with Yael where the deliberate MPIM-authority clamp (findAvailableSlots.ts:323-335) makes context.senderRole 'colleague' even though Idan typed it himself, so the filter never populated and no Graph free/busy read happened on that branch","verdict":"already-fixed","state":"built","invariant":"claim-backing-must-match-the-claims-subject","note":"ALREADY FIXED AT HEAD by 4.3.2 (commit 1bc4fcb): findAvailableSlots.ts:648-650 now computes candidateAttendeeBusyEmails on `!ignoreAttendeeBusy && attendeeEmails.length > 0`, ROLE-INDEPENDENT, feeding busyFilterEmails into a real getFreeBusyForDecision - so attendee_busy_collision can fire on that branch now. Corroborated independently by the owner's own gh#156 close-out comment. THE DISCOVERY CONFLATED TWO BRANCHES and the lane separated them: :391-397 is the candidate_slots point-validation branch and is where the false confirm came from (fixed); :338-343 is the main spread-search branch, whose walker-level drop-filter IS still role-gated - but that is NOT 'calendar never read', because annotateSlotsWithAttendeeStatus (findAvailableSlots.ts:1417-1453, v2.7.0, unrelated to 4.3.2) runs unconditionally on that path and DOES perform a real getFreeBusyForDecision read for the attendee against the offered slots. That is the deliberate tag-don't-drop design for colleague searches (M6: never silently lose an option to an attendee conflict), not a defect. No files touched, typecheck clean."}
{"date":"2026-07-30","runId":"direct-a1c117bf","lane":"matchmaker","source":"verify","ref":"MANAGER-ERROR-missing-log-line-read-as-proof","finding":"the Manager relayed 'the only Querying calendar lines are idan@' as proof that the attendee's calendar was never read, and it proves nothing","rootCause":"src/connectors/graph/calendarReads.ts:577 - the 'Querying calendar' line is emitted ONLY by getCalendarEventsImpl (the calendarView/events fetch). The free/busy path getFreeBusyImpl (calendarReads.ts:936-963, the getSchedule POST) has NO log line at all on success, so its absence is not evidence of a skipped call","verdict":"already-fixed","state":"built","note":"BOOKKEEPING ROW, recorded so the inference is not repeated. This is the 'trace the runtime, not the design' lesson in a new costume: a SILENT SUCCESS is not a missing call. The claim originated in a verify discovery and the Manager carried it forward into a dispatch brief and into two owner-facing messages without checking which function emits the line. The correction cost one dispatch that returned already-fixed - cheap, and it also proved 4.3.2 covered the real half. Rule for future evidence: before treating a missing log line as an absent action, confirm the code path HAS a log line to miss."}
{"date":"2026-07-30","runId":"direct-a1c117bf","lane":"matchmaker","source":"verify","ref":"VERIFIEDCLEAN-ERROR-attendee-busy-collision-emitted-nowhere","finding":"a verifiedClean entry over-claimed that attendee_busy_collision is 'emitted nowhere', and the Manager passed that on as an established starting fact in a dispatch brief","rootCause":"the entry conflated two true things into one false one: checkSlot (src/utils/scheduleRules.ts:511) genuinely never constructs the kind, but it IS emitted at src/connectors/graph/findAvailableSlots.ts:1005, aggregated at :1334-1335, and consumed by src/skills/meetings/ops/violationLabels.ts:17 and src/skills/meetings/ops/handlers/moveMeeting.ts:731","verdict":"already-fixed","state":"built","note":"BOOKKEEPING ROW. The declared-but-dead reading was WRONG: attendee-busy is deliberately kept out of checkSlot and lives in the search walker, re-invoked wherever a book-time attendee check is needed - stated explicitly in createMeeting.ts:1071-1075 ('checkSlot above only covers OWNER rules; reuse the finder for the candidate window'), and reachable with NO role gate from move_meeting's attendee re-check (moveMeeting.ts:680-706) and createMeeting's efficiency counter-offer (createMeeting.ts:1080-1098). So it is declared-and-implemented-elsewhere-BY-DESIGN. No wire-or-delete action is owed, and the offending verifiedClean entry has been narrowed in state.json. THE GENERAL POINT, which is why this is a row and not a footnote: a WRONG proven-clean entry is worse than a missing one, because it is passed forward as settled and silences the check that would catch it. This one propagated a false premise into a dispatch brief within a day of being written."}
{"date":"2026-07-30","runId":"direct-a1c117bf","lane":"matchmaker","source":"verify","ref":"attendee-status-shipped-without-its-note","finding":"the colleague-path attendee_status annotation ships with no accompanying _note, while every sibling condition in the same payload gets one, and the tool schema documents the field only for the ignore_attendee_availability=true case","rootCause":"src/skills/meetings.ts:276 documents attendee_status only for the ignore-availability case; the default colleague-path annotation from annotateSlotsWithAttendeeStatus ships the same field bare, unlike _attendee_conflicts_note, _over_optional_note and the other siblings","verdict":"flagged-for-owner","state":"open","note":"FLAGGED BY THE LANE, NOT BUILT, and it was right not to build it: it is a real structural asymmetry, but there is no proof it caused the observed 'I haven't actually checked' line - that could equally reflect a genuine 'unknown' status on the turn. Severity low. Carried in state.pendingOverflow. Worth naming because a field the model receives with no note about what it means is exactly how a hedge or an over-claim gets invented downstream, and every sibling in that payload already has one."}
{"date":"2026-07-30","runId":"direct-aeaa5d8b","lane":"matchmaker","source":"owner","ref":"precheck-claims-booking-parity-it-does-not-have","finding":"the availability pre-check told the model unconditionally that its answer matches what happens at booking time, so Maelle could offer a slot in prose that booking then refused - she floated a 16:15 squeeze to Michal, pushed back when he accepted, and it was booked at 16:15 anyway","rootCause":"src/utils/availabilityPreCheck.ts, the closing paragraph of renderPromptBlock (found by grepping the claim text, not by trusting the ticket's stale line number). checkSlot IS the one shared validator and there is NO duplicate ladder - the divergence is two different MOMENTS and two different INPUTS feeding it: (1) within_lead_time / in_the_past are evaluated against Date.now() at each call (scheduleRules.ts:340-346, isWithinBookingLeadTime), and the pre-check runs on the inbound message while the real create_meeting/move_meeting checkSlot (planMeeting.ts:491-507) runs after the reply, an accept, or an owner-approval round-trip; (2) the pre-check's detectedCategory is a cheap Haiku guess from the raw message, while planMeeting.ts:320-350 classifies with a separate Sonnet pass against a real subject/body that does not exist yet","verdict":"built","state":"built","invariant":"one-fact-one-derivation","note":"OPEN SINCE 2026-07-27, closed on his 'do as you suggested'. gh#164 was folded in as EVIDENCE, not dispatched separately (M2) - and the lane produced the exact corroboration: 'too soon to book on his side today' is VERBATIM the within_lead_time label at src/skills/meetings/ops/violationLabels.ts:18. So gh#164 is fully explained by cause (1) with no code error on either side; real time simply moved between the two calls. THE MANAGER'S FRAMING WAS WRONG AND THE LANE CORRECTED IT: the brief offered 'make them one derivation' as the preferred shape on the assumption there were two rule ladders to merge. There are not. No code change can make 'now' at pre-check time equal 'now' at booking time, or hand the pre-check a subject line that does not exist yet - so shape (1) was unreachable by construction, not merely expensive. Built shape (2), make the claim honest: the injected paragraph now states accurate scope, names the lead-time clock and the category guess as the two things that can legitimately disagree with a later booking call, and adds behavioural instructions - do not tell a colleague a time is booked until the tool call confirms it, and if a delayed booking is refused where the block said BOOKABLE, say so plainly rather than insisting. THREE STRING/COMMENT EDITS, ZERO LOGIC CHANGE. Also corrected two comments that asserted the false parity as flat fact (the header doc and the v3.3.7 occupancy comment), per rule 11 - leaving them would have been a second false claim in the same file. Grepped for the old phrases: zero other source or test dependencies. Typecheck green."}
{"date":"2026-07-30","runId":"owner-revisit-2026-07-30","lane":"gatekeeper","source":"owner","ref":"gh#156-b","finding":"Yael asked whether Sunday/Tuesday work for her too and Maelle answered 'Yep, both work for Yael too' with no calendar read behind it, admitting the false confirm only after being pressed twice","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts - candidateAttendeeBusyEmails was gated on isOwnerInitiatedSearch, and the MPIM clamp made the owner's own message read as 'colleague', so the busy filter never populated and 'available: true' came from Yael's stored work hours alone. NOT the output gate - the claim was unbacked because the READ never happened","verdict":"already-fixed","state":"built","invariant":"claim-backing-must-match-the-claims-subject","note":"REVISIT CLOSED. The owner agreed a three-step plan on 2026-07-30 - revert the gate, fix the root, then revisit the gate 'when it's a much smaller question about a much rarer case' - and the root came back ALREADY-FIXED by 4.3.2 (commit 1bc4fcb, role-independent candidateAttendeeBusyEmails feeding a real getFreeBusyForDecision). That is DEPLOYED: production runs 4.3.2. So the reported symptom cannot recur from its actual cause, and the revisit's answer is that no gate is owed for it. The reverted gate would ALSO not have caught it - the verify proved its pre-filter returned early whenever a calendar-read tool NAME appeared in the tape, which was true on both evidenced turns. gh#156 complaint 2 is closable. TWO RESIDUALS RECORDED, NEITHER DISPATCHED, both from the verify that overturned the gate: (1) THE EMAIL LEG has no calendar-confirmation coverage at all, and it is the highest-harm surface for this class because a false 'both times work for her' is forwarded verbatim to an external reader - but there is ZERO evidence of it firing, so it is a hardening idea and not a bug row; (2) IF a gate is ever wanted here, the route is EXTENDING claimChecker's existing 'action' mode, which caught this very draft in production (maelle-2026-07-28.log:372, action_type 'other'), not adding a third mode - that was the verify's own reuse-before-add finding and it is the thing to remember if this ever comes back. Do not re-raise gh#156-b as an output-gate bug."}
{"date":"2026-07-30","runId":"direct-a2743f11","lane":"outrider","source":"verify","ref":"gh#166","finding":"raising NEWS_BRIEF_TIMEOUT_MS 8_000 to 20_000 also lengthened the ON-DEMAND brief, where a person is actually waiting - the wait behind the eye-emoji grew from 8s to 20s with no reply","rootCause":"src/tasks/briefs.ts:31 - NEWS_BRIEF_TIMEOUT_MS is a module constant that never consults the `force` arg, while sendMorningBriefing is called synchronously with force=true from src/connectors/slack/app/processMessage.ts:517 and src/tasks/skill.ts:598 when the owner asks for the brief in Slack","verdict":"needs-owner-decision","state":"built","note":"OVERTURNED BY THE VERIFY, and the catch is well-aimed: IN THIS SAME WAVE he declined a different fix with the words 'i don't want more latency', so shipping a 12s user-facing regression on the one path where somebody waits contradicts a ruling he made hours earlier. The 20s is correct for the 07:00 routine where nobody waits. THE LEVER IS IN SCOPE AND UNBUILT: gate the window on the `force` arg already available at briefs.ts:631. The Manager NAMED this exposure in the report when the fix landed and then did not treat it as blocking - the verify is right that it should have been. Not harm, a regression. Ask goes to outrider."}
{"date":"2026-07-30","runId":"direct-a2743f11","lane":"matchmaker","source":"verify","ref":"gh#158-availability-owner-not-attendee","finding":"the bail-and-forget fires on a much wider trigger than the symptom it was built for, so a deterministic pre-check silently stops covering cases nobody asked to lose","rootCause":"src/core/orchestrator/buildTurnContext.ts:318-334 unions input.extractedAttendeeEmails into resolvedMeetingAttendees - addresses the EMAIL transport pulls off forwarded headers - so `namedAttendeeEmails` non-empty means 'who is on the thread', not 'whose availability was asked about'","verdict":"needs-owner-decision","state":"built","invariant":"established-fact-needs-a-refuter","note":"OVERTURNED. HIS CONDITION HOLDS AND WAS RE-DERIVED, not relayed: availabilityGate.ts:266 is a module-local Map, forgetHardBlockedSlot (:334-339) filters that array and nothing else - no I/O, no LLM; the only acting reader (runOutputGates.ts:881-949) returns the draft untouched on an empty ledger (:892) and its sole effect on a fire is rewriteBlockedSlotClaim's TEXT; recordHardBlockedSlot is called only at availabilityPreCheck.ts:943 inside the loop the bail returns before reaching; grantRelaxed and policy_exception appear nowhere in the file. FEWER ENTRIES STRICTLY MEANS THE FLOOR REWRITES LESS. Clearing cannot become permission to book. PROVEN. THE OVERTURN IS ABOUT SCOPE, and three concrete losses: (1) on a colleague-role EMAIL turn any recipient other than the owner bails the pre-check entirely; (2) a plain group booking loses it - 'is 10am tomorrow with Idan?' arms NOT BOOKABLE, then 'can Yael join at 10am as well?' bails AND the forgetter DELETES the 10:00 block the previous turn's own checkSlot established about the owner, so the natural reply becomes 'yes, 10am works' - that is gh#165's shape two messages deep; (3) buildTurnContext.ts:659 stashes confirmed-bookable slots only when result.ran, so a bailed turn also loses the is-he-free-then-hold-it path. No booking risk - a CLAIM risk about the owner's own calendar, which is exactly what the floor exists for. ASK: narrow the bail to the contributor that carries the premise (classifyTurn's named people), or better bail on ASKS ABOUT A THIRD PARTY'S AVAILABILITY rather than NAMES ONE; and consider scoping the forget rather than deleting."}
{"date":"2026-07-30","runId":"direct-a2743f11","lane":"matchmaker","source":"verify","ref":"precheck-claims-booking-parity-it-does-not-have","finding":"the honest-caveat half is right, but one added sentence is a third copy of an existing rule and the fix does not reach the ticket it was built on","rootCause":"src/utils/availabilityPreCheck.ts renderPromptBlock's new closing paragraph duplicates a rule that already exists at src/core/orchestrator/systemPrompt.ts:707-708 (with 'Booked it.' as the named wrong example) and at src/skills/meetings.ts:1375 - and systemPrompt.ts:714 explicitly states that booking-specific honesty rules live in the meetings skill section","verdict":"needs-owner-decision","state":"built","invariant":"one-fact-one-derivation","note":"OVERTURNED on three counts. (1) The caveat half is CONFIRMED right and its premise verified independently: checkSlot really is the one validator (scheduleRules.ts:526+ is the only ladder; findAvailableSlots.ts:1101 and createMeeting's runSlotCheck both route through it), and the old unconditional parity claim was a promise the code does not keep. (2) THIRD COPY of the never-say-booked rule, in a utils/ file that whoever edits prompt honesty rules will never open - one invariant, three places, and the budget cost is ~+900 characters on every colleague turn the pre-check fires, roughly a third of it duplicated. (3) THE WAVE'S OWN EVIDENCE TICKET IS NOT REACHED: gh#164 is 'the offer was ok, the second pushback was not' - she offered 16:15, he accepted, she refused in prose, and it was booked at 16:15 anyway. Fix 7 changes no verdict, and its new instruction covers the case AFTER a refused tool call, but there was no booking attempt at the moment she pushed back. So the sequence is not prevented, and the sentence arguably SANCTIONS the pushback he objected to rather than steering toward the escalation she eventually made. ROUTING, as asked: the caveat half is matchmaker's (it describes matchmaker's own code); the two behavioural instructions are INSTRUCTOR's, and one of them should not exist at all. THE MANAGER OVERSTATED THIS: 'gh#164 is now fully explained' was true of the CAUSE and got reported as if the fix handled it. It does not."}
{"date":"2026-07-30","runId":"direct-a2743f11","lane":"matchmaker","source":"verify","ref":"hardblock-ledger-fifth-invalidation-class","finding":"the hard-block ledger gains a FIFTH invalidation class and it is the first one not grounded in calendar evidence - a regex reading of message text can now disarm a verdict checkSlot established","rootCause":"src/utils/availabilityPreCheck.ts:513-537 (the new forgetter) versus src/utils/availabilityGate.ts:240-262, which enumerates invalidation rules 1-4 and then asserts 'What is left is genuinely narrow and one-directional'","verdict":"needs-owner-decision","state":"built","invariant":"established-fact-needs-a-refuter","note":"THE SHAPE QUESTION, raised by the verify as the one thing to rule on, and it is the honest framing of the gh#158 overturn rather than a separate defect. Small in code; it changes what the ledger MEANS - from 'an entry stands until better knowledge replaces it' to 'or until a message mentions its clock alongside a colleague's name'. HE ASKED FOR THE FORGET, so it is probably intended - but he asked for it as 'do not let the floor assert an owner fact about a third party', and what shipped is broader. Named because new authority over a guard spine is the class that is hard to reverse once other code starts assuming it. Nothing else was added anywhere in the wave: no new skill, tool, table, connection, background job or file."}
{"date":"2026-07-30","runId":"direct-a2743f11","lane":"matchmaker","source":"verify","ref":"calendarreads-comment-describes-a-mode-that-never-shipped","finding":"the entire change to calendarReads.ts is 9 lines of comment describing a subject:null mode that exists in NO committed revision - added and removed inside the same uncommitted session, so a future reader of findDuplicateEvent is told to look for a mode that never shipped","rootCause":"src/connectors/graph/calendarReads.ts - the whole diff for that file is the comment; the design warning inside it is real, the framing is archaeology","verdict":"needs-dependency","state":"built","note":"LANE ASK from the verify, explicitly NOT an overturn and NOT blocking the wrap - the code is clean and no owner judgement is involved. Keep one sentence of the constraint (do not reintroduce a subject-optional mode), drop the history. Goes to matchmaker with the other asks. Also recorded from the same review, low and hygiene: gh#165-b delivers the event id TWICE - as existing_event_id (what update_meeting needs) and interpolated into the human sentence as '(id: …)'. The in-prose copy is the one a model can echo to a colleague as a raw Graph id, and no prompt rule forbids it."}
{"date":"2026-07-30","runId":"direct-a2743f11","lane":"matchmaker","source":"verify","ref":"availabilitygate-doc-undercounts-its-own-callers","finding":"availabilityGate.ts still says forgetHardBlockedSlot has 'Two callers, both cases of this entry is no longer the best knowledge' - there are now three, and the new one is not a better-knowledge case","rootCause":"src/utils/availabilityGate.ts:328-333 (the two-callers claim) and :240-262 (the 'genuinely narrow and one-directional' assertion). Actual call sites: availabilityPreCheck.ts:938, :1160/:1166, and runOutputGates.ts:943","verdict":"needs-dependency","state":"built","note":"DOC DEFECT THIS WAVE INTRODUCED, in the very file the wave's safety argument rests on - which is why it is worth a row rather than a footnote: the next person bounding false-refusal risk from that paragraph will under-count. Doc-only, matchmaker. AND IT CORRECTS A MANAGER-WRITTEN ROW: the pendingOverflow entry hardblock-ledger-has-no-refuter claims 'exactly one writer and one forgetter, both inside availabilityPreCheck.ts (grep-confirmed unique in src)' - runOutputGates.ts:943 has ALWAYS been a third call site. The conclusion of that row is unchanged; its evidence was wrong, and it came from a verify pass, was copied into state.json by the Manager, and stood for a day."}
{"date":"2026-07-30","runId":"direct-a2743f11","lane":"matchmaker","source":"verify","ref":"MANAGER-ERROR-byte-identical-claim-was-false","finding":"the report claimed 'No-location meetings render byte-identically' for the category-classifier fix, and that is false","rootCause":"src/skills/meetings/detectCategory.ts:107 renders an EMPTY LINE when locationHint is absent, so every no-location classification gains one blank line in the prompt","verdict":"already-fixed","state":"built","note":"BOOKKEEPING ROW. Harmless in effect - it matches the existing input.body line's pattern and a blank line is nothing to a Haiku classifier - but the CLAIM was wrong and the verify caught it before it reached verifiedClean, which is exactly where a wrong proven-clean does its damage. Second time in one day that a Manager-relayed 'byte-identical' or 'emitted nowhere' style absolute turned out to be an over-claim. The rule to carry: a byte-identical claim needs the template read, not the intent inferred."}
{"date":"2026-07-30","runId":"direct-a2743f11","lane":"matchmaker","source":"verify","ref":"gh#168","finding":"timezone issues when booking a meeting - Maelle asserted 'no genuine overlap Monday' for an Eastern-time attendee, then self-corrected to '16:00-18:15 your time is overlap, but busy'","verdict":"flagged-for-owner","state":"open","note":"NEW OPEN TICKET, NOT TOUCHED BY THIS WAVE, and named by the verify specifically so nobody reads the wave's timezone-adjacent edits as coverage. Next run's intake. Its transcript also CORROBORATES the struck-through attendee-calendar-never-read correction: 'Tyler's got other things booked 9:00 and 10:45 ET' is an attendee-calendar read surfacing in prose, which is the capability the discovery claimed was missing."}
{"date":"2026-07-30","runId":"wrap-4.3.4","lane":"outrider","source":"github","ref":"gh#167","verdict":"built","state":"wrapped","finding":"the brief never said the news pass had failed, so a broken gather and a quiet news day were indistinguishable","note":"WRAPPED in 4.3.4, commit 1754444. VERIFIED CLEAN by the wave verify. GitHub issue CLOSED with the sha - the only one of the wave that met all three conditions (verdict built, verify satisfied, commit exists, owner said wrap)."}
{"date":"2026-07-30","runId":"wrap-4.3.4","lane":"outrider","source":"github","ref":"gh#166","verdict":"needs-owner-decision","state":"wrapped","finding":"the morning brief dropped its whole news section because the gather lost an 8s race it was allowed 12s to run","note":"WRAPPED in 4.3.4, commit 1754444, BUT SHIPPED WITHOUT A VERIFY SIGN-OFF and the GitHub issue is deliberately LEFT OPEN. The reported defect is fixed; the fix also lengthened the ON-DEMAND brief from 8s to 20s because NEWS_BRIEF_TIMEOUT_MS never consults the `force` arg, and processMessage.ts:517 / skill.ts:598 call sendMorningBriefing synchronously when the owner asks for the brief in Slack. Closing it would bury that regression. ONE FORCE GATE FROM CLOSING - the arg is already in scope at briefs.ts:631. Next run's first item."}
{"date":"2026-07-30","runId":"wrap-4.3.4","lane":"matchmaker","source":"both","ref":"gh#165-b","verdict":"built","state":"wrapped","finding":"asked to be added to a meeting just booked for her, Maelle re-ran create_meeting under an invented subject and collided with her own booking","note":"WRAPPED in 4.3.4, commit 1754444. VERIFIED CLEAN. gh#165 stays OPEN - 165-a was declined as a code fix in favour of a person-memory design, and the verify also flagged that meetings.ts:482/495 still tells the model a colleague may only add or remove themselves, which contradicts the steer this fix installs."}
{"date":"2026-07-30","runId":"wrap-4.3.4","lane":"matchmaker","source":"logs","ref":"category-classifier-blind-to-location","verdict":"built","state":"wrapped","finding":"an onsite request with a stated venue was classified as Meeting rather than Physical, overriding the classifier's own correct suggestion","note":"WRAPPED in 4.3.4, commit 1754444. VERIFIED CLEAN, with one correction to the Manager's own claim: no-location meetings do NOT render byte-identically - detectCategory.ts:107 emits a blank line when locationHint is absent. Harmless and consistent with the existing input.body pattern, but the absolute was wrong."}
{"date":"2026-07-30","runId":"wrap-4.3.4","lane":"matchmaker","source":"github","ref":"152-refresh-stale-cache","verdict":"built","state":"wrapped","finding":"the owner asked Maelle to check again for a slot and did not get a fresh read","note":"WRAPPED in 4.3.4, commit 1754444. VERIFIED CLEAN, and it composes correctly with the gh#158 bail - eventsForWeek is reachable only after the bail returns, so a bailed turn makes zero calendar reads and a blind pre-check still asserts nothing. gh#152 stays OPEN: complaint 2 (dense options) is untouched."}
{"date":"2026-07-30","runId":"wrap-4.3.4","lane":"matchmaker","source":"both","ref":"gh#158-availability-owner-not-attendee","verdict":"needs-owner-decision","state":"wrapped","finding":"an availability question about a named colleague was answered with the owner's own hours, and the hard-block ledger kept asserting it for 45 minutes","note":"WRAPPED in 4.3.4, commit 1754444, SHIPPED WITHOUT A VERIFY SIGN-OFF. The owner's no-booking condition HOLDS and was re-derived from code by the verify, not relayed. The gap is SCOPE: the bail keys on 'a colleague was named' rather than 'a colleague's availability was asked about', and resolvedMeetingAttendees inherits the email transport's forwarded-header addresses. Two named losses: a colleague-role email turn with any non-owner recipient bails the pre-check entirely, and a two-message group booking ('10am with Idan?' then 'can Yael join at 10am?') both bails AND deletes the 10:00 block the earlier turn's own checkSlot established. NEXT RUN: narrow the trigger to asks-about-a-third-party, and scope the forget rather than deleting. gh#158 stays open."}
{"date":"2026-07-30","runId":"wrap-4.3.4","lane":"matchmaker","source":"owner","ref":"precheck-claims-booking-parity-it-does-not-have","verdict":"needs-owner-decision","state":"wrapped","finding":"the pre-check told the model unconditionally that its answer matches what happens at booking time, which the code does not keep","note":"WRAPPED in 4.3.4, commit 1754444, SHIPPED WITHOUT A VERIFY SIGN-OFF. The caveat half is CONFIRMED right and its premise verified independently - checkSlot really is the one validator, and lead-time is clock-relative while the category is a guess made before any subject exists. Three gaps: one added sentence is a THIRD copy of a rule already at systemPrompt.ts:707-708 and meetings.ts:1375, living in a utils/ file nobody editing honesty rules will open, at ~+900 characters on every colleague turn; the two behavioural instructions belong to INSTRUCTOR not matchmaker; and it does not reach gh#164, whose complaint is the pushback AFTER the offer, at a moment when no booking had been attempted. gh#164 stays open and the verify said explicitly not to close it."}
{"date":"2026-07-30","runId":"wrap-4.3.4","lane":"gatekeeper","source":"owner","ref":"gh#156-get-back-to-you-no-followthrough>dep","verdict":"declined","state":"wrapped","finding":"the unbacked_promise gate, built then declined on latency","note":"REVERTED before the 4.3.4 wrap, so nothing of it shipped. Recorded as wrapped so the row closes rather than sitting open forever against work that no longer exists. The 531-line diff (this plus the calendar-confirmation gate) was saved as a patch outside the repo; if either is ever revisited, the design and the reason for refusal are in the two earlier rows. gh#156 stays open."}
{"date":"2026-07-31","runId":"wf_f0cfd84d-5f5","lane":"registrar","source":"both","ref":"B165-c","finding":"after the owner approved a second conflicting booking for Ayala, Maelle told her 'You're all set, added you to the ask I've sent Idan' for a request that was still awaiting_owner, not done","rootCause":"src/tasks/skill.ts:300 - the create_approval tool description's guidance for relaying a colleague-originated PENDING approval gave an example phrasing and no prohibition on completion-sounding language, so the model paraphrased it into a completion claim","verdict":"built","state":"built","note":"Reworded the create_approval description to forbid completion-style openers ('you're all set', 'done', 'sorted', 'added') while a request is pending, replaced the ambiguous example template, and named the exact confusing sentence from the ticket as a negative example. Pure prompt/tone text in a file shepherd owns; no logic, schema or state change. VERIFY CONFIRMED and added two facts worth keeping: the failing turn created a FRESH approval, not a dedup reuse (logs/maelle-2026-07-29.log:764/:767 carry no 'reusing OPEN idempotency match' line), so prompt wording is the correct lever and not the dedup path; and NO output gate is owed - claimChecker.ts:247 covers the attendee-completion family, and this sentence is literally TRUE but ambiguous, so it is correctly outside that gate's remit. A future pass should not re-open this as a missing guard. Also proven: no second spelling of the pending-approval relay rule exists in src, so this edit creates no drift pair."}
{"date":"2026-07-31","runId":"wf_f0cfd84d-5f5","lane":"profiler","source":"both","ref":"B157-c","finding":"asked 'what do you know about Yael?' Maelle answered 'Not much on file yet… no history of meetings or notes with her' - a specific false negative about a colleague whose store row actually holds 44 notes and dozens of interactions","rootCause":"an owner-in-MPIM turn (isOwnerInGroup, effectiveRole clamped to colleague) has ZERO person data and ZERO tools: src/core/orchestrator/systemPrompt.ts:137-139 nulls peopleSection for any non-owner-effective turn, and src/skills/registry.ts:368-450 excludes get_person_memory/recall_interactions, hard-blocked again at src/core/assistant.ts:430. Confirmed via db-query that the history is REAL - logs/maelle-2026-07-28.log:540 shows actionCount:0","verdict":"needs-owner-decision","state":"open","invariant":"a-restriction-is-not-an-absence","note":"NOT BUILT, and correctly so - the lane established the fact and refused to guess the policy. IT IS THE SAME INVARIANT THE OWNER ALREADY APPROVED on this ticket's sibling: matchmaker's calendarReads.ts fix, 'events: [] means withheld, never confirmed empty'. The narrow fix is a colleague-context prompt rule (instructor, systemPrompt.ts ~420-435): with no accessible person data and no tool to check, she must say she cannot check or share from here and never assert 'no history' / 'first interaction'. THE OPEN QUESTION IS HIS AND ONLY HIS: whether to ALSO open a scoped read so she can say 'I do have history with her but don't share notes in a group thread' - that widens what person data is exposed on a colleague-clamped turn, which SESSION_STARTER flags as do-not-loosen without an owner call. This row was deferred to 2026-07-30 by a prior profiler dispatch that asked to first establish whether the history exists. It does. Only the disclosure policy remains."}
{"date":"2026-07-31","runId":"wf_f0cfd84d-5f5","lane":"outrider","source":"github","ref":"G166-a","finding":"the 8s-to-20s news timeout fix also lengthened the ON-DEMAND brief, where somebody is watching the eye emoji","rootCause":"src/tasks/briefs.ts:38 - NEWS_BRIEF_TIMEOUT_MS was a single flat constant used unconditionally in the gather race, applying the scheduled-path budget to the on-demand path too (force=true from processMessage.ts:517 and skill.ts:598, both directly behind the read receipt)","verdict":"needs-owner-decision","state":"built","note":"BUILT, THEN OVERTURNED - and the overturn is not about the code. Added NEWS_BRIEF_TIMEOUT_MS_ON_DEMAND = 8_000 beside the 20_000 scheduled constant, race computes `force ? onDemand : scheduled`. +9/-2, no new state, no dead code. THE VERIFY'S POINT: the MECHANISM is what the owner authorised ('one force gate from closing'); the VALUE 8_000 is a decision he has not made, and 8s is the exact number gh#166 proves is broken - it sits BELOW the inner gather budget (un-timed planNewsGoals + 12s per goal), which IS the budget inversion the ticket names as its root. So on-demand news returns to a coin flip: the ticket's own margins read 5.00 / 5.94 / 7.94 / 8.32s and trending up, two of four at or over the wall. WHEN IT LOSES the Updates section vanishes with NO user-facing word - briefs.ts:717 is a log WARN only and the compose prompt at :510 forbids an apology line - which is verbatim the conversation that produced gh#166. WHAT IT BUYS is ~12s off a brief measured at 40-70s, of which the compose pass alone is 23-43s, so the person waits ~50s either way. 13-14s would keep the news AND return most of the wait. ONE SENTENCE SETTLES IT. SECONDARY, not blocking: `force` now means two things - 'skip the daily dedupe' (its original job at :652) and 'someone is waiting live'. Both current force=true sites are interactive so it is right today, but a future non-interactive force caller silently inherits the tight budget."}
{"date":"2026-07-31","runId":"wf_f0cfd84d-5f5","lane":"matchmaker","source":"both","ref":"B168-a","finding":"booking with Tyler (Eastern), Maelle asserted 'no genuine overlap Monday', then self-corrected mid-thread to '16:00-18:15 your time is overlap, but busy' after the owner pushed back twice - 'why monday falls outside of Tyler hours?' and 'your timezone numbers doesn't make any sense'","verdict":"needs-shaping","state":"open","note":"FLAGGED, NOT DISPATCHED. The final Tuesday recommendation was correct throughout - it is the REASONING shown to the owner that was wrong twice before landing. The shaping question is whether this is a deterministic defect in how the cross-timezone free window is computed or rendered, or the model narrating its own reasoning in prose and self-correcting with no code defect underneath. The lane found NO code citation: scheduleRules.ts's cross-timezone window logic is not obviously wrong and the Tuesday answer was right. gh#168 is the ticket, filed by a verify specifically so no wave's timezone-adjacent work is mistaken for coverage. Severity medium."}
{"date":"2026-07-31","runId":"wf_f0cfd84d-5f5","lane":"matchmaker","source":"github","ref":"G164-a","finding":"Maelle offered Michal a 16:15 squeeze in prose, refused it when he accepted - '16:15 is too soon to book on his side today' - and it was booked at that same 16:15 anyway after being flagged to the owner","verdict":"needs-shaping","state":"open","note":"FLAGGED, NOT DISPATCHED, and this is the SECOND time this ticket has come back. The shaping question is whether it is the same root as the parity row already built in 4.3.4 - and the answer the verify already gave is that THE BUILT CAVEAT-FIX DOES NOT REACH THIS SEQUENCE, because the refusal happens before any booking attempt is made and the new instruction covers the case AFTER a refused tool call. So either the parity row's residual gap gets ruled on, or this needs its own root. Nothing is owed to a lane until that call. Severity low."}
{"date":"2026-07-31","runId":"wf_f0cfd84d-5f5","lane":"matchmaker","source":"github","ref":"G152-b","finding":"booking with Michal after the owner's Weekly-with-Yael conflict was surfaced (10:00-10:40), Maelle offered 11:00 / 12:30 / 15:00 as the clean 25-minute slots but not 10:45 - the slot immediately adjacent to the just-revealed conflict, which the owner pointed out was clean and dense","verdict":"needs-shaping","state":"open","note":"FLAGGED, NOT DISPATCHED. The shaping question is whether the search deliberately excludes the instant immediately after a just-discovered conflict for some other reason (a buffer or step-size rule) or whether this is a plain gap in candidate generation around a newly-revealed busy block. The lane looked for a slot-step-size constant in findAvailableSlots.ts and did not find one to cite, so the root is unproven. This is gh#152's LAST remaining complaint - its other one (the stale cache) shipped in 4.3.4. Severity low."}
{"date":"2026-07-31","runId":"wf_f0cfd84d-5f5","lane":"outrider","source":"verify","ref":"brief-omits-news-with-no-user-facing-word","finding":"the morning brief arrives with no Updates section and nobody is told why - the owner has to notice and ask, so the exact conversation that produced gh#166 will happen again","rootCause":"src/tasks/briefs.ts:717 is a WARN-only log (the whole of gh#167's 'say it failed' work) versus :471 hasNews, :501 the Updates instruction block that silently disappears, and :510 which explicitly instructs the composer NOT to add an apology or a couldn't-find-anything line","verdict":"flagged-for-owner","state":"open","note":"VERIFY DISCOVERY, severity medium, next build's intake, carried in state.pendingOverflow. Pre-existing at HEAD - gh#167 shipped LOG-ONLY, which is exactly what it was scoped to do - but this wave's on-demand 8s budget makes it materially more likely to fire. Worth reading beside G166-a: the reason 8s is a coin flip matters precisely because losing the flip is silent to the person reading the brief. Fixing either one reduces the other's harm."}
{"date":"2026-07-31","runId":"wf_f0cfd84d-5f5","lane":"registrar","source":"verify","ref":"send-briefing-now-desc-promises-standalone-dm","finding":"the model is told send_briefing_now posts 'a new standalone DM - not as a reply in this thread', and then it posts in-thread","rootCause":"src/tasks/skill.ts:290 (the description) versus src/tasks/skill.ts:598 which passes context.threadTs, versus src/tasks/briefs.ts:842 which honours threadTs on the post","verdict":"flagged-for-owner","state":"open","invariant":"tool-description-must-match-handler-behaviour","note":"VERIFY DISCOVERY, severity low, next build's intake, carried in state.pendingOverflow. Pre-existing; found while checking the seam between this wave's two files. A tool description that promises placement it cannot deliver is how the model's choice BETWEEN tools goes wrong - it may pick this tool precisely to get a standalone DM. Same invariant family as update-meeting-desc-claims-self-only, which is also open."}
{"date":"2026-07-31","runId":"wf_f0cfd84d-5f5","lane":"instructor","source":"verify","ref":"B157-c>dep","finding":"add a colleague-context rule to systemPrompt.ts's isOwnerInGroup block so an inaccessible person store produces an honest non-disclosure rather than a fabricated negative","verdict":"needs-owner-decision","state":"open","invariant":"a-restriction-is-not-an-absence","note":"DEPENDENCY ASK, severity HIGH, NOT DISPATCHED because its parent is awaiting the owner. systemPrompt.ts ~420-435: with no accessible person data and no tool to check a named person's history on a colleague-clamped turn, she must give an honest non-disclosure and never assert a specific negative she cannot verify - mirroring the a-restriction-is-not-an-absence fix already shipped for the calendar-absence sibling on this same ticket. Instructor's file, so it cannot be built by profiler. It is one prompt rule and it is ready the moment he rules."}
{"date":"2026-07-31","runId":"wf_f0cfd84d-5f5","lane":"outrider","source":"verify","ref":"G166-a>dep","finding":"if the owner rules for a higher on-demand budget, change only the NEWS_BRIEF_TIMEOUT_MS_ON_DEMAND value so it clears news.ts's 12s per-goal budget","verdict":"needs-owner-decision","state":"open","note":"DEPENDENCY ASK, severity HIGH, NOT DISPATCHED. It is a ONE-CONSTANT edit in src/tasks/briefs.ts and no other change is owed - the verify said so explicitly. This is the cheapest open item in the whole report: his sentence and a number."}
{"date":"2026-07-31","ref":"locationtz-ambiguous-cst-mst","source":"audit","verdict":"already-fixed","note":"backlog re-read: src/utils/locationTz.ts:51 and :64 now read 'Deliberately NO cst/cdt entry (added 2026-07-28, removed 2026-07-29 …)' and the equivalent for mst - the ambiguous static mapping this row warned about was removed the day after it was added, forcing the Sonnet fallback instead of a confident-but-wrong static answer. The in-code dated comment is direct evidence of the revert."}
{"date":"2026-07-31","ref":"forwarding-note-misclassification-owner-tier","source":"audit","verdict":"already-fixed","note":"backlog re-read: src/connectors/email/inbound.ts:266-279 now computes setBy from a deterministic check (provablyOwnersOwnText = statedZoneAppearsInUniqueBody) rather than from hint.source, a model judgement, as the row described. Commit b7a4a8b (4.3.1) is the last to touch this file and matches the shape of the fix."}
{"date":"2026-07-31","ref":"prefilter-depends-on-history-provenance","source":"audit","verdict":"already-fixed","note":"backlog re-read: the cited pre-filter (runOutputGates.ts:929-933 scanning for [<tool> markers) belonged to the reverted gh#156-b / unbacked_promise gate. Grep for unbacked_promise / calendar_confirmation in claimChecker.ts returns ZERO matches - the gate is gone, so the provenance-dependent behaviour this row described no longer exists. Closed by the 2026-07-30 revert, not by a fix."}
{"date":"2026-07-31","ref":"gh#52","recheck":"still-real: src/db/client.ts:153-163 (audit_log) plus :227-265 (owner_user_id ALTER + backfill) show the TENANCY half is already fixed - owner_user_id exists and is backfilled. But the UNDO-SUPPORT half the ticket is actually about remains true: auditLog() at client.ts:900-925 returns void (no row id) and there is no UPDATE path for audit_log at all, only INSERT and a read helper."}
{"date":"2026-07-31","ref":"recurring-category-subject-unmasked","recheck":"still-real: createMeeting.ts:428, :434, :436 read verbatim - existing_subject: match.subject twice, and the same raw value interpolated into the colleague-facing message at :436, with no displaySubject/subjectViewerFor masking anywhere in that block."}
{"date":"2026-07-31","ref":"gh#158-availability-owner-not-attendee","recheck":"still-real: buildTurnContext.ts:318-334 read verbatim - extractedExternalAttendees (from input.extractedAttendeeEmails) is still unioned straight into resolvedMeetingAttendees with no distinction between 'named on the thread' and 'asked about'. The 4.3.4 bail-and-forget shipped; this over-wide trigger is the residual the verify named."}
{"date":"2026-07-31","ref":"vacation-day-reported-as-already-booked","recheck":"still-real: createMeeting.ts:565 still derives the label via humanizeViolationLabel(brokenRule, ownerFirst) fed from diagnostics.rejectedCounts keys. violationLabels.ts:28-29 does have distinct cases for owner_out_of_office vs owner_busy_collision, so the honest label exists - this call site still discards it."}
{"date":"2026-07-31","ref":"hardblock-ledger-has-no-refuter","recheck":"still-real: runOutputGates.ts:905-944 still reads the ledger on EVERY turn via freshHardBlockedSlots/detectAffirmedBlockedSlots against the 45-min TTL from availabilityGate.ts:264, and only forgets entries it itself rewrites. The gh#158 dependency shipped a second forgetter at the bail; the asymmetry this row names is unchanged."}
{"date":"2026-07-31","ref":"gh#165-b>dep","recheck":"still-real: connectors/graph/findAvailableSlots.ts:211 conflictingEvent?: { id: string; subject: string } still has no allDayOutOfOffice field, and createMeeting.ts:622 steers update_meeting(add_attendees) unconditionally whenever brokenRule === owner_busy_collision. Pairs with vacation-day-reported-as-already-booked."}
{"date":"2026-07-31","ref":"attendee-status-shipped-without-its-note","recheck":"still-real: meetings.ts:260-277 confirms attendee_status is documented ONLY inside the ignore_attendee_availability description at :276; no sibling _attendee_status_note field or documentation exists near it, unlike _attendee_conflicts_note and _over_optional_note in the same payload."}
{"date":"2026-07-31","ref":"hardblock-ledger-fifth-invalidation-class","recheck":"still-real: availabilityGate.ts:228-263 (the doc comment) still enumerates exactly FOUR invalidation rules and asserts 'what is left is genuinely narrow and one-directional', with no mention of a regex-text-based forgetter - while availabilityPreCheck.ts:938 is that forgetter, shipped in 4.3.4."}
{"date":"2026-07-31","ref":"calendarreads-comment-describes-a-mode-that-never-shipped","recheck":"still-real: connectors/graph/calendarReads.ts:481 still reads '(v4.3.x, #165b - a subject: null …' - the comment describing a mode that exists in no committed revision was wrapped into 4.3.4 unchanged."}
{"date":"2026-07-31","ref":"availabilitygate-doc-undercounts-its-own-callers","recheck":"still-real, AND WORSE THAN FILED: availabilityGate.ts:328-333 still says forgetHardBlockedSlot has 'Two callers'. Grep confirms FOUR actual call sites - availabilityPreCheck.ts:938, :1160, :1166 and runOutputGates.ts:943. The row was written when it was three."}
{"date":"2026-07-31","ref":"gh#24-cap-checks-wrong-address","recheck":"still-real: connections/email/index.ts:11-55 (the header comment) still documents the same open concern verbatim - recipientRef is validated against `allowed` but passed to replyToMail as opts.to, and the comment itself calls this 'extra assurance on top of' Graph's own addressing rather than the guarantee."}
{"date":"2026-07-31","ref":"replytomail-orphan-draft","recheck":"still-real, NOT independently re-derived this pass beyond confirming replyToMail is still the function name referenced from connections/email/index.ts:72; no recent commit to connectors/graph/mail.ts surfaced, so treated as unchanged (createReply -> patch -> send, orphan draft on partial failure)."}
{"date":"2026-07-31","ref":"gh#144 T2","recheck":"still-real: grepped connectors/slack/app/fileIngestion.ts and handlers.ts for forwardImage/forwardCleanCopy - no matches in either, so no forward-to-owner capability for a clean colleague image exists yet."}
{"date":"2026-07-31","ref":"146c","recheck":"still-real, NOT independently re-read this pass (utils/shadowNotify.ts did not appear in the recent-commits check), so treated as unchanged: the forward is fire-and-forget with no ordering guarantee against shadowThreadAnchors."}
{"date":"2026-07-31","ref":"D2-forward-inert-without-owner-email","recheck":"still-real: utils/attendeeScope.ts:18-39 getOwnerDomain still returns null based purely on profile.user.email, and fileIngestion.ts:140-151 still computes isInternal directly from it with no explicit unset-email guard or warning."}
{"date":"2026-07-31","ref":"securitygate-internal-ref-regex-looser-copy","recheck":"still-real, NOT independently re-read this pass; no recent commit to utils/guards/securityGate.ts found, treated as unchanged from the filed finding."}
{"date":"2026-07-31","ref":"humangate-rewrites-two-part-reply","recheck":"still-real, NOT independently re-read this pass beyond confirming runOutputGates.ts is still the file in question and was touched by other (now-reverted) work this session; no evidence the two-part-reply-as-one-string handling changed."}
{"date":"2026-07-31","ref":"read-recap-has-no-shield","recheck":"still-real: runOutputGates.ts:760-805 read verbatim - the shield logic (matchingToolAlreadyRan / targetMatches / mutationCarried) is still keyed on mutation-shaped markers, consistent with this row's claim that a pure calendar-READ recap has nothing to shield it."}
{"date":"2026-07-31","ref":"three-copies-of-one-rewriter","recheck":"still-real BY REF ONLY, and the reason matters: grepped claimChecker.ts for the two rewriters this row named (unbacked_promise, calendar_confirmation) and found ZERO matches - the code that created the triplication was REVERTED on the owner's decline. The triplication is gone with it; the row survives only as the record of why. Safe to close if he wants it off the list."}
{"date":"2026-07-31","ref":"gh#5-outbound-to-externals-blocked-by-design","recheck":"still-real: connections/email/index.ts is unchanged in the area this row cites (the one-address cap construction at :72-115, read this pass for a different row); the architectural foreclosure of non-owner recipients by construction still stands."}
{"date":"2026-07-31","ref":"D1-dual-image-ingest-loop","recheck":"still-real: fileIngestion.ts:30-89 read verbatim - the image download/scan loop (downloadSlackImage -> scanAndPrepareImage) is still present exactly as described. Grepped handlers.ts for the same function names and found none under those names, so the second loop could not be directly re-confirmed."}
{"date":"2026-07-31","ref":"gh#51 I1","recheck":"still-real: this row describes a MANAGER/DISPATCH-PROCESS error - a piece in an approved build set was silently omitted from the build args - not a src/ code defect, so there is no file to open. Recorded as still-real per its own description; nothing this pass touched the dispatch mechanism. Candidate for the hand-read list rather than a lane."}
{"date":"2026-07-31","ref":"update-meeting-desc-claims-self-only","recheck":"still-real: meetings.ts:478-483 read verbatim - 'Colleague-path: each colleague can ONLY add or remove THEMSELVES… Tool refuses non-self changes from colleagues.' Meanwhile moveMeeting.ts:163-177 confirms an isRequester check granting a colleague full add/remove. This now actively contradicts the steer 4.3.4 shipped."}
{"date":"2026-07-31","ref":"gh#51 (profiler)","recheck":"still-real: grepped connectors/whatsapp.ts for gender/Hebrew handling and found zero matches - no gender-detection wiring exists on that inbound path, consistent with the row's claim."}
{"date":"2026-07-31","runId":"owner-rulings-2026-07-31","lane":"matchmaker","source":"both","ref":"B168-a","finding":"booking with Tyler (Eastern), Maelle asserted 'no genuine overlap Monday' then self-corrected to '16:00-18:15 your time is overlap, but busy' after the owner pushed back twice","verdict":"deferred","state":"open","note":"OWNER DEFERRED 2026-07-31, his words: 'defer for now'. A DEFERRAL IS A ONE-RUN SKIP AND THIS IS DUE ON THE NEXT RUN — it must NOT go on openKnown, and the engine would reject it and report deferredRejected if it did. Nothing about it is resolved: still no code citation, so it arrives needing shaping. The open question is unchanged — is the wrong overlap narration a deterministic defect in how the cross-timezone free window is computed or rendered, or the model narrating its own reasoning in prose and self-correcting with no code defect underneath? The final Tuesday recommendation was correct throughout, which is what makes it ambiguous. gh#168 is the ticket."}
{"date":"2026-07-31","runId":"wf_288712b3-d12","lane":"matchmaker","source":"owner","ref":"O175","finding":"a colleague booking a recurring-category slot was told the REAL SUBJECT of the owner's existing occurrence","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:428/:434/:436 - the raw Graph subject was interpolated into the colleague-facing refusal payload and message with no displaySubject/subjectViewerFor call, unlike Guard B at :583/:618 which already masks","verdict":"built","state":"built","invariant":"colleague-facing-subject-must-go-through-displaySubject","note":"Built on his 'ok to build'. Reused the existing helper rather than inventing a path: one maskedSubject computed via displaySubject(match, profile, subjectViewerFor(context)) replaces all three raw reads. +7/-4. VERIFY CLEAN, and it traced the non-vacuous cases both ways: a private occurrence renders '[Private]', an ordinary one still shows its real subject (matching M12's default, so the fix is not over-broad), the owner path is hard-gated out at :331, and getCalendarEvents does select sensitivity+categories (calendarReads.ts:596) so the masking predicate is not vacuously false."}
{"date":"2026-07-31","runId":"wf_288712b3-d12","lane":"outrider","source":"owner","ref":"G166-a>dep","finding":"the on-demand news budget sat below the inner gather budget, so on-demand Updates were a coin flip whose loss is silent","verdict":"built","state":"built","note":"HIS ANSWER WAS 14s and that is what shipped: NEWS_BRIEF_TIMEOUT_MS_ON_DEMAND 8_000 -> 14_000, clearing news.ts's 12_000 per-goal budget with headroom. One constant, comment updated to say why 14 and not 8. GH#166 IS NOW SATISFIED per the verify - both paths (20s scheduled, 14s on demand) sit above the inner budget, so the budget inversion the ticket names as its root no longer holds on either. The verify also confirmed the split cannot mis-route: routine.ts:108 omits force so the scheduled fire keeps 20s, and both live callers pass true. Closable at the next wrap."}
{"date":"2026-07-31","runId":"wf_288712b3-d12","lane":"instructor","source":"owner","ref":"B157-c>dep","finding":"Maelle asserted 'no history of meetings or notes with her' about a colleague whose person row holds 44 notes","verdict":"built","state":"built","invariant":"a-restriction-is-not-an-absence","note":"Built as the NARROW RULE ONLY, which is what he approved. systemPrompt.ts:433 now forbids the fabricated specific negative on an owner-in-MPIM turn and requires an honest 'I can't check or share that from here' - the exact reply the ticket reports as false. THE SCOPED-READ HALF WAS NOT TOUCHED and remains his unruled decision, not a build gap. Verify noted one residual worth knowing: the plain colleague-to-colleague case (systemPrompt.ts:439 branch) has no equivalent rule, so the same fabrication is still possible there."}
{"date":"2026-07-31","runId":"wf_288712b3-d12","lane":"matchmaker","source":"owner","ref":"G164-a","finding":"Maelle offered Michal a 16:15 squeeze, refused it when he accepted, and it was booked at 16:15 anyway","rootCause":"src/utils/scheduleRules.ts:340-346 isWithinBookingLeadTime has NO MARGIN - it evaluates strictly against Date.now() at the instant it is called, and every call site (availabilityPreCheck.ts:763, createMeeting.ts Guard B, findAvailableSlots.ts, planMeeting.ts:504) reads the same raw bookingLeadTimeHours with no asymmetry between PROPOSE time and COMMIT time. So a slot that clears the floor when she offers it in prose can cross it purely because real minutes elapsed before the accept - both evaluations individually correct, nothing drifting","verdict":"needs-owner-decision","state":"open","note":"HE ASKED A LANE TO FIND THE SOLUTION AND IT DID - then stopped at the one thing it should not invent. The only mechanical fix is a safety margin above the raw lead-time floor applied at the OFFER/narration stage, before she proposes a squeeze in prose, so ordinary reply latency cannot flip an already-offered slot. THAT REQUIRES A POLICY NUMBER AND THE TRADE-OFF CUTS AGAINST M6 (maximise options): every minute of margin permanently removes real, currently-legal near-cutoff slots from ever being offered. Its recommendation if he wants code: a yaml `lead_time_offer_margin_hours` (M7-style, never hardcoded) consulted ONLY when narrating an offer, never at the final booking gate. ITS ALTERNATIVE, which is a different lane: if he would rather keep every legal slot offered and have her own the reversal honestly in ALL cases - not only the create_meeting-attempt case the 4.3.4 caveat already covers - that is a narration-breadth change belonging to INSTRUCTOR, because nothing gates what she says in prose before she has queried anything. NOTE FOR ANYONE READING THE DIFF: the verify flagged G164-a as possibly dropped because no file corresponds to it and it was not among the four fixes handed over. It was not dropped - it built nothing deliberately, which is why there is nothing to verify."}
{"date":"2026-07-31","runId":"wf_288712b3-d12","lane":"instructor","source":"owner","ref":"O176","finding":"the update_meeting description told the model a colleague may only add or remove THEMSELVES, while the code has granted requesters full add/remove since v3.1.4","verdict":"needs-owner-decision","state":"built","note":"HALF BUILT, THEN OVERTURNED - and the overturn is that it is now WORSE than before. meetings.ts:482's prose bullet was corrected to the real requester-scoped rule, but the two SCHEMA FIELD descriptions at :495 (add_attendees) and :508 (remove_attendees) still read 'Colleague-path: only the requesting colleague themselves.' The model reads the field description while FILLING add_attendees, so on a requester's legitimate 'add Dina to the 3pm' it is still steered away - the exact thing O176 exists to remove and the thing that undercuts gh#165-b's steer. Previously the description was consistently wrong; it is now SELF-CONTRADICTORY within one payload, so behaviour depends on which sentence the model weights. RE-DISPATCHED to instructor as O176-finish to complete the same approved change - two field descriptions, correct wording already at :482 to copy, no code. Not new scope: same work, same file, unfinished. His constraint still binds: not changing the rules, requester-scoped and per-meeting, moveMeeting.ts untouched."}
{"date":"2026-07-31","runId":"wf_288712b3-d12","lane":"instructor","source":"verify","ref":"owner-memory-injected-into-clamped-mpim","finding":"the owner's own memory file is injected into a colleague-readable MPIM turn - the one surface the MPIM clamp exists to keep it off","rootCause":"src/core/orchestrator/systemPrompt.ts:779-791 (speakerMemoryBlock) and :799-810 (verifiedSenderBlock) both gate on `if (isOwner || !senderId) return ''` - and in a clamped MPIM isOwner is FALSE while senderId is the OWNER's slack id, so getPersonMemory(senderId) returns his row and the block renders 'MEMORY ON IDAN - what you've learned about them across past conversations' plus his full .md into the group thread's context","verdict":"flagged-for-owner","state":"open","invariant":"owner-data-must-gate-on-isOwnerPath-not-on-not-isOwner","note":"VERIFY DISCOVERY, and the most serious thing this run produced. Latent rather than live - nothing narrates the block today because the PRIVACY FILTER forbids it - so the exposure is one bad paraphrase away rather than already happening. THE GUARD ALREADY EXISTS ONE FILE OVER: buildTurnContext.ts:575-578 protects personWorkBlock against precisely this case and says so in its own comment ('in a clamped MPIM input.userId is the OWNER's id, so falling to the colleague branch would build a per-person block out of the owner's own row and inject it into the group thread'). The sibling blocks in systemPrompt.ts never got it. RECOMMEND HE PROMOTE THIS out of the discovery queue: it is the MPIM clamp he has repeatedly said not to loosen, being loosened by code rather than by a decision."}
{"date":"2026-07-31","runId":"wf_288712b3-d12","lane":"instructor","source":"verify","ref":"create-approval-desc-contradicts-requester-grant","finding":"Maelle tells a colleague who requested a meeting that adding someone needs Idan's sign-off, and raises an approval for a change that colleague could have made directly","rootCause":"src/tasks/skill.ts:300 (the create_approval description, edited by B165-c in the prior run) says 'If the change is theirs to make pending sign-off - e.g. the person who requested the meeting is adding attendees - say so plainly' versus moveMeeting.ts:171-210 where the requester's add/remove applies DIRECTLY with no approval at all","verdict":"flagged-for-owner","state":"open","invariant":"tool-text-must-match-the-gate-it-describes","note":"VERIFY DISCOVERY, severity medium, and it is a THIRD spelling of the same rule O176 is fixing - which is the point. skill.ts:300 ships on EVERY Slack turn (create_approval is in CORE_MODULES) while meetings.ts:482 ships on meeting turns, so the two contradict each other inside one context window. Worth reading together with O176 rather than separately: the requester grant is now described in three places and they do not agree. It also means the prior run's own B165-c edit, which was verified clean on its own terms, left this contradiction standing."}
{"date":"2026-07-31","runId":"wf_288712b3-d12","lane":"outrider","source":"verify","ref":"nested-news-timeout-not-derived-from-inner-budget","finding":"the news budget can invert again on any edit to either side, and the symptom is silent - the Updates section just stops appearing, which is the whole of gh#166 recurring","rootCause":"src/tasks/briefs.ts:31-45 - the outer/inner relation is now asserted in PROSE in two comments and enforced by nothing. Three magic numbers across two files with no import between them: NEWS_BRIEF_TIMEOUT_MS 20_000, NEWS_BRIEF_TIMEOUT_MS_ON_DEMAND 14_000, and news.ts's NEWS_PER_GOAL_TIMEOUT_MS 12_000","verdict":"flagged-for-owner","state":"open","invariant":"nested-timeout-must-derive-from-inner-budget","note":"VERIFY DISCOVERY, severity medium. Deriving both outer waits FROM the imported inner budget would make the inversion unrepresentable rather than merely commented against. His 'one constant, nothing else' constraint correctly kept the lane out of this, so it needs its own ruling - which is exactly the right outcome, not a gap. Note the shape: gh#166 has now been fixed twice by adjusting a number, and both times the relation that matters was left as prose."}
{"date":"2026-07-31","runId":"wf_288712b3-d12","lane":"matchmaker","source":"verify","ref":"order-violation-subject-unmasked","finding":"a colleague can be told the real subject of a meeting the owner marked private - the same leak O175 just closed, one branch over in the same handler","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:689 interpolates the RAW predecessor.subject from getEventEndInstant into the order_violation refusal with no displaySubject call. The branch (:673-696) sits outside any senderRole gate - the colleague gate closes before it and the next role branch is :831 - and both create_meeting and get_calendar are in COLLEAGUE_ALLOWED_TOOLS, so a colleague turn reaches it by supplying must_be_after_event_id","verdict":"flagged-for-owner","state":"open","invariant":"colleague-facing-subject-must-go-through-displaySubject","note":"VERIFY DISCOVERY, severity low, LATENT - it needs the model to pass an event id, so it is not observed firing. NOT ESTABLISHED and the lane must settle it: whether getEventEndInstant's return even carries sensitivity/categories. Without those displaySubject cannot mask there and the fix is a different shape than O175's. Same invariant as O175, which is why it is worth doing while that code is fresh."}
{"date":"2026-07-31","runId":"wf_288712b3-d12","lane":"instructor","source":"verify","ref":"O176>dep","finding":"update_meeting's schema still tells the model the opposite of the bullet just corrected","rootCause":"src/skills/meetings.ts:495 (add_attendees) reads 'Owner-path: any people. Colleague-path: only the requesting colleague themselves.' and :508 (remove_attendees) the equivalent - both contradict :482 and moveMeeting.ts:162-210","verdict":"needs-dependency","state":"open","note":"DEPENDENCY ASK, severity HIGH - and it was NOT left for him. Re-dispatched immediately as O176-finish, because it is the unfinished half of a change he already approved with a constraint, in the same file, and completing approved work is not a new decision. Description text only, correct wording already at :482, moveMeeting.ts untouched."}
{"date":"2026-07-31","runId":"direct-a32bd9e4","lane":"instructor","source":"owner","ref":"O176-finish","finding":"update_meeting's two schema FIELD descriptions still said a colleague may only add or remove themselves, contradicting the prose bullet corrected minutes earlier in the same payload","rootCause":"src/skills/meetings.ts:495 (add_attendees) and :508 (remove_attendees) versus the corrected :482 - the model reads the field description while FILLING the field, so the stale text steered it away from a legitimate requester-adds-a-third-party call","verdict":"built","state":"built","invariant":"tool-text-must-match-the-gate-it-describes","note":"COMPLETED the same change he approved with a constraint - two description strings, net zero lines, no code, moveMeeting.ts untouched. Both now state the identical invariant as :482: whoever REQUESTED this specific meeting may add or remove others on IT, per-meeting and not a blanket colleague permission, with a non-requester refused as colleague_not_requester and routed to owner approval. IT PROVED CLOSURE RATHER THAN CLAIMING IT: grepped src for the themselves-phrasings and found zero hits outside the three now-consistent copies in meetings.ts. ONE HONEST LIMIT ON THAT CLAIM - it is true of the TOOL SCHEMA, not of the repo: skill.ts:300's create_approval description still says the requester's change needs sign-off, which is the separate flagged row create-approval-desc-contradicts-requester-grant. Three places described this rule, two now agree. A VERIFIER PASS IS OWED on this hand dispatch and is deliberately deferred to the pre-wrap pass rather than spending an Opus run on two description strings the wave verify already had the file open for - declaring the skip and its reason per A37 rather than leaving it implied."}
{"date":"2026-07-31","runId":"owner-rulings-2026-07-31","lane":"matchmaker","source":"github","ref":"G152-b","finding":"booking with Michal after the owner's Weekly-with-Yael conflict was surfaced (10:00-10:40), Maelle offered 11:00 / 12:30 / 15:00 as the clean 25-minute slots but did not offer 10:45 - the slot immediately adjacent to the just-revealed conflict, which the owner pointed out was clean and dense","verdict":"declined","state":"open","note":"OWNER DECLINED 2026-07-31, his words: 'ok drop'. Recorded rather than deleted so nobody re-raises it. It was gh#152's LAST remaining complaint - the stale-cache half shipped in 4.3.4 - and the root was never proven: the lane looked for a slot-step-size or buffer constant in findAvailableSlots.ts and found none to cite. The Manager's own recommendation was defer-or-drop for the same reason. gh#152 can now be closed at a wrap on the strength of its built half plus this decline, since nothing on that ticket remains open. Do not re-propose."}
{"date":"2026-07-31","runId":"direct-a08906ec","lane":"instructor","source":"github","ref":"G164-a","finding":"Maelle offered Michal a 16:15 squeeze, refused it when he accepted, and it was booked at 16:15 anyway - the pushback read as arbitrary","rootCause":"src/utils/scheduleRules.ts:340-346 evaluates within_lead_time against Date.now() with no propose/commit asymmetry, so the same legal slot is offered before the cutoff and refused after it. THE GAP WAS NARRATION BREADTH: the only existing caveat (availabilityPreCheck.ts renderPromptBlock, ~:1339) armed ONLY on 'a booking attempt made later is refused', never on a reversal caught in prose with no tool call - which is exactly what happened here","verdict":"built","state":"built","invariant":"one-fact-one-derivation","note":"OWNER RULED OPTION (2) on 2026-07-31, quoting the recommendation back: 'its free, it keeps M6 intact, and the complaint in the ticket was that the pushback read as arbitrary - not that 16:15 got offered.' ZERO BEHAVIOUR CHANGE, grep-confirmed: scheduleRules.ts, isWithinBookingLeadTime, bookingLeadTimeHours and every yaml value untouched. Every slot legal at offer time is still offered; only what she is told to SAY changed. IT DID THE THING THE LAST ATTEMPT FAILED AT - the 'never claim booked before tool success' rule went from THREE spellings to TWO (systemPrompt.ts:707-708 and meetings.ts:1375; the availabilityPreCheck copy is DELETED, not reworded, -218 chars), and the new lead-time-reversal instruction exists in exactly ONE place. Home chosen per systemPrompt.ts:714's own routing, which states booking honesty rules live in the meetings skill section. AND IT MEASURED A COST RATHER THAN CLAIMING A WASH, which is why this row is worth reading: -218 chars off a NARROW-firing tier (only when the precheck runs on a colleague-stated clock) against +575 on the meetings tool-description tier (whenever the meetings skill is in scope), so it is a real net increase of roughly +357 on the common path. It tightened the addition from a 747-char draft to 575 and says it could not go lower without dropping the 'whether or not a tool call was made' clause - the one line that actually fixes the complaint. A deliberate tier trade the ruling itself forced, since the cheaper trigger would need the code-side margin he ruled out. Typecheck green."}
{"date":"2026-07-31","runId":"owner-backlog-clean","lane":"registrar","source":"owner","ref":"re-ask-revival-posts-bare-asktext","finding":"re-ask revival posts bare askText on a resolving decision surface","rootCause":"never established - the row cites no file, which is why it is being closed","verdict":"declined","state":"open","note":"OWNER DECLINED 2026-07-31: 'clean backlog items that can't be recover. if its important they will raise again.' CITES NO FILE, so no backlog pass can ever re-read it and no lane can be pointed at it - it would have stood in --open forever, diluting a list he rules from. Closing it is the honest terminal state, not a loss: the symptom, if real, recurs in the logs or a ticket and re-enters through the scout with a location attached. NOT to be re-raised as a NEW finding on the strength of this row alone. Kept OUT of this sweep because they cite no file for a different reason: two rows he deferred TO 2026-07-30 (due, not lost), gh#168 and B168-a (a ticket filed last night), gh#164 (open ticket, revised to the parity root), gh#156-b>dep (the correct root the verify found), and gh#158 (a ticket-coverage marker, never a code finding)."}
{"date":"2026-07-31","runId":"owner-backlog-clean","lane":"matchmaker","source":"owner","ref":"f2b-rule-1-has-no-allowrelaxed","finding":"F2b rule 1 has no allowRelaxed gate - the lone exception in the ladder","rootCause":"never established - the row cites no file, which is why it is being closed","verdict":"declined","state":"open","note":"OWNER DECLINED 2026-07-31: 'clean backlog items that can't be recover. if its important they will raise again.' CITES NO FILE, so no backlog pass can ever re-read it and no lane can be pointed at it - it would have stood in --open forever, diluting a list he rules from. Closing it is the honest terminal state, not a loss: the symptom, if real, recurs in the logs or a ticket and re-enters through the scout with a location attached. NOT to be re-raised as a NEW finding on the strength of this row alone. Kept OUT of this sweep because they cite no file for a different reason: two rows he deferred TO 2026-07-30 (due, not lost), gh#168 and B168-a (a ticket filed last night), gh#164 (open ticket, revised to the parity root), gh#156-b>dep (the correct root the verify found), and gh#158 (a ticket-coverage marker, never a code finding)."}
{"date":"2026-07-31","runId":"owner-backlog-clean","lane":"gatekeeper","source":"owner","ref":"FLOOR-PRODUCTION-RECORD-0-for-2","finding":"the availability floor's first-day production record was zero catches and two false positives - an observation, never a located defect","rootCause":"never established - the row cites no file, which is why it is being closed","verdict":"declined","state":"open","note":"OWNER DECLINED 2026-07-31: 'clean backlog items that can't be recover. if its important they will raise again.' CITES NO FILE, so no backlog pass can ever re-read it and no lane can be pointed at it - it would have stood in --open forever, diluting a list he rules from. Closing it is the honest terminal state, not a loss: the symptom, if real, recurs in the logs or a ticket and re-enters through the scout with a location attached. NOT to be re-raised as a NEW finding on the strength of this row alone. Kept OUT of this sweep because they cite no file for a different reason: two rows he deferred TO 2026-07-30 (due, not lost), gh#168 and B168-a (a ticket filed last night), gh#164 (open ticket, revised to the parity root), gh#156-b>dep (the correct root the verify found), and gh#158 (a ticket-coverage marker, never a code finding)."}
{"date":"2026-07-31","runId":"owner-backlog-clean","lane":"instructor","source":"owner","ref":"orphaned-coremodule-prompt-sections","finding":"OutreachCoreSkill and TasksSkill carry getSystemPromptSection prose with no established location","rootCause":"never established - the row cites no file, which is why it is being closed","verdict":"declined","state":"open","note":"OWNER DECLINED 2026-07-31: 'clean backlog items that can't be recover. if its important they will raise again.' CITES NO FILE, so no backlog pass can ever re-read it and no lane can be pointed at it - it would have stood in --open forever, diluting a list he rules from. Closing it is the honest terminal state, not a loss: the symptom, if real, recurs in the logs or a ticket and re-enters through the scout with a location attached. NOT to be re-raised as a NEW finding on the strength of this row alone. Kept OUT of this sweep because they cite no file for a different reason: two rows he deferred TO 2026-07-30 (due, not lost), gh#168 and B168-a (a ticket filed last night), gh#164 (open ticket, revised to the parity root), gh#156-b>dep (the correct root the verify found), and gh#158 (a ticket-coverage marker, never a code finding)."}
{"date":"2026-07-31","runId":"owner-backlog-clean","lane":"matchmaker","source":"owner","ref":"availability-asserted-from-unsearched-window","finding":"DISCOVERY, never built - 'Yes, 11am tomorrow is free for her too' asserted from a search that did not cover it","rootCause":"never established - the row cites no file, which is why it is being closed","verdict":"declined","state":"open","note":"OWNER DECLINED 2026-07-31: 'clean backlog items that can't be recover. if its important they will raise again.' CITES NO FILE, so no backlog pass can ever re-read it and no lane can be pointed at it - it would have stood in --open forever, diluting a list he rules from. Closing it is the honest terminal state, not a loss: the symptom, if real, recurs in the logs or a ticket and re-enters through the scout with a location attached. NOT to be re-raised as a NEW finding on the strength of this row alone. Kept OUT of this sweep because they cite no file for a different reason: two rows he deferred TO 2026-07-30 (due, not lost), gh#168 and B168-a (a ticket filed last night), gh#164 (open ticket, revised to the parity root), gh#156-b>dep (the correct root the verify found), and gh#158 (a ticket-coverage marker, never a code finding)."}
{"date":"2026-07-31","runId":"owner-backlog-clean","lane":"slacker","source":"owner","ref":"owner-role-split-within-one-turn","finding":"DISCOVERY, never built - inside one MPIM turn the owner is treated as a colleague by some paths and as owner by others","rootCause":"never established - the row cites no file, which is why it is being closed","verdict":"declined","state":"open","note":"OWNER DECLINED 2026-07-31: 'clean backlog items that can't be recover. if its important they will raise again.' CITES NO FILE, so no backlog pass can ever re-read it and no lane can be pointed at it - it would have stood in --open forever, diluting a list he rules from. Closing it is the honest terminal state, not a loss: the symptom, if real, recurs in the logs or a ticket and re-enters through the scout with a location attached. NOT to be re-raised as a NEW finding on the strength of this row alone. Kept OUT of this sweep because they cite no file for a different reason: two rows he deferred TO 2026-07-30 (due, not lost), gh#168 and B168-a (a ticket filed last night), gh#164 (open ticket, revised to the parity root), gh#156-b>dep (the correct root the verify found), and gh#158 (a ticket-coverage marker, never a code finding)."}
{"date":"2026-07-31","runId":"owner-backlog-clean","lane":"matchmaker","source":"owner","ref":"gh#157-calendar-filter-and-history-gap","finding":"FLAGGED, clarity ambiguous, nothing ever dispatched - three live possibilities, none narrowed","rootCause":"never established - the row cites no file, which is why it is being closed","verdict":"declined","state":"open","note":"OWNER DECLINED 2026-07-31: 'clean backlog items that can't be recover. if its important they will raise again.' CITES NO FILE, so no backlog pass can ever re-read it and no lane can be pointed at it - it would have stood in --open forever, diluting a list he rules from. Closing it is the honest terminal state, not a loss: the symptom, if real, recurs in the logs or a ticket and re-enters through the scout with a location attached. NOT to be re-raised as a NEW finding on the strength of this row alone. Kept OUT of this sweep because they cite no file for a different reason: two rows he deferred TO 2026-07-30 (due, not lost), gh#168 and B168-a (a ticket filed last night), gh#164 (open ticket, revised to the parity root), gh#156-b>dep (the correct root the verify found), and gh#158 (a ticket-coverage marker, never a code finding)."}
{"date":"2026-07-31","runId":"direct-ab5e3a68","lane":"registrar","source":"verify","ref":"B165-c","finding":"the create_approval description now tells the model a requester's attendee change is pending sign-off, while meetings.ts says the requester acts directly - a direct contradiction inside one context window, on the broadest tier there is","rootCause":"src/tasks/skill.ts:300 - B165-c's edit rewrote and LENGTHENED a neighbouring clause beyond the approved completion-language fix. Its example ('the person who requested the meeting is adding attendees... I've sent your request to add yourself to the meeting to Idan') describes a state that cannot exist: moveMeeting.ts:162-210 applies a requester's change DIRECTLY and only a non-requester gets colleague_not_requester","verdict":"needs-owner-decision","state":"built","invariant":"tool-text-must-match-the-gate-it-describes","note":"OVERTURNED - THE WAVE'S ONE REAL SEAM DEFECT, and the verify said explicitly that B165-c's own edit is incomplete. The honesty half is correct and net-new; the collateral rewrite is the problem, and it was never part of what the owner approved. THREE FAULTS AT ONCE: it steers the model toward create_approval for a call that would have SUCCEEDED (the exact defect O176 just removed from meetings.ts, and the mechanism gh#165-b depends on); it REINTRODUCES the self-only framing into a module that ships on EVERY Slack turn, in the same wave that deleted self-only from meetings.ts; and where the two files were previously both stale and roughly consistent, they now contradict each other directly. THAT IS THE WAVE'S OWN PRECEDENT ONE FILE OVER - the previous verify overturned O176's half-fix for exactly this, 'a half-fix is worse than the original, because the payload became self-contradictory'. RESOLVED SAME NIGHT: dispatched to INSTRUCTOR (the report assigns this text to instructor; shepherd made the edit, and two lanes on one file is the documented overwrite hazard) to replace the example with the case that actually raises an approval - a colleague asking to be added to a meeting they did NOT request. Roughly net-zero characters, since this tier fires on every turn."}
{"date":"2026-07-31","runId":"direct-ab5e3a68","lane":"instructor","source":"verify","ref":"B157-c-calendar-read-analogy-false","finding":"the honest-refusal rule contains the clause 'same as a calendar read here', and that is FALSE - get_calendar IS in COLLEAGUE_ALLOWED_TOOLS (registry.ts:370), so a calendar read on that surface is permitted, not restricted","rootCause":"src/core/orchestrator/systemPrompt.ts:433 - the analogy was added alongside the correct honesty rule","verdict":"needs-dependency","state":"built","note":"A KNOWN-FALSE STATEMENT SITTING IN THE HONESTY PROMPT, which is why it was fixed rather than deferred even though the verify called it honest to defer. It is false on the very surface it governs: gh#157's complaint 3 was 'she is lying as we have many meetings, I assume she gets no tool', so a line implying calendar reads are restricted there risks producing THE OPPOSITE FAILURE from the one the rule fixes. Dispatched to instructor to drop the analogy, ~-30 chars, rest of the rule untouched. The honesty rule itself is correct and needed - this is four words inside it."}
{"date":"2026-07-31","runId":"direct-ab5e3a68","lane":"instructor","source":"verify","ref":"g164a-comment-outweighs-its-deletion","finding":"a 792-character comment explains a 218-character deletion, narrates text that no longer exists, and asserts something false","rootCause":"src/utils/availabilityPreCheck.ts - the comment claims the clock-mechanism fact is 'unique to this file and not stated anywhere else', which is false as of the same commit: meetings.ts:1377 states it too","verdict":"needs-dependency","state":"built","note":"3.6x the size of what it removed, and the false uniqueness claim is the dangerous part - the next lane trusting it edits one copy and leaves the other, which is precisely how three spellings accumulated in the first place. Dispatched to instructor to trim to one line and delete the claim. NOT deleting the remaining mechanism sentence from the shipped prompt: on an owner turn the pre-check block does not render, so the meetings.ts copy is load-bearing there and the duplication is deliberate."}
{"date":"2026-07-31","runId":"direct-ab5e3a68","lane":"matchmaker","source":"verify","ref":"movemeeting-comment-still-says-self-only","finding":"moveMeeting.ts:104's comment still says 'we (a) gate colleague-path to self-only' while :155-210 in the same file says the requester-controls gate REPLACED that","verdict":"flagged-for-owner","state":"open","invariant":"tool-text-must-match-the-gate-it-describes","note":"VERIFY DISCOVERY, severity low, next build's intake. Same class as O176 one file over - and it is the comment a lane reads BEFORE editing the gate, which is what makes a low-severity comment defect worth a row."}
{"date":"2026-07-31","runId":"direct-ab5e3a68","lane":"instructor","source":"verify","ref":"honest-refusal-rule-is-mpim-only","finding":"the honest-refusal rule lives in the isOwnerInGroup branch, so the owner clamped in a CHANNEL takes systemPrompt.ts:439 instead and can still fabricate the exact gh#157 negative","verdict":"flagged-for-owner","state":"open","invariant":"a-restriction-is-not-an-absence","note":"VERIFY DISCOVERY, severity medium, and a HIGHER-severity read of the residual the report already named: the report flagged the colleague-to-colleague gap, this is the same code path with the OWNER as the victim. Also connects to gh#154, which documents isOwnerInGroup as MPIM-only (processMessage.ts:128) and parks the partial-flag-per-surface question pending his ruling - so this wave GREW the pattern that ticket is about, without contradicting it."}
{"date":"2026-07-31","runId":"direct-ab5e3a68","lane":"outrider","source":"verify","ref":"prompt-budget-grew-1836-chars-unmeasured-by-anything","finding":"the wave adds ~+1,836 rendered prompt characters net, and nothing in the framework tracks this - the single most expensive line lands on the broadest tier to fix a wording defect","rootCause":"per tier: skill.ts create_approval 436 -> 886 = +450 on EVERY Slack turn; meetings.ts +1,150 on every meetings turn; systemPrompt.ts owner-in-MPIM +454 narrow; availabilityPreCheck -218 narrow","verdict":"flagged-for-owner","state":"open","note":"VERIFY DISCOVERY, and it is a STANDARD finding rather than a defect - no single row is wrong, the wave is simply a net add with no meter on it. Roughly 40% of that always-on +450 is the rewritten example carrying the B165-c contradiction, so fixing that finding shrinks the broadest tier at the same time. Worth a row because the prompt is a budget (his own rule) and this is the first time anyone has counted it per tier."}
{"date":"2026-07-31","runId":"direct-a6ce99b4","lane":"instructor","source":"verify","ref":"B165-c","finding":"the create_approval description told the model a requester's attendee change is pending sign-off, contradicting meetings.ts on every Slack turn","rootCause":"src/tasks/skill.ts:300 - the example described a state that cannot occur: moveMeeting.ts:171-210 sets isRequester from ownerInfo.requesterSlackId === context.userId, and colleague_not_requester fires ONLY when !isRequester, so a requester adding attendees to their own meeting routes to no approval at all","verdict":"built","state":"built","invariant":"tool-text-must-match-the-gate-it-describes","note":"OVERTURN RESOLVED. The example now describes the case that ACTUALLY raises an approval - a colleague who did NOT request the meeting asking to be added - mirroring meetings.ts:482. The completion-language sentence was left untouched, as it was the approved half. THE ALWAYS-ON TIER SHRANK: 438 -> 380 chars, -58, so fixing the contradiction also reduced the broadest prompt tier rather than adding to it. Root proven by reading BOTH files rather than grepping one, which is what the brief asked for. All three statements now agree: moveMeeting applies a requester's change directly, meetings.ts:482/:495/:508 say requester-scoped with non-requester routed to approval, and skill.ts:300 describes only the non-requester case."}
{"date":"2026-07-31","runId":"direct-a6ce99b4","lane":"instructor","source":"verify","ref":"B157-c-calendar-read-analogy-false","finding":"a known-false clause in the honesty prompt claimed calendar reads are restricted on the colleague surface","verdict":"built","state":"built","note":"Dropped ', same as a calendar read here' (~-31 chars). Falsity verified directly at registry.ts:370, which lists get_calendar inside COLLEAGUE_ALLOWED_TOOLS - so the analogy told the model a PERMITTED tool is restricted, on the exact surface where gh#157 complained she already under-uses her calendar access. The person-history restriction the rule actually enforces is untouched."}
{"date":"2026-07-31","runId":"direct-a6ce99b4","lane":"instructor","source":"verify","ref":"g164a-comment-outweighs-its-deletion","finding":"a 792-character comment explained a 218-character deletion and asserted a false uniqueness claim","verdict":"built","state":"built","note":"11-line comment replaced with 3 lines that name the actual duplication (meetings.ts's MEETINGS HONESTY block) and cross-reference meetings.ts:1377 instead of claiming uniqueness. The shipped prompt paragraph was NOT touched - that copy is load-bearing on the colleague surface where this block renders, so the duplication is deliberate and now documented as such rather than denied."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"matchmaker","source":"both","ref":"gh#158-b","finding":"Idan asked whether Levana was free at 10am then 11am and Maelle answered from HIS OWN hours and calendar, not hers","rootCause":"src/utils/availabilityPreCheck.ts:494-541 — the colleague-path owner-scoped point-check had no concept of a named third party's calendar and injected a false 'settled' verdict grounded in the owner's own hours","verdict":"already-fixed","state":"wrapped","note":"Repaired and DEPLOYED before this run: the bail at availabilityPreCheck.ts:517-541 returns ran:false whenever resolvedMeetingAttendees is non-empty, and forgets stale owner-ledger hard blocks at the same instant so the output-time floor has nothing to misfire on. Shipped 2026-07-30 20:44 in 4.3.4 (1754444), live at cf30a52. COST A FULL DISPATCH to establish: gh#158 is still open on its other two complaints, so the scout re-read the FIXED complaint off the live ticket and nothing on alreadyBuilt could match it — a fixed complaint on an open ticket keeps costing a dispatch until the ticket closes. The shipping commit names a residual of its own: the bail fires whenever ANY colleague is named, not only when the question is about them, so it disables the owner's own point-check on turns that merely mention an attendee. Over-broad, not wrong-subject; left as-is."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"matchmaker","source":"both","ref":"gh#165-d","finding":"A colleague asking to book over the owner's all-day day-off was steered to 'add attendees' to the vacation block","rootCause":"src/connectors/graph/findAvailableSlots.ts:1113-1122 — the `if (!verdict.passes)` early-continue sits ABOVE :1145, the ONLY writer of diagnosticsOut.conflictingEvent, and scheduleRules.ts:646 returns passes:false whenever overCommitment is set unless allowRelaxed or isFloatingBlock is true","verdict":"needs-owner-decision","recommend":"build — dispatch gh#165-d>dep; one lane, the same two files, it makes ~50 lines already written do something, and it revives gh#165-b which has NEVER FIRED ONCE since it shipped","state":"open","invariant":"fact-published-on-a-path-its-reader-never-takes","note":"VERIFY OVERTURN. This wave's fix is in the tree (findAvailableSlots.ts + createMeeting.ts, +49 lines, nothing deleted) and is INERT — the new branch, the `all_day` log field and both new diagnostics fields have no reachable reader, so the next person to see a day-off steer will believe it was handled. THE LARGER FINDING IS NOT THIS WAVE'S WORK: gh#165-b, approved and shipped in 4.3.4/4.3.5, has never fired in production (zero `conflicting_event_id` lines in any log file), and it REPLACED a findDuplicateEvent lookup that WAS reachable and did name an existing event — so #165 is now WORSE than before that fix. Also introduced here: the all-day distinction is spelled a FOURTH time with a FIFTH predicate (scheduleRules.ts:653 and availabilityGate.ts:116 gate on allDayOutOfOffice, violationLabels.ts:28 on the day-level owner_out_of_office kind, this one on isAllDay). The lane's wider predicate is right on the merits — most synced PTO carries no Outlook OOF status — which is exactly why it belongs at the shared root rather than as a fifth local spelling. The verify refuses shipping as-is; the alternative to the dep ask is reverting both files to HEAD."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"matchmaker","source":"verify","ref":"gh#165-d>dep","finding":"Make the conflict fact reachable at the root, then keep the steer honest","rootCause":"src/connectors/graph/findAvailableSlots.ts:1113-1122 (populate conflictingEvent on the REJECTED path), src/utils/scheduleRules.ts:299-316 (carry isAllDay structurally instead of string-matching the display value 'all day'), src/skills/meetings/ops/handlers/createMeeting.ts (broken_rule_label still says 'conflicts with another meeting' on the day-off branch)","verdict":"needs-owner-decision","recommend":"build — same change as gh#165-d, blocked on that ruling","state":"open","invariant":"fact-published-on-a-path-its-reader-never-takes","note":"DEFERRED DEP ASK — not dispatched because its parent verdict is awaiting the owner. SAME CHANGE as gh#165-d: do not count two fixes. Three parts. (1) Populate diagnosticsOut.conflictingEvent inside the `!verdict.passes` branch when violation_kind is owner_busy_collision, where verdict.overCommitment is already present (scheduleRules.ts:656 spreads slotFacts into that return) — that one move revives BOTH gh#165-b's add-attendees steer and this wave's day-off branch, neither of which has ever fired. (2) Add `isAllDay?: true` to RuleCheckResult['overCommitment'] at scheduleRules.ts:299-316, set from the same isAllDay read that already produces `window` at :596, and drop the `window === 'all day'` comparison — `window` is documented as a human render, so the day its wording changes the add-attendees steer silently returns on vacation days. (3) Fix broken_rule_label on the new all-day branch: it is still humanizeViolationLabel('owner_busy_collision') = 'conflicts with another meeting on Idan's calendar', so the payload's message says 'day off, not a meeting' while the field documented for verbatim paste into create_approval ask_text claims a clashing meeting. That also closes the unruled backlog row `vacation-day-reported-as-already-booked`."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"matchmaker","source":"github","ref":"gh#158-a","finding":"Idan asked to book with Levana Bagants; the attendee pre-resolution named Yael Aharon and resolved her to yael.a@reflectiz.com, and Maelle then insisted she had only checked Levana's and Idan's calendars","rootCause":"src/core/orchestrator/buildTurnContext.ts:292 — turnMeetingPeople carried a name never mentioned on that turn; the harm lands at src/skills/meetings/ops/handlers/createMeeting.ts:144-166, where context.resolvedMeetingAttendees is unioned into the real invite unconditionally","verdict":"needs-shaping","recommend":"build — SAME ROOT as the confirmed backlog row `classifier-guess-reaches-real-calendar-invite`, which answers the shaping question: gate the union so a classifier guess never reaches a real invite unconfirmed","state":"open","invariant":"classifier-guess-reaches-a-real-world-action","note":"MANAGER MERGE (M2). The scout returned this needs-shaping, asking whether classifyTurn's per-turn extraction carries a name across turns in an MPIM and whether it ever should. It does not need a reproduction to be actionable: `classifier-guess-reaches-real-calendar-invite` was re-read THIS RUN and confirmed still-real at createMeeting.ts:151-166. Whatever put Yael in the list, the harm is that an unconfirmed guess reaches a real calendar invite — one root, one lane, one gate. Whether the carry-over should happen at all is a separate and much smaller question once the invite is gated."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"profiler","source":"both","ref":"gh#157-b","finding":"Asked what she knows about Yael Aharon in an MPIM, Maelle said 'not much on file yet, no history of meetings or notes with her, this looks like our first real interaction' — that person row holds 44 notes and dozens of interactions","rootCause":"src/core/orchestrator/systemPrompt.ts:137-139 — peopleSection is nulled when !isOwner, and registry.ts:368-450 excludes the person-memory tools on the same turn shape, so she has zero data and zero tools and reports the absence as a finding rather than as a limit","verdict":"needs-shaping","recommend":"build — the narrow honesty half only (she must say she cannot check from here and may never assert a specific negative); the scoped-read half stays yours","state":"open","note":"SAME ROW as the backlog entry B157-c, re-read and confirmed still-real this run — one row, not two. The narrow fix is not in dispute and 4.3.5 already shipped its MPIM half. What is open: (a) the rule is MPIM-only, so a clamped owner in a CHANNEL takes systemPrompt.ts:439 and can still fabricate the same negative; (b) the genuinely owner-only call — whether a clamped MPIM turn gets a scoped person-data read so she can say 'I do have history with her but I don't share notes in a group thread'. (b) widens what is exposed on a colleague-clamped surface, which is the do-not-loosen-without-a-ruling area. gh#157 was commented PARTIAL at the 4.3.5 wrap for exactly this."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"matchmaker","source":"both","ref":"gh#164","finding":"Maelle told Michal 16:15 was too soon to squeeze in; Michal said 'ok so squeeze into 16:15' and Maelle pushed back again saying 16:15 was not available after all","rootCause":"src/utils/scheduleRules.ts:340-346 — isWithinBookingLeadTime measures the 4h colleague buffer from the clock at the instant it is called, with no asymmetry between offering a slot and committing to it, so 16:15 was legal until 12:15 and illegal after","verdict":"needs-shaping","recommend":"decline — the root is established, you ruled on it on 2026-07-31, and the remedy you chose shipped in 4.3.5; the offer-then-refuse sequence itself you declined to change","state":"open","note":"The scout's shaping question was whether this is a fresh recurrence of the parity gap or a case predating that fix. It is neither: nothing changed, the CLOCK moved, and the same function gave the correct answer both times. Owner's ruling, 2026-07-31: 'I'd take (2): it's free, it keeps M6 intact, and the complaint in the ticket was that the pushback read as arbitrary — not that 16:15 got offered.' The narration — she names what moved — shipped in 4.3.5 at meetings.ts:1377 and is deployed at cf30a52. The only mechanical alternative was a safety margin above the lead-time floor applied at offer time, and he declined it because it buys 'never reverses' by silently withholding real, currently-legal near-cutoff slots. gh#164 was commented PARTIAL at the 4.3.5 wrap; a decline here closes it."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"matchmaker","source":"both","ref":"gh#168-a","finding":"Booking with Tyler in Eastern time, Maelle asserted 'no genuine overlap Monday', self-corrected to a wrong reason, then corrected again to a third answer","rootCause":"unestablished — the scout could not prove one; its hypothesis is the requester-timezone frame resolution used by the availability pre-check","verdict":"needs-owner-decision","recommend":"build — high severity, a repeat pattern in his own words, and it was deferred by the last run so it is DUE; a dispatch establishes the root and fixes it in one pass","state":"open","note":"AMBIGUOUS intake, so it is shown rather than auto-fixed. Same row as the deferred backlog entry B168-a (deferred 2026-07-31 — a one-run skip, therefore due now). Three successive answers to one question is the silent-wrongness tier, and the owner calls it a repeat of the timezone confusion pattern rather than a one-off."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"matchmaker","source":"verify","ref":"broken-rule-label-is-a-verb-phrase-in-an-adjective-slot","finding":"Every colleague told a time clashes with the owner's calendar gets 'That time is conflicts with another meeting on Idan's calendar for Idan', and the approval raised off it inherits the same phrasing","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:652 — the template drops brokenRuleLabel into an adjective slot, against violationLabels.ts:29/:31 which return a verb phrase","verdict":"queued-next-run","recommend":"build — one template fix, and it is worth a slot because the model is then told to paste that exact phrase verbatim into create_approval ask_text","state":"open","invariant":"fact-published-on-a-path-its-reader-never-takes","note":"VERIFY DISCOVERY, severity low, matchmaker — NOT this wave's work and NOT a row on his desk; it drains through state.pendingOverflow into the head of the next build. LIVE AT HEAD and confirmed emitted in production: logs/maelle-2026-07-29.log:759 carries broken_rule_label 'conflicts with another meeting on Idan's calendar'. This is the branch that actually fires for a busy collision — the two above it are unreachable, which is row gh#165-d. Most other labels are adjectival ('outside Idan's work hours', 'off that day'), so the template breaks only on the two collision labels."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"matchmaker","source":"audit","ref":"remaining-cached-decision-reads","verdict":"already-fixed","state":"wrapped","note":"backlog re-read: src/utils/availabilityPreCheck.ts:706-719 — the code comment explicitly documents the fix, \"v4.3.x (row 'remaining-cached-decision-reads') — now reads via getOwnerEventsForDecision, ALWAYS 'live'\", replacing the old plain getCalendarEvents cached-by-default call. Row retired."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"outrider","source":"audit","ref":"G166-a","verdict":"already-fixed","state":"wrapped","note":"backlog re-read: src/tasks/briefs.ts:39-48 — NEWS_BRIEF_TIMEOUT_MS (20s, scheduled) and NEWS_BRIEF_TIMEOUT_MS_ON_DEMAND (14s, force=true) are now two separate constants with the on-demand one kept narrower, exactly addressing the finding that the fix had lengthened the on-demand path too. Shipped in 4.3.5 (b4fda5d). Row retired."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"instructor","source":"audit","ref":"create-approval-desc-contradicts-requester-grant","verdict":"already-fixed","state":"wrapped","note":"backlog re-read: src/tasks/skill.ts was rewritten in commit b4fda5d (4.3.5); the offending sentence ('if the change is theirs to make pending sign-off…') no longer exists anywhere in the codebase (grepped, zero matches), and :300 now carries non-contradicting text about relaying approvals in flight. This was the pre-wrap overturn corrected before release. Row retired."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"instructor","source":"audit","ref":"O176>dep","verdict":"already-fixed","state":"wrapped","note":"backlog re-read: read verbatim at src/skills/meetings.ts:495 and :508 — both now read 'Owner-path: any people. Colleague-path: whoever REQUESTED this specific meeting can add/remove others on it', matching :482 exactly; the old 'only the requesting colleague themselves' wording is gone. Shipped in 4.3.5 (b4fda5d) as O176-finish. Row retired."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"gatekeeper","source":"audit","ref":"gh#156-get-back-to-you-no-followthrough>dep>dep","verdict":"already-fixed","state":"wrapped","recheck":"fixed 2026-07-31: grepped src/utils/claimChecker.ts and src/utils/guards/runOutputGates.ts for 'unbacked_promise' — zero matches at HEAD cf30a52. The gate this dependency question was about was reverted before the 4.3.4 wrap (1754444), so the ordering question is moot.","note":"Row retired. The engine held this row back for naming no commit; the commit is 1754444 and the proof is the zero-match grep against both named files at HEAD."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"matchmaker","source":"audit","ref":"slot-hold-release-dm-role-gate","recheck":"moved 2026-07-31, still real, now at src/skills/meetings/ops/handlers/createMeeting.ts:1409-1428 — read verbatim: the hold-release DM only checks `!h.holder_slack_id || h.holder_slack_id === context.userId` before sending, with no check of context.senderRole or the booking channel at all. Same defect as originally filed, shifted ~55 lines down by an unrelated verify-readback insertion earlier in the file.","recommend":"build — gate the DM on booking-channel/role so an email-triggered booking cannot Slack-DM a colleague on the owner's behalf","note":"The engine flagged this as unconfirmed because its `evidence` field named no file while the citation sat in `whereNow`. The lane did open the code and did name the new location; that citation is folded into this recheck line so the row is genuinely confirmed. The engine's check reading only `evidence` and ignoring `whereNow` is filed as a framework row."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"matchmaker","source":"audit","ref":"classifier-guess-reaches-real-calendar-invite","recheck":"moved 2026-07-31, still real, now at src/skills/meetings/ops/handlers/createMeeting.ts:144-166 — read verbatim: context.resolvedMeetingAttendees is still unioned into the real invite unconditionally at :151-166. Same defect, shifted ~6 lines by an added comment block.","recommend":"build — gate the union so a classifier guess never reaches a real invite without confirmation","invariant":"classifier-guess-reaches-a-real-world-action","note":"SAME ROOT as this run's gh#158-a (Yael resolved onto a Levana booking) — one root, one gate. The engine flagged this as unconfirmed because the citation sat in `whereNow` rather than `evidence`; it is folded into this recheck line."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"matchmaker","source":"audit","ref":"order-violation-subject-unmasked","recheck":"still-real 2026-07-31: read verbatim at src/skills/meetings/ops/handlers/createMeeting.ts:689 — the order_violation error message interpolates the raw `predecessor.subject` with no privacy mask, same as filed.","recommend":"build — mask predecessor.subject the same way other colleague-readable refusals mask a private meeting's subject"}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"matchmaker","source":"audit","ref":"movemeeting-comment-still-says-self-only","recheck":"still-real 2026-07-31: read verbatim at src/skills/meetings/ops/handlers/moveMeeting.ts:104 ('we (a) gate colleague-path to self-only') versus :155-210, which implements the v3.1.4 requester-controls gate, not self-only.","recommend":"build — one-line comment correction, no behaviour change"}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"outrider","source":"audit","ref":"brief-omits-news-with-no-user-facing-word","recheck":"still-real 2026-07-31: read verbatim at src/tasks/briefs.ts:719-725 — a timed-out or empty news gather only logs WARN/INFO; nothing reaches the composed brief text, so the owner is never told why Updates is missing.","recommend":"build — surface a short user-facing note when news was dropped for a timeout versus genuinely empty"}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"outrider","source":"audit","ref":"nested-news-timeout-not-derived-from-inner-budget","recheck":"still-real 2026-07-31: read verbatim at src/tasks/briefs.ts:31-48 — the outer/inner timeout relationship is asserted only in prose comments ('don't lower this back below the inner budget without re-timing both sides'), with no code-level assertion or derivation tying the two constants together.","recommend":"build — derive the outer timeout from the inner one, or assert it at startup, so an edit to either cannot silently invert the relationship"}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"outrider","source":"audit","ref":"prompt-budget-grew-1836-chars-unmeasured-by-anything","recheck":"still-real 2026-07-31: no metering code found anywhere in the prompt-building path (systemPrompt.ts, skill.ts, meetings.ts) — the +1,836 char growth remains uncounted by any mechanism. A standing observation, not a single fixable line.","recommend":"defer — needs a metering mechanism designed rather than a one-line fix; low urgency"}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"instructor","source":"audit","ref":"owner-memory-injected-into-clamped-mpim","recheck":"still-real 2026-07-31: read verbatim at src/core/orchestrator/systemPrompt.ts:779-810 — speakerMemoryBlock and verifiedSenderBlock both still gate on `if (isOwner || !senderId) return ''`, same as filed; no fix landed.","recommend":"build — gate both blocks on the same isOwnerPath condition buildTurnContext.ts:576-585 already uses for personWorkBlock, so a clamped MPIM cannot render the owner's own memory file into a colleague-readable group thread"}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"instructor","source":"audit","ref":"honest-refusal-rule-is-mpim-only","recheck":"still-real 2026-07-31: read verbatim at src/core/orchestrator/systemPrompt.ts:420-449 — the honest-refusal instruction ('you have NO accessible person data on this turn… NEVER assert a specific negative') lives only inside the isOwnerInGroup branch at :433; the colleague/channel branch at :439-449 carries no equivalent.","recommend":"build — extend the same honest-refusal instruction to the non-MPIM colleague/channel branch"}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"instructor","source":"audit","ref":"send-briefing-now-desc-promises-standalone-dm","recheck":"still-real 2026-07-31: read verbatim — src/tasks/skill.ts:290 still promises 'a new standalone DM - not as a reply in this thread', while :598 passes context.threadTs through and src/tasks/briefs.ts:844-845 honours threadTs on the post when supplied.","recommend":"build — fix the tool description to match the actual in-thread behaviour when threadTs is set"}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"profiler","source":"audit","ref":"gh#51","recheck":"still-real 2026-07-31: read verbatim — src/connectors/whatsapp.ts:273-287 still drops every non-owner sender silently before any body is read (the Step 1 trust gate), so Hebrew gender detection per the ticket still has nowhere to wire into on this transport.","recommend":"decline — already ruled needs-owner-decision with the ticket's premise rewritten and redirected to Slack; nothing new to build here"}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"profiler","source":"audit","ref":"B157-c","recheck":"still-real 2026-07-31: read verbatim at src/core/orchestrator/systemPrompt.ts:137-139 — peopleSection is nulled when !isOwner, confirmed still the case; matches gh#157's Yael-history complaint exactly and was emitted as issue gh#157-b this run.","recommend":"build — the narrow honesty half; the wider disclosure-policy question stays the owner's, per this row's own note","note":"MERGED into gh#157-b for this run's report — one row on his desk, not two."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"matchmaker","source":"both","ref":"gh#164","finding":"Maelle told Michal 16:15 was too soon, Michal said 'ok so squeeze into 16:15', and she pushed back again saying it was not available after all","rootCause":"src/utils/scheduleRules.ts:340-346 — isWithinBookingLeadTime measures the 4h colleague buffer from the clock at the instant it is called, so 16:15 was legal until 12:15 and illegal after; the same function was right both times","verdict":"declined","state":"open","note":"OWNER DECLINED 2026-07-31: 'so decline.' Nothing changed — the CLOCK moved. His earlier ruling the same day chose narration over a safety margin ('it is free, it keeps M6 intact, and the complaint in the ticket was that the pushback read as arbitrary — not that 16:15 got offered'), and that narration shipped in 4.3.5 at meetings.ts:1377 and is deployed at cf30a52. The only mechanical alternative was withholding real, currently-legal near-cutoff slots, which he refused. The offer-then-refuse sequence stands by decision, not by oversight. THE GITHUB TICKET CLOSES AT THE NEXT WRAP — it was commented PARTIAL at the 4.3.5 wrap and this decline retires the remaining half."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"profiler","source":"github","ref":"gh#51","finding":"Save Hebrew gendered forms per person on the WhatsApp transport","rootCause":"src/connectors/whatsapp.ts:273-287 — the Step 1 trust gate drops every non-owner sender silently before any message body is read, so there is nowhere on that transport for the ticket's fix to wire into","verdict":"declined","state":"open","note":"OWNER DECLINED 2026-07-31: 'we do not have any whatsapp for now, decline.' Re-read and confirmed still-real earlier the same run, then declined on the PRODUCT fact rather than the code fact — the transport is dormant, so the row cannot earn a slot. Stays visible so nobody re-raises it. If WhatsApp is ever revived this is the first row to reopen."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"profiler","source":"github","ref":"gh#51 (profiler)","finding":"duplicate ref for gh#51 — the same WhatsApp Hebrew-gender ticket written with a lane suffix","verdict":"declined","state":"open","note":"SAME FINDING as gh#51, closed by the same ruling ('we do not have any whatsapp for now, decline'). Recorded separately only because the malformed ref kept it as its own line in --open carrying NO VERDICT AT ALL. One finding, two spellings; this retires the second."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"profiler","source":"both","ref":"gh#157-b","finding":"Asked what she knows about Yael Aharon in an MPIM, Maelle said 'no history of meetings or notes with her, this looks like our first real interaction' — that person row holds 44 notes","rootCause":"src/core/orchestrator/systemPrompt.ts:137-139 — peopleSection is nulled when !isOwner, and registry.ts:368-450 excludes the person-memory tools on the same turn shape, so she has zero data and zero tools and reports the absence as a finding rather than as a limit","verdict":"converted","state":"open","note":"→ gh#154 'Owner authority across chat surfaces'. OWNER'S INSTRUCTION 2026-07-31: 'close this row and add a comment to the github ticket that work on the permission layer to MPIM to make sure it will be resolved there.' The narrow honesty half already shipped for the MPIM branch in 4.3.5; what remains is the PERMISSION LAYER question — whether a clamped MPIM turn gets a scoped person-data read so she can say 'I do have history with her but I do not share notes in a group thread' — and that is the same design question gh#154 already holds for rows 103, 112 and W3. He explicitly refused a further partial fix. gh#157 commented rather than closed: two of its three complaints remain."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"profiler","source":"logs","ref":"B157-c","finding":"the clamped-turn person-data blackout, reported as a specific false negative about a colleague","verdict":"converted","state":"open","note":"→ gh#154 'Owner authority across chat surfaces'. SAME FINDING as gh#157-b, merged for this run's report at the Manager's call and closed by the same owner ruling. Recorded separately because it survived as its own line in --open. Re-read and confirmed still-real earlier in this same run at systemPrompt.ts:137-139, so this is a closure by DECISION, not by a fix."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"profiler","source":"github","ref":"gh#157-no-history-with-colleague","finding":"she told Yael she has 'no history of meetings or notes' with a colleague whose record holds 44 notes","verdict":"converted","state":"open","note":"→ gh#154 'Owner authority across chat surfaces'. THIRD SPELLING of gh#157-b / B157-c — one finding, three refs, all closed by the owner's single 2026-07-31 ruling. This one had been deferred to 2026-07-30 and never came back, which is exactly how three refs for one bug accumulate."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"instructor","source":"audit","ref":"honest-refusal-rule-is-mpim-only","finding":"the honest-refusal rule lives only inside the isOwnerInGroup branch, so a clamped owner in a CHANNEL takes a different path and can still fabricate the same specific negative","rootCause":"src/core/orchestrator/systemPrompt.ts:420-449 — the instruction sits at :433 inside the owner-in-group branch; the colleague/channel branch at :439-449 carries no equivalent","verdict":"converted","state":"open","note":"→ gh#154 'Owner authority across chat surfaces'. OWNER'S INSTRUCTION 2026-07-31: 'close it and ref to gh#154 solution, no need for partial fix.' This was report row o#179 and the Manager had recommended BUILD; he overruled that in favour of solving the per-surface question once in the permission layer rather than spreading the same prose to a second branch. Same judgement he made on gh#157-b in the same message, and it is the M6 read — four rows now point at one missing seam rather than four missing patches."}
{"date":"2026-07-31","runId":"wf_4bbfc750-1a9","lane":"matchmaker","source":"logs","ref":"gh#158-availability-answered-about-owner-not-attendee","finding":"'is Levana free tomorrow at 10am?' was answered about the OWNER's working hours rather than hers, until he said 'you are not answering what I am asking'","rootCause":"src/utils/availabilityPreCheck.ts:494-541 — the colleague-path owner-scoped point-check had no concept of a named third party's calendar","verdict":"already-fixed","state":"wrapped","note":"CLOSED on this run's own proof, not on assumption: gh#158-b re-derived the same root and established the repair is present and DEPLOYED — the bail at availabilityPreCheck.ts:517-541 returns ran:false whenever resolvedMeetingAttendees is non-empty, shipped 2026-07-30 20:44 in 4.3.4 (1754444) and live at cf30a52. This row had been DEFERRED to 2026-07-30 and never came back, so it sat open across the release that fixed it. The ref `gh#158-availability-owner-not-attendee` is NOT closed by this — its latest row describes the over-broad bail trigger, a different and still-live finding."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"matchmaker","source":"owner","ref":"o#178","finding":"A colleague asking to book something that must follow another meeting got that meeting's REAL subject back in the refusal, private or not","rootCause":"src/connectors/graph/calendarReads.ts:1149 — getEventEndInstant's Graph .select fetched only id,subject,end, so it carried no sensitivity/categories for isEventPrivate to read; createMeeting.ts:689 then interpolated predecessor.subject raw","verdict":"built","state":"built","note":"getEventEndInstant now also selects sensitivity+categories (additive, widened return); the order_violation refusal computes displaySubject(predecessor, context.profile, subjectViewerFor(context)) — the SAME helper Guard B already calls at :431, no second masking path. THE REAL ROOT WAS ONE LAYER DEEPER THAN THE ROW SAID: the row named the interpolation, but masking there would have been inert because the fetch never carried the privacy fields. Traced: private predecessor -> [Private]; owner outside an MPIM -> raw subject unchanged per M12; owner in MPIM -> masked; ordinary non-private predecessor -> byte-identical to before, so no over-redaction. The other caller of getEventEndInstant reads .end/.subject only in a log line."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"matchmaker","source":"owner","ref":"gh#158-a","finding":"Idan asked to book with Levana Bagants; the attendee pre-resolution named Yael Aharon, resolved her to yael.a@reflectiz.com, and Maelle then insisted she had only checked Levana's and Idan's calendars","rootCause":"src/core/orchestrator/buildTurnContext.ts:203 — turnMeetingPeople was assigned straight from classifyTurn with no check that a returned name is present in THIS turn's raw message, and classifyTurn is fed up to 4 turns of recentContext, so a name from history flowed unchecked onto the real invite via createMeeting.ts:151-166 and findAvailableSlots.ts:452-469","verdict":"built","state":"built","invariant":"classifier-guess-reaches-a-real-world-action","note":"Gated at the ONE producer, not the three consumers: each returned name is kept only if it appears as a case/whitespace-normalized substring of the current turn's raw message. This leans on classifyTurn's own documented contract ('copy them as written, do NOT invent, translate, or normalize') — a genuinely-named attendee is by definition a substring, so an ordinary booking is untouched and gains no confirmation step. Traced: the reported bleed case drops Yael and keeps Levana; 'book 30 min with Simon' unchanged; Hebrew/RTL safe because toLowerCase is a no-op on Hebrew and the check matches the verbatim script; punctuation and double-space variance handled by the \\s+ collapse. DELIBERATELY OUT OF SCOPE and named by the lane: a same-turn disputing message ('you didn't check Yael, book with Levana') keeps Yael because she IS textually present — that discrimination is classifyTurn's semantic job, not this structural gate's. ALSO CLOSES the backlog row classifier-guess-reaches-real-calendar-invite, which was the same root."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"matchmaker","source":"owner","ref":"classifier-guess-reaches-real-calendar-invite","finding":"a person the owner never asked to invite can end up on a REAL calendar invite","verdict":"built","state":"built","invariant":"classifier-guess-reaches-a-real-world-action","note":"CLOSED BY gh#158-a — one change, do not count two fixes. The gate landed at the producer (buildTurnContext.ts:203) rather than at the union this row cited, which is the deeper fix: the union has three consumers and the producer has one."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"matchmaker","source":"owner","ref":"gh#165-d","finding":"A colleague asking to book over the owner's all-day day off was steered to 'add attendees' to the vacation block — and the mechanism gh#165-b shipped for this had never fired once","rootCause":"src/connectors/graph/findAvailableSlots.ts:1108-1121 — the !verdict.passes early-continue sat ABOVE the only writer of diagnosticsOut.conflictingEvent, so a colleague's STRICT single-slot check could never learn which event it collided with; and scheduleRules.ts carried only the OOF-gated allDayOutOfOffice, with no broader 'is this all-day at all' fact","verdict":"built","state":"built","invariant":"fact-published-on-a-path-its-reader-never-takes","note":"RESEND after the owner refused the first attempt ('if the verify refuse, it has a reason and this solution is wrong'), built from a tree reverted to HEAD. All three prescribed parts landed: (1) the single writer was factored into stampConflictingEvent() and called from BOTH the strict-reject branch — new, and the actual fix — and the existing relaxed-pass branch, so gh#165-b's steer is reachable for the first time since it shipped; (2) isAllDay?: true added to RuleCheckResult['overCommitment'] at the shared root, set from the same ev.isAllDay read that produces window, so no string-matching a display value and no fifth local predicate; (3) the all-day branch suppresses the add-attendees steer entirely and reuses the existing owner_out_of_office label only when OOF is CONFIRMED, keeping M11 — a held all-day block that is not actually OOF is not 'he is away'. The lane kept isAllDay and allDayOutOfOffice distinct on purpose: the broad one suppresses the steer, the narrow one governs the wording."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"matchmaker","source":"verify","ref":"gh#165-d>dep","finding":"Make the conflict fact reachable at the root, then keep the steer honest","verdict":"built","state":"built","invariant":"fact-published-on-a-path-its-reader-never-takes","note":"CLOSED BY gh#165-d — SAME CHANGE, do not count two fixes. All three parts of the ask landed in that one dispatch."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"matchmaker","source":"logs","ref":"gh#165-b>dep","finding":"suppress the update_meeting(add_attendees) steer when the conflicting event is an all-day out-of-office","verdict":"built","state":"built","note":"CLOSED BY gh#165-d — same change. This row had been open since gh#165-b shipped, describing a suppression that could not have worked because the field it keyed on was never populated on the colleague path."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"matchmaker","source":"verify","ref":"vacation-day-reported-as-already-booked","finding":"a colleague asking to book on a day the owner is on vacation is told the time is 'already booked', which invites 'then what about 30 minutes later?' and the answer is the same all day","verdict":"built","state":"built","note":"CLOSED BY gh#165-d, and verified by reading the code rather than taking the claim: createMeeting.ts:624-631 now distinguishes the two cases the row said were collapsed — allDayOutOfOffice yields the owner_out_of_office label, any other all-day commitment yields 'blocked all day by another commitment', and :652 replaces the refusal text with 'his whole day is already taken by X … there is no meeting there to add anyone to'. The 'ask 30 minutes later' loop is closed because the reply now says the whole day."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"matchmaker","source":"both","ref":"gh#168-a","finding":"Booking with Tyler in Eastern time, Maelle asserted 'no genuine overlap Monday', self-corrected to a wrong reason, then corrected again to a third answer","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts — day_summary reported only top_reasons/blocked_by for a day rejected on outside_attendee_work_hours and never a computed fact about what the overlap actually IS, so the model converted the two zones itself and got the window backwards","verdict":"built","state":"built","invariant":"timezone-math-is-code-never-inference","note":"ROOT ESTABLISHED FROM THE RUNTIME, not the design: logs/maelle-2026-07-30.log:390-478 plus the full transcript in conversation_threads (thread_ts=1785419720.832769). Reply 2 ('why does Monday fall outside Tyler's hours') had no tool-provided overlap fact to quote, so it did its own arithmetic and reported '3:30-7:30am ET' — which were actually the relaxed-recovery's off-hours candidate slots, not the true office-hours overlap. FIX: attendeeHoursGroundingNotes() computes the blocked attendee's stored hours into a real window for that exact date using the existing reinterpretClockInZone, attached as attendee_hours_note and wired once so every return branch inherits it — the same 'grounded string, never mental arithmetic' pattern this file already uses for _requested_time_local. THE VERIFY OVERTURNED IT ONCE and the Manager sent it straight back rather than to the owner, because the verify itself said 'not the owner's decision — a wording defect inside the lane's own new string'."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"matchmaker","source":"verify","ref":"gh#168-a>dep","finding":"the grounded note said 'convert to HH:mm-HH:mm in your own zone' while the numbers are the OWNER's zone, and find_available_slots is colleague-allowed with day_summary attached under no role gate — so on a colleague turn the note told the model the owner-zone window belonged to the READER, then instructed it to quote those numbers verbatim","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts:142","verdict":"built","state":"built","invariant":"timezone-math-is-code-never-inference","note":"Fixed by naming the zone instead of using a deictic pronoun: 'convert to HH:mm-HH:mm in <ownerTz> (<OwnerFirst>'s zone)', matching the file's existing non-deictic patterns at :283 and :1030. One string plus an ownerFirstName param threaded to the single call site at :953. NO role gate added and the zone is not re-derived — the owner-zone window is legitimate information on a colleague turn; the defect was that it was MISLABELLED. Worth recording why this mattered more than its size: a grounded string carries more authority than the model's own arithmetic, so a mislabelled one manufactures exactly the error the note was built to prevent. NO SEPARATE VERIFY PASS was run on this fix (X37): it is one string literal inside text the wave's Opus pass had already read and prescribed the replacement for, and a full pass over it would be disproportionate. Typecheck green; the combined tree typechecks clean."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"outrider","source":"owner","ref":"o#180","finding":"The morning brief arrives with no Updates section and no explanation, so the owner cannot tell whether there was no news or the fetch timed out","rootCause":"src/tasks/briefs.ts:719 — the if (!gathered) branch is the only place that can distinguish a Promise.race timeout from a completed-but-empty gather, and it only logged a WARN; nothing reached the composer, so a timed-out pass and a quiet news day produced byte-identical output","verdict":"built","state":"built","note":"A newsTimedOut flag set only in the timeout branch, threaded to generateBriefingText as a trailing optional param, deriving newsIncomplete = !hasNews && newsTimedOut. Two additive effects: the zero-items early return appends one short sentence instead of the plain 'All clear', and the newsBlock instruction now also fires on newsIncomplete telling the composer to add exactly ONE short line noting the check did not finish — explicitly forbidding invented content, and distinct from the existing 'omit entirely, no apology' instruction which still governs the genuinely-quiet day. THIS IS THE HALF gh#167 NEVER BUILT: he approved gh#167 on 2026-07-30 and the lane shipped it log-only by design, which is why he asked 'didn't we approve it many times?'."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"instructor","source":"owner","ref":"o#177","finding":"On a clamped group turn the owner's OWN memory file renders into a colleague-readable context — 'MEMORY ON IDAN' plus his full learned .md, plus his own identity as the VERIFIED SENDER","rootCause":"src/core/orchestrator/systemPrompt.ts:780/800 — speakerMemoryBlock and verifiedSenderBlock gated their early return on isOwner alone (the post-clamp EFFECTIVE role), which a clamped MPIM forces to 'colleague' at buildTurnContext.ts:35-42, while senderId stays the owner's raw slack id — so getPersonMemory(senderId) resolved to his own row","verdict":"built","state":"built","invariant":"owner-clamp-fixed-per-surface","note":"isOwnerTyping = isOwner || isOwnerInGroup, reusing the exact split buildTurnContext.ts already uses to gate its sibling personWorkBlock against this identical leak. Both guards changed from isOwner to isOwnerTyping. +12/-2, one file, PURE NARROWING — it removes rendering in exactly one additional case and can never widen what a path already saw, so token effect is a reduction on that path and zero elsewhere. READ THE DISCOVERY BESIDE THIS ROW BEFORE CALLING IT DONE: isOwnerInGroup is computed MPIM-ONLY at processMessage.ts:128 while the senderRole clamp also fires for CHANNELS at :139, so the same leak is still live in a real channel. This fix is itself per-surface — the shape the owner had just refused twice in the same session and sent to gh#154."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"instructor","source":"verify","ref":"owner-memory-still-renders-in-a-real-channel","finding":"In a real Slack CHANNEL where the owner posts, his own learned memory file and a VERIFIED SENDER stanza about him can still render into a surface every channel member reads — the same leak o#177 just closed for group DMs","rootCause":"src/core/orchestrator/systemPrompt.ts:79 — isOwnerTyping is isOwner || isOwnerInGroup, and isOwnerInGroup is computed MPIM-only at src/connectors/slack/app/processMessage.ts:128 while the senderRole clamp to 'colleague' also fires for channels at :139. On a channel turn isOwner is false, isOwnerInGroup is false, senderId is still the owner's, and both blocks at :790/:810 render","verdict":"queued-next-run","recommend":"build — o#177's own fix is per-surface and this is the surface it left open; the predicate that needs widening is isOwnerInGroup at processMessage.ts:128, not a third guard in systemPrompt.ts","state":"open","invariant":"owner-clamp-fixed-per-surface","note":"VERIFY DISCOVERY, severity medium, instructor. THE MOST IMPORTANT THING IN THIS WAVE'S RETURN, because it is the pattern the owner named twice in the same session: he closed gh#157-b and o#179 to gh#154 with 'no need for partial fix' precisely because every owner-clamp fix so far has been per-surface, and the fix he approved minutes later is per-surface too. Channels are a WIDER audience than an MPIM, so the surface left open is the worse one. This belongs in gh#154's design rather than as a fifth patch — but note it is a ONE-PREDICATE change (isOwnerInGroup), which is an argument for doing it now and letting gh#154 own the policy."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"matchmaker","source":"verify","ref":"candidate-validation-branch-has-no-grounded-window","finding":"The same 'why is that day excluded?' question gets un-grounded, model-computed timezone arithmetic when the caller validated specific candidate times instead of searching a window — identical to the incident gh#168-a fixed, on the sibling branch","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts:904-912 — the candidate_validation return carries broken_rule_label (a time-free phrase for outside_attendee_work_hours) and _requested_time_local, but no computed attendee-hours window","verdict":"queued-next-run","recommend":"build — the mechanism now exists as attendeeHoursGroundingNotes(); this is calling it from the sibling branch, not designing anything","state":"open","invariant":"timezone-math-is-code-never-inference","note":"VERIFY DISCOVERY, severity low, matchmaker. The lane declared this branch deliberately out of scope and said so, which is the right call for one dispatch — but it means the gh#168 class of error is closed on one branch and open on its sibling. Cheap now that the helper exists."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"matchmaker","source":"verify","ref":"isallday-read-through-as-any-six-times","finding":"A one-character typo in the hard scheduling validator's all-day detection would compile clean and silently disable every all-day protection — vacation days would read as free and nothing would fail","rootCause":"src/utils/scheduleRules.ts:609 (and :513, :902, :957, :958) read the structural fact as (ev as any).isAllDay because RuleCheckInput's events type does not declare it; gh#165-d added a sixth instance at :613 following the house pattern","verdict":"queued-next-run","recommend":"build — declare isAllDay on RuleCheckInput's event type and delete all six casts; it is a type declaration plus six deletions, and it removes a silent-failure mode from the hard validator","state":"open","invariant":"structural-fact-read-through-any","note":"VERIFY DISCOVERY, severity low as rated, but read the failure mode before agreeing with the rating: `as any` removes the only check that would catch a misspelling in the predicate that protects the owner's days off, and this wave made it load-bearing for the day-off steer suppression too. CalendarEvent already declares isAllDay: boolean (calendarTypes.ts:8), so the fix is declaring it on the validator's own input type — no runtime change at all."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"matchmaker","source":"verify","ref":"three-dead-geteventendinstant-imports","finding":"Three meeting handlers import a Graph function they never call — dead code that makes a reader believe those files anchor to event ends, and that a future privacy change to getEventEndInstant must consider them","rootCause":"src/skills/meetings/ops/handlers/moveMeeting.ts:17, findAvailableSlots.ts:17 and calendarReads.ts:17 each import getEventEndInstant with exactly one occurrence per file — the import and no call site","verdict":"queued-next-run","recommend":"build — three line deletions, and this wave is exactly the case the row warns about: o#178 changed getEventEndInstant's return type and three files falsely appeared to be affected","state":"open","note":"VERIFY DISCOVERY, severity low, matchmaker. Left over from the pass-B extraction that copied the import block verbatim. Cheapest row in the ledger."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"matchmaker","source":"verify","ref":"broken-rule-label-is-a-verb-phrase-in-an-adjective-slot","recheck":"STILL REAL 2026-07-31 after gh#165-d, and the lane's ticketCoverage claim that it is 'gone for busy collisions' is WRONG — checked by reading the code rather than recording the claim. createMeeting.ts:655 still renders `That time is ${brokenRuleLabel} for ${ownerFirst}`, and for a plain TIMED collision :632 still yields labelFor('owner_busy_collision') = 'conflicts with another meeting on Idan's calendar'. So 'That time is conflicts with another meeting on Idan's calendar for Idan.' is still live, and the timed conflict is the COMMON case. What gh#165-d did fix is the all-day case, which takes :652's own sentence and never reaches :655 — a branch that did not exist before this wave.","recommend":"build — one template fix at createMeeting.ts:655, and the model is still told to paste that exact phrase verbatim into create_approval ask_text","verdict":"queued-next-run","state":"open","invariant":"fact-published-on-a-path-its-reader-never-takes","note":"KEPT OPEN DELIBERATELY. Recording this rather than the lane's claim is the 'trace the runtime, not the description' rule applied to a verdict return: a coverage sentence is a description, and closing a row on one is how a false closure enters the ledger."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"matchmaker","source":"audit","ref":"order-violation-subject-unmasked","finding":"a colleague can be told the real subject of a meeting the owner marked private via the order_violation refusal","verdict":"built","state":"built","note":"CLOSED BY o#178 — ONE CHANGE, do not count two fixes. MANAGER BOOKKEEPING DEFECT, recorded rather than quietly fixed: when this backlog row was promoted into report.md it was given a NEW report id (o#178) and built under that id, leaving this original ref open. That is the same one-finding-many-refs duplication the Manager criticised on gh#157 (three refs) and gh#51 (two) EARLIER THE SAME DAY, and it created three more instances in one wave. THE RULE THAT WAS MISSING: a promoted backlog row keeps its slug as the ledger `ref` and the o# number is only its handle on the report — or, if a new ref is minted, the closure must name both. Filed as a framework row."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"outrider","source":"audit","ref":"brief-omits-news-with-no-user-facing-word","finding":"the morning brief arrives with no Updates section and nobody is told why","verdict":"built","state":"built","note":"CLOSED BY o#180 — ONE CHANGE, do not count two fixes. Same promoted-row aliasing defect as order-violation-subject-unmasked: built under the new report id while this original ref stayed open."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"instructor","source":"audit","ref":"owner-memory-injected-into-clamped-mpim","finding":"the owner's own memory file is injected into a colleague-readable MPIM turn","verdict":"built","state":"built","note":"CLOSED BY o#177 — ONE CHANGE, do not count two fixes. Same promoted-row aliasing defect. NOTE THE RESIDUAL IS A DIFFERENT ROW, not this one: owner-memory-still-renders-in-a-real-channel covers the channel surface o#177 left open, and it is genuinely open."}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"slacker","source":"verify","ref":"owner-memory-still-renders-in-a-real-channel","recheck":"RE-ROUTED 2026-07-31 from instructor to TRANSPORTER, and the reason matters for whoever picks it up: the SYMPTOM is in systemPrompt.ts (instructor's file) but the durable fix is `isOwnerInGroup` at src/connectors/slack/app/processMessage.ts:128, which is the owner-clamp computation — Transporter's territory by the seam rule (authority by authenticated sender, DM vs MPIM vs channel posture). M3: route by where the fix lives, not where the symptom appeared. A third guard inside systemPrompt.ts would be the fifth per-surface patch on this ground.","recommend":"build — one predicate at processMessage.ts:128; widening it to cover channels closes the surface o#177 left open, and channels are the WIDER audience","state":"open","invariant":"owner-clamp-fixed-per-surface"}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"matchmaker","source":"audit","ref":"slot-hold-release-dm-role-gate","recheck":"ALIAS RECORDED 2026-07-31: this row is report row o#181. Same finding, one ledger ref — when it is built, close THIS ref and name o#181 in the note, so the promoted-row aliasing defect is not repeated.","recommend":"build — gate the DM on booking-channel/role so an email-triggered booking cannot Slack-DM a colleague on the owner's behalf","state":"open"}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"instructor","source":"audit","ref":"send-briefing-now-desc-promises-standalone-dm","recheck":"ALIAS RECORDED 2026-07-31: this row is report row o#182. Same finding, one ledger ref.","recommend":"build — fix the tool description to match the actual in-thread behaviour when threadTs is set","state":"open"}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"outrider","source":"audit","ref":"nested-news-timeout-not-derived-from-inner-budget","recheck":"ALIAS RECORDED 2026-07-31: this row is report row o#183. Same finding, one ledger ref.","recommend":"build — derive the outer timeout from the inner one, or assert it at startup, so an edit to either cannot silently invert the relationship","state":"open"}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"matchmaker","source":"audit","ref":"movemeeting-comment-still-says-self-only","recheck":"ALIAS RECORDED 2026-07-31: this row is report row o#184. Same finding, one ledger ref.","recommend":"build — one-line comment correction, no behaviour change","state":"open"}
{"date":"2026-07-31","runId":"wf_6f4ad121-d9a","lane":"outrider","source":"audit","ref":"prompt-budget-grew-1836-chars-unmeasured-by-anything","recheck":"ALIAS RECORDED 2026-07-31: this row is report row o#185. Same finding, one ledger ref.","recommend":"defer — needs a metering mechanism designed rather than a one-line fix; low urgency","state":"open"}
{"date":"2026-07-31","runId":"wf_6e41a1e4-f2c","lane":"matchmaker","source":"owner","ref":"slot-hold-release-dm-role-gate","finding":"An email-triggered booking could Slack-DM a colleague on the owner's behalf","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:1444 — the hold-release loop's only guard was the holder check, then it called getConnection(ownerId,'slack') and sendDirect regardless of which transport the booking arrived on; getConnection is hardcoded to 'slack' so it resolves even on an email turn","verdict":"built","state":"built","note":"+11/-0. `if (context.channel !== 'slack') { log; continue; }` after the holder check, before getConnection. THE HOLD RELEASE ITSELF IS UNTOUCHED — only the outbound DM is gated, so the slot is still freed on an email booking. Traced all five paths: slack unaffected; email and whatsapp skip and log; no-holder still caught by the pre-existing check; and a DEFERRED APPROVAL REPLAY still notifies, because the replay machinery stamps channel 'slack' (requests/runner.ts:395, deferredActionReplay.ts:79) and that replay genuinely happens on the Slack surface. Report row o#181 — same finding, one ledger ref, per X101."}
{"date":"2026-07-31","runId":"wf_6e41a1e4-f2c","lane":"matchmaker","source":"verify","ref":"broken-rule-label-is-a-verb-phrase-in-an-adjective-slot","finding":"'That time is conflicts with another meeting on Idan's calendar for Idan' — a verb phrase in an adjective slot, and the model is told to paste that phrase verbatim into create_approval ask_text","rootCause":"src/skills/meetings/ops/violationLabels.ts:29 and :31 — owner_busy_collision and its legacy alias returned a verb phrase while every other label in the switch is an adjectival fragment built to slot into 'That time is X'","verdict":"built","state":"built","invariant":"fact-published-on-a-path-its-reader-never-takes","note":"+2/-2 plus a 3-line comment. FIXED AT THE TWO LABELS, NOT THE TEMPLATE — 'in conflict with another meeting on X's calendar' — so there is one spelling and no template special-case. Checked all four consumers rather than just the reported one: createMeeting.ts:655 now reads grammatically, moveMeeting.ts:739/:755 reads at least as well as before, findAvailableSlots.ts:869/:1436/:1448 reads correctly, and gh#165-d's separate all-day sentence never calls labelFor so it is untouched by design. Grepped for any other consumer of the old exact string: none in code."}
{"date":"2026-07-31","runId":"wf_6e41a1e4-f2c","lane":"matchmaker","source":"verify","ref":"isallday-read-through-as-any-six-times","finding":"A one-character typo in the hard scheduling validator's all-day detection would compile clean and silently disable every all-day protection","rootCause":"src/utils/scheduleRules.ts:513, 609, 613, 902, 957, 958 — six `(ev as any).isAllDay` casts on values already typed CalendarEvent (RuleCheckInput.events at :220), where isAllDay is already a required boolean (calendarTypes.ts:8)","verdict":"built","state":"built","invariant":"structural-fact-read-through-any","note":"THE BRIEF WAS WRONG ABOUT THE FIX AND THE LANE CORRECTED IT: I said to declare isAllDay on RuleCheckInput's event type, but the field was ALREADY declared non-optional on the type actually in use — the casts were dead defensive scaffolding, not a missing declaration. So the fix is six cast removals and nothing else, byte-identical at runtime, and typecheck green proves the direct property access is legal. Smaller and better than the change asked for."}
{"date":"2026-07-31","runId":"wf_6e41a1e4-f2c","lane":"matchmaker","source":"verify","ref":"candidate-validation-branch-has-no-grounded-window","finding":"'Why is that day excluded?' still got model-computed timezone arithmetic when the caller validated specific candidate times rather than searching a window — the same failure gh#168-a fixed, on the sibling branch","rootCause":"src/skills/meetings/ops/handlers/findAvailableSlots.ts:864-870 — the candidate_validation per-result branch computed broken_rule_label but never called the existing attendeeHoursGroundingNotes() at :119-149","verdict":"built","state":"built","invariant":"timezone-math-is-code-never-inference","note":"+16 lines. Derives the blocked email from the brokenRule string, which already carries an `outside_attendee_work_hours:<email>` suffix from trackReject, then calls THE SAME helper gh#168-a added and attaches attendee_hours_note mirroring the day-level field name. NO NEW PHRASING WAS WRITTEN — reusing gh#168-a's string is what carries its named-zone correction forward automatically rather than by anyone remembering to restate it."}
{"date":"2026-07-31","runId":"wf_6e41a1e4-f2c","lane":"matchmaker","source":"verify","ref":"three-dead-geteventendinstant-imports","finding":"Three meeting handlers imported a Graph function they never call","rootCause":"moveMeeting.ts:17, handlers/findAvailableSlots.ts:17, handlers/calendarReads.ts:17","verdict":"built","state":"built","note":"-3 lines. Verified exactly one occurrence per file before deleting. This wave is why it mattered: o#178 widened getEventEndInstant's return type and three files falsely appeared affected."}
{"date":"2026-07-31","runId":"wf_6e41a1e4-f2c","lane":"matchmaker","source":"audit","ref":"movemeeting-comment-still-says-self-only","finding":"moveMeeting.ts:104's comment said the colleague path is gated to self-only while :155-210 implements the v3.1.4 requester rule","verdict":"built","state":"built","note":"Comment-only, +3/-1. Reworded to the SAME words the tool description already uses at meetings.ts:482/:495/:508, with a pointer to those lines, so the rule has one spelling rather than a second. Report row o#184."}
{"date":"2026-07-31","runId":"wf_6e41a1e4-f2c","lane":"outrider","source":"audit","ref":"nested-news-timeout-not-derived-from-inner-budget","finding":"The news budget could invert again on an edit to either side, and the symptom is silent — the Updates section just vanishes","rootCause":"src/tasks/briefs.ts:39-48 — two independent literals whose required relationship to news.ts's private NEWS_PER_GOAL_TIMEOUT_MS was asserted only in a prose comment in a different file","verdict":"built","state":"built","note":"Exported NEWS_PER_GOAL_TIMEOUT_MS from news.ts as the single source of truth, then DERIVED both outer values: scheduled = inner + 8_000 = 20_000, on-demand = inner + 2_000 = 14_000. BOTH DEPLOYED NUMBERS UNCHANGED, so it is a structural fix with zero behaviour delta. The lane RE-VERIFIED the margins against production rather than trusting the comment: gather completes 4.1s / 4.4s / 7.4s after the preceding step across three days of logs. It read o#180 first as instructed and noted correctly that the two are complementary — a derivation stops the inversion, the user-facing note stops it being silent. Report row o#183."}
{"date":"2026-07-31","runId":"wf_6e41a1e4-f2c","lane":"instructor","source":"audit","ref":"send-briefing-now-desc-promises-standalone-dm","finding":"The model was told send_briefing_now posts 'a new standalone DM - not as a reply in this thread', and it posts in-thread","rootCause":"src/tasks/skill.ts:290","verdict":"built","state":"built","invariant":"tool-description-matches-actual-behavior","note":"BUILT ON A RESEND, AND THE FAULT WAS THE MANAGER'S BRIEF. The lane's first edit replaced the false absolute with a CONDITIONAL — 'in-thread if called from one, otherwise as a new standalone DM' — because the brief told it to describe exactly that. The verify overturned it and was right: SkillContext.threadTs is declared `threadTs: string`, REQUIRED, at src/skills/types.ts:89 (checked directly, not taken on the verify's word), and Slack always supplies thread_ts||ts, so the standalone case cannot occur. The brief invented a branch the code does not have — the same defect class the whole wave was closing. Final string: 'Send the morning briefing immediately, posted in the current thread.' 102 -> 68 chars, net -34 on the always-on tier. Report row o#182."}
{"date":"2026-07-31","runId":"wf_6e41a1e4-f2c","lane":"slacker","source":"verify","ref":"owner-memory-still-renders-in-a-real-channel","finding":"In a real Slack CHANNEL where the owner posts, his own learned memory file and a VERIFIED SENDER stanza render into a surface every channel member reads — the surface o#177 left open","rootCause":"src/core/orchestrator/systemPrompt.ts:94 — isOwnerTyping has no channel term: isOwner is false (the role clamp fires for channels at processMessage.ts:139) and isOwnerInGroup is false (computed MPIM-only at :128), while senderId is still the owner's","verdict":"needs-owner-decision","recommend":"build — but the SHAPE is his: an authenticated-identity test (one line, retires the flag, closes MPIM + channel + colleague-test together) or a third per-surface boolean wired through transporter","state":"open","invariant":"owner-clamp-fixed-per-surface","note":"TWO LANES TRIED AND BOTH STOPPED CORRECTLY. Transporter returned needs-dependency having edited NOTHING, because the processMessage call-site edit alone would fail typecheck against OrchestratorInput — and it confirmed isOwnerInChannel at processMessage.ts:138 is pre-existing and correct, so nothing there needed changing. Outrider then built the plumbing (the OrchestratorInput field plus the buildSystemPromptParts arg) and THE VERIFY OVERTURNED IT ON TWO GROUNDS: it does not run without transporter's remaining piece, and gh#154's body records the per-surface-flag shape as REJECTED in favour of authenticated identity — so ticketCoverage returned #154 as CONTRADICTED, the wave moving against an open ticket. THE MANAGER REVERTED THE INERT PLUMBING (the systemPrompt param and its isOwnerTyping term, OrchestratorInput.isOwnerInChannel, the buildTurnContext arg) under the settled-disposition rule: the verify proved it inert, so removing it is cleanup rather than a product call. Typecheck green after; o#177's MPIM fix is intact, and a comment at systemPrompt.ts:94 now names this open row and the fork so the next reader is not misled into thinking channels are covered. THE VERIFY'S OWN RECOMMENDATION IS THE BETTER HALF: `senderId === profile.user.slack_user_id` is already the spelling at runOutputGates.ts:215 and both values are in scope at systemPrompt.ts:803 — one line closes every surface and deletes the flag rather than adding a third."}
{"date":"2026-07-31","runId":"wf_6e41a1e4-f2c","lane":"instructor","source":"verify","ref":"owner-memory-still-renders-in-a-real-channel>dep","finding":"replace the isOwnerTyping surface-flag OR with the authenticated-identity test and delete the flag, or wire the flag through transporter — pending the gh#154 ruling","verdict":"needs-owner-decision","recommend":"build — same change as its parent, blocked on the one-flag-or-identity ruling","state":"open","invariant":"owner-clamp-fixed-per-surface","note":"SAME CHANGE as owner-memory-still-renders-in-a-real-channel — do not count two fixes. Recorded as its own row only because a deferred dependency ask must be visible: an unreported ask is indistinguishable from one that never happened."}
{"date":"2026-07-31","runId":"wf_6e41a1e4-f2c","lane":"matchmaker","source":"verify","ref":"showas-read-through-as-any","finding":"A one-character typo in the hard validator's working-elsewhere / free detection compiles clean and silently disables the classification — a WE marker or a free block would read as a hard commitment, or the reverse, with nothing failing","rootCause":"src/utils/scheduleRules.ts:956 (`if ((ev as any).showAs === 'free') continue;`), also :513, :515 and :957 — showAs IS declared on CalendarEvent (calendarTypes.ts:10) as a string union, so the cast removes both the property-name check and the value-union check","verdict":"queued-next-run","recommend":"build — the identical hole just closed for isAllDay, in the SAME lines, and the cast is provably not load-bearing because the field is already declared","state":"open","invariant":"structural-fact-read-through-any","note":"VERIFY DISCOVERY, severity low, matchmaker. Found by the pass looking at the isAllDay fix and asking what else in those same lines wears the same cast — which is the most useful thing a discovery can be: same invariant, one file, method already proven. Note it removes a VALUE check too, not just a property-name check: showAs is a union, so `'fre'` would also compile today."}
{"date":"2026-07-31","runId":"wf_6e41a1e4-f2c","lane":"matchmaker","source":"logs","ref":"B168-a","finding":"booking with Tyler (Eastern), Maelle asserted 'no genuine overlap Monday' then self-corrected twice after the owner pushed back","verdict":"built","state":"built","invariant":"timezone-math-is-code-never-inference","note":"CLOSED BY gh#168-a — ONE CHANGE, do not count two fixes. Fourth instance today of the promoted-row aliasing defect filed as X101: this row was the log-side twin of the ticket finding, the scout merged them into gh#168-a, the fix shipped under that id and this ref stayed open and DEFERRED. The new overdue-deferral check in ledger-stats is what surfaced it — it had outlived 4 runs. That check is doing exactly what X77 and X101 were reaching for: a row nobody can act on now announces itself instead of rotting."}
{"date":"2026-07-31","runId":"wf_6e41a1e4-f2c","lane":"registrar","source":"audit","ref":"amend-reason-bypasses-id-veto","finding":"the id veto guards the amendment counter but not the `reason` typed beside it, so rationale is concatenated straight into body and amendProse unchecked","rootCause":"src/core/requests/resolver.ts:910, :919, :931, :934","verdict":"needs-owner-decision","recommend":"build — route the reason through the same veto the counter already passes; the veto exists to stop id confusion and the reason is the field most likely to carry one","state":"open","note":"NO LONGER `deferred` — SURFACED. It was deferred to 2026-07-28 and has outlived TWELVE runs without coming back, which the new overdue-deferral check in ledger-stats caught. A deferral is a one-run skip; twelve runs of silence is not a deferral, it is a lost row, so it is on his desk with a recommendation instead. Nobody has re-read the cited lines since it was filed — if he says build, the lane should confirm the line numbers first."}
{"date":"2026-07-31","runId":"wf_6e41a1e4-f2c","lane":"slacker","source":"audit","ref":"D2-forward-inert-without-owner-email","finding":"the colleague-image forward is one config field away from being silently inert — getOwnerDomain returns null when user.email is unset, so isInternal is always false","verdict":"needs-owner-decision","recommend":"build — make the missing field loud rather than silent; a guard that cannot fire looks identical to a guard that passed","state":"open","note":"NO LONGER `deferred` — SURFACED after outliving NINE runs. This is the exact class the manifest exists to catch, applied to product code: a mechanism that does nothing while reporting success. It differs from the framework instances only in that nobody is reading a manifest for it."}
{"date":"2026-07-31","runId":"wf_6e41a1e4-f2c","lane":"outrider","source":"audit","ref":"D1-dual-image-ingest-loop","finding":"the image download/scan loop exists twice and has already drifted twice","rootCause":"src/connectors/slack/app/fileIngestion.ts:42-83 and src/connectors/slack/app/handlers.ts:1210-1235","verdict":"needs-owner-decision","recommend":"build — one spine, delete the copy; a duplicate that has drifted twice will drift a third time","state":"open","note":"NO LONGER `deferred` — SURFACED after outliving NINE runs. Note the new `cleaner` agent (rule tag C) now owns exactly this territory — dead code and duplicated paths outside the prompt layer — so this may be its row rather than outrider's if he wants it built."}
{"date":"2026-07-31","runId":"wf_6e41a1e4-f2c","lane":"slacker","source":"audit","ref":"146c","finding":"making the forward fire-and-forget removed the ordering guarantee that the forward and the reply receipt collapse into ONE owner-DM thread","rootCause":"src/utils/shadowNotify.ts:98-131 threads only if shadowThreadAnchors already holds an anchor for the conversation key","verdict":"needs-owner-decision","recommend":"decline — the forward still arrives and only its threading is unordered; declining closes it so it stops resurfacing every run","state":"open","note":"NO LONGER `deferred` — SURFACED after outliving NINE runs. Recommending DECLINE rather than build is the point of surfacing it: nine runs of carrying a cosmetic threading row costs more attention than the defect costs him, and `declined` is a real answer that closes it for good."}
{"date":"2026-08-01","runId":"wf_c12b1794-265","lane":"matchmaker","source":"github","ref":"gh#165-a","finding":"The colleague who asked for the meeting is left off the invite list","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:167-197 and findAvailableSlots.ts:573-599 only ever DROPPED the colleague requester (requester_is_attending===false); neither had an ADD branch for the default attending case, so the tool descriptions at meetings.ts:255/:361-363 promised a default the code never implemented","verdict":"needs-owner-decision","recommend":"build — dispatch gh#165-a>dep>dep (three parts, one lane, same two files); the alternative is reverting both files, because what is in the tree now does not fix the reported case AND removes real availability","state":"open","note":"VERIFY OVERTURNED IT ON FOUR GROUNDS and two are serious. (1) COVERAGE GAP — the add-block is colleague-path-only, but gh#165's own transcript went through the owner-APPROVAL replay (deferredActionReplay.ts:76, senderRole 'owner'), where the block is gated out, so the requester is STILL missing on any escalated booking. The sibling drop-block already resolves identity via args.requester_slack_id and meetings.ts:367 documents it 'even on the owner path'; the new comment claiming the two branches can never disagree on the requester is FALSE. (2) SILENT LOSS — findAvailableSlots.ts:573 makes the requester's work hours a HARD clip on every colleague search, and she can never be skipped (attendeeAvailability.ts:123-125 fallbackTimezone), so an AUTO-GUESSED Sun-Thu 09:00-18:00 now removes the owner's real Tue 20:30-23:59 window, and day_summary.attendee_hours_note names her own email as the blocker — she can tell the asker that her own guessed hours blocked her. Plus one extra getFreeBusy per colleague search and autoPresentTz collapsing to '' with a third-zone requester, which disables the cross-TZ rendering gh#24 B2 was built for. THE TICKET ASKED ABOUT THE INVITE LIST ONLY. (3) A clamped owner in an MPIM gets pushed into the attendee list; createMeeting is saved by dedupe, findAvailableSlots wastes a duplicate calendar read. createMeeting.ts:1518 already carries the exclusion both blocks need. (4) DEFEATED GUARD — the sensitivity gate at :245-265 keys on 'is the colleague in args.attendees', which the new block now guarantees on the default path, so it can no longer refuse anything unless requester_is_attending===false."}
{"date":"2026-08-01","runId":"wf_c12b1794-265","lane":"matchmaker","source":"verify","ref":"gh#165-a>dep>dep","finding":"Resolve the requester the way the drop-block does, keep her out of the hard availability clip, and exclude the clamped owner","verdict":"needs-owner-decision","recommend":"build — same change as gh#165-a, blocked on that ruling","state":"open","note":"SAME CHANGE as gh#165-a — do not count two fixes. Three parts. (a) Honour args.requester_slack_id and run on the OWNER path too, so the approval/deferred replay actually adds her — that is the path gh#165's transcript took. (b) Keep her in the roster, the annotation and the invite but OUT of the hard attendeeAvailability clip, or clip only when her hours are curated (source==='manual'), so a guessed 09:00-18:00 stops deleting the owner's real evening availability. (c) Add the context.userId !== profile.user.slack_user_id exclusion both blocks need, which createMeeting.ts:1518 already carries."}
{"date":"2026-08-01","runId":"wf_c12b1794-265","lane":"instructor","source":"github","ref":"gh#156-c","finding":"Maelle said she would get back to the colleague and never did — the unbacked relay promise","rootCause":"src/core/orchestrator/systemPrompt.ts:737-738 (RULE 3) already names this exact phrase and scenario verbatim; the gap is enforcement, not wording","verdict":"needs-dependency","recommend":"decline — he ruled on this one day earlier under a different id ('ok so don't fix it. i don't want more latency'), and nothing technical has changed","state":"open","note":"THE LANE REFUSED CORRECTLY AND SAID WHY: RULE 3 already states the case by name, so a stronger example would be a duplicate chasing a model-compliance gap the prompt layer cannot close alone. Its dependency then came back needs-owner-decision having touched no files, because enforcing RULE 3 without an LLM classifier is impossible under the no-regex-on-multilingual-natural-language rule, and a classifier reproduces exactly the latency he rejected — up to 3 Haiku calls plus 2 Sonnet rewrites on a chatty colleague reply. This is the second time this row has been raised and refused on the same grounds; recommending decline so it stops returning."}
{"date":"2026-08-01","runId":"wf_c12b1794-265","lane":"matchmaker","source":"verify","ref":"relay-only-colleague-gets-an-invite-on-the-approval-replay","finding":"A colleague who only relayed a message ('tell Idan I want to meet Tal') still gets a calendar invite to a meeting she is not in, whenever the booking lands through the owner-approval replay — an invite sent outward that a later correction cannot undo","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:891 — on any non-colleague context requesterId falls back to undefined, so the `if (requesterId)` scrub at :892 is skipped entirely; the owner path has only the OPTIONAL args.requester_slack_id (meetings.ts:367) to identify her, so the Bug-4 relay protection is prose-only exactly where the deferred replay runs","verdict":"queued-next-run","recommend":"build — a real-world outbound action on the wrong person, and it is the same identity-resolution gap gh#165-a>dep part (a) fixes, so it should ride with that dispatch","state":"open","invariant":"fact-published-on-a-path-its-reader-never-takes","note":"VERIFY DISCOVERY, severity medium, matchmaker. Worth more than its severity: this is a real invite to a real person, and the M4 tier for that is 2, not 3. It shares a root with gh#165-a's part (a) — both are 'the colleague path resolves the requester and the owner path does not' — so building them apart would touch the same lines twice."}
{"date":"2026-08-01","runId":"wf_c12b1794-265","lane":"usher","source":"github","ref":"TICKET-COVERAGE-2026-08-01","finding":"All six open Bug tickets read and every complaint accounted for","verdict":"audit","state":"open","note":"6 tickets, 16 complaints, 2 emitted, 14 DROPPED WITH A NAMED REASON EACH — which is the X80 defect fixed, and it changes the conclusion: the tickets were not under-emitting, they were already FIXED. Of the 14: TEN dropped as already-built (#168's single complaint, 2 of #165's 4, all 3 of #158's, 1 of #157's, 1 of #156's), TWO were praise rather than complaints ('its good that she said no to me', 'Ayala's request was handled well'), TWO belong to open design rows already converted (#157's inconsistent-privacy-stance and the 44-notes question -> gh#154; #151's standing-rule -> gh#155), and ONE is too vague to act on (#156's 'why first going to online or person?' — no code enforces a location-type prompt to cite). THE alreadyBuilt DEVIATION PAID FOR ITSELF: 36 refs passed, 10 dropped by triage, alreadyFixed count ZERO — no lane paid a dispatch to rediscover a shipped fix, against gh#158-b's full dispatch for exactly that on 2026-07-31. ENGINE WARNING WORTH KEEPING: 4 tickets (#168 #158 #157 #151) appear in no issue id, so nothing downstream can compute their coverage — correct here because they emitted nothing, but it is the same shape as the real gh#156 failure."}
{"date":"2026-08-01","runId":"wf_c12b1794-265","lane":"matchmaker","source":"github","ref":"gh#165-a","finding":"The colleague who asked for the meeting is left off the invite list","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:167-197 and findAvailableSlots.ts:573-599 only ever DROPPED the colleague requester; neither had an ADD branch for the default attending case","verdict":"needs-owner-decision","recommend":"build — the bug is real and unfixed, but scope it to the INVITE LIST ONLY: resolve the requester the way the drop-block does (honour args.requester_slack_id, run on the owner path so the approval replay adds her), exclude the clamped owner, and DO NOT touch attendeeAvailability","state":"open","note":"OWNER REVERTED 2026-08-01: 'revert all the bad code'. Both files are back at HEAD (5d01d9c), typecheck green, patch saved to scratchpad/gh165a-reverted.patch. THE BUG IS STILL OPEN — reverting removed a fix that did not fix it, so nothing regressed. WHAT THE ATTEMPT GOT WRONG, so the next one does not repeat it: it was colleague-path-only while gh#165's own transcript went through the owner-approval replay (deferredActionReplay.ts:76) where the block was gated out, so the reported case was never covered; and it made the requester's work hours a HARD CLIP on every colleague search (findAvailableSlots.ts:573), where she can never be skipped, so an auto-guessed Sun-Thu 09:00-18:00 deleted the owner's real Tue 20:30-23:59 window and day_summary named her own email as the blocker. It also defeated the sensitivity gate at :245-265 (which keys on 'is the colleague in args.attendees') and pushed a clamped owner into the attendee list. THE TICKET ASKED ABOUT THE INVITE LIST ONLY — the availability clip was scope the ticket never requested and is what made the change net-harmful."}
{"date":"2026-08-01","runId":"wf_c12b1794-265","lane":"matchmaker","source":"verify","ref":"gh#165-a>dep>dep","finding":"Resolve the requester the way the drop-block does, keep her out of the hard availability clip, and exclude the clamped owner","verdict":"needs-owner-decision","recommend":"build — folded into gh#165-a now that the parent is reverted; it is the specification for the next attempt, not a separate change","state":"open","note":"REVERTED WITH ITS PARENT 2026-08-01. Its three parts survive as the brief for the next attempt and are recorded on gh#165-a's own row: (a) honour args.requester_slack_id and run on the owner path; (b) invite list only, no attendeeAvailability clip; (c) add the context.userId !== profile.user.slack_user_id exclusion createMeeting.ts:1518 already carries."}
{"date":"2026-08-01","runId":"wf_c12b1794-265","lane":"matchmaker","source":"verify","ref":"relay-only-colleague-gets-an-invite-on-the-approval-replay","recheck":"STILL REAL after the 2026-08-01 revert — it was never fixed by the reverted code, it was DISCOVERED beside it. createMeeting.ts:891 still falls back to requesterId undefined on any non-colleague context, so the relay scrub at :892 is skipped exactly where the deferred replay runs.","recommend":"build — a real outbound invite to the wrong person, and it shares its root with gh#165-a part (a); build them together or the same lines get touched twice","verdict":"queued-next-run","state":"open","invariant":"fact-published-on-a-path-its-reader-never-takes"}
{"date":"2026-08-01","runId":"wf_c12b1794-265","lane":"instructor","source":"github","ref":"gh#156-c","finding":"Maelle told a colleague she would get back to her and never did — the unbacked relay promise","rootCause":"src/core/orchestrator/systemPrompt.ts:737-738 (RULE 3) already names this exact phrase and scenario; the gap is enforcement, not wording","verdict":"declined","state":"open","note":"OWNER DECLINED 2026-08-01: 'gh#156-c - decline'. Third time this row has been raised and the second time it was refused on technical grounds before reaching him: RULE 3 states the case verbatim so another example is a duplicate, and enforcing it needs an LLM classifier that reproduces exactly the latency he rejected one day earlier ('ok so don't fix it. i don't want more latency') — up to 3 Haiku calls plus 2 Sonnet rewrites on a chatty colleague reply. THIS DECLINE IS WHAT LETS gh#156 CLOSE: it was the ticket's last unaccounted complaint."}
{"date":"2026-08-01","runId":"wf_c12b1794-265","lane":"matchmaker","source":"github","ref":"gh#165-a","finding":"The colleague who asked for the meeting is left off the invite list","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:167-197 and findAvailableSlots.ts:573-599 — no ADD branch for the default attending case","verdict":"needs-owner-decision","recommend":"build — DUE NEXT RUN; scope it to the invite list only, resolve the requester on the owner path, and do not touch attendeeAvailability","state":"deferred","note":"OWNER DEFERRED 2026-08-01: 'lets defere the two gh#165 for next run'. A deferral is a ONE-RUN SKIP, so this comes back in the pending-owner group on the very next run with this recommendation — it is not parked. CONSEQUENCE HE SHOULD KNOW: gh#165 cannot close at the next wrap while this stands; the other five tickets can. The reverted attempt's three lessons stay on this row as the brief: honour args.requester_slack_id and run on the OWNER path (the approval replay is the path the ticket's own transcript took), invite list ONLY with no attendeeAvailability clip (that clip is what deleted the owner's real Tue 20:30-23:59 window), and exclude the clamped owner using the check createMeeting.ts:1518 already carries."}
{"date":"2026-08-01","runId":"wf_c12b1794-265","lane":"matchmaker","source":"verify","ref":"relay-only-colleague-gets-an-invite-on-the-approval-replay","finding":"A colleague who only relayed a message gets a real calendar invite to a meeting she is not in, whenever the booking lands through the owner-approval replay","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:891 — requesterId falls back to undefined on any non-colleague context, so the relay scrub at :892 is skipped exactly where the deferred replay runs","verdict":"needs-owner-decision","recommend":"build — DUE NEXT RUN, and build it WITH gh#165-a: same identity-resolution root, same lines","state":"deferred","note":"OWNER DEFERRED 2026-08-01: 'lets defere the two gh#165 for next run'. Deferred as the second of the two, so it returns next run alongside gh#165-a. Worth more than its medium severity when it does: this is a real invite reaching a real person, which is M4 tier 2 (a wrong real-world action), not tier 3. Building it apart from gh#165-a would touch the same lines twice.","invariant":"fact-published-on-a-path-its-reader-never-takes"}
{"date":"2026-08-01","runId":"wf_655d8245-78f","lane":"registrar","source":"audit","ref":"amend-reason-bypasses-id-veto","finding":"The owner amends an approval and the free-text reason he types rides into the requester's DM unchecked, while the counter beside it passes an id veto","rootCause":"src/tasks/skill.ts:1319-1344 — the amend branch of resolve_approval runs the COUNTER through renderCounter's id veto at :1331 but never checks args.reason, which is later spliced verbatim into the requester relay at resolver.ts:910/:919/:931/:934","verdict":"built","state":"built","note":"BUILT, AND THE LANE MOVED THE FIX UPSTREAM OF WHERE THE ROW POINTED. The row cited resolver.ts's four render sites; the lane gated the single STORE-TIME entry point instead, after proving by grep that resolveRequest's 'amend' verdict has exactly one origin — the other two call sites (handlers.ts:871-875 emoji reactions, threadBoundApprovalAutoResolve.ts:243-248) only ever produce approve/reject with fixed non-free-text reasons. So one guard closes the gap completely and resolver.ts's rendering needed no change, because it can now only ever see a reason that already passed. Shape: exported textCarriesInternalWorkItemId() beside the existing INTERNAL_WORK_ITEM_ID_RE so the regex is REUSED rather than copied, then one guard in the amend branch refusing with the existing `unrelayable_counter` error code — no new error taxonomy. The four cited line numbers were re-checked against disk and are unchanged since the row was filed twelve runs ago. Deliberately NOT extended to the reject verdict or to notifyOwnerOfColleaguePushback (owner-facing, no withholding needed) — reject is the discovery below."}
{"date":"2026-08-01","runId":"wf_655d8245-78f","lane":"slacker","source":"audit","ref":"D2-forward-inert-without-owner-email","finding":"The colleague-image forward is one config field away from being silently inert — getOwnerDomain returns null when user.email is unset, so isInternal is always false","rootCause":"NONE — the premise does not hold against the code. src/config/userProfile.ts:85 declares user.email as a REQUIRED CompanyEmailSchema (z.string().email(), no .optional()), and a missing or invalid email fails LOUDLY at load (userProfile.ts:590-595, :716-722 — logger.error plus console.error, the profile never boots). The null branch in attendeeScope.ts:18-23 is unreachable for any profile that runs.","verdict":"already-fixed","state":"built","note":"THE ROW WAS WRONG AND SAT DEFERRED FOR NINE RUNS BEING WRONG. Not fixed by anyone — the failure mode it described cannot reproduce, because the schema already does exactly what the row asked for: it makes the missing field loud. The lane re-derived this from code rather than building on the brief, which is shared rule 6 working. IT ALSO NAMED THE REAL AMBIGUITY AND CORRECTLY DECLINED TO BUILD IT: the sender-side email can genuinely be invisible (a missing users:read.email scope), but getSenderRole==='colleague' at that call site intentionally includes Slack Connect guests, so a null senderDomain is the CORRECT external-exclusion outcome and is not distinguishable from a scope misconfiguration without guessing. THE ENGINE WARNED 'alreadyBuilt should have caught this cheaper' AND THAT WARNING IS A FALSE POSITIVE HERE: alreadyBuilt lists fixes that exist, and this row never had one. The cheap catch for a false-premise row is a re-read, which is what the backlog pass is for — and this row citing attendeeScope.ts would have been eligible for one."}
{"date":"2026-08-01","runId":"wf_655d8245-78f","lane":"registrar","source":"verify","ref":"reject-reason-bypasses-id-veto","finding":"The same leak just closed on `amend` is still live on `reject`, which is the more common verdict: the owner rejects with 'clashes with req_1753812004_k3m1' and the colleague's DM reads that raw internal id back to them","rootCause":"src/tasks/skill.ts:1317 builds { verdict: 'reject', reason: args.reason } with no veto at all — the new amend gate sits 33 lines BELOW it — and src/core/requests/resolver.ts:887 splices that reason verbatim into the requester body. It also enters the Haiku prompt at resolver.ts:1015 where, unlike amend, nothing validates the output (:1032 gates the fault check on composeAmend).","verdict":"queued-next-run","recommend":"build — the identical defect on the more common verdict, the guard now exists one function over, and reject has NO output validation where amend does","state":"open","invariant":"internal-id-reaches-an-outward-message","note":"VERIFY DISCOVERY, severity medium, shepherd — and the cheapest high-value row in the ledger right now: textCarriesInternalWorkItemId() was exported this wave, so this is one call at skill.ts:1317. The lane explicitly scoped reject OUT of the amend fix and said so, which is why this is a discovery rather than an omission. Note the second half, which is worse than the first: amend's Haiku composition has a fault check and reject's does not, so a rejected requester can receive an unvalidated composed message."}
{"date":"2026-08-01","runId":"wf_655d8245-78f","lane":"registrar","source":"verify","ref":"amend-reason-bypasses-id-veto>dep","finding":"Two code comments tag this work as `#157`, which is an unrelated open Bug about MPIM calendar privacy","rootCause":"src/core/requests/resolver.ts:1274 and src/tasks/skill.ts:1344","verdict":"needs-owner-decision","recommend":"build — two-token comment correction; a wrong ticket tag sends the next reader to the wrong ticket, which is the same class as the comments this loop has been fixing all week","state":"open","note":"DEFERRED DEP ASK, not dispatched because it arrived after its parent was built. Trivial in size and worth doing on the next touch of either file rather than as its own dispatch — fold it into whatever ships next in shepherd's territory."}
{"date":"2026-08-01","runId":"wf_655d8245-78f","lane":"matchmaker","source":"github","ref":"gh#165-a","finding":"The colleague who asked for the meeting is left off the invite list","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:167-197 and findAvailableSlots.ts:573-599 — no ADD branch for the default attending case","verdict":"needs-owner-decision","recommend":"build — DUE NOW; invite list only, resolve the requester on the owner path, do not touch attendeeAvailability","state":"open","note":"THE ONE-RUN SKIP IS SERVED. He deferred this at 00:30 with 'lets defere the two gh#165 for next run', and wf_655d8245-78f then ran at 01:05 without it — so the next run happened and the deferral is spent. Surfaced by the new overdue-deferral gate on its FIRST possible firing, one run after the deferral rather than nine or twelve, which is the whole point of that check. Back on his desk with the reverted attempt's three lessons intact: honour args.requester_slack_id and run on the OWNER path (the approval replay is the path the ticket's transcript took), invite list ONLY with no attendeeAvailability clip, and exclude the clamped owner via the check createMeeting.ts:1518 already carries."}
{"date":"2026-08-01","runId":"wf_655d8245-78f","lane":"matchmaker","source":"verify","ref":"relay-only-colleague-gets-an-invite-on-the-approval-replay","finding":"A colleague who only relayed a message gets a real calendar invite to a meeting she is not in, whenever the booking lands through the owner-approval replay","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:891 — requesterId falls back to undefined on any non-colleague context, so the relay scrub at :892 is skipped exactly where the deferred replay runs","verdict":"needs-owner-decision","recommend":"build — DUE NOW, and build it WITH gh#165-a: same identity-resolution root, same lines","state":"open","invariant":"fact-published-on-a-path-its-reader-never-takes","note":"THE ONE-RUN SKIP IS SERVED — same as gh#165-a, and this row is what the overdue gate actually named. M4 tier 2 rather than 3 when it comes back: a real invite reaching a real person is a wrong real-world action, not silent wrongness, and a later correction does not unsend it."}
{"date":"2026-08-01","runId":"direct-d1-146c","lane":"slacker","source":"verify","ref":"146c","finding":"Making the colleague-image forward fire-and-forget removed the ordering guarantee that the forward and the reply receipt collapse into ONE owner-DM thread","rootCause":"src/utils/shadowNotify.ts:98-131 threads only if shadowThreadAnchors already holds an anchor for the conversation key, and an unawaited forward can arrive before that anchor exists","verdict":"declined","state":"open","note":"OWNER DECLINED 2026-08-01 ('agree for both suggestions', on a recommend-decline). The forward still arrives and the image still reaches him — only the grouping is unordered, so he gets two DMs instead of one thread. DECLINED BECAUSE OF ITS AGE, NOT ITS MERIT: nine runs of re-reading and re-deciding a cosmetic threading row costs more attention than the defect costs him, and `declined` is the answer that stops it resurfacing. One row to reopen if the un-threaded DMs ever actually annoy him."}
{"date":"2026-08-01","runId":"direct-d1-146c","lane":"slacker","source":"verify","ref":"D1-dual-image-ingest-loop","finding":"The image download-and-scan loop exists twice and has already drifted twice","rootCause":"src/connectors/slack/app/fileIngestion.ts:42-83 and src/connectors/slack/app/handlers.ts:1210-1235","verdict":"needs-owner-decision","recommend":"build — one spine, delete the copy; the divergence has already happened twice so a third is the base case, and the loop carries the malicious-image block decision","state":"open","note":"OWNER APPROVED BUILD 2026-08-01, dispatched to TRANSPORTER not outrider — RE-ROUTED and the reason is M3: both files are connectors/slack/app/, which is transporter's territory (inbound routing plus per-channel media features), and the row's original outrider assignment predates that seam being settled. NOT routed to the new `cleaner` agent despite duplicated-path being exactly its charter, because the cleaner is deliberately not in rotation — the owner dispatches it himself and no engine calls it. WHY THIS IS MORE THAN HYGIENE: the loop is where the malicious-image block decision lives (the `if (!block) continue` survivor path that keeps a suspicious image out of the forward), so two copies of it means one can stop blocking while the other still does, and nothing would fail."}
{"date":"2026-08-01","runId":"direct-aba9f84f","lane":"matchmaker","source":"github","ref":"gh#165-a","finding":"The colleague who asked for the meeting is left off the invite list","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:857-881 (pre-fix) — requester identity existed ONLY as a DROP branch, fell back to context.userId on the colleague path, and resolved to undefined on every other path including the owner-approval replay (deferredActionReplay.ts:74-83 sets senderRole 'owner' and context.userId = ownerUserId). There was no ADD branch at all for the default attending case, which meetings.ts:255 and :361-363 already promise.","verdict":"built","state":"built","note":"BUILT, one file, +93/-35, typecheck green. THE LANE FOUND THE CAUSE UNDER THE CAUSE, and it explains why the reverted attempt could never have worked even with an owner-path branch: the tool schema at meetings.ts:365-367 tells the model to OMIT requester_slack_id on the colleague path, so that identity was never stamped into args — meaning any _deferred_action_hint snapshot taken before the fix carried no way to re-identify her on replay. The fix therefore STAMPS args.requester_slack_id back onto args the first time it resolves from the colleague fallback, so it survives into every later hint snapshot and into the replay. One resolution now runs before every early return and every hint capture (the first at Guard B, :419): requester_slack_id first, else the genuinely-colleague speaker, else undefined — and it nulls out if it ever equals the owner's own slack id. The MPIM-clamped-owner exclusion reuses the same test the shadow-DM gate already uses rather than a new one. CONSTRAINT (b) HELD: handlers/findAvailableSlots.ts was read, confirmed to carry the same drop-only pattern, and LEFT UNTOUCHED — adding her to that tool's attendeeEmails would immediately pull her into loadAttendeeAvailabilityForEmails's hard work-hours clip, which is exactly the failure the previous attempt was reverted for, and that tool has no invite-list concept so the row does not apply there. Sensitivity gate (createMeeting.ts:294-321 and its twin bookingRequest.ts:338-370) verified NOT neutered: it still refuses whenever requester_is_attending === false, which is the relay/EA case it exists to block."}
{"date":"2026-08-01","runId":"direct-aba9f84f","lane":"matchmaker","source":"verify","ref":"relay-only-colleague-gets-an-invite-on-the-approval-replay","finding":"A colleague who only relayed a message gets a real calendar invite to a meeting she is not in, whenever the booking lands through the owner-approval replay","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:891 (pre-fix) — requesterId fell back to undefined on any non-colleague context, so the relay scrub at :892 silently no-opped exactly where the deferred replay runs","verdict":"built","state":"built","invariant":"fact-published-on-a-path-its-reader-never-takes","note":"CLOSED BY THE SAME CHANGE as gh#165-a — one change, do not count two fixes. It is fixed because requesterId no longer depends on context.userId, which becomes the OWNER's on replay: the identity is stamped into args when first resolved, so the scrub can still find her one hop later. That is the same insight that fixed the add branch, which is why the two were dispatched together — building them apart would have touched the same lines twice and probably fixed only one."}
{"date":"2026-08-01","runId":"direct-a6c920c4","lane":"slacker","source":"verify","ref":"D1-dual-image-ingest-loop","finding":"The per-image download-and-injection-scan loop existed twice and had already drifted twice","rootCause":"src/connectors/slack/app/fileIngestion.ts and src/connectors/slack/app/handlers.ts each carried their own copy of the loop — the security boundary where the block decision lives","verdict":"built","state":"built","note":"BUILT, net -7 lines across two files. Extracted to downloadAndScanImageBatch (fileIngestion.ts:22-59); both call sites use it, and handlers.ts lost its now-unused downloadSlackImage import. THE DIVERGENCE WAS REAL AND WAS PRESERVED RATHER THAN MERGED AWAY, which is the finding worth more than the refactor: the two copies differed on DOWNLOAD FAILURE, not on the block verdict. processImageFileShare (DM/MPIM/live) posts the friendly message then ABORTS the whole turn, unless the catch-up replay path sets degradeOnDownloadFailure and lets the batch continue so an unfetchable image does not drop an answerable question; the channel @mention handler always CONTINUES to the next file, because that turn still has doc text and thread directives to fold in. Both kept via a stopOnDownloadFailure parameter plus an abortedOnDownloadFailure result flag that reproduces the original early return exactly, including discarding partial images collected before the abort. THE BLOCK/SCAN DECISION ITSELF WAS ALREADY IDENTICAL AND WAS NOT TOUCHED — same inputs, same verdict, same survivor set at both call sites, and scanAndPrepareImage is unchanged. Two cosmetic differences were unified deliberately and named: the three friendly error strings (kept fileIngestion.ts's wording) and a vacuous `if (f.url_private)` guard in handlers.ts (the download already required that field to succeed). Left outside scope and said so: the colleague-image forward, which only ever existed on one path, and each caller's pre-loop 4-image cap."}
{"date":"2026-08-01","runId":"examiner-a9f06431","lane":"matchmaker","source":"verify","ref":"gh#165-a","finding":"The requester-ADD sits ABOVE both sensitivity gates, and both gates decide by asking whether the colleague's email is in `attendees` — which the ADD now guarantees","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts:232-253 (the ADD) versus :302-322 and its twin src/skills/meetings/bookingRequest.ts:338-370 (the gates)","verdict":"needs-owner-decision","recommend":"resend — the examiner prescribed the exact fix (move the ADD below the gate) and proved the move safe, so it went straight back to matchmaker rather than to his desk","state":"open","invariant":"deterministic-control-replaced-by-a-model-supplied-flag","note":"OVERTURNED BY THE COMBINED PASS, and this is a PRIVACY REGRESSION, not a style note. On the default path (flag unset, colleague has an email in people_memory) NEITHER sensitivity gate can refuse any more; all that remains is requester_is_attending === false, which is a MODEL-SUPPLIED flag — so a deterministic privacy control became a prompt control. Second consequence of the same ordering and the worse one: when the model omits the requester from attendees but does not set the flag (the schema at meetings.ts:354 demands both), the ADD sends a REAL GRAPH INVITE — subject, body, time, full attendee list — to someone the meeting is not about, which is exactly the harm of the relay-only row fixed in the same change, arriving through the default branch. THE MOVE IS SAFE AND THE PASS PROVED IT: nothing between :232 and :322 reads attendees except that gate, and all nine {...args} snapshots are at :751 or later, so the stamp and the DROP ordering the fix depends on are untouched. RESENT, not escalated, because the examiner framed it as an ask to matchmaker and named the one-block fix."}
{"date":"2026-08-01","runId":"examiner-a9f06431","lane":"matchmaker","source":"verify","ref":"createmeeting-attendee-busy-gate-now-fires-on-a-fresh-book","finding":"A colleague booking a time SHE HERSELF is busy at now gets 'Book anyway, or pick a different time?' where the booking used to just happen — the lane's claim that planMeeting's attendee-busy check is advisory-only is false for a fresh book","rootCause":"src/skills/meetings/planMeeting.ts:748-757 pushes gates.push({ kind: 'attendee_busy' }) whenever !allowRelaxed && intent !== 'move', and gh#165-a's ADD now puts the requester into that check","verdict":"queued-next-run","recommend":"build — decide it deliberately in the resend rather than leaving it as a side effect; either keep the confirmation and say why, or exclude the requester from that gate","state":"open","note":"VERIFY DISCOVERY folded into the gh#165-a resend rather than queued separately, because it is the same block moving. IMPORTANT ON WHAT IT IS NOT: there is no work-hours clip anywhere in this change, so the regression that got the previous attempt reverted is genuinely not re-created — the examiner checked handlers/findAvailableSlots.ts is absent from git diff and that createMeeting's own pipeline hard-clips nothing on attendee hours. This is a new confirmation prompt, not lost availability."}
{"date":"2026-08-01","runId":"examiner-a9f06431","lane":"matchmaker","source":"verify","ref":"clamped-owner-predicate-has-a-second-spelling","finding":"isGenuineColleague at createMeeting.ts:189 is a second spelling of the clamped-owner predicate already at :1545 in the same file, and the comment claims it was reused when it was not","verdict":"queued-next-run","recommend":"build — collapse to one; folded into the gh#165-a resend since both sites are in the file being edited","state":"open","invariant":"owner-clamp-fixed-per-surface","note":"VERIFY DISCOVERY. Matters beyond tidiness because open Improvement gh#154 proposes replacing that clamp with a single ownerClampSurface fact: with two spellings, that change must hit both or the invite roster and the shadow-DM will disagree about who the colleague is. Third instance today of the owner-clamp-per-surface invariant."}
{"date":"2026-08-01","runId":"examiner-a9f06431","lane":"gatekeeper","source":"verify","ref":"sensitivity-rule-has-two-implementations-that-must-change-together","finding":"createMeeting.ts:302-322 and bookingRequest.ts:338-370 are the same rule — a colleague may set sensitivity only if she is on the attendee list — they fire on the same call, and this wave's single ADD disabled BOTH at once","verdict":"queued-next-run","recommend":"build — one spine for the rule, or at minimum a test that fails when the two disagree; the next fix applied to one will silently leave the other standing","state":"open","invariant":"one-rule-two-implementations","note":"VERIFY DISCOVERY, and the proof is this wave itself: a single misplaced block neutered two independent copies of one privacy rule simultaneously, which is exactly what two implementations of one rule buys you. Routed to gatekeeper rather than matchmaker because the durable question is how a privacy rule is enforced once, not how createMeeting builds a roster."}
{"date":"2026-08-01","runId":"examiner-a9f06431","lane":"slacker","source":"verify","ref":"D1-dual-image-ingest-loop","recheck":"CONFIRMED BUILT by the combined pass 2026-08-01: one loop at fileIngestion.ts:19-59, both copies deleted, net negative. The security verdict is BYTE-IDENTICAL (`if (!block) { hadSecurityRefusal = true; continue; }`) with the fail-closed gate at :95-104 still reading both flags, and stopOnDownloadFailure + abortedOnDownloadFailure reproduce the old early return including discarding partial images (the caller returns at :93, before the forward and before processMessage). The two cosmetic cuts took nothing: the channel path's three download-failure strings were replaced by the DM wording, and its `if (f.url_private)` guard is gone — safe, because reaching that push means downloadSlackImage already succeeded on the same value.","recommend":"build — already built; this row is closed","verdict":"built","state":"built"}
{"date":"2026-08-01","runId":"examiner-a9f06431","lane":"registrar","source":"verify","ref":"amend-reason-bypasses-id-veto","recheck":"CONFIRMED BUILT by the combined pass 2026-08-01: the guard at skill.ts:1350-1356 sits inside the sole amend branch, before `decision` is built at :1357 and stored at :1363, and is reachable — the counter checks above it return only on an unrelayable or empty counter, which is orthogonal to `reason`. It calls one export over the existing regex constant rather than a second copy.","recommend":"build — already built; this row is closed","verdict":"built","state":"built"}
{"date":"2026-08-01","runId":"examiner-a9f06431","lane":"slacker","source":"audit","ref":"D2-forward-inert-without-owner-email","recheck":"SPOT-CHECK CONFIRMED 2026-08-01 — the examiner stands behind the already-fixed closure: userProfile.ts:60 is z.string().email().refine(...), required at :85; a failure throws at :590-595 and loadAllProfiles catches at :716-722 and simply never puts the profile in the map. getOwnerDomain (attendeeScope.ts:18-23) returns null only for an email with no @ or a trailing @, both rejected by that schema, so the forward gate at fileIngestion.ts:143-154 cannot be inert for want of an owner email.","verdict":"already-fixed","state":"built","note":"This is the row-closed-with-no-diff population the examiner brief must always carry, and it is why: a closure asserted by one lane now has a second read behind it."}
{"date":"2026-08-01","runId":"direct-af8807cf","lane":"matchmaker","source":"github","ref":"gh#165-a","finding":"The colleague who asked for the meeting is left off the invite list","rootCause":"src/skills/meetings/ops/handlers/createMeeting.ts — the requester-ADD ran ABOVE both sensitivity gates, so 'is the colleague's email in attendees' always read true on the default path","verdict":"built","state":"built","note":"BUILT ON THE RESEND, one file, typecheck green. The ADD moved to immediately AFTER the local sensitivity gate; the DROP branch stayed put because it only removes and never inflated the check. ONE MOVE CLOSES BOTH GATES, AND THE MANAGER VERIFIED THAT CLAIM DIRECTLY rather than taking it or spending a second Opus pass: gate 1 does `delete args.sensitivity` at createMeeting.ts:306 when the colleague is not on the pre-ADD list, and the twin gateSensitivity (bookingRequest.ts:343-344) returns immediately on `raw === undefined`, so it never reaches its own attendee check and the ADD cannot fool it. If gate 1 allowed the sensitivity, she genuinely was on attendees before the ADD and the twin agrees for the right reason. bookingRequest.ts needed no edit. ALSO IN THIS RESEND: the duplicate clamped-owner predicate is collapsed — the shadow-DM gate at :1573 now reads isGenuineColleague instead of re-typing the conjunction, with a comment naming gh#154 so the coming ownerClampSurface change has one place to touch. NO SEPARATE VERIFY PASS (X37): one block moved to a position the examiner itself prescribed and proved safe, plus one predicate collapse; the only new claim was the two-gates one, checked by reading both sites."}
{"date":"2026-08-01","runId":"direct-af8807cf","lane":"matchmaker","source":"verify","ref":"createmeeting-attendee-busy-gate-now-fires-on-a-fresh-book","finding":"A colleague booking a time she herself is busy at now gets 'Book anyway, or pick a different time?' where the booking used to just happen","rootCause":"src/skills/meetings/planMeeting.ts:666-757 — the internal-attendee freebusy check sees the requester once the ADD makes her a real attendee","verdict":"built","state":"built","note":"RESOLVED AS A DELIBERATE LANE DECISION rather than left as a side effect, which is what the resend asked for. THE LANE KEPT IT and the reasoning holds: once the requester is a real attendee, checking her calendar like every other attendee is the consistent answer under M2 (one validator, no per-person special-casing), and excluding her would need a new exclusion predicate the gate does not otherwise have — which the dispatch's own no-new-predicate constraint forbids. It fires only when initiator is owner, on a fresh non-relaxed book, and only when she is genuinely busy; the same one-time confirmation every other attendee already triggers, never a silent block. Documented in the code at createMeeting.ts:328-336, not only here."}
{"date":"2026-08-01","runId":"direct-af8807cf","lane":"matchmaker","source":"verify","ref":"clamped-owner-predicate-has-a-second-spelling","finding":"isGenuineColleague was a second spelling of the clamped-owner predicate already typed inline at the shadow-DM gate in the same file","verdict":"built","state":"built","invariant":"owner-clamp-fixed-per-surface","note":"COLLAPSED to one definition with two call sites (defined at createMeeting.ts:195, reused at :1573). The comment names gh#154 so the proposed ownerClampSurface fact has a single site to replace — which was the point: with two spellings, that change would have left the invite roster and the shadow-DM disagreeing about who counts as the colleague."}
{"date":"2026-08-01","runId":"direct-af8807cf","lane":"gatekeeper","source":"verify","ref":"sensitivity-rule-has-two-implementations-that-must-change-together","recheck":"RE-SCOPED 2026-08-01 after the gh#165-a resend. Both implementations still exist but they are no longer symmetric: createMeeting.ts:302-322 is now AUTHORITATIVE on this path because it deletes args.sensitivity, and bookingRequest.ts's gateSensitivity returns early on undefined so it can only ever agree. It remains the sole gate for other callers of normalizeBookingRequest, so it is not dead. Severity drops from 'both can be neutered at once' — which this wave proved by doing it — to 'one rule with a redundant second site on one path'.","recommend":"build — still worth one spine, or a test that fails when the two disagree, but no longer urgent now that the outer gate is authoritative","verdict":"queued-next-run","state":"open","invariant":"one-rule-two-implementations"}
{"date":"2026-08-01","runId":"direct-a4562976","lane":"registrar","source":"verify","ref":"amend-reason-bypasses-id-veto>dep","finding":"Two comments added by the amend-reason fix tag the work #157, an unrelated open Bug about MPIM calendar privacy","rootCause":"src/core/requests/resolver.ts:1274 and src/tasks/skill.ts:1344","verdict":"built","state":"built","note":"Comment-only, zero behaviour change, same line count (6 each). Both tags now name the real work item `amend-reason-bypasses-id-veto` instead of #157. WHY IT EARNED A DISPATCH: the examiner called it a WRONG-CLOSE RISK — a #157 tag sitting in the committed diff reads as coverage that ticket never received, and the next person deciding whether #157 can close would have seen it and been misled. Same defect class as most of this wave: text asserting something the code does not do. With this corrected, nothing blocks #157 from closing on its own merits (1 complaint built, 2 converted to gh#154, 1 was praise)."}
