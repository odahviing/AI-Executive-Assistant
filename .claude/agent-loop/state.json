{
  "_thisFileNote": "CURRENT STATE ONLY, and that is the rule this file lost: the keys documented in .claude/skills/manager/SKILL.md 'State you own', ONE operating note each, and the run in progress or the last one (its `note` = what went in, `_resultNote` = what came out). Everything older is HISTORY and history is ledger.jsonl, which is append-only and queryable. Reduced 2026-07-31 from 114 KB / 44 prose keys against 7 documented ones; every ruling in those 44 was verified to be a ledger row already before a byte was deleted. A `_previousRun*` key, a second note on one field, or a note about a run that has ended means it is regrowing - move it to the ledger and delete it. The wrap is where this is checked.",
  "lastSeenIso": "2026-08-29T23:06:21+0000",
  "_lastSeenIsoNote": "Set to the run's START, not its end. wf_386bf586-bf1 ran 2026-08-03T12:20 -> ~12:54 (duration_ms 2008790); anything said during that window is still unreviewed, and re-reading it costs almost nothing while skipping it can lose a real bug.",
  "_lastRunNote": "status MUST be one of complete | running | stopped. `stopped` exists because a run the owner kills leaves `running` set forever, and the resume rule then re-fired on every subsequent turn — a restart loop that spawned wf_79ce66bb-efc while wf_62f4811d-7fe was already dead. NEVER auto-resume: a dead run and a slow one are indistinguishable from this file, so resuming is a guess. Report the state and ask.",
  "lastWrapIso": "2026-08-30T15:59:42+03:00",
  "_lastCleanShaNote": "The CLEANER's own watermark (X131) — a commit SHA, not a timestamp, and deliberately NOT lastWrapIso: they are two different clocks and reusing one for both is what conflated the cleaner's scope with the bouncer's. A sha because `git log --name-only <sha>..HEAD` is an exact range anchor, where a `--since` timestamp reads COMMIT DATE and silently skips or double-counts across a rebase, a cherry-pick or clock skew. ADVANCED 2026-08-28 by a second unscoped full sweep (Opus, owner-dispatched between wraps) — full src/ (205 files) + scripts/ (28) + .claude/*.md (52), detect-only over all YAML + framework surfaces; zero dead files/exports found, several dead-code/stale-citation fixes landed, uncommitted. Set to 45736ff7488aa14165ca30f83bffac6ed1c259ac (HEAD at the time — the wrap-4.7.5 bookkeeping commit; the cleaner never commits and deliberately never looked at the uncommitted tree on top of it, per C4). ONLY AN UNSCOPED SWEEP ADVANCES IT (X135) — a scoped run advances nothing, because this field means all of src/ is judged up to this commit. The rule and the two sentences the chat must print are in SKILL.md's `cleaner` entry and cleaner.md C4.",
  "lastCleanSha": "6bc8f4027fce0d5e078fb9b4a04c3ba7b844cb46",
  "_nextReportIdNote": "ONE counter for report-row ids; assign then increment, NEVER renumber and NEVER reuse. The FORMAT is defined in exactly one place — the `#` bullet in .claude/skills/manager/SKILL.md — and is deliberately NOT restated here.",
  "nextReportId": 261,
  "_inFlightNote": "What is being worked RIGHT NOW — add on dispatch, remove on return, and check it before dispatching so one item is never sent twice. EMPTY THIS IN THE SAME TURN A RUN RETURNS: a stale entry is the STOP 1 lock and refuses the next run. It went stale for three consecutive rounds on 2026-08-06 because every return fed straight into the next dispatch instead of into this file, and the final bouncer pass is what caught it.",
  "inFlight": [],
  "_pendingOverflowNote": "Verify DISCOVERIES and over-cap items, shaped for args.issues. Pass on the next `build` and the engine prepends them itself, then delete what manifest.carry names. RECONCILED 2026-08-30 (wrap-4.8.2): all 3 prior entries (gatekeeper-offday-hedge-recurs-on-date-range, permission-granted-reply-pays-unneeded-rewrite-call, createmeeting-requesterid-wrong-colleague-gets-event-rights) were built this wrap and drained. Replaced with the 3 new discoveries this wrap's own two build waves surfaced.",
  "pendingOverflow": [
    {
      "id": "notified-flag-must-follow-confirmed-send",
      "lane": "matchmaker",
      "severity": "medium",
      "clarity": "clear",
      "source": "verify",
      "symptom": "A closure record can claim the colleague was notified (closure_reason _and_notified_requester) even when the DM failed to send, there was no Slack connection, or the relay was skipped as already-notified.",
      "evidence": "src/utils/closeMeetingArtifacts.ts:766 -- verify discovery from the 2026-08-30 discoveries-fix wave, unrelated to that wave's own fixes."
    },
    {
      "id": "vanished-sweep-request-close-invisible",
      "lane": "matchmaker",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "symptom": "A vanished-meeting sweep that closes only a request (no meeting artifact) logs nothing and reports cleaned=0, so an externally-cancelled meeting behind a pending approval is cleaned up invisibly.",
      "evidence": "src/utils/cleanupVanishedMeetingArtifacts.ts:173 -- verify discovery from the 2026-08-30 discoveries-fix wave, unrelated to that wave's own fixes."
    },
    {
      "id": "brief-narration-rule-matches-unwritten-value",
      "lane": "instructor",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "symptom": "In the morning brief, a meeting-cascade closure narration rule matches a value nothing ever writes, so it silently never applies.",
      "evidence": "src/tasks/briefs.ts:564 -- verify discovery from the 2026-08-30 discoveries-fix wave, unrelated to that wave's own fixes."
    },
    {
      "id": "delete-approval-resolved-by-unrelated-positive-mutation",
      "lane": "registrar",
      "severity": "low-medium",
      "clarity": "clear",
      "source": "verify",
      "symptom": "The widened payloadReferencesMeeting can resolve a pending DELETE approval off an unrelated positive mutation on the same meeting -- owner moves/updates a meeting a colleague has an open cancel-request against, and it closes 'resolved' with a wrong locked-in-the-new-time message instead of leaving the cancel decision to the owner.",
      "evidence": "src/utils/closeMeetingArtifacts.ts:754-759,820 -- found during the 2026-08-30 pre-wrap adversarial pass (WRAP_UP.md step 9)."
    }
  ],
  "_verifiedCleanNote": "Passed to bugger/feature as `priorClean` so a verify does not re-audit settled ground. ONE PROVEN CLAIM PER ENTRY. DROP an entry the moment a wave changes the code it describes. PASS IT VERBATIM: `priorCleanDropped` keys on the INDEX in the array you passed (X84). UPDATED 2026-08-05 run (wf_5c89df46-081): dropped 15 of 98 entries invalidated by this run's diff (meetings.ts, ops.ts, calendarIssues.ts, autoMove.ts, checkHealth.ts, findAvailableSlots.ts, coordinator.ts citations), kept 83, added 19 fresh claims from this run's bouncer pass. Now 102 entries.",
  "verifiedClean": [
    "violation_kind 'owner_busy_collision' is emitted by scheduleRules rule 8 alone, and allowRelaxed makes rule 8 pass — so gh#165-d's new strict-reject stamp and the pre-existing relaxed-pass stamp are mutually exclusive per slot, and first-hit-wins is preserved by stampConflictingEvent's early return.",
    "humanizeViolationLabel has a real 'owner_out_of_office' case (src/skills/meetings/ops/violationLabels.ts:28), so gh#165-d's OOF label cannot collapse to 'unknown' and cannot misroute a known all-day collision into the \"I can't tell which rule flagged it\" message.",
    "Slack @mentions reach the orchestrator as \"Real Name (slack_id: Uxxx)\" (src/connectors/slack/app/helpers.ts:86-97), so gh#158-a's substring gate cannot drop a mention-named attendee. Any future reasoning about that gate can take this as established.",
    "gh#158-a can only SHRINK resolvedMeetingAttendees, and the gh#158 pre-check bail keys on that list being non-empty — so the only new behaviour on that seam is the pre-check running on a turn that genuinely named nobody, which is the correct answer for such a turn. The gate cannot make a bailing turn un-bail for a name the message did contain.",
    "o#180's newsTimedOut is assigned at exactly one site (src/tasks/briefs.ts:740, inside `if (!gathered)`), so a completed-but-empty news gather leaves the brief byte-identical to pre-fix. The quiet-day/no-apology rule is intact.",
    "`npx tsc --noEmit` is green with all eight wave files on disk (run once, after reading the full diff).",
    "wf_4bbfc750-1a9 VERIFY: `npx tsc --noEmit -p E:/Code/Maelle/tsconfig.json` is clean across the whole working tree with both gh#165-d edits in place — the two new optional fields on diagnosticsOut.conflictingEvent are structurally assignable at moveMeeting's narrower local diagnostics type, which declares no conflicting field at all",
    "wf_4bbfc750-1a9 SPOT-CHECK: the gh#158 colleague bail is present at HEAD — availabilityPreCheck.ts:517-541 returns the empty result (ran:false, no promptBlock) whenever namedAttendeeEmails is non-empty, and forgets any named instant from the hard-block ledger first, so the output-time floor has nothing stale to fire on",
    "WAVE-VERIFY 2026-07-30: availabilityPreCheck.ts — recordHardBlockedSlot is called only at :943 inside the verdict loop, unreachable from the :495 bail; grantRelaxed and policy_exception appear nowhere in the file",
    "WAVE-VERIFY 2026-07-30: all 25 `overCommitment` references read named subfields or truthiness; none spreads the object, so the new required `id` cannot reach another caller's payload",
    "WAVE-VERIFY 2026-07-30: availabilityPreCheck.ts:562 and :1149 compute `today` with the identical expression, so the main fallback extraction and the forgetter's extractRawPairs cannot disagree about what 'tomorrow' means",
    "WAVE-VERIFY 2026-07-30: eventsForWeek is reachable only AFTER the :495 bail returns — the live-read swap (152-refresh) and the gh#158 bail do not interact, and a bailed turn makes zero calendar reads",
    "wf_6852af85-afc VERIFY: gatherNews (src/skills/news.ts:255-304) can neither throw nor resolve to undefined — a whole-body try/catch returns {...EMPTY_BUNDLE} at :302 and every early return is a NewsBundle (:281, :299) — so `!gathered` in briefs.ts:707 is exactly and only the Promise.race timeout branch. gh#167's three-way branch rests on a true premise",
    "wf_27f03aca-0dd VERIFY: `npx tsc --noEmit` is clean across the whole working tree with all four fixes in place — every namedAttendeeEmails / locationHint / viewer / conflictingEvent / overCommitment.id signature change is applied consistently at every call site",
    "wf_27f03aca-0dd VERIFY: the new update_meeting(add_attendees) steer cannot produce an unapproved real-world action — moveMeeting.ts:162-210 refuses a non-requester colleague with `colleague_not_requester` and routes it to owner approval with a deferred_action_hint; only the meeting's own requester applies a change directly",
    "wf_27f03aca-0dd VERIFY: locationHint is a live, populated field end-to-end, not a dead parameter — args.location -> bookingRequest.ts:171 -> planMeeting.ts:161 -> planMeeting.ts:327/:348 -> detectCategory, and resolveLocation already consumes the same value at planMeeting.ts:462. No new fetch, no new LLM call",
    "wf_27f03aca-0dd VERIFY: resolveNamedInternalAttendees excludes the owner twice over — by name at resolveAttendeeEmails.ts:180 and by the exclude set at :211 — so a colleague asking only about the owner ('is Idan free at 12:30?') keeps a non-empty pre-check. The gh#158 bail cannot fire on the file's own primary use case",
    "wf_27f03aca-0dd VERIFY: gh#158's root cause is established from production logs, not from the lane's summary — logs/maelle-2026-07-28.log:669 (resolved levana.b@, 12:24:09.680) is followed by :671/:677 (pre-check normalized 2026-07-29T10:00, injected notBookable) and :721/:722 (11:00 injected bookable:1). The owner-scoped pre-check is where both wrong-subject answers came from",
    "wf_27f03aca-0dd VERIFY: the 2026-07-30 second wave adds no new part to the system — no new file, tool, skill, table, column, background job or output-path guard. Every addition is an optional parameter or field on an existing contract (namedAttendeeEmails, locationHint, diagnosticsOut.conflictingEvent, overCommitment.id), and the one deletion (findDuplicateEvent's null mode) removes a path rather than layering over it",
    "wf_6852af85-afc VERIFY: this wave adds no new part to the system — no new file, tool, skill, table, column, background job or output-path guard. The only contract change is the widening of an existing shared primitive (findDuplicateEvent), and both of its prior callers are unaffected",
    "wf_e2b7aeeb-325 VERIFY: `npx tsc --noEmit -p tsconfig.json` is clean across the whole working tree with all 20 fixes on disk (run once, after reading the full diff)",
    "wf_e2b7aeeb-325 VERIFY: coordinator.ts's two deleted UPDATE statements matched nothing on live data: every tasks row with skill_ref LIKE 'out_%' is cancelled/completed/failed/informed (13/40/8/14), none in the ('new','in_progress','pending_colleague') sets they targeted — the removal cannot orphan a legacy row",
    "wf_e2b7aeeb-325 VERIFY: mail.ts replyToMail's happy path is byte-identical post-fix; the cleanup deletes only the draftId createReply returned and always rethrows the original error, so sendDirect still reports send_failed and inbound.ts still treats the send as never-delivered",
    "wf_e2fd1caf-c84 VERIFY: HardBlockedSlot.durationMin can never be missing or Infinity: availabilityPreCheck.ts:807-812 and :818-823 set it under the identical guard as rejection_reason, allowed_durations is schema-non-empty (userProfile .min(1)), and recordHardBlockedSlot has exactly one caller (:959).",
    "wf_e2fd1caf-c84 VERIFY: the cloneable-default rewrite is output-identical for the only configured tenant: idan.yaml office_days [Monday,Wednesday,Thursday] union home_days [Sunday,Tuesday] sorted by WEEK_ORDER equals Sunday..Thursday, hours 09:00-18:00 — the same tuple as the deleted ISRAEL_DEFAULT.",
    "wf_e2fd1caf-c84 VERIFY: textScrubber.ts imports only luxon (its single import line, :20), so INTERNAL_WORK_ITEM_ID_RE's new home introduces no cycle and utils/securityGate.ts now has no dependency on core at all.",
    "wf_e2fd1caf-c84 VERIFY: multiple simultaneously-open outbound DMs to one colleague are real in live data (4 on 2026-04-26 to U02K5PN5K4Y, 4 on 2026-07-13 to U0F28ES4V), and no constraint prevents two rows with followup_closed_at NULL — so recentOutboundContext's new multi-candidate loop is reachable, not dead on arrival.",
    "wf_e2fd1caf-c84 VERIFY: `npx tsc --noEmit -p tsconfig.json` is clean across the whole working tree with all eleven fixes on disk (run once, after reading the full diff).",
    "wf_e2fd1caf-c84 VERIFY: no stragglers remain for this wave's renames and deletions: zero references anywhere in src or scripts to deterministic_under_10min, findOpenOutboundForColleague, DETERMINISTIC_WINDOW_MINUTES, or _attendee_status_note (one comment names the removal, which is correct residue).",
    "wf_94e9e397-06a VERIFY: gh#169-a cannot be bypassed by retrying a different approval_id: a second id that also matches the owner's message re-enters the same ambiguity refusal, and one that does not match fails namedMatch and lands on the pre-existing 'Not anchored' refusal. No id binds an ambiguous message.",
    "wf_94e9e397-06a VERIFY: o#190's four citations are exact at the current tree: forgetHardBlockedSlot has exactly five call sites (availabilityPreCheck.ts:954, :1183, :1189; runOutputGates.ts:1035, :1061) and each carries the rule the doc assigns it.",
    "wf_94e9e397-06a VERIFY: the hebrew tokenizer change is a pure widening for ASCII: \\p{L}\\p{N} is a strict superset of A-Za-z0-9, and the .filter((lower,i) => rawTokens[i]) index pairing is safe because .map preserves length and order — so English subject tokenization is unchanged apart from accented words no longer splitting.",
    "wf_94e9e397-06a VERIFY: `npx tsc --noEmit -p tsconfig.json` is clean across the whole working tree with all five of this wave's edits on disk (run once, after reading the full diff).",
    "wf_a964b44c-dc9 VERIFY: o#187's merge is verdict-preserving with no third role to leak through: skills/types.ts:93 types senderRole as exactly 'owner' | 'colleague', so the shared gate's inverted test (trust owner) is equivalent to the deleted inline test (gate colleague).",
    "wf_a964b44c-dc9 VERIFY: the two-part email reply mechanism is fully retired with no dead residue: zero references to CUT_LINE_RE / forYouPart / forwardPart / forYouVerdict / forwardVerdict anywhere in src or scripts, and only two historical rationale comments still name PART 1.",
    "wf_a964b44c-dc9 VERIFY: gh#175-a's prompt cut took no capability with it: the surviving forwardable block still demands each candidate time in every attendee's own local zone plus duration, subject and context, so the per-slot zone visibility PART 1's Zone line existed for is still required in the external text.",
    "wf_a964b44c-dc9 VERIFY: a Hebrew or Cyrillic 3-letter subject word can never become a distinctive needle regardless of the stopword list: the 3-char exception at closeLoopOnOwnerHandled.ts:163-169 requires /[A-Za-z]/ in the raw token.",
    "wf_a964b44c-dc9 VERIFY: gh#175-b's new field is additive-only on an existing contract: locationModeChangeNotice has exactly one reader (moveMeeting.ts:1232) and createMeeting ignores it, so no other PlanAction consumer is affected.",
    "wf_a964b44c-dc9 VERIFY: this wave adds no new part to the system — no new file, table, column, tool, skill, background job or output-path guard. The one new LLM call (classifyOwnerAssertsDecision) is a second instance of a pattern already in the same file, on a fallback path only.",
    "wf_a964b44c-dc9 VERIFY: `npx tsc --noEmit -p tsconfig.json` is clean across the whole working tree with all seven fixes on disk (run once, after reading the full diff).",
    "PRE-WRAP FINAL VERIFY: runOutputGates' new live re-check cannot false-clear on an outage: getOwnerEventsForDecision throws, the inner catch sets confirmed=true.",
    "PRE-WRAP FINAL VERIFY: the PRESERVE cross-reuse claim for gh#175-b holds regardless of the isOnline-signal defect — predictor and planMeeting source priorStartIso/existingLocation/existingIsOnline from the same Graph fields, so a same-day-type candidate hits preserve_existing in both and emits nothing.",
    "PRE-WRAP FINAL VERIFY: `npx tsc --noEmit` is clean across the FULL accumulated tree (all 5 waves + the direct gh#175-b re-fix), exit 0.",
    "wf_386bf586-bf1 VERIFY: move_meeting cannot execute on an email turn — absent from CHANNEL_TOOL_CLAMP.email and refused at both shipping (filterToolsByScope, registry.ts:310) and dispatch (registry.ts:675-681) — so the _attendee_busy_note half of the strip is an inert no-op, not a behaviour change on any channel.",
    "wf_386bf586-bf1 VERIFY: deferredActionReplay is not a second path for an owner-facing aside to reach an email reply: it hardcodes channel:'slack' (deferredActionReplay.ts:79) and only inspects the returned object for error/success:false/ok:false sentinels (:114-124) — it never narrates the payload.",
    "wf_386bf586-bf1 VERIFY: isSubjectStopword fails in the safe direction: over-filtering can only empty `tokens`, and with no token and no counterpart match messageReferencesRequest returns false (closeLoopOnOwnerHandled.ts:193), which blocks a close and leaves the row for tomorrow's brief.",
    "wf_386bf586-bf1 VERIFY: the Hebrew proclitic set is declared exactly once (closeLoopOnOwnerHandled.ts:128) and read by both hasWholeWordMatch and isSubjectStopword, and SUBJECT_STOPWORDS has exactly one filter call site — no parallel list, no second spelling to drift.",
    "wf_386bf586-bf1 VERIFY: the prefixed-stopword filter was reachable before the fix, so it is not a fix to a non-problem: a 6-char token like \"הפגישה\" passes the `lower.length >= 4` test at closeLoopOnOwnerHandled.ts:186 and was returned as a distinctive anchor.",
    "wf_386bf586-bf1 VERIFY: the batched classifier preserves the old fail-open semantics exactly: a throw, a non-numeric reply and an out-of-range number all resolve to candidates[0], which is the row the old per-candidate loop matched first via `return true`.",
    "wf_386bf586-bf1 VERIFY: `npx tsc --noEmit -p tsconfig.json` is clean across the whole working tree with all three of this wave's edits on disk, exit 0.",
    "wf_386bf586-bf1 VERIFY: this wave adds no new part to the system: no new file, table, column, tool, skill, spine, output-path guard or background job. The one signature change (classifyResponseVsNewTopic) is on a module-private function with a single caller, and the one new function (isSubjectStopword) is a private helper over an existing set.",
    "BOUNCER (uncommitted-tree pass): owner_override_not_applied is not a third owner-facing aside reaching an email reply — clampedRelaxedNotice (bookingRequest.ts:526-532) requires isMpim===true AND senderRole==='colleague'; the email leg is always senderRole:'owner', never MPIM.",
    "BOUNCER (uncommitted-tree pass): manage_working_elsewhere (the verbMap key the cleaner removed) had zero references anywhere in src, scripts or config — genuinely dead, not a fix to a non-problem.",
    "wf_1866d179-744 VERIFY: _attendee_busy_note is unreachable on the email leg for BOTH tools that share moveMeeting.ts: CHANNEL_TOOL_CLAMP.email is exactly {find_available_slots, create_meeting, get_person_memory, log_interaction}, and the note's only writer sits inside handleMoveMeeting past the :459 boundary. Removing its delete is inert.",
    "wf_1866d179-744 VERIFY: displaySubject returns the PRIVATE_MASK identity string and nothing else on the masked path (displaySubject.ts:104), so scheduleRules' `subj !== PRIVATE_MASK` test catches exactly the masked case and cannot swallow a real subject.",
    "wf_1866d179-744 VERIFY: claimChecker's new cross-reference resolves to real content: RULE A at :201 states the object-scoped exemption plus all three exclusions in full and declares itself canonical for the whole prompt.",
    "wf_1866d179-744 VERIFY: tasks/index.ts's `AND type IN (DISPATCHERS keys)` cannot hide a live task: TaskType's union (tasks/types.ts:26-45) is exactly the five keys in DISPATCHERS (dispatchers/index.ts:25-31), so only legacy type strings are excluded.",
    "wf_1866d179-744 VERIFY: availabilityGate.ts's corrected citations are exact: forgetHardBlockedSlot is called in runOutputGates.ts at :999 and :1025 only (plus the :903 import).",
    "wf_1866d179-744 VERIFY: getTenantWorkdaysForTimezone is warm on every application path: loadAllProfiles (src/index.ts:58, boot) calls loadUserProfile for each YAML, and loadUserProfile is the function that populates profileCache (userProfile.ts:681) — the same guarantee getProfileByEmail already relies on.",
    "wf_1866d179-744 VERIFY: known_contacts has zero readers and zero writers in src outside the v3.2.0 migration's own drop, so client.ts:186's DROP cannot remove data any code reads.",
    "wf_1866d179-744 VERIFY: removing the 24h SQL cutoff in recentOutboundContext changes only which rows can be auto-expired, never which row can be matched: findOpenOutboundsForColleague has one caller (:315) and pass 1 still filters every >24h row out of `eligible` before the classifier.",
    "wf_1866d179-744 VERIFY: venues.ts's inline of isTeamsPlaceholderLocation is behaviour-identical: same two comparisons, same order, same returns, against the same already-lowered/trimmed input.",
    "wf_1866d179-744 VERIFY: the gh#180-b change cannot regress the base case: with a conversationKey and an empty anchor cache, shadowNotify.ts:120-140 still sends a plain top-level owner DM, so an auto-move with no subsequent revert produces byte-identical output to pre-wave.",
    "wf_1866d179-744 VERIFY: scripts/measure-prompt.ts is gone at HEAD with no remaining functional reference in src/, scripts/, or package.json; scripts/measure-prompts.cjs is untouched and still the live tool.",
    "wf_1866d179-744 VERIFY: `npx tsc --noEmit` is green across the whole working tree with all 18 of this wave's changes on disk, and again after both bounced rows' second-attempt edits.",
    "wf_1866d179-744 VERIFY: this wave adds no new part to the system — no new file, table, column, tool, skill, background job or output-path guard. getTenantWorkdaysForTimezone is a second cache-only accessor on an existing pattern (getProfileByEmail), not a new subsystem.",
    "wf_0a380292-84a VERIFY: the gh#179-a relay append cannot be mis-keyed by the channel argument: getConversationHistory selects on thread_ts alone (conversations.ts:14) and appendToConversation upserts ON CONFLICT(thread_ts) (:40), so res.ref only ever affects channel_id on a brand-new row.",
    "wf_0a380292-84a VERIFY: scripts/ contains exactly 22 .cjs/.mjs/.ts files including scripts/local, and both scripts/measure-prompts.cjs and scripts/_dump-prompts.cjs exist — ARCHITECTURE_MAP.md:86's corrected count and names are accurate.",
    "wf_0a380292-84a VERIFY: this wave adds no new part to the system — no new file, table, column, tool, skill, output-path guard or background job. isKnownRequestThreadAnchor is one query on the existing requests spine, DISMISSAL_NEVER_EXPIRES is a sentinel on an existing field, and the resolver's append reuses the pattern already at coordinator.ts:344/meetingReschedule.ts:269/briefs.ts:891.",
    "wf_0a380292-84a VERIFY: `npx tsc --noEmit -p tsconfig.json` is clean across the whole working tree with all nine of this wave's changes on disk, exit 0.",
    "wf_0a380292-84a VERIFY: working tree typechecks clean at HEAD (npx tsc --noEmit, no diagnostics).",
    "BOUNCER 2026-08-04: MPIM slack-id leak fix's two branches traced against SLACK_ID_SHAPE regex — fabricated value drops to '', genuine id converts to <@U...> with lead whitespace preserved; GRAPH_ID_RE/BARE_SLACK_ID_RE/SENTINEL_RE do not collide with the new rule.",
    "BOUNCER 2026-08-04 (re-check): both dead strip branches (_attendee_busy_colleague_note, _attendee_unverified_note) are confirmed unreachable on the email leg — isOwnerInitiatedSearch is always true there (sole construction site inbound.ts:307) — removing them was a pure deletion, not a behavior change.",
    "BOUNCER 2026-08-04: getRevertibleAutoMove and getRecentlyAutoMovedEventIds (requests.ts:376,397) both require closure_reason='auto_move_executed' — gh#180-c's cancelled-on-verify-fail record is neither revertible nor read as a recent auto-move by move_meeting's own-change detector.",
    "calendar_issues enforces UNIQUE (owner_user_id, event_id) on the LIVE table (read back from sqlite_master; zero rows in GROUP BY owner_user_id, event_id HAVING COUNT(*)>1) — any future claim that one event can hold two issue rows is refuted at the schema, and dismissOverlapIssue's SELECT keys on exactly that pair.",
    "findMeetingOwner admits an id via the Graph-organizer fallback (findMeetingOwner.ts:103-126) as well as via a requests row, and returns requesterSlackId null when neither resolves — so the organizing-not-attending gate cannot admit a stranger's event, though its comment names only the requests spine.",
    "requester_is_attending is a live tool arg on find_available_slots (meetings.ts:361) with an explicit EA/organiser instruction at :1238 — the organizing-not-attending branch is model-reachable, not dead.",
    "Both defrag abut helpers set fix_failed AND fix_error on every failure path (autoMove.ts:378 pull, :442/:475 push, plus the shared executeInternalAutoMove at :152/:208), so checkHealth.ts:1075's preservation branch is reachable; pull's :378 and push's :475 messages are byte-identical, which is what its `!==` guard exists to suppress.",
    "verify.got is a pre-formatted human string in the owner's zone (\"EEE d MMM 'at' HH:mm\", calendarReads.ts:1333), not a raw ISO — the auto-move read-back message is readable as written.",
    "This wave adds no new part to the system — no new file, table, column, tool, skill, background job or output-path guard. The only additions are an in-memory optional field (AttendeeAvailabilityEntry.assumed), a hoisted local, a moved util, and prose; the one new cross-module dependency (find_available_slots -> findMeetingOwner) reuses an existing helper on the existing requests spine.",
    "meetings.ts:1132-1139 contains no definition of attendee_status's per-value semantics ('busy'/'tentative'/'oof' vs 'free') — only that it is a conflict tag and whose calendar it names — so the concrete mapping restored in _attendee_unverified_note duplicates nothing in the system prompt.",
    "`npx tsc --noEmit -p tsconfig.json` is clean across the whole working tree with all 16 rows of the 2026-08-05 wave on disk, exit 0.",
    "runTiers revert BOUNCER 2026-08-06: git diff HEAD on calendarReads.ts shows zero changed code lines - the runTiers reorder is a genuine, complete revert to the original clean-strict -> WE-soft -> clean-relaxed order, with only an added historical comment.",
    "less_preferred_label BOUNCER 2026-08-06: the field is populated only when over_optional is a non-empty string, and interpolates nothing from it after the final wording pass (bare 'less preferred' string, no subject, no re-derive) - no new leak class.",
    "less_preferred_label BOUNCER 2026-08-06: ops.ts's email-leg scrub (~136-147) deletes less_preferred_label in the same slot walk as over_optional, same channel==='email' gate - Slack is unaffected, the email leg gets neither field.",
    "`npx tsc --noEmit` clean across the whole working tree with the runTiers revert + less_preferred_label (all 3 wording rounds) on disk.",
    "BOUNCER 2026-08-10 (hand-run wave): src/db/calendarIssues.ts's getSuppressedEventIds gates on status IN ('approved','dismissed','resolved'), so the new missing_category suppression pre-filter cannot suppress a not-yet-settled question.",
    "BOUNCER 2026-08-10: src/db/migrations/v4_5_3_calendar_issues_axis.ts backs up before the destructive change, asserts row-count parity inside the transaction, is idempotent via hasColumn, and no foreign key anywhere references calendar_issues.",
    "BOUNCER 2026-08-10: src/utils/calendarDensity.ts's bidirectional earlierConnectiveStart (gh#188) introduces no circular import — calendarDensity does not import floatingBlocks — and no overlap-producing candidate is reachable on either side given a free requested slot.",
    "BOUNCER 2026-08-10: src/skills/meetings/ops/handlers/calendarReads.ts:780's broadened notFound match sits on the FIRST Graph call in the delete path, so the invalid-id cascade is cut before it starts.",
    "BOUNCER 2026-08-10: scripts/check-design-door.cjs section 35 genuinely invokes ledger-stats.cjs via execFileSync against a temp ledger and asserts field-by-field — not a mocked check.",
    "BOUNCER 2026-08-10 (retry verification): the email leg (transport:'email', set at inbound.ts:344) is the dispatch condition routing to runEmailLegGates — ctx.transport!=='email' cannot be true there, so gh#194-b's backstop is provably excluded from the email leg.",
    "BOUNCER 2026-08-10 (retry verification): context.authority is a required, unclamped field on SkillContext (skills/types.ts:105) — freeform-escalation-refused-silently-drops-owner-question's context.authority!=='colleague' gate is correct in both directions (room-clamped owner skips, real colleague in DM or room still fires).",
    "BOUNCER 2026-08-10 (retry verification): approval-amend-routes-through-reschedule-not-merge's two named exits are both real — resolving the approval drops it from getAwaitingOwnerRequests, and messaging from another thread fails the owner_dm_thread_ts/terminal_dm_msg_ts===context.threadTs test. Neither is a guess.",
    "findAvailableSlots' new wrong_day_type write is reachable and collision-free: workHours.ts:134 lets a per-date override set isWorkday=false on a workweek-named day, and the oofDayKeys branch at findAvailableSlots.ts:964 `continue`s every cursor step of an OOF day, so the two branches can never write the same dayKey.",
    "rejectedCounts (findAvailableSlots.ts:799) and dayReasons (:805) are both declared INSIDE the auto-expand while(true) at :305, so they reset per expansion iteration and the widened :1431 gate preserves last-iteration-wins.",
    "The only day_summary consumer outside the meetings handler, turnHelpers.ts:254-279, reads it exclusively when slots.length===0 and only to name a top reason — populating daySummary in more cases cannot change a successful search's claim-checker summary.",
    "meetings.ts:215 explicitly authorizes the \"day off / not a workday\" narration for top_reasons===['wrong_day_type'], so reusing that label for an override off-day is narratable truthfully and is not limited to the home-day/in-person example at :207.",
    "markSubjectRaised has exactly one caller — logEngagement.ts:206-208, on coda DELIVERY — so a validator-dropped coda never stamps last_assistant_initiated_at, and gh#198's resolve-on-read sweep (stateMachine.ts:300-311, gated on that stamp) could not already have applied negative feedback for a dropped coda.",
    "recordSubjectUnanswered's `last_assistant_initiated_at = NULL` write is a no-op on the new call path: the picker only offers a subject whose marker is already clear (stateMachine.ts:313-317 plus the resolve-on-read pass above), so the new call cannot erase another raise's pending reconciliation.",
    "Logging a bounded reply preview is an existing pattern, not a new disclosure class: handlers.ts:362 (80 chars), routine.ts:322 (120), availabilityGate.ts:647 (200) and postReply.ts:585 (40) already log message/draft text, and the new line reuses this file's own 350-char shadowPreview (postReply.ts:163-167).",
    "`npx tsc --noEmit -p E:/Code/Maelle/tsconfig.json` is clean across the whole working tree with all six wave files on disk, exit 0 (run once, after reading the full diff).",
    "This wave adds no new part to the system — no new file, table, column, tool, skill, spine, background job or output-path guard, and no new LLM call. Every change is one log line, one RULES sentence in an existing prompt, one conjunct on an existing gate, one dayReasons entry, and one call to an already-shipped counter.",
    "30 open GitHub issues pulled (`gh issue list --state open --json number,title,labels --limit 60`) — all Feature/Idea/Roadmap items; none is satisfied, partially satisfied or contradicted by this wave.",
    "sendOofReengagement's return type is 'closed'|'rearmed' and its sole caller runColleagueOofRecheck already types for both -- the widened return cannot break a caller.",
    "coordinator.ts's closeOutreachReplyIfResolvedThisTurn Slack post now gates on (mutated||booked) only; logEvent remains unconditional across mutated/booked/freshApproval since it is a durable get_briefing-only trace, never a live notification.",
    "npx tsc --noEmit -p E:/Code/Maelle/tsconfig.json is clean across the whole working tree with all 7 files of the o#245/246/247/248/250 wave on disk, exit 0.",
    "The runner's work-hours re-arm terminates: nextWorkingHourStart returns the current instant once inside the window, so the fire re-armed at colleagueBase fails its own >now+60s test and sends. An invalid tz degrades to Date.parse(...)=NaN -> false -> send, never a throw or a stuck row.",
    "no_issue_tracking is referenced in exactly one file (checkHealth.ts, two sites) across all of src/ -- findAvailableSlots and attendeeAvailability never filter on it, so a mixed private meeting was already treated as busy by the slot finder and the defrag could not write a move on top of it even before this wave.",
    "For this owner a mixed private meeting can never become an auto-move candidate: private_emails holds one external gmail address (config/users/idan.yaml:226), so isProtected always returns 'has external attendee' and checkHealth.ts:1326 / :1350 skip it. The carve-out can only add an occupant, never a movable.",
    "npx tsc --noEmit -p E:/Code/Maelle/tsconfig.json exits 0 with all three wave files on disk (run once, after reading the full diff).",
    "gh issue list --state open: 29 open issues, all Feature/Roadmap/Idea or Improvement+Idea; none is satisfied, partially satisfied or contradicted by this wave. #108 (cross-midnight work hours) is adjacent to the work-hours gate but is neither advanced nor contradicted by it.",
    "No shape change (B6 clean): the two new closure_reason values are free-form strings consumed by the brief's generic closure narration at src/tasks/briefs.ts:561-565 — no enum, no schema migration; channel_id/channel_name/attachments ride the existing request `details` JSON, deliberately not a new outreach_jobs column.",
    "npm run typecheck (tsc --noEmit) is green across all four touched files.",
    "bouncer narrow recheck 2026-08-23: skipsIssueTracking (checkHealth.ts, renamed from isMixedMeeting during bounce round 1) collapses the no_issue_tracking exclusion to one boolean at both call sites (:398, :1296) with no category-membership re-check remaining at either -- the dedup this row's predecessor claimed but only half-delivered is now genuinely complete.",
    "bouncer narrow recheck 2026-08-23: `npx tsc --noEmit -p tsconfig.json` is clean across the whole working tree with all 5 bounce-round fixes on disk, exit 0.",
    "outreach.ts's deleted `colleagueBase > sendAt` ternary is OUTPUT-identical, not just instant-identical: toTimerInstant (core/requests/types.ts:302) and colleagueWorkTimeBaseFromNow (utils/responseDeadline.ts:127) both end in `.toUTC().toISO()`, and nextWorkingHourStart returns its `fromMs` DateTime untouched when the anchor is inside the window (responseDeadline.ts:39) — so the equality case emits the same string to the millisecond.",
    "nextWorkingHourStart can never return an instant earlier than its `fromMs` argument: its three exits are `start` (only when dt < start), `dt` itself, and next-day hoursStart after a +1 day step (responseDeadline.ts:34-43). This is the premise every colleague-hours floor rests on, and it holds.",
    "isColleagueSendDeferred preserves the old fail-open exactly: an invalid timezone yields toISO()===null, Date.parse of that is NaN, the comparison is false, and the send proceeds — identical to the four inline copies it replaced.",
    "All four rearm sites keep their own nextCheckHandler after the swap (colleague_oof_recheck, oof_reengage_reask, reschedule_reask, send_scheduled_outreach) — the shared helper introduced no cross-wiring.",
    "runner.ts:289's `> Date.now() + 60_000` is the OWNER work-hours nag gate (workTimeBaseFromNow plus an expires_at comparison), a different rule on a different subject — correctly NOT folded into the colleague helper, and not a surviving duplicate.",
    "SendOptions.attachments is live end to end on a channel post: Connection.postToChannel (connections/types.ts:160) -> connections/slack/index.ts:90 -> messaging.ts:255-260 uploadAttachments. The plumbing behind the outreach fix is real; only the model-facing schema line is wrong.",
    "outreach.ts's DM branch surfaces no `attachments_failed` to the model either (594-597), so the immediate channel-post fix introduces no new reporting asymmetry between the two branches.",
    "downloadSlackImage's error branch always carries `detail` (vision/index.ts:42 declares `detail: string`; every return at :61/:70/:80/:89/:99 sets it), so processMessage.ts:63's added log field can never be undefined.",
    "The new image regex cannot regress the multi-image case: the producer joins URLs with a single space (processMessage.ts:350) and the old `\\S+` already stopped there — the change only affects the last/only URL, which used to swallow the closing bracket.",
    "Image re-attach is owner-1:1-only and only when no fresh image arrived this turn (processMessage.ts:325) — no colleague, MPIM or channel path can reach the changed regex.",
    "threadStatusState's queued-status timer closes over `state` rather than re-reading the map (messaging.ts:575-586), so an evicted entry's timer still delivers and re-inserts, and the re-insert cannot loop (the re-inserted entry has no pending). The new comment on that branch is accurate.",
    "B6 clean: this wave adds no new part of the system — no new file, table, column, tool, skill, spine, background job, output-path guard or LLM call. The only additions are one exported helper inside an existing util (replacing four inline copies), one third argument on an existing Connection call, one regex character class, and two log fields.",
    "`npx tsc --noEmit -p tsconfig.json` is clean across the whole working tree with all six wave files on disk, exit 0 (run once, after reading the full diff).",
    "29 open GitHub issues pulled (`gh issue list --state open --json number,title,labels --limit 60`) — all Feature/Improvement paired with Idea/Roadmap/Next; none is satisfied, partially satisfied or contradicted by this wave.",
    "src/connections/slack/index.ts:86-92 forwards opts.attachments verbatim into messaging.ts postToChannel, which genuinely consumes it (withAttachments -> uploadAttachments, messaging.ts:254-261) on both the first-try and the auto-join retry — the Connection hop does not swallow attachments, no need to re-check this seam for any future attachment fix.",
    "touchThreadStatusState (src/connections/slack/messaging.ts:531) has exactly three call sites — 565, 581, 589 — and every entry any of them inserts has `timer` and `pending` either both set or both unset, so the eviction branch's `pending` test is a complete test of \"is work still owed here\".",
    "The status throttle timer (src/connections/slack/messaging.ts:575-587) closes over the state object rather than re-reading the map, so removing a key cannot cancel it and its callback re-inserts the same object — the 500-entry bound still holds after the eviction change.",
    "npm run typecheck is green across the full wave tree (all six modified src files), so the added third argument at outreach.ts:472-476 matches Connection.postToChannel's declared SendOptions shape.",
    "wf_d649e3b2-1f0 VERIFY: Typecheck is green across the whole working tree — npx tsc --noEmit -p tsconfig.json, exit 0.",
    "wf_d649e3b2-1f0 VERIFY: the outreach attachments tool-description is true on every path it names — immediate DM outreach.ts:587, immediate channel outreach.ts:475, scheduled DM runner.ts:727, scheduled channel runner.ts:688, stash at outreach.ts:377, upload at messaging.ts:254-261 — all six sites read directly.",
    "wf_d649e3b2-1f0 VERIFY: the four code citations in the rewritten librarian L7 are line-exact at HEAD (stateMachine.ts:402/423-424; orchestrator/index.ts:1021-1026, :1470; the kept :172-184), and turnLeftWorkPending has exactly ONE assignment and ONE read in the repo.",
    "wf_d649e3b2-1f0 VERIFY: the availabilityGate docstring's two new false-fire entries reproduce verbatim from live Cloud Logging (2026-08-24T12:07:06/08/12/13Z) including both drafts and the shared collision.",
    "wf_d649e3b2-1f0 VERIFY: the image re-attach regex widening is contained — reattachRecentThreadImage has exactly one caller, gated to owner 1:1 with no fresh image (processMessage.ts:332); each history row carries at most one '[Image …]' block, so the lazy prefix cannot cross into another message.",
    "wf_d649e3b2-1f0 VERIFY: the new delete_meeting resolver branch is correctly scoped to tool==='delete_meeting' — move_meeting/update_meeting hitting the same sentinel still fall to the generic awaiting_owner path; requester wording does not overclaim (matches the genuine-success template).",
    "wf_d649e3b2-1f0 VERIFY: no path in the delete_meeting resolver diff can close a request as cancelled on an unverified 404 — the only close on that route is gated on approveData.confirmed_gone===true, a key never advertised in the tool schema.",
    "src/connectors/graph/findAvailableSlots.ts — the two day-type comment blocks (:871-878, :1440-1451) are true at HEAD: the per-date off-day gate writes `vacation_or_off_day` (:1019) and the in-person/home-day mismatch writes `wrong_day_type` (:1028), and those are the only two writers of either label into `dayReasons`. No code path changed; nothing to re-check here next wave.",
    "src/skills/outreach.ts:466 — the `:340` citation resolves to the actual `const attachmentsArg` declaration. Verified by reading :320-346, not by trusting the lane's claim.",
    "notifyRequesterOfDecision works correctly with a ctx of `{profile}` alone: every consumer of ctx in the function body is `ctx.profile` (:1024, :1045, :1204, :1294) plus two optional flags (`wasAwaitingColleague`, `alreadyMessagedRequesterIds`); the Slack send goes through the connections registry (:1038-1039), never `ctx.app`. So the new call site cannot fail for want of an App — the second flag being absent is a behavioural gap, reported as part of the overturn, not a crash.",
    "The new `closed_by_owner` arm is reachable and correctly wired at every terminal-outcome site: the double-notify stamp (:955), the composer branch (:1187/:1247) and stampIfTerminal (:1307) all include it, so it cannot half-behave as a terminal verdict. No other switch over the verdict union was left unhandled — tsc --noEmit is clean over the full program.",
    "The wave compiles: `npm run typecheck` (tsc --noEmit) run by me at HEAD, zero output.",
    "The KPMG live failure was a SAME-TURN race (identical threadTs 1787632273.667359 on the 08:53:44 bind and the 08:53:48 close), so a turn-scoped Set is the right shape — a cross-turn race would have needed persisted state.",
    "Cost of the added compose is measured, not estimated: exactly 1 scanner close in 30 days of live logs, so ~1 extra Haiku call per month — nowhere near a W12.2 sign-off threshold.",
    "npm run typecheck (tsc --noEmit) is clean at HEAD (wf_e3cb45de-439, all 4 built fixes on disk).",
    "wf_580bb8d0-6c7 VERIFY: requests.owner_user_id is the owner's Slack user id, not an internal/tenant key: it is the key passed to getConnection (resolver.ts:1005, requesterRelay.ts:132) and reads U0F28CK6H on live-born rows — so `requesterSlackId === row.owner_user_id` can only ever be true for the owner himself and cannot suppress a colleague relay.",
    "wf_580bb8d0-6c7 VERIFY: after this wave no requester-facing DM path can target the owner: the complete set of sites that send to requester_slack_id is resolver.ts:1386, requesterRelay.ts:157-158, briefs.ts:953 and colleagueOofReengage.ts:261 — the first two are now guarded, briefs.ts:944 already excludes ownerUserId, and colleagueOofReengage's id comes from OOF detection on a real colleague.",
    "wf_580bb8d0-6c7 VERIFY: the pre-fix send was genuinely self-referential, not hypothetical: the closed_by_owner body is the deterministic third-person template at resolver.ts:1067 ('X closed this out himself — nothing more needed from you here'), and the LLM re-compose prompt at :1219 keeps the same third-person framing.",
    "wf_580bb8d0-6c7 VERIFY: runReminderFire frames a third-party reminder off target_slack_id gated by remindingSomeoneElse (runner.ts:335-348), never off requester_slack_id — an owner-created self-reminder cannot produce an 'X asked me to remind you' about himself. No further instance of this symptom class hides in the reminder path.",
    "wf_580bb8d0-6c7 VERIFY: no new part of the system — no new file, table, column, tool, skill, spine, background job, output-path guard or LLM call; the wave is two conjuncts on two existing early returns plus comments.",
    "wf_580bb8d0-6c7 VERIFY: `npm run typecheck` (tsc --noEmit) run by me at HEAD with both edits on disk: zero output, exit 0.",
    "wf_580bb8d0-6c7 VERIFY: 30 open GitHub issues pulled (gh issue list --state open --limit 60) — all Feature/Improvement paired with Idea/Roadmap/Next; none is satisfied, partially satisfied or contradicted by this wave.",
    "runOutputGates.ts builds approvalGrantContext only inside `if (latestRow)` (:417), so rewriteOwningTheMiss's new permission_granted comment claim — 'routed here only when a request row EXISTS for the thread' — is TRUE, not an assumed premise. Any future reasoning about that seam can take it as established.",
    "claimChecker.ts:1028's four-way prompt ternary is exhaustive and order-safe: each guard is a distinct actionType equality (invented_fact -> ungrounded_slot_claim -> permission_granted -> generic), so no branch can shadow another and the generic phantom-action prompt is still reached for every other action type.",
    "createMeeting.ts's requesterId is zeroed for the owner at :231 before any consumer reads it, and :1959 was the LAST consumer still re-deriving from senderRole — after this wave no site in that handler derives requester identity from the surface-clamped role.",
    "skill.ts:703's new condition is shape-identical to its two siblings (:90, :340), and for a genuine colleague `authority` is always 'colleague' (processMessage.ts:145) — so the real-colleague mint, the only high-volume path, is behaviourally byte-identical to pre-fix.",
    "deriveOriginSurface (core/requests/types.ts:278) returns 'room' off origin_is_mpim BEFORE it reads initiated_by_role, so the residual `initiated_by_role='colleague'` still stamped on an owner-in-room approval (skill.ts:1266) cannot mis-narrate the surface. That residual is inert, not a latent second half of the bug.",
    "`npx tsc --noEmit -p tsconfig.json` run by me at HEAD with all three wave files on disk: zero diagnostics, exit 0.",
    "No new part of the system (B6 clean): no new file, table, column, tool, skill, spine, background job, output-path guard or LLM call. The claimChecker addition is a fourth branch in an existing prompt family reusing the same tool schema and the same resolveMinimalRedactionFallback helper.",
    "30 open GitHub issues pulled (gh issue list --state open --limit 60) — all Feature/Improvement paired with Idea/Roadmap/Next. None is satisfied, partially satisfied or contradicted by this wave.",
    "Both claimChecker symptoms are established from live production logs by me, not relayed from the lane: the permission_granted inversion actually SHIPPED to a colleague (2026-08-27T17:33:00Z) and the owner_fact hedge rewrite fired with no veto (2026-08-29T20:02:09Z)."
  ],
  "_carryNote": "The last run's own counts, copied off its manifest the moment it returned. A number the engine did not emit is null — never omitted and never estimated, because absent must read as not-recorded.",
  "carry": {
    "editorRan": null,
    "fromEditor": 5,
    "fromQueue": 0,
    "fromBacklog": 0,
    "buildable": 5,
    "dispatched": 5,
    "built": 5,
    "confirmedOtherLane": 0,
    "alreadyFixed": 0,
    "needsOwner": 0,
    "stillBlocked": null,
    "neverDispatched": null,
    "newlyVerbed": 0,
    "newlyVerbedBuild": 0,
    "rounds": 2,
    "onHisDesk": 0,
    "decisionBudget": 12,
    "overBudget": 0,
    "verifyRan": true,
    "fixesToCheck": 5,
    "waveFilesNamed": 4,
    "overturned": 0,
    "discoveries": 3,
    "ticketsTouched": 0,
    "verifiedCleanReturned": 14,
    "priorCleanDropped": 0,
    "earnedRules": null,
    "invariantsTagged": 0,
    "invariantsOf": 5,
    "bounced": 2,
    "bounceEligible": 2,
    "bounceRecheckRan": true,
    "outcomeTraced": 3,
    "outcomeCandidates": 5,
    "jointTraced": 0,
    "jointCandidates": 0,
    "observableChecked": 5,
    "observableClosed": 5
  },
  "lastRun": {
    "id": "hand-dispatched-2026-08-30-evening",
    "status": "complete",
    "startedAt": "2026-08-30T19:35:00.000Z",
    "note": "Live regression response, same evening as the 4.8.2 wrap: a colleague's plain availability question got hedged instead of answered. Root-caused to CLASS 2 (built and shipped THIS SAME DAY in 4.8.2) misreading the availability precheck's correct zero-tool-call answer as an unverified fabrication. Owner's ruling: revert CLASS 2 outright rather than patch around it. Root cause then fixed properly: the precheck's verdicts now thread into OrchestratorOutput.toolSummaries as synthetic lines, recognized as real grounding by the slot_grounding checker (matchmaker built the producer, gatekeeper built the consumer recognition, sequential dependency hand-off). Verified by a NEW mechanism, built concurrently this same evening by another session in response to this exact incident: the golden-path battery (.claude/GOLDEN_PATHS.md, 30 fixed regression items) now runs before every wrap's Fable pass. This wrap: battery ran clean (30/30 pass, only harmless stale-anchor citations); one full-tree Fable pass found one real issue in the battery's own framework wiring (a J-to-Z item rename missed 4 references, would have caused a false zero-item alarm) -- architect fixed it, a second Fable pass confirmed clean. Cleaner also ran an unscoped incremental sweep in parallel (16 commits since last watermark), fixed one stale src citation and corrected two doc-vs-code contradictions in project_architecture.md. 2 built items ledgered (the CLASS 2 revert, the precheck-grounding fix). Nothing committed until this wrap.",
    "notBuilt": []
  }
}
