{
  "lastSeenIso": "2026-07-29T15:50:00+0300",
  "_lastSeenIsoNote": "Set to the run's START, not its end. wf_b81e5b49-35b ran 17:16-18:18; anything said during that hour is still unreviewed, and re-reading an hour costs almost nothing while skipping it can lose a real bug.",
  "_lastRunNote": "status MUST be one of complete | running | stopped. `stopped` exists because a run the owner kills leaves `running` set forever, and the resume rule then re-fired on every subsequent turn — a restart loop that spawned wf_79ce66bb-efc while wf_62f4811d-7fe was already dead. NEVER auto-resume: a dead run and a slow one are indistinguishable from this file, so resuming is a guess. Report the state and ask.",
  "lastRun": {
    "id": "wf_33541300-121",
    "status": "complete",
    "notBuilt": ["158-attendee-misresolution"],
    "reportRows": [160, 161, 162, 163, 164, 165, 166],
    "note": "Owner-triggered full pass, 2026-07-29 afternoon, invoked by scriptPath (not name). WIDE WINDOW: sinceIso 2026-07-27T17:16 means ~2 days unreviewed, and that window contains the whole email-leg rollout and his testing of it. alreadyBuilt passed EMPTY and that is correct - HEAD d1898e0 is deployed (boot 13:50:12Z, 4.3.1) and the tree was clean at launch, so nothing is built-but-undeployed. openKnown carries 12 rows: 7 deferred, 4 converted (gh#154, gh#155, infra chat), plus gh#52/gh#45 parked at the 4.3.1 wrap. Tree clean at launch means the verify sees ONLY this run's work - the cleanest verify condition in a week."
  },
  "_previousRun": {
    "id": "wf_b81e5b49-35b",
    "status": "complete",
    "note": "First run on the new scout pipeline. 8 agents, 62 min, 4 lanes (meeting/requests/slack/context), 394 tool uses, 1.14M subagent tokens. 7 findings -> 8 atomic: 4 built (C #153, H #149, G #150, D>dep), 2 needs-owner (A, F), 1 needs-shaping (B), 1 already-fixed (D). Verify ran, overturned 1 (A). ENGINE BUG: manifest reported alreadyBuilt.passedIn:0 although 11 refs were passed in args - the payload never reached triage, and the direct consequence is the `already-fixed` warning (D paid a full dispatch for nothing). Sixth instance of the 'a step did nothing while the run reported success' class; belongs to the infrastructure chat, NOT to a bug lane. Log review itself was healthy: cutoffUtcUsed 2026-07-26T16:19:00Z matched the watermark and filesRead covered BOTH the 07-26 and 07-27 files."
  },
  "lastWrapIso": "2026-07-30T12:06:00+0300",
  "_lastWrap433Note": "v4.3.3 wrapped + pushed as 0d61f42 on 2026-07-30 12:06. FRAMEWORK ONLY — zero src/ files in the diff, which is why it is a PATCH and why NO DEPLOY IS OWED: production at 4.3.2 already equals HEAD for every line of product code, so `alreadyBuilt` is legitimately EMPTY on the next run and the verify sees only that run's own work. 16 framework items, 12 of them the same shape (a mechanism that does nothing and looks exactly like success): the report may no longer print a number it did not compute, the ledger can record a completion and a refusal, a row whose cited file has moved is flagged RE-READ, the tier check reads each charter's model line, and scripts/check-syntax.cjs replaces `node --check` (which exits 0 without parsing an engine). Backlog is now a source on EVERY run. Architect ledger: 49 rows, 0 open. NOT verified by the verifier and that is deliberate — no src/ change reaches a real person from this diff, each item was proven by execution against the real engine, and all six framework files pass check-syntax with their canaries failing as required.",
  "_lastWrap432Note": "v4.3.2 wrapped 2026-07-30 00:35. PATCH per the owner explicitly ('patch version, not minor') even though the framework half added a new agent, a new ledger file and a new script - his call, and the CHANGELOG names it under Changed rather than burying it. Bundled the architect chat's framework work as instructed ('take all uncommitted code, including the framework code'): SESSION_STARTER, all seven lane charters, the manager SKILL, bugger.js, feature.js, ledger-stats.cjs, plus the new architect.md / architect-ledger.jsonl / architect-file.cjs. NOTHING WAS CLOSED on GitHub - gh#156, gh#157 and gh#158 are all PARTIAL and were commented rather than closed, because the verify would not count gh#158's first complaint as covered while row 160 stays prompt-only and unproven, and each ticket has at least one complaint deferred to 2026-07-30. Six product fixes across three files; 3 product files vs 18 framework/bookkeeping files in the diff.",
  "openIssues": [],
  "pendingOverflow": [],
  "_inFlightNote": "What is being worked RIGHT NOW: [{ref, lane, description, dispatchedAt}]. Add on dispatch, remove on return, and CHECK IT BEFORE DISPATCHING — hand-dispatching has no memory unless it is kept here. On 2026-07-26 across nine direct dispatches, P1 went to requests twice and P19 ran in guard and slack concurrently; one may have overwritten the other. An entry older than ~30 min with nothing running is probably a dead agent: say so and ask, never silently assume either way.",
  "inFlight": [],
  "_inFlightClearedNote": "Emptied 2026-07-29 after the verify returned. The verifier flagged that all three dispatch entries were still here, one of them literally renamed to end in -RETURNED, alongside a report.md that still said ZERO built and carried an undispatched dependency ask. It was right on both counts. Renaming an entry is not removing it, and inFlight must be EMPTY the moment a lane returns - it is the STOP 1 lock, so a stale entry refuses the next run he asks for.",
  "_charterRulesDeclinedNote": "2026-07-29, OWNER RULED ON ALL NINE earned rules from the feature wave: 'all not agreed, they are not charter stuff.' Every one was declined. The signal to carry forward: a charter describes HOW A LANE WORKS - its scope, its boundaries, its standing method. A per-fix principle distilled from one build ('an audit row records the PRE state', 'a receipt reports what the message contained') is a good sentence about that fix and NOT charter material, however true it is. Both engines surface `earnedRules` / `charterRule` on every piece, so future waves will keep proposing them: report them once, expect them to be declined, and never let a lane treat one as authorised. Do not re-propose these nine.",
  "_inFlightRunNote": "CLEARED 2026-07-29 — feature build wf_bb6eac4f-e8f RETURNED (9 agents, 1.14M subagent tokens, 432 tool calls, 57 min, 0 errors). 12 pieces approved, 11 built, 2 needs-owner-decision (R144 and T2, both overturned by the verify), 1 cross-lane ask raised AND built (O3-followup -> shepherd). MANAGER ERROR THIS RUN: I1 was in the approved ten but I omitted it from the build args — 12 pieces were dispatched, not 13. The verifier caught the silence and independently established that ZERO lines was the correct answer for I1 anyway (systemPrompt.ts:655-658 steers off the gender field and never claims the person stated it), so nothing is owed. But a dropped piece that happens to be a no-op is still a dropped piece; the lesson is to count the args against the approved list before dispatching. ALSO: `understood` was not passed back, so every builder re-derived its area — pass it next time.",
  "_transporterCorrectionsNote": "Round 2 corrected THREE orchestrator claims, all in the same direction — the orchestrator inferred a mechanism from a symptom without reading the persisted turn text. (1) Row 133 was NOT lossy extraction: run 3 had NO genuine forward at all, because Maelle's prior reply had cut the chain (row 130), so extractParticipants was parsing Outlook's reply-quote header and correctly found only maelle@ + idan@ — which the owner/assistant filter then emptied. Row 133 is a SYMPTOM of row 130, not a second root. (2) Row 129's 6am was NOT an ignored timezone: run 2's chain states no zone anywhere, so the owner's own sanctioned tier-3 fallback applied correctly. The 'He needs EST time' signal is in run 3's note, AFTER the bad offer. So 129 is a MISSING CAPABILITY, not a defect that fired. (3) locationTz mapped 'et' but NOT 'est' — the actual evidenced text — so the orchestrator's 'extraction is cheap, it already maps et' was wrong and B1 would have silently failed on the exact case. Transporter added EST/EDT, CST/CDT, MST/MDT, PST/PDT.",
  "_laneBoundaryChangeNote": "2026-07-29 00:08-00:09 the owner moved the Graph MAIL layer from outrider to TRANSPORTER (outrider keeps only the Graph CLIENT layer — auth, tokens, graphClient). So connectors/graph/mail*.ts and scripts/email-auth.mjs are transporter's now. Round 1 routed mail.ts to outrider under the OLD boundary; that work is built and correct, but future mail-layer work goes to transporter. Consequence: row 130 no longer needs cross-lane sequencing — transporter owns both halves.",
  "_round3Note": "Round 3 = instructor ALONE, row 135: split the reply into a short 'what I assumed / what I need from you' block above a CLEAN forwardable body. Escalated from nice-to-have to REQUIRED by evidence — the recovered run-2 reply ended 'should I loop in Philip Lewis, Jim Douglass, and Ali Momin on the invite?', a question to the owner inside text meant to be forwarded verbatim. One string cannot be both. Dispatch AFTER transporter returns so the frame describes what was actually built. Then one verifier pass (Opus) over the combined diff.",
  "_round1DoneNote": "Round 1 of the email-test fixes RETURNED 2026-07-29: outrider (130a reply capability + 131 social gate), profiler (129b timezone persistence contract), gatekeeper (134 honest rewrite logging). All built, typecheck green. TWO orchestrator claims were corrected by the lanes, both the same error — reading a LOG LINE as an ACTION: (1) no topic was burned, the coda log comes from a different block and the subject stamp only fires on confirmed Slack delivery; (2) row 126's dateVerifier never actually rewrote the Hebrew text — the swap guard prevented the change and only the log claimed 'correcting'. Row 126 is a false-positive FLAG, not a silent edit. NEW FINDING from the recovered reply text: the reply is NOT forwardable verbatim (it ends asking the owner whether to loop in three CC'd parties), which escalates row 135 from nice-to-have to required.",
  "_round2Note": "HELD ON THE OWNER — 2026-07-28, his instruction: 'don't run round 2 after round 1, wait for me to do a change.' Do NOT dispatch round 2 when outrider returns; outrider's return is NOT the trigger. He is making a change first and will say when. Then: transporter carries FOUR items in ONE dispatch (132+133+137 as one — extraction feeds the existing attendee_emails route, no parallel path; 129a extract the stated zone per the owner's precedence; 129c pass present_in_timezone; 130b switch sending to the reply action inheriting the one-address cap). All four touch connectors/email/* and connections/email/*, and two lanes in that directory is what produced row 120. Round 3 = instructor alone (row 135, state the assumptions), LAST.",
  "_sequencingNote": "context is deliberately HELD until outer returns, so row 121's prose reachability filter sees the tool set outer's row-122 fix produces. Row 120 is held until slack returns, because it edits the same inbound.ts — dispatching those two together is what CAUSED row 120.",
  "_directDispatchNote": "Three DIRECT dispatches (independent items, different lanes) rather than the bugger workflow — nothing to route, nothing entangled. Going direct moves two jobs onto the Manager: read every return for a dependency ask, and run ONE verifier pass over the combined diff by hand. Both are owed before any wrap.",
  "_featureWaveNote": "wf_a9bc2bf7-43b (FEATURE build, gh#24 email chain) RETURNED 2026-07-28 — inFlight cleared, no lane is writing. 9 approved: 5 built, 4 needs-owner-decision after the verify overturned 4. Ledger append still owed. Three of the four overturns are lane-internal (E1 doc defect, a false completeness claim, robustness) and route back to their lanes; ONE is the owner's: E9's clamp also excluded ALWAYS_ON_TOOLS, so an email turn cannot get_person_memory — it cannot read the rows E6 creates. That is the seam class only a combined verify finds, and it came from the orchestrator's own 'and NOTHING else' spec.",
  "_featurePlan_2026_07_29": {
    "planRunId": "wf_a3bfe2a3-abe",
    "status": "BUILT 2026-07-29 by wf_bb6eac4f-e8f. UNCOMMITTED — the owner wraps. Typecheck clean across the whole tree. See report.md for the rows.",
    "refs": [
      "gh#144",
      "gh#52",
      "gh#45",
      "gh#51"
    ],
    "cost": "6 agents, 564,734 subagent tokens, 229 tool calls, 15.5 min, 0 errors. It corrected the premise on THREE of the four tickets — do not re-plan these, read this block.",
    "approvedPieces": [
      {
        "id": "T0",
        "lane": "transporter",
        "needs": [],
        "what": "Thread the already-computed Haiku imageDescPart (processMessage.ts:298-303) into the owner shadow via a new optional inboundAttachmentNote on PostReplyInput, so the receipt stops reading (image attached, no caption). Independent of every other gh#144 piece."
      },
      {
        "id": "T1",
        "lane": "transporter",
        "needs": [],
        "what": "EXTRACT the sendDM upload loop (connections/slack/messaging.ts:135-163) into one shared uploadAttachments helper and call it from BOTH sendDM and slackPostToChannel, so SendOptions.attachments is real on the owner path. Extract, never clone. Dead code if O1 and T2 are declined."
      },
      {
        "id": "O1",
        "lane": "outrider",
        "needs": [
          "T1"
        ],
        "what": "shadowNotify.ts gains attachments and forwards it on all four send paths, reusing the conversation-anchor cache so the image threads under the conversation already being read. Watch the v1_shadow_mode gate: shadow off currently means the image silently never forwards."
      },
      {
        "id": "T2",
        "lane": "transporter",
        "needs": [
          "O1"
        ],
        "what": "The actual forward, at INGESTION: inside processImageFileShare per-file loop immediately after the scan passes (fileIngestion.ts:81), where the url is already in scope. Code-driven, not model-driven."
      },
      {
        "id": "M1",
        "lane": "matchmaker",
        "needs": [
          "O3"
        ],
        "what": "LIVE DEAD-CODE BUG, one-line fix. create_meeting audit row writes details.start (calendarMutations.ts:300) but its only reader expects event_start_iso (calendarReads.ts:220-227), so the did-you-book-that recall for creates has never once fired. Rename the key."
      },
      {
        "id": "M2",
        "lane": "matchmaker",
        "needs": [
          "O3"
        ],
        "what": "moveMeeting.ts records where the meeting went but never where it was. The pre-move state is already fetched by the recurring probe at :810-824 and thrown away; add original_start, original_end, original_tz at :1441. Zero extra Graph calls."
      },
      {
        "id": "O3",
        "lane": "outrider",
        "needs": [],
        "what": "audit_log is the ONLY stateful table with no owner_user_id, and its reader feeds meeting subjects into the model context via the owner-DM calendar recall. Add owner_user_id plus index, make it required on the writer so all 12 sites supply it, add an owner filter. NEVER default to the first profile."
      },
      {
        "id": "P1",
        "lane": "profiler",
        "needs": [],
        "what": "Third tier in genderDetect.ts: FIRST-PERSON Hebrew morphology only, in the person own message, wired to SLACK not email/WhatsApp. Gate on detectMessageLanguage script range plus a new yaml advanced opt-in, default off. Fire only when gender is unknown, so one call per person ever."
      },
      {
        "id": "I1",
        "lane": "instructor",
        "needs": [
          "P1"
        ],
        "what": "systemPrompt.ts:655-658 wording for the third state, an inferred rather than stated gender. May legitimately be ZERO lines if P1 lands at a steering tier; that is an acceptable outcome, not a failure."
      },
      {
        "id": "O4",
        "lane": "outrider",
        "needs": [],
        "what": "whatsapp.ts:6 header falsely claims nothing imports startWhatsApp; index.ts:5 imports it and :174 calls it. Comment-only correction to what is actually true: wired at boot, inert per-profile without whatsapp_phone, owner-phone-only inbound."
      }
    ],
    "deferredPieces": [
      "S1 — attachments on the daily decision thread approval ask. Deferred deliberately: an image is not a decision, and shipping both surfaces creates a double-delivery problem because the ingestion forward fires before the orchestrator and cannot know an approval is coming."
    ],
    "ownerDecisions": {
      "gh52_substrate": "The requests spine, NOT audit_log. His rule, better than anything in the ticket: REVERTIBILITY FOLLOWS SPINE MEMBERSHIP. Not on the spine means not revertible; wanting it revertible means putting it on the spine, not building a second undo substrate beside it.",
      "gh52_cancellation": "A reverted cancellation is NOT an undo. Graph /cancel has already emailed every attendee and is not retractable; re-creating mints a new event id, re-invites everyone, and orphans requests.outcome_external_event_id, calendar_issues.event_id and the thread ledger. She refuses honestly and offers a re-book.",
      "gh52_cascade": "Inverse cascade OUT OF SCOPE. There is no reopen path anywhere in src; closeRequest no-ops on terminal rows; the ticket instruction to fire closeMeetingArtifacts on revert is backwards and would risk double-relaying to colleagues.",
      "gh51_tier": "It must actually steer output AND stay correctable by BOTH the owner and the person themselves. Not auto — the render gates at db/people.ts:1451 and :1563 mask auto as unknown, so the feature would be dead on arrival. ONE DETAIL LEFT: confirm equal-rank write behaviour so a later self-declaration beats an earlier inferred value.",
      "gh144_surface": "The colleague-conversation shadow only. S1 deferred.",
      "gh144_scope": "Every clean colleague image. Filtering needs intent-judgment, which provably cannot run on the arrival turn."
    },
    "notWorthBuilding": [
      "gh#52 prompt rule — already shipped verbatim at skills/meetings.ts:110.",
      "gh#52 revert_action as a SECOND tool — collides with revert_last_auto_move over the same trigger phrases. It must SUBSUME and DELETE the old one or not be built.",
      "gh#51 as originally specified — email and WhatsApp both drop every non-owner sender before reading any body, so it buys zero behaviour change plus an LLM bill.",
      "gh#144 SkillContext / OrchestratorInput plumbing — a model-driven forward cannot fire on the arrival turn (processMessage.ts:275 reads history before the url is appended at :307).",
      "gh#144 colleague DOCUMENTS — dropped entirely today at handlers.ts:137, so this means opening a new ingestion path, not extending one. Separate, larger ticket."
    ],
    "held": [
      "gh#52 the actual revert tool (needs the spine work first)",
      "gh#45 — specified fix is a regression, body rewritten, architectural call belongs to gh#155",
      "gh#155 — the parent question: making a skill MD prefs list code-actionable"
    ],
    "toDispatchTheBuild": "Workflow({scriptPath: '.claude/workflows/feature.js', args: {mode: 'build', pieces: [the ten above], answers: {the six ownerDecisions above}, priorClean: state.verifiedClean}}). Report rows 143, 144 and 145 are ALSO approved and awaiting dispatch — fold them in as instructor/transporter/matchmaker pieces rather than dispatching those lanes twice, since the per-agent floor is 70-85 turns.",
    "reportRowRulings": "142 declined and removed from report.md (owner: not sure its a problem even). 143 approved, re-routed matchmaker to INSTRUCTOR since it is prompt-tiering work. 144 approved (transporter). 145 approved (matchmaker). 125 and 126 remain deferred.",
    "buildRunId": "wf_bb6eac4f-e8f"
  },
  "_nextReportIdNote": "Stable report-row ids, ONE flat sequence, no letter prefixes. Assign the current value when a row FIRST enters report.md, then increment. NEVER renumber and NEVER reuse — even after a row is wrapped and deleted, so `14` means the same thing next month. A row that reappears keeps its original number. Starts at 100 deliberately: the rows written before 2026-07-26 evening carry legacy labels (P1-P32, D7-D8, B1-B6, N1-N2) from four undefined prefix schemes nobody documented, which is exactly why this counter exists. Those keep their labels until the next full report rewrite migrates them; new rows take 100+, so no collision is possible in the meantime.",
  "_reportIdGapNote": "148-159 ARE UNUSED and must stay that way. On 2026-07-29 the Manager read the counter as 120 from stale context, feared a collision with the email waves' rows 120-147, and assigned this run 160-166 to clear them. The counter was already correct at 148 - the feature waves DID advance it - so no collision was ever possible and the jump was unnecessary. Harmless (a gap, not an overlap) and recorded rather than quietly renumbered: 160-166 are assigned, 148-159 are simply skipped, nothing below 167 is ever reused. Lesson, and it is the one the owner is chasing in the manager chat: READ the counter, do not remember it.",
  "nextReportId": 175,
  "_verifiedCleanNote": "Passed to bugger/feature as `priorClean` so a verify does not re-audit settled ground. DROP an entry the moment a wave changes the code it describes — a stale 'proven clean' silences a real check, which is worse than having no list. When in doubt, drop it. PRUNED at the 4.2.1 wrap: six entries deleted because 4.2.1 changed the code they described — the checkSlot ladder gained a work-band fit computed above it and `occupancy` became `slotFacts`; `honest_hard_reason`'s reason gate became slot-based rather than counter-based; the all-day-WE occupancy classification changed; `recordAssistantThreadPost` no longer exists; and the typecheck stamp was superseded.",
  "verifiedClean": [
    "The walker's ISO-week event fetch is a strict SUPERSET of the searched range — startOf(week) of the first day, endOf(week) of the last — so a multi-week search cannot validate against a partial calendar",
    "The `mutated=` marker port is faithful: all 22 alternations of the old tool-name matcher are present in MUTATION_DOMAIN, and MUTATION_OK_RE leaves the pinned tool tape byte-identical",
    "runCodaGates fails CLOSED on both legs and returns a verdict carrying no text, so it is structurally incapable of corrupting a coda",
    "The date verifier cannot corrupt a reply — it swaps only the weekday token inside the detector's own matched span, and is a no-op when the lookup agrees",
    "pickSpreadSlots `anchorMode:'first_round'` is byte-equivalent to the pre-2026-07-26 behaviour for both existing callers (the move handler and the must-be recovery)",
    "The new delete_meeting success payload still produces `[delete_meeting OK <subject>] mutated=book`: mutationOutcome (turnHelpers.ts:70-85) reads `success:true`, MUTATION_DOMAIN maps delete_meeting→'book' (:109), and renderToolSummary's OK branch (:292-293) is unchanged — so the claim-checker shield still covers a cancel narration, including a mixed sweep where one call returned event_not_found",
    "The `event_not_found` payload can never be mistaken for a cancellation: `error` is a string so renderToolSummary's error-first branch (turnHelpers.ts:199-201) renders `[delete_meeting FAILED: event_not_found]`, and mutationDomain returns null at :137, so no `mutated=` marker and no entry in the pinned action tape (MUTATION_OK_RE matches ' OK' only)",
    "No circular import was introduced by calendarMutations.ts importing `verifyEventDeleted` from './calendarReads' — src/connectors/graph/calendarReads.ts imports only luxon, logger, graphClient and calendarTypes (no calendarMutations, no barrel)",
    "`getEventType` (connectors/graph/calendarReads.ts:898-927) selects start/end and returns `start.timeZone` with no Prefer header, so the handler's `DateTime.fromISO(startDateTime, {zone: startTimeZone ?? 'UTC'}).setZone(ownerTz)` label derivation is bound to the event's own zone and never to the server clock",
    "The removed decline-relay surface is fully dead: zero live references in src to `decline_and_relay`, `suggestedDmText`, `organizerSlackId`, `relay_status`, or `initiatorSlackId` (only stale `.claude/**` docs and one explanatory comment)",
    "The orchestrator's delete idempotency short-circuit (index.ts:489-510) and its ledger prune (:782-799) are provably dead code: both read `toolUse.input.event_id ?? .id` while delete_meeting's schema declares only `meeting_id` (meetings.ts:518-525). Inert, not harmful — the handler now owns both jobs",
    "calendar_issues `issue_class` is plain `TEXT NOT NULL` with no CHECK constraint, so adding the `missing_category` value needs no migration (verified live: db-query returns 51 rows across 5 classes, zero missing_category ever)",
    "markStaleResolved mints TERMINAL `resolved` rows for FUTURE-dated events routinely in production — verified live, e.g. a 2026-07-27 overlap row and three 07-2x floating-block rows carrying `[auto-stale: conditions vanished]`. Any future reasoning about calendar_issues suppression can take this as established fact",
    "Routine/brief history rows cannot trip the ack-guard (buildTurnContext.ts:941-963) or the action tape (:418): both key on a `[<tool> OK` substring in the assistant row, and the routine/brief writers store prose only",
    "A4's scrubber change is byte-identical on all 12 P3-protected forms — RUNTIME-verified by compiling the real file and running an 18-case matrix plus 3 assertion groups (both 2026-07-21 drafts, `<@U|label>`, naked 9- and 11-char ids, `@U`, all-caps prose, Hebrew-with-mention, channel ref, URL)",
    "The `find_slack_channel` tool has never been invoked — the log hits are the tool-catalog array being logged, not calls",
    "4.2.1 VERIFY: conversation history is written exactly ONCE on the reply path, at postReply Step 3b, above both the ack-return and sendReply; no third writer exists on that path",
    "4.2.1 VERIFY: every `await` on both legs of runOutputGates sits inside a try (including both internal wrappers), so the gate stack cannot throw out into postReply",
    "4.2.1 VERIFY: zero reads or writes of `outreach_jobs.status` remain — `OutreachJob` has no `status` member so every `SELECT *` consumer discards it structurally, the INSERT uses an explicit column list, and the UPDATE key-strip is present and load-bearing",
    "4.2.1 VERIFY: the `outreach_jobs.status` DROP COLUMN is the ONLY schema change in the whole 4.2.1 diff — a grep for added ALTER/DROP/CREATE TABLE/CREATE INDEX across all 53 files returns that one line. initSchema is safe on both a fresh and an existing DB",
    "4.2.1 VERIFY: the day-summary noise fold keys on the validator's own `outsideWorkHours` fact (one construction site, spread into every return), so an IN-HOURS blocker can never be relabeled; every single-window caller reads the global `rejectedCounts`, which keeps true per-slot labels",
    "4.2.1 VERIFY: `grantRelaxed` grants only on `senderRole === 'owner'`; every replaced site spelled either that same conjunction or an exact alias of it, so no site widened. The policy_exception replay still books, via a synthetic owner context",
    "4.2.1 VERIFY: `parseGraphFreeBusySlot` returns offset-carrying ISO, so the bare-instant filter resolves absolutely — the naive-string/server-zone hazard does not exist, and the covering-block filter can neither invent nor drop a block",
    "4.2.1 VERIFY — CROSS-CUTTING, DO NOT REVERT INDEPENDENTLY: P25's occupancy-above-work-hours promotion and P32's all-day-WE demotion are load-bearing on EACH OTHER. Without P32, the promotion turns every all-day-WE day into a hard NOT BOOKABLE on the colleague pre-check instead of an escalatable 'outside his hours'. BLAST RADIUS GREW ON 2026-07-27 (re-derived, invariant itself still INTACT: occupancyRoleOf returns 'ignore' for an all-day workingElsewhere at scheduleRules.ts:498, and isAllDayOutOfOffice requires showAs==='oof' at :482, so an all-day-WE day produces no overCommitment -> no owner_busy_collision -> the soft ESCALATABLE branch -> NO ledger entry). Reverting P32 would now ALSO make an all-day-WE day an owner_busy_collision, which is in HARD_FLOOR_KINDS, so it would arm an OUTPUT-TIME REWRITE that flatly refuses every time on a working-elsewhere day - not merely a wrong verdict but a corrected reply asserting it",
    "2026-07-27 VERIFY: `displaySubject` (displaySubject.ts:83) returns `[Private]` ONLY when `isEventPrivate` — otherwise it returns the RAW SUBJECT. Do not assume a default `viewer:'other'` masks a subject; it masks only a private event. This corrected a reassurance the verifier itself got half wrong",
    "_DROPPED 2026-07-27 evening: the planMeeting->nearbyAlternatives equivalence entry, because wf_b81e5b49-35b's item D deleted the try/catch around the nearby-alternatives search in planMeeting.ts. Superseded by the entry below that covers the new state.",
    "wf_b81e5b49-35b VERIFY: planMeeting's deletion of the try/catch around the nearby-alternatives search is safe in combination with D's always-live decision reads - findNearbyAlternatives catches everything and returns empty (nearbyAlternatives.ts:228-233), and the statements now outside it cannot throw, so a Graph outage still degrades to escalate_approval",
    "wf_b81e5b49-35b VERIFY: `npx tsc --noEmit` clean across the whole working tree; every ReadFreshness/ProcessMessageParams/summarizeCounter/toTimerInstant signature change is consistently applied at all call sites",
    "wf_b81e5b49-35b VERIFY: G's audience split is exhaustive - every `text` occurrence in connectors/slack/app/processMessage.ts is accounted for; model/history/inbound-queue read `framedText` and every remaining `text` reader is human-facing or a preview",
    "wf_b81e5b49-35b VERIFY: no output gate's DECISION depends on postReply's `userMessage` - runOutputGates.ts:241 and :882 pass it only to verifyDates whose `_userMessage` param is unused (dateVerifier.ts:161), and the availability floor reads only profile/result. G's narrowing of that string is inert for the gate stack",
    "wf_b81e5b49-35b VERIFY: H's outreach anchoring is complete at the arming site - outreach.ts:241 is the value that becomes next_check_at (db/jobs.ts:184-186); the second scheduled_at at :280 is inside `if (isFuture)` and is only the tool's return payload. The past-send_at double-send is closed",
    "wf_b81e5b49-35b VERIFY: every reader of `scheduled_at` is zone-aware (tasks/briefs.ts:208,:239-240, buildTurnContext.ts:374-375), and the requests-spine `details.due_at` written at tasks/skill.ts:444 has NO reader anywhere in src",
    "wf_b81e5b49-35b VERIFY: C's amend gate and the amend relay are the SAME predicate (summarizeCounter) and the emptiness test is on the RENDERED string, so `false` and `0` survive while a failed ISO falls back to the raw ISO - no counter key can be silently dropped again",
    "wf_b81e5b49-35b VERIFY: D's cache-key change preserves invalidation on both scopes - `getCalendarEvents|<email>|<cleanStart>|<cleanEnd>|<tz>` still contains `|<email>|`, matching the cross-turn drop and the new per-turn memo sweep in calendarCache.ts:83-88",
    "_DROPPED 2026-07-27 by the three-wave verify: the 'A's frame conversion cannot shift anchorDays by a day' entry. Both cited line ranges no longer exist, and its second half is now FALSE BY DESIGN - availabilityPreCheck.ts:886-889 hands the alternatives search BOTH readings and a cross-midnight second reading is deliberately a different day (:880-885 says so). Only the owner-zone half survives, below. Leaving it as proven clean would have silenced the check that finds the blocking arming-tier defect.",
    "2026-07-27 VERIFY (narrowed): verdicts[].date/.time are in the OWNER's zone on both extraction paths - that half holds. Do NOT extend it to anchorDays: the frame fork can legitimately produce a second anchor day, which also flips pickSpreadSlots from 'exhaustive' to 'first_round' for a cross-zone asker",
    "2026-07-27 VERIFY: the clamped-MPIM owner collapses to the pre-fix default, confirmed empirically - people_memory for U0F28CK6H (Idan) is Asia/Jerusalem, identical to the profile zone, so requesterTz === tz and the two-reading frame fork is a NO-OP on his own group turns",
    "2026-07-27 VERIFY: `owner_override_not_applied` cannot false-positive onto a colleague. SkillContext.userId/senderRole are REQUIRED and set once from input (orchestrator/index.ts:396-406); of the four runOrchestrator call sites only processMessage passes a non-owner role - whatsapp.ts:342, requests/runner.ts:393 and routine.ts:184 all pass senderRole:'owner' - so `senderRole==='colleague' && userId===profile.user.slack_user_id` is reachable ONLY when the authenticated owner speaks on a clamped surface. No transport can produce a colleague sender carrying the owner's id",
    "2026-07-27 VERIFY: the four-sided framing contract agrees - context.ts declares `framing`, both handlers declare instead of fusing (handlers.ts:704, :1225), processMessage.ts:96 composes prefix+text+suffix byte-identically to the old fused string, and the split is by AUDIENCE with every remaining `text` reader human-facing. The addressee gate correctly dropped its ^<<...>> un-fuse regex, which never matched the roster/thread-action/file framing anyway",
    "wf_b81e5b49-35b VERIFY: A's requester-zone wiring reads the right row - processMessage.ts:655 passes `userId: senderId`, so getPersonMemory(input.userId) at buildTurnContext.ts:574 is the ASKING colleague's people row (the owner's id on a clamped MPIM, collapsing to the pre-fix default); canonicalZone is applied at extraction (:259-261) so the EST/MST/HST fixed-offset mapping is genuinely in force",
    "wf_b81e5b49-35b VERIFY: D's ReadFreshness conversion is behaviour-preserving at both tool handlers - `force_refresh === true ? 'force' : 'cached'` is exactly the old boolean, and getOwnerEventsForDecision is called once per SEARCH in findAvailableSlots (:428), not once per day",
    "2026-07-27 VERIFY: `Settings.throwOnInvalid` is set NOWHERE in src (grepped, zero hits), so an invalid DateTime yields the string 'Invalid DateTime' rather than throwing — which is why moving arithmetic outside a catch in nearbyAlternatives is safe (`.isValid` filters it, NaN durations take the same route)",
    "2026-07-27 VERIFY: `tape.includes('mutated=book')` is exact, not a heuristic — MutationDomain is literally 'book' | 'task' | 'message' | 'other' (turnHelpers.ts:105), so the substring matches the calendar family and nothing else",
    "2026-07-27 VERIFY: the double offered-slots write in one turn is safe — recordOfferedSlots MERGES into the prior entry and preserves searchFingerprint on omit (offeredSlotsStash.ts:88-101), and buildTurnContext.ts:599 skips its own write when bookableStarts.length===0, which is precisely the all-blocked case where nearbyAlternatives writes. No overwrite, no fingerprint wipe",
    "2026-07-27 VERIFY: `needs_confirmation` and `needs_owner_approval` are explicitly NOT `ok` in mutationOutcome (turnHelpers.ts:141), so an owner-confirmation turn carries NO `mutated=book` marker. Any guard that stands down on a completed mutation is blind to that turn by design — do not assume a confirmation request looks like a mutation",
    "wf_bb6eac4f-e8f VERIFY: R143's meetings.ts prose split renders BYTE-IDENTICALLY on every Slack turn, derived from the code rather than from a dump: CHANNEL_TOOL_CLAMP has no 'slack' entry (registry.ts:276-280) so filterToolsByScope returns allTools unfiltered with scopes=undefined; all five gated meetings tools (get_calendar, analyze_calendar, move_meeting, delete_meeting, check_join_availability) are unconditional entries in MeetingsSkill.getTools; and create_approval lives in TASKS_MODULE which CORE_MODULES always includes (registry.ts:19) — so all six ships() gates plus isSlackNative are true on Slack for any role and any scope",
    "wf_bb6eac4f-e8f VERIFY: R143 is live in production and not a no-op: src/core/orchestrator/systemPrompt.ts:380 passes the real turn `channel` into skill.getSystemPromptSection, and the 4th param is declared on the Skill interface at src/skills/types.ts:58",
    "wf_bb6eac4f-e8f VERIFY: O3 introduces no circular import: src/config/userProfile.ts imports only fs, path, js-yaml, zod and utils/logger — nothing in that closure reaches back into src/db — so db/client.ts's new `import { loadAllProfiles }` is a leaf edge and the inline backfill cannot re-enter getDb()",
    "wf_bb6eac4f-e8f VERIFY: O3's audit_log writer and reader agree on the owner id on the path M1 unblocks: OpCtx.userEmail is `context.profile.user.email` (src/skills/meetings/ops.ts:84-85), so resolveOwnerUserId (calendarMutations.ts:12-19) resolves through getProfileByEmail to the same profile.user.slack_user_id the reader passes at calendarReads.ts:218 — create_meeting rows are not excluded by the new owner filter",
    "wf_bb6eac4f-e8f VERIFY: audit_log has exactly two read paths in src, both at src/skills/meetings/ops/handlers/calendarReads.ts:219-220 (get_calendar's empty-window recall), and both are now owner-filtered. There is no other reader anywhere",
    "wf_bb6eac4f-e8f VERIFY: audit_log has exactly two WRITE paths in src: the typed auditLog() helper in db/client.ts and the raw INSERT in src/core/requests/closeRequest.ts:99-116. Both now supply owner_user_id",
    "wf_bb6eac4f-e8f VERIFY: P1's owner constraint that a person can always override their own inferred gender holds with no store change: setCoreFieldWithProvenanceById refuses only when `currentValue && newRank < currentRank` (src/db/people.ts:412-419), and person(2) against person(2) is not strictly less, so a later confirm_gender at equal rank writes",
    "wf_bb6eac4f-e8f VERIFY: P1's Hebrew gate is deterministic and free: detectMessageLanguage returns the literal string 'Hebrew' from a character-range scan (src/utils/detectMessageLanguage.ts:27, 44) with zero model calls, so the one Haiku call is genuinely gated",
    "wf_bb6eac4f-e8f VERIFY: No prompt change was owed for P1 (the I1 question): src/core/orchestrator/systemPrompt.ts:655-658 steers Hebrew gendered forms off the gender field and never asserts that the person stated it, so a person-tier inferred gender creates no honesty defect in the wording",
    "wf_bb6eac4f-e8f VERIFY: T0's rendered receipt is well-formed: imageDescPart is already `' — <desc>'` (processMessage.ts:306), so `[Image${inboundAttachmentNote}]` yields `[Image — <desc>]`, matching the bracket shape the history writer uses at :310-311; and the note never reaches runOutputGates",
    "wf_bb6eac4f-e8f VERIFY: T2's forward cannot launder a suspicious image: imageUrls is pushed only on the `if (!block) continue` survivor path at fileIngestion.ts:81, and the forward sits after `if (images.length === 0) return` — the invariant is structural, not ordering-dependent",
    "wf_bb6eac4f-e8f VERIFY: T2's forwarded url cannot reach the unscanned owner re-attach path: the forward never calls appendToConversation and never touches the owner's threadTs, and reattachRecentThreadImage is owner-only and reads the owner's own history",
    "wf_bb6eac4f-e8f VERIFY: T2's and postReply's shadow posts collapse into ONE owner-DM thread: both pass `conversationKey: threadTs` (fileIngestion.ts:126, postReply.ts:620) against shadowNotify's `${ownerId}:${conversationKey}` anchor cache, and fileIngestion's threadTs is the same value it hands processMessage at :162",
    "wf_bb6eac4f-e8f VERIFY: T2's external-domain gate is live rather than silently always-false: getOwnerDomain (src/utils/attendeeScope.ts:18-23) returns a bare lowercased domain and the sender side is lowercased identically before comparison",
    "wf_bb6eac4f-e8f VERIFY: T1's uploadAttachments is a true extraction, not a clone: the 28-line loop is gone from sendDM, both callers go through the one helper, and the `safeThreadTs(opts.threadTs) ?? res.ts` thread fallback is preserved verbatim in both",
    "wf_bb6eac4f-e8f VERIFY: O1 is inert for all 11 pre-existing shadowNotify call sites: none passes attachments, undefined is forwarded, and messaging.ts's `opts.attachments && length > 0` guard short-circuits before any Slack call",
    "wf_bb6eac4f-e8f VERIFY: `npx tsc --noEmit` is clean across the whole working tree with all 13 pieces in place — every ownerUserId, selfText, channel, attachments and inboundAttachmentNote signature change is consistently applied at every call site",
    "wf_33541300-121 VERIFY: `meeting_people` is extracted at EXACTLY ONE site (classifyTurn.ts) and reaches every consumer through the single `resolvedMeetingAttendees` route — the email leg deliberately joins that same route at buildTurnContext.ts:304-334 rather than forking a second one. No duplication at this seam, and none pre-existing",
    "wf_33541300-121 VERIFY: the three consumers of `resolvedMeetingAttendees` are buildTurnContext.ts:266-301, findAvailableSlots.ts:394-411 and createMeeting.ts:150-156 — all three unchanged by this wave, so no contract, type or call shape moved. Note the createMeeting one is why a classifier guess can reach a real invite",
    "wf_33541300-121 VERIFY: `needMeetingPeople` is unconditionally TRUE for any interactive message longer than one character on every channel (buildTurnContext.ts:179) — so any future prompt-budget question about the meetingPeopleSection starts from 'per turn', not 'per scheduling turn'",
    "wf_33541300-121 VERIFY: the classify_turn tool SCHEMA (classifyTurn.ts:287-295) and the response parse (:359, :375) are untouched by prompt-text edits inside meetingPeopleSection, so the only possible behaviour delta from such an edit is the classifier's own judgement",
    "wf_33541300-121 VERIFY: the gh#158 root-cause chain was RE-DERIVED independently from logs/maelle-2026-07-28.log:597-605 rather than taken from the lane's summary — the wrong data does enter at extraction, and the union at findAvailableSlots.ts:394-411 needed no change for the reported symptom"
  ],
  "_lastWrapNote": "v4.3.1 WRAPPED + PUSHED as b7a4a8b on 2026-07-29 (40 files, +1195/-281, typecheck clean). PATCH per the owner explicitly, even though O3 added an audit_log column + migration which WRAP_UP would normally call minor-shaped - his call, flagged at the time and recorded under a Migration heading in the CHANGELOG rather than buried. Bundled the parallel sessions work as instructed (\"take other chat code\"): the seven lane charters, .claude/skills/manager/SKILL.md, .claude/workflows/feature.js, scripts/ledger-stats.cjs. gh#144 CLOSED with a comment naming three residuals (channel/@mention images not forwarded; external/guest excluded by the domain gate; the gate is inert without user.email) and the fact it was solved by ingestion-time forwarding rather than the ticket own items 2/3. NOTHING ELSE was closed - gh#24 stays partial, gh#51/#52/#45/#155 stay open. NOT DEPLOYED: the commit is under the owner author so the deploy watcher does not auto-pull. Boot stamp should read version 4.3.1 / gitSha b7a4a8b after npm run deploy."
}