{
  "_thisFileNote": "CURRENT STATE ONLY, and that is the rule this file lost: the keys documented in .claude/skills/manager/SKILL.md 'State you own', ONE operating note each, and the run in progress or the last one (its `note` = what went in, `_resultNote` = what came out). Everything older is HISTORY and history is ledger.jsonl, which is append-only and queryable. Reduced 2026-07-31 from 114 KB / 44 prose keys against 7 documented ones; every ruling in those 44 was verified to be a ledger row already before a byte was deleted. A `_previousRun*` key, a second note on one field, or a note about a run that has ended means it is regrowing - move it to the ledger and delete it. The wrap is where this is checked.",
  "lastSeenIso": "2026-08-03T12:20:00+0300",
  "_lastSeenIsoNote": "Set to the run's START, not its end. wf_386bf586-bf1 ran 2026-08-03T12:20 -> ~12:54 (duration_ms 2008790); anything said during that window is still unreviewed, and re-reading it costs almost nothing while skipping it can lose a real bug.",
  "_lastRunNote": "status MUST be one of complete | running | stopped. `stopped` exists because a run the owner kills leaves `running` set forever, and the resume rule then re-fired on every subsequent turn — a restart loop that spawned wf_79ce66bb-efc while wf_62f4811d-7fe was already dead. NEVER auto-resume: a dead run and a slow one are indistinguishable from this file, so resuming is a guess. Report the state and ask.",
  "lastWrapIso": "2026-08-03T18:32:09+03:00",
  "_lastCleanShaNote": "The CLEANER's own watermark (X131) — a commit SHA, not a timestamp, and deliberately NOT lastWrapIso: they are two different clocks and reusing one for both is what conflated the cleaner's scope with the bouncer's. A sha because `git log --name-only <sha>..HEAD` is an exact range anchor, where a `--since` timestamp reads COMMIT DATE and silently skips or double-counts across a rebase, a cherry-pick or clock skew. ADVANCED 2026-08-03 by direct-cleaner-full-sweep-2026-08-03: the FIRST unscoped run (prior run was scoped to src/utils and correctly left this null) — full src/ (199 files/73.5k LOC) + scripts/ + .claude/*.md, detect-only over all YAML + framework surfaces. Set to aeab9630b5e9874a425511e6e70dce98034f7f58 (HEAD at the time, unchanged throughout — the cleaner never commits). ONLY AN UNSCOPED SWEEP ADVANCES IT (X135) — a scoped run advances nothing, because this field means all of src/ is judged up to this commit. The rule and the two sentences the chat must print are in SKILL.md's `cleaner` entry and cleaner.md C10.",
  "lastCleanSha": "aeab9630b5e9874a425511e6e70dce98034f7f58",
  "_nextReportIdNote": "ONE counter for report-row ids; assign then increment, NEVER renumber and NEVER reuse. The FORMAT is defined in exactly one place — the `#` bullet in .claude/skills/manager/SKILL.md — and is deliberately NOT restated here.",
  "nextReportId": 203,
  "_inFlightNote": "Nine items held by engine run wf_6e41a1e4-f2c. EMPTY THIS IN THE SAME TURN IT RETURNS — a stale entry is the STOP 1 lock and refuses the next run.",
  "inFlight": [],
  "_pendingOverflowNote": "Verify DISCOVERIES and over-cap items, shaped for args.issues. Pass on the next `build` and the engine prepends them itself, then delete what manifest.carry names. EXPANDED 2026-08-03 by two hand-dispatched bouncer passes (uncommitted-tree + retroactive v4.4.3 audit): email-optional-subject-leak-findavailableslots REFRAMED, then BUILT by wf_ad941677-935 (owner said fix now) — removed from this list. securitygate-dedup-narrowed-coord-trigger also BUILT by the same run (bounced once, cleared on retry) — removed from this list. That run's verify added 2 new discoveries in its place: a broader non-private-subject email leak (SubjectViewer has no external tier) and a PRIVATE_MASK token that can leak as literal text.",
  "pendingOverflow": [
    {
      "id": "events-table-defined-twice-in-client-ts",
      "lane": "handyman",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "symptom": "The events table is created twice in the same file — byte-identical CREATE TABLE IF NOT EXISTS events + CREATE INDEX blocks.",
      "evidence": "src/db/client.ts:138 and :310 — delete the second (dead) definition; both are IF NOT EXISTS so harmless today but real duplication."
    },
    {
      "id": "session-starter-stale-line-runoutputgates-215",
      "lane": "architect",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "symptom": "SESSION_STARTER.md cites runOutputGates.ts:215 for the ownerIsActing check; the actual (and correct) line is 210.",
      "evidence": "SESSION_STARTER.md — one-line citation fix, architect/owner territory."
    },
    {
      "id": "email-viewer-no-external-tier-for-nonprivate-subjects",
      "lane": "matchmaker",
      "severity": "medium",
      "clarity": "clear",
      "source": "verify",
      "invariant": "payload-scoped-to-caller",
      "symptom": "On an email-driven slot search, non-private internal meeting subjects and owner trade-off narration (\"over your optional <subject>\", \"his day is loaded around then\") still ride into the client-forwarded reply — SubjectViewer only has owner|other, no external tier, so 'other' (designed for internal colleagues) is still granted on the email leg for anything not marked private.",
      "evidence": "src/utils/displaySubject.ts (SubjectViewer type) vs src/skills/meetings/ops/handlers/findAvailableSlots.ts:1696-1741 (the _*_note family, none channel-scoped). Needs a design call: a real external/other split, or channel-scope the _*_note family the way ops.ts:90-93 already does."
    },
    {
      "id": "private-mask-token-can-leak-as-literal-text",
      "lane": "matchmaker",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "symptom": "A masked \"[Private]\" placeholder token can appear as literal text in a client-facing email because scheduleRules.ts:592's fallback catches an empty subject but not the PRIVATE_MASK token itself.",
      "evidence": "src/utils/scheduleRules.ts:592 — extend the fallback to also catch PRIVATE_MASK."
    },
    {
      "id": "stranded-coordination-tasks-render-as-active",
      "lane": "handyman",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "symptom": "4 abandoned 'coordination'-type tasks (May-June 2026, a type removed in 3.5.0) sit in pending_colleague with due_at NULL and still render as 'ACTIVE IN THIS THREAD — you already committed to these' if the owner ever replies in one of those old threads, though no dispatcher can execute that type.",
      "evidence": "src/tasks/index.ts:101 (no staleness/live-type bound on the pending_colleague filter) and buildTurnContext.ts:479-482 (renders them)."
    },
    {
      "id": "auto-expired-24h-close-reason-never-written",
      "lane": "slackmaster",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "symptom": "The 'auto_expired_24h' outreach-job close reason can never fire — the query feeding that branch already filters to the identical 24h cutoff, so it's dead code and followup_closed_at accumulates NULL forever on old rows. Independently confirmed by the retroactive v4.4.3 bouncer pass too.",
      "evidence": "src/connectors/slack/recentOutboundContext.ts:321 (the dead branch) vs the query's own cutoff at :115/:122."
    },
    {
      "id": "db-schema-resurrection:src/db/client.ts:initSchema",
      "lane": "handyman",
      "severity": "low",
      "clarity": "clear",
      "source": "audit",
      "symptom": "initSchema unconditionally CREATE TABLE IF NOT EXISTS known_contacts on every boot; the one-time migration meant to drop it no-ops on every boot (production is already migrated), so the drop never runs again — an empty, zero-reader/zero-writer table is silently resurrected forever.",
      "evidence": "src/db/client.ts:initSchema (~line 79) vs db/migrations/v3_2_0_person_store.ts:71. Confirmed live via node scripts/db-query.cjs: table present, 0 rows. Independently named already in .claude/agents/profiler.md as a handyman cleanup, not yet actioned."
    },
    {
      "id": "tonight-wave-introduced-small-defects-ops",
      "lane": "matchmaker",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "symptom": "ops.ts:77's comment cites gh#189, a GitHub issue that doesn't exist (collides with report row o#189) — should read email-aside-leak-no-channel-scope. ops.ts:92 deletes _attendee_busy_note, which can never execute since move_meeting is absent from CHANNEL_TOOL_CLAMP.email — dead code shipped despite the lane's own verifiedClean note flagging it as inert.",
      "evidence": "src/skills/meetings/ops.ts:77,92."
    },
    {
      "id": "tonight-wave-introduced-small-defects-outbound",
      "lane": "slackmaster",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "symptom": "recentOutboundContext.ts:24-28/:41-44 header comment still describes the OLD per-candidate binary-RESPONSE mechanism after tonight's fix replaced it with one discriminative call across all candidates.",
      "evidence": "src/connectors/slack/recentOutboundContext.ts:24-28,41-44."
    },
    {
      "id": "reserve-slot-project-doc-deleted-with-live-inbound-citations",
      "lane": "handyman",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "symptom": "Tonight's cleaner sweep deleted .claude/RESERVE_SLOT_PROJECT.md, but two live src/ comments still cite it — the slot_holds schema's storage-analysis rationale is now unrecoverable except from git.",
      "evidence": "src/db/client.ts:527, src/db/slotHolds.ts:10 — replace the dead doc pointer with the actual rationale inline."
    },
    {
      "id": "verbmap-still-incomplete-after-cleanup",
      "lane": "handyman",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "symptom": "verbMap still has ~9 dead keys of the same class the cleaner removed one of (edit_task, cancel_task, get_calendar_issues, update_calendar_issue, get_company_knowledge, create_routine, get_routines, update_routine, delete_routine) and is missing four LIVE tool names (manage_calendar_issue, manage_routine, manage_knowledge, update_task) — a turn whose only mutation is one of those four renders a vague fallback instead of a grounded verb.",
      "evidence": "src/core/orchestrator/index.ts (verbMap)."
    },
    {
      "id": "hardblock-ledger-sixth-invalidation-class",
      "lane": "matchmaker",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "symptom": "availabilityGate.ts:371-382's citations to forgetHardBlockedSlot are stale again — cites runOutputGates.ts :1035/:1061, real calls are at :999/:1025 (off by 36 lines, from a since-reverted gatekeeper branch shifting them back). Third occurrence of this citation-drift class in one wave.",
      "evidence": "src/utils/availabilityGate.ts:371-382 vs src/utils/guards/runOutputGates.ts:999,1025."
    },
    {
      "id": "workinghoursdefault-utils-imports-config-upward",
      "lane": "matchmaker",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "invariant": "structural-fact-read-through-any",
      "symptom": "workingHoursDefault.ts:56 adds a new utils -> config runtime require, in the same wave whose sibling fix (cloneable-default) was specifically about not letting utils depend upward on tenant config.",
      "evidence": "src/utils/workingHoursDefault.ts:56 — restructure so config is passed in rather than required from utils."
    },
    {
      "id": "claimchecker-dedup-not-actually-deduplicated",
      "lane": "gatekeeper",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "symptom": "o#186's fix claimed the confirmed/checked exemption was reduced to a cross-reference between claimChecker.ts:200 and :251, but both sites still state the exemption and its exclusion list in full.",
      "evidence": "src/utils/claimChecker.ts:200,251 — collapse to one canonical statement plus a cross-reference, per the original intent."
    },
    {
      "id": "findavailableslots-presentation-zone-expr-duplicated",
      "lane": "matchmaker",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "symptom": "The same presentation-zone expression is duplicated character-for-character at findAvailableSlots.ts:1402 and :1565 — must change together, and the fix's own comment names the copy instead of sharing it.",
      "evidence": "src/skills/meetings/ops/handlers/findAvailableSlots.ts:1402,1565 — extract to one shared helper."
    },
    {
      "id": "venues-dead-residue-from-reverted-175b",
      "lane": "handyman",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "symptom": "isTeamsPlaceholderLocation in db/venues.ts:365 is extraction residue left behind by the gh#175-b revert — one caller, where the code already was inline before extraction.",
      "evidence": "src/db/venues.ts:365."
    },
    {
      "id": "getawaitingownerrequests-parent-id-filter-gap",
      "lane": "registrar",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "symptom": "getAwaitingOwnerRequests filters parent_request_id IS NULL, so gh#169-a's new uniqueness check only sees top-level approvals — unproven whether an awaiting_owner CHILD request can exist and be missed.",
      "evidence": "src/db/requests.ts (getAwaitingOwnerRequests query) — confirm whether child rows are reachable in practice; if so extend the uniqueness scan."
    },
    {
      "id": "dead-script-measure-prompt-ts",
      "lane": "handyman",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "symptom": "scripts/measure-prompt.ts is dead/broken beside the working scripts/measure-prompts.cjs.",
      "evidence": "scripts/measure-prompt.ts — delete on next scripts/ touch."
    }
  ],
  "_verifiedCleanNote": "Passed to bugger/feature as `priorClean` so a verify does not re-audit settled ground. ONE PROVEN CLAIM PER ENTRY. DROP an entry the moment a wave changes the code it describes. PASS IT VERBATIM: `priorCleanDropped` keys on the INDEX in the array you passed (X84). UPDATED 2026-08-03 after wf_386bf586-bf1: dropped 0, kept all 66, added 12 new claims from this build's verify. UPDATED AGAIN same day after a hand-dispatched bouncer pass on the uncommitted tree (owner asked for two findings-only bouncer passes to confirm the new Q1/Q1b charter questions ran): added 4 more claims that pass established beyond what the original verify checked (the second consumer of messageReferencesRequest, the verbMap dead-key confirmation, the 20-of-21 clean doc-deletion count). Now 82 entries. NOTE: this run's `priorClean` arg only carried a 14-entry subset (context-compaction artifact) rather than the full prior list — priorCleanDropped came back 0 regardless since verify only re-checks the wave's own changed files, so nothing was silently re-audited, but pass the FULL array next time.",
  "verifiedClean": [
    "o#178 masking cannot fail open for want of a Graph field: isEventPrivate (src/utils/displaySubject.ts:90-101) consults ONLY event.sensitivity and event.categories, and getEventEndInstant's .select now carries both. There is no third field to add.",
    "violation_kind 'owner_busy_collision' is emitted by scheduleRules rule 8 alone, and allowRelaxed makes rule 8 pass — so gh#165-d's new strict-reject stamp and the pre-existing relaxed-pass stamp are mutually exclusive per slot, and first-hit-wins is preserved by stampConflictingEvent's early return.",
    "humanizeViolationLabel has a real 'owner_out_of_office' case (src/skills/meetings/ops/violationLabels.ts:28), so gh#165-d's OOF label cannot collapse to 'unknown' and cannot misroute a known all-day collision into the \"I can't tell which rule flagged it\" message.",
    "Slack @mentions reach the orchestrator as \"Real Name (slack_id: Uxxx)\" (src/connectors/slack/app/helpers.ts:86-97), so gh#158-a's substring gate cannot drop a mention-named attendee. Any future reasoning about that gate can take this as established.",
    "gh#158-a can only SHRINK resolvedMeetingAttendees, and the gh#158 pre-check bail keys on that list being non-empty — so the only new behaviour on that seam is the pre-check running on a turn that genuinely named nobody, which is the correct answer for such a turn. The gate cannot make a bailing turn un-bail for a name the message did contain.",
    "o#177 is a cut that took nothing valuable: the owner's identity and authority in a clamped MPIM are still stated in full by authLine (src/core/orchestrator/systemPrompt.ts:430-436) and his preferences come from ownerPreferenceBlocks, so neither removed block carried a capability or any of Maelle's voice.",
    "threadPeopleBlock (src/core/orchestrator/systemPrompt.ts:778) was never part of the o#177 leak — it passes user.slack_user_id as the exclude argument. Do not re-open it as a third leaking block.",
    "o#180's newsTimedOut is assigned at exactly one site (src/tasks/briefs.ts:740, inside `if (!gathered)`), so a completed-but-empty news gather leaves the brief byte-identical to pre-fix. The quiet-day/no-apology rule is intact.",
    "`npx tsc --noEmit` is green with all eight wave files on disk (run once, after reading the full diff).",
    "wf_4bbfc750-1a9 VERIFY: `npx tsc --noEmit -p E:/Code/Maelle/tsconfig.json` is clean across the whole working tree with both gh#165-d edits in place — the two new optional fields on diagnosticsOut.conflictingEvent are structurally assignable at moveMeeting's narrower local diagnostics type, which declares no conflicting field at all",
    "wf_4bbfc750-1a9 SPOT-CHECK: the gh#158 colleague bail is present at HEAD — availabilityPreCheck.ts:517-541 returns the empty result (ran:false, no promptBlock) whenever namedAttendeeEmails is non-empty, and forgets any named instant from the hard-block ledger first, so the output-time floor has nothing stale to fire on",
    "WAVE-VERIFY 2026-07-30: availabilityPreCheck.ts — recordHardBlockedSlot is called only at :943 inside the verdict loop, unreachable from the :495 bail; grantRelaxed and policy_exception appear nowhere in the file",
    "WAVE-VERIFY 2026-07-30: all 25 `overCommitment` references read named subfields or truthiness; none spreads the object, so the new required `id` cannot reach another caller's payload",
    "WAVE-VERIFY 2026-07-30: availabilityPreCheck.ts:562 and :1149 compute `today` with the identical expression, so the main fallback extraction and the forgetter's extractRawPairs cannot disagree about what 'tomorrow' means",
    "WAVE-VERIFY 2026-07-30: eventsForWeek is reachable only AFTER the :495 bail returns — the live-read swap (152-refresh) and the gh#158 bail do not interact, and a bailed turn makes zero calendar reads",
    "wf_6852af85-afc VERIFY: gatherNews (src/skills/news.ts:255-304) can neither throw nor resolve to undefined — a whole-body try/catch returns {...EMPTY_BUNDLE} at :302 and every early return is a NewsBundle (:281, :299) — so `!gathered` in briefs.ts:707 is exactly and only the Promise.race timeout branch. gh#167's three-way branch rests on a true premise",
    "wf_27f03aca-0dd VERIFY: `npx tsc --noEmit` is clean across the whole working tree with all four fixes in place — every namedAttendeeEmails / locationHint / viewer / conflictingEvent / overCommitment.id signature change is applied consistently at every call site",
    "wf_27f03aca-0dd VERIFY: the new update_meeting(add_attendees) steer cannot produce an unapproved real-world action — moveMeeting.ts:162-210 refuses a non-requester colleague with `colleague_not_requester` and routes it to owner approval with a deferred_action_hint; only the meeting's own requester applies a change directly",
    "wf_27f03aca-0dd VERIFY: locationHint is a live, populated field end-to-end, not a dead parameter — args.location -> bookingRequest.ts:171 -> planMeeting.ts:161 -> planMeeting.ts:327/:348 -> detectCategory, and resolveLocation already consumes the same value at planMeeting.ts:462. No new fetch, no new LLM call",
    "wf_27f03aca-0dd VERIFY: resolveNamedInternalAttendees excludes the owner twice over — by name at resolveAttendeeEmails.ts:180 and by the exclude set at :211 — so a colleague asking only about the owner ('is Idan free at 12:30?') keeps a non-empty pre-check. The gh#158 bail cannot fire on the file's own primary use case",
    "wf_27f03aca-0dd VERIFY: gh#158's root cause is established from production logs, not from the lane's summary — logs/maelle-2026-07-28.log:669 (resolved levana.b@, 12:24:09.680) is followed by :671/:677 (pre-check normalized 2026-07-29T10:00, injected notBookable) and :721/:722 (11:00 injected bookable:1). The owner-scoped pre-check is where both wrong-subject answers came from",
    "wf_27f03aca-0dd VERIFY: the 2026-07-30 second wave adds no new part to the system — no new file, tool, skill, table, column, background job or output-path guard. Every addition is an optional parameter or field on an existing contract (namedAttendeeEmails, locationHint, diagnosticsOut.conflictingEvent, overCommitment.id), and the one deletion (findDuplicateEvent's null mode) removes a path rather than layering over it",
    "wf_6852af85-afc VERIFY: this wave adds no new part to the system — no new file, tool, skill, table, column, background job or output-path guard. The only contract change is the widening of an existing shared primitive (findDuplicateEvent), and both of its prior callers are unaffected",
    "wf_e2b7aeeb-325 VERIFY: `npx tsc --noEmit -p tsconfig.json` is clean across the whole working tree with all 20 fixes on disk (run once, after reading the full diff)",
    "wf_e2b7aeeb-325 VERIFY: gh#158's snapshot cannot be aliased away: buildTurnContext.ts:366 rebuilds resolvedMeetingAttendees with a spread (`= [...resolvedMeetingAttendees, ...added]`) rather than pushing, so namedInternalAttendeeEmails captured at :317 stays named-only. This was the one way that fix could have been a silent no-op",
    "wf_e2b7aeeb-325 VERIFY: coordinator.ts's two deleted UPDATE statements matched nothing on live data: every tasks row with skill_ref LIKE 'out_%' is cancelled/completed/failed/informed (13/40/8/14), none in the ('new','in_progress','pending_colleague') sets they targeted — the removal cannot orphan a legacy row",
    "wf_e2b7aeeb-325 VERIFY: the new per-request FIFO queue in resolver.ts cannot self-deadlock: deferredActionReplay calls executeToolCall on the registered tool handler directly (no model turn, no tool surface), so resolveRequestInner has no path back into resolveRequest for the same id even though runDeferredAction (:301/:602) runs before closeRequest (:316/:656)",
    "wf_e2b7aeeb-325 VERIFY: 'deterministic_match' and 'deterministic_under_10min' have zero remaining readers in src and scripts, so narrowing OutreachJob.followup_close_reason and matchedVia is a pure deletion — legacy DB rows carrying the old string are never compared against the type",
    "wf_e2b7aeeb-325 VERIFY: preferredSlotStatus.presentation_local uses the same renderClockInZone(start, ownerTz, presentTz) argument order and the same (present_in_timezone || autoPresentTz) fallback as the main-slots site at findAvailableSlots.ts:1620-1624, and omits the key when the zone resolves empty",
    "wf_e2b7aeeb-325 VERIFY: mail.ts replyToMail's happy path is byte-identical post-fix; the cleanup deletes only the draftId createReply returned and always rethrows the original error, so sendDirect still reports send_failed and inbound.ts still treats the send as never-delivered",
    "wf_e2b7aeeb-325 VERIFY: gh#24's one-address cap is enforced on the address that is actually sent: connections/email/index.ts:88-96 validates recipientRef+cc+bcc against ownerEmailAddresses, :115 passes the validated recipientRef as `to`, and graph/mail.ts:452-458 writes it as toRecipients — Graph's own From/Reply-To inference is overridden",
    "wf_e2fd1caf-c84 VERIFY: getOwnerEventsForDecision cannot fail soft: calendarReads.ts:442-468 retries once and then THROWS CalendarOfflineError at :466 — it never returns an empty array on an outage. o#189's `confirmed = true` catch is therefore the genuine outage path, and the availability floor cannot false-clear an established block by reading zero events.",
    "wf_e2fd1caf-c84 VERIFY: HardBlockedSlot.durationMin can never be missing or Infinity: availabilityPreCheck.ts:807-812 and :818-823 set it under the identical guard as rejection_reason, allowed_durations is schema-non-empty (userProfile .min(1)), and recordHardBlockedSlot has exactly one caller (:959).",
    "wf_e2fd1caf-c84 VERIFY: the cloneable-default rewrite is output-identical for the only configured tenant: idan.yaml office_days [Monday,Wednesday,Thursday] union home_days [Sunday,Tuesday] sorted by WEEK_ORDER equals Sunday..Thursday, hours 09:00-18:00 — the same tuple as the deleted ISRAEL_DEFAULT.",
    "wf_e2fd1caf-c84 VERIFY: meetings.ts:1125's 'OWNER FREE, REQUESTER BUSY' instruction is unconditional — it sits after the ` : ''} that closes the owner-path ternary at :1119 — so o#186's deletion of _attendee_status_note did not remove the only copy of that guidance.",
    "wf_e2fd1caf-c84 VERIFY: the @-mention auto-add removal took no capability with it: the resolvedMeetingAttendees union at findAvailableSlots.ts:435 covers a mention-named colleague, because a Slack mention reaches the orchestrator as 'Real Name (slack_id: Uxxx)'.",
    "wf_e2fd1caf-c84 VERIFY: textScrubber.ts imports only luxon (its single import line, :20), so INTERNAL_WORK_ITEM_ID_RE's new home introduces no cycle and utils/securityGate.ts now has no dependency on core at all.",
    "wf_e2fd1caf-c84 VERIFY: multiple simultaneously-open outbound DMs to one colleague are real in live data (4 on 2026-04-26 to U02K5PN5K4Y, 4 on 2026-07-13 to U0F28ES4V), and no constraint prevents two rows with followup_closed_at NULL — so recentOutboundContext's new multi-candidate loop is reachable, not dead on arrival.",
    "wf_e2fd1caf-c84 VERIFY: `npx tsc --noEmit -p tsconfig.json` is clean across the whole working tree with all eleven fixes on disk (run once, after reading the full diff).",
    "wf_e2fd1caf-c84 VERIFY: no stragglers remain for this wave's renames and deletions: zero references anywhere in src or scripts to deterministic_under_10min, findOpenOutboundForColleague, DETERMINISTIC_WINDOW_MINUTES, or _attendee_status_note (one comment names the removal, which is correct residue).",
    "wf_94e9e397-06a VERIFY: gh#169-a's uniqueness query is LIVE, not a dead branch: requests.owner_user_id is written from input.ownerUserId (src/db/requests.ts:94) and systemPrompt.ts:180 queries that same column with user.slack_user_id, so getAwaitingOwnerRequests(ownerRow.owner_user_id) at skill.ts:1340 returns the owner's real open approvals.",
    "wf_94e9e397-06a VERIFY: gh#169-a's uniqueness set cannot miss a row the model was shown: getAwaitingOwnerRequests (src/db/requests.ts:226-234, parent_request_id IS NULL AND state='awaiting_owner') is the identical query behind the PENDING APPROVALS prompt block and list_pending_approvals (skill.ts:1471), so the whole approval surface is top-level-only and the ambiguity check spans exactly it.",
    "wf_94e9e397-06a VERIFY: gh#169-a cannot be bypassed by retrying a different approval_id: a second id that also matches the owner's message re-enters the same ambiguity refusal, and one that does not match fails namedMatch and lands on the pre-existing 'Not anchored' refusal. No id binds an ambiguous message.",
    "wf_94e9e397-06a VERIFY: gh#169-a leaves anchored replies untouched: namedMatch is `!anchored && ...` (skill.ts:1327), so an anchored reply can never enter the `if (namedMatch)` uniqueness block at :1339 and never reaches the refusal at :1347.",
    "wf_94e9e397-06a VERIFY: o#190's four citations are exact at the current tree: forgetHardBlockedSlot has exactly five call sites (availabilityPreCheck.ts:954, :1183, :1189; runOutputGates.ts:1035, :1061) and each carries the rule the doc assigns it.",
    "wf_94e9e397-06a VERIFY: o#194 removed no documentation: every paragraph of the deleted resolver.ts block survives elsewhere (the mint-site citations and match rationale at textScrubber.ts:193-212, the sendDirect-path rationale folded into resolver.ts:1298-1308), and the two surviving comments make different claims, so the deletion created no new duplicate.",
    "wf_94e9e397-06a VERIFY: o#188's two read points now agree: claimChecker.ts:201 and :254 both exclude 'confirmed with <person>' / 'confirmed the booking' / 'confirmed the meeting' from the object-scoped exemption. Text-level proof only, not exercised against live Haiku.",
    "wf_94e9e397-06a VERIFY: the hebrew tokenizer change is a pure widening for ASCII: \\p{L}\\p{N} is a strict superset of A-Za-z0-9, and the .filter((lower,i) => rawTokens[i]) index pairing is safe because .map preserves length and order — so English subject tokenization is unchanged apart from accented words no longer splitting.",
    "wf_94e9e397-06a VERIFY: `npx tsc --noEmit -p tsconfig.json` is clean across the whole working tree with all five of this wave's edits on disk (run once, after reading the full diff).",
    "wf_a964b44c-dc9 VERIFY: o#187's merge is verdict-preserving with no third role to leak through: skills/types.ts:93 types senderRole as exactly 'owner' | 'colleague', so the shared gate's inverted test (trust owner) is equivalent to the deleted inline test (gate colleague).",
    "wf_a964b44c-dc9 VERIFY: the shared gateSensitivity cannot throw on a malformed attendees arg: createMeeting.ts:130-141 refuses a non-array with error 'invalid_attendees' before the call, so dropping the old Array.isArray guard is safe.",
    "wf_a964b44c-dc9 VERIFY: the two-part email reply mechanism is fully retired with no dead residue: zero references to CUT_LINE_RE / forYouPart / forwardPart / forYouVerdict / forwardVerdict anywhere in src or scripts, and only two historical rationale comments still name PART 1.",
    "wf_a964b44c-dc9 VERIFY: gh#175-a's prompt cut took no capability with it: the surviving forwardable block still demands each candidate time in every attendee's own local zone plus duration, subject and context, so the per-slot zone visibility PART 1's Zone line existed for is still required in the external text.",
    "wf_a964b44c-dc9 VERIFY: the decisiveness gate cannot cost anything on the common path or bind on a failure: skill.ts:1376-1394 runs the Haiku call only after !anchored && messageReferencesRequest already passed, and 'unsure', 'not_decided' and a throw all land on the pre-existing needs_clarification refusal.",
    "wf_a964b44c-dc9 VERIFY: a Hebrew or Cyrillic 3-letter subject word can never become a distinctive needle regardless of the stopword list: the 3-char exception at closeLoopOnOwnerHandled.ts:163-169 requires /[A-Za-z]/ in the raw token.",
    "wf_a964b44c-dc9 VERIFY: gh#175-b's new field is additive-only on an existing contract: locationModeChangeNotice has exactly one reader (moveMeeting.ts:1232) and createMeeting ignores it, so no other PlanAction consumer is affected.",
    "wf_a964b44c-dc9 VERIFY: this wave adds no new part to the system — no new file, table, column, tool, skill, background job or output-path guard. The one new LLM call (classifyOwnerAssertsDecision) is a second instance of a pattern already in the same file, on a fallback path only.",
    "wf_a964b44c-dc9 VERIFY: `npx tsc --noEmit -p tsconfig.json` is clean across the whole working tree with all seven fixes on disk (run once, after reading the full diff).",
    "PRE-WRAP FINAL VERIFY: deleting the @-mention auto-add loses nothing: helpers.ts:29 rewrites <@U...> to 'Real Name (slack_id: U...)' on every inbound path and processMessage.ts:349 now does it for backfilled history too, so the deleted regex could never match; the colleague still lands in attendeeEmails via classifyTurn -> resolveNamedInternalAttendees -> the union at findAvailableSlots.ts:446.",
    "PRE-WRAP FINAL VERIFY: location_mode_change reaches the model on both search shapes in findAvailableSlots.ts — spreads at :1607, :1664, :1686, flag present in the wrapper condition at :1799.",
    "PRE-WRAP FINAL VERIFY: workingHoursDefault refactor is behavior-identical for the live tenant: office Mon/Wed/Thu union home Sun/Tue = Sun-Thu at 09:00-18:00, exactly the removed ISRAEL_DEFAULT; loadUserProfile is cached (userProfile.ts:572), so the added cost is one readdirSync.",
    "PRE-WRAP FINAL VERIFY: coordinator.ts's two deleted UPDATE tasks...WHERE skill_ref statements are safe: message_colleague mints no per-send tasks row (db/jobs.ts:319), and closure flows through getLinkedRequestIdForOutreach -> closeRequest, which is what the brief and get_my_tasks read.",
    "PRE-WRAP FINAL VERIFY: runOutputGates' new live re-check cannot false-clear on an outage: getOwnerEventsForDecision throws, the inner catch sets confirmed=true.",
    "PRE-WRAP FINAL VERIFY: the PRESERVE cross-reuse claim for gh#175-b holds regardless of the isOnline-signal defect — predictor and planMeeting source priorStartIso/existingLocation/existingIsOnline from the same Graph fields, so a same-day-type candidate hits preserve_existing in both and emits nothing.",
    "PRE-WRAP FINAL VERIFY: gh#175-b's email gate is real: connectors/email/inbound.ts:308 sets channel:'email', the same field the pre-existing email slot-stash keys on.",
    "PRE-WRAP FINAL VERIFY: `npx tsc --noEmit` is clean across the FULL accumulated tree (all 5 waves + the direct gh#175-b re-fix), exit 0.",
    "wf_386bf586-bf1 VERIFY: the ops.ts:90-93 email strip is live, not a dead branch: SkillContext.channel is assigned from input.channel (orchestrator/index.ts:416), connectors/email/inbound.ts:308 passes 'email', and create_meeting is in CHANNEL_TOOL_CLAMP.email (registry.ts:277-279) — so override_notice from createMeeting.ts:1753 genuinely reaches that delete.",
    "wf_386bf586-bf1 VERIFY: deleting override_notice / _attendee_busy_note breaks no consumer: each has exactly one writer (createMeeting.ts:1753, moveMeeting.ts:1577) and zero readers anywhere in src or scripts, and no output gate reads either key.",
    "wf_386bf586-bf1 VERIFY: move_meeting cannot execute on an email turn — absent from CHANNEL_TOOL_CLAMP.email and refused at both shipping (filterToolsByScope, registry.ts:310) and dispatch (registry.ts:675-681) — so the _attendee_busy_note half of the strip is an inert no-op, not a behaviour change on any channel.",
    "wf_386bf586-bf1 VERIFY: deferredActionReplay is not a second path for an owner-facing aside to reach an email reply: it hardcodes channel:'slack' (deferredActionReplay.ts:79) and only inspects the returned object for error/success:false/ok:false sentinels (:114-124) — it never narrates the payload.",
    "wf_386bf586-bf1 VERIFY: isSubjectStopword fails in the safe direction: over-filtering can only empty `tokens`, and with no token and no counterpart match messageReferencesRequest returns false (closeLoopOnOwnerHandled.ts:193), which blocks a close and leaves the row for tomorrow's brief.",
    "wf_386bf586-bf1 VERIFY: the Hebrew proclitic set is declared exactly once (closeLoopOnOwnerHandled.ts:128) and read by both hasWholeWordMatch and isSubjectStopword, and SUBJECT_STOPWORDS has exactly one filter call site — no parallel list, no second spelling to drift.",
    "wf_386bf586-bf1 VERIFY: the prefixed-stopword filter was reachable before the fix, so it is not a fix to a non-problem: a 6-char token like \"הפגישה\" passes the `lower.length >= 4` test at closeLoopOnOwnerHandled.ts:186 and was returned as a distinctive anchor.",
    "wf_386bf586-bf1 VERIFY: the batched classifier's \"number 1 = most recent\" prompt and its fail-open are both correct: findOpenOutboundsForColleague orders `datetime(sent_at) DESC` (recentOutboundContext.ts:123) and `eligible` is built in iteration order, so candidates[0] is the newest open outbound.",
    "wf_386bf586-bf1 VERIFY: the batched classifier preserves the old fail-open semantics exactly: a throw, a non-numeric reply and an out-of-range number all resolve to candidates[0], which is the row the old per-candidate loop matched first via `return true`.",
    "wf_386bf586-bf1 VERIFY: no row of any TaskType o#192 removed can reach a dispatcher lookup: live DB shows reminder and follow_up only in cancelled/informed and research/briefing absent entirely, so getTasksDueNow's `status IN ('new','pending_colleague') AND due_at IS NOT NULL` (tasks/index.ts:124-125) matches none of them.",
    "wf_386bf586-bf1 VERIFY: `npx tsc --noEmit -p tsconfig.json` is clean across the whole working tree with all three of this wave's edits on disk, exit 0.",
    "wf_386bf586-bf1 VERIFY: this wave adds no new part to the system: no new file, table, column, tool, skill, spine, output-path guard or background job. The one signature change (classifyResponseVsNewTopic) is on a module-private function with a single caller, and the one new function (isSubjectStopword) is a private helper over an existing set.",
    "BOUNCER (uncommitted-tree pass): owner_override_not_applied is not a third owner-facing aside reaching an email reply — clampedRelaxedNotice (bookingRequest.ts:526-532) requires isMpim===true AND senderRole==='colleague'; the email leg is always senderRole:'owner', never MPIM.",
    "BOUNCER (uncommitted-tree pass): the Hebrew stopword over-filtering fails safe at BOTH consumers of messageReferencesRequest, not just the one this wave's own verify checked — refuse-close at closeLoopOnOwnerHandled.ts:269, and 'Not anchored' at tasks/skill.ts:1411. No path where an empty token list causes an incorrect close or bind.",
    "BOUNCER (uncommitted-tree pass): manage_working_elsewhere (the verbMap key the cleaner removed) had zero references anywhere in src, scripts or config — genuinely dead, not a fix to a non-problem.",
    "BOUNCER (uncommitted-tree pass): of the 21 .claude/*.md docs the cleaner deleted, only RESERVE_SLOT_PROJECT.md has live inbound citations from src/ comments (db/client.ts:527, db/slotHolds.ts:10, already flagged as its own row) — the other 20 are referenced nowhere in .claude, src, scripts or CLAUDE.md."
  ],
  "_inFlightClearedNote": "FULLY EMPTIED 2026-08-03: 7 of 8 cleared when wf_e2fd1caf-c84 returned; the 8th (173abc, direct slackmaster dispatch) cleared when it returned separately — verdict already-fixed, nothing to build.",
  "lastRun": {
    "id": "wf_ad941677-935",
    "status": "complete",
    "startedAt": "2026-08-03T13:10:00+0300",
    "note": "Owner-ordered build, 2 named issues, both built clean. Privacy leak (matchmaker) fixed at root — subjectViewerFor now channel-aware. Security regex (gatekeeper) BOUNCED ONCE — attempt 1 was a patch (comment claiming parity), attempt 2 fixed root (deleted duplicate regex, imported canonical) and cleared. 2 new discoveries queued. gh#154 partial-progress noted (email viewer split landed; the 3 Slack clamp surfaces #154 actually asks about are untouched).",
    "notBuilt": []
  }
}
