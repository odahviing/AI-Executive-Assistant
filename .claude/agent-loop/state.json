{
  "_thisFileNote": "CURRENT STATE ONLY, and that is the rule this file lost: the keys documented in .claude/skills/manager/SKILL.md 'State you own', ONE operating note each, and the run in progress or the last one (its `note` = what went in, `_resultNote` = what came out). Everything older is HISTORY and history is ledger.jsonl, which is append-only and queryable. Reduced 2026-07-31 from 114 KB / 44 prose keys against 7 documented ones; every ruling in those 44 was verified to be a ledger row already before a byte was deleted. A `_previousRun*` key, a second note on one field, or a note about a run that has ended means it is regrowing - move it to the ledger and delete it. The wrap is where this is checked.",
  "lastSeenIso": "2026-07-31T11:08:00+0300",
  "_lastSeenIsoNote": "Set to the run's START, not its end. wf_b81e5b49-35b ran 17:16-18:18; anything said during that hour is still unreviewed, and re-reading an hour costs almost nothing while skipping it can lose a real bug.",
  "_lastRunNote": "status MUST be one of complete | running | stopped. `stopped` exists because a run the owner kills leaves `running` set forever, and the resume rule then re-fired on every subsequent turn — a restart loop that spawned wf_79ce66bb-efc while wf_62f4811d-7fe was already dead. NEVER auto-resume: a dead run and a slow one are indistinguishable from this file, so resuming is a guess. Report the state and ask.",
  "lastWrapIso": "2026-07-31T23:32:41+03:00",
  "_nextReportIdNote": "ONE counter for report-row ids; assign then increment, NEVER renumber and NEVER reuse. The FORMAT is defined in exactly one place — the `#` bullet in .claude/skills/manager/SKILL.md — and is deliberately NOT restated here. The old text was, and it said 'no letter prefixes' after the owner authorised a source letter on 2026-07-30 (G github · L logs · B both · O owner, e.g. G176), so this file contradicted the spec within hours. Flagged by the architect applying X53, which was scoped out of touching state.json. Two definitions of one format is the defect; a pointer is not.",
  "nextReportId": 186,
  "_inFlightNote": "Nine items held by engine run wf_6e41a1e4-f2c. EMPTY THIS IN THE SAME TURN IT RETURNS — a stale entry is the STOP 1 lock and refuses the next run. Six are matchmaker in one dispatch, which is deliberate: they all touch createMeeting.ts / findAvailableSlots.ts and one lane means one writer, not six racing on the same files (STOP 1c).",
  "inFlight": [],
  "_pendingOverflowNote": "Verify DISCOVERIES and over-cap items, shaped for args.issues. Pass on the next `build` and the engine prepends them itself, then delete what manifest.carry names. PRUNED 2026-07-31 after wf_6e41a1e4-f2c: five entries left because they were BUILT this wave. HEAD OF THE QUEUE: showas-read-through-as-any, the sibling of the cast hole just closed. HIS STANDING RULING: the queued discoveries are saved for tomorrow and are not to be folded into a build until he says so. REMOVED owner-memory-still-renders-in-a-real-channel from this queue: it is a PENDING-OWNER row on report.md awaiting his one-flag-or-identity ruling, and a row that is both queued and on his desk gets dispatched twice — the same double-count X101 is about. It comes back through `build` when he rules, not through the overflow drain.",
  "pendingOverflow": [
    {
      "id": "showas-read-through-as-any",
      "lane": "matchmaker",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "invariant": "structural-fact-read-through-any",
      "symptom": "A one-character typo in the hard validator's working-elsewhere / free detection compiles clean and silently disables the classification — a WE marker or a free block would read as a hard commitment, or the reverse, with nothing failing.",
      "evidence": "src/utils/scheduleRules.ts:956 (`if ((ev as any).showAs === 'free') continue;`), also :513, :515 and :957. showAs IS declared on CalendarEvent (calendarTypes.ts:10) as a string union, so the cast removes both the property-name check AND the value-union check — `'fre'` would compile today. The identical hole was just closed for isAllDay in the same lines, so the method is proven."
    },
    {
      "id": "hardblock-ledger-has-no-refuter",
      "lane": "matchmaker",
      "severity": "medium",
      "clarity": "clear",
      "source": "verify",
      "symptom": "Maelle can rewrite a true reply into a false refusal about a time that has since freed up. A slot established as blocked keeps overriding drafts for up to 45 minutes even when nothing in the current turn re-checked it — and the reader is told a 'deterministic calendar check has ALREADY established' it.",
      "evidence": "The hard-block ledger has exactly one writer and one forgetter, both inside src/utils/availabilityPreCheck.ts (:909, :914 — grep-confirmed unique in src), but its reader src/utils/guards/runOutputGates.ts:881-923 fires on every turn against a 45-minute TTL (src/utils/availabilityGate.ts:264). Any turn where the pre-check does not run — an owner turn, a message with no schedulable signal, a Haiku-gate miss, and now a turn naming a colleague — inherits the previous verdict with no way to refute it. The gh#158 fix WIDENS this; the asymmetry itself predates it. This is why the verify would not sign off on gh#158, and why its option (a) is the minimum."
    },
    {
      "id": "read-recap-has-no-shield",
      "lane": "gatekeeper",
      "severity": "medium",
      "clarity": "clear",
      "source": "verify",
      "symptom": "A truthful recap of a calendar READ done last turn can be inverted into a false 'actually, I haven't checked that yet' — the reply retracts something Maelle really did do, and the owner then re-asks for work already finished. Latent, not observed firing.",
      "evidence": "src/utils/guards/runOutputGates.ts:779-792 — the claim-checker's recap shield is `mutationCarried = toolSummariesText.includes('mutated=' + verdict.action_type)`, and ONLY MUTATIONS carry a mutated= marker (turnHelpers.ts mutationDomain). Read tools carry none, so a recap of get_calendar / find_available_slots / check_join_availability has nothing that can shield it, and rewriteOwningTheMiss's SAFE-MISS clause (claimChecker.ts:622) is the only thing between it and an inverted true statement. The shield was built for mutations while the guard it protects also judges reads."
    },
    {
      "id": "prefilter-depends-on-history-provenance",
      "lane": "gatekeeper",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "symptom": "The new pre-filter's behaviour depends on where conversation history came from, so the same thread can be gated differently after a restart — an identical turn is checked or skipped depending on provenance.",
      "evidence": "src/utils/guards/runOutputGates.ts:929-933 scans ctx.history assistant content for `[<tool>` markers, which src/connectors/slack/postReply.ts:513-515 writes only on the DB path; rows re-fetched from the Slack API carry none. logs/maelle-2026-07-28.log at 12:10:14 shows the merge — 'Thread messages merged from Slack', dbCount 6, mergedCount 10, missedCount 4 — so four of ten rows in that thread had no markers by construction. Same root family as the gh#156-b overturn: keying on a proxy rather than the fact. NOTE: both files this cites were REVERTED on 2026-07-30, so this describes code that no longer exists in the tree — it is only actionable if gh#156-b is ever rebuilt on a marker-based pre-filter, which is itself the thing to avoid."
    },
    {
      "id": "attendee-status-shipped-without-its-note",
      "lane": "matchmaker",
      "severity": "low",
      "clarity": "clear",
      "source": "verify",
      "symptom": "The colleague-path attendee_status annotation reaches the model bare, with no note saying what it means, while every sibling condition in the same payload carries one — and the tool schema documents the field only for the ignore_attendee_availability=true case.",
      "evidence": "src/skills/meetings.ts:276 documents attendee_status only for the ignore-availability case; the default colleague-path annotation from annotateSlotsWithAttendeeStatus (findAvailableSlots.ts:1417-1453) ships it with no _note, unlike _attendee_conflicts_note, _over_optional_note and the rest. FLAGGED NOT BUILT by the lane, correctly: there is no proof it caused the observed 'I haven't actually checked' line, which could equally reflect a genuine 'unknown' status. Worth carrying because a field the model receives with no note about its meaning is how a hedge or an over-claim gets invented downstream."
    }
  ],
  "_verifiedCleanNote": "Passed to bugger/feature as `priorClean` so a verify does not re-audit settled ground. ONE PROVEN CLAIM PER ENTRY, nothing else: a `_DROPPED`/`_CORRECTION` note is history and belongs in ledger.jsonl, and an entry here that is not a claim is handed to the examiner under a heading that says SETTLED. DROP an entry the moment a wave changes the code it describes — a stale 'proven clean' silences a real check, which is worse than having no list; re-proving costs one pass, missing a regression costs a person, so when in doubt drop it. PASS IT VERBATIM: `priorCleanDropped` keys on the INDEX in the array you passed (X84), so a hand-trimmed list comes back unmatchable. PRUNED 2026-07-31 (X99): 109 members -> 38 claims. Deleted the whole pre-4.3.2 block below the wf_6852af85-afc entries, which no run had passed since — the file's own stale warning offered 'prune the old block or keep passing empty', and every recent wave chose empty and hand-trimmed instead, which is what broke the index keying. Deleted the 9 underscore annotations with it.",
  "verifiedClean": [
    "o#178 masking cannot fail open for want of a Graph field: isEventPrivate (src/utils/displaySubject.ts:90-101) consults ONLY event.sensitivity and event.categories, and getEventEndInstant's .select now carries both. There is no third field to add.",
    "getEventEndInstant has exactly two call sites (src/skills/meetings/ops/handlers/createMeeting.ts:64 and :702); the :64 anchor path puts .subject in a logger.info only (:77), so the widened return type reaches no user-facing text other than the one o#178 fixed.",
    "violation_kind 'owner_busy_collision' is emitted by scheduleRules rule 8 alone, and allowRelaxed makes rule 8 pass — so gh#165-d's new strict-reject stamp and the pre-existing relaxed-pass stamp are mutually exclusive per slot, and first-hit-wins is preserved by stampConflictingEvent's early return.",
    "humanizeViolationLabel has a real 'owner_out_of_office' case (src/skills/meetings/ops/violationLabels.ts:28), so gh#165-d's OOF label cannot collapse to 'unknown' and cannot misroute a known all-day collision into the \"I can't tell which rule flagged it\" message.",
    "gh#165-d incidentally repairs the verb-phrase-in-an-adjective-slot garble for owner_busy_collision: that sentence fired only because conflictingEvent was always undefined on a strict colleague reject, and with it populated the grammatical branch (createMeeting.ts:653) wins. Re-read the parked row before re-dispatching it.",
    "The subject in gh#165-d's new all-day message is already privacy-masked at production: it comes from checkSlot's overCommitment.subject, which is displaySubject(ev, profile, viewer) at src/utils/scheduleRules.ts:601.",
    "Slack @mentions reach the orchestrator as \"Real Name (slack_id: Uxxx)\" (src/connectors/slack/app/helpers.ts:86-97), so gh#158-a's substring gate cannot drop a mention-named attendee. Any future reasoning about that gate can take this as established.",
    "gh#158-a can only SHRINK resolvedMeetingAttendees, and the gh#158 pre-check bail keys on that list being non-empty — so the only new behaviour on that seam is the pre-check running on a turn that genuinely named nobody, which is the correct answer for such a turn. The gate cannot make a bailing turn un-bail for a name the message did contain.",
    "o#177 is a cut that took nothing valuable: the owner's identity and authority in a clamped MPIM are still stated in full by authLine (src/core/orchestrator/systemPrompt.ts:430-436) and his preferences come from ownerPreferenceBlocks, so neither removed block carried a capability or any of Maelle's voice.",
    "threadPeopleBlock (src/core/orchestrator/systemPrompt.ts:778) was never part of the o#177 leak — it passes user.slack_user_id as the exclude argument. Do not re-open it as a third leaking block.",
    "o#180's newsTimedOut is assigned at exactly one site (src/tasks/briefs.ts:740, inside `if (!gathered)`), so a completed-but-empty news gather leaves the brief byte-identical to pre-fix. The quiet-day/no-apology rule is intact.",
    "gh#168-a's day_summary enrichment mutates the array in place at handlers/findAvailableSlots.ts:950, strictDaySummary captures that same reference afterwards at :1166, and the recovery pass REPLACES diagnosticsOut.daySummary rather than mutating it (connectors/graph/findAvailableSlots.ts:1390) — so the note reaches the model on the early-empty, relaxed-recovery and full-success exits alike.",
    "`npx tsc --noEmit` is green with all eight wave files on disk (run once, after reading the full diff).",
    "wf_4bbfc750-1a9 VERIFY: `npx tsc --noEmit -p E:/Code/Maelle/tsconfig.json` is clean across the whole working tree with both gh#165-d edits in place — the two new optional fields on diagnosticsOut.conflictingEvent are structurally assignable at moveMeeting's narrower local diagnostics type, which declares no conflicting field at all",
    "wf_4bbfc750-1a9 SPOT-CHECK: the gh#158 colleague bail is present at HEAD — availabilityPreCheck.ts:517-541 returns the empty result (ran:false, no promptBlock) whenever namedAttendeeEmails is non-empty, and forgets any named instant from the hard-block ledger first, so the output-time floor has nothing stale to fire on",
    "wf_f0cfd84d-5f5 VERIFY: the src/tasks/briefs.ts force-gate call-site census is COMPLETE — sendMorningBriefing has exactly three callers in src: routine.ts:108 (force omitted, 20s scheduled), processMessage.ts:517 and skill.ts:598 (both literal true, 8s on-demand). There is no scripts/ caller, so the scheduled path gh#166 was filed about cannot regress",
    "wf_f0cfd84d-5f5 VERIFY: src/tasks/skill.ts:300 is INSIDE the create_approval tool-description template literal (which opens at :295 and runs to :325), not a separately gated prompt section — so with create_approval living in TASKS_MODULE which CORE_MODULES always includes, the reworded text ships on every Slack turn and cannot be hidden by a role or channel gate",
    "wf_f0cfd84d-5f5 VERIFY: gh#165's failing turn created a FRESH approval, not a dedup reuse — logs/maelle-2026-07-29.log:764 and :767 carry no 'create_approval — reusing OPEN idempotency match' line, so the handler returned created:true / reused_existing:false. The confusing sentence was the model's own composition, which is why prompt wording is the correct lever and not the dedup path at skill.ts:988-1049",
    "wf_f0cfd84d-5f5 VERIFY: the claim-checker already covers the attendee-completion family and NO output gate is owed for gh#165 — claimChecker.ts:247 instructs claim_specifics_mismatch for exactly 'added Yael to the invite when only move_meeting ran', and runOutputGates.ts:777/:796 branch on it. gh#165's sentence is literally TRUE but ambiguous, so it is correctly outside that gate's remit. A future pass should NOT re-open this as a missing guard",
    "WAVE-VERIFY 2026-07-30: availabilityGate.ts:266 + :334-339 — the hard-block ledger is a module-local Map and forgetHardBlockedSlot only filters it. No I/O, no LLM, no return value",
    "WAVE-VERIFY 2026-07-30: runOutputGates.ts:881-949 — the ledger's only ACTING reader returns initialReply unchanged on an empty ledger (:892), and its sole effect on a fire is rewriteBlockedSlotClaim's text. No tool call or mutation exists in the function, so deleting entries can only make the floor act LESS. This is the owner's no-booking condition, re-derived independently rather than relayed",
    "WAVE-VERIFY 2026-07-30: availabilityPreCheck.ts — recordHardBlockedSlot is called only at :943 inside the verdict loop, unreachable from the :495 bail; grantRelaxed and policy_exception appear nowhere in the file",
    "WAVE-VERIFY 2026-07-30: all 25 `overCommitment` references read named subfields or truthiness; none spreads the object, so the new required `id` cannot reach another caller's payload",
    "WAVE-VERIFY 2026-07-30: availabilityPreCheck.ts:562 and :1149 compute `today` with the identical expression, so the main fallback extraction and the forgetter's extractRawPairs cannot disagree about what 'tomorrow' means",
    "WAVE-VERIFY 2026-07-30: eventsForWeek is reachable only AFTER the :495 bail returns — the live-read swap (152-refresh) and the gh#158 bail do not interact, and a bailed turn makes zero calendar reads",
    "wf_0cebe938-c81 VERIFY: postReply persists tool markers RAW into assistant history (connectors/slack/postReply.ts:513-515) and renderToolSummary emits them bracket-prefixed (turnHelpers.ts:236-239, :207, :211), so a `[<tool>` substring test DOES match DB-written history rows. The marker plumbing is real — it is the BREADTH of the skip that is wrong, not the mechanism",
    "wf_0cebe938-c81 VERIFY: ownerIsActing (runOutputGates.ts:215, senderId === profile.user.slack_user_id) is TRUE for an owner speaking in an MPIM — confirmed against production, logs/maelle-2026-07-28.log 'processMessage — role determined' rawRole owner / effectiveRole colleague / isOwnerInGroup true with senderId U0F28CK6H, and the claim-checker duly ran on all three of that thread's owner turns. Any future reasoning about which leg an owner-in-group turn takes can take this as established",
    "wf_6852af85-afc VERIFY: gatherNews (src/skills/news.ts:255-304) can neither throw nor resolve to undefined — a whole-body try/catch returns {...EMPTY_BUNDLE} at :302 and every early return is a NewsBundle (:281, :299) — so `!gathered` in briefs.ts:707 is exactly and only the Promise.race timeout branch. gh#167's three-way branch rests on a true premise",
    "wf_6852af85-afc VERIFY: NEWS_BRIEF_TIMEOUT_MS has exactly one reader (src/tasks/briefs.ts:696) and none of the three sendMorningBriefing callers (tasks/dispatchers/routine.ts:108, connectors/slack/app/processMessage.ts:517, tasks/skill.ts:598) imposes an outer timeout, so the 20s window cannot be clipped by a caller — the only exposure is wall-clock wait, not a truncated pass",
    "wf_27f03aca-0dd VERIFY: `npx tsc --noEmit` is clean across the whole working tree with all four fixes in place — every namedAttendeeEmails / locationHint / viewer / conflictingEvent / overCommitment.id signature change is applied consistently at every call site",
    "wf_27f03aca-0dd VERIFY: overCommitment.id (scheduleRules.ts:587) can only ever be a real, overlapping commitment — it is assigned inside the one occupancy-scan iteration that has already passed `role !== 'ignore' && role !== 'optional'` (:571-582) and the overlap test `evStart < slotEnd && evEnd > slotStart` (:575). The class of event the previous attempt was overturned for (an occupancyRoleOf-ignored free/floating block starting at the requested time) is structurally excluded",
    "wf_27f03aca-0dd VERIFY: the new update_meeting(add_attendees) steer cannot produce an unapproved real-world action — moveMeeting.ts:162-210 refuses a non-requester colleague with `colleague_not_requester` and routes it to owner approval with a deferred_action_hint; only the meeting's own requester applies a change directly",
    "wf_27f03aca-0dd VERIFY: locationHint is a live, populated field end-to-end, not a dead parameter — args.location -> bookingRequest.ts:171 -> planMeeting.ts:161 -> planMeeting.ts:327/:348 -> detectCategory, and resolveLocation already consumes the same value at planMeeting.ts:462. No new fetch, no new LLM call",
    "wf_27f03aca-0dd VERIFY: resolveNamedInternalAttendees excludes the owner twice over — by name at resolveAttendeeEmails.ts:180 and by the exclude set at :211 — so a colleague asking only about the owner ('is Idan free at 12:30?') keeps a non-empty pre-check. The gh#158 bail cannot fire on the file's own primary use case",
    "wf_27f03aca-0dd VERIFY: gh#158's root cause is established from production logs, not from the lane's summary — logs/maelle-2026-07-28.log:669 (resolved levana.b@, 12:24:09.680) is followed by :671/:677 (pre-check normalized 2026-07-29T10:00, injected notBookable) and :721/:722 (11:00 injected bookable:1). The owner-scoped pre-check is where both wrong-subject answers came from",
    "wf_27f03aca-0dd VERIFY: the 2026-07-30 second wave adds no new part to the system — no new file, tool, skill, table, column, background job or output-path guard. Every addition is an optional parameter or field on an existing contract (namedAttendeeEmails, locationHint, diagnosticsOut.conflictingEvent, overCommitment.id), and the one deletion (findDuplicateEvent's null mode) removes a path rather than layering over it",
    "wf_6852af85-afc VERIFY: the availability floor cannot rewrite gh#165-b's intended outcome into a refusal — utils/guards/runOutputGates.ts:895 stands the floor down when `result.bookingOccurred === true || tape.includes('mutated=book')`, and a successful update_meeting is in the book mutation domain (core/orchestrator/turnHelpers.ts:108) and emits an `[update_meeting OK …]` tape entry matched by MUTATION_OK_RE (:363)",
    "wf_6852af85-afc VERIFY: this wave adds no new part to the system — no new file, tool, skill, table, column, background job or output-path guard. The only contract change is the widening of an existing shared primitive (findDuplicateEvent), and both of its prior callers are unaffected"
  ],
  "_discoveriesDeferredNote": "OWNER RULING 2026-07-31: \"the 15 discoveries, save for tomorrow\". THERE IS A SCOPE CONFLICT HERE AND IT WAS RAISED WITH HIM RATHER THAN RESOLVED SILENTLY. He said this AFTER approving the ten rulable rows the Manager listed, and SIX of the nine now building are themselves queued discoveries: owner-memory-still-renders-in-a-real-channel, candidate-validation-branch-has-no-grounded-window, isallday-read-through-as-any-six-times, three-dead-geteventendinstant-imports, broken-rule-label-is-a-verb-phrase-in-an-adjective-slot, send-briefing-now-desc-promises-standalone-dm. The reading taken, and stated to him: his approval of a named list governs those rows, and \"save for tomorrow\" governs the REST of the queued pile — the 4 he never saw in the table (hardblock-ledger-has-no-refuter, read-recap-has-no-shield, prefilter-depends-on-history-provenance, attendee-status-shipped-without-its-note). pendingOverflow is left INTACT either way, so nothing is lost whichever way he rules; if he meant the six to wait, the run can be killed and those six re-queued with no bookkeeping to undo.",
  "_inFlightClearedNote": "EMPTIED 2026-07-31 in the same turn wf_6e41a1e4-f2c returned and the send-briefing resend came back. A stale entry is the STOP 1 lock and refuses the next run.",
  "lastRun": {
    "id": "wf_655d8245-78f",
    "status": "complete",
    "startedAt": "2026-08-01T01:05:00+0300",
    "note": "PRESET build of his 2026-08-01 rulings: 2 builds across 2 lanes (transporter D2-forward-inert-without-owner-email, shepherd amend-reason-bypasses-id-veto), gh#156-c DECLINED, and BOTH gh#165 rows DEFERRED at his word ('lets defere the two gh#165 for next run'). Both builds are OVERDUE DEFERRALS the new ledger-stats check surfaced — 9 and 12 runs respectively — so this is the first wave where the overdue gate directly produced work. alreadyBuilt empty and inert on a preset. openKnown not passed: the scout is skipped and pendingOverflow is not passed, so it has no job. pendingOverflow NOT passed and NOT drained — his standing ruling holds the queued discoveries for tomorrow. priorClean passed as 12 entries scoped to the two lanes' files rather than all 38, and that is a deliberate deviation recorded here: nothing in the array touches resolver.ts or attendeeScope.ts, so the rest would buy the examiner nothing on this wave. CONSEQUENCE OF THE DEFERRAL HE SHOULD SEE AT THE WRAP: five of the six open Bug tickets can close, gh#165 cannot.",
    "_previousRunC12b1794": {
      "id": "wf_c12b1794-265",
      "status": "complete"
    },
    "notBuilt": []
  }
}
