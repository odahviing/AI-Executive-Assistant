# Agent-loop report

**gh#24 — semi-manual email chain. Built in the tree, UNCOMMITTED.**

**The wrap is AVAILABLE.** The final verify's explicit ruling: *acceptable to wrap, **not** acceptable to flip `channels.email.enabled: true` on.* Typecheck green on the whole tree. A plain unscoped owner Slack turn is byte-identical before/after — closed by derivation, conditional on only the Slack connection being registered.

**Nothing here has ever executed.** The mailbox did not exist while any of it was written and the channel is off, so "built" means typechecks + traced, never exercised. **Your first forward is the real test.**

## Nothing is waiting on a decision — row 124 RULED and BUILT

Owner's ruling, verbatim: *"keep same prompt as internal, she is still same personally for external."* The persona now renders regardless of scope, on every channel including email, as a second deliberate exception alongside `news` — on an explicit **identity vs capability** axis: the filter polices capability prose (a promise that dies on `not_permitted`), and identity prose is not that.

**Measured, not estimated:** an unscoped owner Slack turn is byte-identical (0); a scope-narrowed turn costs **+304 tokens**; the email leg **+349**. Against row 121's ~53.5k-char saving that is roughly **2%** — the ruling bought the personality back for almost nothing.

Two things the lane corrected rather than relayed. The orchestrator's premise *"the persona block teaches no tools"* was an overstatement — it does instruct `note_about_person` calls; what makes it safe is that the block says the save never replaces the reply, and the end-of-chat capture pass is the designed backstop, so no honesty gap reopens. And the email frame's precedence clause was found **ambiguous** once the persona began rendering there (it was written while the persona was silently absent), so it was strengthened: *"same person, just the register a stranger reads, not a colleague."*

| # | The chat problem | The issue | The solution | Agent | Risk | Status |
|---|---|---|---|---|---|---|
| 124 | On "book me with Dana Tuesday" she reads clipped and transactional — the warmth is gone on scheduling and task messages, though chit-chat still sounds like her | The reachability filter shipped and it also gates the **PERSONA / friend-of-the-team block** (`social.ts:277`), whose two tools live in the `people` scope and are not always-on. So her personality no longer renders on a `meetings` or `tasks` turn. This row was at *pending owner — recommend defer* and shipped inside row 121 | One exception in the same shape the `news` block already uses. Persona teaches no tools, so gating it on tool reachability is a category error — the filter exists for honesty about *capability*, not identity | instructor | Keeping it saves ~13.4k tokens per scope-narrowed owner turn. The bulk of that came from the big capability blocks, not the persona section, so restoring it should cost little — the lane will report the exact delta | **pending owner — recommend build (restore the persona)** |

## Done, verified, waiting only on your wrap

| # | The issue | Agent | Status |
|---|---|---|---|
| 120 | A failed send lost the forward permanently. Now: no retry, a Slack DM naming the original sender + subject, and the verify confirmed the DM cannot lie — the only statements below `sendDirect` are the two history writes, inside their own catch | transporter | **built** |
| 121 | The prompt taught tools the clamp strips. Owner branch now filters on reachability; `channelsYouCanReach` no longer offers Slack on a clamped turn; the `general.ts` twin threads `channel`. **PARTLY CLOSED — see the go-live gate** | instructor + outrider | **built (partial)** |
| 122 | The clamp was an intersection, so a classifier misfire silently stripped scheduling. Now authoritative — returns before any scope logic. **Deviation:** the Haiku call still runs, and that is correct — `classifyTurn`'s `meetingPeople` is what makes attendee resolution deterministic on a forwarded chain, so skipping it would have cost the feature its participant extraction. The orchestrator's original "skip the classifier" recommendation was wrong | outrider | **built** |
| 123 | Two copies of the owner-address rule under a comment claiming one. Now one body in `connections/email/ownerAddresses.ts`; grep confirms zero other spellings | transporter | **built** |
| — | Cleanup: dead `buildSkillsPromptSection` deleted; `scripts/measure-prompt.ts` now measures the live path instead of a string-replace reconstruction | outrider | **built** |
| — | Cleanup: the `CHANNEL_TOOL_CLAMP` doc block no longer asserts the old ordering in the present tense. Row 120 exists because a lane built ordering logic on a wrong comment in this feature — second such defect in gh#24 | outrider | **built** |
| 125 | The slot-hold release DM is not role-gated, so an email booking can Slack-DM a colleague whose hold was released | matchmaker | **deferred** — owner: "not important for now". Left open, NOT declined |

## Live test round — collecting, not fixing yet

`channels.email.enabled: true` since 2026-07-28. **First real email turn ran end to end** (22:48:24 → 22:49:17): sender gate → extraction (2 participants) → clamp shipped exactly 4 tools → `get_person_memory` supplied the attendee's address → 6 slots → all three gates, with `audience:'external'` passed for the first time ever → reply delivered.

**The verify pass paid for itself, provably.** Pre-resolution had "Natan Amid" as `unresolved`; `get_person_memory` is what produced `natanamid@gmail.com`. Under the clamp as originally specified (`find_available_slots` + `create_meeting` only) that call was blocked — this exact test would have failed with no address for the attendee. That is the "E6 and E9 are each right alone and wrong together" finding, confirmed in production.

**Three live runs, all completed end to end** — 19:48:24 (Natan, Hebrew, 2 participants, 53s) · 19:58:53 (James/Kevel, English, 5 participants, 48s) · 20:02:23 (round-2 reply on the Kevel thread, 4 people named, 26s). Prompt cache created once (46k) and reused across all three. Seven findings, sorted by harm.

| # | The chat problem | The issue | The solution | Agent | Risk | Status |
|---|---|---|---|---|---|---|
| 129 | She offered a client 6:15am his time, and would have booked it | **An external's timezone is never read from the chain.** `attendeeAvailability — auto-loaded: javery@kevel.co(Asia/Jerusalem, 09:00-18:00)` — James got the OWNER's zone and hours, so the finder scored 13:15 Israel as comfortable mid-afternoon. 13:15 IL = 06:15 EST. It also rejected 310 slots against hours that were never his. `attendeeAvailability` reads only `people_memory`; a new external isn't there, and nothing looks at the email body, which on this transport is where a stated timezone actually lives | Extract a stated timezone during participant extraction and persist it on the person row — `attendeeAvailability` already reads `people_memory`, so it picks it up for free and the Matchmaker lane needs no change | transporter → profiler (sequential) | **Highest harm in the batch.** `create_meeting` is in the clamp, so this can BOOK a 6am with a client. Every new external hits it. NOTE: this is not closed by the owner's "assume my timezone" ruling — that covers *no signal*; here the chain stated EST | pending owner — recommend build |
| 132 | Round 2 offered times that ignored every attendee | `find_available_slots` ran with **`attendee_emails: []`** while the classifier had named four people (James, Philip Lewis, Jim Douglass, Ali Momin). Owner's calendar only. Run 2 resolved `javery@kevel.co` correctly, so the reply-chain shape specifically broke resolution | Needs diagnosis before a fix — why the extracted set reached the search on a `Fw:` and not on a `Re: Fw:` | matchmaker (likely) | Offers a client times nobody on their side can make | pending owner — recommend build |
| 130 | "in maelle respond she cut the original email, its not good, its reply, keep the chain so i can reply again" | **The reply is not a reply.** Sending goes through `/me/sendMail` with a freshly composed body — no quoted original, no `In-Reply-To`/`References`. **One root, three symptoms:** the chain is cut · the owner cannot reply on it · and round-2 offer binding can never work, because his reply starts a NEW conversation. Proven: run 2 key `…AK94DvNz…` vs run 3 key `…AP8PGoLx…` — different, so the stash could not bind and run 3 fell to the `candidate_slots` path. The stash itself is fine (`data/offered-slots-email.json`, 1,486 bytes, written during run 2) — it is the key that never matches | Use Graph's reply action on the original message id instead of `sendMail`: quoting, threading headers and subject prefix come free, and the recipient is the original sender by construction rather than chosen — so the one-address cap becomes structural. **The cap's enforcement point moves; it must be inherited, not bypassed** | transporter | **Agent corrected 2026-07-29:** was `outrider → transporter (sequential)`. The Graph MAIL layer moved to Transporter, so this is one lane now — recorded as a correction, not as though it always was. Highest leverage in the batch — one fix closes three symptoms and unblocks round 2 | pending owner — recommend build |
| 133 | — | **Extraction disagrees with the classifier about who is in the conversation.** Run 3: `participantCount: 2` while the classifier named **four**. Run 2: `participantCount: 5` for one named person. Neither obviously has it right | Reconcile the two, or make the reply state its extracted set loudly enough that the owner catches the miss | transporter | Silent. Feeds 132, and the owner is the only check | pending owner — recommend build |
| 131 | A conversation topic was silently spent on a message that was never sent | **The social spine fired on an email turn**, exactly as the verify predicted. Run 3: `Social coda DUE on a task turn — transport composes and posts it separately`, `mode: continue`, topic *"Two Steps from Hell"* → `markSubjectRaised` fired, so the subject is **burned** — but `inbound.ts` reads only `result.reply` and drops `result.socialCoda`, so nothing was ever delivered | Gate the social spine off non-Slack transports | profiler (spine) + outrider (the transport gate) | Silent loss of social cadence. Was a predicted discovery; now confirmed in production | pending owner — recommend build |
| 126 | The client-bound email had its date text silently rewritten though the draft was already correct | dateVerifier logged `writtenWeekday: "שלישי"` and `correctWeekday: "שלישי"` — **identical** — as a mismatch, then "corrected deterministically". **Now narrowed: Hebrew-specific.** Both English runs ran the extractor and reported no mismatch | A Hebrew comparison/normalisation fix, not a general one | gatekeeper | A guard that EDITS client-bound text on a false positive is worse than one that stands down | pending owner — recommend build |
| 134 | — | Run 2: `humanGate — rewrote draft` with `originalPreview` and `rewritePreview` **identical**. Either the change is past the 120-char preview, or a gate reported a rewrite it did not make | Make the log honest about whether a rewrite occurred | gatekeeper | Low on its own, but same family as 126: gates editing client-bound text must be truthful about when they act | pending owner — recommend build |
| 127 | — | The Hebrew draft said "ביום שלישי היום (28.7)" while every slot found was 29 Jul. May be a legitimate "writing today" preamble | Owner reads the email text | matchmaker? | Unknown | pending owner — needs your read, lowest priority |
| ~~128~~ | — | ~~Subject arrived as `"Fw: "`~~ | **RESOLVED — not a bug.** Run 2 shows `"Fw: Kevel / Reflectiz"` coming through cleanly; the first forward simply had no subject | — | None | closed |

### RULED 2026-07-28 — the sameness invariant, and the one gap it leaves

The owner challenged the whole shape: *"its the SAME ROUTE — email is just the transport… if i will send the same email, copy paste to slack to maelle, i will get the same answer by word."*

**Verified: the reasoning path IS shared.** `runOrchestrator`, `find_available_slots`, `create_meeting`, `attendeeAvailability`, `scheduleRules`, the person store and all three gates are called unchanged. Run 2's rejection breakdown (`focus_time_office`, `floating_block_no_room`, `owner_busy_collision`) is the ordinary Slack rule set firing on an email turn. No forked planner, no forked spine.

**Five divergences are SANCTIONED — owner: "i agree with the table you made, those are allowed":** the 4-tool clamp · the external reply frame · gates at `audience:'external'` · no social coda · no request row. Note these are incompatible with literal word-for-word identity, and deliberately so — the owner asked for a different register for externals. The invariant he actually wants is **identical reasoning, adapted wording.**

**ONE gap is NOT sanctioned — owner: "the only gap is second participant -> please resolve and use the same format".** A second participant-resolution path was built for email only (`connectors/email/extractParticipants.ts` + `htmlToText.ts`), while Slack resolves through `resolveAttendeeEmails` + `classifyTurn.meetingPeople`. Two answers to "who is in this", not joined up — which is why run 3 called the finder with `attendee_emails: []` while four people were named. **Rows 132, 133 and 137 are one item with one root:** extraction's only job is to hand addresses into the SAME `attendee_emails` route Slack uses, then get out of the way. Extraction itself is still needed — the normal path resolves by slack_id, name and directory, and `javery@kevel.co` is in none of those.

**Also closed by owner ruling:** **138** — gates may keep rewriting client-bound text silently (*"in email, its ok"*). **140** — booking on first contact is acceptable (*"if we have bug we can fix"*). **141** — window selection follows the same logic as Slack; the orchestrator's "no rule" framing was wrong, the three windows tracked what each email asked for. **126** — deferred, not declined (*"ignore for now"*). **139** — folded into 130.

### Design and product issues — the roots under those bugs

Not defects. Each is a decision that was never made, and each explains one or more rows above.

| # | The chat problem | The issue | The solution | Agent | Risk | Status |
|---|---|---|---|---|---|---|
| 135 | "the last offer was 6am in the morning, why?" — he had to ask, because the reply gave no way to tell | **She never states her assumptions, so the gatekeeper cannot gate.** The whole design rests on him validating before forwarding, but the reply carries only the times — not the timezone assumed, the window searched, or the duration chosen. The one assumption that was wrong is invisible in the output | A short "what I assumed" block above the forwardable text: timezone used per attendee, window searched, duration, and the attendee set. He deletes it before forwarding | instructor | Would have caught row 129 on sight. Without it, every future wrong assumption is also invisible | pending owner — product decision, recommend build |
| 136 | — | **There is no timezone input for a new external.** `people_memory` is empty for someone she has never met, and the chain is unstructured prose. So the fallback is not a safety net, it is the *only* path — and it silently produces the owner's zone for everyone | Decide the source of truth: extract a stated zone from the chain and persist it on the person row, infer from the email domain, or ask him once per new external | profiler | Root of 129. Patching the fallback does not fix it; there is nothing to fall back *from* | pending owner — product decision, recommend build |
| 137 | — | **Two extractors both answer "who is in this conversation" and disagree.** `extractParticipants` reads addresses from the forwarded header block; `classifyTurn.meetingPeople` reads names from prose. Run 3: 2 vs 4. Neither is authoritative and nothing reconciles them | Name one authoritative source and make the other advisory | transporter | Root of 132 and 133. This is the parallel-spine pattern the charters exist to prevent, arrived at by accident | pending owner — product decision, recommend build |
| 138 | — | **The gates silently rewrite the text he is supposed to validate.** Two of seven bugs are gates acting invisibly (126, 134). On a leg whose entire safety model is "the owner reads it before it goes out", a gate that edits without saying so removes the thing being relied on | On this leg, gates FLAG rather than rewrite — or state in the reply that they changed something | gatekeeper | Undermines the gatekeeper premise the feature is built on | pending owner — product decision |
| 139 | — | **The transport was built to SEND a message; the product is to PARTICIPATE in a thread.** `sendMail` was a reasonable choice for "deliver a reply" and the wrong one for "stay in a conversation" — which is why one fix closes three symptoms | Reply action on the original message; see row 130 | transporter | Already covered by 130's fix. Recorded so the *reason* is not lost, since it is the class of mistake, not the instance | pending owner — same fix as 130 |
| 140 | — | **The clamp is per-channel, but the risk is per-round.** Round 1 ("when can you meet?") only ever needs to search, yet `create_meeting` ships on it too — so a misread or spoofed first email can book. Round 2 is the round that needs to write | Consider a per-round clamp: search-only until an offer exists in the stash, write enabled once it does | outrider | The write clamp bounded the damage to "junk meetings"; this would bound it further, to "no meetings on a first contact" | pending owner — product decision |
| 141 | — | **The search window has no rule.** Three runs, three windows with no stated basis: 1 day (run 1), 7 days (run 2), 2 days (run 3). Run 1's single day is why four of its six options were within 45 minutes of each other | Decide what an open-ended "when can you meet" should search, and make it deterministic rather than a per-turn model judgement | matchmaker | Inconsistent client-facing offers; run 1's clustering reads badly | pending owner — product decision |

**Closed by owner ruling, not a bug:** an external attendee with no known timezone is assumed to be in the owner's zone (`natanamid@gmail.com` → `Asia/Jerusalem, 09:00-18:00`). Owner: *"its ok, if you don't get timezone, assume my timezone so its ok."* This settles the owner-initiated case, which is every email turn. It does **not** close the separate M3 item about using the *requester's* zone when a colleague in another zone asks — there the two differ.

## The go-live gate — close these BEFORE `channels.email.enabled: true`

Neither blocks the wrap. Both block the switch.

1. **`meetings.ts`'s ~400-line prose still renders in full on the email leg.** Two of its nine tools survive the clamp, so skill-level gating cannot suppress the rest — it still teaches `move_meeting` (:1154, :1204, :1363), `delete_meeting` (:1282, :1298), `create_approval(policy_exception)` (:1091, :1105-1110) and *"Want me to flag it so Idan can ping them?"* (:1368), all clamp-blocked, on the leg forwarded verbatim to a client. Splitting that monolith into channel-aware paragraphs is next-run work. The new `EMAIL REPLY` frame is a partial prompt-only mitigant, not a fix.
2. **The social spine is not transport-gated and the email leg is now a consumer.** `inbound.ts` passes `interactive: true`, so `chooseSocialDirective` runs; on `kind:'other'` it can inject *"one plain human question from a fresh angle"* into the reply — in direct conflict with the frame. Quieter and worse: on `mode:'continue'` the turn calls `markSubjectRaised`, so an email turn **burns the owner's social cadence for a coda that is never delivered** (`inbound.ts` reads only `result.reply`).

## Actions, not rows

**Azure, unblocked, do whenever.** App `010dbc9c-` → API permissions → Microsoft Graph → **Delegated** (never Application) → `Mail.ReadWrite` + `Mail.Send` → optionally grant admin consent. Then Authentication → Add a platform → **Web** → `http://localhost:8734/callback`. No new secret, no new app registration. Then `node scripts/email-auth.mjs` — **and sign in AS `maelle@reflectiz.com`, not as the owner**: a delegated token belongs to whoever signs in, so signing in as Idan would point her at his own mailbox. Listener times out after 5 minutes.

**A restart is needed regardless of email** to pick up the `assistant.email` correction.

**Two changes no diff will show at wrap:** `config/users/idan.yaml` is gitignored (the `assistant.email` fix lives only there), and live DB row `p_SELF_U0F28CK6H` was repaired out of band — the verify confirmed it is the only row holding a `maelle%` address, so no collision was created.

## Parked by owner decision — do not re-raise

[#2](https://github.com/odahviing/AI-Executive-Assistant/issues/2) and [#5](https://github.com/odahviing/AI-Executive-Assistant/issues/5) are future bidirectional work; the one-address cap deliberately forecloses them. [#154](https://github.com/odahviing/AI-Executive-Assistant/issues/154) unruled — the wave answers a neighbouring version of its question 3 by keying a trust ceiling on `ChannelId`, which is a precedent to cite when it is ruled, not a closure. **Keep #24 open past the wrap** — row 121 is only partly closed. The 11 pre-existing backlog rows the owner declined to work this session stay open, not declined.

**Discoveries for the next run:** the orphaned `getSystemPromptSection` prose on two CORE_MODULES (`## OUTREACH` / `## TASKS` / `## APPROVALS` have never rendered) · the colleague leg still lists Slack in `channelsYouCanReach` though `message_colleague` never ships for a colleague · `default_routing.external` is read by no code · `offeredSlotsStash` has no profile partition · an unread mail cannot distinguish "failed to answer" from "answered, failed to mark" · a stray `kind='self'` test row `p_SELF_U12345TEST` in the production DB.

**Framework items — infrastructure chat, not a lane's:** `Workflow({name:…})` runs a session-cached copy of the engine, so a mid-session edit is silently ignored · the N6 arg guard is still missing from `bugger.js` · `understoodThreaded: 0` was a silent join failure on mismatched refs · `ledger-stats` push% reads 0% on a wave where the verify overturned 4 of 9 · two open ledger rows carry no `ref` so nothing can ever close them · a stale worktree at `.claude/worktrees/fervent-ramanujan-92de6e/`.
