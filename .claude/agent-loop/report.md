# Agent-loop report

**Empty — v4.4.5 wrapped. 0 rows await you.** Standing backlog: **24 open rows** across 24 bugs · 2 still-real · 22 need a re-read · 0 cite no file · **24 rulable · 20 waiting on a verb** · 20 are QUEUED for the next build and drain themselves (`node scripts/ledger-stats.cjs --open`).

**Built and uncommitted — this is what a wrap ships (0):** none. Both lane fixes (gh#170's person accessors, thread context on automated paths) ship inside this wrap.

**Charter work is on gh#181, not here** — the sixteen missing rules, the Editor's drafted charter awaiting your approval, the bouncer analysis, and the handyman draft. Six cold-read findings were read and deliberately deferred past this wrap: `G7`'s missing whitelist that `G8` still depends on · the `imageGuard` hand-off that dropped three true facts including the 2026-07-26 unlocalized-refusal ruling · `A13`(b)'s missing observable · a ledger row rewritten in place while `spend.cjs` gained a rename map that reads history without mutating it · a dated verbatim quote edited to a word he did not say · the framer's two required fields that no `F` rule names.
