# Report — cumulative since the 4.8.4 wrap

```
Since 4.8.4 — 4 dispatches: 3 owner-triggered builds, 1 hand-run nightly pass replacing the disabled cron
out: 11 built · 0 confirmed-other-lane · 1 needs-owner-decision · 1 bounced · 5/5 joint-traced · 9 discoveries queued
board: 7 open rows — 0 still-real · 7 need a re-read · 0 cite no file · 7 rulable · 6 queued and self-draining   (node scripts/ledger-stats.cjs --open)
```
**1 row awaits you** — a documentation-convention question, not a defect; everything else on the board drains itself into the next build.

**Built and uncommitted — this is what a wrap ships (11):** `move-meeting-counts-the-moving-event-against-its-own-per-day-cap` a same-day move no longer counts the moving meeting against its own cap · `category-quota-cap-and-count-leak-to-colleagues` a colleague hears the saturation, never the cap or the count · `bare-verb-binds-to-a-resolved-approval-after-code-already-identified-the-open-one` a one-word decision binds to the approval the code identified · `automove-ignores-over-optional-and-lands-on-a-we-event` auto-move prefers a clean slot over a Working-Elsewhere one · `internal-colleague-travelling-to-owners-city-still-forced-online` the internal half of the guest fix shipped in 4.8.4 · `hedge-suppression-is-trip-scoped-not-meeting-date-scoped` the timezone hedge is scoped to the date booked, not to today · `reverted-auto-reading-never-retires-temp-row` a temp timezone reading retires when the divergence ends · `timezone-ask-retries-delivery-against-a-closed-row` the persistence ask no longer revives a closed row · `log-verifyeventdeleted-malformed-id-blocks-cleanup` a malformed event id is classified as confirmed-gone so the artifact sweep can finish · `log-verifyeventdeleted-malformed-id-blocks-cleanup>dep` the producer that persisted that id unvalidated · `whatsapp-timestamp-unit-mismatch-ms-vs-seconds` one shared field, one unit

**pending owner (1)**

| Lane · ref | What happened | Your options | Risk |
|---|---|---|---|
| cleaner · `line-number-citations-in-prose-are-a-recurring-tax` | The same four prose citations to `db/people.ts` went stale twice in eighteen hours — repaired at the 4.8.4 wrap, stale again by 4.8.5 — because that file grew ~200 lines in a day and will keep growing | **Recommend: build.** The cleaner's proposal: cite an exported symbol by NAME with no line number (a reader greps it in one step, same cost as trusting a stale digit and being wrong, and it can never go stale); keep the line range only where the target is an anonymous comment block with no identifier to search for. Needs your word because it changes a convention across the docs, not because it is risky | None — documentation only. Declining means paying this sweep every wrap |

9 queued for the next build, detail in each ref's ledger row. The two worth knowing: `owner-own-trip-read-is-not-date-scoped-books-online-after-trip-ends` (matchmaker, medium — booking for a date after your trip ends while you are still away lands it online instead of the office; this wave date-scoped every attendee travel read and missed your own) and `outreach-model-supplied-tz-written-as-a-slack-reading` (registrar, medium — an outreach can write a model-guessed zone tagged as a Slack reading, undercutting the provenance honesty 4.8.4 shipped). Then `boundhint-note-does-not-strip-quotes-from-colleague-text` · `timezone-ask-revival-corner-nondm-resolution` · `categoryrules-owner-worded-colleague-explanation-is-dead` · `people-ts-scope-header-cites-an-unreachable-path`.

Golden battery 30/30 with 0 fails, run over this exact tree; 3 stale anchors re-pinned and verified by hand. One Fable pass over the full accumulated diff: code PASSED with zero overturns, all 5 joint seams compose. It DID overturn the bookkeeping — the nightly run's return had never been folded into `state.json`, so `pendingOverflow` still listed 5 fixes that run had already built; a later build would have re-dispatched them. Reconciled before the commit. `Maelle-Manager-Nightly` is still disabled and must be re-enabled at this wrap — see ref `OPEN-ACTION-reenable-nightly-manager-cron`.
