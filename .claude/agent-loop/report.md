# Report — cumulative since the 4.7.1 wrap

```
board: net 0 → 2 open rows — 0 still-real · 1 need a re-read · 1 cite no file · 2 rulable · 0 waiting on a verb   (node scripts/ledger-stats.cjs --open)
0 rows await you: both open rows are QUEUED and drain themselves on the next build
```

**Pending owner (0)** — empty, nothing on your desk right now.

**Built and uncommitted (0):** nothing — this follow-up ships everything that was outstanding: a real news-brief fabrication fix (Maelle could claim a source mentioned the owner's own company when it never did — now a deterministic per-tenant entity check runs before a source can be framed that way), the venue-search timezone-map item from the last cleaner sweep was traced and ruled NOT a bug (a map miss is a harmless no-op, not breakage), and this session's own framework cleanup (cleaner's `audit` verdict renamed to `needs-judgment` to stop colliding with the ledger's own unrelated `audit` meaning, plus 6 stale architect-ledger rows closed).
