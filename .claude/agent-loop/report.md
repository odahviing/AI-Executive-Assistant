# Agent-loop report

**Empty — v4.5.0 wrapped. 0 rows await you.** Standing backlog: **25 open rows** across 25 bugs · 1 still-real · 22 need a re-read · 2 cite no file · **25 rulable · 0 waiting on a verb** · 7 are QUEUED for the next build and drain themselves (`node scripts/ledger-stats.cjs --open`). The wave that shipped here is behind the wrap stamp; its own figures are in the CHANGELOG and the ledger.

**Built and uncommitted — this is what a wrap ships (30):** `o#214` person .md no longer renders into a room · `o#216` three raw-subject handlers masked, Graph projection widened · `o#217` the attendee unmask stops firing for the room-clamped owner · `o#218` the sixth viewer predicate deleted · `o#219` colleague-triggered research runs lose owner privilege · `o#220` a two-sender batch no longer runs as the owner · `o#221` move_meeting stops refusing him in a room · `o#222` the rule-bend disclosure reaches the zero-slot path · `o#223` the escalation is raised in code, not by the model · `o#224` the honesty check no longer rewrites a truthful grant away · `o#225` she stops speaking in the room while an approval waits · `o#226` a channel recognises him as the owner · `o#227` 47 paying threads down to 11, and the 25-char claim reaches the checker · `o#228` social provenance stamped by leg, with a backfill · `o#229` the rank gate rebuilt against what the log actually holds · `o#230` the calendar widening reverted whole · `update-meeting-write-door` a colleague can no longer rename a meeting he cannot see · `delete-meeting-success-path-raw-subject` · `room-mask-looser-than-dm` · `social-directive-block-no-room-gate` · `marksubjectraised-fires-on-a-suppressed-room-turn` · `thread-people-block-prints-member-pii-in-a-room` · `thread-request-status-section-keyed-on-isowner` · `escalation-narrated-into-the-room` · `email-leg-lost-byte-identity` restored · `shippedtoolnames-computed-without-authority` · `owner-brief-masked-his-own-personal-events` · `profile-line-strip-reverted` · `room-bend-disclosure-contradicted-itself` · plus the o#215 close, ruled not-a-defect.

**What this release does NOT contain, stated because it was asked for and is not built:** the capability behind `o#230` — a colleague learning that a meeting exists, its time and the attendee names. It was built, proved a net privacy regression on four counts, and was reverted whole on the owner's ruling. It returns as its own item, and it needs one decision he has not yet given: whether naming an EXTERNAL attendee is covered, since his ruling was about an internal colleague whose name is ambient in the workspace.

**The verification, stated honestly:** five build rounds. Rounds 1–4 each had a full adversarial perimeter pass; the last found three overturns, all now fixed. Round 5 got a scoped trace at 4/4. The final two changes — both direct owner rulings — got the engine's combined verify and typecheck, not a separate trace. Every round's repairs hollowed out an earlier round's at least once, and only full-diff passes ever caught that class.

**Cost:** ~9.7M tokens against a normal night's 150–250k, recorded in the CHANGELOG with the shape that would have been cheaper.
