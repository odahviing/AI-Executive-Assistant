# Report — cumulative since the 4.8.4 wrap

```
Wrap 4.8.4 — in: 3 owner-triggered runs · 1 hand dispatch · 1 unattended nightly run
out: 30 built · 1 confirmed-other-lane · 0 needs-owner-decision · 7 bounced · 4/4 joint-traced · 7 discoveries queued
board: 5 open rows — 0 still-real · 5 need a re-read · 0 cite no file · 5 rulable · 5 queued and self-draining   (node scripts/ledger-stats.cjs --open)
```
**5 rows await you** — v4.8.4 wrapped, 0 new from this wrap; all 5 are queued discoveries that drain themselves into the next build.

**Built and uncommitted — this is what a wrap ships (30):** `stale-stored-tz-treated-as-live-remote-signal` a stored zone stops meaning physically remote · `stale-stored-tz-treated-as-live-remote-signal>dep` the same-rank auto clobber closed at one chokepoint · `o#265-stored-attendee-tz-drives-search-unsurfaced` availability states its timezone assumption · `o#265-stored-attendee-tz-drives-search-unsurfaced>dep` the store-side half of the same · `pre-existing-clobbered-tz-now-locked-wrong-forever` a persistent differing zone earns one owner-gated question · `pre-existing-clobbered-tz-now-locked-wrong-forever>dep` that ask raised on the requests spine · `capturepass-haiku-zone-still-clobbers-established-tz` the capture pass mirrors what landed · `capturepass-haiku-zone-still-clobbers-established-tz>dep` the hedge names slack-vs-chat correctly · `tz-assumption-note-skipped-when-owner-travelling` the hedge no longer goes silent when the owner travels · `attendee-travel-hedge-contradicts-the-clip-it-describes` the hedge no longer contradicts its own arithmetic · `unresolvable-trip-zone-gets-no-hedge-at-all` an unresolvable destination keeps its hedge · `tz-assumption-note-can-be-spoken-twice` deduped on person_id · `guest-travelling-to-owners-city-forced-online` a guest visiting the owner's city is not forced online · `o#264-booked-on-i-will-move-it>dep` I-will-do-it-myself no longer reads as book-it · `o#266-coda-fired-mid-task-on-open-question` the coda waits for an unanswered question · `coda-no-suppression-on-unanswered-prose-followup` the same, closed at its root · `thread-match-tier-no-deferred-action-check` an undecided cancel survives an unrelated move · `delete-approval-resolved-by-unrelated-positive-mutation` the sibling tier of the same · `log-movemeeting-refuse-not-owners-unhandled` move refuses cleanly on an unreadable event · `cancel-path-failed-organizer-lookup-becomes-fact` cancel no longer declines on unproven ownership · `log-visitor-meeting-mode-crosstz-default-wrong` a stated in-person visit outranks a stored zone · `vanished-sweep-request-close-invisible` a request-only cleanup is counted and logged · `brief-narration-rule-matches-unwritten-value` brief narration matches a value actually written · `residence-md-can-state-what-the-db-refused` the memory file records only what stored · `self-row-keeps-old-zone-reconcile-refires-every-boot>dep` the self row tracks the owner's configured zone · `log-selfcapture-note-inverts-correction-polarity` a correction is no longer recorded inverted · `internal-only-people-helper-exported-as-api` a private helper is no longer exposed · `approval-subkind-union-enforces-nothing` subkind validated where approvals are minted · `backfill-registrar-2026-08-31` · `backfill-instructor-2026-08-31` two lane-day coverage backfills

| Lane · ref | What happened | Your options | Risk |
|---|---|---|---|

5 queued for the next build, detail in each ref's ledger row: `reverted-auto-reading-never-retires-temp-row` (librarian, medium — a hedge can name a Slack reading for up to a week after Slack stopped reading it) · `timezone-ask-retries-delivery-against-a-closed-row` (registrar, low) · `internal-colleague-travelling-to-owners-city-still-forced-online` (matchmaker, low — the internal half of a guest-facing fix that shipped here) · `hedge-suppression-is-trip-scoped-not-meeting-date-scoped` (matchmaker, low) · `whatsapp-timestamp-unit-mismatch-ms-vs-seconds` (diplomat, low — dormant transport).

Two adversarial Fable passes over the full tree, both PASS with zero overturns; the second was re-run from scratch because the nightly cron landed ten more fixes after the first had already read the tree. Golden battery run twice, 30/30 both times, 0 fail, 16 stale anchors re-pinned. One owner ruling settled the release's shape: a stored timezone is always temporary unless he says otherwise, and he is the only door to promotion.
